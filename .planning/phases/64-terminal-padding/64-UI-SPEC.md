---
phase: 64
slug: terminal-padding
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-10
---

# Phase 64 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none |
| Icon library | none |
| Font | "Cascadia Code", "MesloLGS NF", "Fira Code", monospace (source: style.css line 41) |

No shadcn, no Tailwind. Custom CSS in `frontend/src/style.css`. Design tokens are inline hex values and px values consistent with the existing codebase pattern.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing — matches `.sidebar__item padding: 8px`, `.tab-status-bar padding: 0 8px` |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none. All values are multiples of 4.

**Terminal padding:** `8px` on all sides (symmetric). Uses the `sm` token (8px), matching `.sidebar__item` and `.tab-status-bar` horizontal rhythm. The CSS rule is `.xterm { padding: 8px; }`.

---

## Typography

This phase introduces no new typography. The terminal renders monospace text via xterm.js using its internal font metrics. The surrounding UI uses the following existing scale:

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body / sidebar label | 13px | 400 (regular) | 1.0 (single-line UI labels) |
| Tab label | 13px | 400 (regular) | 1.0 |
| Status bar text | 12px | 400 (regular) | 1.0 |
| Status bar state badge | 11px | 400 (regular) | 1.0 with letter-spacing 0.05em |

Source: style.css grep — font-size 11px, 12px, 13px, 14px observed; 14px is used only on `.tab__close` icon. No heading-scale typography in this phase.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #1a1b26 | App background, terminal container background, active tab background |
| Secondary (30%) | #16161e | Tab bar, sidebar, status bar, inactive backgrounds |
| Accent (10%) | #7aa2f7 | Active tab bottom border only |
| Destructive | #f7768e | Close button hover only — no destructive actions in this phase |

Accent reserved for: **active tab bottom border** (`border-bottom: 2px solid #7aa2f7` on `.tab--active`). This phase adds no new accent usage.

Supporting palette (not accent — informational/chrome):
- `#9aa5ce` — sidebar icon default color
- `#c0caf5` — primary text / active state text
- `#565f89` — muted text, inactive tab text, status bar default text
- `#292e42` — border/divider color
- `#1e2030` — hover surface color
- `#9ece6a` — "on" state indicator (green)
- `#3b4261` — selection/hover on interactive chrome elements

Source: style.css lines 39–295 (codebase scan).

---

## Phase-Specific Visual Contract

This is a pure CSS polish phase. The entire visual change is:

**Target element:** `.xterm` (the element created by `term.open()` inside `containerRef`)
**CSS rule to add:**
```css
/* Inset terminal text from the container edges (PAD-01). */
.xterm {
  padding: 8px;
}
```

**Location:** `frontend/src/style.css` — xterm overrides block (after line 11, before the Reset section)

**Visual outcome:**
- Top inset: 8px gap between terminal text and top edge of `.terminal-container`
- Bottom inset: 8px gap between terminal text and top of status bar
- Left inset: 8px gap between terminal text and left edge
- Right inset: 8px gap between terminal text and right edge
- All four edges consistent across every open terminal session (single selector covers all xterm instances)

**No other visual changes in this phase.** No new components, no layout changes, no color changes.

---

## Copywriting Contract

This phase has no user-facing copy. It is a visual layout adjustment only.

| Element | Copy |
|---------|------|
| Primary CTA | not applicable — no CTA in this phase |
| Empty state heading | not applicable — terminal is always present when tab is active |
| Empty state body | not applicable |
| Error state | not applicable — CSS rule has no failure mode visible to users |
| Destructive confirmation | not applicable — no destructive actions |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not required — shadcn not initialized |
| third-party | none | not applicable |

No component registry used. No new npm dependencies added. Source: RESEARCH.md — "No new dependencies are required for this phase."

---

## Interaction Contract

No new interactions introduced. Existing terminal interactions (typing, scrolling, resizing) are unchanged. The only behavioral consequence is that `fitTerminal()` will compute 2 fewer columns (16px removed from horizontal space for `padH = 8 + 8`) and 2 fewer rows (16px removed from vertical space for `padV = 8 + 8`) compared to the pre-padding state. This is correct and expected — the terminal fits within the padded area.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
