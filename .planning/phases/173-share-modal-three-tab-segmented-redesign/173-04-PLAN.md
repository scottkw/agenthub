---
phase: 173-share-modal-three-tab-segmented-redesign
plan: 04
type: execute
wave: 2
depends_on: ["173-01", "173-02"]
files_modified:
  - frontend/src/components/SessionShare/ShareLinkCard.tsx
  - frontend/src/components/__tests__/ShareLinkCard.test.tsx
autonomous: true
requirements: [SM-06]
must_haves:
  truths:
    - "ShareLinkCard renders: title · truncated URL (full URL in title= attribute) · Copy/Open/QR actions · join code (via shared CodeDisplay) · scope description directly beneath (SM-06)"
    - "The same component renders correctly for all usages (tailnet Read-Only, tailnet Full Access, Internet public URL)"
    - "QR encodes the join-code exchange URL (via joinURLFor), NOT the raw capability token"
  artifacts:
    - "frontend/src/components/SessionShare/ShareLinkCard.tsx — new reusable link row"
    - "frontend/src/components/__tests__/ShareLinkCard.test.tsx — structure + QR-target tests"
  key_links:
    - "ShareLinkCard wraps the shared CodeDisplay (plan 02) for the join-code pill and uses the .share-linkcard* classes (plan 01)"
    - "Copy uses the existing ClipboardSetText/handleCopy pattern; Open uses BrowserOpenURL; QR uses GetCapabilityQRCode — no new plumbing"
  prohibitions:
    - "MUST NOT re-derive QR target URL logic — reuse joinURLFor(capURL, code) verbatim (Information Disclosure threat T-173-02)"
    - "MUST NOT introduce a JS truncate() helper — use CSS ellipsis (.share-linkcard__url) with full URL in title= (D-06)"
    - "MUST NOT build a client-side QR library — reuse the existing GetCapabilityQRCode Go binding"
---

<objective>
Extract the one reusable `ShareLinkCard` (title · truncated URL · Copy/Open/QR · join code · scope description) that replaces the four ad-hoc hand-laid link rows. Each card owns its own Copy/Open/QR state (per-card QR-error state — a small, desirable encapsulation improvement over today's single shared `qrError` slot; see RESEARCH Assumption A1).

Purpose: SM-06 — consistent link presentation with the scope description attached directly beneath the link it describes (fixes the "orphaned scope paragraphs" defect).
Output: `ShareLinkCard.tsx` + test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-DESIGN-129.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-RESEARCH.md
@frontend/src/components/SessionSharePanel.tsx

# Reuse verbatim from SessionSharePanel.tsx:
#   joinURLFor(capURL, code) ~:387-393 (QR target = /join?code=..., NOT the token)
#   handleCopy(url, setter) ~:353 (wraps ClipboardSetText)
#   GetCapabilityQRCode binding (server-generated base64 PNG) — existing QR pattern
#   CodeDisplay — now imported from ./SessionShare/shared (plan 02)
# Classes .share-linkcard* ship in plan 01.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ShareLinkCard.tsx (reusable link row, per-card Copy/Open/QR)</name>
  <read_first>
    - frontend/src/components/SessionSharePanel.tsx :438-525 (Read-Only + Full Access rows) and :531-601 (Internet public row) — the three shapes this card unifies
    - frontend/src/components/SessionSharePanel.tsx :353 (handleCopy), :387-393 (joinURLFor), QR fetch call site
    - frontend/src/components/SessionShare/shared.tsx (CodeDisplay export from plan 02)
    - frontend/src/style.css `.share-linkcard*` (plan 01)
  </read_first>
  <files>frontend/src/components/SessionShare/ShareLinkCard.tsx</files>
  <behavior>
    - Renders `.share-linkcard` with `.share-linkcard__top` containing `.share-linkcard__title` (title), `.share-linkcard__url` (URL text, `title={url}` full URL, CSS-truncated), and `.share-linkcard__actions` (Copy, Open, QR buttons).
    - Renders `.share-linkcard__join` wrapping the shared CodeDisplay for the join code.
    - Renders `.share-linkcard__desc` (scope description) directly beneath.
    - Copy button copies the URL (existing handleCopy pattern); Open button calls BrowserOpenURL(url); QR button toggles a QR image fetched via GetCapabilityQRCode targeting joinURLFor(url, code).
    - Each card instance keeps independent copied/QR-open/QR-b64/QR-error state.
  </behavior>
  <action>
    Create `frontend/src/components/SessionShare/ShareLinkCard.tsx` per D-06/SM-06. Props: `title`, `url`, `code`, `description`, plus whatever id/session context `GetCapabilityQRCode` needs (mirror the existing call signature). Import `CodeDisplay` from `./shared`. Reuse `joinURLFor` and the `handleCopy`/`BrowserOpenURL`/`GetCapabilityQRCode` patterns verbatim (copy them into this module or a small shared helper — do NOT re-invent the QR target URL). Use the `.share-linkcard*` classes; migrate the old inline `style={{...color:'#9aa5ce'}}` scope-paragraph styling into `.share-linkcard__desc` (no inline hex). Per-card QR-error state is intentional (RESEARCH A1).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit 2>&1 | grep -i "ShareLinkCard" && echo "TSC ERRORS" && exit 1 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Compiles clean; exports `ShareLinkCard`.
    - Uses shared `CodeDisplay` and `.share-linkcard*` classes; no inline hex.
    - QR target derived via `joinURLFor`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ShareLinkCard.test.tsx — structure + QR-target + desc-beneath contract</name>
  <read_first>
    - frontend/src/components/__tests__/SessionSharePanel.test.tsx (raw react-dom render + Wails binding mock pattern for GetCapabilityQRCode/ClipboardSetText/BrowserOpenURL)
    - frontend/src/components/SessionShare/ShareLinkCard.tsx (Task 1)
  </read_first>
  <files>frontend/src/components/__tests__/ShareLinkCard.test.tsx</files>
  <behavior>
    - Renders title text, a `.share-linkcard__url` with title= equal to the full URL, and Copy/Open/QR action buttons.
    - Renders the join code (CodeDisplay) and the scope description text inside `.share-linkcard__desc`, positioned after the link.
    - Clicking QR calls GetCapabilityQRCode with a target containing `/join?code=` (join-URL, not the raw token).
    - Clicking Open calls BrowserOpenURL with the URL.
  </behavior>
  <action>
    Write `ShareLinkCard.test.tsx` (raw react-dom/client + flushSync, mock the Wails bindings). Assert structure via classes/attributes/text (title= full URL, desc beneath), and assert the QR fetch target string contains `/join?code=` to lock the Information-Disclosure mitigation (T-173-02). No computed-color assertions.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/components/__tests__/ShareLinkCard.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - All behavior cases green; QR-target `/join?code=` assertion present.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
Info-Disclosure surface: the QR must encode the single-use join-exchange URL, not the capability token (a photographed token QR would be a leak). Mitigation: reuse `joinURLFor` verbatim + a test asserting the QR target contains `/join?code=` (T-173-02). Full STRIDE register in 173-05-PLAN.md.
</threat_model>

<verification>
`pnpm vitest run` for the new test green; `pnpm build` succeeds.
</verification>

<success_criteria>
One reusable link card renders the unified layout for every tier with the scope description attached beneath and QR encoding the join URL — proven by tests.
</success_criteria>

<output>
Create `.planning/phases/173-share-modal-three-tab-segmented-redesign/173-04-SUMMARY.md` when done.
</output>
