---
phase: 173-share-modal-three-tab-segmented-redesign
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Hub/ShareSegmentedControl.tsx
  - frontend/src/components/__tests__/ShareSegmentedControl.test.tsx
  - frontend/src/components/Hub/SessionShareModal.tsx
  - frontend/src/components/__tests__/SessionShareModal.test.tsx
  - TESTING.md
autonomous: true
gap_closure: true
requirements: [SM-07, SM-05]

must_haves:
  truths:
    - "SM-07 (keyboard): after ArrowRight/ArrowLeft on the segmented control, DOM focus moves to the newly-active tab button — document.activeElement is that button, so a subsequent Tab leaves the tablist from the correct segment"
    - "SM-07 (colorblind text label): the Internet toggle's text state label never reads 'On' during the pending/uncommitted risk-panel window — it reads a distinct pending label ('Confirm…') until SetSessionFunnel actually commits, then reads 'On'"
    - "SM-05 (residual WR-01): funnelOn resyncs from session.funnelActive when it changes out-of-band, so the Internet tabs and the state label reflect server truth after an external Funnel disable/expiry while the modal is open"
  artifacts:
    - "frontend/src/components/Hub/ShareSegmentedControl.tsx — btnRefs ref map + .focus() call inside moveSelection"
    - "frontend/src/components/Hub/SessionShareModal.tsx — pending-aware internet toggle-state label + funnelOn resync effect"
    - "frontend/src/components/__tests__/ShareSegmentedControl.test.tsx — document.activeElement focus-movement regression test"
    - "frontend/src/components/__tests__/SessionShareModal.test.tsx — pending-label regression test"
  key_links:
    - "ShareSegmentedControl moveSelection → btnRefs.current[next.id]?.focus() (arrow key moves real DOM focus)"
    - "SessionShareModal internet toggle-state span → label gated strictly on funnelOn for the 'On' text"
---

<objective>
Close the two source-confirmed SM-07 accessibility gaps that blocked Phase 173's clean pass (verifier score 6/8), plus the cheap same-file SM-05 residual (WR-01). This is surgical gap closure over already-shipped work — do NOT re-plan or re-touch the passing 173-01..07 output.

Gap #1 (WR-03, FAILED): `ShareSegmentedControl.moveSelection` changes the active tab via `onSelect` but never moves DOM focus, breaking the WAI-ARIA roving-tabindex contract the component's own docstring claims. After an arrow key + Tab, focus skips the whole tablist.

Gap #2 (WR-02, PARTIAL): the Internet toggle's text state label reads "On" the instant `riskPanelOpen` becomes true — during the pending, cancelable window before `SetSessionFunnel` commits — defeating the colorblind ground-truth signal the label exists to provide (owner is colorblind).

Secondary (WR-01, non-blocking, SM-05): `funnelOn` never resyncs from `session.funnelActive`, so an out-of-band Funnel disable/expiry leaves the Internet tabs incorrectly enabled and (compounding Gap #2) the label reads "On". Same file as Gap #2, cheap — included as a secondary task.

Purpose: satisfy the literal SM-07 acceptance text ("roving tabindex" and "On/Off/N-A text labels") the verifier found unmet, with regression tests that assert the ground-truth behavior (activeElement moves; label is not "On" while pending) — the existing tests only asserted `onSelect`, which is why the gap shipped.
Output: focus-following arrow-nav, a pending-aware toggle label, a server-truth resync effect, two new regression tests, and a light TESTING.md reconciliation.

Do NOT expand into WR-04..WR-07 or IN-01..IN-04 — the verifier explicitly ruled those pre-existing/out-of-scope for this phase (recommend a follow-up issue, not this plan).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-VERIFICATION.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-REVIEW.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-CONTEXT.md
</context>

<artifacts_this_phase_produces>
New/changed symbols and test names introduced by this plan:
- `btnRefs` — a `useRef<Record<string, HTMLButtonElement | null>>({})` per-tab button-ref map in `ShareSegmentedControl.tsx`.
- A distinct pending-state text label string `'Confirm…'` on the Internet toggle-state span (the exact glyph is Claude's discretion; it must be visibly distinct from `'On'` and `'Off'`).
- A `funnelOn` ↔ `session.funnelActive` resync `useEffect` in `SessionShareModal.tsx`.
- New test: focus-movement regression in `ShareSegmentedControl.test.tsx` asserting `document.activeElement` is the newly-active tab button after ArrowRight/ArrowLeft.
- New test: pending-label regression in `SessionShareModal.test.tsx` asserting the Internet toggle-state text is NOT `'On'` while the risk panel is open/uncommitted, and reads `'On'` only after the commit.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Roving-tabindex focus follow in ShareSegmentedControl (Gap #1 / WR-03 / SM-07)</name>
  <files>frontend/src/components/Hub/ShareSegmentedControl.tsx, frontend/src/components/__tests__/ShareSegmentedControl.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/ShareSegmentedControl.tsx — moveSelection (lines 57-64) and the tab button render (lines 88-101); note there is ZERO `.focus(` call in the file today.
    - frontend/src/components/__tests__/ShareSegmentedControl.test.tsx — the existing arrow-key tests (lines 126-168) only assert `onSelect` was called; the `renderControl` helper uses `createRoot` + `flushSync` and appends `container` to `document.body` (real DOM, so `.focus()` and `document.activeElement` work).
    - 173-VERIFICATION.md gap #1 (lines 9-18) and 173-REVIEW.md WR-03 (lines 60-74) — the suggested fix (btnRefs map + `btnRefs.current[next.id]?.focus()`).
  </read_first>
  <behavior>
    - After the active tab receives ArrowRight, document.activeElement is the button for the next enabled tab (per moveSelection's wrap/skip-disabled order).
    - After ArrowLeft, document.activeElement is the button for the previous enabled tab (wraps to last enabled tab from the first).
    - The pre-confirm single-enabled-tab case (only 'tailnet' enabled): ArrowRight wraps to 'tailnet' and focus lands back on the tailnet button (no focus escapes to a disabled button).
  </behavior>
  <action>
    Add a per-tab button ref map `btnRefs` (a `useRef<Record<string, HTMLButtonElement | null>>({})`) at the top of the `ShareSegmentedControl` component. Attach it on each rendered tab button with a ref callback keyed by `t.id` (assign the element into `btnRefs.current[t.id]`). Inside `moveSelection`, immediately after the existing `onSelect(next.id)` call, call `btnRefs.current[next.id]?.focus()` so DOM focus follows selection per the WAI-ARIA APG tablist pattern the docstring (lines 6-8) already claims. Do NOT change the click handler, the tabIndex roving logic, the aria attributes, or the danger-glyph/`N/A` sub-label logic — this is a focus-only addition. Keep the fix keyed on `next.id` (not `active`, which is a stale prop inside the same render).

    In the test file, add a new `describe`/`it` (in or beside the existing "arrow-key navigation (SM-07)" block) that renders TABS_POST_CONFIRM active='tailnet', focuses the active tab button first, dispatches an ArrowRight keydown, flushes, and asserts `document.activeElement` is the 'internet-ro' tab button (match by textContent containing 'Read-only'). Add a companion assertion for ArrowLeft landing focus on the 'internet-fa' (Full access) button (wrap-to-last). Assert on the button element identity / textContent, not on `onSelect` alone — asserting `onSelect` is exactly what let the gap ship. The `renderControl` helper already mounts into `document.body`, so `document.activeElement` is meaningful; use the same `flushSync` + `dispatchEvent(new KeyboardEvent(...))` pattern as the existing arrow tests.
  </action>
  <verify>
    <automated>grep -n '\.focus(' frontend/src/components/Hub/ShareSegmentedControl.tsx && grep -n 'btnRefs' frontend/src/components/Hub/ShareSegmentedControl.tsx && cd frontend && pnpm exec vitest run src/components/__tests__/ShareSegmentedControl.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '\.focus(' frontend/src/components/Hub/ShareSegmentedControl.tsx` returns at least one match (previously zero — this is the WR-03 fix).
    - `grep -n 'btnRefs' ShareSegmentedControl.tsx` returns the ref map declaration + at least one usage.
    - `ShareSegmentedControl.test.tsx` exits 0 under `pnpm exec vitest run` and includes an assertion referencing `document.activeElement`.
  </acceptance_criteria>
  <done>ArrowRight/ArrowLeft move real DOM focus to the newly-active tab button; a regression test asserts document.activeElement and passes; no change to click, aria, roving-tabindex, or danger-glyph behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pending-aware toggle label + funnelOn server-truth resync in SessionShareModal (Gap #2 / WR-02 + WR-01 / SM-07, SM-05)</name>
  <files>frontend/src/components/Hub/SessionShareModal.tsx, frontend/src/components/__tests__/SessionShareModal.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/SessionShareModal.tsx — `toggleStateLabel` helper (lines 28-31), the Internet toggle-state span call at line 704 (`toggleStateLabel(funnelOn || riskPanelOpen, funnelDisabled)`), the checkbox/aria-checked at lines 693/695, the `funnelOn` state seed (line 336) with its only three `setFunnelOn` call sites, and the existing tab-transition effect (lines 359-367) that already resets `tab` to 'tailnet' when funnelOn flips false.
    - frontend/src/components/__tests__/SessionShareModal.test.tsx — the risk-panel tests (lines 528-560): `renderModal`, `findFunnelToggle`, `findByText`, `tick`, `enableFunnel`, `mockedSetSessionFunnel`, and the fact that flipping the toggle ON renders `.hub-funnel-risk-panel--open` and hides `.share-segbar`. The toggle-state text lives in the `.settings-panel__toggle-state` span.
    - 173-VERIFICATION.md gap #2 (lines 19-27) + WR-01 note (line 114); 173-REVIEW.md WR-02 (lines 54-58) and WR-01 (lines 40-52).
  </read_first>
  <behavior>
    - When the Internet toggle is flipped ON and the risk panel is open but SetSessionFunnel has NOT been called, the `.settings-panel__toggle-state` text is the pending label ('Confirm…'), NOT 'On'.
    - After the "Enable internet share" CTA commits (funnelOn true), the state text reads 'On'.
    - When funnelDisabled, the state text reads 'N/A'.
    - WR-01: if `session.funnelActive` changes to false out-of-band (new prop) while funnelOn is true, funnelOn resyncs to false (and the existing transition effect resets tab to 'tailnet').
  </behavior>
  <action>
    Gap #2 (WR-02): change ONLY the Internet toggle-state text so 'On' is gated strictly on `funnelOn`. The pending window (`riskPanelOpen && !funnelOn`) must render a distinct pending label — use `'Confirm…'` (glyph is discretion, but it must not be 'On' or 'Off'). Preserve the existing 'N/A' when `funnelDisabled` and 'Off' when neither. Implement by giving this call site a three-state expression (e.g. `funnelDisabled ? 'N/A' : funnelOn ? 'On' : riskPanelOpen ? 'Confirm…' : 'Off'`) or by extending `toggleStateLabel` with a pending argument — do not regress the other two toggles (Share / Browse) that also call `toggleStateLabel`. Leave the checkbox `checked` and `aria-checked` reflecting `funnelOn || riskPanelOpen` (the "about to enable" affordance is fine for the control's checked state, per WR-02's second option) — only the TEXT label changes. Per D-07/SM-07: the text label is the colorblind ground-truth signal and must reflect committed state, not pending intent.

    Secondary (WR-01 / SM-05): add a `useEffect` that resyncs `funnelOn` from `session.funnelActive` when they diverge — early-return if equal, else `setFunnelOn(session.funnelActive)`; dependency array keyed on `session.funnelActive`. This corrects the stale comment at lines 331-332 (which claims a sync effect exists — it does not). The existing prevFunnelOnRef transition effect (lines 359-367) already resets `tab` to 'tailnet' when funnelOn flips false, so the resync alone restores server-truth tab-availability; do not duplicate that tab reset. Update the misleading comment to describe the real resync effect.

    Tests: add a pending-label regression `it` in the risk-panel describe block. Reuse the existing setup — `renderModal({ webEnabled: true, webServerMode: 'tailscale' })`, await the seeding `tick()`, flip `findFunnelToggle(c)` ON — then assert (a) `.hub-funnel-risk-panel--open` is present (pending window) AND `.settings-panel__toggle-state` textContent is NOT 'On' (assert it equals the pending label 'Confirm…'), and `mockedSetSessionFunnel` has NOT been called. Then drive the CTA commit via the existing `enableFunnel`/`findByText(c,'Enable internet share')` + React.act pattern and assert the state text now reads 'On'. Keep every assertion attribute/text based (owner is colorblind — never computed color).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/__tests__/SessionShareModal.test.tsx --reporter=dot && grep -n 'session.funnelActive === funnelOn\|setFunnelOn(session.funnelActive)' src/components/Hub/SessionShareModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `SessionShareModal.test.tsx` exits 0 under `pnpm exec vitest run` and contains a new assertion that the toggle-state text is not 'On' while `.hub-funnel-risk-panel--open` is present.
    - `grep -n 'setFunnelOn(session.funnelActive)' SessionShareModal.tsx` returns a match (the WR-01 resync effect).
    - The Internet toggle-state 'On' text is reachable only when `funnelOn` is true (verified by the passing pending-label test).
  </acceptance_criteria>
  <done>The Internet toggle text label reads a distinct pending value ('Confirm…') during the uncommitted risk-panel window and only 'On' once funnel is committed; funnelOn resyncs from session.funnelActive on out-of-band change; both proven by new regression tests.</done>
</task>

<task type="auto">
  <name>Task 3: Full-gate reverify + TESTING.md reconciliation</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md Section 4 Traceability rows for SM-07 (lines 401-403) — both `ShareSegmentedControl.test.tsx` and `SessionShareModal.test.tsx` already have SM-07 rows; NO new test FILES are added (only new cases in existing files), so the Suite Manifest count (147) and file list do NOT change.
    - CLAUDE.md standing rule (Section 6 of TESTING.md): update Section 4 descriptions when test coverage changes; run `bash tests/check-traceability-paths.sh` clean.
  </read_first>
  <action>
    No new test files were created (Tasks 1-2 append cases to existing files), so do NOT touch the Suite Manifest count or file list. Lightly update the two existing SM-07 Traceability row descriptions (lines 402-403) to mention the new coverage: ShareSegmentedControl row — add "arrow-key nav moves document.activeElement to the newly-active tab (roving-tabindex focus-follow, gap-closure 173-08)"; SessionShareModal row — add "Internet toggle text label reads a distinct pending value (not 'On') during the uncommitted risk-panel window (173-08)". Keep the repo-relative `.tsx` path column unchanged. Then run the reverify gate below. If any gate fails, fix the offending file before completing (do not edit TESTING.md to mask a failure).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit && pnpm exec vite build && pnpm exec vitest run src/components/__tests__/ShareSegmentedControl.test.tsx src/components/__tests__/SessionShareModal.test.tsx src/components/__tests__/HoldToConfirmButton.test.tsx src/components/__tests__/ShareLinkCard.test.tsx src/components/__tests__/TailnetTab.test.tsx src/components/__tests__/InternetReadOnlyTab.test.tsx src/components/__tests__/InternetFullAccessTab.test.tsx --reporter=dot && cd .. && bash tests/check-traceability-paths.sh</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` exits 0 (no output).
    - `pnpm exec vite build` succeeds.
    - All 7 phase-173 vitest files (the original 8 minus the deleted SessionSharePanel; ShareSegmentedControl + SessionShareModal now carrying the new cases) run green.
    - `bash tests/check-traceability-paths.sh` prints "OK: all traceability paths exist" and exits 0.
  </acceptance_criteria>
  <done>Full reverify gate green (tsc, vite build, targeted vitest, traceability); TESTING.md SM-07 rows note the new gap-closure coverage without changing the file count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (session.funnelActive) → client display state | Funnel exposure truth crosses from the daemon into the modal; the display must reflect it faithfully |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-173-08-01 | Spoofing (misleading affordance) | SessionShareModal internet toggle-state label | low | mitigate | Gate the 'On' text strictly on committed `funnelOn`; pending window shows 'Confirm…' so the user never believes sharing is live while it is only pending (WR-02). |
| T-173-08-02 | Tampering (stale display) | SessionShareModal funnelOn vs session.funnelActive | low | mitigate | Resync `funnelOn` from server truth so out-of-band Funnel disable/expiry disables the Internet tabs and clears the 'On' label (WR-01). The write-mint path already reads `session.funnelActive` directly and is unaffected — no exploitable gap; display-only correction. |
| T-173-08-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan — purely local source edits + tests; legitimacy gate not applicable. |

No new trust boundaries are introduced. The security-critical write-capability mint control (`HoldToConfirmButton` in `InternetFullAccessTab`) is out of scope and already gated on `session.funnelActive` server truth (verified in 173-REVIEW.md Summary).
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` clean and `pnpm exec vite build` succeeds.
- The 7 phase-173 vitest files all green, including the two new regression tests (focus-movement + pending-label).
- `grep -n '\.focus(' ShareSegmentedControl.tsx` returns a match; `grep -n 'setFunnelOn(session.funnelActive)' SessionShareModal.tsx` returns a match.
- `bash tests/check-traceability-paths.sh` exits 0.
</verification>

<success_criteria>
- SM-07 gap #1 closed: arrow-key nav moves DOM focus to the newly-active tab, asserted via `document.activeElement`.
- SM-07 gap #2 closed: the Internet toggle text label never reads 'On' during the pending/uncommitted risk-panel window, asserted by a regression test.
- SM-05 residual (WR-01) closed: `funnelOn` resyncs from `session.funnelActive`.
- All reverify gates green; TESTING.md reconciled without a file-count change.
- No changes to 173-01..07 passing behavior; WR-04..WR-07 / IN-01..IN-04 left untouched.
</success_criteria>

<output>
Create `.planning/phases/173-share-modal-three-tab-segmented-redesign/173-08-SUMMARY.md` when done
</output>
