---
phase: 173-share-modal-three-tab-segmented-redesign
plan: 05
type: execute
wave: 3
depends_on: ["173-01", "173-02", "173-04"]
files_modified:
  - frontend/src/components/SessionShare/TailnetTab.tsx
  - frontend/src/components/SessionShare/InternetReadOnlyTab.tsx
  - frontend/src/components/SessionShare/InternetFullAccessTab.tsx
  - frontend/src/components/__tests__/TailnetTab.test.tsx
  - frontend/src/components/__tests__/InternetReadOnlyTab.test.tsx
  - frontend/src/components/__tests__/InternetFullAccessTab.test.tsx
autonomous: true
requirements: [SM-04, SM-06]
must_haves:
  truths:
    - "TailnetTab renders exactly two ShareLinkCards (Read-Only Link, Full Access Link), each with its scope description attached beneath (SM-06)"
    - "InternetReadOnlyTab renders the public URL (ShareLinkCard) + reusable public read code + a Disable-internet button, incl. warming/timed-out sub-states (SM-06)"
    - "InternetFullAccessTab hosts the entire public-write flow: danger explainer → Enable public write access → hold-to-confirm gate (expiry select + HoldToConfirmButton) → armed summary (write URL / single-use code / countdown / Disable public write) (SM-04)"
    - "The write-gate / hold-to-confirm markup is ABSENT from TailnetTab and InternetReadOnlyTab renders (SM-04 wall-off, D-04, T-173-01)"
    - "Focus moves to the Disable-public-write button the instant the gate arms (focus-management effect preserved)"
  artifacts:
    - "frontend/src/components/SessionShare/{TailnetTab,InternetReadOnlyTab,InternetFullAccessTab}.tsx — three tab-body renderers extracted from SessionSharePanel.tsx"
    - "frontend/src/components/__tests__/{TailnetTab,InternetReadOnlyTab,InternetFullAccessTab}.test.tsx — incl. the SM-04 negative wall-off test"
  key_links:
    - "Tabs receive all RPC results as props from the shell (plan 06); they never call Wails bindings for state transitions — onDisableFunnel/onGateConfirm/onDisableGateWrite are passed down"
    - "InternetFullAccessTab keeps the hold button disabled until the connection is warm (funnelActive && !warmingUp), even though the tab itself is reachable once confirmed"
  prohibitions:
    - "MUST NOT change which capabilities/tokens are issued, TTL semantics, Funnel teardown logic, or the 3s hold-gate behavior — layout/IA only (D-08, out-of-scope)"
    - "MUST NOT render any public-write / command-execution UI outside InternetFullAccessTab (SM-04, T-173-01 Elevation-of-Privilege)"
    - "MUST NOT conflate the funnelActive prop (server poll truth) with the shell's funnelOn state — keep warming/live sub-state keyed off funnelActive/warmingUp/warmupTimedOut (RESEARCH Pitfall 3)"
---

<objective>
Refactor the single-column `SessionSharePanel.tsx` into three focused tab-body renderers — `TailnetTab`, `InternetReadOnlyTab`, `InternetFullAccessTab` — extracting the existing blocks verbatim (behavior unchanged) and re-homing the dangerous public-write / command-execution flow ENTIRELY inside `InternetFullAccessTab` (SM-04). `SessionSharePanel.tsx` itself is deleted by the shell plan (06) once the modal stops importing it; this plan only creates the three tab components + their tests.

Purpose: SM-04 (wall off command-execution) + SM-06 (ShareLinkCard usage). The highest-value new test here is the SM-04 negative assertion: the write-gate markup is absent from the two non-danger tabs.
Output: three tab components + three test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-DESIGN-129.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-RESEARCH.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-CONTEXT.md
@frontend/src/components/SessionSharePanel.tsx
@frontend/src/components/__tests__/SessionSharePanel.test.tsx

# Block boundaries (HEAD — re-verify): Read-Only row :438-480, Full Access row :482-525,
# Internet(public) section :531-601, PUBLIC WRITE gate hub-funnel-write-gate :603-724.
# Local state to move WITH its block: read/write copy+QR -> TailnetTab (or per-ShareLinkCard);
# funnelCopied/showFunnelQR/funnelQRb64/funnelQRError -> InternetReadOnlyTab;
# gateExpirySeconds/gateCopied/showGateQR/gateQRb64/gateQRError/gateNowSec/disableGateBtnRef/
# hadGateResultRef -> InternetFullAccessTab (incl. the countdown + focus-management effects).
# funnelEngaged = warmingUp||warmupTimedOut||funnelActive gates the Internet sub-states.
# Hold button disabled prop: !(funnelActive && !warmingUp) — preserve this nuance.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TailnetTab + InternetReadOnlyTab (extract link/public rows, use ShareLinkCard)</name>
  <read_first>
    - frontend/src/components/SessionSharePanel.tsx :438-525 (Read-Only + Full Access rows) and :531-601 (Internet public section incl. warmup/timed-out/disable)
    - frontend/src/components/SessionShare/ShareLinkCard.tsx (plan 04) and shared.tsx CodeDisplay (plan 02)
  </read_first>
  <files>frontend/src/components/SessionShare/TailnetTab.tsx, frontend/src/components/SessionShare/InternetReadOnlyTab.tsx</files>
  <behavior>
    - TailnetTab renders two ShareLinkCards: "Read-Only Link" (readURL/readCode + read scope desc) and "Full Access Link" (writeURL/writeCode + full scope desc). No public-write UI.
    - InternetReadOnlyTab renders the public URL as a ShareLinkCard (funnelUrl + publicReadCode) + the reusable public read code + a "Disable internet share" button (onDisableFunnel), and the "Starting up… (TLS warming up)" / timed-out sub-states keyed off warmingUp/warmupTimedOut/funnelActive. No public-write UI.
  </behavior>
  <action>
    Create `TailnetTab.tsx` and `InternetReadOnlyTab.tsx`, extracting the corresponding blocks from `SessionSharePanel.tsx` verbatim in behavior (D-08 unchanged), swapping the hand-laid link rows for `ShareLinkCard` instances (SM-06/D-06). Props flow down from the shell (readURL/writeURL/readCode/writeCode for Tailnet; funnelUrl/publicReadCode/warmingUp/warmupTimedOut/funnelActive/onDisableFunnel for Internet-RO). Keep the Internet warming/live sub-state logic keyed off `funnelActive`/`warmingUp`/`warmupTimedOut` exactly as today (RESEARCH Pitfall 3) — do not couple it to the shell's `funnelOn`. Move each block's local copy/QR state into the tab (or let ShareLinkCard own per-card state). No public-write markup in either file.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit 2>&1 | grep -iE "TailnetTab|InternetReadOnlyTab" && echo "TSC ERRORS" && exit 1 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both compile clean and export their components.
    - Tailnet uses two ShareLinkCards; Internet-RO uses a ShareLinkCard + Disable button + warmup sub-states.
    - Neither file contains `hub-funnel-write-gate` or `HoldToConfirmButton`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create InternetFullAccessTab (public-write flow walled off here, verbatim)</name>
  <read_first>
    - frontend/src/components/SessionSharePanel.tsx :603-724 (hub-funnel-write-gate: danger box, expiry select, HoldToConfirmButton, armed result, countdown, focus effect)
    - frontend/src/components/SessionShare/shared.tsx (HoldToConfirmButton export, plan 02)
  </read_first>
  <files>frontend/src/components/SessionShare/InternetFullAccessTab.tsx</files>
  <behavior>
    - Idle: renders the danger explainer (⚠ terminal-exposure copy) + "Enable public write access…" button (onGateConfirm path opens the gate).
    - Gate open: expiry `<select>` (fixed enum, no free-text) + HoldToConfirmButton (disabled until funnelActive && !warmingUp) + Cancel.
    - Armed: write URL + single-use write code + live countdown + "Disable public write" button; focus moves to that button the instant it arms.
    - No tailnet/read-only link UI appears here.
  </behavior>
  <action>
    Create `InternetFullAccessTab.tsx`, moving the `hub-funnel-write-gate` block (`SessionSharePanel.tsx:603-724`) verbatim, including the danger copy, the fixed-enum expiry select, `HoldToConfirmButton` (imported from `./shared`), the armed-summary result, the countdown tick effect, and the `disableGateBtnRef`/`hadGateResultRef` focus-management effect. Move the gate-local state (`gateExpirySeconds`, `gateCopied`, `showGateQR`, `gateQRb64`, `gateQRError`, `gateNowSec`) into this component. Props from shell: `onGateConfirm`, `writeGateUrl`, `writeGateCode`, `writeGateExpiresAt`, `writeGateUsed`, `onDisableGateWrite`, plus `funnelActive`/`warmingUp` for the hold-button disabled nuance `!(funnelActive && !warmingUp)`. Preserve the fixed-enum expiry input (V5 — never a free-text expiry). Do NOT change teardown/TTL/hold-gate semantics (out-of-scope).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit 2>&1 | grep -i "InternetFullAccessTab" && echo "TSC ERRORS" && exit 1 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Compiles clean; exports the component.
    - Contains the `hub-funnel-write-gate` markup + HoldToConfirmButton + focus effect + fixed-enum expiry select.
    - Countdown + focus-on-arm behavior preserved.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Per-tab tests incl. the SM-04 wall-off negative test</name>
  <read_first>
    - frontend/src/components/__tests__/SessionSharePanel.test.tsx (assertions to redistribute; raw react-dom render + binding mock pattern)
    - the three tab components from Tasks 1-2
  </read_first>
  <files>frontend/src/components/__tests__/TailnetTab.test.tsx, frontend/src/components/__tests__/InternetReadOnlyTab.test.tsx, frontend/src/components/__tests__/InternetFullAccessTab.test.tsx</files>
  <behavior>
    - TailnetTab: two ShareLinkCards render with correct titles + scope descriptions beneath; write-gate/hold markup ABSENT.
    - InternetReadOnlyTab: public URL card + reusable code + Disable button render; warming/timed-out sub-states render on the right props; write-gate/hold markup ABSENT.
    - InternetFullAccessTab: danger explainer present; gate open→arm flow present; armed summary shows write URL/code/countdown/Disable; hold button disabled until warm.
  </behavior>
  <action>
    Redistribute the relevant assertions from `SessionSharePanel.test.tsx` into three new per-tab test files (raw react-dom/client + flushSync; mock Wails bindings). The single highest-value new test (SM-04 / T-173-01): assert that the public-write markers (`hub-funnel-write-gate` class and the hold-to-confirm button) are ABSENT from `TailnetTab` and `InternetReadOnlyTab` rendered output, and PRESENT only in `InternetFullAccessTab`. All assertions attribute/text/class based (non-hue). Do not delete `SessionSharePanel.test.tsx` yet — the shell plan (06) deletes it together with `SessionSharePanel.tsx` to avoid a red suite mid-wave.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/components/__tests__/TailnetTab.test.tsx src/components/__tests__/InternetReadOnlyTab.test.tsx src/components/__tests__/InternetFullAccessTab.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - All three test files green.
    - SM-04 negative assertion present in both TailnetTab and InternetReadOnlyTab tests.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local owner UI → daemon RPC | Toggles/CTAs cross into `SetSessionFunnel` / `SetSessionFunnelWrite` (unchanged this phase) |
| shared session URL → public internet (Funnel) | A leaked full-access link = remote command execution on the owner's machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-173-01 | Elevation of Privilege | Public-write / command-execution UI leaking into TailnetTab or InternetReadOnlyTab via a refactor mistake | high | mitigate | Preservation: write-gate arming stays behind the hold-to-confirm gate AND renders ONLY in InternetFullAccessTab (SM-04). Enforced by an explicit negative test asserting `hub-funnel-write-gate` + hold button are ABSENT from the other two tabs. |
| T-173-02 | Information Disclosure | QR encoding the raw capability token instead of the join-exchange URL | high | mitigate | Reuse `joinURLFor(capURL, code)` verbatim (plan 04); test asserts QR target contains `/join?code=`. |
| T-173-03 | Tampering | Free-text expiry input replacing the fixed-enum select during "cleanup" | medium | mitigate | Preservation: keep the fixed-enum expiry `<select>` (V5); never introduce a free-text expiry. |
| T-173-04 | Elevation of Privilege | Capability/token/TTL/Funnel-teardown behavior changing under a layout refactor | high | accept (preserve) | Out-of-scope by D-08; no RPC/handler moves into tabs — all bindings stay in the shell. Regression net = existing modal/panel/daemon tests staying green. |

Preservation guarantees (must-NOT): (a) public-write arming still requires the hold-to-confirm gate; (b) no capability/token/TTL/Funnel-teardown behavior changes; (c) the dangerous write flow renders ONLY in the Full-access tab and nowhere else (SM-04).
</threat_model>

<verification>
`pnpm vitest run` for the three new tab tests green; `SessionSharePanel.test.tsx` still green (not yet deleted); `pnpm build` succeeds; SM-04 negative test present.
</verification>

<success_criteria>
The single-column panel is decomposed into three tab bodies with the command-execution flow provably walled off inside the Full-access tab, ShareLinkCard used for every link row, and all preserved behaviors (teardown/TTL/hold-gate/focus/warmup) intact.
</success_criteria>

<output>
Create `.planning/phases/173-share-modal-three-tab-segmented-redesign/173-05-SUMMARY.md` when done.
</output>
