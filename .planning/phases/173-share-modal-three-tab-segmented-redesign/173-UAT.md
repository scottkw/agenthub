---
status: complete
phase: 173-share-modal-three-tab-segmented-redesign
source: [173-VERIFICATION.md]
started: 2026-07-08T14:27:51Z
updated: 2026-07-08T16:05:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Whole-dialog scroll confinement (SM-02)
expected: Control strip stays pinned; only the tab-body region swaps/scrolls; no whole-dialog scrollbar at normal viewport height; at a short viewport with the Full-access armed-result (tallest) state, only `.hub-share-modal__tabpanel` scrolls and the card stays within `calc(100vh - 64px)`.
result: pass
source: automated (dev-browser live harness — uat-173-harness, real modal-frame DOM + real ShareSegmentedControl + real InternetFullAccessTab armed state, real style.css)
evidence: |
  At a 1000×600 viewport: modal height = 536px = maxHeightCap (100vh − 64); computed max-height = 536px.
  `.hub-share-modal__body` does NOT scroll (scrollHeight 486 == clientHeight 486, but body kept as outer bound overflow-y:auto).
  Only `.hub-share-modal__tabpanel` scrolls (scrollHeight 298 > clientHeight 212).
  After scrolling the tabpanel to bottom (scrollTop 86), the segbar top (278) and first toggle-row top (97) are unchanged → control strip stays pinned.

### 2. Full-access tab colorblind-safe ring (SM-07)
expected: In a live build, the Full-access tab's active-state ring is a `box-shadow` inset (not a background hue swap) and is visually distinguishable to a colorblind viewer, alongside the ⚠ glyph. (Source-confirmed: style.css:6629-6631 `box-shadow: inset 0 0 0 1px var(--hub-destructive)`; ShareSegmentedControl.tsx:74 prefixes the danger sub-label with `⚠ `.)
result: pass
source: automated (dev-browser live harness — computed-style read, verified at source not by eye per colorblind-owner memory)
evidence: |
  Active `.share-seg.is-active.is-danger` computed box-shadow = `rgb(247, 118, 142) 0px 0px 0px 1px inset`
  (= --hub-destructive #f7768e), sub-label text = "⚠ Full access", sub color = rgb(247,118,142).
  Its background = rgba(122,162,247,0.12) — the SAME neutral blue active tint as the other active tabs,
  NOT a red hue swap. Non-active tabs have box-shadow: none. The danger distinction is carried entirely by
  the inset ring + ⚠ glyph + red sub-text (all non-hue-dependent cues).

### 3. prefers-reduced-motion hold-to-confirm fallback (SM-07)
expected: With prefers-reduced-motion enabled at the OS level, opening the Full-access tab's gate degrades the hold-to-confirm control to a single plain confirm click (no timed fill bar).
result: pass
source: automated (dev-browser live harness — matchMedia(reduce) forced + page.emulateMedia reducedMotion:reduce)
evidence: |
  Normal-motion branch (no-preference): hold button label = "Hold 3s to confirm", `.hub-funnel-write-gate__hold-fill` present, reduced-variant class absent.
  Reduced-motion branch: hold button label = "Confirm", NO `.hub-funnel-write-gate__hold-fill`, `.hub-funnel-write-gate__hold-btn--reduced-motion` present, button enabled (single click, no timed hold). Screenshot confirms plain "Confirm" button in place of the fill bar.

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — all human-verification items passed via live dev-browser harness]
