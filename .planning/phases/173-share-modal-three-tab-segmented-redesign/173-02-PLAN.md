---
phase: 173-share-modal-three-tab-segmented-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/SessionShare/shared.tsx
  - frontend/src/components/SessionSharePanel.tsx
  - frontend/src/components/__tests__/HoldToConfirmButton.test.tsx
autonomous: true
requirements: [SM-07, SM-08]
must_haves:
  truths:
    - "CodeDisplay and HoldToConfirmButton are exported from a shared module so TailnetTab / InternetReadOnlyTab / InternetFullAccessTab / ShareLinkCard can import them (D-09)"
    - "HoldToConfirmButton renders a plain single-click confirm (no timed fill) when prefers-reduced-motion: reduce is active, and the timed 3s hold otherwise (SM-07/D-07)"
    - "The existing SessionSharePanel.test.tsx still passes after the move (regression-safe scaffolding)"
  artifacts:
    - "frontend/src/components/SessionShare/shared.tsx — exported CodeDisplay + HoldToConfirmButton (moved verbatim; hold gets an additive reduced-motion branch)"
    - "frontend/src/components/__tests__/HoldToConfirmButton.test.tsx — reduced-motion fallback + normal-hold assertions"
  key_links:
    - "SessionSharePanel.tsx imports CodeDisplay/HoldToConfirmButton from the new shared module (repointed) so nothing double-defines them"
  prohibitions:
    - "MUST NOT change the 3s hold-gate timing contract or the SetSessionFunnelWrite semantics — the reduced-motion path is ADDITIVE (still a deliberate confirm), not a change to the safety gate (SM-08, D-09)"
    - "MUST NOT introduce @testing-library/react — use raw react-dom/client + flushSync like every existing share test"
    - "MUST NOT leave a second copy of CodeDisplay/HoldToConfirmButton in SessionSharePanel.tsx (single source of truth)"
---

<objective>
Hoist the two private, non-exported components `CodeDisplay` (~SessionSharePanel.tsx:114) and `HoldToConfirmButton` (~:29) into a shared, exported module so the new per-tab components and `ShareLinkCard` can reuse them (D-09 requires them "as-is"). While hoisting `HoldToConfirmButton`, add the `prefers-reduced-motion` plain-confirm fallback required by D-07/SM-07.

**Decision (resolves the D-07 vs D-09 tension flagged in RESEARCH Pitfall 1):** D-09's "preserve HoldToConfirmButton as-is" governs the SAFETY GATE CONTRACT — the 3s deliberate hold and the `SetSessionFunnelWrite` semantics are unchanged. D-07's reduced-motion fallback is ADDITIVE: under `prefers-reduced-motion: reduce` the control degrades to a single deliberate confirm click (still a deliberate arming action, not an accidental one). This is compatible with SM-08's "3s hold-gate unchanged" because the gate's purpose (require deliberate action to arm public write) is preserved.

Purpose: Regression-safe scaffolding — zero behavior change to the normal hold path, plus the new a11y fallback, verified against the existing panel test suite before any tab extraction.
Output: `shared.tsx`, repointed `SessionSharePanel.tsx`, new hold-button test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-RESEARCH.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-CONTEXT.md
@frontend/src/components/SessionSharePanel.tsx
@frontend/src/components/__tests__/SessionSharePanel.test.tsx

# HEAD refs: HoldToConfirmButton ~:29-109, CodeDisplay ~:114-163 (both private fn components).
# Hold internals: startHold uses HOLD_DURATION_MS (3000) + setInterval tick — preserve verbatim.
# style.css already has .hub-funnel-write-gate__hold-fill { transition: none } under
# @media (prefers-reduced-motion: reduce) — CSS-only today; the BEHAVIORAL fallback is new.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared.tsx (exported CodeDisplay + HoldToConfirmButton), repoint SessionSharePanel imports</name>
  <read_first>
    - frontend/src/components/SessionSharePanel.tsx :1-27 (imports), :29-109 (HoldToConfirmButton), :114-163 (CodeDisplay), and every internal call site of both
  </read_first>
  <files>frontend/src/components/SessionShare/shared.tsx, frontend/src/components/SessionSharePanel.tsx</files>
  <action>
    Create `frontend/src/components/SessionShare/shared.tsx`. Move `HoldToConfirmButton` and `CodeDisplay` verbatim from `SessionSharePanel.tsx` into it as named exports (including their prop interfaces, `HOLD_DURATION_MS`/`HOLD_TICK_MS` constants, and any imports they need — heroicons, react hooks). Do NOT change their behavior in this task. In `SessionSharePanel.tsx`, delete the moved definitions and add an import of `CodeDisplay`/`HoldToConfirmButton` from `./SessionShare/shared`. Keep the write-gate `hub-funnel-write-gate__*` class names and the hold-fill markup identical so `style.css` and existing tests keep matching.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/components/__tests__/SessionSharePanel.test.tsx --reporter=dot && grep -q "export function HoldToConfirmButton\|export const HoldToConfirmButton" src/components/SessionShare/shared.tsx && grep -q "export function CodeDisplay\|export const CodeDisplay" src/components/SessionShare/shared.tsx && ! grep -q "function HoldToConfirmButton" src/components/SessionSharePanel.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `shared.tsx` exports both components; `SessionSharePanel.tsx` imports them and no longer defines them locally.
    - Existing `SessionSharePanel.test.tsx` passes unchanged.
    - `pnpm build` succeeds.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add prefers-reduced-motion plain-confirm fallback to HoldToConfirmButton</name>
  <read_first>
    - frontend/src/components/SessionShare/shared.tsx (HoldToConfirmButton after the move)
    - 173-RESEARCH.md "Pitfall 1" (reduced-motion behavioral fallback is new)
    - frontend/src/components/__tests__/SessionSharePanel.test.tsx (matchMedia mock pattern, if any; raw react-dom render pattern)
  </read_first>
  <files>frontend/src/components/SessionShare/shared.tsx, frontend/src/components/__tests__/HoldToConfirmButton.test.tsx</files>
  <behavior>
    - Given prefers-reduced-motion: reduce, the control renders a single confirm button (no hold-fill element) whose click calls onConfirm exactly once.
    - Given no reduced-motion preference, the control keeps the existing timed 3s hold (hold-fill element present) and only calls onConfirm after the full hold.
    - The `disabled` prop still fully disables both paths (no confirm while disabled).
    - The visible text label is present in both paths (SM-07 keeps a text label).
  </behavior>
  <action>
    Add a `prefers-reduced-motion: reduce` check inside `HoldToConfirmButton` (read via `window.matchMedia('(prefers-reduced-motion: reduce)').matches`, guarded for test env). When reduced-motion is active, render a plain `<button>` that calls `onConfirm` on a single click and shows the same text label — NO timed fill, NO setInterval. When not active, render the existing hold gesture unchanged. Keep the `disabled` guard on both branches. Do NOT change `HOLD_DURATION_MS` or any timing on the non-reduced path. Add `HoldToConfirmButton.test.tsx` using raw `react-dom/client` + `flushSync`, mocking `matchMedia` to toggle both branches, asserting: (a) reduced-motion → single click fires onConfirm and no `.hub-funnel-write-gate__hold-fill` element; (b) non-reduced → hold-fill element present and a bare click does NOT fire onConfirm.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/components/__tests__/HoldToConfirmButton.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Both behavior cases asserted and green; assertions are attribute/text/structure-based (no computed color).
    - Non-reduced 3s hold path byte-behavior unchanged (existing SessionSharePanel write-gate tests still pass).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
Scaffolding plan; no capability/token/RPC change. Preservation guarantee: the 3s hold-to-confirm SAFETY GATE and `SetSessionFunnelWrite` semantics are unchanged — the reduced-motion branch is an additive a11y affordance that still requires a deliberate confirm action. Full STRIDE register in 173-05-PLAN.md.
</threat_model>

<verification>
`pnpm vitest run` for both share test files green; `pnpm build` succeeds; single source of truth for the two hoisted components.
</verification>

<success_criteria>
CodeDisplay + HoldToConfirmButton are exported/shared with zero regression, and the hold button honors `prefers-reduced-motion` with a plain deliberate-confirm fallback — safety timing contract intact.
</success_criteria>

<output>
Create `.planning/phases/173-share-modal-three-tab-segmented-redesign/173-02-SUMMARY.md` when done.
</output>
