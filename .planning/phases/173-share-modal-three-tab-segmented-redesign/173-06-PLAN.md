---
phase: 173-share-modal-three-tab-segmented-redesign
plan: 06
type: execute
wave: 4
depends_on: ["173-01", "173-03", "173-05"]
files_modified:
  - frontend/src/components/Hub/SessionShareModal.tsx
  - frontend/src/components/__tests__/SessionShareModal.test.tsx
  - frontend/src/components/__tests__/SessionShareModal.disconnect.test.tsx
  - frontend/src/components/SessionSharePanel.tsx
  - frontend/src/components/__tests__/SessionSharePanel.test.tsx
autonomous: true
requirements: [SM-01, SM-05, SM-07, SM-08]
must_haves:
  truths:
    - "The three toggles + divider form a fixed control strip at the top of .hub-share-modal__body; toggling a control never reflows/pushes down content already on screen (SM-01, D-01)"
    - "When shareEnabled && riskPanelOpen (internet toggled, not yet confirmed), FunnelRiskPanel renders as a transient confirm view that REPLACES the segmented panel region — not injected above the links (SM-05/D-05)"
    - "When shareEnabled && (!internetEnabled || funnelOn), the ShareSegmentedControl + active tab body render; the tab body sits in the single .hub-share-modal__tabpanel scroll region (SM-02/SM-03)"
    - "Both Internet segments are aria-disabled until funnelOn (internetConfirmed); on successful confirm the active tab defaults to Internet·Read-only; disabling internet resets the active tab to Tailnet (SM-05)"
    - "Each toggle shows an On/Off/N-A text state label in addition to knob position (SM-07)"
    - "Modal renders at width min(520px, calc(100vw - 48px)) and inline style={} layout is migrated to CSS classes (SM-08)"
  artifacts:
    - "frontend/src/components/Hub/SessionShareModal.tsx — tab state machine, ShareSegmentedControl + tab dispatch, transient confirm view, On/Off/N-A labels"
    - "Updated SessionShareModal.test.tsx + disconnect test asserting the new structure (attribute/text-based, non-hue)"
  key_links:
    - "internetEnabled = funnelOn || riskPanelOpen (existing); internetConfirmed = funnelOn (existing); confirm-view condition simplifies to riskPanelOpen — NO new parallel state (RESEARCH)"
    - "All RPC handlers/props (SetSessionFunnel, gate handlers, cachedShare, funnelUrl, writeGate*) stay in the shell and flow down to tabs; onOpenHelp/handleOpenHelp preserved (FUI-06)"
    - "SessionSharePanel.tsx + SessionSharePanel.test.tsx deleted once the modal dispatches to the three tabs"
  prohibitions:
    - "MUST NOT add parallel internetEnabled/internetConfirmed state — reuse riskPanelOpen/funnelOn (RESEARCH anti-pattern; single source of truth)"
    - "MUST NOT inject the risk panel inline above the link content — it REPLACES the panel region (D-05)"
    - "MUST NOT change capabilities/tokens/TTL/Funnel-teardown/3s hold-gate behavior (D-08, out-of-scope)"
    - "MUST NOT distinguish tab/toggle state by hue alone — On/Off/N-A text labels + ⚠ glyph + inset ring carry the state (SM-07)"
---

<objective>
Wire the shell: turn `SessionShareModal.tsx` into the fixed control strip + swappable-panel host. Add the tab state machine (reusing existing `riskPanelOpen`/`funnelOn` — NO new parallel state), render `ShareSegmentedControl` + the three tab bodies in the single `.hub-share-modal__tabpanel` scroll region, reposition `FunnelRiskPanel` as the transient confirm view (replacing the panel, not injected inline), add On/Off/N-A toggle labels, migrate inline `style={}` layout to CSS classes, and delete the now-obsolete `SessionSharePanel.tsx` (+ its test). Update the modal test suites in lockstep so the wave lands green.

Purpose: SM-01 (fixed strip, no reflow), SM-05 (state machine + transient confirm + default-RO + reset-Tailnet), SM-07 (On/Off/N-A labels), SM-08 (width already bumped in plan 01; tests updated here).
Output: reworked shell + updated modal tests; deleted panel + panel test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-DESIGN-129.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-RESEARCH.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-CONTEXT.md
@frontend/src/components/Hub/SessionShareModal.tsx
@frontend/src/components/Hub/FunnelRiskPanel.tsx
@frontend/src/components/__tests__/SessionShareModal.test.tsx
@frontend/src/components/__tests__/SessionShareModal.disconnect.test.tsx

# HEAD refs (re-verify): body .hub-share-modal__body ~:541; toggles ~:567/:607/:661;
# inline FunnelRiskPanel mount ~:691-700; funnelError ~:701-703; SessionSharePanel mount ~:707-728.
# State: funnelOn :323, riskPanelOpen :324, funnelError :326. handleFunnelToggle :344.
# Mapping: internetEnabled = funnelOn||riskPanelOpen ; internetConfirmed = funnelOn ;
#   confirm-view-showing = riskPanelOpen (simplified). Reset-to-Tailnet + default-RO effects are NEW.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tab state machine + ShareSegmentedControl + tab dispatch + transient confirm view</name>
  <read_first>
    - frontend/src/components/Hub/SessionShareModal.tsx :318-370 (funnelOn/riskPanelOpen/handleFunnelToggle/enable/cancel) and :541-733 (body render, inline FunnelRiskPanel, SessionSharePanel mount + all 17 props)
    - frontend/src/components/Hub/ShareSegmentedControl.tsx + exported ShareTab type (plan 03)
    - frontend/src/components/SessionShare/{TailnetTab,InternetReadOnlyTab,InternetFullAccessTab}.tsx (plan 05)
  </read_first>
  <files>frontend/src/components/Hub/SessionShareModal.tsx</files>
  <action>
    Add a `tab` state (`ShareTab`, default `'tailnet'`). Add two effects (RESEARCH Pitfall 4): when `funnelOn` becomes true, set `tab='internet-ro'` (default to the safer landing per D-05); when `funnelOn` becomes false, reset `tab='tailnet'`. Replace the `SessionSharePanel` mount and the inline `FunnelRiskPanel` (at ~:691) with the D-05 state machine in the swappable region below the divider:
    (a) `!shareEnabled` → an empty hint "Turn on 'Share the session' to get a link.";
    (b) `shareEnabled && riskPanelOpen` (internet toggled, not confirmed) → render `FunnelRiskPanel` as the transient confirm view IN the swappable region (NOT inline above links), and move the `funnelError` slot to render with it (RESEARCH A3); preserve `onOpenHelp={handleOpenHelp}` (FUI-06);
    (c) `shareEnabled && cachedShare && !riskPanelOpen` → render `ShareSegmentedControl` (Tailnet·Private always enabled; Internet·Read-only + Internet·⚠ Full-access disabled unless `funnelOn`, and also disabled when `funnelDisabled`; Full-access `danger:true`) followed by the active tab body wrapped in a `.hub-share-modal__tabpanel` div. Dispatch: `tailnet`→`TailnetTab`, `internet-ro`→`InternetReadOnlyTab`, `internet-fa`→`InternetFullAccessTab`, redistributing all former SessionSharePanel props (keep `funnelActive={session.funnelActive}` distinct from `funnelOn` per Pitfall 3). Keep every RPC handler in the shell; do not move bindings into tabs. Remove the dead `import { SessionSharePanel }`.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit 2>&1 | grep -i "SessionShareModal" && exit 1 || (grep -q "ShareSegmentedControl" src/components/Hub/SessionShareModal.tsx && grep -q "hub-share-modal__tabpanel" src/components/Hub/SessionShareModal.tsx && ! grep -q "from '../SessionSharePanel'" src/components/Hub/SessionShareModal.tsx && echo OK)</automated>
  </verify>
  <acceptance_criteria>
    - Modal compiles clean; renders ShareSegmentedControl + tab dispatch in `.hub-share-modal__tabpanel`.
    - Risk panel renders in the swappable region only when `riskPanelOpen`, replacing (not preceding) the segmented panel.
    - Default-to-Read-only-on-confirm and reset-to-Tailnet-on-disable effects present.
    - No import of SessionSharePanel remains.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: On/Off/N-A toggle labels + migrate inline style={} to classes + delete SessionSharePanel</name>
  <read_first>
    - frontend/src/components/Hub/SessionShareModal.tsx :567-687 (the three toggle rows with inline style cursor/opacity/pointerEvents)
    - frontend/src/components/__tests__/SessionSharePanel.test.tsx (confirm no other production file imports SessionSharePanel before deleting)
  </read_first>
  <files>frontend/src/components/Hub/SessionShareModal.tsx, frontend/src/components/SessionSharePanel.tsx, frontend/src/components/__tests__/SessionSharePanel.test.tsx</files>
  <action>
    Add an On/Off/N-A state text label to each of the three toggle rows per D-07/SM-07 (e.g. a `settings-panel__toggle-state` span reading "On"/"Off", and "N/A" when the control is disabled/not-applicable — Browse toggle when `!shareEnabled`, Internet toggle when `funnelDisabled`). Migrate the inline `style={{ cursor... }}`, `style={{ opacity: 0.6, pointerEvents: 'none' }}`, and `style={{ margin... }}` layout on the toggle rows/sections to CSS classes (add the classes to `style.css` if needed; keep colors via `--hub-*`). Then delete `frontend/src/components/SessionSharePanel.tsx` and `frontend/src/components/__tests__/SessionSharePanel.test.tsx` (its coverage now lives in the per-tab test files from plan 05). Verify no production import of SessionSharePanel remains.
  </action>
  <verify>
    <automated>cd frontend && test ! -f src/components/SessionSharePanel.tsx && test ! -f src/components/__tests__/SessionSharePanel.test.tsx && ! grep -rq "from '../SessionSharePanel'\|from './SessionSharePanel'" src && grep -q "toggle-state\|On/Off\|'N/A'\|>On<\|>Off<" src/components/Hub/SessionShareModal.tsx && pnpm exec tsc --noEmit >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Three toggles each show an On/Off/N-A text label.
    - Toggle-row inline layout styles migrated to classes.
    - SessionSharePanel.tsx + its test deleted; no dangling imports; project compiles.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update SessionShareModal.test.tsx + disconnect test to the new structure (non-hue)</name>
  <read_first>
    - frontend/src/components/__tests__/SessionShareModal.test.tsx (existing 37 tests — extend, keep FUI-01/02/05 confirm+warmup coverage)
    - frontend/src/components/__tests__/SessionShareModal.disconnect.test.tsx
  </read_first>
  <files>frontend/src/components/__tests__/SessionShareModal.test.tsx, frontend/src/components/__tests__/SessionShareModal.disconnect.test.tsx</files>
  <action>
    Update the modal test suites (raw react-dom/client + flushSync; NO @testing-library) to the new structure, adding assertions for: control-strip DOM stability across a toggle (SM-01 — the three toggle rows keep their position/order when a toggle flips); the tab body lives in a single `.hub-share-modal__tabpanel` region and `.hub-share-modal__body` does not carry the whole-dialog scroll (SM-02, structural/class assertion); `role="tablist"` + three `role="tab"` with `aria-selected` (SM-03); both Internet tabs `aria-disabled` until confirmed, default tab = Internet·Read-only after confirm, reset-to-Tailnet on internet-disable (SM-05, Pitfall 4); On/Off/N-A text labels present (SM-07); modal width source string `min(520px, calc(100vw - 48px))` present in style.css (SM-08 source-string gate). Repair any existing assertions broken by the restructure (e.g. those that asserted the inline FunnelRiskPanel position or SessionSharePanel presence). All assertions attribute/text/role/class based — never computed color.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/components/__tests__/SessionShareModal.test.tsx src/components/__tests__/SessionShareModal.disconnect.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Both modal test suites green with the new assertions (SM-01/02/03/05/07/08).
    - No hue/computed-color assertions.
    - Pre-existing behavioral coverage (confirm gesture, warmup→live URL) preserved.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
Preservation-focused (full STRIDE register in 173-05-PLAN.md). Shell-specific must-NOT guarantees:
(a) public-write arming still requires the hold-to-confirm gate and renders ONLY via `InternetFullAccessTab` (SM-04) — the shell dispatches the write flow to exactly one tab;
(b) no capability/token/TTL/Funnel-teardown behavior changes — every RPC handler stays in the shell, unchanged (D-08);
(c) tab-availability gating prevents reaching the Full-access body before `funnelOn` (T-173-01) — Internet segments are `aria-disabled`/`disabled` until confirmed, asserted in Task 3.
</threat_model>

<verification>
`pnpm vitest run` for both modal suites green; `pnpm build` (tsc + vite build) succeeds; SessionSharePanel removed; no new parallel state.
</verification>

<success_criteria>
The Share modal is a fixed control strip + swappable three-tab panel with a transient confirm view, colorblind-safe On/Off/N-A labels, correct tab-availability/default/reset behavior, and green modal tests — with zero change to the sharing capability model.
</success_criteria>

<output>
Create `.planning/phases/173-share-modal-three-tab-segmented-redesign/173-06-SUMMARY.md` when done.
</output>
