---
phase: 173-share-modal-three-tab-segmented-redesign
plan: 03
type: execute
wave: 2
depends_on: ["173-01"]
files_modified:
  - frontend/src/components/Hub/ShareSegmentedControl.tsx
  - frontend/src/components/__tests__/ShareSegmentedControl.test.tsx
autonomous: true
requirements: [SM-03, SM-07]
must_haves:
  truths:
    - "ShareSegmentedControl renders role=tablist with three role=tab buttons, aria-selected reflecting the active tab (SM-03)"
    - "Roving tabindex: the active tab has tabIndex=0, all others tabIndex=-1 (SM-07)"
    - "ArrowLeft/ArrowRight move selection between ENABLED segments, wrapping at ends and skipping disabled segments (SM-07)"
    - "Disabled segments carry aria-disabled + disabled and show 'N/A' sub text; clicking them does not select (SM-05/SM-07)"
    - "The Full-access segment carries the is-danger class and a ⚠ glyph in its label — distinguished without hue (SM-07)"
  artifacts:
    - "frontend/src/components/Hub/ShareSegmentedControl.tsx — new role=tablist component"
    - "frontend/src/components/__tests__/ShareSegmentedControl.test.tsx — a11y contract tests"
  key_links:
    - "Component emits onSelect(id) only for enabled segments; the shell (plan 06) owns the active-tab state"
  prohibitions:
    - "MUST NOT rely on hue to distinguish the danger tab — use the ⚠ glyph in the label + the is-danger class (inset ring is CSS from plan 01)"
    - "MUST NOT add a UI library (Radix/headless) — hand-roll the roving tabindex per the WAI-ARIA APG pattern, consistent with every other control in this app"
    - "Test assertions MUST be attribute/text/role based, never computed color (owner is colorblind)"
---

<objective>
Build the new `ShareSegmentedControl` — a real `role="tablist"` with three `role="tab"` segments (Tailnet·Private / Internet·Read-only / Internet·⚠ Full-access), roving tabindex, arrow-key navigation that skips disabled segments, and colorblind-safe danger styling. This is net-new UI infrastructure — the codebase has no existing tablist/roving-tabindex precedent (grep-confirmed).

Purpose: SM-03 (segmented control that will swap the panel body) + SM-07 (keyboard-operable, colorblind-safe). Standalone component with its own test — the shell wires it in plan 06.
Output: `ShareSegmentedControl.tsx` + test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-DESIGN-129.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-RESEARCH.md
@frontend/src/components/Hub/FunnelRiskPanel.tsx

# Classes .share-segbar/.share-seg/.share-seg__main/.share-seg__sub/.is-active/.is-danger
# ship in plan 01 (style.css). RESEARCH "Pattern 1: Roving tabindex tablist" has the reference
# implementation incl. the ArrowLeft/ArrowRight handler that the DESIGN sketch omits.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ShareSegmentedControl.tsx (tablist, roving tabindex, arrow-key nav, danger glyph)</name>
  <read_first>
    - 173-RESEARCH.md "### Pattern 1: Roving tabindex tablist" (reference impl with arrow-key handler)
    - 173-DESIGN-129.md "Segmented control (a11y)" sketch + "Colorblind & accessibility affordances"
    - frontend/src/style.css `.share-seg*` rules (added in plan 01) to match class names exactly
  </read_first>
  <files>frontend/src/components/Hub/ShareSegmentedControl.tsx</files>
  <behavior>
    - Renders a `.share-segbar` container with role=tablist and aria-label.
    - Each tab: role=tab, aria-selected = (active === id), aria-disabled + disabled for gated tabs, tabIndex = active ? 0 : -1, className share-seg (+ is-active when active, + is-danger for the danger tab).
    - `.share-seg__main` shows the main label; `.share-seg__sub` shows the sub label, or 'N/A' when disabled.
    - onClick selects only when not disabled.
    - onKeyDown ArrowRight/ArrowLeft move selection among enabled tabs (wrap, skip disabled), preventDefault.
    - The danger tab's label includes a ⚠ glyph.
  </behavior>
  <action>
    Create `frontend/src/components/Hub/ShareSegmentedControl.tsx` implementing D-03/D-07/SM-03/SM-07 exactly per the RESEARCH "Pattern 1" reference (which adds the ArrowLeft/ArrowRight roving handler the DESIGN sketch omits). Props: `tabs` (array of `{ id, main, sub, disabled?, danger? }`), `active` (current id), `onSelect(id)`. Use the `.share-seg*` classes from plan 01; do not inline styles or hex colors. The danger sub label carries the ⚠ glyph (belt-and-suspenders with the is-danger inset ring). Provide a stable `ShareTab` id union type (`'tailnet' | 'internet-ro' | 'internet-fa'`) exported for the shell to reuse.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep -i "ShareSegmentedControl" && echo "TSC ERRORS" && exit 1 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Component compiles clean; exports `ShareSegmentedControl` and the `ShareTab` type.
    - Uses `.share-seg*` classes (plan 01), no inline hex.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ShareSegmentedControl.test.tsx — a11y + roving + arrow-nav contract</name>
  <read_first>
    - frontend/src/components/__tests__/FunnelRiskPanel.test.tsx (raw react-dom/client + flushSync render+dispatch pattern to copy)
    - frontend/src/components/Hub/ShareSegmentedControl.tsx (from Task 1)
  </read_first>
  <files>frontend/src/components/__tests__/ShareSegmentedControl.test.tsx</files>
  <behavior>
    - role=tablist present with exactly three role=tab children.
    - aria-selected true on the active tab, false on others.
    - Active tab tabIndex=0, others -1.
    - Disabled Internet tabs (when not confirmed) have aria-disabled=true and sub text 'N/A'; clicking a disabled tab does not call onSelect.
    - ArrowRight from the active enabled tab calls onSelect with the next enabled tab id (wraps, skips disabled).
    - The danger tab has the is-danger class and a ⚠ glyph in its rendered text.
  </behavior>
  <action>
    Write `ShareSegmentedControl.test.tsx` using raw `react-dom/client` `createRoot` + `flushSync` (NO @testing-library). Cover every behavior bullet with attribute/text/role/class assertions only — never computed color. Include one case with both Internet tabs disabled (pre-confirm) and one with all enabled (post-confirm) to prove arrow-nav skips disabled segments.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/components/__tests__/ShareSegmentedControl.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - All behavior cases green; assertions attribute/text/role/class only.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
Presentational control; no capability/token/RPC. It emits selection only for enabled segments — but tab-availability gating (which prevents reaching the Full-access body pre-confirm) is enforced by the shell in plan 06 and asserted structurally there. Full STRIDE register in 173-05-PLAN.md.
</threat_model>

<verification>
`pnpm vitest run` for the new test green; `pnpm build` succeeds; no hue-based assertions.
</verification>

<success_criteria>
A keyboard-operable, colorblind-safe segmented control exists and is proven by tests to expose the correct ARIA/roving/arrow-nav contract, ready for the shell to wire.
</success_criteria>

<output>
Create `.planning/phases/173-share-modal-three-tab-segmented-redesign/173-03-SUMMARY.md` when done.
</output>
