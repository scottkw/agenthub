---
phase: 173-share-modal-three-tab-segmented-redesign
plan: 07
type: execute
wave: 5
depends_on: ["173-06"]
files_modified:
  - TESTING.md
autonomous: true
requirements: [SM-08]
must_haves:
  truths:
    - "TESTING.md Suite Manifest vitest count reflects the net test-file delta for this phase (new per-tab + component tests minus the deleted SessionSharePanel.test.tsx) (repo standing rule)"
    - "TESTING.md Traceability map has a repo-relative file-path row for every new share test file"
    - "bash tests/check-traceability-paths.sh exits 0 (every mapped path exists on disk)"
    - "Full vitest suite is green AND tsc --noEmit && vite build succeeds (the wails build gate vitest alone does not cover)"
  artifacts:
    - "TESTING.md — updated Suite Manifest count + Traceability rows for the phase's test files"
  key_links:
    - "New test files: ShareSegmentedControl.test.tsx, ShareLinkCard.test.tsx, TailnetTab.test.tsx, InternetReadOnlyTab.test.tsx, InternetFullAccessTab.test.tsx, HoldToConfirmButton.test.tsx; deleted: SessionSharePanel.test.tsx"
  prohibitions:
    - "MUST NOT put test names in the Traceability path column — repo-relative file paths only (.ts/.tsx/.go/.sh), per agenthub/CLAUDE.md standing rule"
    - "MUST NOT declare the phase done on a green vitest run alone — the tsc && vite build gate must also pass (project memory: vitest tolerates TS errors the build rejects)"
---

<objective>
Close the phase's regression bookkeeping and run the full quality gate. Per the AgentHub standing rule (`agenthub/CLAUDE.md` + TESTING.md Section 6), any added/renamed/removed test file requires updating the Suite Manifest count (Section 2) and the Traceability map (Section 4, repo-relative paths only), then running `bash tests/check-traceability-paths.sh`. Finally, prove the whole phase green with the full vitest suite + the `tsc && vite build` gate.

Purpose: SM-08's "tests updated" bookkeeping + the phase-level green gate before `/gsd-verify-work`.
Output: reconciled `TESTING.md`; full-suite + build proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@TESTING.md
@.planning/phases/173-share-modal-three-tab-segmented-redesign/173-VALIDATION.md

# Standing rule (agenthub/CLAUDE.md): Suite Manifest (Section 2) count + Traceability (Section 4,
# repo-relative file paths only) + run `bash tests/check-traceability-paths.sh` before committing.
# vitest group count today = 142 *.test.ts/tsx files. Net delta = (+6 new) - (1 deleted panel test) = +5.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile TESTING.md Suite Manifest + Traceability map</name>
  <read_first>
    - TESTING.md Section 2 (Suite Manifest — vitest count + notes) and Section 4 (Traceability map — existing share/funnel rows + column format)
    - The list of test files actually created/deleted in plans 02-06 (verify on disk before counting)
  </read_first>
  <files>TESTING.md</files>
  <action>
    Recount the vitest (frontend) group in the Suite Manifest to match the actual `*.test.ts/tsx` file count after this phase (net +5: added `ShareSegmentedControl.test.tsx`, `ShareLinkCard.test.tsx`, `TailnetTab.test.tsx`, `InternetReadOnlyTab.test.tsx`, `InternetFullAccessTab.test.tsx`, `HoldToConfirmButton.test.tsx`; removed `SessionSharePanel.test.tsx`) — confirm the real number with `find frontend/src -name '*.test.ts*' | wc -l` rather than trusting the arithmetic. Add a Phase 173 Suite Manifest note describing the redistribution (SessionSharePanel.test.tsx split into per-tab suites). Add a Traceability row for each NEW share test file with a repo-relative file path only (no test names in the path column), mapping to the SM-0x requirement(s) it covers. If a deleted-file path was previously mapped, remove/repoint that row.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && bash tests/check-traceability-paths.sh && grep -q "TailnetTab.test.tsx\|InternetFullAccessTab.test.tsx" TESTING.md && ! grep -q "SessionSharePanel.test.tsx" TESTING.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `check-traceability-paths.sh` exits 0.
    - Suite Manifest count matches `find frontend/src -name '*.test.ts*' | wc -l`.
    - New share test files mapped by repo-relative path; deleted panel test no longer referenced.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full phase gate — vitest suite + tsc && vite build</name>
  <read_first>
    - 173-VALIDATION.md "Sampling Rate" / "Manual-Only Verifications" (the live-UAT items deferred to /gsd-verify-work)
  </read_first>
  <files>TESTING.md</files>
  <action>
    Run the full frontend vitest suite and the build gate. If anything is red, fix within the touched files (do not descope). Record the pass in the plan SUMMARY. Note the three manual-only verifications from 173-VALIDATION.md (no whole-dialog scroll / no reflow-on-toggle; ⚠ glyph + inset-ring colorblind check at source; prefers-reduced-motion hold fallback) as `/gsd-verify-work` live-UAT items — they need a rendered viewport (jsdom has no layout engine) and are not automatable here.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run && pnpm exec tsc --noEmit && pnpm exec vite build</automated>
  </verify>
  <acceptance_criteria>
    - Full vitest suite green.
    - `tsc --noEmit` clean and `vite build` succeeds.
    - Manual-only UAT items recorded for /gsd-verify-work.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
No code change (docs + gate only). The phase's preservation guarantees (SM-04 wall-off, unchanged capability/token/TTL/teardown/hold-gate) are enforced by tests in plans 02-06; this plan proves the whole suite green. Full STRIDE register in 173-05-PLAN.md.
</threat_model>

<verification>
`check-traceability-paths.sh` exits 0; full vitest suite green; `tsc && vite build` succeeds.
</verification>

<success_criteria>
TESTING.md reflects the phase's test-file changes with valid repo-relative paths, and the entire frontend suite + build gate is green — ready for `/gsd-verify-work` (with the three live-UAT items flagged).
</success_criteria>

<output>
Create `.planning/phases/173-share-modal-three-tab-segmented-redesign/173-07-SUMMARY.md` when done.
</output>
