---
phase: 60-local-network-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/webserver/selfcert.go
  - internal/webserver/auth.go
  - internal/webserver/localip.go
  - internal/webserver/server.go
  - internal/webserver/selfcert_test.go
  - internal/webserver/auth_test.go
  - internal/webserver/localip_test.go
  - internal/webserver/server_test.go
autonomous: true
requirements: [NET-01, NET-02, NET-03]

must_haves:
  truths:
    - "Self-signed TLS cert uses P256 curve and includes IP SAN for the bind address"
    - "Basic Auth middleware returns 401 with WWW-Authenticate header when no credentials provided"
    - "Basic Auth middleware returns 200 when correct password provided"
    - "GetLANIP returns a non-loopback private IPv4 address and excludes Tailscale CGNAT range"
    - "WebServer starts in local mode with self-signed cert and auth middleware"
    - "BaseURL returns IP-based URL in local mode, FQDN-based URL in tailscale mode"
  artifacts:
    - path: "internal/webserver/selfcert.go"
      provides: "P256 CA+leaf self-signed cert generation"
      exports: ["GenerateSelfSignedCert"]
    - path: "internal/webserver/auth.go"
      provides: "HTTP Basic Auth middleware"
      exports: ["basicAuthMiddleware"]
    - path: "internal/webserver/localip.go"
      provides: "LAN IP selection with Tailscale exclusion"
      exports: ["GetLANIP"]
    - path: "internal/webserver/server.go"
      provides: "Mode-aware Config, Start, BaseURL"
      contains: "Mode string"
  key_links:
    - from: "internal/webserver/server.go"
      to: "internal/webserver/selfcert.go"
      via: "startLocal() calls GenerateSelfSignedCert"
      pattern: "GenerateSelfSignedCert"
    - from: "internal/webserver/server.go"
      to: "internal/webserver/auth.go"
      via: "startLocal() wraps mux with basicAuthMiddleware"
      pattern: "basicAuthMiddleware"
    - from: "internal/webserver/server.go"
      to: "internal/webserver/localip.go"
      via: "consumer uses GetLANIP to populate Config.BindIP"
      pattern: "GetLANIP"
---

<objective>
Build the webserver-layer local mode infrastructure: self-signed TLS cert generation, HTTP Basic Auth middleware, LAN IP selection, and mode-aware Config/Start/BaseURL.

Purpose: Enables the web server to operate over local network HTTPS with password protection when Tailscale is unavailable.
Output: Three new Go files (selfcert.go, auth.go, localip.go) plus modified server.go with mode dispatch, and comprehensive tests for all four.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-local-network-fallback/60-RESEARCH.md

<interfaces>
<!-- Current webserver types and functions the executor needs -->

From internal/webserver/server.go:
```go
type Config struct {
    BindIP    string
    Port      int
    FQDN      string
    TLSConfig *tls.Config
}

func NewWebServer(cfg Config, manager *relay.HubManager) (*WebServer, error)
func (ws *WebServer) Start() error
func (ws *WebServer) BaseURL() string
func (ws *WebServer) Addr() string
func (ws *WebServer) Stop() error
```

From internal/webserver/server_test.go:
```go
// selfSignedTLSForTest() — existing P256 CA+leaf pattern to extract into selfcert.go
// This is the reference implementation for GenerateSelfSignedCert
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create selfcert.go, auth.go, localip.go with tests (Wave 0 + RED + GREEN)</name>
  <files>
    internal/webserver/selfcert.go
    internal/webserver/selfcert_test.go
    internal/webserver/auth.go
    internal/webserver/auth_test.go
    internal/webserver/localip.go
    internal/webserver/localip_test.go
  </files>
  <read_first>
    internal/webserver/server.go
    internal/webserver/server_test.go
  </read_first>
  <behavior>
    - TestGenerateSelfSignedCert: cert uses P256 curve (not P521); cert includes IP SAN matching input IP; TLS handshake succeeds with returned tls.Config; cert NotAfter is ~365 days from now
    - TestBasicAuthMiddleware_Unauthorized: request with no Authorization header returns 401; response includes WWW-Authenticate: Basic realm="AgentHub"
    - TestBasicAuthMiddleware_Authorized: request with correct password returns 200; any username accepted (only password matters)
    - TestGetLANIP: returns a non-empty string; returned IP is not 127.x.x.x (loopback); returned IP is valid IPv4
    - TestGetLANIP_ExcludesTailscale: isTailscaleIP correctly identifies 100.64.x.x through 100.127.x.x as Tailscale CGNAT
  </behavior>
  <action>
    **selfcert.go** — Create `internal/webserver/selfcert.go` in package `webserver`:
    - Function signature: `func GenerateSelfSignedCert(ip string) (*tls.Config, error)`
    - Extract the P256 CA+leaf pattern from `selfSignedTLSForTest()` in `server_test.go` (lines ~29-88)
    - CA cert template: SerialNumber=1, Org="AgentHub Local", IsCA=true, KeyUsage=KeyUsageCertSign, NotBefore=now-1min, NotAfter=now+365days
    - Leaf cert template: SerialNumber=2, CommonName=ip, IPAddresses=[]net.IP{net.ParseIP(ip)}, KeyUsage=KeyUsageDigitalSignature, ExtKeyUsage=[]x509.ExtKeyUsage{x509.ExtKeyUsageServerAuth}, NotBefore=now-1min, NotAfter=now+365days
    - CRITICAL: Use `elliptic.P256()` — NOT P521. Chrome rejects P521 with `tls: illegal parameter`
    - Return `&tls.Config{Certificates: []tls.Certificate{tlsCert}, MinVersion: tls.VersionTLS12}`
    - Imports: crypto/ecdsa, crypto/elliptic, crypto/rand, crypto/tls, crypto/x509, crypto/x509/pkix, encoding/pem, math/big, net, time

    **auth.go** — Create `internal/webserver/auth.go` in package `webserver`:
    - Function signature: `func basicAuthMiddleware(password string) func(http.Handler) http.Handler`
    - Use `r.BasicAuth()` from stdlib to parse Authorization header
    - On failure (ok=false or pass!=password): set `WWW-Authenticate: Basic realm="AgentHub"` header, return `http.Error(w, "Unauthorized", http.StatusUnauthorized)`
    - On success: call `next.ServeHTTP(w, r)`
    - Username is ignored — only password matters

    **localip.go** — Create `internal/webserver/localip.go` in package `webserver`:
    - Function signature: `func GetLANIP() (string, error)`
    - Helper: `func isTailscaleIP(ip net.IP) bool` — returns true for 100.64.0.0/10 (ip[0]==100 && ip[1]>=64 && ip[1]<=127)
    - Helper: `func ipFromInterface(ifaces []net.Interface, name string) string` — find interface by name, return first private IPv4
    - Helper: `func firstPrivateIPv4(iface net.Interface) string` — iterate addrs, skip loopback/link-local/Tailscale, return first IPv4 in 10.x/172.16-31.x/192.168.x ranges
    - First pass: try preferred interface names ["en0", "eth0", "wlan0"]
    - Second pass: iterate all interfaces, skip loopback and down interfaces, return first private IPv4
    - Return error "no suitable LAN IP found" if nothing found
    - Comment in code: preference order for multi-interface machines (VPN + Wi-Fi) per STATE.md concern

    **Tests** — Write tests FIRST (RED), then verify they fail, then create implementations (GREEN):
    - selfcert_test.go: TestGenerateSelfSignedCert — call GenerateSelfSignedCert("192.168.1.100"), verify returned tls.Config is non-nil, start a test TLS server with it, dial with InsecureSkipVerify, verify handshake succeeds. Also parse the leaf cert and verify: PublicKeyAlgorithm==x509.ECDSA, check IPAddresses contains 192.168.1.100
    - auth_test.go: TestBasicAuthMiddleware_Unauthorized — create test handler behind middleware, send request with no auth, assert 401 + WWW-Authenticate header. TestBasicAuthMiddleware_Authorized — send request with basic auth (user="x", pass="testpass"), assert 200
    - localip_test.go: TestGetLANIP — call GetLANIP(), assert non-empty, assert not "127.x.x.x". TestGetLANIP_ExcludesTailscale — call isTailscaleIP with net.ParseIP("100.100.100.100"), assert true; call with net.ParseIP("192.168.1.1"), assert false
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test ./internal/webserver/... -run "TestGenerateSelfSignedCert|TestBasicAuthMiddleware|TestGetLANIP" -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/webserver/selfcert.go contains `func GenerateSelfSignedCert(ip string) (*tls.Config, error)`
    - internal/webserver/selfcert.go contains `elliptic.P256()` (NOT P521)
    - internal/webserver/selfcert.go contains `x509.KeyUsageCertSign` for CA and `x509.ExtKeyUsageServerAuth` for leaf
    - internal/webserver/auth.go contains `func basicAuthMiddleware(password string) func(http.Handler) http.Handler`
    - internal/webserver/auth.go contains `WWW-Authenticate`
    - internal/webserver/localip.go contains `func GetLANIP() (string, error)`
    - internal/webserver/localip.go contains `func isTailscaleIP(ip net.IP) bool`
    - internal/webserver/localip.go contains `100` and `64` and `127` (CGNAT range check)
    - go test ./internal/webserver/... -run "TestGenerateSelfSignedCert|TestBasicAuthMiddleware|TestGetLANIP" exits 0
  </acceptance_criteria>
  <done>All three new files exist with exported functions, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add Mode+Password to Config, implement startLocal/startTailscale dispatch, mode-aware BaseURL + Mode accessor</name>
  <files>
    internal/webserver/server.go
    internal/webserver/server_test.go
  </files>
  <read_first>
    internal/webserver/server.go
    internal/webserver/server_test.go
    internal/webserver/selfcert.go
    internal/webserver/auth.go
  </read_first>
  <action>
    **Modify internal/webserver/server.go:**

    1. Add two fields to `Config` struct (after TLSConfig):
       ```go
       Mode     string // "tailscale" | "local" — required
       Password string // non-empty when Mode == "local"; Basic Auth password
       ```

    2. Add `Mode()` accessor method on `*WebServer`:
       ```go
       func (ws *WebServer) Mode() string { return ws.config.Mode }
       ```

    3. Rename current `Start()` body to `startTailscale()`:
       - Extract the existing Start() body (lines 115-147) into `func (ws *WebServer) startTailscale() error`
       - Keep the exact same logic: TLSConfig override check, port fallback, etc.

    4. Create `func (ws *WebServer) startLocal() error`:
       - Call `GenerateSelfSignedCert(ws.config.BindIP)` to get tlsCfg
       - If ws.config.TLSConfig is non-nil (test override), use that instead
       - Same port fallback logic as startTailscale: try `ws.config.Port`, fallback to :0
       - `tls.Listen("tcp", addr, tlsCfg)` — bind to specific LAN IP, NOT 0.0.0.0
       - Store listener in ws.listener under lock
       - Wrap `ws.mux` with `basicAuthMiddleware(ws.config.Password)`: `handler := basicAuthMiddleware(ws.config.Password)(ws.mux)`
       - `go http.Serve(ln, handler)` — serve with auth-wrapped handler

    5. Replace `Start()` with dispatch:
       ```go
       func (ws *WebServer) Start() error {
           switch ws.config.Mode {
           case "local":
               return ws.startLocal()
           default:
               return ws.startTailscale()
           }
       }
       ```

    6. Make `BaseURL()` mode-aware:
       - If `ws.config.Mode == "local"`: return `fmt.Sprintf("https://%s:%s", ws.config.BindIP, port)`
       - Otherwise (tailscale): return `fmt.Sprintf("https://%s:%s", ws.config.FQDN, port)` (existing behavior)

    **Add tests to internal/webserver/server_test.go:**

    - `TestLocalModeStart`: Create a WebServer with Config{BindIP: "127.0.0.1", Port: 0, Mode: "local", Password: "testpass"}, call Start(), verify server responds to HTTPS request with correct Basic Auth (use InsecureSkipVerify TLS client). Request without auth should get 401. Request with auth should get 200.
    - `TestBaseURL_LocalMode`: Create a WebServer with Config{BindIP: "192.168.1.50", Mode: "local"}, use TLSConfig override, Start(), verify BaseURL() returns "https://192.168.1.50:{port}" (not an FQDN).
    - `TestBaseURL_TailscaleMode`: Verify existing behavior preserved — BaseURL returns FQDN-based URL when Mode is "tailscale" or empty.

    **Ensure existing tests still pass** — the refactored Start() with default case handles the existing Tailscale tests that don't set Mode.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test ./internal/webserver/... -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/webserver/server.go Config struct contains `Mode string`
    - internal/webserver/server.go Config struct contains `Password string`
    - internal/webserver/server.go contains `func (ws *WebServer) Mode() string`
    - internal/webserver/server.go contains `func (ws *WebServer) startLocal() error`
    - internal/webserver/server.go contains `func (ws *WebServer) startTailscale() error`
    - internal/webserver/server.go contains `case "local":` in Start()
    - internal/webserver/server.go BaseURL contains `ws.config.Mode == "local"` branch
    - internal/webserver/server.go startLocal contains `basicAuthMiddleware(ws.config.Password)`
    - internal/webserver/server.go startLocal contains `GenerateSelfSignedCert`
    - server_test.go contains `TestLocalModeStart` and `TestBaseURL_LocalMode`
    - go test ./internal/webserver/... exits 0
  </acceptance_criteria>
  <done>WebServer supports both tailscale and local modes with proper TLS, auth middleware, and mode-aware BaseURL. All webserver tests pass including existing ones.</done>
</task>

</tasks>

<verification>
All webserver package tests pass:
```bash
cd /Users/ken/dev/agenthub && go test ./internal/webserver/... -v
```

Specific test coverage:
- TestGenerateSelfSignedCert — P256 cert with IP SAN
- TestBasicAuthMiddleware_Unauthorized — 401 without creds
- TestBasicAuthMiddleware_Authorized — 200 with correct password
- TestGetLANIP — returns valid non-loopback IP
- TestGetLANIP_ExcludesTailscale — filters CGNAT range
- TestLocalModeStart — server starts and responds with auth
- TestBaseURL_LocalMode — IP-based URL returned
</verification>

<success_criteria>
- Three new Go files exist: selfcert.go, auth.go, localip.go
- server.go Config has Mode and Password fields
- Start() dispatches to startLocal() or startTailscale() based on Mode
- BaseURL() returns IP-based URL in local mode
- All webserver tests pass (existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/60-local-network-fallback/60-01-SUMMARY.md`
</output>
