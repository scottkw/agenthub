---
phase: 134-modal-interaction
plan: 05
type: execute
wave: 4
depends_on: ["134-01", "134-02", "134-04"]
files_modified:
  - frontend/src/components/Hub/HubPanel.tsx
  - frontend/src/components/Hub/HubPanel.test.tsx
  - frontend/src/App.tsx
  - frontend/src/style.css
autonomous: true
requirements: [MODAL-01, MODAL-03, MODAL-06]
must_haves:
  truths:
    - "Clicking a local card opens the HubModal; the modal mounts inside the Hub surface with relayPort/theme/pluginConfig from App.tsx"
    - "Clicking a remote card without a cached cap calls onRequestRemoteCap (Phase 122 join-code flow) instead of opening the modal directly (MODAL-06)"
    - "After a successful cap exchange for a pending remote session, the Hub modal auto-opens for that session"
    - "The modal CSS exists: overlay z-index 200, grow/shrink keyframes inside the no-preference guard, reduced-motion fallback, token-only colors"
  artifacts:
    - path: "frontend/src/components/Hub/HubPanel.tsx"
      provides: "modalState + handleCardClick (cap gate) + pendingModalSessionId + HubModal render + onCardClick threading + new props"
      contains: "HubModal"
    - path: "frontend/src/App.tsx"
      provides: "relayPort/terminalTheme/pluginConfig + remoteCapsCached + onRequestRemoteCap + onCapAcquired wiring to HubPanel"
      contains: "onRequestRemoteCap"
    - path: "frontend/src/style.css"
      provides: "all .hub-modal* rules + grow/shrink keyframes + reduced-motion guards"
      contains: "hub-modal-overlay"
  key_links:
    - from: "HubPanel.tsx handleCardClick"
      to: "onRequestRemoteCap (App.tsx)"
      via: "remote-without-cap gate"
      pattern: "onRequestRemoteCap"
    - from: "App.tsx handleModalExchange"
      to: "HubPanel onCapAcquired"
      via: "cap-acquired signal opens the modal"
      pattern: "onCapAcquired"
    - from: "HubPanel.tsx"
      to: "HubModal"
      via: "modalState render"
      pattern: "<HubModal"
---

<objective>
Integrate everything: HubPanel gains modal state, the MODAL-06 remote cap gate, and renders HubModal; App.tsx passes relayPort/theme/pluginConfig and wires the remote cap-acquired signal; style.css gets all the modal CSS + animation keyframes + reduced-motion guards.

Purpose: MODAL-01 (modal opens on card click), MODAL-03 (interactive terminal mounted via HubModal→HubInteractiveModal→TerminalPanel with real relayPort/theme), MODAL-06 (remote sessions reuse the locked Phase 122 join-code exchange — uncapped remote clicks route to RemoteJoinCodeModal, and the modal auto-opens after the cap is acquired).

MODAL-06 cap-gate state lives in App.tsx (134-RESEARCH.md finding 4). HubPanel must NOT open the modal for an uncapped remote session — it calls `onRequestRemoteCap`. After Phase 122's `handleModalExchange` succeeds, App.tsx signals HubPanel via `onCapAcquired(sessionId)` and HubPanel opens the pending modal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-modal-interaction/134-PATTERNS.md
@.planning/phases/134-modal-interaction/134-UI-SPEC.md
@.planning/phases/134-modal-interaction/134-04-SUMMARY.md
@.planning/phases/134-modal-interaction/134-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor uses directly. -->

HubModal props (from 134-04):
```typescript
interface HubModalProps {
  session: SessionInfo; sourceRect: DOMRect; relayPort: number;
  fontSize: number; theme: ITheme; pluginConfig?: PluginSettings | null; onClose: () => void
}
```

SessionCardGrid already accepts onCardClick (from 134-02): `onCardClick?: (session: SessionInfo, rect: DOMRect) => void`.

Confirmed App.tsx facts (verified by planner):
- `relayPort` state (number | null) — line ~113; passed to TerminalPanel instances already.
- `terminalTheme: ITheme` — line ~266.
- `pluginConfig: PluginSettings | null` — line ~159.
- `remoteCapsCached: Set<string>` — line ~209.
- `joinModalForSession` state + `setJoinModalForSession` — lines ~212, ~1087, ~1391.
- `handleModalExchange` (line ~1101) on success adds the id to remoteCapsCached and currently calls `handleOpenFileBrowser(pending.id, pending.name)`.
- `<HubPanel ... />` rendered at line ~1357 with sessions/error/onNewSession/onRename/onOpenSession/remoteSessions/isActive.
- `<RemoteJoinCodeModal>` rendered at line ~1585 with remoteSession={joinModalForSession} onExchange={handleModalExchange} onClose.
- `DEFAULT_FONT_SIZE = 14` (line ~69); `fontSizes` record (line ~141).

CSS token + animation contract: see 134-PATTERNS.md "style.css" section and 134-UI-SPEC.md "Animation Keyframes" + "Modal Layout Anatomy" — copy the rules verbatim (they are pre-derived).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HubPanel modal state + MODAL-06 cap gate + HubModal render + new props</name>
  <files>frontend/src/components/Hub/HubPanel.tsx, frontend/src/components/Hub/HubPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/HubPanel.tsx (the file being modified — HubPanelProps interface ~line 130, useState block ~line 180, handleAssignGroup ~line 298, the SessionCardGrid render ~line 322)
    - frontend/src/components/Hub/HubModal.tsx (from 134-04 — props shape)
    - .planning/phases/134-modal-interaction/134-PATTERNS.md ("HubPanel.tsx" section — new props, modal state, handleCardClick callback, HubModal render, onCardClick threading)
    - .planning/phases/134-modal-interaction/134-RESEARCH.md (Open Question 1 — pendingModalSessionId approach for auto-open after cap; Code Examples "Hub Modal Open State")
    - frontend/src/components/Hub/HubPanel.test.tsx (extend with source-inspection MODAL-06 assertions)
  </read_first>
  <behavior>
    - Source: HubPanelProps adds `relayPort?: number`, `terminalTheme?: ITheme`, `pluginConfig?: PluginSettings | null`, `remoteCapsCached?: Set<string>`, `onRequestRemoteCap?: (session: { id: string; name: string; hostname: string }) => void`, `onRegisterCapAcquired?: (fn: (sessionId: string) => void) => void` (or equivalent signal — see action).
    - Source: `modalState: { session: SessionInfo; sourceRect: DOMRect } | null` useState.
    - Source: `pendingModalSessionId` useState (string | null) tracking a remote session awaiting cap.
    - Source: `handleCardClick(session, rect)` — if remote (`session.hostname !== ''`) and NOT in `remoteCapsCached`, sets pendingModalSessionId and calls `onRequestRemoteCap(...)` and returns; otherwise sets modalState.
    - Source: a cap-acquired handler that, when a sessionId matching pendingModalSessionId is signaled, opens the modal for that session.
    - Source: threads `onCardClick={handleCardClick}` to SessionCardGrid.
    - Source: renders `<HubModal>` when `modalState && relayPort !== undefined`, passing session/sourceRect/relayPort/fontSize(DEFAULT)/theme/pluginConfig/onClose.
  </behavior>
  <action>
    Add the new props to `HubPanelProps` and destructure them. Import HubModal from './HubModal', SessionInfo type, ITheme, and the PluginSettings alias.

    Add `const [modalState, setModalState] = useState<{ session: SessionInfo; sourceRect: DOMRect } | null>(null)` and `const [pendingModalSessionId, setPendingModalSessionId] = useState<string | null>(null)`.

    `handleCardClick = useCallback((session, rect) => { const isRemote = !!session.hostname && session.hostname !== ''; if (isRemote && !remoteCapsCached?.has(session.id)) { setPendingModalSessionId(session.id); onRequestRemoteCap?.({ id: session.id, name: session.name, hostname: session.hostname }); return; } setModalState({ session, sourceRect: rect }) }, [remoteCapsCached, onRequestRemoteCap])`.

    Cap-acquired auto-open: register a callback so App.tsx can signal a successful exchange. Implement `onRegisterCapAcquired` — HubPanel calls `onRegisterCapAcquired?.(handleCapAcquired)` in an effect, where `handleCapAcquired(sessionId)` finds the session in `sessions` (or `remoteSessions`), and if `sessionId === pendingModalSessionId` opens the modal using the card's last-known rect (if no rect available, fall back to a centered DOMRect — use `new DOMRect(window.innerWidth/2, window.innerHeight/2, 0, 0)`), then clears pendingModalSessionId. (Storing the rect at request time is acceptable: capture it in a ref inside handleCardClick before returning.)

    Thread `onCardClick={handleCardClick}` to the `<SessionCardGrid>` render.

    Render the modal after the main hub body (still inside the returned fragment): `{modalState && relayPort !== undefined && (<HubModal session={modalState.session} sourceRect={modalState.sourceRect} relayPort={relayPort} fontSize={14} theme={terminalTheme ?? ({} as ITheme)} pluginConfig={pluginConfig} onClose={() => setModalState(null)} />)}`. (Use the DEFAULT_FONT_SIZE constant value 14; do not import App's private constant.)

    Extend HubPanel.test.tsx with SOURCE-INSPECTION assertions (`?raw` import of HubPanel.tsx — do NOT mount HubModal, it pulls in xterm): assert `onRequestRemoteCap` is declared in props; `handleCardClick` contains the remote-without-cap gate (`remoteCapsCached` + `onRequestRemoteCap` reference before `setModalState`); `<HubModal` is rendered; `onCardClick={handleCardClick}` is threaded.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run HubPanel --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - HubPanel.test.tsx (extended) passes: source-inspection asserts `onRequestRemoteCap` prop declared, handleCardClick gates remote-without-cap via `remoteCapsCached` and `onRequestRemoteCap`, `<HubModal` rendered, `onCardClick={handleCardClick}` threaded.
    - `grep -c "onRequestRemoteCap" frontend/src/components/Hub/HubPanel.tsx` (filtered `grep -v '^\s*//'`) >= 2 (prop + call site).
    - The remote gate returns BEFORE setModalState (no direct modal open for uncapped remote): verify the early `return` exists in handleCardClick's remote branch.
    - Existing HubPanel tests (filter/search/group/attention) still pass.
    - `cd frontend && pnpm exec tsc --noEmit` — no errors in HubPanel.tsx.
  </acceptance_criteria>
  <done>HubPanel opens the modal on local card click, gates uncapped remote clicks to onRequestRemoteCap, auto-opens after cap acquisition, and renders HubModal.</done>
</task>

<task type="auto">
  <name>Task 2: App.tsx wiring — modal props + remote cap-acquired signal</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (HubPanel render ~line 1357; handleModalExchange ~line 1101; joinModalForSession state ~line 212; relayPort/terminalTheme/pluginConfig/remoteCapsCached)
    - .planning/phases/134-modal-interaction/134-RESEARCH.md (Pattern 5 cap gate, Open Question 1 onCapAcquired recommendation, Pitfall 7 props threading)
    - frontend/src/components/RemoteJoinCodeModal.tsx (the modal triggered by onRequestRemoteCap — reused as-is)
  </read_first>
  <action>
    Pass the new props to `<HubPanel>`: `relayPort={relayPort ?? undefined}`, `terminalTheme={terminalTheme}`, `pluginConfig={pluginConfig}`, `remoteCapsCached={remoteCapsCached}`, and `onRequestRemoteCap={(s) => setJoinModalForSession({ id: s.id, name: s.name, hostname: s.hostname })}`.

    Wire the cap-acquired signal. The existing `joinModalForSession` is shared between the file-browser path and the new Hub-modal path. To distinguish intent, add an optional `intent?: 'files' | 'hub-modal'` field to the `joinModalForSession` state object (default existing callers to `'files'`). When `onRequestRemoteCap` fires from HubPanel, set `intent: 'hub-modal'`.

    In `handleModalExchange` (after `setRemoteCapsCached` adds the id, before/instead of unconditionally calling `handleOpenFileBrowser`): branch on `pending.intent` — if `'hub-modal'`, call the registered HubPanel cap-acquired callback with `pending.id` (do NOT open the file browser); otherwise keep the existing `handleOpenFileBrowser(pending.id, pending.name)` behavior. Hold the HubPanel callback in a ref set via `onRegisterCapAcquired={(fn) => { capAcquiredRef.current = fn }}` passed to `<HubPanel>`.

    Ensure the existing file-browser join callers (lines ~1087, ~1391) still set `intent: 'files'` (or rely on the default). Do not regress the Phase 122 / Phase 130 remote file-browse flow.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit 2>&1 | tail -20 && cd frontend && pnpm test --run App.hub --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onRequestRemoteCap" frontend/src/App.tsx` >= 1 and `grep -c "onRegisterCapAcquired\|capAcquiredRef" frontend/src/App.tsx` >= 1.
    - `handleModalExchange` branches on `intent` (or equivalent) so `'hub-modal'` does NOT call `handleOpenFileBrowser`: `grep -n "intent" frontend/src/App.tsx` shows the discriminator in both the state type and the exchange branch.
    - `relayPort`, `terminalTheme`, `pluginConfig`, `remoteCapsCached` are all passed to `<HubPanel>`: `grep -A12 "<HubPanel" frontend/src/App.tsx` shows all four.
    - `cd frontend && pnpm exec tsc --noEmit` — clean (no errors).
    - Existing App.hub / remote file-browse tests still pass.
  </acceptance_criteria>
  <done>App.tsx supplies the modal's relayPort/theme/pluginConfig, routes uncapped remote clicks to RemoteJoinCodeModal with hub-modal intent, and opens the Hub modal after a successful cap exchange without regressing the file-browse path.</done>
</task>

<task type="auto">
  <name>Task 3: style.css — modal CSS, grow/shrink keyframes, reduced-motion guards</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css (Phase 133 hub-attn-pulse block ~lines 4926-4956 — the @media no-preference + reduced-motion template; the .new-session-overlay ~lines 731-750 for overlay/panel structure; Hub token block ~lines 4096-4194)
    - .planning/phases/134-modal-interaction/134-PATTERNS.md ("style.css" section — overlay, panel, keyframes, no-preference guard, reduced-motion fallback, input focus, send button rules — all pre-derived)
    - .planning/phases/134-modal-interaction/134-UI-SPEC.md (Modal Layout Anatomy, Animation Keyframes, CSS Class Inventory — full class list + values)
    - frontend/src/components/__tests__/style.hub.modal.test.ts (the RED test from 134-01 — make it green)
  </read_first>
  <action>
    Append the Phase 134 modal CSS block to style.css. All colors via `var(--hub-*)` tokens only — NO hardcoded hex (rgba() for the scrim and box-shadow is permitted).

    Rules to add (per 134-PATTERNS.md and UI-SPEC, verbatim values):
    - `.hub-modal-overlay`: position fixed; inset 0; z-index 200; flex centering; `background: rgba(0,0,0,0.6)`.
    - `.hub-modal`: position relative; `width: min(1100px, calc(100vw - 48px))`; `height: min(750px, calc(100vh - 64px))`; `background: var(--hub-surface-elevated)`; `border: 1px solid var(--hub-border)`; border-radius 12px; box-shadow rgba; flex column; overflow hidden.
    - `.hub-modal__header` (48px, flex, gap 8px, padding 0 16px, border-bottom var(--hub-border), flex-shrink 0); `.hub-modal__session-name` (14px/400, var(--hub-text-primary), ellipsis truncation); `.hub-modal__origin-text` (12px var(--hub-text-secondary)); `.hub-modal__close` (min 28x28, transparent, hover accent); `.hub-modal__attn-badge` (var(--hub-attn-badge-bg)/var(--hub-attn-badge-text), 11px).
    - `.hub-modal__body` (flex 1; min-height 0; display flex; flex-direction column; overflow hidden); `.hub-modal__body--interactive`; `.hub-modal__body--briefing` (padding 16px; gap 12px).
    - `.hub-modal__tail` (flex 1; min-height 0; overflow-y auto; `background: var(--hub-preview-bg)`; border var(--hub-border); border-radius 6px; padding 12px; monospace; 12px; line-height 1.5; `color: var(--hub-text-secondary)`; white-space pre-wrap; word-break break-all); `.hub-modal__tail--empty` / `.hub-modal__tail--loading` (centered var(--hub-text-muted)).
    - `.hub-modal__respond` (flex-shrink 0; column; gap 8px); `.hub-modal__respond-label` (11px/600 uppercase letter-spacing 0.08em var(--hub-text-muted)); `.hub-modal__respond-input` (width 100%; min-height 64px; max-height 160px; `background: var(--hub-surface)`; border var(--hub-border); border-radius 6px; `color: var(--hub-text-primary)`; 14px; padding 8px 12px; resize vertical; outline none); `.hub-modal__respond-input:focus` (`border-color: var(--hub-accent)`; outline none); `.hub-modal__respond-footer` (flex; justify-content flex-end; gap 8px); `.hub-modal__send-btn` (`background: var(--hub-accent)`; `color: var(--hub-bg)`; weight 600; 14px; padding 8px 16px; border-radius 4px; border none; cursor pointer) + `:disabled` (opacity 0.5; cursor not-allowed); `.hub-modal__close-btn` (transparent; var(--hub-text-muted); 14px); `.hub-modal__error-banner` (var(--hub-destructive); role alert styling).
    - Keyframes at ROOT scope: `@keyframes hub-modal-grow` (opacity 0→1, transform scale(0.05)→scale(1)); `@keyframes hub-modal-shrink` (reverse); `@keyframes hub-modal-overlay-in` (opacity 0→1); `@keyframes hub-modal-overlay-out` (opacity 1→0).
    - Inside `@media (prefers-reduced-motion: no-preference)`: `.hub-modal--entering { animation: hub-modal-grow 220ms cubic-bezier(0.25,0.46,0.45,0.94) forwards }`; `.hub-modal--exiting { animation: hub-modal-shrink 180ms cubic-bezier(0.55,0,1,0.45) forwards }`; `.hub-modal-overlay--entering { animation: hub-modal-overlay-in 220ms ease forwards }`; `.hub-modal-overlay--exiting { animation: hub-modal-overlay-out 180ms ease forwards }`.
    - `@media (prefers-reduced-motion: reduce)`: `.hub-modal { animation: none; transition: none; opacity: 1; transform: none }`; `.hub-modal-overlay { animation: none; transition: none; opacity: 1 }`.

    The `transform-origin` is NOT set in CSS (it is the runtime inline style from HubModal). Match the existing Phase 133 reduced-motion comment style.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run style.hub --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - style.hub.modal.test.ts (from 134-01) GREEN: overlay z-index 200, all four keyframes declared, `.hub-modal--entering` after the no-preference media index, reduced-motion `animation: none` on `.hub-modal`, `.hub-modal__send-btn` uses `var(--hub-accent)`, `.hub-modal__tail` uses `var(--hub-preview-bg)`.
    - The existing `style.hub.test.ts` (Phase 131-133 contracts) still passes (no regressions to prior Hub CSS).
    - No hardcoded hex in any `.hub-modal*` declaration block (rgba scrim/shadow permitted): the test's no-hex-in-panel-block assertion passes.
    - `@keyframes hub-modal-grow` and `@keyframes hub-modal-shrink` are at root scope (not nested inside the no-preference media block — only the `animation:` assignments are guarded).
  </acceptance_criteria>
  <done>All modal CSS exists with token-only colors, root-scope keyframes, no-preference-guarded animation assignments, and a reduced-motion fallback; the CSS contract test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HubPanel card click → modal open vs cap-request | A remote session click must NOT open a live terminal modal before the Phase 122 capability is acquired |
| App.tsx join-code exchange → cap cache → modal/file-browser dispatch | The cap-acquired signal must route to the correct surface (hub-modal vs files) and must not bypass the join-code gate |
| modal RelayClient WS (via TerminalPanel) → relay server → PTY | Reuses the audited normal-tab + Phase 122 remote relay path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-05-01 | Elevation / auth bypass | HubPanel handleCardClick remote branch | mitigate | For remote sessions, the modal opens ONLY if `remoteCapsCached.has(session.id)`; otherwise handleCardClick returns early after calling `onRequestRemoteCap`, which triggers the existing Phase 122 `RemoteJoinCodeModal` join-code exchange. No new remote-access path; the cap gate is enforced before any TerminalPanel mounts (MODAL-06). |
| T-134-05-02 | Spoofing / wrong-surface dispatch | App.tsx handleModalExchange intent branch | mitigate | The `intent` discriminator on `joinModalForSession` routes a successful exchange to the hub-modal callback OR the file browser, never both; default `'files'` preserves the audited Phase 122/130 file-browse behavior. |
| T-134-05-03 | Tampering | session fields passed to HubModal | accept | HubModal renders session text content (auto-escaped, covered by T-134-04-01). No new rendering here. |
| T-134-05-04 | DoS / resource leak | HubModal unmount on close | mitigate | `onClose={() => setModalState(null)}` UNMOUNTS HubModal (and TerminalPanel), closing the RelayClient WS — no accumulating connections across open/close cycles (134-RESEARCH.md Pitfall 3). |
| T-134-SC | Tampering | npm/pip installs | mitigate | No new packages (134-RESEARCH.md audit: none). No install task. |
</threat_model>

<verification>
Run `cd frontend && pnpm test --run` — full suite green, including all four 134-01 modal test files and the extended HubPanel test. Run `cd frontend && pnpm exec tsc --noEmit` — clean. The Phase 122/130 remote file-browse path must still work (App.hub + remote tests green). Manual-only checks (live grow/shrink animation, briefing round-trip, remote cap-gated modal) are recorded in 134-VALIDATION.md "Manual-Only Verifications" and validated via `/gsd:verify-work`.
</verification>

<success_criteria>
- HubPanel opens the modal on local card click; uncapped remote click routes to onRequestRemoteCap; cap acquisition auto-opens the modal.
- App.tsx supplies relayPort/theme/pluginConfig and the intent-discriminated cap-acquired dispatch without regressing file browse.
- All modal CSS present (overlay z-index 200, root-scope keyframes, no-preference-guarded animations, reduced-motion fallback, token-only colors).
- Full frontend suite green; tsc clean.
</success_criteria>

<output>
Create `.planning/phases/134-modal-interaction/134-05-SUMMARY.md` when done.
</output>
