---
phase: 134-modal-interaction
plan: 08
type: execute
wave: 7
depends_on: ["134-07"]
files_modified:
  - frontend/src/components/Hub/HubPanel.tsx
  - frontend/src/components/Hub/HubPanel.test.tsx
  - frontend/src/App.tsx
  - frontend/src/components/Hub/HubModal.tsx
  - frontend/src/components/Hub/HubBriefingModal.test.tsx
  - frontend/src/components/Hub/HubInteractiveModal.test.tsx
autonomous: true
requirements: [MODAL-06, MODAL-03, MODAL-04]
must_haves:
  truths:
    - "The Hub modal threads the isRemote discriminator down to the terminal/briefing so remote sessions route through the WS proxy seam added in Plan 07"
    - "Dismissing the join-code modal resets pendingModalSessionId/pendingSourceRectRef (no stranded pending state — WR-01)"
    - "An in-flight file-browse join intent is not silently overwritten by a hub-modal cap request (intent preserved — WR-02)"
    - "The interactive terminal modal uses the real per-session fontSize and a working onFontSizeChange instead of a hardcoded 14 (WR-04), and terminalTheme is required (no ({} as ITheme) cast — WR-03)"
    - "Behavioral tests (not ?raw source-string checks) cover: remote-without-cap card click is blocked, briefing Send open->sendInput->close ordering + timeout cleanup, and remote tail rendered from a mocked WS snapshot (WR-07)"
  artifacts:
    - path: "frontend/src/components/Hub/HubPanel.tsx"
      provides: "isRemote threaded into the HubModal render; WR-03 cast removed (terminalTheme required); WR-04 fontSize/onFontSizeChange threaded; WR-01 cancel reset; IN-01 relayPort>0 guard"
      contains: "isRemote"
    - path: "frontend/src/App.tsx"
      provides: "WR-02 guard against overwriting an in-flight joinModalForSession; WR-01 handleCapCancelled wired to the join modal onClose; real fontSizes/onFontSizeChange + terminalTheme passed to HubPanel"
      contains: "joinModalForSession"
    - path: "frontend/src/components/Hub/HubPanel.test.tsx"
      provides: "behavioral test: remote-without-cap click does NOT setModalState (spies onRequestRemoteCap); local click does"
      contains: "onRequestRemoteCap"
    - path: "frontend/src/components/Hub/HubBriefingModal.test.tsx"
      provides: "behavioral tests with a mocked RelayClient: send ordering, timeout cleanup, remote tail from snapshot"
      contains: "RelayClient"
  key_links:
    - from: "HubPanel modal render"
      to: "remote prop on the terminal/briefing modal"
      via: "isRemote discriminator threaded to the Plan 07 seam"
      pattern: "isRemote|remote"
    - from: "App.tsx join modal onClose"
      to: "handleCapCancelled (HubPanel reset)"
      via: "WR-01 cancel teardown"
      pattern: "handleCapCancelled|onCapCancelled"
    - from: "App.tsx onRequestRemoteCap"
      to: "in-flight joinModalForSession guard"
      via: "WR-02 intent preservation"
      pattern: "joinModalForSession"
---

<objective>
Close the remaining 134-REVIEW warnings that fall in scope for this expansion and add the
behavioral test coverage the new Validation Architecture mandates (WR-07). Thread the
isRemote discriminator from HubPanel into the modal so the Plan 07 remote seam is actually
exercised; fix WR-01 (stranded pendingModalSessionId on join-modal dismiss), WR-02
(file-browse join intent overwritten by a hub-modal cap request), WR-03 (unsafe
({} as ITheme) cast), WR-04 (hardcoded fontSize=14 ignoring per-session size/zoom), and
IN-01 (relayPort===0 guard). Add real behavioral tests for the remote routing gate, the
briefing Send ordering/timeout cleanup, and the remote tail snapshot rendering.

DEFER WR-05 (broad document-level Escape stopImmediatePropagation) and WR-06 (focus trap)
to Phase 135 (a11y) per CONTEXT — note the deferral in code comments; do NOT fix them here.
The colorblind-safe non-color-cue constraint remains release-blocking for any badge added.

Purpose: make the remote modal path fully wired and test-proven, and clear the
warning-level debt that the now-functional remote path makes more likely to bite.
Output: HubPanel/App wiring fixes + behavioral tests replacing the ?raw source-string checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-modal-interaction/134-REVIEW.md
@.planning/phases/134-modal-interaction/134-RESEARCH-remote-ws-proxy.md
@.planning/phases/134-modal-interaction/134-07-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase. -->

From frontend/src/components/Hub/HubPanel.tsx:
```ts
// handleCardClick (~340): const isRemote = !!session.hostname && session.hostname !== ''
//   remote-without-cap path sets pendingSourceRectRef + setPendingModalSessionId + onRequestRemoteCap, returns
// handleCapAcquired (~353): clears pendingModalSessionId/pendingSourceRectRef on success only
// render (~450): {modalState && relayPort !== undefined && (<HubModal ... fontSize={14} theme={terminalTheme ?? ({} as ITheme)} ... />)}
// HubPanelProps: relayPort?: number; terminalTheme?: ITheme; onRequestRemoteCap?; onRegisterCapAcquired?
```

From frontend/src/App.tsx:
```ts
const DEFAULT_FONT_SIZE = 14
const [fontSizes, setFontSizes] = useState<Record<string, number>>({})
const terminalTheme: ITheme = (xtermThemes...)[terminalThemeName]   // always non-null (App.tsx:271)
const [joinModalForSession, setJoinModalForSession] = useState<{ id; name; hostname; intent?: 'files' | 'hub-modal' } | null>(null)
const capAcquiredRef = useRef<((sessionId: string) => void) | null>(null)
const handleModalExchange = useCallback(...) // RegisterRemoteCap(pending.id, baseURL, cap); routes by pending.intent
// onRequestRemoteCap={(s) => setJoinModalForSession({ ...s, intent: 'hub-modal' })}   // App.tsx:1380 — currently unconditional (WR-02)
// onFontSizeChange handler exists: handleFontSizeChange(sessionId, delta) (App.tsx:854, used by tabs at 1549-1550)
// RemoteJoinCodeModal rendered at 1601 with onClose={() => setJoinModalForSession(null)} (WR-01: no HubPanel reset)
```

From Plan 07: TerminalPanel/HubInteractiveModal accept optional `remote?: boolean`;
HubBriefingModal accepts optional `remote?: boolean`. The HubModal shell renders one or the
other based on attention routing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread isRemote + fix WR-03/WR-04/IN-01 in the HubPanel modal render</name>
  <files>frontend/src/components/Hub/HubPanel.tsx, frontend/src/components/Hub/HubModal.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/components/Hub/HubPanel.tsx (HubPanelProps ~149-160, handleCardClick ~340, modal render ~450-460)
    - frontend/src/components/Hub/HubModal.tsx (where it renders HubInteractiveModal/HubBriefingModal — pass remote/fontSize through)
    - frontend/src/App.tsx (terminalTheme ~271, fontSizes ~141, handleFontSizeChange ~854, HubPanel render ~1377-1381)
    - .planning/phases/134-modal-interaction/134-REVIEW.md (WR-03, WR-04, IN-01, IN-02)
  </read_first>
  <action>
    Thread the remote discriminator end-to-end: compute isRemote at the modal render in HubPanel
    (same rule as handleCardClick: `!!session.hostname && session.hostname !== ''`), pass a `remote`
    prop into HubModal, and have HubModal forward `remote` to HubInteractiveModal (TerminalPanel) and
    HubBriefingModal (the Plan 07 seams). This is what actually routes remote sessions through the
    WS proxy.
    WR-03: make `terminalTheme` REQUIRED on HubPanelProps and drop `({} as ITheme)`; App already
    supplies a non-null terminalTheme. Update App's HubPanel render accordingly (it already passes
    terminalTheme).
    WR-04 + IN-02: replace the hardcoded `fontSize={14}` with the real per-session size — add
    `fontSizes`/`onFontSizeChange` props to HubPanelProps, pass `fontSizes[session.id] ?? DEFAULT_FONT_SIZE`
    and a working `onFontSizeChange` from App (reuse handleFontSizeChange(session.id, delta)) down through
    HubModal -> HubInteractiveModal. Import or reference DEFAULT_FONT_SIZE rather than a bare 14. Wire
    onFontSizeChange through HubModal -> HubInteractiveModal (remove the dead no-op default path noted in IN-02).
    IN-01: change the modal render guard to `relayPort !== undefined && relayPort > 0` (mirror the tab grid).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "({} as ITheme)" frontend/src/components/Hub/HubPanel.tsx` == 0 (cast removed)
    - terminalTheme is required on HubPanelProps (no `?` after terminalTheme in the props type)
    - `grep -c "fontSize={14}" frontend/src/components/Hub/HubPanel.tsx` == 0; the render uses `fontSizes[...] ?? DEFAULT_FONT_SIZE`
    - `grep -n "relayPort > 0" frontend/src/components/Hub/HubPanel.tsx` returns a match (IN-01 guard)
    - `grep -n "remote" frontend/src/components/Hub/HubPanel.tsx` and HubModal.tsx show the discriminator threaded to the modal bodies
    - onFontSizeChange is threaded from App through HubModal to HubInteractiveModal (no remaining `?? (() => {})` no-op default reachable in the modal path)
    - `pnpm exec tsc --noEmit` is clean
  </acceptance_criteria>
  <done>The modal threads isRemote to the Plan 07 seam, uses real fontSize/onFontSizeChange, requires terminalTheme (cast gone), and guards relayPort>0.</done>
</task>

<task type="auto">
  <name>Task 2: Fix WR-01 (cancel reset) + WR-02 (intent overwrite guard)</name>
  <files>frontend/src/components/Hub/HubPanel.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/components/Hub/HubPanel.tsx (handleCardClick ~340, handleCapAcquired ~353, onRegisterCapAcquired effect ~371)
    - frontend/src/App.tsx (onRequestRemoteCap ~1380, handleModalExchange ~1106-1133, RemoteJoinCodeModal onClose ~1601-1607, capAcquiredRef ~221)
    - .planning/phases/134-modal-interaction/134-REVIEW.md (WR-01, WR-02)
  </read_first>
  <action>
    WR-01: add a `handleCapCancelled` callback in HubPanel that resets pendingModalSessionId and
    pendingSourceRectRef, register it with App alongside handleCapAcquired (mirror the existing
    onRegisterCapAcquired ref pattern — e.g. onRegisterCapCancelled / capCancelledRef), and have App
    invoke it from the RemoteJoinCodeModal `onClose` (before/with setJoinModalForSession(null)) so a
    dismissed join modal does not strand pending state. Only fire the cancel reset for the hub-modal
    intent path (do not disturb file-browse).
    WR-02: guard App's `onRequestRemoteCap` so it does NOT overwrite an in-flight joinModalForSession
    belonging to a different intent/session — e.g. `if (joinModalForSession) return` (or key by session
    id) before `setJoinModalForSession({ ..., intent: 'hub-modal' })`, preserving an already-open
    file-browse (intent undefined/'files') request. Add a brief comment citing WR-02.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "handleCapCancelled\|CapCancelled\|onCapCancelled" frontend/src/components/Hub/HubPanel.tsx` returns matches (reset callback exists)
    - The cancel callback resets both pendingModalSessionId and pendingSourceRectRef
    - App's RemoteJoinCodeModal onClose invokes the cancel reset (not just setJoinModalForSession(null))
    - App's onRequestRemoteCap guards against an in-flight joinModalForSession: `grep -n "joinModalForSession" frontend/src/App.tsx` shows a guard before the setter on the hub-modal path
    - `pnpm exec tsc --noEmit` is clean
  </acceptance_criteria>
  <done>Dismissing the join modal clears pending state; a hub-modal cap request no longer clobbers an in-flight file-browse join intent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Behavioral tests (WR-07) — remote gate, briefing send/timeout, remote tail</name>
  <files>frontend/src/components/Hub/HubPanel.test.tsx, frontend/src/components/Hub/HubBriefingModal.test.tsx, frontend/src/components/Hub/HubInteractiveModal.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/HubPanel.test.tsx (the existing MODAL-06 ?raw block ~734-761 to replace/augment)
    - frontend/src/components/Hub/HubBriefingModal.test.tsx (the existing ?raw block to replace)
    - frontend/src/components/Hub/HubBriefingModal.tsx (handleSend ordering + remote tail from Plan 07)
    - frontend/src/lib/relayClient.ts (RelayClient surface to mock: constructor opts, onOpen/onOutput callbacks, sendInput, close)
    - .planning/phases/134-modal-interaction/134-REVIEW.md (WR-07 — the mandated behavioral assertions)
    - .planning/phases/134-modal-interaction/134-RESEARCH-remote-ws-proxy.md (Validation Architecture > Phase Requirements -> Test Map: FE-ROUTE-01, CR-03-01, TAIL-01)
  </read_first>
  <behavior>
    - FE-ROUTE-01: render HubPanel; a remote-without-cap card click does NOT call setModalState (spy onRequestRemoteCap is called instead); a local card click DOES open the modal
    - CR-03-01: mock RelayClient; briefing Send opens -> sendInput(text+'\n') -> close in that order; on a forced timeout (onOpen never fires) the client is closed and no sendInput occurs; after timeout a late onOpen does NOT call sendInput (settled guard)
    - TAIL-01: mock a proxied RelayClient that delivers a scrollback snapshot via onOutput; assert the remote briefing renders the stripped last-N lines (not "No recent output available") and never calls GetSessionTailLines
  </behavior>
  <action>
    Replace the ?raw source-string assertions for these components with real behavioral tests
    (Vitest + Testing Library; mock RelayClient via vi.mock on ../../lib/relayClient; xterm may be
    mocked to allow HubModal to mount in jsdom). Write FE-ROUTE-01 in HubPanel.test.tsx (spy
    onRequestRemoteCap, assert setModalState NOT called for remote-no-cap, called for local).
    Write CR-03-01 + TAIL-01 in HubBriefingModal.test.tsx (mock RelayClient: assert send ordering,
    timeout cleanup, no post-abandon send, and remote tail rendered from a mocked onOutput snapshot;
    assert GetSessionTailLines is NOT called when remote). Augment HubInteractiveModal.test.tsx with a
    behavioral mount asserting the remote prop selects the proxy seam (e.g. the mocked RelayClient is
    constructed with the remote opt). Keep any still-valuable existing assertions but the new tests
    must exercise runtime behavior, not source strings.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run HubPanel HubBriefingModal HubInteractiveModal</automated>
  </verify>
  <acceptance_criteria>
    - HubPanel.test.tsx has a test that spies onRequestRemoteCap and asserts setModalState is NOT invoked for a remote-without-cap click, and IS for a local click (not a ?raw indexOf check)
    - HubBriefingModal.test.tsx mocks RelayClient and asserts: open->sendInput->close ordering; timeout path closes the client with zero sendInput calls; a late onOpen after timeout does not call sendInput
    - HubBriefingModal.test.tsx asserts remote tail renders from a mocked onOutput snapshot AND GetSessionTailLines is not called in the remote case
    - `grep -c "?raw" frontend/src/components/Hub/HubBriefingModal.test.tsx` shows the briefing behavioral tests no longer rely solely on ?raw (at least the send/tail tests use a mocked RelayClient)
    - `pnpm test --run HubPanel HubBriefingModal HubInteractiveModal` passes
  </acceptance_criteria>
  <done>Behavioral tests prove the remote routing gate, briefing send ordering/timeout cleanup, and remote tail snapshot rendering — closing WR-07 for the touched components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| join-code modal state machine | Stranded/overwritten intent could route a cap to the wrong consumer (WR-01/WR-02). |
| remote discriminator | A mis-threaded isRemote would route a remote session to the local relay (re-introducing CR-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-08-01 | Tampering | join intent overwrite (WR-02) | mitigate | Guard onRequestRemoteCap against an in-flight joinModalForSession so a file-browse cap request is not redirected to the hub-modal consumer |
| T-134-08-02 | Repudiation/State | stranded pendingModalSessionId (WR-01) | mitigate | handleCapCancelled resets pending state on join-modal dismiss |
| T-134-08-03 | Spoofing | remote session attaches local relay (CR-01 regression) | mitigate | isRemote threaded to the Plan 07 seam; FE-ROUTE-01 behavioral test guards the gate |
| T-134-08-04 | Information Disclosure | read-only/remote distinction lost to SR users (IN-04) | accept | aria-label origin + colorblind-safe non-color badge belong to the Phase 135 a11y pass; documented deferral |
| T-134-08-DEFER | n/a | WR-05 broad Escape suppression, WR-06 focus trap | transfer | Explicitly deferred to Phase 135 (a11y) per CONTEXT; comment the deferral, do not fix here |
| T-134-08-SC | Tampering | npm installs | accept | No new dependencies; tests use existing Vitest + Testing Library + vi.mock |
</threat_model>

<verification>
- `cd frontend && pnpm test --run` green (full frontend suite); `pnpm exec tsc --noEmit` clean
- WR-01/WR-02/WR-03/WR-04 fixed; IN-01 guard added; isRemote threaded to the proxy seam
- Behavioral tests (FE-ROUTE-01, CR-03-01, TAIL-01) pass and exercise runtime behavior, not source strings
- WR-05 and WR-06 explicitly deferred to Phase 135 with in-code comments (not fixed here)
- Manual-only (phase gate): live two-peer remote terminal round-trip + remote briefing prompt render + remote font zoom
</verification>

<success_criteria>
- isRemote threaded HubPanel -> HubModal -> terminal/briefing seams
- WR-01, WR-02, WR-03, WR-04, IN-01 fixed; WR-05/WR-06 deferred with comment
- Behavioral tests replace ?raw checks for the remote gate, briefing send/timeout, and tail
- tsc clean; full frontend suite green
</success_criteria>

<output>
Create `.planning/phases/134-modal-interaction/134-08-SUMMARY.md` when done.
</output>
