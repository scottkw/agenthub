---
phase: 134-modal-interaction
plan: 02
type: execute
wave: 2
depends_on: ["134-01"]
files_modified:
  - frontend/src/components/Hub/SessionCard.tsx
  - frontend/src/components/Hub/SessionCardGrid.tsx
  - frontend/src/components/Hub/SessionCard.test.tsx
autonomous: true
requirements: [MODAL-01]
must_haves:
  truths:
    - "Clicking the card body fires onCardClick with the session and the card's bounding rect"
    - "Clicking the Open button or menu button does NOT open the modal (stopPropagation), preserving the Phase 131 re-attach Open button"
    - "Enter/Space on a focused card fires onCardClick (A11Y-02 partial)"
    - "Dragging a card does not trigger the modal"
  artifacts:
    - path: "frontend/src/components/Hub/SessionCard.tsx"
      provides: "onCardClick prop + article onClick/onKeyDown + stopPropagation on Open and menu buttons"
      contains: "onCardClick"
    - path: "frontend/src/components/Hub/SessionCardGrid.tsx"
      provides: "onCardClick threaded through both render paths to SessionCard"
      contains: "onCardClick"
  key_links:
    - from: "SessionCardGrid.tsx"
      to: "SessionCard"
      via: "onCardClick prop passed in both named-group and workDir-group render paths"
      pattern: "onCardClick=\\{onCardClick\\}"
    - from: "SessionCard.tsx article onClick"
      to: "onCardClick callback"
      via: "getBoundingClientRect passed to handler"
      pattern: "getBoundingClientRect"
---

<objective>
Wire the card-click → modal trigger through SessionCard and SessionCardGrid, and fix the two missing `e.stopPropagation()` calls (Open button line 374, menu button line 254) so the existing Phase 131 re-attach Open button COEXISTS with the new modal interaction rather than double-firing.

Purpose: This is the top-priority coexistence guarantee from 134-RESEARCH.md and the locked memory `project_phase134_reattach_button`. Without stopPropagation on BOTH buttons, clicking "Open" or the menu would also open the modal.
Output: SessionCard gains `onCardClick`, a body onClick + keyboard handler, and stopPropagation guards; SessionCardGrid threads the prop. No modal component is mounted here — this plan only emits the click contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-modal-interaction/134-PATTERNS.md
@.planning/phases/134-modal-interaction/134-UI-SPEC.md

<interfaces>
<!-- The click contract this plan emits — consumed by HubPanel in 134-05. -->
onCardClick?: (session: SessionInfo, rect: DOMRect) => void

<!-- Confirmed source facts (verified by planner): -->
<!-- SessionCard.tsx line 254: menu button onClick={() => setMenuOpen((p) => !p)} — MISSING stopPropagation -->
<!-- SessionCard.tsx line 374: open button onClick={() => onOpenSession(id, name, cli)} — MISSING stopPropagation -->
<!-- SessionCard.tsx line 93: onOpenSession?: (sessionId, name, cli) => void  (existing prop) -->
<!-- The article element already has tabIndex={0} (per UI-SPEC + RESEARCH) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionCard onCardClick prop + body click/keyboard + stopPropagation fixes</name>
  <files>frontend/src/components/Hub/SessionCard.tsx, frontend/src/components/Hub/SessionCard.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/SessionCard.tsx (the file being modified — read the SessionCardProps interface, the article element, the menu button ~line 254, the Open button ~line 374)
    - frontend/src/components/Hub/SessionCard.test.tsx (existing test file to extend)
    - .planning/phases/134-modal-interaction/134-PATTERNS.md ("SessionCard.tsx" section — exact article onClick/onKeyDown body and both stopPropagation edits)
    - .planning/phases/134-modal-interaction/134-UI-SPEC.md ("Card Click vs Open Button Disambiguation")
  </read_first>
  <behavior>
    - Source: SessionCardProps declares `onCardClick?: (session: SessionInfo, rect: DOMRect) => void`.
    - Source: the article `onClick` handler calls `onCardClick?.(session, (e.currentTarget as HTMLElement).getBoundingClientRect())`.
    - Source: the article onClick guards via `.closest('.hub-card__open')`, `.closest('.hub-card__menu-btn')`, `.closest('.hub-card__menu')`, `.closest('.InlineSessionName input')`, and `isDragging` — returning early for each.
    - Source: the article `onKeyDown` calls onCardClick on `Enter` or `' '` (Space) after `e.preventDefault()`.
    - Source: the Open button onClick is `(e) => { e.stopPropagation(); onOpenSession(id, name, cli) }`.
    - Source: the menu button onClick is `(e) => { e.stopPropagation(); setMenuOpen((p) => !p) }`.
  </behavior>
  <action>
    Add `onCardClick?: (session: SessionInfo, rect: DOMRect) => void` to `SessionCardProps` and destructure it in the component signature.

    Modify the `<article className="hub-card …">` element: add an `onClick` handler that (1) reads `e.target as HTMLElement`, (2) returns early if `target.closest()` matches any of `.hub-card__open`, `.hub-card__menu-btn`, `.hub-card__menu`, `.InlineSessionName input`, (3) returns early if `isDragging`, (4) otherwise calls `onCardClick?.(session, (e.currentTarget as HTMLElement).getBoundingClientRect())`. Add an `onKeyDown` handler firing `onCardClick` on `Enter`/Space with `e.preventDefault()`. Preserve the existing `draggable`, `onDragStart`, `onDragEnd`, `aria-label`, `tabIndex={0}`, and className composition exactly.

    Fix the Open button (~line 374): change `onClick={() => onOpenSession(id, name, cli)}` to `onClick={(e) => { e.stopPropagation(); onOpenSession(id, name, cli) }}`. Fix the menu button (~line 254): change `onClick={() => setMenuOpen((p) => !p)}` to `onClick={(e) => { e.stopPropagation(); setMenuOpen((p) => !p) }}`. Do NOT change the group-assign menu item onClicks (lines ~271/285) — they live inside `.hub-card__menu` which the body guard already excludes.

    Extend SessionCard.test.tsx with source-inspection assertions (`?raw` import of SessionCard.tsx) confirming: `onCardClick` is declared; the article onClick contains `getBoundingClientRect`; the body guard contains `closest('.hub-card__open')`; the Open button onClick contains `e.stopPropagation()`; the menu button onClick contains `e.stopPropagation()`; the onKeyDown contains `Enter`.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run SessionCard --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - SessionCard.test.tsx (source-inspection) passes: asserts `onCardClick` declared, `getBoundingClientRect` present in article onClick, `closest('.hub-card__open')` guard present, `e.stopPropagation()` present in BOTH the Open and menu button handlers, `Enter` present in onKeyDown.
    - `grep -c "e.stopPropagation()" frontend/src/components/Hub/SessionCard.tsx` (filtered: `grep -v '^\s*//'`) is >= 2.
    - `cd frontend && pnpm exec tsc --noEmit` reports no new type errors in SessionCard.tsx.
    - Pre-existing SessionCard behavior (badge, status, dim, attention, InlineSessionName) is unchanged — those test assertions still pass.
  </acceptance_criteria>
  <done>SessionCard accepts onCardClick, fires it on body click + Enter/Space, and both the Open and menu buttons stopPropagation so they never open the modal.</done>
</task>

<task type="auto">
  <name>Task 2: SessionCardGrid threads onCardClick through both render paths</name>
  <files>frontend/src/components/Hub/SessionCardGrid.tsx</files>
  <read_first>
    - frontend/src/components/Hub/SessionCardGrid.tsx (the file being modified — read SessionCardGridProps and BOTH SessionCard render sites: named-group path ~line 239 and workDir-group path ~line 283)
    - .planning/phases/134-modal-interaction/134-PATTERNS.md ("SessionCardGrid.tsx" section — the exact prop addition and threading edit)
  </read_first>
  <action>
    Add `onCardClick?: (session: SessionInfo, rect: DOMRect) => void` to `SessionCardGridProps`, destructure it in the component, and pass `onCardClick={onCardClick}` to EVERY `<SessionCard>` render site (both the named-group render path and the workDir-group render path). Do not alter grouping, sorting, drag, or attention logic.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run SessionCardGrid --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onCardClick={onCardClick}" frontend/src/components/Hub/SessionCardGrid.tsx` is >= 2 (both render paths).
    - SessionCardGridProps source contains `onCardClick?: (session: SessionInfo, rect: DOMRect) => void`.
    - `cd frontend && pnpm exec tsc --noEmit` reports no new type errors in SessionCardGrid.tsx.
    - Existing SessionCardGrid tests still pass.
  </acceptance_criteria>
  <done>SessionCardGrid forwards onCardClick to all SessionCard instances in both grouping paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click/keyboard → card handler | DOM events cross into app logic; event target must be disambiguated so a privileged action (Open existing tab) is not conflated with the new modal-open action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-02-01 | Elevation/Confused-deputy | SessionCard article onClick | mitigate | Body onClick guards via `.closest()` on Open/menu/rename children AND both child buttons call `e.stopPropagation()` — a click on "Open" performs ONLY the tab-open action, never silently also opening a modal (defense-in-depth, two independent guards). |
| T-134-02-02 | Tampering | session.name / session fields rendered in card | accept | React text content auto-escapes; no `dangerouslySetInnerHTML`. No change to rendering here. |
| T-134-SC | Tampering | npm/pip installs | mitigate | No new packages (134-RESEARCH.md audit: none). No install task. |
</threat_model>

<verification>
Run `cd frontend && pnpm test --run SessionCard SessionCardGrid --reporter=verbose` — both green. Run `cd frontend && pnpm exec tsc --noEmit` — no new errors. The full suite (`pnpm test --run`) must remain green; the new modal component tests from 134-01 stay red (their targets are implemented in 134-03/04).
</verification>

<success_criteria>
- onCardClick contract emitted from SessionCard and threaded by SessionCardGrid.
- Open button and menu button both stopPropagation (coexistence guarantee — re-attach Open button not regressed).
- Enter/Space opens the modal path (A11Y-02 partial).
- No type errors; existing card/grid tests green.
</success_criteria>

<output>
Create `.planning/phases/134-modal-interaction/134-02-SUMMARY.md` when done.
</output>
