---
phase: 150-shell-sharing-warning-toggle
plan: 03
type: execute
wave: 2
depends_on: [150-01]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/Hub/HubPanel.tsx
  - frontend/src/components/Hub/SessionShareModal.tsx
  - frontend/src/components/__tests__/SessionShareModal.test.tsx
  - frontend/src/components/__tests__/App.shellWebShare.test.tsx
  - TESTING.md
autonomous: true
requirements: [SET-01]

must_haves:
  truths:
    - "D-02: D-04: The StatusBar share path (App.tsx handleToggleWeb) applies the gate (shell AND warningEnabled AND !warned) per the behavior matrix"
    - "D-09: D-10: The Hub Share modal ON-path reuses ShellWebShareBanner to show the shell warning for shell sessions when enabled and not yet acknowledged (parity)"
    - "D-10: Both surfaces share one shellWebShareWarned authority (App.tsx state), with race-mitigation — no double banner"
    - "D-03: After OFF->ON re-arm, App.tsx re-syncs shellWebShareWarned so the warning fires again"
    - "D-02: D-04: Disabling the warning (warningEnabled=false) suppresses the banner on the Share modal even for shell sessions, in both surfaces"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "shellWebShareWarningEnabled state, mount hydration, handleToggleWeb gate, re-arm re-sync, prop threading"
      contains: "shellWebShareWarningEnabled"
    - path: "frontend/src/components/Hub/SessionShareModal.tsx"
      provides: "cli on ShareSession, SHELL_CLIS gate in handleShareToggle, ShellWebShareBanner render"
      contains: "SHELL_CLIS"
    - path: "frontend/src/components/Hub/HubPanel.tsx"
      provides: "thread shell-warn props from App.tsx through to SessionShareModal"
      contains: "shellWebShareWarningEnabled"
    - path: "TESTING.md"
      provides: "Suite Manifest + traceability + manual M-NN entries for SET-01"
      contains: "SET-01"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "GetShellWebShareWarningEnabled / GetShellWebShareWarned"
      via: "mount hydration + re-arm re-sync"
      pattern: "GetShellWebShareWarningEnabled"
    - from: "frontend/src/components/Hub/SessionShareModal.tsx"
      to: "ShellWebShareBanner"
      via: "interception render in modal body"
      pattern: "ShellWebShareBanner"
    - from: "frontend/src/components/Hub/HubPanel.tsx"
      to: "SessionShareModal shell-warn props"
      via: "prop forwarding from App.tsx"
      pattern: "shellWebShareWarned"
---

<objective>
Wire the shell web-share warning into BOTH share surfaces using a single shared authority
(App.tsx `shellWebShareWarned`). Add the `warningEnabled` gate to the existing StatusBar path
(App.tsx handleToggleWeb), and add interception to the Hub Share modal ON-path (D-09) which
currently bypasses the warning entirely. Re-sync App.tsx state after a re-arm (D-03, pitfall 2).
Register all new/changed tests in TESTING.md per the standing convention.

Purpose: Closes the release-blocking cross-surface parity gap — the warning must fire on the
Hub Share modal (the primary v4.0 share surface), not just the legacy StatusBar toggle.

Output: Both share surfaces respect the same `warningEnabled && !warned` gate via shared state,
with vitest coverage of shell/non-shell/disabled interception and TESTING.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-shell-sharing-warning-toggle/150-CONTEXT.md
@.planning/phases/150-shell-sharing-warning-toggle/150-RESEARCH.md
@.planning/phases/150-shell-sharing-warning-toggle/150-PATTERNS.md

<interfaces>
<!-- Plan 01 (Wave 1) provides these Wails bindings. -->
From frontend/src/wailsjs/go/main/App.d.ts:
  export function GetShellWebShareWarningEnabled(): Promise<boolean>   // default ON
  export function SetShellWebShareWarningEnabled(v: boolean): Promise<void>
  export function GetShellWebShareWarned(): Promise<boolean>           // re-synced after re-arm

<!-- Exact analogs to mirror. -->
From frontend/src/App.tsx:
  - SHELL_CLIS = new Set(['shell','bash','zsh','pwsh','powershell'])  (line 89)
  - shellWebShareWarned state (line 138); mount hydration (lines 536-541)
  - handleToggleWeb gate (line 864): `if (tab && SHELL_CLIS.has(tab.cli) && !shellWebShareWarned)`
  - handleShellWebShareConfirm / handleShellWebShareCancel (lines 883-910) — race-mitigation: set local
    warned synchronously BEFORE awaiting the persist RPC; roll back on error
  - SettingsTab render call site (line 1549); HubPanel render call site (line 1427)

From frontend/src/components/Hub/HubPanel.tsx:
  - HubPanelProps (line 142); shareModalSession: SessionInfo | null (line 262); SessionInfo has cli
  - SessionShareModal render (lines 515-522, currently passes session/webServerMode/webServerRunning/onClose)

From frontend/src/components/Hub/SessionShareModal.tsx:
  - ShareSession interface (lines 14-20) — NO cli field today
  - handleShareToggle (lines 186-199) — calls ToggleWebServing directly, NO warning

From frontend/src/components/ShellWebShareBanner.tsx (full file) — reuse as-is (D-10).

TESTING.md structure: §2 Suite Manifest, §4 Traceability Map (repo-relative .ts/.tsx paths only,
test names in Notes column), §5 Manual Checklist (M-NN). Standing convention §6.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add warningEnabled state + gate + re-arm re-sync to App.tsx and thread props to HubPanel/SettingsTab</name>
  <read_first>
    - frontend/src/App.tsx (lines 85-89 SHELL_CLIS; 128-141 state; 530-545 mount hydration; 853-910 handleToggleWeb + confirm/cancel; 1427-1445 HubPanel render; 1549-1572 SettingsTab render)
    - .planning/phases/150-shell-sharing-warning-toggle/150-PATTERNS.md (App.tsx section)
    - frontend/src/wailsjs/go/main/App.d.ts (Plan 01 bindings)
  </read_first>
  <behavior>
    - On mount, GetShellWebShareWarningEnabled hydrates shellWebShareWarningEnabled (default true on error)
    - handleToggleWeb on a shell session does NOT intercept when shellWebShareWarningEnabled is false (no banner; toggle proceeds)
    - handleToggleWeb on a shell session DOES intercept when enabled && !warned (existing behavior preserved)
    - When onShellWarnEnabledChange(true) fires from SettingsTab, App.tsx re-fetches GetShellWebShareWarned and updates state (re-arm re-sync, D-03)
    - App.tsx passes shellWebShareWarned, shellWebShareWarningEnabled, and the confirm/cancel callbacks down to HubPanel
  </behavior>
  <action>
    In `frontend/src/App.tsx`:
    (1) Add state `const [shellWebShareWarningEnabled, setShellWebShareWarningEnabled] = useState(true)`
    after pendingShellWebToggle (~line 141).
    (2) In the mount effect alongside GetShellWebShareWarned hydration (~lines 536-541), add a
    GetShellWebShareWarningEnabled().then(setShellWebShareWarningEnabled).catch(default true) block.
    (3) Update the handleToggleWeb gate (line 864) to add the `&& shellWebShareWarningEnabled` AND-clause:
    `if (tab && SHELL_CLIS.has(tab.cli) && shellWebShareWarningEnabled && !shellWebShareWarned)`. Add
    `shellWebShareWarningEnabled` to that useCallback dependency array.
    (4) Pass `onShellWarnEnabledChange` to the SettingsTab render (line 1549): a callback that
    setShellWebShareWarningEnabled(enabled) and, when enabled is true, re-fetches GetShellWebShareWarned()
    and updates shellWebShareWarned (pitfall 2 re-arm re-sync). This is the ONLY new SettingsTab prop —
    it is optional on the SettingsTab side (Plan 02), so this wiring completes the contract.
    (5) Thread `shellWebShareWarned`, `shellWebShareWarningEnabled`, `handleShellWebShareConfirm`,
    `handleShellWebShareCancel` into the HubPanel render call (line 1427) as new props.
    Do NOT fork the warned state — App.tsx remains the single authority for both surfaces (pitfall 4).
  </action>
  <acceptance_criteria>
    - App.tsx contains `shellWebShareWarningEnabled` state and a `GetShellWebShareWarningEnabled` hydration call
    - handleToggleWeb gate contains `shellWebShareWarningEnabled &&` before `!shellWebShareWarned`
    - App.tsx SettingsTab render passes `onShellWarnEnabledChange=` and the callback re-fetches GetShellWebShareWarned when enabled
    - App.tsx HubPanel render passes `shellWebShareWarned`, `shellWebShareWarningEnabled`, confirm and cancel callbacks
    - `cd /Users/ken/dev/agenthub/frontend && pnpm tsc --noEmit` exits 0 (will fail until HubPanel accepts the new props — Task 2)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && grep -q 'shellWebShareWarningEnabled &&' src/App.tsx && grep -q 'onShellWarnEnabledChange' src/App.tsx</automated>
  </verify>
  <done>App.tsx owns warningEnabled state, gates the StatusBar path, re-syncs warned after re-arm, and threads props to HubPanel + SettingsTab. (tsc passes after Task 2 wires HubPanel props.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Share-modal interception (HubPanel + SessionShareModal) and add vitest coverage</name>
  <read_first>
    - frontend/src/components/Hub/HubPanel.tsx (lines 142-180 HubPanelProps; 240-265 props destructure + shareModalSession; 510-523 SessionShareModal render)
    - frontend/src/components/Hub/SessionShareModal.tsx (lines 1-30 imports; 14-25 ShareSession + props; 186-199 handleShareToggle)
    - frontend/src/components/ShellWebShareBanner.tsx (full file)
    - frontend/src/components/__tests__/SessionShareModal.test.tsx (existing test setup/mocks)
    - frontend/src/components/__tests__/App.shellWebShare.test.tsx (existing StatusBar warning tests — extend for warningEnabled gate)
  </read_first>
  <behavior>
    - Share modal: shell session + warningEnabled + !warned -> banner shown, ToggleWebServing NOT called
    - Share modal: shell session but warningEnabled=false -> banner NOT shown; ToggleWebServing called
    - Share modal: non-shell (e.g. claude) session -> banner NOT shown; ToggleWebServing called immediately
    - Share modal: confirming the banner invokes the onShellWebShareConfirm callback (App.tsx race-mitigation) then enables share
    - Share modal: cancelling the banner leaves share OFF and calls onShellWebShareCancel
    - App.tsx StatusBar: warningEnabled=false -> no banner on shell toggle (extend existing App.shellWebShare test)
  </behavior>
  <action>
    (1) `frontend/src/components/Hub/HubPanel.tsx`: add `shellWebShareWarned`, `shellWebShareWarningEnabled`,
    `onShellWebShareConfirm`, `onShellWebShareCancel` to HubPanelProps (optional), destructure them, and
    forward them plus `session.cli` (already on shareModalSession: SessionInfo) into the SessionShareModal
    render (lines 515-522). shareModalSession is SessionInfo which has cli — pass the whole object.
    (2) `frontend/src/components/Hub/SessionShareModal.tsx`: add `cli: string` to the ShareSession interface;
    add the new props (shellWebShareWarned?, shellWebShareWarningEnabled?, onShellWebShareConfirm?: () =>
    Promise<void>, onShellWebShareCancel?) to SessionShareModalProps; add local `pendingShellShare` state;
    add a module-level `SHELL_CLIS` Set matching App.tsx:89; in handleShareToggle, before ToggleWebServing,
    intercept: `if (next && SHELL_CLIS.has(session.cli) && shellWebShareWarningEnabled && !shellWebShareWarned)
    { setPendingShellShare(true); return }`; render `<ShellWebShareBanner>` inline in the modal body when
    pendingShellShare is true (onConfirm awaits onShellWebShareConfirm then setShareEnabled(true); onCancel
    clears pendingShellShare + calls onShellWebShareCancel). Import ShellWebShareBanner.
    (3) Extend `frontend/src/components/__tests__/SessionShareModal.test.tsx` with the shell/non-shell/disabled
    interception cases from the behavior block. (4) Extend
    `frontend/src/components/__tests__/App.shellWebShare.test.tsx` with the warningEnabled=false StatusBar
    suppression case.
  </action>
  <acceptance_criteria>
    - SessionShareModal.tsx ShareSession interface contains `cli`
    - SessionShareModal.tsx contains a `SHELL_CLIS` Set and references `ShellWebShareBanner`
    - SessionShareModal.tsx handleShareToggle gate contains `shellWebShareWarningEnabled && !shellWebShareWarned`
    - HubPanel.tsx forwards `shellWebShareWarningEnabled` and the confirm/cancel callbacks to SessionShareModal
    - `cd /Users/ken/dev/agenthub/frontend && pnpm tsc --noEmit` exits 0 (full App.tsx + HubPanel + modal type-check passes)
    - `cd /Users/ken/dev/agenthub/frontend && pnpm vitest run SessionShareModal App.shellWebShare` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm tsc --noEmit && pnpm vitest run SessionShareModal App.shellWebShare</automated>
  </verify>
  <done>Share modal intercepts shell ON-toggles via shared App.tsx authority; non-shell and disabled cases proceed; banner reused (D-10); tsc + vitest green on both surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Register new/changed tests in TESTING.md per the standing convention</name>
  <read_first>
    - TESTING.md (§2 Suite Manifest, §4 Traceability Map, §5 Manual Checklist categories, §6 Standing Convention)
    - .planning/phases/150-shell-sharing-warning-toggle/150-VALIDATION.md (Manual-Only Verifications + Wave 0)
  </read_first>
  <action>
    Update `TESTING.md` for the SET-01 test files added this phase:
    (1) §2 Suite Manifest — add the new Go test files (engine_shell_warn_test.go, api_shell_warn_test.go)
    to the daemon suite group and the new vitest files (SettingsTab.shell-warn-toggle.test.tsx) to the
    frontend suite group; note the extended SessionShareModal.test.tsx / App.shellWebShare.test.tsx.
    (2) §4 Traceability Map — add a row per NEW test file mapping SET-01 to the repo-relative path
    (path column = file path ONLY ending in .go/.tsx; test names go in the Notes column).
    (3) §5 Manual Checklist — add an M-NN entry (next free number) for the live PTY web-share verification
    that cannot be automated: web-share a real shell session via the Hub Share modal AND via the StatusBar
    toggle; confirm the banner fires when enabled+unacked and is absent when disabled; plus a restart-persistence
    manual check (toggle OFF, restart app, confirm warning suppressed). Source these from 150-VALIDATION.md
    Manual-Only Verifications.
    (4) Run `bash tests/check-traceability-paths.sh` and confirm it exits 0.
  </action>
  <acceptance_criteria>
    - TESTING.md §4 contains a row for `internal/daemon/engine_shell_warn_test.go` and one for `frontend/src/components/__tests__/SettingsTab.shell-warn-toggle.test.tsx`, both tagged SET-01, path column = file path only
    - TESTING.md §5 contains a new M-NN entry for the live shell web-share banner verification on both surfaces
    - `bash /Users/ken/dev/agenthub/tests/check-traceability-paths.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && bash tests/check-traceability-paths.sh && grep -q 'engine_shell_warn_test.go' TESTING.md && grep -q 'SettingsTab.shell-warn-toggle.test.tsx' TESTING.md</automated>
  </verify>
  <done>All new SET-01 test files registered in TESTING.md §2/§4; live web-share + restart-persistence manual checks added to §5; traceability path script exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user share-toggle click -> ToggleWebServing | crossing this for a shell session exposes a live PTY over the web |
| App.tsx warned state -> two share surfaces | the single warned authority must not be forked per-surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-08 | Elevation of Privilege | Share modal ON-path (previously unguarded) | mitigate | D-09: add the warningEnabled && !warned gate to handleShareToggle so the primary v4.0 share surface no longer bypasses the shell warning. Covered by SessionShareModal shell-interception vitest. |
| T-150-09 | Tampering | re-arm not reflected in frontend | mitigate | After OFF->ON, App.tsx re-fetches GetShellWebShareWarned (pitfall 2) so a re-armed warning actually fires; re-arm cannot be silently suppressed by stale React state. Covered by re-arm behavior. |
| T-150-10 | Info-disclosure | double-banner / forked warned state | mitigate | Single shellWebShareWarned authority in App.tsx threaded via props/callbacks to both surfaces (pitfall 4); one confirm suppresses the other. |
| T-150-11 | Spoofing | non-shell session wrongly treated as shell | mitigate | SHELL_CLIS membership on session.cli is the canonical shell test (matches engine isShellSession); cli is now threaded onto ShareSession (pitfall 3). Covered by non-shell vitest case. |
| T-150-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit: none). No install tasks. |
</threat_model>

<verification>
- `pnpm tsc --noEmit` exits 0 (App.tsx + HubPanel + SessionShareModal type-check together)
- `pnpm vitest run SessionShareModal App.shellWebShare` exits 0
- `bash tests/check-traceability-paths.sh` exits 0
- Both share surfaces gated by the same warningEnabled && !warned shared state
</verification>

<success_criteria>
- StatusBar path respects warningEnabled (Success Criterion 2/4)
- Hub Share modal ON-path fires the warning for shells (D-09, Success Criterion 4)
- Re-arm re-syncs frontend warned state (D-03, Success Criterion 2)
- Single shared warned authority — no double banner
- TESTING.md updated per standing convention; traceability script green
</success_criteria>

<output>
Create `.planning/phases/150-shell-sharing-warning-toggle/150-03-SUMMARY.md` when done.
</output>
