---
phase: 126-tui-write-parity-editor-shell-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/files_client.go
  - internal/tui/remote_files_client.go
  - internal/tui/remote_files_client_test.go
autonomous: true
requirements: [TUIW-01]
must_haves:
  truths:
    - "FilesClient interface declares exactly 8 methods (4 read + 4 write), no UploadFile"
    - "*daemon.DaemonClient satisfies FilesClient at compile time"
    - "*tui.RemoteFilesClient satisfies FilesClient at compile time"
    - "RemoteFilesClient can WriteFile/DeleteFile/RenameFile/MkdirFile against a remote webserver over HTTPS+cap"
  artifacts:
    - path: "internal/tui/files_client.go"
      provides: "8-method FilesClient interface + daemon compile-time guard"
      contains: "var _ FilesClient = (*daemon.DaemonClient)(nil)"
    - path: "internal/tui/remote_files_client.go"
      provides: "WriteFile/DeleteFile/RenameFile/MkdirFile on RemoteFilesClient"
      contains: "func (c *RemoteFilesClient) WriteFile"
    - path: "internal/tui/remote_files_client_test.go"
      provides: "httptest.TLSServer round-trip tests for the 4 write methods"
      contains: "TestRemoteFilesClient_Write"
  key_links:
    - from: "internal/tui/files_client.go"
      to: "internal/daemon.DaemonClient"
      via: "compile-time interface assertion"
      pattern: "var _ FilesClient = \\(\\*daemon\\.DaemonClient\\)"
    - from: "internal/tui/remote_files_client.go (write methods)"
      to: "remote webserver /api/files endpoints"
      via: "c.filesURL(op, sid, rel) + http.Do"
      pattern: "c\\.filesURL"
---

<objective>
Extend the shared `FilesClient` interface from 4 read methods to 8 (adding `WriteFile`, `DeleteFile`, `RenameFile`, `MkdirFile`), and implement those 4 write methods on `RemoteFilesClient`. This is the contract foundation — every other Phase 126 plan dispatches write ops through this interface.

Purpose: Establishes the duck-typed parity contract so one pipeline drives local (`*daemon.DaemonClient`) AND remote (`*tui.RemoteFilesClient`) write transport. (TUIW-01)
Output: 8-method interface with compile-time guards for BOTH implementers; remote write methods with `httptest.TLSServer` round-trip coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-tui-write-parity-editor-shell-out/126-RESEARCH.md
@.planning/phases/126-tui-write-parity-editor-shell-out/126-PATTERNS.md

<interfaces>
<!-- The EXACT live DaemonClient write signatures (internal/daemon/client.go:513-661). -->
<!-- The interface MUST match these — response structs, NOT error-only. -->
<!-- The architecture doc's error-only sketch is STALE/WRONG. -->

Current FilesClient (internal/tui/files_client.go:23-28) — DO NOT CHANGE the 4 read methods:
```go
ListFiles(ctx context.Context, sessionID, relPath string) ([]files.FileEntry, bool, error)
StatFile(ctx context.Context, sessionID, relPath string) (files.FileEntry, error)
ReadFile(ctx context.Context, sessionID, relPath string) (data []byte, mime string, err error)
HeadFile(ctx context.Context, sessionID, relPath string) (size int64, mime string, mtime time.Time, err error)
```

Live DaemonClient write signatures to match VERBATIM (internal/daemon/client.go:513-661):
```go
WriteFile(ctx context.Context, sessionID, relPath string, data []byte) (files.FileWriteResponse, error)
DeleteFile(ctx context.Context, sessionID, relPath string) (files.FileOpResponse, error)
RenameFile(ctx context.Context, sessionID, oldRel, newRel string) (files.FileOpResponse, error)
MkdirFile(ctx context.Context, sessionID, relPath string) (files.FileOpResponse, error)
```

Existing remote guard to mirror (remote_files_client.go:40):
```go
var _ FilesClient = (*RemoteFilesClient)(nil)
```

Remote test constructor (remote_files_client.go:82): `NewRemoteFilesClientForTest(baseURL, capToken string, httpClient *http.Client) *RemoteFilesClient`
Remote URL helper (remote_files_client.go:89): `c.filesURL(op, sessionID, relPath string) string`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend FilesClient to 8 methods with compile-time guards for both implementers</name>
  <read_first>
    - internal/tui/files_client.go (file being modified — current 4-method interface, lines 1-28)
    - internal/tui/remote_files_client.go:40 (analog — the existing `var _ FilesClient = (*RemoteFilesClient)(nil)` guard shape)
    - internal/daemon/client.go:513-661 (source of the exact write signatures)
    - 126-PATTERNS.md "FilesClient 8-method contract" section (verbatim contract)
  </read_first>
  <action>
    In internal/tui/files_client.go, add the 4 write methods to the FilesClient interface AFTER the 4 read methods, copying the signatures verbatim: `WriteFile(ctx, sessionID, relPath string, data []byte) (files.FileWriteResponse, error)`, `DeleteFile(ctx, sessionID, relPath string) (files.FileOpResponse, error)`, `RenameFile(ctx, sessionID, oldRel, newRel string) (files.FileOpResponse, error)`, `MkdirFile(ctx, sessionID, relPath string) (files.FileOpResponse, error)`. Do NOT add `UploadFile` (descoped per TUIW-06). Return types MUST be the response structs `files.FileWriteResponse` / `files.FileOpResponse`, NOT error-only — the architecture doc's error-only sketch is stale; match the live DaemonClient. Add an import for `github.com/scottkw/agenthub/internal/daemon` and a new compile-time guard `var _ FilesClient = (*daemon.DaemonClient)(nil)` mirroring the existing RemoteFilesClient guard. This guard fails the build if any signature diverges (cheapest TUIW-01 enforcement). Update the interface doc comment to say 8 methods (4 read + 4 write). Run gofmt and golangci-lint on the file.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./internal/tui/... && grep -q 'var _ FilesClient = (\*daemon.DaemonClient)(nil)' internal/tui/files_client.go && echo GUARD_PRESENT</automated>
  </verify>
  <done>internal/tui/files_client.go declares 8 methods, no UploadFile; both `var _ FilesClient = (*daemon.DaemonClient)(nil)` and `var _ FilesClient = (*RemoteFilesClient)(nil)` compile; `go build ./internal/tui/...` exits 0; gofmt clean.</done>
  <acceptance_criteria>
    - `go build ./internal/tui/...` exits 0 (DaemonClient satisfies the interface)
    - `grep -c 'var _ FilesClient' internal/tui/files_client.go internal/tui/remote_files_client.go` shows the daemon guard in files_client.go AND the remote guard in remote_files_client.go
    - `grep -c 'UploadFile' internal/tui/files_client.go` returns 0 (upload excluded)
    - `gofmt -l internal/tui/files_client.go` prints nothing
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the 4 write methods on RemoteFilesClient + httptest.TLSServer round-trip tests</name>
  <read_first>
    - internal/tui/remote_files_client.go (file being modified — read methods ReadFile lines 164-183, filesURL line 89, NewRemoteFilesClientForTest line 82, redactCapFromURL line 106)
    - internal/daemon/client.go:513-661 (analog — body construction for each write method: WriteFile octet-stream PUT, DeleteFile DELETE nil body, RenameFile JSON POST with renameRequest, MkdirFile POST nil body)
    - internal/tui/remote_files_client_test.go (file being modified — existing read-method httptest.TLSServer tests to mirror)
    - 126-PATTERNS.md "remote_files_client.go" section + "CAP-LEAK invariant" section
  </read_first>
  <behavior>
    - WriteFile: PUT to filesURL("write",...), octet-stream body = data, Content-Type application/octet-stream; decode files.FileWriteResponse on 200
    - DeleteFile: DELETE to filesURL("delete",...), nil body; decode files.FileOpResponse on 200
    - RenameFile: POST to filesURL("rename",...), JSON body {oldRel,newRel}, Content-Type application/json; decode files.FileOpResponse on 200
    - MkdirFile: POST to filesURL("mkdir",...), nil body; decode files.FileOpResponse on 200
    - On non-200, error string interpolates ONLY (statusCode, body) — never the URL (CAP-LEAK invariant): a test asserts the cap token does NOT appear in the returned error text
  </behavior>
  <action>
    In internal/tui/remote_files_client.go add `WriteFile`, `DeleteFile`, `RenameFile`, `MkdirFile` mirroring the existing read-method shape (http.NewRequestWithContext → c.http.Do → status check → decode). Use `c.filesURL("write"|"delete"|"rename"|"mkdir", sid, rel)` for the URL. Body construction per method: WriteFile = `http.MethodPut` + `bytes.NewReader(data)` + `Content-Type: application/octet-stream`; DeleteFile = `http.MethodDelete` + nil body; RenameFile = `http.MethodPost` + JSON-marshalled `{oldRel,newRel}` + `Content-Type: application/json`; MkdirFile = `http.MethodPost` + nil body. Decode the JSON response into `files.FileWriteResponse` (WriteFile) or `files.FileOpResponse` (the other three). CAP-LEAK invariant (SECURITY V7, T-122-04-01): on non-200, interpolate ONLY `(resp.StatusCode, strings.TrimSpace(string(body)))` into the error — NEVER the full URL (it carries `cap=`); use `redactCapFromURL` if a URL must appear. In internal/tui/remote_files_client_test.go add `TestRemoteFilesClient_Write`, `TestRemoteFilesClient_Delete`, `TestRemoteFilesClient_Rename`, `TestRemoteFilesClient_Mkdir` using `httptest.NewTLSServer` + `NewRemoteFilesClientForTest`, asserting correct method/path/body and decoded response. Add one cap-leak assertion: force a non-200 and assert the cap token string is absent from the returned error. Run gofmt + golangci-lint.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test ./internal/tui/ -run 'TestRemoteFilesClient_(Write|Delete|Rename|Mkdir)' -count=1</automated>
  </verify>
  <done>RemoteFilesClient implements all 4 write methods; both compile-time guards still pass; the 4 round-trip tests pass; cap-leak assertion passes.</done>
  <acceptance_criteria>
    - `go test ./internal/tui/ -run 'TestRemoteFilesClient_(Write|Delete|Rename|Mkdir)' -count=1` exits 0
    - `go build ./internal/tui/...` exits 0 (RemoteFilesClient still satisfies the 8-method interface)
    - test asserts the cap token does NOT appear in any returned error string
    - `gofmt -l internal/tui/remote_files_client.go internal/tui/remote_files_client_test.go` prints nothing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI → remote webserver | TUI builds HTTPS requests directly (no daemon proxy); cap token travels in the URL query (`cap=`) |
| Error text → logs/screen | Error strings may surface in toasts/logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Information Disclosure | RemoteFilesClient write-method error strings | mitigate | Interpolate ONLY (statusCode, body) into errors; never the full URL (carries `cap=`). Test asserts cap token absent from error text. (T-122-04-01 invariant, SECURITY V7) |
| T-126-02 | Tampering | rename destination / write relPath | accept (mitigated server-side) | TUI sends sandbox-RELATIVE paths only; server-side `validateAndClean` (Phase 123) double-validates source+dest. No TUI-side traversal construction. (SECURITY V5) |
| T-126-03 | Spoofing/Tampering | remote transport | accept | RemoteFilesClient enforces TLS 1.2+ (existing `MinVersion`); cap-token auth issued by Phase 124. No new transport security in this plan. |
| T-126-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — zero new Go modules (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `go build ./internal/tui/...` exits 0 (both implementers satisfy the 8-method interface)
- `go test ./internal/tui/ -run 'TestRemoteFilesClient_' -count=1` green
- `gofmt -l internal/tui/` clean for modified files
- No cap token leaks into any error string (asserted by test)
</verification>

<success_criteria>
- FilesClient is exactly 8 methods (4 read + 4 write, no UploadFile)
- Compile-time guards prove BOTH *daemon.DaemonClient and *tui.RemoteFilesClient satisfy it (partial SC#5 — interface satisfaction half)
- RemoteFilesClient write methods round-trip against httptest.TLSServer
</success_criteria>

<output>
Create `.planning/phases/126-tui-write-parity-editor-shell-out/126-01-SUMMARY.md` when done
</output>
