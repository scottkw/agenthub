---
phase: 126-tui-write-parity-editor-shell-out
plan: 04
type: execute
wave: 4
depends_on: ["126-03"]
files_modified:
  - internal/tui/files.go
  - internal/tui/files_test.go
autonomous: false
requirements: [TUIW-06, TUIW-07]
user_setup: []
must_haves:
  truths:
    - "Pressing u in the Files view shows the on-screen message 'Use desktop or web to upload files.' and attempts no write"
    - "A GitHub issue is filed in scottkw/agenthub tracking the TUI-upload parity gap, referencing Phase 126"
    - "TestFiles_NoSyncFSCalls passes with the new write commands and covers the write-command surface"
    - "A TestFiles_Phase126_Requirements traceability matrix exists, mirroring the Phase 121 convention"
  artifacts:
    - path: "internal/tui/files.go"
      provides: "the u key branch showing the upload-descope message"
      contains: "Use desktop or web to upload files."
    - path: "internal/tui/files_test.go"
      provides: "extended no-sync gate + Phase 126 requirements matrix + upload-descope test"
      contains: "TestFiles_Phase126_Requirements"
  key_links:
    - from: "internal/tui/files.go (u branch)"
      to: "m.files.err / status line"
      via: "set the descope message, dispatch no write cmd"
      pattern: "Use desktop or web to upload files\\."
    - from: "internal/tui/files_test.go (TestFiles_NoSyncFSCalls)"
      to: "files.go + files_cmds.go (Update-path purity)"
      via: "static-grep gate over the write-command surface"
      pattern: "TestFiles_NoSyncFSCalls"
---

<objective>
Close out the phase: add the `u` upload-descope message, file the tracking GitHub issue, extend the no-sync static-grep gate to cover the write commands, and add the Phase 126 requirements traceability matrix.

Purpose: TUIW-06 (formally descoped upload — the one sanctioned cross-surface parity gap, which MUST be tracked via a GitHub issue) and TUIW-07 (the no-sync FS gate must cover write commands). Completes SC#4 and the gate half of SC#5.
Output: the `u` branch + descope copy, a filed `scottkw/agenthub` issue, an extended `TestFiles_NoSyncFSCalls`, and `TestFiles_Phase126_Requirements`.

NOTE: this plan has a blocking human checkpoint (verify the filed GitHub issue) because cross-surface parity is release-blocking and the upload gap must be provably tracked (MEMORY: GitHub issues drive release planning).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-tui-write-parity-editor-shell-out/126-RESEARCH.md
@.planning/phases/126-tui-write-parity-editor-shell-out/126-PATTERNS.md
@.planning/phases/126-tui-write-parity-editor-shell-out/126-03-PLAN.md

<interfaces>
<!-- No-sync gate is the surface that must stay pure. From Plans 02/03: edit/delete/rename/mkdir cmds. -->

No-sync gate (files_test.go:850-878): `files := []string{"files.go", "files_cmds.go"}`; `re := regexp.MustCompile(`+"`"+`\bos\.(ReadDir|Open|OpenFile|Stat)\b`+"`"+`)`; fails on any non-comment match.
Phase 121 matrix analog: `TestFiles_Phase121_Requirements` (files_test.go:1086) — the established per-phase traceability convention.
Upload-descope copy (VERBATIM): "Use desktop or web to upload files."
Status-line surface: `m.files.err = errors.New(...)` renders via `renderFilesStatusLine` (files.go:283) — same surface used for the no-editor error.
DaemonClient.UploadFile exists but is EXCLUDED from FilesClient (descoped). The `u` branch does NOT call any write method.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: u upload-descope branch + extended no-sync gate + Phase 126 requirements matrix</name>
  <files>internal/tui/files.go, internal/tui/files_test.go</files>
  <read_first>
    - internal/tui/files.go:283-288 (analog — renderFilesStatusLine / m.files.err display) and the non-filter switch (files.go:484+)
    - internal/tui/files_test.go:850-878 (file being modified — TestFiles_NoSyncFSCalls gate) and :1086 (TestFiles_Phase121_Requirements matrix analog)
    - 126-PATTERNS.md "files_test.go" section; 126-RESEARCH.md "Pitfall 4" (gate placement rules)
  </read_first>
  <behavior>
    - TestFilesUpload_Descoped: pressing "u" sets m.files.err (or status) to exactly "Use desktop or web to upload files." and returns no write cmd
    - TestFiles_NoSyncFSCalls (extended): still passes — files.go + files_cmds.go contain no forbidden os.* in Update-reachable code; the gate's regex is broadened to also forbid os.Create|Remove|ReadFile|WriteFile in those two files (the editor temp-file I/O lives in files_edit.go, NOT in the gated list, so it is unaffected)
    - TestFiles_Phase126_Requirements: a table-driven matrix asserting each TUIW-0X requirement maps to a present behavior/symbol
  </behavior>
  <action>
    In internal/tui/files.go handleFilesKey non-filter switch add `case s == "u"`: set `m.files.err = errors.New("Use desktop or web to upload files.")` (verbatim) and return no cmd — call NO write method (upload is descoped; UploadFile is not in FilesClient). In internal/tui/files_test.go: (1) add `TestFilesUpload_Descoped` asserting the exact copy and no write cmd. (2) Extend `TestFiles_NoSyncFSCalls`: broaden the regex to `\bos\.(ReadDir|Open|OpenFile|Stat|Create|Remove|ReadFile|WriteFile)\b` and keep the file list `{"files.go","files_cmds.go"}` — confirm both files stay clean (the editor temp-file I/O is in files_edit.go, deliberately NOT in the gated list, keeping the Update-path files pure). (3) Add `TestFiles_Phase126_Requirements` mirroring TestFiles_Phase121_Requirements: a table mapping TUIW-01..07 to an asserted symbol/behavior (e.g. FilesClient method count == 8, resolveEditor present, editorExitMsg handler issues ClearScreen, deleteCmd/renameCmd/mkdirCmd present, upload-descope copy present, no-sync gate covers writes). Run gofmt + golangci-lint. Run the full race suite.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -race ./internal/tui/... ./internal/daemon/... -count=1 && gofmt -l internal/tui/ | (! grep .)</automated>
  </verify>
  <done>`u` shows the verbatim descope message with no write attempt; TestFiles_NoSyncFSCalls passes with the broadened regex; TestFiles_Phase126_Requirements passes; full -race suite green; gofmt clean.</done>
  <acceptance_criteria>
    - `go test -race ./internal/tui/... ./internal/daemon/... -count=1` exits 0
    - `grep -c 'Use desktop or web to upload files\.' internal/tui/files.go` returns >0
    - `grep -c 'TestFiles_Phase126_Requirements' internal/tui/files_test.go` returns >0
    - the no-sync gate regex includes Create|Remove|ReadFile|WriteFile AND files.go/files_cmds.go pass it
    - `gofmt -l internal/tui/` prints nothing
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: File the TUI-upload parity-gap GitHub issue in scottkw/agenthub</name>
  <files>(no source files — runs `gh issue create`)</files>
  <read_first>
    - .planning/phases/126-tui-write-parity-editor-shell-out/126-RESEARCH.md "Project Constraints" (cross-surface parity release-blocking; GitHub issues drive release planning)
    - .planning/REQUIREMENTS.md line 95 (TUIW-06) and line 132 (TUI upload descoped, revisit on demand)
  </read_first>
  <action>
    Use `gh` to file an issue in `scottkw/agenthub` tracking the TUI file-upload parity gap. Title: "TUI Files: file upload parity gap (descoped in v3.5 / Phase 126)". Body: state that TUI Files write parity shipped in Phase 126 (edit/delete/rename/mkdir) EXCEPT upload, which was formally descoped per ROADMAP Phase 126 SC#4 / requirement TUIW-06; the `u` key currently shows "Use desktop or web to upload files."; this issue tracks closing the gap on demand (cross-surface parity is a release-blocking contract). Reference "Discovered during Phase 126" and link the requirement ID TUIW-06. Run: `gh issue create --repo scottkw/agenthub --title "<title>" --body "<body>"`. If `gh` returns an auth error, STOP and surface a checkpoint (do not fabricate an issue number). Capture the resulting issue URL/number for the SUMMARY.
  </action>
  <verify>
    <automated>gh issue list --repo scottkw/agenthub --search "TUI Files file upload parity gap in:title" --json number,title --jq 'length > 0'</automated>
  </verify>
  <done>A GitHub issue exists in scottkw/agenthub titled for the TUI-upload parity gap, referencing Phase 126 / TUIW-06; its URL/number is recorded in the SUMMARY.</done>
  <acceptance_criteria>
    - `gh issue list --repo scottkw/agenthub --search "TUI Files file upload parity gap in:title"` returns a matching issue
    - the issue body references Phase 126 and requirement TUIW-06
    - the issue number/URL is recorded in 126-04-SUMMARY.md
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the TUI-upload parity-gap issue is filed (release-blocking gate)</name>
  <action>Pause for the operator to confirm the GitHub parity-gap issue exists before the phase closes. This is a verification checkpoint AFTER Task 2's automation, not manual work.</action>
  <what-built>The TUI `u` upload-descope message and the filed GitHub parity-gap issue. Cross-surface parity is a release-blocking contract; the sole sanctioned gap (upload) must be provably tracked before the phase can close.</what-built>
  <how-to-verify>
    1. Confirm the GitHub issue exists: `gh issue view <number> --repo scottkw/agenthub` — verify the title references the TUI upload parity gap and the body cites Phase 126 / TUIW-06.
    2. (Optional terminal UAT) Run the TUI, open the Files view, press `u`, and confirm the status line shows exactly "Use desktop or web to upload files." with no crash and no write attempt.
  </how-to-verify>
  <resume-signal>Type "approved" once the issue is confirmed filed, or describe what's missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Update-path code → filesystem | The no-sync gate enforces that files.go/files_cmds.go never do synchronous FS I/O |
| `u` keypress → action | Must surface a message, never attempt an unsupported upload write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-13 | Denial of Service | synchronous FS I/O in Update path | mitigate | TestFiles_NoSyncFSCalls (broadened regex over files.go/files_cmds.go) fails the build if any forbidden `os.*` appears in Update-reachable code; all write FS I/O stays in tea.Cmd closures / files_edit.go. (TUIW-07) |
| T-126-14 | Repudiation | undocumented parity gap | mitigate | The upload gap is documented on-screen AND filed as a GitHub issue (release-blocking parity contract); a blocking human checkpoint verifies the issue exists before phase close. (TUIW-06) |
| T-126-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |
</threat_model>

<verification>
- `go test -race ./internal/tui/... ./internal/daemon/... -count=1` green
- `TestFiles_NoSyncFSCalls` passes with the broadened regex covering write verbs
- `TestFiles_Phase126_Requirements` green (all 7 TUIW IDs mapped)
- `gofmt -l internal/tui/` clean; `golangci-lint run ./internal/tui/...` clean
- GitHub issue filed and verified (blocking checkpoint)
</verification>

<success_criteria>
- SC#4: `u` → "Use desktop or web to upload files." + a follow-up GitHub issue filed
- SC#5 (gate half): TestFiles_NoSyncFSCalls passes with write commands; all write FS I/O via tea.Cmd
- All 7 TUIW requirements traceable via TestFiles_Phase126_Requirements
</success_criteria>

<output>
Create `.planning/phases/126-tui-write-parity-editor-shell-out/126-04-SUMMARY.md` when done (include the filed GitHub issue URL/number)
</output>
