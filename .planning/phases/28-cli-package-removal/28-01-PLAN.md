---
phase: 28-cli-package-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/agenthub-cli/main.go
  - cmd/agenthub-cli/cmd_daemon.go
  - cmd/agenthub-cli/cmd_attach.go
  - cmd/agenthub-cli/cmd_attach_unix.go
  - cmd/agenthub-cli/cmd_attach_windows.go
  - cmd/agenthub-cli/cmd_daemon_test.go
  - cmd/agenthub-cli/cmd_attach_test.go
  - cmd/agenthub-cli/main_test.go
  - README.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "The cmd/agenthub-cli/ directory does not exist"
    - "No active source file references agenthub-cli"
    - "go build ./... succeeds with zero errors"
    - "go test ./... passes with cmd/agenthub-cli absent from output"
  artifacts:
    - path: "README.md"
      provides: "Go packages table without cmd/agenthub-cli row"
      contains: "| `internal/webserver`"
      not_contains: "cmd/agenthub-cli"
  key_links:
    - from: "go build ./..."
      to: "all Go packages"
      via: "Go compiler resolves all imports"
      pattern: "exit 0"
---

<objective>
Delete the dead `cmd/agenthub-cli/` package (8 files) and scrub the one remaining reference from README.md.

Purpose: Phase 27 moved all CLI logic into the root package. The old standalone CLI package is dead code that inflates the repo and could confuse future contributors.
Output: Clean repository with no trace of the old CLI binary package in active source files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-cli-package-removal/28-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete cmd/agenthub-cli/ and remove README reference</name>
  <files>cmd/agenthub-cli/ (deleted), README.md</files>
  <read_first>
    - README.md (lines 65-76 — the Go packages table containing the row to delete)
    - cmd/agenthub-cli/main.go (confirm it is the old standalone entrypoint, not imported elsewhere)
  </read_first>
  <action>
1. Delete the entire `cmd/agenthub-cli/` directory and all 8 files within it:
   - cmd/agenthub-cli/main.go
   - cmd/agenthub-cli/cmd_daemon.go
   - cmd/agenthub-cli/cmd_attach.go
   - cmd/agenthub-cli/cmd_attach_unix.go
   - cmd/agenthub-cli/cmd_attach_windows.go
   - cmd/agenthub-cli/cmd_daemon_test.go
   - cmd/agenthub-cli/cmd_attach_test.go
   - cmd/agenthub-cli/main_test.go

   Command: `rm -rf cmd/agenthub-cli/`

2. Edit README.md: delete line 74 which contains:
   `| `cmd/agenthub-cli` | CLI command implementations |`

   The table should go directly from the `internal/webserver` row to the `web/` row. Do NOT add a replacement row — the CLI commands now live in the root package which is not listed as a separate cmd/ entry.

3. Do NOT edit any files inside `.planning/` or `.claude/` — historical references are planning artifacts, not active source.
4. Do NOT run `go mod tidy` — not needed since all dependencies are shared with root package.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && ls cmd/agenthub-cli 2>/dev/null && echo "FAIL: directory still exists" && exit 1 || echo "PASS: directory deleted"</automated>
  </verify>
  <acceptance_criteria>
    - `ls cmd/agenthub-cli` returns "No such file or directory"
    - README.md does NOT contain the string "agenthub-cli"
    - README.md still contains `| `internal/webserver` |` row (table not broken)
    - README.md still contains `| `web/` |` row (table not broken)
  </acceptance_criteria>
  <done>The cmd/agenthub-cli/ directory is gone and README.md has no reference to it</done>
</task>

<task type="auto">
  <name>Task 2: Verify clean build and no dangling references</name>
  <files>None (verification only)</files>
  <read_first>
    - README.md (confirm the edit from Task 1 is correct)
  </read_first>
  <action>
1. Run `go build ./...` from the repo root. Must exit 0 with no errors. This proves no Go source file imports from the deleted package.

2. Run `go test ./...` from the repo root. All packages must pass. The output must NOT contain `cmd/agenthub-cli`. Expected packages in output:
   - `github.com/agenthub/agenthub` (root)
   - `github.com/agenthub/agenthub/internal/daemon`
   - `github.com/agenthub/agenthub/internal/pty`
   - `github.com/agenthub/agenthub/internal/relay`
   - `github.com/agenthub/agenthub/internal/status`
   - `github.com/agenthub/agenthub/internal/webserver`

3. Run the exhaustive reference grep to confirm CLEAN-02:
   ```
   grep -r "agenthub-cli" . \
     --include="*.go" \
     --include="*.sh" \
     --include="*.yml" \
     --include="*.yaml" \
     --include="*.md" \
     --exclude-dir=".planning" \
     --exclude-dir=".claude"
   ```
   Must return zero results.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./... && echo "BUILD OK" && grep -r "agenthub-cli" . --include="*.go" --include="*.sh" --include="*.yml" --include="*.yaml" --include="*.md" --exclude-dir=.planning --exclude-dir=.claude && echo "FAIL: references found" && exit 1 || echo "PASS: no references"</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0
    - `go test ./...` exits 0 and output does not contain "cmd/agenthub-cli"
    - `grep -r "agenthub-cli"` (scoped to active source files) returns zero results
  </acceptance_criteria>
  <done>Build is green, tests pass, and no active source file references agenthub-cli</done>
</task>

</tasks>

<verification>
Phase-level verification after all tasks complete:

1. `ls cmd/agenthub-cli 2>/dev/null` returns error (directory gone) — CLEAN-01
2. `grep -r "agenthub-cli" . --include="*.go" --include="*.sh" --include="*.yml" --include="*.yaml" --include="*.md" --exclude-dir=.planning --exclude-dir=.claude` returns zero results — CLEAN-02
3. `go build ./...` exits 0 — no import errors
4. `go test ./...` all green — no test regressions
</verification>

<success_criteria>
- cmd/agenthub-cli/ directory does not exist
- Zero references to "agenthub-cli" in active source files (Go, shell, YAML, Markdown outside .planning/.claude)
- `go build ./...` succeeds
- `go test ./...` passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/28-cli-package-removal/28-01-SUMMARY.md`
</output>
