---
phase: 170-public-share-access-codes-reusable-share-lifetime-join-code
plan: 04
type: execute
wave: 4
depends_on: ["170-01", "170-02", "170-03"]
files_modified:
  - TESTING.md
autonomous: true
requirements: [FNL-08]
must_haves:
  truths:
    - "TESTING.md Suite Manifest reflects the one new Go test file (internal/webserver/join_test.go) with updated counts, and a phase note describing the extended test files"
    - "Section 4 has an FNL-08 traceability row for every new/extended test file that covers FNL-08 (joincode_test.go, join_test.go, funnel_test.go, SessionSharePanel.test.tsx)"
    - "A new manual checklist item (M-46) captures the live off-tailnet end-to-end check: recipient types the public URL, enters the reusable code, joins read-only, and a second device reuses the same code"
    - "bash tests/check-traceability-paths.sh exits 0 (every mapped path exists on disk, path column is a repo-relative file path only)"
  artifacts:
    - "TESTING.md: Suite Manifest note + count bump, Section 4 FNL-08 traceability rows, Section 5 Category + M-46 manual item"
  key_links:
    - "The traceability path column contains ONLY repo-relative file paths (.go/.tsx) — test/describe names go in the Notes column, per the standing rule the check script enforces"
  prohibitions:
    - "Do NOT put test names or describe-block suffixes in the path column (breaks check-traceability-paths.sh)"
    - "Do NOT count extended-in-place files as new files — only internal/webserver/join_test.go is a net-new file"
---

<objective>
Reconcile TESTING.md with the Phase 170 test surface per the repo's standing regression-suite convention: update
the Suite Manifest (Section 2), add FNL-08 traceability rows (Section 4), add the live off-tailnet manual check
(Section 5), and prove the traceability map is path-valid with `tests/check-traceability-paths.sh`.

Purpose: TESTING.md is the canonical regression home; every phase that adds/renames tests MUST update it. Phase
170 adds one new Go file and extends four existing test files, plus one behavior (live end-to-end reusable-code
join) that cannot be automated.
Output: an updated TESTING.md that passes the path-check script.

## Artifacts this plan produces
- Suite Manifest phase note + count bump (Go 374 → 375; vitest unchanged at 142; Total 527 → 528)
- Section 4 FNL-08 traceability rows for: internal/capability/joincode_test.go, internal/webserver/join_test.go,
  internal/daemon/funnel_test.go, frontend/src/components/__tests__/SessionSharePanel.test.tsx
- Section 5 new category + manual item M-46 (live off-tailnet reusable-code join)

Note: only `internal/webserver/join_test.go` is a NET-NEW file. joincode_test.go, funnel_test.go, api_test.go, and
SessionSharePanel.test.tsx are all EXTENDED in place and do not change the file counts beyond the one new file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/170-public-share-access-codes-reusable-share-lifetime-join-code-/170-01-SUMMARY.md
@.planning/phases/170-public-share-access-codes-reusable-share-lifetime-join-code-/170-02-SUMMARY.md
@.planning/phases/170-public-share-access-codes-reusable-share-lifetime-join-code-/170-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suite Manifest note + FNL-08 traceability rows</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md (Section 2 Suite Manifest table + phase-note block :5-24; Section 4 traceability map header + format rule :148-152 and the existing FNL-01/03 rows :326-336 to mirror; Section 6 Standing Convention :734+)
    - .planning/phases/170-public-share-access-codes-reusable-share-lifetime-join-code-/170-01-SUMMARY.md, 170-02-SUMMARY.md, 170-03-SUMMARY.md (for the exact test names/files that actually landed)
  </read_first>
  <action>
    In Section 2: bump the Go unit/integration count 374 → 375 (the one new file internal/webserver/join_test.go)
    and the Total 527 → 528; leave vitest at 142 (SessionSharePanel.test.tsx is extended, not new). Add a dated
    phase note (mirroring the existing `> Note: Phase 169-…` entries) summarizing: the reusable JoinCodeManager
    primitive (IssueReusable/Revoke/reusable flag), the new join_test.go, the daemon mint/cache/revoke wiring +
    funnel_test.go extensions, and the frontend publicReadCode row + models.ts sync — listing which files are NEW
    vs EXTENDED.
    In Section 4: add one traceability row per FNL-08-covering test file, using the exact format the check script
    enforces — Requirement ID `FNL-08`, path column = repo-relative FILE PATH ONLY (no test names), Notes column =
    the covering test names/behaviors. Rows for: internal/capability/joincode_test.go, internal/webserver/join_test.go,
    internal/daemon/funnel_test.go, frontend/src/components/__tests__/SessionSharePanel.test.tsx. Match the language
    of the source-artifact test names discovered in the SUMMARYs (do not invent names).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && bash tests/check-traceability-paths.sh && grep -c "FNL-08" TESTING.md</automated>
  </verify>
  <acceptance_criteria>
    - Counts updated (Go 375 / vitest 142 / Total 528) and a Phase 170 note added to Section 2.
    - Four FNL-08 traceability rows exist with file-path-only path columns.
    - `tests/check-traceability-paths.sh` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Manual checklist item for the live off-tailnet reusable-code join</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md (Section 5 category structure: Category I — Tailscale Funnel (FNL) :616, Category T — Funnel Frontend (FUI) :648, Category W — Tailscale Detection Fix :722; highest existing item is M-45 — the new item is M-46)
    - .planning/phases/170-public-share-access-codes-reusable-share-lifetime-join-code-/170-VALIDATION.md (Manual-Only Verifications table — the exact behavior to encode)
  </read_first>
  <action>
    Add a new Section 5 category (e.g. "Category X — Public Share Access Codes (FNL-08)") with item M-46 describing
    the live end-to-end check that cannot be automated: on a Tailscale-connected machine with Funnel granted (a
    PRODUCTION build), enable Funnel for a session, copy the reusable "Public join code (reusable):" from the Share
    modal INTERNET (PUBLIC) section; from an OFF-tailnet device open the public URL, land on the code-entry page,
    enter the code, and confirm a read-only join; then confirm a SECOND off-tailnet device can reuse the SAME code
    (proving reusability), and that after the owner disables the internet share the code no longer resolves (proving
    share-lifetime teardown). Include a `_Why not automatable:_` note (requires a real Funnel-granted tailnet + an
    off-tailnet device) and a `_Source:_` line referencing Phase 170 / FNL-08. Follow the exact formatting of the
    existing M-34/M-45 items.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && grep -q "M-46" TESTING.md && bash tests/check-traceability-paths.sh</automated>
  </verify>
  <acceptance_criteria>
    - M-46 exists under a Public Share Access Codes category, covering the reusable + share-lifetime live checks.
    - The item carries a why-not-automatable rationale and an FNL-08 source reference.
    - `tests/check-traceability-paths.sh` still exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → CI gate | check-traceability-paths.sh runs in CI; a malformed path column fails the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-170-10 | Tampering | traceability path column | low | mitigate | Path column holds repo-relative file paths only; the task runs check-traceability-paths.sh to prove exit 0 before commit |
| T-170-SC | Tampering | package installs | n/a | accept | Docs-only plan; no packages, no code changes |
</threat_model>

<verification>
- `bash tests/check-traceability-paths.sh` exits 0.
- FNL-08 rows and M-46 present in TESTING.md.
- Counts consistent (Go 375 / vitest 142 / Total 528).
</verification>

<success_criteria>
- TESTING.md reconciled with the Phase 170 test surface and passes the path-check script.
- The un-automatable live reusable-code join is captured as M-46.
</success_criteria>

<output>
Create `.planning/phases/170-public-share-access-codes-reusable-share-lifetime-join-code-/170-04-SUMMARY.md` when done
</output>
