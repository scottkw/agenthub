---
status: complete
phase: 170-public-share-access-codes-reusable-share-lifetime-join-code
source: [170-VERIFICATION.md]
started: 2026-07-06T01:25:03Z
updated: 2026-07-06T16:10:00Z
---

## Current Test

[testing complete]

## Tests

### 1. M-46 live off-tailnet reusable-code join + share-lifetime teardown
expected: On a real Tailscale-Funnel-granted machine, enable Funnel for a session, copy the "Public join code (reusable):" value from the Share modal INTERNET (PUBLIC) section. From an off-tailnet device, open the public URL, land on /join, enter the code, confirm a read-only join. From a SECOND off-tailnet device, enter the SAME code and confirm it ALSO joins read-only (proving reusability). Then disable the internet share and confirm the code no longer resolves (proving share-lifetime teardown).
result: pass
notes: "PASS 2026-07-06 live on real Funnel tailnet after fix commit 5a92ddae. Initial run FAILED (blocker): the public URL was funnelUrl = the ephemeral /sessions/{id}?cap= cap link, which 401'd 'capability required' on the off-tailnet phone; the reusable publicReadCode was orphaned text with no openable URL. Fix rewired the public URL/Copy/Open/QR to https://host/join?code=<publicReadCode>. Re-test: (1) phone off-tailnet joined read-only; (2) second off-tailnet device joined read-only with the SAME code (reusability); (3) after Disable internet share, neither device could connect (share-lifetime teardown)."

## Summary

total: 1
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "The Internet (public) share hands the guest a reusable, share-lifetime entry point that lands on /join and accepts the displayed 'Public join code (reusable)'."
  status: resolved
  resolved_by: 5a92ddae
  reason: "The public section set funnelUrl = resp.readUrl (SessionShareModal.tsx:389), i.e. https://host/sessions/{id}?cap=<rTok> — a per-session, grant-bound capability link, NOT https://host/join?code=<publicReadCode>. The panel displayed/copied/QR-encoded that cap link directly. Capabilities are grant-based (capability.go: only IAT, no time TTL) so the link 401'd 'capability required' (capability_mw.go:41/54) whenever the ?cap= was dropped, the grant rotated on a warm-up re-issue, or the daemon restarted (in-memory grants lost). The reusable publicReadCode was minted + shown as text but wired into NO openable URL, so no share link reached /join."
  fix: "SessionSharePanel.tsx now derives publicEntryUrl = https://host/join?code=<publicReadCode> from funnelUrl's origin (mirrors joinURLFor() used for the RO/Full QR path) and uses it for the displayed URL, Copy, Open, and QR. RED→GREEN TDD (3 assertions in SessionSharePanel.test.tsx); full gate green (tsc + 2337 vitest); live re-test PASS on all 3 conditions."
  test: 1
