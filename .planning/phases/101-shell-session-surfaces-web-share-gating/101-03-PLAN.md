---
phase: 101-shell-session-surfaces-web-share-gating
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - frontend/src/components/ShellWebShareBanner.tsx
  - frontend/src/components/__tests__/ShellWebShareBanner.test.tsx
  - frontend/src/App.tsx
  - frontend/src/components/__tests__/App.shellWebShare.test.tsx
  - frontend/src/styles.css
autonomous: true
requirements:
  - SHELL-07
  - SHELL-08

must_haves:
  truths:
    - "ShellWebShareBanner renders the locked heading 'Web sharing this shell will expose arbitrary command execution.' verbatim"
    - "ShellWebShareBanner body line 1 includes the sessionName via interpolation: \"You are about to share 'SESSIONNAME'. Anyone on your tailnet who can reach the daemon will be able to type commands as your user account.\""
    - "ShellWebShareBanner body line 2 is locked: 'Read-only viewers cannot type, but commands you run remain visible to them.'"
    - "ShellWebShareBanner primary CTA reads 'Enable web sharing' verbatim"
    - "ShellWebShareBanner secondary CTA reads 'Cancel' verbatim"
    - "ShellWebShareBanner dismiss button has aria-label 'Dismiss security warning'"
    - "ShellWebShareBanner root element has role='alert' and aria-live='assertive' (NOT polite — differs from PluginToggleBanner)"
    - "ShellWebShareBanner reuses .webgl-recovery-banner BEM class with new modifier --shell-warning for 3px left border #f7768e"
    - "ShellWebShareBanner banner height stays at 53px (locked stack rhythm)"
    - "On mount, ShellWebShareBanner focuses the Cancel button (safe action, per QuitConfirmModal precedent)"
    - "Esc dismisses the banner and fires onCancel"
    - "Click on dismiss button (×) fires onCancel"
    - "Click on 'Enable web sharing' fires onConfirm"
    - "After clicking 'Enable web sharing', primary button shows 'Enabling…' and both buttons are disabled with aria-busy='true' until parent unmounts the banner"
    - "App.tsx handleToggleWeb intercepts ON-toggles for shell sessions when shellWebShareWarned is false"
    - "App.tsx interception: for cli in {shell, bash, zsh, pwsh, powershell} AND enabled=true AND shellWebShareWarned=false → push ShellWebShareBanner into the banner stack instead of calling ToggleWebServing directly"
    - "App.tsx interception: for cli in shell set AND enabled=true AND shellWebShareWarned=true → fall through to existing ToggleWebServing call (no banner)"
    - "App.tsx interception: for AI CLI (cli=claude, etc.) → fall through to existing ToggleWebServing call (no banner ever)"
    - "App.tsx interception: for OFF-toggles (enabled=false) → fall through to existing ToggleWebServing call (no banner)"
    - "On banner confirm: App.tsx fires SetShellWebShareWarned(true), then ToggleWebServing(id, true), then dismisses banner"
    - "On banner cancel: App.tsx does NOT call ToggleWebServing or SetShellWebShareWarned; the toggle returns to OFF state"
    - "App.tsx renders ShellWebShareBanner at TOP of the banner stack when active (priority order per UI-SPEC §Stack ordering)"
    - "While banner is shown, the originating Web On button is in aria-busy='true' / disabled state to prevent double-fire"
    - "App.tsx reads shellWebShareWarned via GetShellWebShareWarned() on mount and reconciles in-memory state synchronously after SetShellWebShareWarned succeeds (mitigates race per RESEARCH §8 'First-toggle persistence race')"
  artifacts:
    - path: frontend/src/components/ShellWebShareBanner.tsx
      provides: "NEW component: role=alert banner with locked copy, two CTAs, dismiss button, focus-on-mount, Esc handling"
      contains: "Web sharing this shell will expose arbitrary command execution"
    - path: frontend/src/components/__tests__/ShellWebShareBanner.test.tsx
      provides: "Vitest cases for copy verbatim, role/aria-live, focus on Cancel, Esc dismiss, onConfirm/onCancel invocation, Enabling state"
      contains: "role"
    - path: frontend/src/App.tsx
      provides: "handleToggleWeb interception + ShellWebShareBanner render slot at top of banner stack + shellWebShareWarned state"
      contains: "ShellWebShareBanner"
    - path: frontend/src/components/__tests__/App.shellWebShare.test.tsx
      provides: "Vitest cases for shell-toggle interception, AI-CLI bypass, OFF-toggle bypass, warned=true bypass, confirm flow, cancel flow"
      contains: "shellWebShareWarned"
    - path: frontend/src/styles.css
      provides: "CSS for .webgl-recovery-banner--shell-warning modifier (3px red left border)"
      contains: "webgl-recovery-banner--shell-warning"
  key_links:
    - from: frontend/src/components/ShellWebShareBanner.tsx
      to: frontend/src/styles.css
      via: "className 'webgl-recovery-banner webgl-recovery-banner--shell-warning'"
      pattern: "webgl-recovery-banner--shell-warning"
    - from: frontend/src/App.tsx
      to: frontend/src/wailsjs/go/main/App.GetShellWebShareWarned
      via: "useEffect on mount calls GetShellWebShareWarned and stores boolean in shellWebShareWarned state"
      pattern: "GetShellWebShareWarned\\("
    - from: frontend/src/App.tsx
      to: frontend/src/wailsjs/go/main/App.SetShellWebShareWarned
      via: "Banner confirm handler calls SetShellWebShareWarned(true) before ToggleWebServing"
      pattern: "SetShellWebShareWarned\\("
    - from: frontend/src/App.tsx
      to: frontend/src/components/ShellWebShareBanner.tsx
      via: "Conditional render at top of banner stack when pendingShellWebToggle is set"
      pattern: "<ShellWebShareBanner"
---

<objective>
Add the one-time security confirmation banner (SHELL-08) and the App.tsx toggle interception that enforces it (SHELL-07 frontend defense-in-depth). When the user clicks "Web On" for a shell session for the first time on this machine, the toggle is intercepted: a ShellWebShareBanner appears at the top of the banner stack with locked security-warning copy, the originating toggle button enters an aria-busy state, and only on explicit Confirm does the daemon receive both SetShellWebShareWarned(true) and ToggleWebServing(true). Cancel/Esc/× dismisses without effect.

Purpose: Closes SHELL-08 (one-time confirmation) and provides defense-in-depth for SHELL-07 (per RESEARCH §4, daemon already disallows auto-enable at api.go:407; the frontend banner is the user-visible gate). This plan does NOT modify NewSessionModal or TabBar (Plan 02 owns those). It does NOT touch CLI or TUI (Plan 04 covers those, and per SHELL-08 the banner is GUI-only — CLI/TUI users are technical enough to understand the implications and the requirement explicitly says "GUI" via "one-time confirmation banner").

Output: 1 new component (ShellWebShareBanner.tsx), 1 new test file, App.tsx modifications, 1 stylesheet modification, 1 new App-level test file (separate from existing App tests to isolate the shell-toggle scenarios).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/101-shell-session-surfaces-web-share-gating/101-UI-SPEC.md
@.planning/phases/101-shell-session-surfaces-web-share-gating/101-RESEARCH.md
@.planning/phases/101-shell-session-surfaces-web-share-gating/101-CONTEXT.md
@.planning/phases/101-shell-session-surfaces-web-share-gating/101-01-SUMMARY.md
@frontend/src/App.tsx
@frontend/src/components/PluginToggleBanner.tsx
@frontend/src/components/WebGLRecoveryBanner.tsx
@frontend/src/components/QuitConfirmModal.tsx
@frontend/src/styles.css

<interfaces>
Plan 101-01 Wails bindings (in place after Wave 1):

  export function GetShellWebShareWarned(): Promise<boolean>;
  export function SetShellWebShareWarned(arg1: boolean): Promise<void>;

Phase 100 existing Wails binding (already in place):

  export function ToggleWebServing(id: string, enabled: boolean): Promise<void>;

The existing App.tsx handleToggleWeb signature is approximately:

  async function handleToggleWeb(id: string, enabled: boolean): Promise<void>

Existing precedent for banner-stack JSX rendering in App.tsx (per Phase 81 BAN-01 + Phase 99 PUI-02): the .banner-stack flex-column container holds individually-rendered banner elements like:

  <div className="banner-stack">
    {pendingShellWebToggle && <ShellWebShareBanner ... />}  // NEW — top priority
    {webglRecoveryVisible && <WebGLRecoveryBanner ... />}
    {localNetworkBannerVisible && <LocalNetworkBanner ... />}
    {updateBannerVisible && <UpdateBanner ... />}
    {pluginToggleBannerVisible && <PluginToggleBanner ... />}
  </div>

Locked copy (UI-SPEC §Web-share security banner copy):

| Element | Copy |
|---------|------|
| Banner role | role="alert", aria-live="assertive" |
| Heading | "Web sharing this shell will expose arbitrary command execution." |
| Body | "You are about to share 'SESSIONNAME'. Anyone on your tailnet who can reach the daemon will be able to type commands as your user account. Read-only viewers cannot type, but commands you run remain visible to them." |
| Primary CTA | "Enable web sharing" |
| Confirming label | "Enabling…" (during in-flight RPC) |
| Secondary CTA | "Cancel" |
| Dismiss-aria-label | "Dismiss security warning" |

Locked visual constants:
- Banner height: 53px (matches .webgl-recovery-banner)
- Left-border accent: 3px solid #f7768e (no new hex)
- New CSS modifier: .webgl-recovery-banner--shell-warning

Shell set (must match Plan 02's tab badge mapping and Plan 100's daemon set):
SHELL_CLIS = {'shell', 'bash', 'zsh', 'pwsh', 'powershell'}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — ShellWebShareBanner component tests (RED, then GREEN with implementation)</name>
  <files>frontend/src/components/__tests__/ShellWebShareBanner.test.tsx, frontend/src/components/ShellWebShareBanner.tsx, frontend/src/styles.css</files>
  <read_first>
    - frontend/src/components/PluginToggleBanner.tsx (closest precedent — banner shape, button layout, dismiss handling)
    - frontend/src/components/__tests__/PluginToggleBanner.test.tsx if it exists (mirror its Vitest structure)
    - frontend/src/components/QuitConfirmModal.tsx (focus-on-mount pattern; killFocusYes=false defaults to Cancel/No)
    - frontend/src/styles.css (.webgl-recovery-banner block — base class to reuse)
    - .planning/phases/101-shell-session-surfaces-web-share-gating/101-UI-SPEC.md §Web-share security banner copy + §Component Contracts: ShellWebShareBanner
  </read_first>
  <behavior>
    Step 1 RED: create the test file with assertions before the component file exists.

    Test cases (ShellWebShareBanner.test.tsx):
    1. "renders heading verbatim" — assert getByText with the exact string "Web sharing this shell will expose arbitrary command execution."
    2. "renders body with sessionName interpolated" — pass sessionName="scratch-bash"; assert the body contains the substring "You are about to share 'scratch-bash'. Anyone on your tailnet who can reach the daemon will be able to type commands as your user account." AND the substring "Read-only viewers cannot type, but commands you run remain visible to them."
    3. "primary CTA reads 'Enable web sharing' verbatim" — assert button text.
    4. "secondary CTA reads 'Cancel' verbatim" — assert button text.
    5. "dismiss button aria-label is 'Dismiss security warning'" — assert getByLabelText.
    6. "root has role=alert and aria-live=assertive" — assert via querySelector or test-id.
    7. "focuses Cancel button on mount" — assert document.activeElement matches the Cancel button after mount (use waitFor for the focus shift).
    8. "Esc fires onCancel" — fire keyDown Escape on the document or banner; assert onCancel called once and onConfirm not called.
    9. "clicking Cancel fires onCancel" — assert onCancel called, onConfirm not.
    10. "clicking dismiss (×) fires onCancel" — assert onCancel called.
    11. "clicking Enable web sharing fires onConfirm" — assert onConfirm called.
    12. "after onConfirm click, primary button shows Enabling… and aria-busy" — assert button text is "Enabling…" and the banner root has aria-busy=true; both buttons are disabled. (The component manages this transient state internally using a local React useState boolean toggled by the click; parent eventually unmounts the banner.)
    13. "BEM class includes both webgl-recovery-banner and webgl-recovery-banner--shell-warning" — assert root element className.

    Step 2 GREEN: implement ShellWebShareBanner.tsx + add CSS modifier. Tests turn green.
  </behavior>
  <action>
    Step 1: Create frontend/src/components/__tests__/ShellWebShareBanner.test.tsx with the 13 test cases. Use the existing Vitest test render helper (likely @testing-library/react render). Import ShellWebShareBanner from '../ShellWebShareBanner' — the import will fail with module-not-found, which is the RED indicator.

    Test fixture shape:
      const defaultProps = {
        sessionName: 'scratch-bash',
        onConfirm: vi.fn(),
        onCancel: vi.fn(),
      };

    For focus-on-mount test, use waitFor:
      await waitFor(() => expect(document.activeElement?.textContent).toBe('Cancel'));

    Step 2: Create frontend/src/components/ShellWebShareBanner.tsx:

      import React, { useEffect, useRef, useState } from 'react';

      export interface ShellWebShareBannerProps {
        sessionName: string;
        onConfirm: () => void;
        onCancel: () => void;
      }

      export const ShellWebShareBanner: React.FC<ShellWebShareBannerProps> = ({ sessionName, onConfirm, onCancel }) => {
        const cancelRef = useRef<HTMLButtonElement>(null);
        const [enabling, setEnabling] = useState(false);

        useEffect(() => {
          // Focus Cancel on mount per UI-SPEC §Accessibility ("Focus moves to Cancel button (safe action)").
          cancelRef.current?.focus();
        }, []);

        useEffect(() => {
          const onKey = (e: KeyboardEvent) => {
            if (e.key === 'Escape') { e.preventDefault(); onCancel(); }
          };
          document.addEventListener('keydown', onKey);
          return () => document.removeEventListener('keydown', onKey);
        }, [onCancel]);

        const handleConfirm = () => {
          if (enabling) return;
          setEnabling(true);
          onConfirm();
        };

        return (
          <div
            className="webgl-recovery-banner webgl-recovery-banner--shell-warning"
            role="alert"
            aria-live="assertive"
            aria-busy={enabling || undefined}
          >
            <div className="webgl-recovery-banner__body">
              <div className="webgl-recovery-banner__heading">
                Web sharing this shell will expose arbitrary command execution.
              </div>
              <div className="webgl-recovery-banner__text">
                You are about to share '{sessionName}'. Anyone on your tailnet who can reach the daemon will be able to type commands as your user account. Read-only viewers cannot type, but commands you run remain visible to them.
              </div>
            </div>
            <div className="webgl-recovery-banner__actions">
              <button
                type="button"
                ref={cancelRef}
                disabled={enabling}
                onClick={onCancel}
                className="webgl-recovery-banner__btn webgl-recovery-banner__btn--secondary"
              >
                Cancel
              </button>
              <button
                type="button"
                disabled={enabling}
                onClick={handleConfirm}
                className="webgl-recovery-banner__btn webgl-recovery-banner__btn--primary-destructive"
              >
                {enabling ? 'Enabling…' : 'Enable web sharing'}
              </button>
              <button
                type="button"
                disabled={enabling}
                onClick={onCancel}
                aria-label="Dismiss security warning"
                className="webgl-recovery-banner__dismiss"
              >
                ×
              </button>
            </div>
          </div>
        );
      };

    NOTE on class names: read the existing PluginToggleBanner / WebGLRecoveryBanner source to confirm the exact child element class names. The pattern uses .webgl-recovery-banner__body, __heading, __text, __actions, __btn, __dismiss — but if the actual existing component uses different names (e.g., __content, __title), mirror those. The tests must align with whatever Task 1's component renders.

    Step 3: styles.css — append:

      .webgl-recovery-banner--shell-warning {
        border-left: 3px solid #f7768e;
      }
      .webgl-recovery-banner__btn--primary-destructive {
        background: #f7768e;
        color: #1a1b26;
        font-weight: 600;
      }
      .webgl-recovery-banner__btn--primary-destructive:hover:not(:disabled) {
        background: #f08aa0;
      }

    If existing PluginToggleBanner already has analogous --primary-destructive or --secondary classes, reuse those instead of duplicating.

    Run: cd frontend && pnpm tsc --noEmit && pnpm test -- --run ShellWebShareBanner
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- --run ShellWebShareBanner 2>&1 | tee /tmp/plan101-03-task1.log; TEST_EXIT=$?; if [ "$TEST_EXIT" -ne 0 ]; then echo "FAIL — see /tmp/plan101-03-task1.log"; exit 1; fi; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ShellWebShareBanner.tsx exists and exports ShellWebShareBanner React component
    - Component file contains the literal string "Web sharing this shell will expose arbitrary command execution." (locked heading)
    - Component file contains the literal string "You are about to share '{sessionName}'"
    - Component file contains the literal string "Read-only viewers cannot type, but commands you run remain visible to them."
    - Component file contains the literal string "Enable web sharing"
    - Component file contains the literal string "Enabling…"
    - Component file contains the literal string "Cancel"
    - Component file contains the literal string "Dismiss security warning"
    - Component file contains role="alert" and aria-live="assertive"
    - styles.css contains .webgl-recovery-banner--shell-warning rule with border-left: 3px solid #f7768e
    - All 13 ShellWebShareBanner Vitest cases pass
    - pnpm tsc --noEmit exits 0
  </acceptance_criteria>
  <done>ShellWebShareBanner component is implemented, tested, and CSS-styled. Ready to be wired into App.tsx in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD — App.tsx web-toggle interception tests (RED) and implementation (GREEN)</name>
  <files>frontend/src/components/__tests__/App.shellWebShare.test.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx full (find handleToggleWeb or equivalent; find banner-stack render JSX; find DetectCLIs useEffect for the analogous mount pattern)
    - frontend/src/components/__tests__/App*.test.tsx (mirror the existing App-level test setup; check how Wails RPCs are mocked — likely vi.mock on the wailsjs module)
    - frontend/src/components/ShellWebShareBanner.tsx (Task 1 output)
    - .planning/phases/101-shell-session-surfaces-web-share-gating/101-UI-SPEC.md §Interaction States (the load-bearing flow diagram)
    - .planning/phases/101-shell-session-surfaces-web-share-gating/101-RESEARCH.md §2.5 + §8 (race mitigation)
  </read_first>
  <behavior>
    Step 1 RED: create App.shellWebShare.test.tsx asserting the interception contract. Test cases:

    1. "calls GetShellWebShareWarned on mount and stores result" — mock GetShellWebShareWarned to return false; mount App; assert the mock was called exactly once.
    2. "renders ShellWebShareBanner when shell session toggles web ON with warned=false" — mock state to include a shell session (cli='bash'); mock shellWebShareWarned=false; simulate the user clicking the Web On button; assert a ShellWebShareBanner appears in the DOM (queryByRole('alert') with the locked heading text).
    3. "does NOT call ToggleWebServing while banner is shown" — same setup as #2; after the click, ToggleWebServing mock was NOT called.
    4. "originating Web On button is aria-busy while banner is shown" — same setup; the button (queried by its label) has aria-busy='true' and is disabled.
    5. "on banner confirm: calls SetShellWebShareWarned(true) then ToggleWebServing(id, true)" — setup as #2; click "Enable web sharing"; assert both mocks called with correct args; SetShellWebShareWarned called BEFORE or in parallel with ToggleWebServing (mitigation per RESEARCH §8: also assert that the local React state shellWebShareWarned is updated synchronously to true before awaiting SetShellWebShareWarned's promise).
    6. "on banner cancel: neither SetShellWebShareWarned nor ToggleWebServing is called" — setup as #2; click Cancel; assert both mocks NOT called; banner unmounts.
    7. "after confirm flow completes, banner unmounts" — setup as #2; click Enable; resolve the RPC promises; assert queryByRole('alert') returns null.
    8. "shell session toggle ON with warned=true skips banner and calls ToggleWebServing directly" — setup with shellWebShareWarned=true; click Web On; assert NO banner appears; ToggleWebServing(id, true) called immediately.
    9. "AI CLI session toggle ON never triggers banner" — setup with cli='claude' and shellWebShareWarned=false; click Web On; assert NO banner; ToggleWebServing(id, true) called.
    10. "shell session toggle OFF never triggers banner" — setup with cli='bash' and existing webEnabled=true; click Web Off (enabled=false); assert NO banner; ToggleWebServing(id, false) called immediately.
    11. "second shell toggle ON after first confirm does not show banner" — flow: shell session A toggle ON → confirm → shell session B toggle ON; second toggle does NOT show banner (because shellWebShareWarned is now true in App state).
    12. "banner appears at top of banner stack" — render with multiple banners visible simultaneously (e.g., LocalNetworkBanner + ShellWebShareBanner); assert ShellWebShareBanner DOM node comes before the others within the .banner-stack container (use compareDocumentPosition or simple firstChild check).

    Step 2 GREEN: implement App.tsx changes.

    Mock setup pattern (top of test file):
      vi.mock('../../wailsjs/go/main/App', () => ({
        DetectCLIs: vi.fn(() => Promise.resolve([])),
        ListShells: vi.fn(() => Promise.resolve([])),
        GetShellWebShareWarned: vi.fn(() => Promise.resolve(false)),
        SetShellWebShareWarned: vi.fn(() => Promise.resolve()),
        ToggleWebServing: vi.fn(() => Promise.resolve()),
        // ... other RPCs the App needs to render — match existing App test setup
      }));
  </behavior>
  <action>
    Step 1: Create frontend/src/components/__tests__/App.shellWebShare.test.tsx with the 12 test cases. Mock the Wails RPC module via vi.mock. Use the existing App-level test helpers if they exist (look for App.saver.test.tsx or similar). The test file should call render(<App />) within a wrapper that mocks session state — likely you'll need to mock the session-list provider or render with seeded sessions via the existing mechanism.

    Step 2: Modify frontend/src/App.tsx:

    1. Add state:
       const [shellWebShareWarned, setShellWebShareWarned] = useState(false);
       const [pendingShellWebToggle, setPendingShellWebToggle] = useState<null | { sessionId: string; sessionName: string }>(null);

    2. Add a useEffect to read the persisted flag on mount:
       useEffect(() => {
         GetShellWebShareWarned().then(v => setShellWebShareWarned(v)).catch(err => {
           console.error('GetShellWebShareWarned failed:', err);
           setShellWebShareWarned(false);
         });
       }, []);

    3. Define the shell-set constant near the top of the App file (or import it from a shared module if one exists):
       const SHELL_CLIS = new Set(['shell', 'bash', 'zsh', 'pwsh', 'powershell']);

    4. Modify the existing handleToggleWeb (or equivalent function that fires ToggleWebServing). Insert interception:
       async function handleToggleWeb(id: string, enabled: boolean): Promise<void> {
         const session = sessions[id];  // adapt to existing session-state access
         if (enabled && session && SHELL_CLIS.has(session.cli) && !shellWebShareWarned) {
           setPendingShellWebToggle({ sessionId: id, sessionName: session.name });
           return;  // do NOT call ToggleWebServing; wait for banner resolution
         }
         await ToggleWebServing(id, enabled);
         // ... existing optimistic UI update
       }

    5. Add the banner handlers (place near handleToggleWeb):
       async function handleShellWebShareConfirm(): Promise<void> {
         if (!pendingShellWebToggle) return;
         const { sessionId } = pendingShellWebToggle;
         // RESEARCH §8 race mitigation: set local state synchronously BEFORE awaiting RPCs.
         setShellWebShareWarned(true);
         setPendingShellWebToggle(null);
         try {
           await Promise.all([
             SetShellWebShareWarned(true),
             ToggleWebServing(sessionId, true),
           ]);
         } catch (err) {
           console.error('Shell web-share confirm failed:', err);
           // Best-effort: roll back local state if persist fails so the banner re-shows on next toggle.
           setShellWebShareWarned(false);
         }
       }
       function handleShellWebShareCancel(): void {
         setPendingShellWebToggle(null);
       }

    6. Render ShellWebShareBanner at the TOP of the banner-stack JSX:
       <div className="banner-stack">
         {pendingShellWebToggle && (
           <ShellWebShareBanner
             sessionName={pendingShellWebToggle.sessionName}
             onConfirm={handleShellWebShareConfirm}
             onCancel={handleShellWebShareCancel}
           />
         )}
         {/* existing banners follow */}
       </div>

    7. Make the Web On button in DaemonManagerPanel/StatusBar aware of pendingShellWebToggle. The simplest approach: pass a derived prop `webToggleBusyFor: string | null` (= pendingShellWebToggle?.sessionId ?? null) to the child components. When the button's session id matches, render `aria-busy="true"` and disabled. If DaemonManagerPanel/StatusBar do NOT accept a prop yet, add it. NOTE: this touches DaemonManagerPanel.tsx and StatusBar.tsx — declare these in files_modified if the executor's read shows they need changes. If a single-line prop pass-through is enough (no test fixture for those components), the changes can stay minimal and not require their own test coverage in this plan (they'll be covered by App-level integration tests).

       FALLBACK: if changing DaemonManagerPanel/StatusBar is more invasive than expected, the aria-busy requirement can be satisfied by setting the state via App.tsx context that the components already consume; or by relying on the banner blocking the UI visually (it's at top of stack). The hard requirement is that ToggleWebServing is NOT called twice — Task 2 already enforces that via the interception. The aria-busy is a UX nicety; if it requires extensive plumbing, defer it and document in SUMMARY.md as "aria-busy not implemented in 101-03; banner-blocked UX is sufficient. Re-evaluate in future polish phase."

    8. Import ShellWebShareBanner from './components/ShellWebShareBanner' and import GetShellWebShareWarned, SetShellWebShareWarned from './wailsjs/go/main/App'.

    Run pnpm test for App.shellWebShare + pnpm tsc --noEmit.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- --run App.shellWebShare 2>&1 | tee /tmp/plan101-03-task2.log; TEST_EXIT=$?; if [ "$TEST_EXIT" -ne 0 ]; then echo "FAIL — see /tmp/plan101-03-task2.log"; exit 1; fi; pnpm test -- --run ShellWebShareBanner 2>&1 | tail -10; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/__tests__/App.shellWebShare.test.tsx exists with 12 named test cases
    - All 12 App.shellWebShare Vitest cases pass
    - All 13 ShellWebShareBanner cases still pass (no regression from Task 1)
    - frontend/src/App.tsx contains the literal "SHELL_CLIS" set definition (or equivalent inline check)
    - frontend/src/App.tsx contains "pendingShellWebToggle" state
    - frontend/src/App.tsx contains "GetShellWebShareWarned" and "SetShellWebShareWarned" imports
    - frontend/src/App.tsx renders ShellWebShareBanner element somewhere in JSX
    - frontend/src/App.tsx contains the comment or code reference to "race" mitigation per RESEARCH §8 (synchronous setShellWebShareWarned(true) before await SetShellWebShareWarned RPC)
    - pnpm tsc --noEmit exits 0
  </acceptance_criteria>
  <done>App.tsx intercepts shell-session web toggles. Banner appears at top of stack. Confirm fires both Wails RPCs in parallel after synchronous local-state update. Cancel/Esc/× all dismiss without side effects. SHELL-07 frontend defense-in-depth and SHELL-08 one-time confirmation closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cross-cutting verification — full frontend test suite + visual smoke notes</name>
  <files>frontend/src/components/__tests__/App.shellWebShare.test.tsx</files>
  <read_first>
    - Output of pnpm test (full Vitest run)
    - Output of pnpm tsc --noEmit
    - Output of go build ./... (the backend hasn't changed, but a smoke check guards against accidental ../ changes)
  </read_first>
  <behavior>
    After this task:
    - All Vitest cases in the frontend pass (including all 23 new cases from Plans 02 + 03).
    - TypeScript typecheck is clean.
    - Go build is unaffected.
    - No file outside the declared files_modified is touched.
    - Plan 04 (CLI + TUI) can run in Wave 2 in parallel with this plan and Plan 02 without conflict because it touches Go-only files (cmd_cli.go, internal/tui/*).
  </behavior>
  <action>
    Run the verification commands. If any test fails, fix it in the appropriate file from Tasks 1/2.

    Document any deviations from the locked copy or visual constants in the eventual SUMMARY.md.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- --run 2>&1 | tail -50 && pnpm tsc --noEmit && cd /Users/ken/dev/agenthub && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - Full Vitest run exits 0 with zero failures
    - pnpm tsc --noEmit exits 0
    - go build ./... exits 0
    - No files outside files_modified were edited (verify via git diff --name-only against the plan's files_modified list)
  </acceptance_criteria>
  <done>Plan 101-03 complete. ShellWebShareBanner exists, App.tsx intercepts toggles, banner appears at top of stack with locked copy. SHELL-07 and SHELL-08 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Banner Confirm click → daemon RPC pair | Two RPCs (SetShellWebShareWarned and ToggleWebServing) fire from a single user click. A race or partial failure could leave the user "warned=true" but with web sharing not actually enabled, or vice versa. Mitigation: Promise.all + try/catch with explicit rollback of local React state on failure. |
| Banner appearance → user action interception | The banner blocks the user's intended toggle until they Confirm or Cancel. A malicious page in the Wails webview could theoretically prevent the banner from mounting (e.g., via DOM manipulation). Accept: the Wails webview is same-origin with the desktop app; if it's compromised, the banner is the least of the user's problems. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-03-01 | Tampering | Banner copy is "improved" by future executor or LLM-driven refactor | mitigate | Locked-copy strings are verbatim in component file and asserted by Vitest tests #1, #3, #4. Any drift fails the test suite. Pattern proven in Phase 99 PUI-02. |
| T-101-03-02 | Elevation of Privilege | User dismisses banner once, then web-share is somehow enabled without confirmation | mitigate | App.tsx interception is the gate; ToggleWebServing is ONLY called after the user clicks Enable web sharing OR after shellWebShareWarned is already true. Test #3 explicitly asserts ToggleWebServing is NOT called while banner is shown. |
| T-101-03-03 | Information Disclosure | sessionName interpolated into body could XSS if it contains HTML | mitigate | React text-node rendering (no dangerouslySetInnerHTML). sessionName is rendered inside a {expression} which escapes by default. |
| T-101-03-04 | Denial of Service | Banner stuck in "Enabling…" state if RPC promise never resolves | accept | Promise.all in handleShellWebShareConfirm will eventually reject or hang on network failure. The pendingShellWebToggle state is set to null synchronously, so the banner unmounts regardless of RPC outcome. If RPC fails, error is logged and local state rolls back so banner shows again on next toggle attempt. |
| T-101-03-05 | Race condition | Two shell sessions toggle ON in rapid succession; second toggle's banner doesn't appear because first SetShellWebShareWarned is in-flight | mitigate | Local React state setShellWebShareWarned(true) fires SYNCHRONOUSLY before the await, so the second toggle's interception check (line: `if (enabled && ... && !shellWebShareWarned)`) sees the updated value. RESEARCH §8 mitigation. |
</threat_model>

<verification>
After all three tasks complete:

  cd frontend
  pnpm test -- --run                           # all Vitest passes (Plans 02 + 03 cases included)
  pnpm tsc --noEmit                            # no TS errors
  cd ..
  go build ./...                               # no backend regression

  # Locked-copy preservation (filter out comments):
  grep -c 'Web sharing this shell will expose arbitrary command execution' frontend/src/components/ShellWebShareBanner.tsx  # >=1
  grep -c 'You are about to share' frontend/src/components/ShellWebShareBanner.tsx                                          # >=1
  grep -c 'Enable web sharing' frontend/src/components/ShellWebShareBanner.tsx                                              # >=1
  grep -c 'Enabling…' frontend/src/components/ShellWebShareBanner.tsx                                                       # >=1
  grep -c 'Dismiss security warning' frontend/src/components/ShellWebShareBanner.tsx                                        # >=1
  grep -c 'webgl-recovery-banner--shell-warning' frontend/src/styles.css                                                    # >=1

  # Interception contract:
  grep -c 'SHELL_CLIS' frontend/src/App.tsx                            # >=1
  grep -c 'pendingShellWebToggle' frontend/src/App.tsx                 # >=2 (declaration + setter use)
  grep -c 'GetShellWebShareWarned' frontend/src/App.tsx                # >=1
  grep -c 'SetShellWebShareWarned' frontend/src/App.tsx                # >=1

All checks pass. SHELL-07 frontend gate active. SHELL-08 closed.
</verification>

<success_criteria>
- 5 files modified (1 new component, 2 new test files, App.tsx, styles.css); zero unexpected files touched
- All locked copy strings appear verbatim — banner heading, body, primary CTA, secondary CTA, "Enabling…", "Dismiss security warning"
- ShellWebShareBanner has role=alert and aria-live=assertive (NOT polite)
- Banner focuses Cancel on mount (safe action per QuitConfirmModal precedent)
- App.tsx interception is correct for all 5 paths: shell+ON+!warned (banner), shell+ON+warned (immediate), AI-CLI+ON (immediate), shell+OFF (immediate), AI-CLI+OFF (immediate)
- App.tsx renders banner at TOP of banner stack
- Race mitigation (RESEARCH §8) is in place: local state set synchronously before await
- Confirm flow: SetShellWebShareWarned(true) + ToggleWebServing(id, true) both fire; banner unmounts
- Cancel flow: neither RPC fires; banner unmounts; toggle returns to OFF
- All 25 new Vitest cases (13 banner + 12 App) pass; existing Vitest cases unaffected
- TypeScript clean, Go build clean
- SHELL-07 defense-in-depth complete; SHELL-08 fully closed
</success_criteria>

<output>
After completion, create .planning/phases/101-shell-session-surfaces-web-share-gating/101-03-SUMMARY.md documenting:
- ShellWebShareBanner component file path and prop signature
- The banner-stack render ordering decision (slot-based vs sort-on-push — RESEARCH §8 recommended slot-based; document which was used)
- The race mitigation pattern (synchronous setShellWebShareWarned(true) before await)
- The shell-cli set duplication ('SHELL_CLIS' constant in App.tsx, also defined in TabBar.tsx via agentBadgeModifier, also in engine.go isShellSession). Note this as acceptable v3.3 duplication per RESEARCH §8 Pitfall "isShellSession constant drift". If a pattern emerges in v3.4, consider extracting to a shared module.
- Disposition of the aria-busy on Web On button decision (implemented in DaemonManagerPanel/StatusBar OR deferred per fallback in Task 2 §7).
- Confirmation that SHELL-07 (frontend gate) and SHELL-08 (one-time banner) are closed; daemon-side SHELL-07 was already satisfied by Phase 87 SEC-01 (api.go:407 no-auto-enable).
</output>
</content>
