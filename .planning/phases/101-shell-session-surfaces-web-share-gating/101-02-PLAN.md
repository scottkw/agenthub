---
phase: 101-shell-session-surfaces-web-share-gating
plan: 02
type: execute
wave: 2
depends_on:
  - 101-01
files_modified:
  - frontend/src/components/NewSessionModal.tsx
  - frontend/src/components/TabBar.tsx
  - frontend/src/App.tsx
  - frontend/src/components/__tests__/NewSessionModal.test.tsx
  - frontend/src/components/__tests__/TabBar.test.tsx
  - frontend/src/styles.css
autonomous: true
requirements:
  - SHELL-01
  - SHELL-06

must_haves:
  truths:
    - "NewSessionModal renders one shell row per DetectedShell prop entry, AFTER all AI CLI rows, with no visible section divider"
    - "Each shell row displays two lines: 'Shell — <displayName>' (13px) and the resolved path (11px mono, color #565f89)"
    - "System default shell (name='shell') renders as the FIRST shell row (above bash/zsh/pwsh/powershell)"
    - "Selected shell row uses border-color #89ddff (shell cyan), NOT the AI-CLI accent blue #7aa2f7"
    - "Selected AI CLI row continues to use border-color #7aa2f7 (no regression)"
    - "When shellsLoading is true and shells is empty, a loading skeleton row renders below the AI CLI list with text 'Loading shells…' (color #565f89, 11px)"
    - "When ListShells() resolves with zero entries, no shell rows and no skeleton render (silent absence per UI-SPEC §Edge Cases)"
    - "When shell row is selected, the args text field is disabled with placeholder 'Arguments are not passed to shell sessions'"
    - "Args memory uses key namespace 'agenthub:args:shell:<name>' when a shell is selected (separate from AI CLI's 'agenthub:args:<cli>')"
    - "TabBar renders an 8px .tab__agent-badge dot to the LEFT of the tab name and to the RIGHT of the status dot (4px gap)"
    - "Tab agent badge has aria-hidden='true' (decorative)"
    - "Tab badge color for cli in {shell, bash, zsh, pwsh, powershell} resolves to #89ddff via .tab__agent-badge--shell"
    - "Tab badge color for the 6 existing AI CLIs (claude, opencode, codex, gemini, cursor, aider) resolves to their respective TokyoNight dark-mode hex per UI-SPEC §Color"
    - "Tab badge color for unknown CLI string falls back to muted #9aa5ce"
    - "Tab hover tooltip extends to include agent type, e.g., '<tab-name> (Shell — bash)' for shell sessions"
    - "App.tsx invokes ListShells() on mount and passes the result plus loading state to NewSessionModal"
    - "Per D-02: shell entries render as separate rows (one per DetectedShell), not a single 'Shell' button with sub-options"
  artifacts:
    - path: frontend/src/components/NewSessionModal.tsx
      provides: "Shell row rendering, 2-line layout, selected-state cyan border, loading skeleton, args-field disable when shell selected, shell-namespaced args memory"
      contains: "shells.map"
    - path: frontend/src/components/TabBar.tsx
      provides: ".tab__agent-badge element + agent-type tooltip suffix"
      contains: "tab__agent-badge"
    - path: frontend/src/App.tsx
      provides: "ListShells() call + shells/shellsLoading state passed to NewSessionModal"
      contains: "ListShells"
    - path: frontend/src/styles.css
      provides: "CSS for .tab__agent-badge + per-agent modifiers + .new-session-modal__agent-btn__detail + shell-row selected border"
      contains: ".tab__agent-badge"
    - path: frontend/src/components/__tests__/NewSessionModal.test.tsx
      provides: "Vitest cases for shell rows, selection cyan border, loading skeleton, args-disable, args-namespace"
      contains: "Shell row"
    - path: frontend/src/components/__tests__/TabBar.test.tsx
      provides: "Vitest cases for agent badge per CLI, shell badge, unknown fallback, tooltip suffix"
      contains: "tab__agent-badge"
  key_links:
    - from: frontend/src/App.tsx
      to: frontend/src/wailsjs/go/main/App.ListShells
      via: "useEffect calls ListShells() and stores result in shells state"
      pattern: "ListShells\\("
    - from: frontend/src/components/NewSessionModal.tsx
      to: frontend/src/wailsjs/go/models.daemon.DetectedShell
      via: "DetectedShell type import + shells prop typing"
      pattern: "DetectedShell"
    - from: frontend/src/components/NewSessionModal.tsx
      to: localStorage
      via: "args namespace key agenthub:args:shell:<name>"
      pattern: "agenthub:args:shell:"
    - from: frontend/src/components/TabBar.tsx
      to: frontend/src/styles.css
      via: ".tab__agent-badge BEM class with --<agent> modifier"
      pattern: "tab__agent-badge--"
---

<objective>
Surface shell sessions in the GUI NewSessionModal and TabBar (SHELL-01 GUI half + SHELL-06 GUI half), and wire App.tsx to call ListShells() on mount and thread the result through. This plan only adds the agent picker + tab badge — the web-share confirmation banner lives in plan 03.

Purpose: Without this plan, the user has no way to pick a shell from the GUI even though Phase 100's daemon API and Plan 101-01's Wails bindings are in place. Per UI-SPEC, copy and color are locked verbatim — executor MUST NOT paraphrase the "Shell — name" prefix to "Shell:" or shorten anything.

Output: 3 component files modified (NewSessionModal.tsx, TabBar.tsx, App.tsx), 1 stylesheet modified (styles.css), 2 test files extended. Zero new components. The CSS uses existing BEM modifiers + a new .tab__agent-badge element + a new .new-session-modal__agent-btn__detail child class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/101-shell-session-surfaces-web-share-gating/101-UI-SPEC.md
@.planning/phases/101-shell-session-surfaces-web-share-gating/101-RESEARCH.md
@.planning/phases/101-shell-session-surfaces-web-share-gating/101-CONTEXT.md
@.planning/phases/101-shell-session-surfaces-web-share-gating/101-01-SUMMARY.md
@frontend/src/components/NewSessionModal.tsx
@frontend/src/components/TabBar.tsx
@frontend/src/App.tsx
@frontend/src/components/__tests__/NewSessionModal.test.tsx
@frontend/src/components/__tests__/TabBar.test.tsx
@frontend/src/styles.css

<interfaces>
Plan 101-01 Wails bindings (in place after Wave 1):

From frontend/src/wailsjs/go/main/App.d.ts:
  export function ListShells(): Promise<Array<daemon.DetectedShell>>;

From frontend/src/wailsjs/go/models.ts:
  export namespace daemon {
    export class DetectedShell {
      name: string;         // "bash" | "zsh" | "pwsh" | "powershell" | "shell"
      displayName: string;  // "bash" | "zsh" | "PowerShell" | "Windows PowerShell" | "system default"
      path: string;         // absolute path
      argv: string[];       // ["-i"] or ["-NoLogo"]
    }
  }

Locked visual constants (UI-SPEC §Locked Visual Constants):

| Token | Value |
|-------|-------|
| Shell badge color (dark) | #89ddff |
| Shell badge color (light) | #3d5a80 |
| Agent badge diameter | 8px |
| Shell row height (modal) | 56px |
| AI CLI row height (modal) | 36px |
| Em-dash prefix | "Shell — " verbatim (U+2014) |
| Modal agent-list gap | 8px |

Locked color set for tab badges (UI-SPEC §Color):

| CLI | Dark hex (used in GUI) |
|-----|------------------------|
| claude   | #7aa2f7 |
| opencode | #9ece6a |
| codex    | #bb9af7 |
| gemini   | #2ac3de |
| cursor   | #e0af68 |
| aider    | #f7768e |
| shell    | #89ddff |
| (unknown) | #9aa5ce |

Locked copy (UI-SPEC §Copywriting):
- Shell agent row primary label: "Shell — DISPLAYNAME" (em-dash, NOT colon)
  - System default: "Shell — system default"
  - bash: "Shell — bash"
  - zsh: "Shell — zsh"
  - pwsh: "Shell — PowerShell"
  - powershell: "Shell — Windows PowerShell"
- Shell row secondary label: the resolved path, mono font, color #565f89, 11px
- Args-disabled placeholder: "Arguments are not passed to shell sessions"
- Loading skeleton text: "Loading shells…" (U+2026 ellipsis, NOT three periods)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — NewSessionModal shell rows + TabBar agent badge tests (RED)</name>
  <files>frontend/src/components/__tests__/NewSessionModal.test.tsx, frontend/src/components/__tests__/TabBar.test.tsx</files>
  <read_first>
    - frontend/src/components/NewSessionModal.tsx (current props shape + DetectedCLI usage)
    - frontend/src/components/TabBar.tsx (current rendering — find the status-dot element + tab-name layout)
    - frontend/src/components/__tests__/NewSessionModal.test.tsx (Vitest patterns: render helper, fireEvent usage, localStorage mocking)
    - frontend/src/components/__tests__/TabBar.test.tsx (existing tab-rendering tests for setup style)
    - .planning/phases/101-shell-session-surfaces-web-share-gating/101-UI-SPEC.md §Component Contracts: NewSessionModal + TabBar
    - .planning/phases/101-shell-session-surfaces-web-share-gating/101-UI-SPEC.md §Test Surface Map (required test names and assertions)
  </read_first>
  <behavior>
    After this task, Vitest cases exist and FAIL (because implementation lands in Task 2 and Task 3):

    NewSessionModal.test.tsx additions (mount with the existing test render helper; pass a shells={[...]} prop containing test fixtures):
    1. "renders one row per shell with Shell em-dash prefix" — pass 3 fixtures (system default + bash + zsh), assert 3 buttons whose primary text matches "Shell — system default", "Shell — bash", "Shell — zsh" exactly (verbatim, em-dash, no colon).
    2. "system default shell row appears first" — pass [bash, system-default, zsh] in arbitrary input order, assert the first shell button (after all AI CLI buttons) reads "Shell — system default".
    3. "shell row shows resolved path as mono secondary line" — assert the bash row contains an element with class containing "__detail" and text "/bin/bash" (fixture path).
    4. "selecting shell row applies shell cyan border modifier" — click the bash shell button, assert it gains aria-pressed=true AND its class includes "--selected-shell" (the modifier added in Task 2).
    5. "selecting AI CLI row applies accent blue border modifier not shell cyan" — regression guard: click claude, assert it gains aria-pressed=true AND class includes "--selected" but NOT "--selected-shell".
    6. "loading skeleton renders when shellsLoading is true and shells is empty" — pass shellsLoading=true and shells=[], assert an element with text "Loading shells…" exists.
    7. "no shell rows when shells prop is empty and not loading" — pass shellsLoading=false and shells=[], assert zero buttons whose text starts with "Shell —".
    8. "args field disabled when shell selected" — click a shell row, query for the args input by its existing label, assert it is disabled AND its placeholder is "Arguments are not passed to shell sessions".
    9. "args field enabled when AI CLI selected" — regression guard: click claude row, assert args input is enabled.
    10. "args namespace key uses shell prefix when shell selected" — seed localStorage["agenthub:args:shell:bash"]="--login" BEFORE selecting bash; assert the AI-CLI key "agenthub:args:bash" is NOT consulted for shell sessions (prevents cross-contamination). If args field is disabled for shells, the test asserts the namespace lookup behavior via a side-effect probe of localStorage.getItem mocking.

    TabBar.test.tsx additions:
    11. "renders agent badge for claude session" — pass a tab fixture with cli='claude', assert the rendered tab contains an element with class "tab__agent-badge" AND modifier class containing "claude".
    12. "renders shell agent badge for cli=shell" — fixture cli='shell', assert badge has modifier "--shell".
    13. "renders shell agent badge for cli=bash" — fixture cli='bash', assert badge has modifier "--shell" (NOT "--bash").
    14. "renders shell agent badge for cli=zsh" — fixture cli='zsh', assert badge has modifier "--shell".
    15. "renders shell agent badge for cli=pwsh" — fixture cli='pwsh', assert badge has modifier "--shell".
    16. "renders shell agent badge for cli=powershell" — fixture cli='powershell', assert badge has modifier "--shell".
    17. "falls back to muted badge for unknown cli" — fixture cli='unknown-tool', assert badge exists but has NO agent-specific modifier among the 7 known modifiers.
    18. "agent badge is aria-hidden" — assert the badge element has aria-hidden="true".
    19. "tab tooltip includes agent type for shell session" — assert the tab's title (or aria-label) for a shell session contains "Shell — bash" (or whatever the agent display form is — match UI-SPEC).
    20. "tab agent badge appears between status dot and tab name" — assert DOM order: status-dot element comes before badge element, badge before tab-name element.
  </behavior>
  <action>
    Append the 10 NewSessionModal cases and 10 TabBar cases described in the behavior block to the respective test files. Use the existing render helpers (renderWithDefaults or similar — read the current test file to find the helper name).

    For fixtures, define a local helper at the top of each test file. Import DetectedShell from '../../wailsjs/go/models' (adjust relative path to match the test file's location).

    For TabBar, use a similar makeTab(overrides) helper that produces the tab/session shape the component currently expects. Read the existing tests to see the exact shape.

    Do NOT yet modify the components — these tests MUST fail (RED). Run the test command and confirm the failures are about missing rendering (e.g., text not found for "Shell — bash" or class "tab__agent-badge"), not about syntax errors.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- --run --reporter=verbose NewSessionModal TabBar 2>&1 | tee /tmp/plan101-02-task1.log; TEST_EXIT=$?; if [ "$TEST_EXIT" -eq 0 ]; then echo "FAIL: tests passed but should have failed RED"; exit 1; fi; grep -qE "Shell — bash|tab__agent-badge|Loading shells" /tmp/plan101-02-task1.log && echo "RED confirmed" || (echo "FAIL: test failures do not reference the expected behavior"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/__tests__/NewSessionModal.test.tsx contains 10 new test cases by name (each name from the behavior block)
    - frontend/src/components/__tests__/TabBar.test.tsx contains 10 new test cases by name
    - Vitest run exits NON-ZERO (RED phase confirmed)
    - Test failure messages reference "Shell —", "tab__agent-badge", or "Loading shells" — confirming the failure is about unimplemented behavior, not a syntax error
  </acceptance_criteria>
  <done>20 new Vitest cases exist. Test runs fail with assertion errors targeted at unimplemented behavior. Task 2 and Task 3 turn them green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement NewSessionModal shell rows + App.tsx ListShells wiring + CSS (GREEN — modal tests pass)</name>
  <files>frontend/src/components/NewSessionModal.tsx, frontend/src/App.tsx, frontend/src/styles.css</files>
  <read_first>
    - frontend/src/components/NewSessionModal.tsx full (props, agent-list render block, args input, selection state)
    - frontend/src/App.tsx (where NewSessionModal is instantiated; where DetectCLIs state lives)
    - frontend/src/styles.css (find .new-session-modal__agent-btn block; note the existing --selected modifier shape)
    - .planning/phases/101-shell-session-surfaces-web-share-gating/101-UI-SPEC.md §Component Contracts: NewSessionModal + §Layout Specifications: modal mockup
    - .planning/phases/101-shell-session-surfaces-web-share-gating/101-UI-SPEC.md §Locked Visual Constants
  </read_first>
  <behavior>
    After this task, NewSessionModal.test.tsx's 10 new cases all pass:
    - NewSessionModal accepts two new optional props: shells (default []) and shellsLoading (default false).
    - The agent-list render block: AI CLI rows first (unchanged), then either shell rows (when shells.length greater than 0) OR a loading skeleton row (when shellsLoading true and shells empty). When both are false/empty, nothing extra renders (silent absence).
    - Shell rows are sorted client-side: the entry with name === "shell" (system default) comes first, then the rest in server-provided order.
    - Each shell row is a .new-session-modal__agent-btn button with TWO children: a primary span (existing class, default content) showing "Shell — DISPLAYNAME", and a new .new-session-modal__agent-btn__detail span showing the resolved path (mono, 11px, color #565f89). The button's min-height grows from 36px to 56px when rendering the 2-line layout.
    - Selected shell row: aria-pressed=true AND the button has a new modifier class .new-session-modal__agent-btn--selected-shell which applies border-color #89ddff.
    - Selected AI CLI row continues to use the existing --selected modifier (border-color #7aa2f7) — no regression.
    - When a shell is selected, the args input is disabled with placeholder "Arguments are not passed to shell sessions".
    - When an AI CLI is selected, args input retains existing enabled behavior.
    - Args memory key changes namespace based on selection: AI CLI uses existing "agenthub:args:CLI", shell uses "agenthub:args:shell:NAME".
    - App.tsx calls ListShells() on mount, stores result in shells state, tracks loading state in shellsLoading. Passes both into NewSessionModal.
  </behavior>
  <action>
    NewSessionModal.tsx:
    1. Import DetectedShell type from '../wailsjs/go/models' (check existing import style for namespace vs type-only).
    2. Extend props interface with shells and shellsLoading (both optional). Default both to [] and false via destructure defaults.
    3. Add a client-side sort: useMemo over shells, putting name === "shell" first.
    4. After the existing clis.map render block, append the shell rows mapping. Use a prefix scheme for selection state: AI CLIs stored as plain name, shells stored as "shell:NAME". The rendered primary label is the literal string "Shell — " + displayName (use Unicode em-dash U+2014 directly in JSX text — easier than HTML entities).
    5. Render a loading skeleton row (.new-session-modal__agent-btn--loading div with text "Loading shells…") only when shellsLoading is true AND sortedShells.length === 0.
    6. Update args-input behavior: disabled when selectedAgent starts with "shell:"; placeholder per locked copy. Args namespace lookup uses "agenthub:args:shell:NAME" for shells.
    7. For the create-session call: strip the "shell:" prefix before passing cli to the daemon (so the daemon receives "bash" / "zsh" / "shell", not "shell:bash").

    App.tsx:
    1. Add state: shells (DetectedShell[]) and shellsLoading (boolean, initial true).
    2. In a useEffect (mirror the existing DetectCLIs pattern), call ListShells() and store result. Use .catch to log + set shells=[]; .finally to set shellsLoading=false.
    3. Import ListShells from './wailsjs/go/main/App' and daemon namespace from './wailsjs/go/models'.
    4. Pass shells and shellsLoading props to NewSessionModal at its instantiation site.

    styles.css — append:

    .new-session-modal__agent-btn--selected-shell {
      border-color: #89ddff;
      background: #1e2030;
    }
    .new-session-modal__agent-btn__detail {
      display: block;
      font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
      font-size: 11px;
      color: #565f89;
      line-height: 1.3;
      margin-top: 4px;
    }
    .new-session-modal__agent-btn--loading {
      color: #565f89;
      font-size: 11px;
      opacity: 0.7;
      pointer-events: none;
    }
    .new-session-modal__agent-btn:has(.new-session-modal__agent-btn__detail) {
      min-height: 56px;
      align-items: flex-start;
      flex-direction: column;
      gap: 4px;
    }

    If :has() is not supported by the project's Wails WebView target, add a .new-session-modal__agent-btn--shell modifier to the React button and target via that class instead (apply unconditionally on every shell row in the React render).

    Run pnpm tsc --noEmit after to catch type errors.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- --run NewSessionModal 2>&1 | tee /tmp/plan101-02-task2.log; TEST_EXIT=$?; if [ "$TEST_EXIT" -ne 0 ]; then echo "FAIL — see /tmp/plan101-02-task2.log"; exit 1; fi; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/NewSessionModal.tsx imports DetectedShell from wailsjs/go/models
    - frontend/src/components/NewSessionModal.tsx contains the literal token "shells.map" OR "sortedShells.map"
    - frontend/src/components/NewSessionModal.tsx contains the literal string "Shell — " (em-dash U+2014)
    - frontend/src/components/NewSessionModal.tsx contains "agenthub:args:shell:"
    - frontend/src/components/NewSessionModal.tsx contains the literal placeholder string "Arguments are not passed to shell sessions"
    - frontend/src/components/NewSessionModal.tsx contains "Loading shells" (U+2026 ellipsis or literal three-dot is acceptable for the grep gate, but the test asserts U+2026)
    - frontend/src/App.tsx contains ListShells reference (import + call)
    - frontend/src/styles.css contains .new-session-modal__agent-btn--selected-shell rule with border-color #89ddff
    - All 10 NewSessionModal Vitest cases pass
    - pnpm tsc --noEmit exits 0
  </acceptance_criteria>
  <done>NewSessionModal shows shell rows with locked copy, locked colors, locked sizes. App.tsx wires ListShells. Task 3 still pending for TabBar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement TabBar agent badge + tooltip + CSS for badge palette (GREEN — TabBar tests pass)</name>
  <files>frontend/src/components/TabBar.tsx, frontend/src/styles.css</files>
  <read_first>
    - frontend/src/components/TabBar.tsx full (status-dot element location, tab-name rendering, existing tooltip/title attribute)
    - frontend/src/styles.css (find .tab__status block for the existing 8px dot pattern to mirror)
    - .planning/phases/101-shell-session-surfaces-web-share-gating/101-UI-SPEC.md §Component Contracts: TabBar + §Layout Specifications: tab bar mockup
    - .planning/phases/101-shell-session-surfaces-web-share-gating/101-UI-SPEC.md §Color: full agent badge palette
  </read_first>
  <behavior>
    After this task, TabBar.test.tsx's 10 new cases all pass:
    - Each rendered tab contains a new element with class "tab__agent-badge" plus an agent-specific modifier class derived from the tab's cli field.
    - The agent badge is 8px x 8px circle, positioned 4px after the status dot and 4px before the tab name (DOM order: status-dot, agent-badge, tab-name).
    - The agent badge has aria-hidden="true".
    - Tab tooltip (title attribute) is extended: for shell sessions, includes "(Shell — DISPLAYNAME)" suffix; for AI CLIs, includes "(CLI Name)" suffix.
    - Color resolution by cli field:
      - claude → --claude (#7aa2f7)
      - opencode → --opencode (#9ece6a)
      - codex → --codex (#bb9af7)
      - gemini → --gemini (#2ac3de)
      - cursor → --cursor (#e0af68)
      - aider → --aider (#f7768e)
      - shell, bash, zsh, pwsh, powershell → --shell (#89ddff)
      - anything else → no agent modifier (badge renders with default muted color #9aa5ce via base class)
  </behavior>
  <action>
    TabBar.tsx:
    1. Add a helper function (place at module top or inside the component):

       function agentBadgeModifier(cli: string): string | null {
         switch (cli) {
           case 'claude':
           case 'opencode':
           case 'codex':
           case 'gemini':
           case 'cursor':
           case 'aider':
             return cli;
           case 'shell':
           case 'bash':
           case 'zsh':
           case 'pwsh':
           case 'powershell':
             return 'shell';
           default:
             return null;
         }
       }

       function agentDisplayName(cli: string): string {
         const shellMap: Record<string, string> = {
           shell: 'system default', bash: 'bash', zsh: 'zsh', pwsh: 'PowerShell', powershell: 'Windows PowerShell',
         };
         if (shellMap[cli] !== undefined) return 'Shell — ' + shellMap[cli];
         const cliMap: Record<string, string> = {
           claude: 'Claude Code', opencode: 'OpenCode', codex: 'OpenAI Codex',
           gemini: 'Gemini CLI', cursor: 'Cursor', aider: 'Aider',
         };
         return cliMap[cli] ?? cli;
       }

    2. In the tab's JSX, between the existing .tab__status element and the tab-name element, insert:

       const modifier = agentBadgeModifier(tab.cli);
       const badgeClass = modifier
         ? `tab__agent-badge tab__agent-badge--${modifier}`
         : 'tab__agent-badge';
       // ...
       <span className={badgeClass} aria-hidden="true" />

    3. Extend the tab's existing title attribute (or aria-label if title is not used today) to append " (DISPLAYNAME)" — e.g., `${tab.name} (${agentDisplayName(tab.cli)})`. Verify the existing title shape first; if the tab has no title at all, add one.

    styles.css — append:

    .tab__agent-badge {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: #9aa5ce; /* default muted for unknown */
      flex-shrink: 0;
      margin-left: 4px;
      margin-right: 4px;
    }
    .tab__agent-badge--claude   { background: #7aa2f7; }
    .tab__agent-badge--opencode { background: #9ece6a; }
    .tab__agent-badge--codex    { background: #bb9af7; }
    .tab__agent-badge--gemini   { background: #2ac3de; }
    .tab__agent-badge--cursor   { background: #e0af68; }
    .tab__agent-badge--aider    { background: #f7768e; }
    .tab__agent-badge--shell    { background: #89ddff; }

    Run pnpm tsc --noEmit and pnpm test to confirm GREEN.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- --run TabBar 2>&1 | tee /tmp/plan101-02-task3.log; TEST_EXIT=$?; if [ "$TEST_EXIT" -ne 0 ]; then echo "FAIL — see /tmp/plan101-02-task3.log"; exit 1; fi; pnpm test -- --run NewSessionModal 2>&1 | tail -20; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/TabBar.tsx contains "tab__agent-badge" (the new element class)
    - frontend/src/components/TabBar.tsx contains agentBadgeModifier function (or equivalent inline switch)
    - frontend/src/components/TabBar.tsx contains aria-hidden="true" on the badge element
    - frontend/src/styles.css contains all 7 .tab__agent-badge--AGENT rules (claude/opencode/codex/gemini/cursor/aider/shell)
    - frontend/src/styles.css contains background #89ddff for --shell
    - All 10 TabBar Vitest cases pass
    - All 10 NewSessionModal cases still pass (no regression)
    - pnpm tsc --noEmit exits 0
  </acceptance_criteria>
  <done>TabBar renders agent badges for every known CLI per the locked palette. Shell sessions are visually distinguished. Tooltip suffix includes agent type. Plans 03 and 04 can now reference selectors like .tab__agent-badge--shell for their own assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ListShells() Wails RPC result → React render | Daemon-provided strings (name, displayName, path) are rendered into the modal. A malicious daemon payload could attempt XSS via the path field. Mitigation: React's default text-node escaping prevents script injection; the path is rendered as text inside a span, never as innerHTML. |
| localStorage args namespace → user input | User-typed args are persisted under "agenthub:args:shell:NAME". For shells, the args field is disabled, so user input cannot enter that key via this UI in Phase 101. The namespace is read-only for shells in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-02-01 | Tampering | DetectedShell.path rendered in mono detail span | mitigate | React text-node rendering (no dangerouslySetInnerHTML). Path is treated as opaque display string; no shell-eval or URL navigation occurs from it. |
| T-101-02-02 | Spoofing | A malicious cli string sneaking past agentBadgeModifier and rendering a misleading badge color | accept | The unknown-cli fallback uses the muted default color, not a specific agent color. No spoofing possible without daemon compromise (and daemon compromise has bigger problems). |
| T-101-02-03 | Information Disclosure | Tab tooltip displays agent type to anyone with desktop access | accept | Same trust posture as the rest of the desktop UI. No new disclosure surface. |
</threat_model>

<verification>
After all three tasks complete:

  cd frontend
  pnpm test -- --run NewSessionModal TabBar
  pnpm tsc --noEmit

  # Verify locked tokens present (filter comments):
  grep -c 'Shell — ' frontend/src/components/NewSessionModal.tsx           # ≥1 (verbatim copy)
  grep -c 'agenthub:args:shell:' frontend/src/components/NewSessionModal.tsx # ≥1
  grep -c 'Arguments are not passed to shell sessions' frontend/src/components/NewSessionModal.tsx # ≥1
  grep -c 'tab__agent-badge' frontend/src/components/TabBar.tsx             # ≥1
  grep -c '#89ddff' frontend/src/styles.css                                  # ≥2 (selected-shell border + --shell badge)
  grep -c '#7aa2f7' frontend/src/styles.css                                  # ≥1 (claude badge — verify no regression)

All tests pass. No file outside files_modified is touched.
</verification>

<success_criteria>
- 6 files modified (3 components, 1 stylesheet, 2 test files); zero new files
- All 20 new Vitest cases pass (10 modal + 10 tab bar)
- No existing Vitest case regresses
- TypeScript typechecks clean (pnpm tsc --noEmit exit 0)
- All locked tokens from UI-SPEC §Locked Visual Constants appear verbatim: "Shell — " prefix, #89ddff color, "Loading shells…" text, "Arguments are not passed to shell sessions" placeholder
- Tab agent badge order is correct: status-dot, agent-badge, tab-name
- SHELL-01 GUI half closed (user can pick shell in modal); SHELL-06 GUI half closed (distinct badge color in tab bar)
- Plan 101-03 unblocked: it consumes the same selectedAgent state but only adds banner gating to the web-share toggle; it does NOT touch NewSessionModal or TabBar
</success_criteria>

<output>
After completion, create .planning/phases/101-shell-session-surfaces-web-share-gating/101-02-SUMMARY.md documenting:
- New NewSessionModal props (shells, shellsLoading) and the selectedAgent prefix scheme ("shell:NAME" for shells)
- New CSS classes (.tab__agent-badge, .tab__agent-badge--AGENT, .new-session-modal__agent-btn--selected-shell, .new-session-modal__agent-btn__detail, .new-session-modal__agent-btn--loading)
- Helper functions added to TabBar.tsx (agentBadgeModifier, agentDisplayName)
- Locked-copy strings rendered verbatim (full list with line:column references)
- Args-namespace contract (shell sessions use "agenthub:args:shell:NAME", AI CLIs unchanged)
- Confirmation that SHELL-01 (GUI half) and SHELL-06 (GUI half) are closed by this plan; the CLI halves of SHELL-02/06 are closed by Plan 04; the TUI half of SHELL-03/06 is closed by Plan 04
</output>
</content>
