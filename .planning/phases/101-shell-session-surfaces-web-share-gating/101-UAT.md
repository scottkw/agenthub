---
status: complete
phase: 101-shell-session-surfaces-web-share-gating
source:
  - .planning/phases/101-shell-session-surfaces-web-share-gating/101-VERIFICATION.md
started: 2026-05-16T16:17:20Z
updated: 2026-05-16T17:10:00Z
---

## Current Test

[testing complete]

## Tests

### 1. GUI new-session modal — visual layout and copy
expected: |
  Open the new-session modal. AI CLI rows appear first (Claude Code, OpenCode, OpenAI Codex, Gemini CLI). After them, shell rows appear without a divider. The first shell row reads "Shell — system default" with the resolved path below in mono. Subsequent rows read "Shell — bash", "Shell — zsh" (and "Shell — PowerShell", "Shell — Windows PowerShell" on Windows). Em-dash is U+2014, NOT a colon. Selected shell row has a cyan (#89ddff) border, NOT the AI-CLI blue (#7aa2f7).
result: skipped
reason: "Superseded by Phase 107 SHELL-10 — multi-shell rows collapsed to a single 'Shell' entry with a Settings-driven binary path picker. The current GUI new-session modal contract is owned by Phase 107 (107-UI-SPEC.md / 107-UI-REVIEW.md), not Phase 101. Test text describes pre-107 design."

### 2. GUI tab bar — agent badge visual continuity
expected: |
  Create one of each agent type. Each tab carries an 8px circular agent badge to the LEFT of the tab name and to the RIGHT of the existing status dot, with 4px gap between dots. Shell sessions render cyan (#89ddff). The 6 AI CLIs render their existing dark-mode colors. Hovering a shell tab shows tooltip text like "Double-click or right-click to rename · Shell — bash".
result: pass
evidence: |
  Source-level verification (user is colorblind; visual color differentiation verified at source):
    - 8px circular badge: frontend/src/style.css:1042-1044 (width:8px;height:8px;border-radius:50%)
    - 4px gap: style.css:1045 (margin-right: 4px)
    - Position status→badge→name: TabBar.tsx:193-217
    - Shell cyan #89ddff: style.css:1055
    - 6 distinct AI-CLI colors: style.css:1049-1054 (claude/opencode/codex/gemini/cursor/aider all distinct hex)
    - Shell variants collapse to single 'shell' modifier: TabBar.tsx:18-36 (agentBadgeModifier)
    - Tooltip em-dash suffix: TabBar.tsx:177-179
  User screenshot also confirmed the two-dot pattern is present and structurally correct on a live shell session tab.

### 3. TUI agent picker cycle order
expected: |
  Launch TUI (./agenthub --tui). Open new-session modal. Cycle Right through agents. Order should be: Claude Code → OpenAI Codex → Gemini CLI → OpenCode → Shell — system default → Shell — bash → Shell — zsh → (wraps to first AI CLI). Shell sessions in the session-list render a slate-cyan badge color distinct from the 6 AI badges.
result: pass
notes: |
  User confirmed cycle order verbatim: Claude Code → OpenAI Codex → Gemini CLI → OpenCode → Shell — system default → Shell — bash → Shell — zsh → wraps to Claude Code. Matches Phase 101's documented contract exactly.

  Launch command correction for future runs: TUI is a subcommand, not a flag — use `./build/bin/agenthub.app/Contents/MacOS/agenthub tui` (NOT `--tui`). The flag form falls through to Wails GUI startup and freezes without a TTY.

  Color portion verified at source (user is colorblind): see Gaps section follow-up — TUI shell badge color resolution is in internal/tui/styles.go (Phase 101-02 commit). Cycle code confirms collapse-to-shell badge: see internal/tui/modal.go:26 ordering and update_test.go:1153-1165 cycle assertion.

  Follow-up: user flagged that TUI (and CLI) should match the GUI's Phase 107 collapse — a single "Shell" entry with the binary selected via Settings, instead of 3 rows (system default / bash / zsh). Phase 107 was GUI-only by design. Captured in Gaps as a v3.4 backlog item.

### 4. CLI smoke — agenthub new shell
expected: |
  Run ./agenthub new shell ~/tmp. Session UUID prints to stdout. agenthub list shows the new session with shell agent. Run ./agenthub new shell --shell=zsh and confirm cli=zsh. Run ./agenthub new shell --shell=nope and confirm exit 1 with locked stderr: agenthub new shell: unknown shell "nope" (allowed: bash, zsh, pwsh, powershell, or omit for system default).
result: skipped
reason: "CLI surface contract (--shell flag, allowed-shell error string) is being reworked by the new Phase 108 (TUI/CLI shell collapse — release blocker for v3.3). Re-test against Phase 108's new contract once it ships; current contract is no longer the target."

### 5. GUI shell web-share banner — visual fidelity and first-toggle flow
expected: |
  Create a shell session (e.g. "Shell — bash"). On the daemon manager panel, click Web On for that session. A red-bordered banner appears at the TOP of the banner stack with heading "Web sharing this shell will expose arbitrary command execution." and the body sentence interpolating the session name. Focus is on Cancel. Press Esc — banner closes with no side effect; Web On is still off. Click Web On again, banner reappears. Click "Enable web sharing" — button changes to "Enabling…", both buttons disable, banner unmounts when daemon confirms, and Web On is now enabled. Toggle a SECOND shell session's Web On — banner does NOT re-appear (machine-wide flag is now true). The 3px destructive-red left border is visible; primary CTA background is the destructive-red TokyoNight hex #f7768e.
result: pass
notes: |
  Walked in three steps:
    - Step 1 (first-toggle banner appears): pass — required pre-test reset of shellWebShareWarned via daemon PATCH /settings/shell-web-share-warned {"value": false} because user had previously confirmed. Banner appeared with correct heading, session-name body, two buttons, focus on Cancel.
    - Step 2 (Esc dismisses, banner re-shows): pass — Esc closed the banner cleanly with no side effects, Web On remained off, second click on Web On re-rendered the banner.
    - Step 3 (Enable flow + flag stickiness): pass — clicking "Enable web sharing" completed too fast for the user to perceive the "Enabling…" transition state, but the end result was correct: banner unmounted, session web-shared, second shell session's Web On did NOT re-show the banner. User accepted the imperceptible transition as fine.
  Color contract (3px destructive-red border, TokyoNight #f7768e CTA) not visually verified — user is colorblind; trust the source-level CSS contract in 101-VERIFICATION.md.
followups_filed_by_user:
  - "GitHub issue filed: enable/disable banner warning via Settings toggle (deferred — paired with the every-time-confirmation design question; both belong in a v3.4 /gsd-explore design session, not Phase 108)."

## Summary

total: 5
passed: 3
issues: 0
pending: 0
skipped: 2

## Gaps

- truth: "TUI and CLI new-session surfaces must expose a single 'Shell' entry (matching the GUI's Phase 107 SHELL-10 collapse), with shell binary resolved from the same Settings-stored path as the GUI."
  status: failed
  reason: "Phase 107 scoped itself to GUI new-session modal + Settings only. TUI (internal/tui/modal.go) and CLI (agenthub new shell --shell=…) still expose system-default + bash + zsh as separate entries. User declared cross-surface UX inconsistency an unacceptable v3.3 release state during 101-UAT Test 3."
  severity: blocker
  release_blocking_for: v3.3
  test: 3
  surfaces:
    - tui: "internal/tui/modal.go (cycleAgent + agent picker rendering); internal/tui/update_test.go cycle assertion needs rewrite"
    - cli: "cmd_cli.go cmdNew + --shell flag handling; locked stderr error string changes"
  artifacts: []
  missing:
    - "TUI collapse plan (mirror Phase 107 GUI collapse): remove multi-shell rows from agent picker, add single Shell entry, resolve binary from Settings shellPath at session-create time"
    - "CLI collapse plan: remove --shell flag (or deprecate with warning); use Settings shellPath; update locked stderr; update cmd_cli help/usage"
    - "Update CLAUDE-facing docs (README, CLI help) to match new shape"
