---
phase: 131-hub-foundation-static-session-cards
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/daemon/types.go
  - internal/daemon/engine.go
  - internal/daemon/engine_test.go
  - app.go
  - frontend/src/wailsjs/go/main/App.d.ts
autonomous: true
requirements: [CARD-04, CARD-05, CARD-06, GRID-02]
must_haves:
  truths:
    - "ListSessions() returns each session's working directory, viewer count, exit code, and duration to the frontend"
    - "A session's workDir reaches the Wails-bound SessionInfo so the Hub can group by working directory"
  artifacts:
    - path: internal/daemon/types.go
      provides: "daemon.SessionInfo.WorkDir field"
      contains: "WorkDir"
    - path: internal/daemon/engine.go
      provides: "WorkDir populated from sessionWorkDirs in ListSessions()"
      contains: "sessionWorkDirs[s.ID]"
    - path: internal/daemon/engine_test.go
      provides: "Go test coverage for WorkDir population in ListSessions()"
      contains: "WorkDir"
    - path: app.go
      provides: "ViewerCount, ExitCode, Duration, WorkDir on the Wails-bound SessionInfo + propagation in ListSessions()"
      contains: "WorkDir"
    - path: frontend/src/wailsjs/go/main/App.d.ts
      provides: "workDir field on the TS SessionInfo interface"
      contains: "workDir"
  key_links:
    - from: "internal/daemon/engine.go ListSessions()"
      to: "e.sessionWorkDirs"
      via: "map read inside held RLock"
      pattern: "sessionWorkDirs\\[s\\.ID\\]"
    - from: "app.go ListSessions()"
      to: "daemon.SessionInfo"
      via: "field propagation in result mapping"
      pattern: "WorkDir:\\s+s\\.WorkDir"
---

<objective>
Close the backend data gap that blocks the Hub card layer. The Wails-bound `app.go SessionInfo`
struct is missing `ViewerCount`, `ExitCode`, `Duration`, and `WorkDir`; and `daemon.SessionInfo`
is missing `WorkDir` entirely (working directory is currently stored only in the engine's
`sessionWorkDirs` map). Without this wiring, CARD-05 always shows 0 viewers, CARD-06 shows no
uptime/duration, and GRID-02 cannot group by working directory (all cards fall into "Other").

Purpose: Every card-data component in Waves 1-2 reads these fields from the `ListSessions()` RPC.
This MUST land first.
Output: `daemon.SessionInfo.WorkDir` field, engine population, `app.go` struct + propagation,
updated TypeScript binding stub, and Go coverage for the WorkDir population path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-hub-foundation-static-session-cards/131-RESEARCH.md
@.planning/phases/131-hub-foundation-static-session-cards/131-PATTERNS.md

<interfaces>
<!-- Current daemon.SessionInfo (internal/daemon/types.go lines 20-34) ends at FilesWrite — WorkDir is NOT present. -->
<!-- The WorkDir at types.go line 40 belongs to CreateRequest, a DIFFERENT struct. Do not confuse them. -->
<!-- engine.ListSessions() (engine.go lines 404-471) holds e.mu.RLock via defer (line 410); -->
<!--   e.sessionWorkDirs is a map[string]string keyed by session ID, populated at engine.go line 355. -->
<!-- app.go SessionInfo (lines 29-46) ends at FilesWrite — missing all four Hub fields. -->
<!-- app.go ListSessions() result mapping is at lines 352-372. -->
<!-- App.d.ts already declares viewerCount/exitCode/duration (lines 15-17); only workDir is missing. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WorkDir to daemon.SessionInfo and populate it in ListSessions()</name>
  <files>internal/daemon/types.go, internal/daemon/engine.go, internal/daemon/engine_test.go</files>
  <read_first>
    - internal/daemon/types.go (SessionInfo struct, lines 19-34)
    - internal/daemon/engine.go (ListSessions, lines 404-471; sessionWorkDirs field at line 36; population at line 355)
    - internal/daemon/engine_test.go (existing ListSessions test patterns)
  </read_first>
  <behavior>
    - Given a created session with a resolved working directory, ListSessions() returns a SessionInfo whose WorkDir equals e.sessionWorkDirs[sessionID].
    - Given a session with no entry in sessionWorkDirs, WorkDir is the empty string (no panic).
  </behavior>
  <action>
    In internal/daemon/types.go, add a `WorkDir string` field with json tag `workDir` to the
    `SessionInfo` struct, placed after `FilesWrite`. Add an inline comment referencing Phase 131 /
    GRID-02 explaining it is populated from the engine's sessionWorkDirs map.

    In internal/daemon/engine.go ListSessions(), add `WorkDir: e.sessionWorkDirs[s.ID]` to the
    `SessionInfo{...}` literal appended at lines 450-468. This map read is safe because the function
    already holds `e.mu.RLock()` via the defer at line 410 — do NOT acquire a new lock. Add an inline
    comment noting the lock is already held.

    Add a Go test in internal/daemon/engine_test.go that creates a session, asserts the returned
    SessionInfo.WorkDir is non-empty and matches the resolved work dir, and asserts a session with no
    sessionWorkDirs entry yields an empty WorkDir without panic. Follow the existing ListSessions test
    pattern in engine_test.go (reuse its session-creation helpers; do not invent a new harness).
    Run gofmt on all modified Go files.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && gofmt -l internal/daemon/types.go internal/daemon/engine.go internal/daemon/engine_test.go | grep -q . && echo UNFORMATTED && exit 1; go test ./internal/daemon/... -count=1 -run ListSessions</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'WorkDir' internal/daemon/types.go` shows the new field inside SessionInfo (not only CreateRequest).
    - `grep -c 'sessionWorkDirs\[s\.ID\]' internal/daemon/engine.go` is >= 1.
    - `go test ./internal/daemon/... -run ListSessions` passes including the new WorkDir assertions.
    - `gofmt -l` reports no diffs on the three modified files.
  </acceptance_criteria>
  <done>daemon.SessionInfo carries WorkDir, ListSessions() populates it from sessionWorkDirs, and a Go test proves the population path.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate the four fields through app.go and update the App.d.ts binding stub</name>
  <files>app.go, frontend/src/wailsjs/go/main/App.d.ts</files>
  <read_first>
    - app.go (SessionInfo struct lines 29-46; ListSessions mapping lines 343-372)
    - frontend/src/wailsjs/go/main/App.d.ts (SessionInfo interface lines 6-22)
  </read_first>
  <action>
    In app.go, add four fields to the `SessionInfo` struct after `FilesWrite`: `ViewerCount int`
    (json `viewerCount`), `ExitCode *int` (json `exitCode,omitempty`), `Duration *int`
    (json `duration,omitempty`), and `WorkDir string` (json `workDir`). Add a Phase 131 comment
    naming the requirements these serve (CARD-04..06, GRID-02).

    In app.go ListSessions() (the result mapping at lines 354-369), add the four propagations from
    the daemon source struct: `ViewerCount: s.ViewerCount`, `ExitCode: s.ExitCode`,
    `Duration: s.Duration`, `WorkDir: s.WorkDir`. These mirror the existing HomeDir/FilesWrite
    propagation pattern — omitting them silently drops them (the same class of UAT bug that the
    HomeDir comment at lines 363-366 documents).

    In frontend/src/wailsjs/go/main/App.d.ts, add `workDir: string` to the `SessionInfo` interface
    (viewerCount/exitCode/duration are already declared at lines 15-17). Add a comment referencing
    Phase 131 / GRID-02. Run gofmt on app.go.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && gofmt -l app.go | grep -q . && echo UNFORMATTED && exit 1; go build ./... && grep -q "workDir" frontend/src/wailsjs/go/main/App.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - app.go SessionInfo struct contains ViewerCount, ExitCode, Duration, WorkDir.
    - `grep -c 'WorkDir:\s*s\.WorkDir' app.go` is >= 1 (propagation present in ListSessions mapping).
    - `go build ./...` succeeds.
    - App.d.ts SessionInfo interface contains a `workDir: string` line.
  </acceptance_criteria>
  <done>app.go's Wails-bound SessionInfo exposes all four Hub fields, ListSessions() propagates them, the project builds, and the TS binding declares workDir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon engine → Wails RPC | Session metadata (incl. working-directory path) crosses from the daemon into the GUI process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01 | Information disclosure | WorkDir path exposed via ListSessions() | accept | WorkDir is the user's own local session cwd, already known to the same user; ListSessions is the same single-user local RPC that already returns session names/hostnames. No new principal gains access. |
| T-131-02 | Tampering | npm/go installs | accept | No new packages installed in this plan (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `go test ./internal/daemon/... -count=1` green.
- `go build ./...` succeeds.
- App.d.ts declares workDir.
</verification>

<success_criteria>
ListSessions() returns workDir, viewerCount, exitCode, and duration for every session, end to end
from the engine through the Wails binding to the TypeScript type.
</success_criteria>

<output>
Create `.planning/phases/131-hub-foundation-static-session-cards/131-01-SUMMARY.md` when done
</output>
