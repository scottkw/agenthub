---
phase: 131-hub-foundation-static-session-cards
plan: 05
type: execute
wave: 3
depends_on: [131-04]
files_modified:
  - frontend/src/components/TabBar.tsx
  - frontend/src/components/Sidebar.tsx
  - frontend/src/components/__tests__/Sidebar.test.tsx
  - frontend/src/App.tsx
  - frontend/src/components/__tests__/App.hub.test.tsx
  - frontend/src/style.css
  - frontend/src/components/__tests__/style.hub.test.ts
autonomous: true
requirements: [HUB-01, HUB-02, HUB-04, GRID-01, GRID-06, CARD-08]
must_haves:
  truths:
    - "Clicking 'Hub' in the sidebar opens the Hub surface as a new top-level tab, coexisting with (not replacing) the Sessions panel"
    - "The Hub polls ListSessions() every 3s only while it is the active surface and renders HubPanel"
    - "New session on the Hub opens the existing NewSessionModal"
    - "Hub CSS custom properties exist for both dark and light themes; the grid uses auto-fill minmax(240px, 1fr); dimmed-card and reduced-motion rules are present"
  artifacts:
    - path: frontend/src/components/Sidebar.tsx
      provides: "Hub sidebar item (Squares2X2Icon) + active-state indicator"
      contains: "onOpenHub"
    - path: frontend/src/App.tsx
      provides: "HUB_TAB, handleOpenHub, Hub poll, HubPanel render, terminal-exclusion update"
      contains: "HUB_TAB"
    - path: frontend/src/style.css
      provides: "Hub theme tokens, grid, dim, spin-under-reduced-motion, sidebar active"
      contains: "--hub-bg"
  key_links:
    - from: "Sidebar.tsx Hub button"
      to: "App.tsx handleOpenHub"
      via: "onOpenHub prop"
      pattern: "onOpenHub"
    - from: "App.tsx Hub poll"
      to: "ListSessions() every 3s while active"
      via: "useEffect gated on activeId === HUB_TAB.id"
      pattern: "HUB_TAB\\.id"
    - from: "App.tsx HubPanel"
      to: "NewSessionModal"
      via: "onNewSession → setShowNewSessionModal(true)"
      pattern: "setShowNewSessionModal\\(true\\)"
---

<objective>
Integrate the Hub into the app shell: extend the TabBar Tab type with `'hub'`, add the Hub item +
active-state to the Sidebar, wire `HUB_TAB` + `handleOpenHub` + the 3s poll + the `<HubPanel>` render
+ the terminal-exclusion updates in App.tsx, and append the Hub CSS (theme tokens for dark/light, the
responsive grid, dimmed-card rule, reduced-motion-guarded spin, and the sidebar active class).

Purpose: This makes the Hub reachable and themed. It is the final wave — it depends on HubPanel
existing (Plan 04).
Output: TabBar/Sidebar/App.tsx edits, the Hub CSS block, and wiring + CSS-contract tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/131-hub-foundation-static-session-cards/131-UI-SPEC.md
@.planning/phases/131-hub-foundation-static-session-cards/131-PATTERNS.md
@frontend/src/App.tsx
@frontend/src/components/Sidebar.tsx
@frontend/src/components/TabBar.tsx

<interfaces>
<!-- App.tsx anchors (VERIFIED current line numbers): -->
<!--   Tab constants: DAEMON_MANAGER_TAB line 86, REMOTE_SESSIONS_TAB line 87 -->
<!--   showNewSessionModal state: line 116; setShowNewSessionModal(true) used at line 729 -->
<!--   daemon-manager poll useEffect: lines ~873-900 (gated on activeId === DAEMON_MANAGER_TAB.id) -->
<!--   handleOpenDaemonManager: line ~952; handleOpenRemoteSessions: line ~1071 (exact analog for handleOpenHub) -->
<!--   <Sidebar ...> render: line ~1242 (onOpenDaemonManager line 1245) -->
<!--   DaemonManagerPanel render gate: line ~1273; RemoteSessionsPanel: line ~1360 -->
<!--   daemonError empty-filter (line 1399) and terminal map (line 1438) BOTH list excluded t.type values -->
<!--     and BOTH must add 'hub' so the Hub tab is not treated as a terminal. -->
<!-- Sidebar.tsx: SidebarProps interface line 13; Heroicons import block line ~2; -->
<!--   copy the Sessions button (onOpenDaemonManager / ServerStackIcon) as the Hub button template. -->
<!-- TabBar.tsx Tab type union: line 8. -->
<!-- From Plan 04: HubPanel { sessions, error, onNewSession, onRename } at frontend/src/components/Hub/HubPanel.tsx -->
<!-- Test patterns: App.nav.test.tsx uses `import raw from '../../App.tsx?raw'` source inspection; -->
<!--   style.contrast.test.ts reads style.css text via readFileSync. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TabBar type + Sidebar Hub item + App.tsx wiring</name>
  <files>frontend/src/components/TabBar.tsx, frontend/src/components/Sidebar.tsx, frontend/src/components/__tests__/Sidebar.test.tsx, frontend/src/App.tsx, frontend/src/components/__tests__/App.hub.test.tsx</files>
  <read_first>
    - frontend/src/components/TabBar.tsx (Tab type union, line 8)
    - frontend/src/components/Sidebar.tsx (SidebarProps line 13, Heroicons import, Sessions button pattern)
    - frontend/src/App.tsx (lines 86-87, 116, 873-900, 952, 1071-1079, 1242-1248, 1273, 1399, 1438)
    - 131-PATTERNS.md (Sidebar.tsx and App.tsx sections — exact additions)
    - frontend/src/components/__tests__/Sidebar.test.tsx and App.nav.test.tsx (test patterns)
  </read_first>
  <action>
    In TabBar.tsx line 8, add `| 'hub'` to the Tab `type?` union.

    In Sidebar.tsx: add `Squares2X2Icon` to the @heroicons/react/24/outline import; add `onOpenHub: () => void`
    and `activePanel?: string` to SidebarProps; add a Hub `<button>` (copy the Sessions button pattern —
    Squares2X2Icon, label "Hub", aria-label "Hub", onClick={onOpenHub}) placed before the "New Session"
    button, with className `sidebar__item${activePanel === '__hub__' ? ' sidebar__item--active' : ''}`.
    Update Sidebar.test.tsx to assert the Hub button is present and fires onOpenHub, and that the active
    class is applied when activePanel === '__hub__'.

    In App.tsx: add `const HUB_TAB: Tab = { id: '__hub__', name: 'Hub', sessionId: '', cli: '', type: 'hub' }`
    after the existing tab constants; add `hubSessions` and `hubError` state; add `handleOpenHub`
    (mirror handleOpenRemoteSessions — reuse an existing hub tab if present else append HUB_TAB and set
    active); add a poll useEffect mirroring the daemon-manager poll but gated on `activeId === HUB_TAB.id`
    (3s interval, cancelled flag, setHubSessions/setHubError, early-return when mode === 'web'); pass
    `onOpenHub={handleOpenHub}` and `activePanel={activeId ?? undefined}` to <Sidebar>; render
    `{activeId === HUB_TAB.id && <HubPanel sessions={hubSessions} error={hubError}
    onNewSession={() => setShowNewSessionModal(true)} onRename={handleRenameTab} />}` after the
    DaemonManagerPanel gate; and add `t.type !== 'hub'` to BOTH terminal-exclusion sites (the daemonError
    empty-filter at line 1399 and the terminal map's early-return at line 1438). Import HubPanel from
    './components/Hub/HubPanel'. Confirm handleRenameTab exists; if its signature differs from
    (id, name) => void, adapt the onRename wiring to call the existing rename handler.

    Create frontend/src/components/__tests__/App.hub.test.tsx using the `App.tsx?raw` source-inspection
    pattern (like App.nav.test.tsx) to assert: HUB_TAB constant exists with type 'hub'; onOpenHub is wired
    to Sidebar; the Hub poll is gated on HUB_TAB.id; HubPanel is rendered with onNewSession →
    setShowNewSessionModal(true); and 'hub' is excluded in both terminal-exclusion sites (so the Hub does
    not render a terminal and the Sessions panel is not replaced — HUB-02 coexistence).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm vitest run src/components/__tests__/Sidebar.test.tsx src/components/__tests__/App.hub.test.tsx && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.test.tsx proves the Hub button fires onOpenHub and applies the active class.
    - App.hub.test.tsx proves HUB_TAB/type 'hub', onOpenHub wiring, poll gating on HUB_TAB.id, HubPanel render with onNewSession→setShowNewSessionModal(true), and 'hub' present in BOTH terminal-exclusion sites.
    - `grep -c "t.type !== 'hub'" frontend/src/App.tsx` >= 2.
    - `pnpm exec tsc --noEmit` reports no type errors.
  </acceptance_criteria>
  <done>The Hub is reachable from the sidebar as a coexisting top-level tab that polls sessions and renders HubPanel; New session opens the existing modal. Tests + typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Hub CSS (theme tokens, grid, dim, reduced-motion, sidebar active) + CSS-contract test</name>
  <files>frontend/src/style.css, frontend/src/components/__tests__/style.hub.test.ts</files>
  <read_first>
    - 131-UI-SPEC.md (Theme Contract dark+light tables, Layout Contract grid, Motion Contract, Source-Level Verification Checklist)
    - 131-PATTERNS.md (style.css section — token block, grid, group header, dim, spin guard, sidebar active)
    - frontend/src/style.css (existing .remote-panel__peer-header ~line 1577, .tab__agent-badge ~1054, .tab__rename-input ~142)
    - frontend/src/components/__tests__/style.contrast.test.ts (CSS-text test pattern)
  </read_first>
  <action>
    Append a Phase 131 Hub block to frontend/src/style.css. Add a `:root` block with ALL `--hub-*`
    custom properties from the UI-SPEC dark table, and a `[data-ui-theme="light"]` block with ALL
    `--hub-*` values from the light table. Include the two HUB-04 LIGHT THEME inline comments for the
    accent (#3d6fe8 = 4.5:1) and destructive (#c0394f = 4.7:1) WCAG verifications from the Source-Level
    Verification Checklist.

    Add the Hub layout/structure rules from the UI-SPEC: `.hub` surface, `.hub__header`,
    `.hub__grid-scroll`, `.hub__group`, `.hub__group-header` (11px/600/uppercase/letter-spacing 0.08em,
    mirroring .remote-panel__peer-header), the responsive grid `.hub__card-row`
    (grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); gap 8px; max-width 1440px), `.hub-card`
    (min-width 240px; max-width 360px; border 1px var(--hub-border); radius 8px; padding 12px 16px; hover
    → border-hover + surface-elevated), the card metadata/status/badge/origin/viewer rows, filter pill
    + search styles, and the empty/error-state styles. Add `.hub-card--dim`
    (opacity: var(--hub-dim-opacity); background: var(--hub-card-dim-bg)) per CARD-08, with a comment that
    error cards are NOT dimmed.

    Wrap the running-icon spin in `@media (prefers-reduced-motion: no-preference)` around
    `.hub-card__status-icon--spin { animation: hub-spin 0.8s linear infinite; }` plus the `@keyframes hub-spin`
    — the animation MUST NOT be declared outside the reduced-motion guard (Pitfall 6 / Motion Contract).
    Add `.sidebar__item--active` (Pitfall 8) using the accent token. All Hub rules reference `var(--hub-*)`
    tokens — NO hardcoded hex inside `.hub`/`.hub__`/`.hub-card` rules (status-dot reinforcement comments
    excepted, where the hex is documented as reinforcement-only).

    Create frontend/src/components/__tests__/style.hub.test.ts reading style.css text and asserting:
    `--hub-bg` declared; `[data-ui-theme="light"]` block present with `--hub-accent`/`--hub-destructive`;
    `repeat(auto-fill, minmax(240px, 1fr))` present (GRID-01); `.hub-card--dim` with `var(--hub-dim-opacity)`
    (CARD-08); the spin animation appears only inside a `prefers-reduced-motion: no-preference` block;
    `.sidebar__item--active` present.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm vitest run src/components/__tests__/style.hub.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - style.hub.test.ts proves dark+light tokens, GRID-01 grid declaration, CARD-08 dim rule, reduced-motion-guarded spin, and sidebar active class.
    - `grep -c "prefers-reduced-motion" frontend/src/style.css` >= 1 and the hub-spin animation is inside that guard.
    - `grep -c "data-ui-theme=\"light\"" frontend/src/style.css` >= 1.
    - `grep -c "repeat(auto-fill, minmax(240px, 1fr))" frontend/src/style.css` >= 1.
  </acceptance_criteria>
  <done>The Hub is themed for dark and light, the grid is responsive, dimmed cards and reduced-motion fallbacks are correct, and the sidebar active state is styled — proven by the CSS-contract test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub poll → frontend state | ListSessions() RPC result drives Hub rendering (same data as the existing Sessions panel) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-10 | Denial of service | Hub poll runs even when Hub inactive | mitigate | Poll useEffect early-returns unless activeId === HUB_TAB.id and mode !== 'web' (Pitfall 7); verified by App.hub.test.tsx poll-gating assertion. |
| T-131-11 | Tampering | Hub replaces Sessions panel | mitigate | HUB_TAB is a distinct tab; 'hub' added to terminal-exclusion sites only; App.hub.test.tsx asserts the daemon-manager gate is untouched (HUB-02 coexistence). |
| T-131-12 | Tampering | npm installs | accept | No new packages (RESEARCH audit: none). |
</threat_model>

<verification>
- `cd frontend && pnpm vitest run` (full suite) green.
- `pnpm exec tsc --noEmit` clean.
- Manual (deferred to /gsd:verify-work, live app): open Hub, confirm Sessions panel still reachable; toggle theme and confirm tokens apply (colorblind user verifies hex at source per VALIDATION Manual-Only table).
</verification>

<success_criteria>
The Hub is a reachable, themed, coexisting top-level surface that polls sessions only while active
and opens the existing create modal — proven by wiring tests, the CSS-contract test, and a clean
typecheck.
</success_criteria>

<output>
Create `.planning/phases/131-hub-foundation-static-session-cards/131-05-SUMMARY.md` when done
</output>
