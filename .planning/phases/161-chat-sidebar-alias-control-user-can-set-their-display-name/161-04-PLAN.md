---
phase: 161-chat-sidebar-alias-control-user-can-set-their-display-name
plan: 04
type: execute
wave: 3
depends_on: [161-01, 161-02, 161-03]
files_modified:
  - frontend/e2e/chat-parity.spec.ts
  - TESTING.md
autonomous: false
requirements: [ALIAS-UI-01, ALIAS-UI-02]
user_setup: []

must_haves:
  truths:
    - "An automated cross-surface e2e proves: setting an alias from one web-share client updates the OTHER client's presence roster (ALIAS-UI-01 cross-surface; ALIAS-UI-02 roster propagation)."
    - "TESTING.md is the single consolidated home for every test added/extended this phase (Go self-frame tests, relayClient tests, ChatPanel tests, the e2e) per the binding regression convention."
    - "Behaviors that are genuinely live-app-only (desktop owner ↔ real Tailnet web guest propagation; web computed-name pre-fill) are recorded as Section 5 manual M-NN items and confirmed via a human-verify checkpoint."
  artifacts:
    - "frontend/e2e/chat-parity.spec.ts: cross-surface alias-propagation test"
    - "TESTING.md: §2 Suite Manifest delta + §4 traceability rows for ALIAS-UI-01/02 + §5 Category S manual items"
  key_links:
    - "TESTING.md updates for ALL phase test files are consolidated here (Plans 161-01..03 deliberately did NOT touch TESTING.md to keep Wave-1 file sets disjoint). The §4 path column must be repo-relative .go/.ts/.tsx paths only; run tests/check-traceability-paths.sh before commit."
---

<objective>
Close the phase with cross-surface proof and the binding regression-convention bookkeeping:
1. Extend the existing Phase 155 chat-parity Playwright spec with an alias-propagation test: client A sets an alias, client B's presence roster reflects it — the automatable half of cross-surface parity (ALIAS-UI-01/02). The stub-PTY Playwright fixture can drive two web clients but cannot exercise a real Tailnet computed-name pre-fill.
2. Consolidate TESTING.md for every test added/extended across Plans 161-01..03 plus this spec (Suite Manifest §2 delta, Traceability §4 rows, Manual §5 items), then run the traceability-path check. Plans 161-01..03 intentionally left TESTING.md untouched so the parallel Wave-1 plans had disjoint file sets — this plan owns the whole TESTING.md delta.
3. Human-verify the live-only behaviors (desktop ↔ real Tailnet web guest propagation; web pre-fill shows the guest's computed name) that no automated fixture can prove.

Purpose: Satisfy the cross-surface parity requirement (ALIAS-UI-01 is release-relevant parity), prove the roster-propagation half of ALIAS-UI-02, and keep TESTING.md canonical per the standing rule.

Output: extended chat-parity.spec.ts; TESTING.md §2/§4/§5 updates with passing traceability check; human-verify sign-off.
</objective>

<artifacts_this_plan_produces>
New/extended artifacts (MANDATORY for drift verification):
- frontend/e2e/chat-parity.spec.ts: a new test asserting alias set on web client A appears in web client B's `.chat-presence` roster.
- TESTING.md: §2 Suite Manifest Phase-161 delta note; §4 traceability rows mapping ALIAS-UI-01 and ALIAS-UI-02 to their test files (relayClient.test.ts, ChatPanel.test.tsx, chat-parity.spec.ts, internal/relay + internal/webserver identity tests); §5 new Category S manual M-NN items for live cross-surface propagation + web computed-name pre-fill.

No NEW source symbols — this plan is tests + docs only. The phase's complete new-symbol set is listed in Plans 161-01 (MsgSelf/SelfPayload/MakeSelfFrame), 161-02 (sendAliasSet, MSG_SELF/onSelf), and 161-03 (validateAlias, alias control, CSS).
</artifacts_this_plan_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-chat-sidebar-alias-control-user-can-set-their-display-name/161-RESEARCH.md
@.planning/phases/161-chat-sidebar-alias-control-user-can-set-their-display-name/161-VALIDATION.md

@frontend/e2e/chat-parity.spec.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend chat-parity.spec.ts with cross-surface alias propagation</name>
  <files>frontend/e2e/chat-parity.spec.ts</files>
  <read_first>
    - frontend/e2e/chat-parity.spec.ts lines 50-135 (the two-RW-web-client harness: ctx1/ctx2, page1/page2, chat-toggle open, the WS-ready gate, the broadcast assertion pattern to mirror)
    - frontend/src/components/Hub/ChatPanel.tsx (the alias control selector + commit interaction added in Plan 161-03 — read its final class names: .chat-panel__alias and the edit input)
  </read_first>
  <action>
    Add a test to the existing "chat parity gate" describe in frontend/e2e/chat-parity.spec.ts that reuses the two-web-client harness (two RW contexts on the same /app session). After both panels are open and WS-ready (reuse the existing readiness gate that waits for .chat-msg.first or the presence roster), drive client A's alias control (the .chat-panel__alias affordance from Plan 161-03): open it, set a new unique alias, commit. Then assert client B's presence roster (.chat-presence) reflects the new alias (e.g. the roster entry's title/aria or initial updates) within a generous timeout, mirroring the existing broadcast-assertion + WS-frame diagnostic pattern. Keep it resilient to the virtualizer (use .first()/roster locators, not text-filtered message rows, per the Phase 155-06 note). Do not assert past-message relabeling (per-message snapshot — RESEARCH Pitfall 3); assert roster propagation only.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && npx playwright test e2e/chat-parity.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/chat-parity.spec.ts` passes including the new alias-propagation test (and the existing PARITY-01 tests still pass).
    - The new test sets an alias on one client and asserts the OTHER client's `.chat-presence` roster reflects it (cross-surface propagation).
    - The test does NOT assert past-message relabeling.
  </acceptance_criteria>
  <done>The chat-parity spec proves alias set on one web client propagates to the other client's roster; full spec green.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate TESTING.md (Suite Manifest, Traceability, Manual) + run path check</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md §2 Suite Manifest (line 22) and its per-phase delta-note convention (the long Phase 15x notes around line 34)
    - TESTING.md §4 Requirement→Test Traceability Map (line 94) — row format: requirement ID | repo-relative path | framework | description
    - TESTING.md §5 Manual Regression Checklist (line 231) and the latest category (Category R — Phase 159, line 444) to find the next free M-NN number and add a new Category S
    - TESTING.md §6 Standing Convention (line 458)
    - CLAUDE.md (regression-test convention: §2 manifest, §4 traceability with .go/.ts/.tsx repo-relative paths, run tests/check-traceability-paths.sh)
  </read_first>
  <action>
    Update TESTING.md for every test added/extended this phase: (1) §2 Suite Manifest — add a Phase 161 delta note recording the extended files (Go: internal/relay/protocol_presence_test.go, internal/relay/server_identity_test.go, internal/webserver/identity_test.go gained MsgSelf round-trip + on-connect self-frame tests; vitest: src/lib/relayClient.test.ts gained sendAliasSet + 0x37/onSelf tests, src/components/Hub/ChatPanel.test.tsx gained validateAlias + alias-control render/RO-enabled/commit/pre-fill tests; Playwright: e2e/chat-parity.spec.ts gained the alias-propagation test) and adjust the running file/total counts consistent with the existing notes. (2) §4 Traceability — add rows mapping ALIAS-UI-01 and ALIAS-UI-02 to their test files, path column repo-relative .go/.ts/.tsx only (no test names in the path column). (3) §5 — add a new Category S — Chat-Sidebar Alias Control (Phase 161) with the next free M-NN numbers for the two live-only behaviors: live desktop-owner ↔ real-Tailnet web-guest alias propagation, and web-share computed-name pre-fill; include behavior description, why it cannot be automated (live relay + real Tailnet, web-share WS blocks automated input), and source (161-VALIDATION.md Manual-Only Verifications; ALIAS-UI-01/02). Then run the traceability-path check.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && bash tests/check-traceability-paths.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/check-traceability-paths.sh` exits 0 (all §4 path-column entries are valid repo-relative file paths).
    - §4 has at least one row each for ALIAS-UI-01 and ALIAS-UI-02 with repo-relative .go/.ts/.tsx paths.
    - §5 has a new Category S with two M-NN manual items (live cross-surface propagation + web pre-fill), each with behavior/why-manual/source.
    - §2 records the Phase 161 test-file delta.
  </acceptance_criteria>
  <done>TESTING.md is the consolidated, convention-compliant home for all Phase 161 tests; the traceability-path check passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify live cross-surface alias propagation + web pre-fill</name>
  <action>Blocking human verification. Perform the steps in how-to-verify against a live daemon + real Tailnet web guest, then wait for the resume signal. No code changes in this task.</action>
  <what-built>The shared ChatPanel alias control (header "chatting as: «name» ✏️"), the MsgSelf self-identity frame for web pre-fill, and cross-surface propagation via the existing AliasStore/NotifyPresence backend. Automated coverage proves wire framing, client validation, control behavior, and two-web-client roster propagation. The following require a live daemon + real Tailnet web guest and cannot be automated.</what-built>
  <how-to-verify>
    1. Build/run the live app (wails dev or a signed build) and start a session.
    2. Open the session's chat on the desktop (Hub modal or terminal tab). Confirm the header shows "chatting as: «current name»" pre-filled with the owner default (hostname) or your persisted alias.
    3. Change your alias to a new name and commit. Confirm: (a) the presence roster updates to the new name immediately, and (b) a NEW message you send shows the new author name. Past messages keep their old author name (expected — per-message snapshot, NOT a bug).
    4. Open the SAME session as a remote web-share guest via the shared /sessions/{id} link (which Phase 159 redirects to /app). Confirm the guest's chat header pre-fills with the guest's Tailscale computed name (web self-identity frame working).
    5. From the web guest, set an alias. Confirm the desktop roster + the guest's own roster update to the new name for all participants.
    6. Confirm a read-only web guest (viewer cap) can STILL open and use the alias control (it is not disabled) — even though their chat Send button is disabled.
    7. Confirm the control copy communicates the alias is a global display name (applies across sessions), not per-session.
  </how-to-verify>
  <resume-signal>Type "approved" if all live checks pass, or describe what failed (e.g. web pre-fill empty, RO control disabled, roster did not update).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| two web clients ↔ shared session | The e2e exercises cross-client broadcast; no new trust boundary — same WS contract verified in Phase 155. |
| docs (TESTING.md) | Non-executable; no runtime trust surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-02 | Tampering | alias render in roster (e2e observes it) | low | mitigate | The e2e additionally guards that a propagated alias renders as text in the roster (no script execution) — belt-and-suspenders over the ChatPanel unit proof (SEC-03 lineage). |
| T-161-03 | Elevation of Privilege | RO guest alias control (human-verify step 6) | low | accept | Human-verify confirms RO can set an alias (D-06) while chat-send stays gated — the accepted design, explicitly checked. |
| T-161-SC | Tampering | package installs | n/a | accept | No npm/Go installs this plan — RESEARCH Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
- `npx playwright test e2e/chat-parity.spec.ts` green (alias propagation + existing PARITY-01 tests).
- `bash tests/check-traceability-paths.sh` exits 0.
- Full phase gate before /gsd-verify-work: `go test ./internal/relay/... ./internal/webserver/... && cd frontend && npx vitest run && npx tsc --noEmit && npx playwright test e2e/chat-parity.spec.ts`.
- Human-verify checkpoint approved for the live-only behaviors.
</verification>

<success_criteria>
- Cross-surface alias propagation proven automatically (two web clients) and confirmed live (desktop ↔ Tailnet guest).
- TESTING.md consolidates all Phase 161 tests per the standing convention; path check passes.
- Web pre-fill, RO-enabled control, and global-scope copy confirmed live.
</success_criteria>

<output>
Create `.planning/phases/161-chat-sidebar-alias-control-user-can-set-their-display-name/161-04-SUMMARY.md` when done.
</output>
