---
phase: 52-remote-sessions-gui-panel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/RemoteSessionsPanel.tsx
  - frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx
  - frontend/src/style.css
autonomous: true
requirements:
  - REM-02
  - REM-03

must_haves:
  truths:
    - "RemoteSessionsPanel renders sessions grouped by peer hostname"
    - "Each session row shows name, cliType badge, status dot, and Open Session button"
    - "Loading state shows spinner and 'Probing peers...' text when no data"
    - "Empty state shows 'No remote peers found' and 'No tailnet peers are running AgentHub.'"
    - "Open Session button calls onOpen(session.url)"
    - "Each peer group shows 'Shows web-enabled sessions only' sub-label below hostname header"
  artifacts:
    - path: "frontend/src/components/RemoteSessionsPanel.tsx"
      provides: "Remote sessions panel component"
      exports: ["RemoteSessionsPanel", "RemoteSessionsPanelProps", "RemotePeerSessions", "RemoteSession"]
    - path: "frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx"
      provides: "Source inspection and DOM tests for RemoteSessionsPanel"
      contains: "RemoteSessionsPanel"
    - path: "frontend/src/style.css"
      provides: "BEM CSS for .remote-panel block and @keyframes spin"
      contains: ".remote-panel"
  key_links:
    - from: "frontend/src/components/RemoteSessionsPanel.tsx"
      to: "frontend/src/wailsjs/wailsjs/runtime/runtime"
      via: "BrowserOpenURL import for future wiring"
      pattern: "onOpen"
---

<objective>
Create the RemoteSessionsPanel component with loading, empty, and populated states, plus all BEM CSS and comprehensive tests. This is the visual layer — independent of the Go binding and App.tsx wiring.

Purpose: Provides the presentational component that displays remote sessions grouped by peer hostname. Designed as a pure props-driven component (no data fetching) for testability.
Output: RemoteSessionsPanel.tsx, RemoteSessionsPanel.test.tsx, CSS additions to style.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-remote-sessions-gui-panel/52-RESEARCH.md
@.planning/phases/52-remote-sessions-gui-panel/52-UI-SPEC.md

<interfaces>
<!-- Existing patterns the executor must follow exactly -->

From frontend/src/components/DaemonManagerPanel.tsx (reference component):
```typescript
export interface DaemonManagerPanelProps {
  sessions: SessionInfo[]
  sessionStatuses: Record<string, string>
  webServerRunning: boolean
  webEnabled: Record<string, boolean>
  onKill: (id: string) => void
  onToggleWeb: (id: string) => void
}

export function DaemonManagerPanel({ ... }: DaemonManagerPanelProps): React.ReactElement {
  // ...
}
```

From frontend/src/components/__tests__/DaemonManagerPanel.test.tsx (test conventions):
```typescript
import raw from '../../components/DaemonManagerPanel.tsx?raw'
import { DaemonManagerPanel } from '../../components/DaemonManagerPanel'
import { createRoot } from 'react-dom/client'
import { flushSync } from 'react-dom'
```

From frontend/src/components/WelcomeTab.tsx (BrowserOpenURL import pattern):
```typescript
import { BrowserOpenURL } from '../wailsjs/wailsjs/runtime/runtime'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RemoteSessionsPanel component</name>
  <files>frontend/src/components/RemoteSessionsPanel.tsx</files>
  <read_first>
    - frontend/src/components/DaemonManagerPanel.tsx (reference component structure, BEM naming, props pattern)
    - frontend/src/components/WelcomeTab.tsx (BrowserOpenURL import path)
    - .planning/phases/52-remote-sessions-gui-panel/52-UI-SPEC.md (visual contract: typography, colors, spacing, copy)
  </read_first>
  <action>
Create `frontend/src/components/RemoteSessionsPanel.tsx` with the following exact content:

```typescript
import React from 'react'

export interface RemoteSession {
  id: string
  name: string
  cliType: string
  status: string
  url: string
}

export interface RemotePeerSessions {
  hostname: string
  sessions: RemoteSession[]
}

export interface RemoteSessionsPanelProps {
  peers: RemotePeerSessions[]
  loading: boolean
  onOpen: (url: string) => void
}

export function RemoteSessionsPanel({ peers, loading, onOpen }: RemoteSessionsPanelProps): React.ReactElement {
  if (loading && peers.length === 0) {
    return (
      <div className="remote-panel">
        <div className="remote-panel__loading">
          <div className="remote-panel__spinner" />
          Probing peers...
        </div>
      </div>
    )
  }
  if (!loading && peers.length === 0) {
    return (
      <div className="remote-panel">
        <div className="remote-panel__empty">
          <div className="remote-panel__empty-title">No remote peers found</div>
          <div className="remote-panel__empty-body">No tailnet peers are running AgentHub.</div>
        </div>
      </div>
    )
  }
  return (
    <div className="remote-panel">
      {peers.map((peer) => (
        <div key={peer.hostname} className="remote-panel__peer">
          <div className="remote-panel__peer-header">{peer.hostname}</div>
          <div className="remote-panel__peer-meta">Shows web-enabled sessions only</div>
          <div className="remote-panel__session-list">
            {peer.sessions.map((s) => (
              <div key={s.id} className="remote-panel__session-row">
                <span
                  className={`remote-panel__status remote-panel__status--${s.status}`}
                  title={s.status}
                />
                <span className="remote-panel__name">{s.name}</span>
                <span className="remote-panel__cli">{s.cliType}</span>
                <div className="remote-panel__actions">
                  <button
                    className="remote-panel__btn remote-panel__btn--open"
                    onClick={() => onOpen(s.url)}
                    title="Open in browser"
                    aria-label={`Open ${s.name} in browser`}
                  >
                    Open Session
                  </button>
                </div>
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>
  )
}
```

Key design decisions per UI-SPEC:
- `onOpen` is a callback prop (not directly calling BrowserOpenURL) for testability. App.tsx will wire BrowserOpenURL.
- Loading state shows spinner + text per UI-SPEC "Probing peers..." copy.
- Empty state has two lines: title "No remote peers found" and body "No tailnet peers are running AgentHub." per UI-SPEC copywriting contract.
- Status dot uses `remote-panel__status--{status}` modifier pattern (matches daemon-panel convention).
- Button text is "Open Session" per UI-SPEC primary CTA.
- All buttons have `title` and `aria-label` per UI-SPEC accessibility contract.
- "Shows web-enabled sessions only" sub-label rendered as `.remote-panel__peer-meta` below each peer header per UI-SPEC copywriting contract and RESEARCH.md Pitfall 4.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/RemoteSessionsPanel.tsx contains `export function RemoteSessionsPanel`
    - frontend/src/components/RemoteSessionsPanel.tsx contains `export interface RemoteSessionsPanelProps`
    - frontend/src/components/RemoteSessionsPanel.tsx contains `export interface RemotePeerSessions`
    - frontend/src/components/RemoteSessionsPanel.tsx contains `export interface RemoteSession`
    - frontend/src/components/RemoteSessionsPanel.tsx contains `remote-panel__session-row`
    - frontend/src/components/RemoteSessionsPanel.tsx contains `remote-panel__spinner`
    - frontend/src/components/RemoteSessionsPanel.tsx contains `No remote peers found`
    - frontend/src/components/RemoteSessionsPanel.tsx contains `No tailnet peers are running AgentHub.`
    - frontend/src/components/RemoteSessionsPanel.tsx contains `Open Session`
    - frontend/src/components/RemoteSessionsPanel.tsx contains `onOpen(s.url)`
    - frontend/src/components/RemoteSessionsPanel.tsx contains `aria-label`
    - frontend/src/components/RemoteSessionsPanel.tsx contains `remote-panel__peer-meta`
    - frontend/src/components/RemoteSessionsPanel.tsx contains `Shows web-enabled sessions only`
  </acceptance_criteria>
  <done>RemoteSessionsPanel component exists with loading, empty, and populated states matching UI-SPEC exactly, including peer-meta sub-label</done>
</task>

<task type="auto">
  <name>Task 2: Add BEM CSS for .remote-panel and @keyframes spin</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css (existing .daemon-panel block for measurement reference, end of file for append location)
    - .planning/phases/52-remote-sessions-gui-panel/52-UI-SPEC.md (exact CSS values: colors, spacing, typography, spinner)
  </read_first>
  <action>
Append the following CSS block to the END of `frontend/src/style.css`. Do NOT modify any existing rules. The values below are verbatim from the UI-SPEC design contract:

```css
/* ── Remote Sessions Panel ─────────────────────────────── */
.remote-panel {
  display: flex;
  flex-direction: column;
  width: 100%;
  height: 100%;
  padding: 24px;
  background: #1a1b26;
  overflow-y: auto;
  box-sizing: border-box;
}

.remote-panel__loading {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  height: 100%;
  color: #565f89;
  font-size: 14px;
}

.remote-panel__spinner {
  width: 20px;
  height: 20px;
  border: 2px solid #292e42;
  border-top-color: #7aa2f7;
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.remote-panel__empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  gap: 8px;
}

.remote-panel__empty-title {
  color: #565f89;
  font-size: 14px;
  font-weight: 600;
}

.remote-panel__empty-body {
  color: #565f89;
  font-size: 13px;
}

.remote-panel__peer {
  display: flex;
  flex-direction: column;
  gap: 2px;
  margin-bottom: 20px;
}

.remote-panel__peer-header {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: #565f89;
  padding-bottom: 8px;
  border-bottom: 1px solid #292e42;
  margin-bottom: 4px;
}

.remote-panel__peer-meta {
  font-size: 11px;
  color: #565f89;
  margin-bottom: 8px;
}

.remote-panel__session-list {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.remote-panel__session-row {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 10px 12px;
  border-radius: 6px;
  background: #16161e;
  border: 1px solid #292e42;
}

.remote-panel__session-row:hover {
  border-color: #3b4261;
}

.remote-panel__status {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  flex-shrink: 0;
}

.remote-panel__status--running { background: #3b82f6; }
.remote-panel__status--idle { background: #22c55e; }
.remote-panel__status--waiting { background: #f59e0b; }
.remote-panel__status--errored { background: #ef4444; }

.remote-panel__name {
  font-size: 13px;
  color: #c0caf5;
  font-weight: 400;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.remote-panel__cli {
  font-size: 11px;
  color: #565f89;
  background: #1e2030;
  padding: 2px 8px;
  border-radius: 3px;
  flex-shrink: 0;
}

.remote-panel__actions {
  margin-left: auto;
  display: flex;
  gap: 6px;
  flex-shrink: 0;
}

.remote-panel__btn {
  padding: 4px 10px;
  border-radius: 4px;
  border: 1px solid #292e42;
  background: transparent;
  color: #a9b1d6;
  cursor: pointer;
  font-size: 11px;
  font-family: inherit;
}

.remote-panel__btn:hover {
  background: #1e2030;
  color: #c0caf5;
}

.remote-panel__btn--open {
  background: #7aa2f7;
  color: #1a1b26;
  font-weight: 600;
  border: none;
}

.remote-panel__btn--open:hover {
  background: #89b4fa;
}
```

Note: `.remote-panel__peer-header` margin-bottom reduced from 8px to 4px to accommodate the new `.remote-panel__peer-meta` sub-label between header and session list. The total visual spacing (4px + meta height + 8px) preserves the peer group separation.

CRITICAL: The `@keyframes spin` does NOT already exist in style.css. Verify before appending. If a `spin` keyframe already exists, skip that declaration.
  </action>
  <verify>
    <automated>grep -c "remote-panel" /Users/ken/dev/agenthub/frontend/src/style.css && grep -c "#7aa2f7" /Users/ken/dev/agenthub/frontend/src/style.css</automated>
  </verify>
  <acceptance_criteria>
    - style.css contains `.remote-panel {`
    - style.css contains `.remote-panel__loading`
    - style.css contains `.remote-panel__spinner`
    - style.css contains `@keyframes spin`
    - style.css contains `.remote-panel__empty`
    - style.css contains `.remote-panel__empty-title`
    - style.css contains `.remote-panel__empty-body`
    - style.css contains `.remote-panel__peer-header`
    - style.css contains `.remote-panel__peer-meta`
    - style.css contains `.remote-panel__session-row`
    - style.css contains `.remote-panel__status--running`
    - style.css contains `.remote-panel__status--idle`
    - style.css contains `.remote-panel__status--waiting`
    - style.css contains `.remote-panel__status--errored`
    - style.css contains `.remote-panel__name`
    - style.css contains `.remote-panel__cli`
    - style.css contains `.remote-panel__btn--open`
    - style.css contains `background: #7aa2f7` (accent CTA)
    - style.css contains `letter-spacing: 0.08em` (peer header)
    - grep count for "remote-panel" is >= 26 (enough rules including peer-meta)
  </acceptance_criteria>
  <done>All .remote-panel BEM CSS rules including .remote-panel__peer-meta and @keyframes spin added to style.css per UI-SPEC design contract</done>
</task>

<task type="auto">
  <name>Task 3: Create RemoteSessionsPanel tests</name>
  <files>frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/__tests__/DaemonManagerPanel.test.tsx (test conventions: ?raw import, createRoot, flushSync, source inspection)
    - frontend/src/components/RemoteSessionsPanel.tsx (just created — verify exports and class names)
  </read_first>
  <action>
Create `frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx`:

```typescript
import { describe, it, expect, vi, afterEach } from 'vitest'
import React from 'react'
import { createRoot } from 'react-dom/client'
import { flushSync } from 'react-dom'
import raw from '../../components/RemoteSessionsPanel.tsx?raw'
import type { RemoteSessionsPanelProps, RemotePeerSessions } from '../../components/RemoteSessionsPanel'
import { RemoteSessionsPanel } from '../../components/RemoteSessionsPanel'

const mockPeers: RemotePeerSessions[] = [
  {
    hostname: 'macbook-pro',
    sessions: [
      { id: 'sess-1', name: 'claude 1', cliType: 'claude', status: 'running', url: 'https://macbook-pro.ts.net:7443/sessions/sess-1' },
      { id: 'sess-2', name: 'codex 1', cliType: 'codex', status: 'idle', url: 'https://macbook-pro.ts.net:7443/sessions/sess-2' },
    ],
  },
  {
    hostname: 'dev-server',
    sessions: [
      { id: 'sess-3', name: 'claude 2', cliType: 'claude', status: 'waiting', url: 'https://dev-server.ts.net:7443/sessions/sess-3' },
    ],
  },
]

function renderPanel(props: Partial<RemoteSessionsPanelProps> = {}) {
  const defaults: RemoteSessionsPanelProps = {
    peers: [],
    loading: false,
    onOpen: vi.fn(),
  }
  const merged = { ...defaults, ...props }
  const container = document.createElement('div')
  document.body.appendChild(container)
  const root = createRoot(container)
  flushSync(() => {
    root.render(React.createElement(RemoteSessionsPanel, merged))
  })
  return { container, root }
}

afterEach(() => {
  document.body.innerHTML = ''
})

// --- Source Inspection Tests ---

describe('RemoteSessionsPanel source inspection', () => {
  it('exports RemoteSessionsPanel function component', () => {
    expect(raw).toContain('export function RemoteSessionsPanel')
  })

  it('exports RemoteSessionsPanelProps interface', () => {
    expect(raw).toContain('export interface RemoteSessionsPanelProps')
  })

  it('exports RemotePeerSessions interface', () => {
    expect(raw).toContain('export interface RemotePeerSessions')
  })

  it('exports RemoteSession interface', () => {
    expect(raw).toContain('export interface RemoteSession')
  })

  it('uses BEM class remote-panel__session-row', () => {
    expect(raw).toContain('remote-panel__session-row')
  })

  it('uses BEM class remote-panel__spinner', () => {
    expect(raw).toContain('remote-panel__spinner')
  })

  it('uses BEM class remote-panel__peer-header', () => {
    expect(raw).toContain('remote-panel__peer-header')
  })

  it('uses BEM class remote-panel__btn--open', () => {
    expect(raw).toContain('remote-panel__btn--open')
  })

  it('contains Open Session button text', () => {
    expect(raw).toContain('Open Session')
  })

  it('calls onOpen with session url', () => {
    expect(raw).toContain('onOpen(s.url)')
  })

  it('contains web-enabled sessions constraint copy', () => {
    expect(raw).toContain('Shows web-enabled sessions only')
  })

  it('uses BEM class remote-panel__peer-meta for constraint sub-label', () => {
    expect(raw).toContain('remote-panel__peer-meta')
  })
})

// --- DOM Rendering Tests ---

describe('RemoteSessionsPanel DOM', () => {
  it('renders loading state when loading and no peers', () => {
    const { container } = renderPanel({ loading: true, peers: [] })
    const loading = container.querySelector('.remote-panel__loading')
    expect(loading).toBeTruthy()
    expect(loading!.textContent).toContain('Probing peers...')
    const spinner = container.querySelector('.remote-panel__spinner')
    expect(spinner).toBeTruthy()
  })

  it('renders empty state when not loading and no peers', () => {
    const { container } = renderPanel({ loading: false, peers: [] })
    const empty = container.querySelector('.remote-panel__empty')
    expect(empty).toBeTruthy()
    const title = container.querySelector('.remote-panel__empty-title')
    expect(title).toBeTruthy()
    expect(title!.textContent).toBe('No remote peers found')
    const body = container.querySelector('.remote-panel__empty-body')
    expect(body).toBeTruthy()
    expect(body!.textContent).toBe('No tailnet peers are running AgentHub.')
  })

  it('renders peer group headers', () => {
    const { container } = renderPanel({ peers: mockPeers })
    const headers = container.querySelectorAll('.remote-panel__peer-header')
    expect(headers.length).toBe(2)
    expect(headers[0].textContent).toBe('macbook-pro')
    expect(headers[1].textContent).toBe('dev-server')
  })

  it('renders peer-meta sub-label below each peer header', () => {
    const { container } = renderPanel({ peers: mockPeers })
    const metas = container.querySelectorAll('.remote-panel__peer-meta')
    expect(metas.length).toBe(2)
    expect(metas[0].textContent).toBe('Shows web-enabled sessions only')
    expect(metas[1].textContent).toBe('Shows web-enabled sessions only')
  })

  it('renders session rows with name, cli, and status', () => {
    const { container } = renderPanel({ peers: mockPeers })
    const rows = container.querySelectorAll('.remote-panel__session-row')
    expect(rows.length).toBe(3)

    // First row: claude 1
    const name0 = rows[0].querySelector('.remote-panel__name')
    expect(name0!.textContent).toBe('claude 1')
    const cli0 = rows[0].querySelector('.remote-panel__cli')
    expect(cli0!.textContent).toBe('claude')
    const status0 = rows[0].querySelector('.remote-panel__status')
    expect(status0!.classList.contains('remote-panel__status--running')).toBe(true)
  })

  it('Open button calls onOpen with session url', () => {
    const onOpen = vi.fn()
    const { container } = renderPanel({ peers: mockPeers, onOpen })
    const buttons = container.querySelectorAll('.remote-panel__btn--open')
    expect(buttons.length).toBe(3)

    // Click the first Open button
    ;(buttons[0] as HTMLButtonElement).click()
    expect(onOpen).toHaveBeenCalledWith('https://macbook-pro.ts.net:7443/sessions/sess-1')
  })

  it('shows data (not loading) when loading=true but peers exist', () => {
    const { container } = renderPanel({ loading: true, peers: mockPeers })
    // Should show peer data, not the loading spinner
    const headers = container.querySelectorAll('.remote-panel__peer-header')
    expect(headers.length).toBe(2)
    const loadingEl = container.querySelector('.remote-panel__loading')
    expect(loadingEl).toBeNull()
  })
})
```

CRITICAL: The last test verifies that when `loading=true` but `peers.length > 0`, the data is shown (not the spinner). This prevents the 30s refresh flicker anti-pattern described in RESEARCH.md Pitfall 1.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test run RemoteSessionsPanel</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx exists
    - Test file contains `import raw from '../../components/RemoteSessionsPanel.tsx?raw'`
    - Test file contains `describe('RemoteSessionsPanel source inspection'`
    - Test file contains `describe('RemoteSessionsPanel DOM'`
    - Test file contains at least 12 `it(` blocks (10 original + 2 new for peer-meta)
    - Test file contains `Shows web-enabled sessions only` assertion
    - Test file contains `remote-panel__peer-meta` assertion
    - `pnpm test run RemoteSessionsPanel` exits 0 with all tests passing
  </acceptance_criteria>
  <done>RemoteSessionsPanel has comprehensive source inspection and DOM tests covering loading, empty, populated states, peer-meta sub-label, and Open button callback; all tests pass</done>
</task>

</tasks>

<verification>
```bash
cd /Users/ken/dev/agenthub/frontend && pnpm test run RemoteSessionsPanel
```
</verification>

<success_criteria>
- RemoteSessionsPanel.tsx exports the component with correct props interface
- RemoteSessionsPanel.tsx renders "Shows web-enabled sessions only" sub-label per peer group
- All BEM CSS rules in style.css match UI-SPEC design contract values, including .remote-panel__peer-meta
- All source inspection and DOM tests pass
- Loading, empty, and populated states render correctly
- Open Session button calls onOpen with the session URL
</success_criteria>

<output>
After completion, create `.planning/phases/52-remote-sessions-gui-panel/52-02-SUMMARY.md`
</output>
