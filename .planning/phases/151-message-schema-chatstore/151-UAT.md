---
status: complete
phase: 151-message-schema-chatstore
source: [151-01-SUMMARY.md, 151-02-SUMMARY.md, 151-03-SUMMARY.md]
started: 2026-06-25T22:00:00Z
updated: 2026-06-25T22:45:00Z
---

## Current Test

[testing complete]

## Tests

### 1. JSONL persistence + restart survival
expected: Messages append to <sessionID>.jsonl; after daemon restart the file is intact and GET /api/chat/<id>/history returns the full thread.
result: pass
source: automated-evidence
evidence: TestChatRoutes_RestartSurvival + TestChatStoreRestartSurvival (ran fresh, -count=1, post-fix). Non-circular: Engine 1 writes via real AppendMessage to a real temp JSONL; a FRESH Engine 2 over the same dir (real restart + loadFromDisk) serves a REAL http.Get to /api/chat/<id>/history and the response decodes to exactly the 2 written messages in order. Real serve path via httptest.NewServer(api.RelayHandler()), not a mock.

### 2. Late-join full history replay
expected: A participant joining after a restart or late connection receives the complete message history in a single response — no messages missing.
result: pass
source: automated-evidence
evidence: Same restart-survival route test returns the complete thread (2/2 messages) in a single GET response after a fresh engine load. Messages() returns a full in-order copy; the route encodes the entire slice in one response body.

### 3. KillSession removes JSONL — no orphans
result: pass
source: automated-evidence
evidence: TestEngineChatStoreFor_AfterKill (ran fresh). Real session lifecycle: CreateSession(ctx, "cat", …) spawns a REAL PTY process and store; AppendMessage writes a real JSONL file; os.Stat confirms it EXISTS; KillSession then makes ChatStoreFor ok=false AND os.Stat confirms os.IsNotExist (file deleted, no orphan). This is an in-process live drive of the real engine, not a stub.

### 4. 10,000-message hard cap
result: pass
source: automated-evidence
evidence: TestChatCapEnforcement (ran fresh, 0.24s). Appends exactly MaxChatMessages=10,000 via real AppendMessage; the 10,001st returns ErrChatCapReached; mirror stays at 10,000 AND the on-disk file has exactly 10,000 lines (no extra write). No production REST ingestion surface exists at phase 151 (the writer arrives in phase 152), so this internal guarantee is verified at the unit boundary against real disk I/O.

### 5. Markdown export round-trips all fields
result: pass
source: automated-evidence
evidence: TestChatExportFields (ran fresh) writes 3 messages with distinct AuthorAlias/AuthorID/Content and asserts each field appears in real Export() output; injection marker covered separately. Export is served over real HTTP on both the relay loopback route and the cap-gated webserver route (text/markdown + Content-Disposition headers asserted in route tests). IN-02 hardening (invalid session id → 404) confirmed by TestChatRoutes_InvalidSessionID.

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0

## Gaps

[none]

## Deferred to later phases (not phase-151 gaps)

Phase 151 builds daemon-side persistence + read-only REST routes only. The following
cannot be exercised end-to-end until later phases exist and are intentionally NOT
counted as phase-151 gaps:

- **Real inbound message ingestion** — nothing calls AppendMessage in production yet;
  the relay/WS chat-ingestion path is Phase 152 (Relay Protocol + Identity + Presence).
  Until then, a live daemon serves an empty thread for a real session (correct behavior).
- **GUI / web-share chat panel rendering + cross-surface parity** — the user-visible
  chat UI is a later milestone phase. The REST contract it will consume is verified here.
