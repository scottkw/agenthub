---
phase: 130-remote-browse-gui-on-ramp
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx
autonomous: true
requirements: [RB-04]
must_haves:
  truths:
    - "Failing/updated vitest assertions encode honest per-peer states: Unreachable badge, No shareable sessions text, never false No remote peers found"
    - "Existing 'Shows web-enabled sessions only' assertions are updated to 'Shows shareable sessions' (UI-SPEC copy contract)"
  artifacts:
    - path: "frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx"
      provides: "RB-04 per-peer honest-state rendering tests"
      contains: "Unreachable"
  key_links:
    - from: "RemoteSessionsPanel.test.tsx"
      to: "RemoteSessionsPanel reachable prop"
      via: "peer fixtures with reachable: false / reachable: true + empty sessions"
      pattern: "reachable"
---

<objective>
Update and extend the RemoteSessionsPanel vitest suite to encode the RB-04 honest-state contract BEFORE the panel implementation lands. This includes (a) updating the existing copy assertions to the UI-SPEC string "Shows shareable sessions", and (b) adding new failing tests for the per-peer states: unreachable badge, reachable-but-zero-sessions text, and the invariant that "No remote peers found" is NEVER shown when at least one peer was probed.

Purpose: The panel currently renders all peers uniformly and drops empty peers upstream. These tests lock the honest-state behavior the colorblind user depends on — every state must be text-labeled, never color-only.

Output: One extended test file. The new per-peer-state tests MUST be RED (the panel does not yet branch on `reachable`); the updated copy assertions will also be RED until the panel implementation (plan 04) changes the literal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-remote-browse-gui-on-ramp/130-UI-SPEC.md
@.planning/phases/130-remote-browse-gui-on-ramp/130-RESEARCH.md

<interfaces>
<!-- The RemotePeerSessions shape the panel consumes. `reachable` is ADDED in plan 04.
     Tests in THIS plan pass fixtures with `reachable` and assert per-peer states. -->

RemotePeerSessions (frontend, extended in plan 04):
  { hostname: string; reachable: boolean; sessions: RemoteSession[] }
RemoteSession: { id, name, cliType (or cli_type per binding), status, url }

UI-SPEC Copywriting Contract (the assertions to encode):
  - Peer meta line (sessions shown): "Shows shareable sessions"  (was "Shows web-enabled sessions only")
  - Reachable, zero sessions: title "No shareable sessions", body "This peer has no sessions with web-sharing enabled."
  - Unreachable peer: text badge "Unreachable"
  - Empty panel (zero peers probed): "No remote peers found" + "No tailnet peers are running AgentHub." — ONLY for genuinely zero peers.

Existing test anchors (RemoteSessionsPanel.test.tsx):
  - line 90: expect(raw).toContain('Shows web-enabled sessions only')   ← update to 'Shows shareable sessions'
  - line 116: expect(title!.textContent).toBe('No remote peers found')   ← keep, but scope to zero-peer case
  - lines 134-135: expect(metas[n].textContent).toBe('Shows web-enabled sessions only')  ← update to 'Shows shareable sessions'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update copy assertions + add RB-04 per-peer-state tests (RED)</name>
  <files>frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx (full file — existing fixtures, the source-inspection test at ~line 90, the DOM meta assertions at ~lines 134-135, the empty-panel test at ~line 116)
    - frontend/src/components/RemoteSessionsPanel.tsx (current render to understand existing structure, props, and class names)
    - .planning/phases/130-remote-browse-gui-on-ramp/130-UI-SPEC.md (Copywriting Contract; State Inventory §Per-peer session-list states; Accessibility Contract)
    - .planning/phases/130-remote-browse-gui-on-ramp/130-RESEARCH.md (Pitfall 5 — exact test lines to update; Pattern 4 render branching)
  </read_first>
  <behavior>
    - Updated copy: every assertion of the literal "Shows web-enabled sessions only" (source-inspection at ~line 90 and DOM assertions at ~lines 134-135) becomes "Shows shareable sessions". No remaining reference to the old string anywhere in the test file.
    - New: renders "Unreachable" text for a peer fixture with reachable=false; the word "Unreachable" is present in the DOM (text-first, per colorblind mandate) — not merely a color class.
    - New: renders "No shareable sessions" title + "This peer has no sessions with web-sharing enabled." body for a peer with reachable=true and sessions=[].
    - New: given a peers array with at least one probed peer (reachable=false OR reachable=true-empty), the panel does NOT render "No remote peers found" — that empty-state copy appears only when peers.length === 0.
    - Existing: the "No remote peers found" empty-state test is preserved but scoped to the zero-peer input.
  </behavior>
  <action>Edit frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx. Replace all occurrences of the literal 'Shows web-enabled sessions only' with 'Shows shareable sessions' (source-inspection assertion ~line 90 and DOM meta assertions ~lines 134-135). Add fixtures carrying the new `reachable` field. Add three describe/it blocks: (1) an unreachable peer (reachable:false) renders text "Unreachable"; (2) a reachable peer with sessions:[] renders "No shareable sessions" and the body "This peer has no sessions with web-sharing enabled."; (3) a panel given ≥1 probed peer never renders "No remote peers found" (query the DOM and assert absence). Use the project's existing render/query helpers already imported in this test file (testing-library + vitest). Do NOT modify RemoteSessionsPanel.tsx in this plan — the new state tests MUST be RED until plan 04. camelCase per CLAUDE.md. Run `cd frontend && pnpm test -- RemoteSessionsPanel` after editing to confirm RED on the new/updated assertions.</action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm test -- RemoteSessionsPanel.test 2>&1 | grep -Eq "fail|FAIL|✗|✖|[1-9][0-9]* failed" &amp;&amp; echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Shows web-enabled sessions only' frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx` returns 0 (old literal fully removed).
    - The test file contains assertions for 'Shows shareable sessions', 'Unreachable', 'No shareable sessions', and 'This peer has no sessions with web-sharing enabled.'.
    - A test asserts "No remote peers found" is absent when ≥1 peer is probed.
    - `pnpm test -- RemoteSessionsPanel.test` is RED (panel impl not yet updated) — verify prints RED-CONFIRMED.
    - RemoteSessionsPanel.tsx is unmodified by this plan.
  </acceptance_criteria>
  <done>Panel test suite updated for the new copy and extended with RB-04 honest-state tests; suite is RED pending plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend RPC → React panel | The panel renders peer/session metadata only (no caps/content). This plan's tests assert state rendering, not data exposure (covered by plan 01 RB-03 tests). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-04 | Information Disclosure (UX integrity) | misleading panel state hiding a reachable peer | mitigate | Tests pin the honest-state contract: a probed peer is never reported as "No remote peers found"; unreachable and empty states are distinctly text-labeled |
| T-130-SC | Tampering | npm installs | accept | No new packages; uses existing vitest/testing-library already in frontend |
</threat_model>

<verification>
- `cd frontend && pnpm test -- RemoteSessionsPanel.test` → RED (new state tests fail; copy assertions fail until panel updates the literal)
- RED here is EXPECTED. Plan 04 implementation turns the suite green.
- `grep -c 'Shows web-enabled sessions only' frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx` → 0
</verification>

<success_criteria>
- Old copy literal fully removed from the test file.
- New RB-04 per-peer-state tests present and RED.
- Colorblind contract encoded: state distinctions asserted via text labels, not color classes.
- No source component modified.
</success_criteria>

<output>
Create `.planning/phases/130-remote-browse-gui-on-ramp/130-02-SUMMARY.md` when done
</output>
