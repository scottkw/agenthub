---
phase: 130-remote-browse-gui-on-ramp
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/webserver/sessions_meta_test.go
  - internal/tailnet/tailnet_test.go
  - app_test.go
  - internal/daemon/relay_remote_files_test.go
autonomous: true
requirements: [RB-01, RB-03, RB-05]
must_haves:
  truths:
    - "Failing Go tests assert the /api/sessions/meta endpoint shape (metadata only, no cap, no content)"
    - "Failing Go tests assert FetchAllPeerSessionsMeta includes unreachable and reachable-but-empty peers (no silent drop)"
    - "Failing Go test asserts GetRemoteSessionsWithMeta exposes a Reachable field per peer"
    - "Failing relay-surface test (RB-05) drives discover→pick→browse through api.RelayHandler()"
  artifacts:
    - path: "internal/webserver/sessions_meta_test.go"
      provides: "RB-01/RB-03 webserver endpoint contract tests"
      contains: "TestSessionsMeta"
    - path: "internal/tailnet/tailnet_test.go"
      provides: "RB-01 FetchAllPeerSessionsMeta no-silent-drop tests"
      contains: "TestFetchAllPeerSessionsMeta"
    - path: "app_test.go"
      provides: "RB-01 GetRemoteSessionsWithMeta Reachable-field test"
      contains: "TestGetRemoteSessionsWithMeta"
    - path: "internal/daemon/relay_remote_files_test.go"
      provides: "RB-05 relay-surface discover→pick→browse test"
      contains: "TestRemoteFiles_DiscoverAndBrowse_RelaySurface"
  key_links:
    - from: "internal/daemon/relay_remote_files_test.go"
      to: "api.RelayHandler()"
      via: "httptest.NewServer(api.RelayHandler())"
      pattern: "httptest.NewServer\\(api.RelayHandler\\(\\)\\)"
---

<objective>
Create the Wave 0 failing Go tests for Phase 130's backend surfaces BEFORE any implementation exists. These tests encode the discover→list→pick contract and the RB-03 no-enumeration security guarantee. They MUST be RED at the end of this plan (the production functions they call do not exist yet).

Purpose: Lock the behavioral contract first (Nyquist). The RB-05 relay-surface test is release-blocking — it guards the exact v3.5 blind spot where only the webserver/fixture surface was tested while the relay loopback (the path the Wails GUI actually uses) was broken across 4 layers.

Output: Four Go test files (one new, three extended) that compile-fail or assert-fail against current main because `handleSessionsMeta`, `FetchAllPeerSessionsMeta`, `GetRemoteSessionsWithMeta`, and the `/api/sessions/meta` fixture handler do not yet exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-remote-browse-gui-on-ramp/130-RESEARCH.md
@.planning/phases/130-remote-browse-gui-on-ramp/130-VALIDATION.md

<interfaces>
<!-- Contracts the tests assert against. These functions/handlers are CREATED in Wave 1 (plan 03).
     The tests in THIS plan reference them and MUST fail to compile/assert until plan 03 lands. -->

Webserver (internal/webserver/server.go) — to be created in plan 03:
  GET /api/sessions/meta  → handleSessionsMeta (open, NO requireCapability wrapper)
  Response: JSON array of { id, name, cli_type, status, url } — metadata ONLY.
  Existing helpers the handler uses: ws.webEnabledSessions() (server.go:201),
  ws.sessionResolver (server.go:84, signature: func(id) (name, cliType, status, hostname string)),
  ws.BaseURL() (server.go:398).

Tailnet (internal/tailnet/sessions.go) — to be created in plan 03:
  type ShareableSessionMeta struct { ID, Name, CLIType, Status, URL string }
  type PeerSessionMetaGroup struct { Hostname string; Reachable bool; Sessions []ShareableSessionMeta }
  func FetchPeerSessionsMeta(ctx, peer Peer) ([]ShareableSessionMeta, bool)
  func FetchAllPeerSessionsMeta(ctx, peers []Peer) []PeerSessionMetaGroup
  Existing: type Peer (has DNSName, Hostname, TailscaleIPs), DefaultProbePort const.

app.go (main package) — to be created in plan 03:
  type RemotePeerSessions struct { Hostname string; Reachable bool; Sessions []RemoteSession }  (Reachable added)
  func (a *App) GetRemoteSessionsWithMeta() []RemotePeerSessions

Relay test harness (internal/daemon/remote_files_parity_test.go + relay_remote_files_test.go) — EXISTING:
  newFixtureRemotePeer(t) *httptest.Server  (parity_test.go:88) — mounts /api/files/list, /api/files/stat, /api/files/read, /api/files/write, /join/exchange under a cap guard
  newDaemonAPIWithUpstreamCert(t, upstream) *daemon.API  (parity_test.go:151)
  depositCapOnSocket(t, socketSrv, sessionID, baseURL, cap)  (relay_remote_files_test.go:46)
  Existing example: TestRemoteFiles_MountedOnRelay (relay_remote_files_test.go:63) — drives httptest.NewServer(api.RelayHandler())
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RB-01/RB-03 webserver metadata-endpoint tests (RED)</name>
  <files>internal/webserver/sessions_meta_test.go</files>
  <read_first>
    - internal/webserver/server.go (handleListSessions at :733, webEnabledSessions at :201, sessionResolver at :84, BaseURL at :398, setupRoutes at :429)
    - internal/webserver/server_test.go (existing test patterns for constructing a WebServer + httptest)
    - .planning/phases/130-remote-browse-gui-on-ramp/130-RESEARCH.md (Code Examples §handleSessionsMeta; Security Domain table)
  </read_first>
  <behavior>
    - TestSessionsMeta_ReturnsWebEnabledSessions: GET /api/sessions/meta with N web-enabled sessions returns 200 + JSON array of N items, each with id/name/cli_type/status/url populated from sessionResolver.
    - TestSessionsMeta_NoCap: GET /api/sessions/meta with NO cap header returns 200 (not 401) — proves the endpoint is open, unlike cap-gated /api/sessions.
    - TestSessionsMeta_EmptyWhenNoneEnabled: with zero web-enabled sessions, returns 200 + empty JSON array `[]` (not null, not 404).
    - TestSessionsMeta_NoCapInResponse (RB-03): the response body contains NO `cap`, `token`, `grant`, `grants`, `content`, or signing-key fields — assert by decoding into a permissive map and checking the allowed key set is exactly {id,name,cli_type,status,url}.
  </behavior>
  <action>Create internal/webserver/sessions_meta_test.go in package webserver (or webserver_test, matching the existing server_test.go package). Construct a WebServer with a sessionResolver stub and a populated webEnabled set using the same construction pattern already used in server_test.go. Drive requests via httptest against the mux from setupRoutes() (or ws.Handler() if exposed). Assert the four behaviors above against the to-be-created GET /api/sessions/meta route and handleSessionsMeta handler. For RB-03, decode each response item into map[string]any and assert the key set equals exactly {"id","name","cli_type","status","url"} — fail if any cap/grant/content/token key is present. Do NOT implement the handler — these tests MUST fail (compile or assert) on current main. Follow CLAUDE.md Go conventions (gofmt, no silent fallbacks).</action>
  <verify>
    <automated>go test ./internal/webserver/... -run TestSessionsMeta -count=1 2>&1 | grep -Eq "FAIL|undefined|cannot|no such|build failed" &amp;&amp; echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - File internal/webserver/sessions_meta_test.go exists and defines TestSessionsMeta_ReturnsWebEnabledSessions, TestSessionsMeta_NoCap, TestSessionsMeta_EmptyWhenNoneEnabled, TestSessionsMeta_NoCapInResponse.
    - `go test ./internal/webserver/... -run TestSessionsMeta` is RED (the route/handler does not exist yet) — the verify command prints RED-CONFIRMED.
    - The RB-03 test asserts an exact allowed-key whitelist {id,name,cli_type,status,url}; no cap/grant/content key tolerated.
  </acceptance_criteria>
  <done>Webserver metadata tests committed and RED; allowed-key whitelist encoded for RB-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RB-01 tailnet + app.go RPC tests (RED)</name>
  <files>internal/tailnet/tailnet_test.go, app_test.go</files>
  <read_first>
    - internal/tailnet/sessions.go (FetchPeerSessions :38, FetchAllPeerSessions :83, the silent-drop at :93, doFetchSessions :114, DefaultProbePort, type Peer)
    - internal/tailnet/tailnet_test.go (existing test patterns — httptest servers acting as fake peers, Peer construction)
    - app.go (GetRemoteSessions :1082, RemotePeerSessions :58, RemoteSession :49)
    - app_test.go (existing App construction / DaemonClient stubbing pattern)
    - .planning/phases/130-remote-browse-gui-on-ramp/130-RESEARCH.md (Code Examples §FetchPeerSessionsMeta/FetchAllPeerSessionsMeta/GetRemoteSessionsWithMeta; doFetchSessionsMeta nil-vs-empty note)
  </read_first>
  <behavior>
    - TestFetchAllPeerSessionsMeta_IncludesUnreachablePeers: given a peer whose /api/sessions/meta is unreachable (closed server / connection refused), FetchAllPeerSessionsMeta still emits a group for it with Reachable=false and Sessions=[] (empty slice, not dropped).
    - TestFetchAllPeerSessionsMeta_EmptySessionsNotDropped: given a reachable peer that returns 200 `[]`, the group has Reachable=true and Sessions=[] — the peer is NOT dropped (this is the RB-01 fix vs sessions.go:93).
    - TestFetchAllPeerSessionsMeta_PopulatedPeer: given a reachable peer returning 200 with 2 metas, the group has Reachable=true and 2 sessions with fields mapped.
    - TestFetchPeerSessionsMeta_IPFallback: when the DNS host fails but a TailscaleIP is present, the IP-fallback path is exercised (mirror the existing FetchPeerSessions IP-fallback test if one exists; otherwise assert the fallback returns the served metas with ServerName set).
    - TestGetRemoteSessionsWithMeta_ReachableField (app_test.go): GetRemoteSessionsWithMeta returns []RemotePeerSessions where each element carries a Reachable bool reflecting the underlying group; an unreachable peer maps to Reachable=false with empty Sessions.
  </behavior>
  <action>Extend internal/tailnet/tailnet_test.go with the four TestFetchAllPeerSessionsMeta_*/TestFetchPeerSessionsMeta_* tests, using httptest servers that serve /api/sessions/meta (200 with JSON array, 200 `[]`, and a closed/refused server for the unreachable case). Build Peer values pointing DNSName/TailscaleIPs at the httptest hosts following the existing fake-peer test pattern. Extend app_test.go with TestGetRemoteSessionsWithMeta_ReachableField, stubbing the DaemonClient's ListTailnetPeers to return fake peers and asserting the Reachable field surfaces. Reference the to-be-created ShareableSessionMeta/PeerSessionMetaGroup/FetchAllPeerSessionsMeta/FetchPeerSessionsMeta and App.GetRemoteSessionsWithMeta — do NOT implement them. Tests MUST be RED. Assert the critical nil-vs-empty distinction: unreachable→Reachable=false, reachable-empty→Reachable=true+len 0. Run gofmt.</action>
  <verify>
    <automated>go test ./internal/tailnet/... -run TestFetchAllPeerSessionsMeta -count=1 2>&1 | grep -Eq "FAIL|undefined|cannot|build failed" &amp;&amp; go test . -run TestGetRemoteSessionsWithMeta -count=1 2>&1 | grep -Eq "FAIL|undefined|cannot|build failed" &amp;&amp; echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - tailnet_test.go defines TestFetchAllPeerSessionsMeta_IncludesUnreachablePeers, TestFetchAllPeerSessionsMeta_EmptySessionsNotDropped, TestFetchAllPeerSessionsMeta_PopulatedPeer, TestFetchPeerSessionsMeta_IPFallback.
    - app_test.go defines TestGetRemoteSessionsWithMeta_ReachableField.
    - Both run RED (functions undefined) — verify prints RED-CONFIRMED.
    - The unreachable-vs-reachable-empty distinction is explicitly asserted (Reachable=false vs Reachable=true with empty slice).
  </acceptance_criteria>
  <done>Tailnet and app.go RPC tests committed and RED; the no-silent-drop and nil-vs-empty contracts are encoded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write RB-05 relay-surface discover→pick→browse test (RED, release-blocking)</name>
  <files>internal/daemon/relay_remote_files_test.go</files>
  <read_first>
    - internal/daemon/relay_remote_files_test.go (TestRemoteFiles_MountedOnRelay :63, depositCapOnSocket :46, the httptest.NewServer(api.RelayHandler()) pattern :75)
    - internal/daemon/remote_files_parity_test.go (newFixtureRemotePeer :88, newDaemonAPIWithUpstreamCert :151, the mux.HandleFunc fixture pattern :106, /join/exchange :140, fixtureCap)
    - internal/daemon/relay_remote_files.go (wrapRelayWithRemoteFiles, handleRemoteFilesList)
    - .planning/phases/130-remote-browse-gui-on-ramp/130-RESEARCH.md (Pitfall 7 — exact RB-05 scope; Common Pitfalls §relay surface)
  </read_first>
  <behavior>
    - TestRemoteFiles_DiscoverAndBrowse_RelaySurface: a fixture remote peer exposes GET /api/sessions/meta (returning one shareable session's metadata) in addition to the existing file routes. The test (1) confirms the metadata is fetchable from the fixture peer, (2) deposits a cap for that session via depositCapOnSocket, then (3) drives GET /api/files/remote/{sessionID}/list through httptest.NewServer(api.RelayHandler()) — the loopback the Wails webview uses — and asserts a 200 with the fixture's file listing. This ties discover→pick→browse together on the relay surface, not the webserver/fixture surface alone.
  </behavior>
  <action>Extend internal/daemon/relay_remote_files_test.go with TestRemoteFiles_DiscoverAndBrowse_RelaySurface. Reuse newFixtureRemotePeer to build the upstream peer, but add a `GET /api/sessions/meta` handler to its mux that returns a one-element JSON array of {id,name,cli_type,status,url} for the fixture session id (extend newFixtureRemotePeer to mount this route, or build a local mux wrapping it — keep the existing file routes intact). Construct the daemon API via newDaemonAPIWithUpstreamCert, deposit the cap with depositCapOnSocket, stand up relaySrv := httptest.NewServer(api.RelayHandler()), and issue GET {relaySrv.URL}/api/files/remote/{sid}/list asserting status 200 and a body containing the fixture's listed entry. Do NOT modify production relay code — this test MUST be RED only because the fixture's /api/sessions/meta route and the discover step do not exist yet (the file-browse relay path itself already works; the test should fail to compile/assert until the fixture handler is added here and the meta endpoint exists). Run gofmt.</action>
  <verify>
    <automated>go test ./internal/daemon/... -run TestRemoteFiles_DiscoverAndBrowse_RelaySurface -count=1 2>&1 | grep -Eq "FAIL|undefined|cannot|build failed|no test" &amp;&amp; echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - relay_remote_files_test.go defines TestRemoteFiles_DiscoverAndBrowse_RelaySurface.
    - The test drives the browse step through httptest.NewServer(api.RelayHandler()) (grep-verifiable: the test body uses api.RelayHandler()).
    - The test asserts a metadata fetch (discover) AND a relay file-list 200 (browse) — both steps present.
    - Test is RED on current main — verify prints RED-CONFIRMED.
    - Existing TestRemoteFiles_MountedOnRelay and other relay tests are unmodified and still present.
  </acceptance_criteria>
  <done>RB-05 relay-surface test committed and RED; discover and relay-browse steps are both exercised through api.RelayHandler().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tailnet peer → webserver (`100.x` IP) | Remote tailnet caller hits the new open `/api/sessions/meta`; network-layer trust (Tailscale WireGuard mesh). Tests assert the response carries metadata only. |
| Wails webview → relay loopback (`127.0.0.1`) | The GUI's actual browse path; RB-05 test exercises this surface, not just the webserver fixture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-01 | Information Disclosure | `/api/sessions/meta` response (test-asserted) | mitigate | TestSessionsMeta_NoCapInResponse pins an exact allowed-key whitelist {id,name,cli_type,status,url}; any cap/grant/content/token key fails the test |
| T-130-02 | Information Disclosure | unreachable/empty peer drop hiding wrong state | mitigate | tailnet tests assert no peer is silently dropped (Reachable discriminator), preventing a future regression that masks reachability |
| T-130-03 | Tampering | relay-surface blind spot (v3.5-class) | mitigate | RB-05 test drives the discover→pick→browse path through api.RelayHandler() — the GUI loopback — closing the surface that was untested in v3.5 |
| T-130-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase (RESEARCH §Package Legitimacy Audit: not applicable); no install tasks |
</threat_model>

<verification>
- `go test ./internal/webserver/... -run TestSessionsMeta -count=1` → RED (handler/route absent)
- `go test ./internal/tailnet/... -run TestFetchAllPeerSessionsMeta -count=1` → RED
- `go test . -run TestGetRemoteSessionsWithMeta -count=1` → RED
- `go test ./internal/daemon/... -run TestRemoteFiles_DiscoverAndBrowse_RelaySurface -count=1` → RED
- All four RED states are EXPECTED and required at end of this plan. Implementation (plan 03) turns them green.
</verification>

<success_criteria>
- Four Go test files created/extended (sessions_meta_test.go new; tailnet_test.go, app_test.go, relay_remote_files_test.go extended).
- Every named test from VALIDATION.md Wave 0 (Go side) exists and is RED.
- RB-03 whitelist and RB-05 relay-surface coverage are both encoded.
- No production code modified — only test files.
- gofmt clean.
</success_criteria>

<output>
Create `.planning/phases/130-remote-browse-gui-on-ramp/130-01-SUMMARY.md` when done
</output>
