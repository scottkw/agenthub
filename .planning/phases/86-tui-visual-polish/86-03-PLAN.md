---
phase: 86-tui-visual-polish
plan: 03
type: execute
wave: 3
depends_on: [86-02]
files_modified: []
autonomous: false
requirements: [TUI-01, TUI-02, TUI-03, TUI-04]
must_haves:
  truths:
    - "Human has verified the TUI renders correctly with two-pane layout"
    - "Human has verified sidebar navigation and tab switching works"
    - "Human has verified session rows display colored agent badges"
    - "Human has verified TokyoNight color palette appears correct"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete TUI visual polish implementation.

Purpose: The TUI layout, colors, and interactions must be visually confirmed by the user since automated tests cannot validate visual aesthetics, alignment, or color rendering in a real terminal.

Output: Approval or issue list from visual inspection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/86-tui-visual-polish/86-CONTEXT.md
@.planning/phases/86-tui-visual-polish/86-UI-SPEC.md
@.planning/phases/86-tui-visual-polish/86-01-SUMMARY.md
@.planning/phases/86-tui-visual-polish/86-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of TUI visual polish</name>
  <files>none</files>
  <action>
Build and launch TUI for human visual verification. Complete TUI visual overhaul: two-pane sidebar + content layout, horizontal tab bar, bordered session frames with titles, colored [agent] badges, Home tab with branding/stats, Settings tab (read-only), Remote tab, focus-aware navigation (Tab toggles, [/] cycles tabs, arrow keys in sidebar), and TokyoNight color palette.

Pre-requisite: Ensure the daemon is running. Create at least 1 session if none exist.

Launch: `cd /Users/ken/dev/agenthub && go run . tui`

Verify sidebar (D-01): Left side shows narrow vertical sidebar with Home, Sessions, Remote, Settings. Sessions highlighted with accent color. Sidebar background slightly darker than content.

Verify tab bar (D-04): Top of content area shows "Sessions" tab (bold, underlined, accent color).

Verify session frame (D-08, TUI-01): Session list inside rounded-corner bordered frame. Frame has "Sessions" title in top border. Column headers inside frame with separator below.

Verify agent badges (D-11, D-12, TUI-03): Colored tags like [claude], [opencode]. Distinct colors per agent. Selected row highlighted.

Verify navigation (D-02, D-03, D-05, TUI-02): Tab toggles sidebar/content focus. Up/Down in sidebar. Enter opens tab. [/] cycles tabs.

Verify Home tab (D-13, D-14): AgentHub title, version, tagline, session stats, web/Tailscale status, hints.

Verify color palette (D-15, TUI-04): TokyoNight theme -- dark blue background, blue accents, status colors.

Verify attach/detach (D-06): Attach with Enter, detach with Ctrl-\, state preserved.

Verify help overlay: ? opens help showing Tab, [, ] bindings.
  </action>
  <verify>Human visual inspection confirms all elements render correctly</verify>
  <done>User types "approved" confirming the TUI visual polish meets expectations, or describes specific issues for remediation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A -- verification checkpoint only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | N/A | Verification | accept | No code changes in this plan |
</threat_model>

<verification>
Human visual inspection of running TUI.
</verification>

<success_criteria>
- User confirms sidebar renders correctly with 4 items
- User confirms tab navigation works (Tab, [, ], Enter)
- User confirms bordered session frame with title
- User confirms colored agent badges
- User confirms Home tab content
- User confirms TokyoNight palette
- User confirms attach/detach preserves state
</success_criteria>

<output>
After completion, create `.planning/phases/86-tui-visual-polish/86-03-SUMMARY.md`
</output>
