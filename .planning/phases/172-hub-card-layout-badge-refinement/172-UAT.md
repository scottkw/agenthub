---
status: complete
phase: 172-hub-card-layout-badge-refinement
source: [172-01-SUMMARY.md]
started: 2026-07-08T10:21:09Z
updated: 2026-07-08T10:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Status stays the primary top-line signal (D-03)
expected: Status text/icon stays above the chip row, unchipified, keeping the running spin animation and the attention pulse.
result: pass
source: automated
coverage_id: D1

### 2. Consolidated agent · origin quiet-chip row (D-01)
expected: Agent + origin render as one row of outlined quiet chips (7px rounded-rect, 1px border, transparent bg, muted text); origin chip is fully muted with no green-local/blue-remote color coding.
result: pass
source: automated
coverage_id: D2

### 3. INTERNET / FULL ACCESS filled exposure line (D-04/D-05)
expected: INTERNET and FULL ACCESS are the only filled chips, on their own dedicated right-aligned exposure line; both coexist when funnelActive AND funnelWriteActive; FULL ACCESS keeps its notched clip-path, 700 weight, LockOpenIcon.
result: pass
source: automated
coverage_id: D3

### 4. Muted meta line: uptime · viewers · Connected (D-06)
expected: Uptime, viewer count, and remote Connected/Available render together on one muted meta line below the chip row, with dot separators only between rendered items.
result: pass
source: automated
coverage_id: D4

### 5. Dark/light theme token parity for the new chip/meta rules (D-02)
expected: Both dark (:root) and light ([data-ui-theme="light"]) themes resolve the new chip/exposure/meta rules correctly through --hub-* tokens (visual/computed-style parity); chips remain readable and correctly colored in both themes.
result: pass

### 6. Spacing between the Open/Share button row and the mini-preview window
expected: A small amount of vertical separation between the Open/Share action row and the mini-preview window below it, showing clear separation.
result: issue
reported: "it looks like we need some padding between the buttons and the preview window on the card. Not much just enough to show separation"
severity: cosmetic
fix: "Added margin-bottom:8px to .hub-card__row5 (frontend/src/style.css). tsc+vite build passes. User re-confirmed on rebuilt app 2026-07-08 — separation reads right. RESOLVED."

## Summary

total: 6
passed: 5
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "The Open/Share action row has clear vertical separation from the mini-preview window below it"
  status: failed
  reason: "User reported: it looks like we need some padding between the buttons and the preview window on the card. Not much just enough to show separation"
  severity: cosmetic
  test: 6
  root_cause: ".hub-card__row5 (Open/Share action row, style.css:5112) has margin-top:4px but no margin-bottom; .hub-card__preview immediately follows with only a border-top, so the buttons sit flush against the preview window."
  artifacts:
    - path: "frontend/src/style.css"
      issue: ".hub-card__row5 rule (~line 5112) lacks a margin-bottom to separate the action row from the following .hub-card__preview"
  missing:
    - "Add a small margin-bottom (~8px) to .hub-card__row5 so the Open/Share buttons show separation from the mini-preview window"
  debug_session: ""
  resolution: "Fixed inline — margin-bottom:8px added to .hub-card__row5 (style.css:5112). Build (tsc+vite) passes. User re-confirmed on rebuilt app 2026-07-08 — RESOLVED."
