---
phase: 140-ui-spec-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [CARRY-02]
must_haves:
  truths:
    - "Issue #93 carries a comment recording the v4.0 triage outcome (all 7 deferred #78 items re-deferred)"
    - "The triage comment states the in-scope subset for Phase 141 is: none"
    - "Issue #93 remains OPEN after the triage"
  artifacts: []
  key_links:
    - from: "Phase 140 plan"
      to: "GitHub issue #93"
      via: "gh issue comment"
      pattern: "gh issue comment 93"
---

<objective>
Execute the #93 backlog triage for v4.0: post a GitHub comment on issue #93 recording the
triage outcome — all 7 deferred #78 fidelity items are re-deferred, the in-scope subset for
Phase 141 is none — and leave issue #93 OPEN.

Purpose: CARRY-02 requires the deferred #78 Hub-fidelity backlog (#93) to be triaged at
planning, with the in-scope subset delivered and the remainder explicitly re-deferred with
#93 updated. Per D-14/D-15 this triage is an execution action belonging to this phase, not
something already done in discussion. This satisfies ROADMAP success criterion #3.

Output: a comment on GitHub issue #93; #93 state OPEN. No repository files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/140-ui-spec-gate/140-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Post the v4.0 triage comment on issue #93 and confirm it stays open</name>
  <read_first>
    - .planning/phases/140-ui-spec-gate/140-CONTEXT.md (D-14 the 7 items + "in-scope subset: none"; D-15 this is an execution action; <deferred> section enumerates the 7 items verbatim)
    - .planning/REQUIREMENTS.md (CARRY-02 wording)
    - .planning/ROADMAP.md (Phase 140 success criterion #3; Phase 141 scope)
  </read_first>
  <action>
    First confirm the current state of issue #93 with `gh issue view 93 --json number,state,title`
    and confirm authentication with `gh auth status` (if `gh` returns an auth error, raise a
    blocking auth checkpoint and stop — do not fabricate the comment).

    Post a single comment to issue #93 using `gh issue comment 93 --body-file <path>` (write
    the body to a temp file first to preserve markdown formatting; do not inline a long
    multi-line body on the command line). The comment MUST record, per D-14:
    - Header: "v4.0 triage (Phase 140 UI-Spec Gate)".
    - Outcome: all 7 deferred #78 fidelity items are RE-DEFERRED; in-scope subset for Phase
      141 is **none** — each item is a net-new feature needing backend data or a new surface
      and does not fit a restyle milestone.
    - The 7 enumerated items verbatim from the CONTEXT <deferred> section: per-session usage
      metrics on cards (overlaps #67); formal "projects" model; member/collaborator avatars +
      presence; structured "agent suggests" briefings; session detail / chat-thread page;
      tweaks panel; pixel-faithful #78 port (conflicts with Hub-first).
    - A note that #93 is kept OPEN as the parent for these future-phase candidates.

    Do NOT close #93. After posting, verify the comment landed with
    `gh issue view 93 --comments` and verify state with `gh issue view 93 --json state`.
    Capture the resulting comment URL into the SUMMARY.
  </action>
  <acceptance_criteria>
    - `gh issue view 93 --json state` returns state "OPEN".
    - `gh issue view 93 --comments` output contains the string "v4.0 triage" (the triage comment is present).
    - The triage comment text contains "re-deferred" (case-insensitive) and "none" (the in-scope subset).
    - The triage comment enumerates all 7 items (contains the strings "usage metrics", "projects", "avatars" or "members", "agent suggests" or "briefings", "session detail" or "chat-thread", "tweaks panel", "pixel-faithful").
    - Issue #93 is NOT closed (no close event performed by this plan).
  </acceptance_criteria>
  <verify>
    <automated>gh issue view 93 --json state -q .state | grep -q "OPEN" && gh issue view 93 --comments 2>/dev/null | grep -qi "v4.0 triage" && gh issue view 93 --comments 2>/dev/null | grep -qi "re-deferred" && gh issue view 93 --comments 2>/dev/null | grep -qi "tweaks panel"</automated>
  </verify>
  <done>Issue #93 carries the v4.0 triage comment recording all 7 items re-deferred and in-scope subset = none, all 7 items are enumerated, and #93 remains OPEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local agent → GitHub API | One authenticated `gh issue comment` write to scottkw/agenthub issue #93. No untrusted input is consumed; the comment body is authored from locked CONTEXT decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-02 | Spoofing | `gh` auth identity | accept | `gh` uses the operator's existing authenticated session; comment is attributed to the authenticated GitHub user. Confirm `gh auth status` before writing. |
| T-140-03 | Repudiation | issue #93 comment | accept | GitHub records author + timestamp on the comment (auditable). |
| T-140-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |

ASVS L1: attack surface is a single authenticated issue comment. No high-severity threats. If `gh` is unauthenticated, the executor raises a blocking auth checkpoint rather than proceeding.
</threat_model>

<verification>
- ROADMAP success criterion #3 satisfied: #93 reviewed, in-scope subset (none) assigned to
  Phase 141, remainder re-deferred, #93 updated with the triage comment.
- No deferred idea is pulled into scope (all 7 stay deferred per D-14).
</verification>

<success_criteria>
- A triage comment exists on issue #93 recording all 7 #78 items re-deferred and in-scope
  subset for Phase 141 = none (CARRY-02, ROADMAP criterion #3).
- Issue #93 remains OPEN.
</success_criteria>

<output>
Create `.planning/phases/140-ui-spec-gate/140-02-SUMMARY.md` when done (include the comment URL).
</output>
