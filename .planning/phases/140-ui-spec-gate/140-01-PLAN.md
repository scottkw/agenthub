---
phase: 140-ui-spec-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/140-ui-spec-gate/140-UI-SPEC.md
autonomous: true
requirements: [RDS-01]
must_haves:
  truths:
    - "A UI-spec artifact records Direction 01 'Refined Native' as the chosen redesign direction"
    - "The spec names the standalone HTML as the canonical visual source of truth for Phase 141"
    - "The spec resolves every comp-vs-shipped conflict (D-08..D-13) in favor of the shipped Hub-first structure"
    - "The spec preserves the canonical blue accent tokens and explicitly rejects the standalone's periwinkle"
    - "The spec flags that the Hub has no comp and that the restyle is recolor-only"
  artifacts:
    - path: ".planning/phases/140-ui-spec-gate/140-UI-SPEC.md"
      provides: "The chosen redesign direction + conflict reconciliation + accent lock + restyle-depth instruction for Phase 141"
      contains: "Direction 01 — Refined Native"
      min_lines: 80
  key_links:
    - from: ".planning/phases/140-ui-spec-gate/140-UI-SPEC.md"
      to: "frontend/src/style.css"
      via: "accent token reference"
      pattern: "--hub-accent"
    - from: ".planning/phases/140-ui-spec-gate/140-UI-SPEC.md"
      to: "agenthub-v4.0-redesign/AgentHub Redesign (standalone).html"
      via: "canonical visual source reference"
      pattern: "standalone"
---

<objective>
Produce the Phase 140 UI-spec artifact: a markdown document that formally records the
chosen v4.0 redesign direction (Direction 01 "Refined Native"), names the canonical
visual source, reconciles the comp against the already-shipped Hub-first structure
(structural decisions win every conflict), locks the blue accent at the hex level, and
instructs Phase 141 that the restyle is recolor-only.

Purpose: Phase 141 (Redesign Implementation) needs an unambiguous, decision-complete
spec so it can restyle surfaces without re-litigating direction, accent, or scope. This
artifact is the contract Phase 141 builds against.

Output: `.planning/phases/140-ui-spec-gate/140-UI-SPEC.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/140-ui-spec-gate/140-CONTEXT.md
@.planning/phases/138-hub-first-navigation/138-CONTEXT.md
@.planning/phases/137-share-modal-cap-model/137-CONTEXT.md
@.planning/phases/139-card-rendering-tab-strip/139-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the chosen-direction + accent-lock sections of the UI spec</name>
  <read_first>
    - .planning/phases/140-ui-spec-gate/140-CONTEXT.md (locked decisions D-01..D-15 — source of truth)
    - .planning/ROADMAP.md (Phase 140 goal + 3 success criteria; Phase 141 goal)
    - .planning/REQUIREMENTS.md (RDS-01..04, CARRY-01/02 wording)
    - frontend/src/style.css (the canonical --hub-accent tokens: #7aa2f7 dark / #3d6fe8 light, lines ~3906, ~3957)
    - agenthub-v4.0-redesign/AgentHub UI redesign/AgentHub Redesign Directions.dc.html (the three-directions chooser — what was NOT chosen)
  </read_first>
  <action>
    Create `.planning/phases/140-ui-spec-gate/140-UI-SPEC.md` with these exact top-level
    section headings, in order:
    `# AgentHub v4.0 UI Spec (Phase 140 decision gate)`,
    `## Chosen Direction`,
    `## Canonical Visual Source`,
    `## Review Provenance`,
    `## Rejected Directions`,
    `## Locked Accent & Visual Feel`.

    Under "Chosen Direction": state verbatim that the direction is "Direction 01 — Refined
    Native" (sidebar + tabs, comfortable density, flat/native look, tmux-style footer status
    bar) per D-01. Under "Canonical Visual Source": name the file
    `agenthub-v4.0-redesign/AgentHub Redesign (standalone).html` as the visual source of
    truth for Phase 141 per D-02, and note it is a JS-bundled SPA that renders only in a
    browser. Under "Review Provenance": record D-03 (authored/exported in Claude Design;
    reviewed via the c-*.png screenshots; no extra live-browser pass required to lock; live
    walkthrough available in Phase 141 if needed) — this satisfies ROADMAP success
    criterion #1. Under "Rejected Directions": record D-04 (no cross-direction mix;
    Directions 02 Command Workspace and 03 Mission Control not adopted in whole or part).

    Under "Locked Accent & Visual Feel": encode D-05 and D-06. State that the blue accent is
    kept and name the canonical tokens `--hub-accent: #7aa2f7` (dark) and `#3d6fe8` (light,
    WCAG AA verified on #ffffff) as preserved in `frontend/src/style.css`. State explicitly
    that the standalone's CSS accent `#7C8CFF` (periwinkle violet) is REJECTED/incidental
    and Phase 141 must NOT change the accent. Include a colorblind verification instruction:
    accent is verified at the hex level in source, never by eye. Record D-06 visual feel
    (comfortable density, flat/native — not glassy/rounded, tmux-style footer status bar,
    both light + dark themes) and the D-07 carry-forward constraint (Phase 141 honors
    prefers-reduced-motion and colorblind-safe semantics; not re-litigated here).
  </action>
  <acceptance_criteria>
    - `.planning/phases/140-ui-spec-gate/140-UI-SPEC.md` exists.
    - File contains the literal string "Direction 01 — Refined Native".
    - File contains literal strings "#7aa2f7", "#3d6fe8", and "#7C8CFF".
    - The line containing "#7C8CFF" also contains "REJECTED" or "rejected".
    - File contains "--hub-accent" and references "frontend/src/style.css".
    - File contains the string "colorblind" and an instruction to verify accent at the hex level (string "hex").
    - File contains a "Rejected Directions" section naming both "Command Workspace" and "Mission Control".
    - `grep -c "^## " 140-UI-SPEC.md` returns >= 5 (the headings above, excluding the H1).
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/140-ui-spec-gate/140-UI-SPEC.md" && grep -q "Direction 01 — Refined Native" ".planning/phases/140-ui-spec-gate/140-UI-SPEC.md" && grep -q "#7C8CFF" ".planning/phases/140-ui-spec-gate/140-UI-SPEC.md" && grep -i "#7C8CFF" ".planning/phases/140-ui-spec-gate/140-UI-SPEC.md" | grep -qi "reject" && grep -q "#7aa2f7" ".planning/phases/140-ui-spec-gate/140-UI-SPEC.md" && grep -q "#3d6fe8" ".planning/phases/140-ui-spec-gate/140-UI-SPEC.md" && grep -qi "colorblind" ".planning/phases/140-ui-spec-gate/140-UI-SPEC.md"</automated>
  </verify>
  <done>The spec records Direction 01 as chosen, names the canonical standalone source, records review provenance, rejects Directions 02/03, and locks the blue accent at hex level with the periwinkle explicitly rejected and a colorblind verification instruction.</done>
</task>

<task type="auto">
  <name>Task 2: Author the conflict-reconciliation + restyle-depth sections (RDS-03 feed)</name>
  <read_first>
    - .planning/phases/140-ui-spec-gate/140-CONTEXT.md (D-08..D-13 conflict resolutions)
    - .planning/phases/140-ui-spec-gate/140-UI-SPEC.md (the file from Task 1 — append, do not overwrite)
    - .planning/phases/138-hub-first-navigation/138-CONTEXT.md (NAV-02..05, CARD decisions — the winning structure)
    - .planning/phases/137-share-modal-cap-model/137-CONTEXT.md (SHARE cap model that supersedes the comp's Sessions-page share controls)
    - .planning/phases/139-card-rendering-tab-strip/139-CONTEXT.md (shipped card mini-preview / tab-strip)
    - agenthub-v4.0-redesign/AgentHub UI redesign/c-sessions.png (the comp Sessions page being dropped)
    - agenthub-v4.0-redesign/AgentHub UI redesign/c-remote.png (the comp Remote page being dropped)
  </read_first>
  <action>
    Append to `.planning/phases/140-ui-spec-gate/140-UI-SPEC.md` these exact section
    headings, in order:
    `## Conflict Reconciliation (structural decisions win — RDS-03)`,
    `## Hub Has No Comp`,
    `## Restyle Depth`,
    `## Surface Map`,
    `## Phase 141 Hand-off`.

    Under "Conflict Reconciliation": document each of D-08 through D-11 as a resolved
    conflict. For each, state the comp's element and the winning shipped decision:
    D-08 sidebar — comp shows Home/Remote/Sessions/+ New Session/Settings; winning structure
    is Home/Hub/Settings (Phase 138 NAV-02..05); drop Remote, Sessions, and +New Session
    sidebar entries; session creation stays on the Hub HubFilterBar "New Session" button
    (CARD-01). D-09 Sessions page — NOT reintroduced as a page; its controls live on Hub
    cards + per-card overflow (Kill, Browse files — Phase 138) and the share modal (Phase
    137 cap model). D-10 Remote page — NOT reintroduced; remote sessions live in the unified
    local+remote Hub grid with colorblind-safe card provenance (CARD-02). D-11 share copy —
    the comp footer "Share links are on the Sessions tab" must be reworded/removed; there is
    no Sessions tab; sharing is via the Hub / share modal. Format each as an explicit
    Comp → Resolution pair so all six of D-08..D-13 are unambiguously traceable.

    Under "Hub Has No Comp": document D-12 — the standalone contains no Hub surface; Phase
    141 derives the Hub restyle by applying Refined Native's visual language (typography,
    spacing, blue accent, card treatment) to the existing shipped Hub; there is no pixel comp
    to copy; flag this explicitly. Under "Restyle Depth": document D-13 — recolor only;
    Phase 141 applies Refined Native color/typography/spacing to the existing share dialog
    and Hub cards but does NOT change their layout or interaction model; the comp's
    Sessions-page share-control layout is NOT harvested; shipped Phase 137 share model and
    shipped card interactions are preserved.

    Under "Surface Map": list the surviving surfaces Phase 141 restyles (Welcome→Home, Hub,
    terminal/session, File Browser, Editor, Settings) and note that the comp's "Welcome"
    maps to the shipped "Home" sidebar item. Under "Phase 141 Hand-off": a short checklist
    that ties this spec to RDS-02/03/04 + CARRY-01 (Phase 141 scope) so the downstream plan
    has a single entry point.
  </action>
  <acceptance_criteria>
    - File documents all six decision IDs D-08, D-09, D-10, D-11, D-12, D-13 (each ID string present).
    - "Conflict Reconciliation" section names the winning sidebar structure "Home" / "Hub" / "Settings".
    - File states the Sessions page and Remote page are NOT reintroduced (string "NOT reintroduced" or "not reintroduced" present at least twice).
    - "Hub Has No Comp" section present and states there is no comp / no pixel comp for the Hub.
    - "Restyle Depth" section present and contains "recolor only" (case-insensitive) and states layout/interaction are preserved.
    - "Surface Map" section lists Welcome, Hub, File Browser, Editor, Settings, and the word "terminal" or "session".
    - File references Phase 137 (share model) and Phase 138 (nav structure).
  </acceptance_criteria>
  <verify>
    <automated>f=".planning/phases/140-ui-spec-gate/140-UI-SPEC.md"; for id in D-08 D-09 D-10 D-11 D-12 D-13; do grep -q "$id" "$f" || { echo "missing $id"; exit 1; }; done && grep -qi "recolor only" "$f" && grep -q "Conflict Reconciliation" "$f" && grep -q "Restyle Depth" "$f" && grep -q "Surface Map" "$f" && [ "$(grep -ci 'not reintroduced' "$f")" -ge 2 ]</automated>
  </verify>
  <done>The spec resolves all six conflict/depth decisions (D-08..D-13) in favor of the shipped Hub-first structure, flags the Hub-has-no-comp situation, locks the restyle to recolor-only, maps the surviving surfaces, and hands off cleanly to Phase 141.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan only writes a local markdown document. No untrusted input, no network, no code execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-01 | Tampering | 140-UI-SPEC.md content | accept | Local doc authored by the agent from locked CONTEXT decisions; committed to git (auditable). Negligible attack surface. |
| T-140-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. No dependency supply-chain surface. |

ASVS L1: attack surface negligible (writes markdown only). No high-severity threats. No blocking checkpoints required.
</threat_model>

<verification>
- `140-UI-SPEC.md` exists and satisfies both task automated checks.
- The spec satisfies ROADMAP success criteria #1 (direction recorded after review) and #2
  (conflicts reconciled, structural decisions win).
- All locked decisions D-01..D-13 are encoded; no deferred idea (the 7 #93 items,
  Directions 02/03 distinctive elements) is adopted.
</verification>

<success_criteria>
- The chosen redesign direction (Direction 01 — Refined Native) is recorded in the UI-spec
  artifact with review provenance (RDS-01, ROADMAP criterion #1).
- Every comp-vs-shipped conflict (D-08..D-13) is called out and resolved in favor of the
  shipped Hub-first structure (RDS-03 feed, ROADMAP criterion #2).
- The blue accent is locked at the hex level and the standalone periwinkle is rejected;
  colorblind verification instruction present (D-05).
</success_criteria>

<output>
Create `.planning/phases/140-ui-spec-gate/140-01-SUMMARY.md` when done.
</output>
