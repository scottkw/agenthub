---
phase: 153-session-pty-bridge
plan: 03
type: execute
wave: 3
depends_on: ["153-02"]
files_modified:
  - internal/webserver/server.go
  - internal/webserver/inject_test.go
  - TESTING.md
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "The web-share read pump routes MsgSessionInject through the same hub.HandleInject gate as the relay path (cross-surface parity)."
    - "A RO-JWT client on the web-share path that sends a hand-crafted MsgSessionInject receives a MsgInjectError NAK and causes zero PTY writes (SEC-01, web path, enforced server-side not by UI suppression)."
    - "TESTING.md records the three new Go test files and maps them to MENTION-02/03 and SEC-01/SEC-02 (repo standing convention)."
  artifacts:
    - "case relay.MsgSessionInject in internal/webserver/server.go handleWSSRelay read pump"
    - internal/webserver/inject_test.go
    - "TESTING.md Suite Manifest §2 (Go count 359 → 362) + Traceability §4 rows"
  key_links:
    - "Web-path sub.ReadOnly derives from claims.Perms == \"read\" on the signed JWT — the gate cannot be bypassed by URL params (D-24/SEC-04)."
    - "Both WS entry paths share one hub.HandleInject implementation — the relay and web read pumps are the only two call sites."
---

<objective>
Bring the web-share WS path to parity with the relay path for `@session` injection and prove the
SEC-01 server-side gate holds on the web entry path against a hand-crafted RO-JWT frame. Then
register all three new Go test files in TESTING.md per the repo standing convention.

Purpose: SEC-01 must hold on BOTH WS entry paths (Pitfall 5: gating the relay path but forgetting
the web path is a silent SEC-01 hole). The web path derives RO status from a signed JWT
(`claims.Perms == "read"`), a different mechanism than the relay path's query param — so it needs
its own adversarial test. Cross-surface parity is release-blocking for this project.
Output: New read-pump case in `internal/webserver/server.go`; new
`internal/webserver/inject_test.go`; updated `TESTING.md`.

UI scope note: still no composer/affordance — this is the web-share daemon path (D-01). The
adversarial test uses a hand-crafted WS frame, not a UI action.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-session-pty-bridge/153-CONTEXT.md
@.planning/phases/153-session-pty-bridge/153-RESEARCH.md
@.planning/phases/153-session-pty-bridge/153-PATTERNS.md
@.planning/phases/153-session-pty-bridge/153-02-PLAN.md
@internal/webserver/server.go
@TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add web-share read-pump inject case (claims.Perms gate)</name>
  <files>internal/webserver/server.go</files>
  <read_first>
    - internal/webserver/server.go lines 1007-1008 (PATTERNS.md §"internal/webserver/server.go") —
      `readonly := claims.Perms == "read"` is the web-path RO derivation (sourced from the signed
      JWT, not a URL param).
    - internal/webserver/server.go lines 1116-1122 (MsgInput) and 1139-1155 (MsgAliasSet) — the
      read-pump cases to mirror, with the `relay.` package prefix on all protocol symbols.
    - 153-PATTERNS.md §"internal/webserver/server.go" — the exact case body with relay. prefixes.
  </read_first>
  <action>
    In internal/webserver/server.go handleWSSRelay's read-pump switch, add
    `case relay.MsgSessionInject:` after the MsgAliasSet case. Unmarshal payload into a
    relay.InjectPayload; on JSON error OR empty Text, continue (silent drop). Otherwise call
    hub.HandleInject(sub, ip.Text); on error, send relay.MakeInjectErrorFrame(err.Error()) to
    sub.Msgs via the non-blocking select/default → go sub.CloseSlow() pattern. The case is
    structurally identical to the relay-path case but with relay. prefixes on every protocol
    symbol. Add a comment noting MsgChatSend (0x31) is chat-only and NEVER writes to PTY (D-02), and
    that sub.ReadOnly here is sourced from the signed JWT (claims.Perms == "read", line ~1008) and
    cannot be bypassed by URL params (D-04). Do NOT add a MsgChatSend dispatch case (Phase 154).
    encoding/json and the relay import are already present in this file.
  </action>
  <acceptance_criteria>
    - `go build ./...` succeeds.
    - `grep -c 'case relay.MsgSessionInject' internal/webserver/server.go` returns 1.
    - The web inject case routes through hub.HandleInject (no direct WriteInput in the case) — the
      same shared gate + sanitizer as the relay path.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./...</automated>
  </verify>
  <done>handleWSSRelay dispatches relay.MsgSessionInject to hub.HandleInject with a NAK on rejection, mirroring the relay path with claims-derived RO status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Web-path adversarial RO-JWT inject rejection test (SEC-01)</name>
  <files>internal/webserver/inject_test.go</files>
  <read_first>
    - internal/relay/server_identity_test.go (PATTERNS.md §"internal/webserver/inject_test.go") —
      test-server setup + adversarial hand-crafted WS frame style.
    - internal/webserver/server.go line 1008 — the claims.Perms == "read" gate the test must
      exercise; and existing webserver test files for how they mint a capability.Claims{Perms:"read"}
      JWT and present it as the ?cap= query param (the requireCapability middleware validates it
      before handleWSSRelay runs).
    - 153-RESEARCH.md "Pitfall 5" + "Per-Task Verification Map" row SEC-01 (web) — the exact
      automated command and the counting-PTY-writer assertion.
  </read_first>
  <behavior>
    - TestInjectRO_WebPath (SEC-01 web): mint a RO capability JWT (Perms:"read"), dial
      handleWSSRelay with it, send a hand-crafted relay.MsgSessionInject frame (byte prefix + JSON
      relay.InjectPayload) directly; the client receives a relay.MsgInjectError NAK frame within a
      short timeout AND the counting PTY writer records exactly zero writes. This proves the gate
      holds server-side on the web path regardless of any client-side suppression.
  </behavior>
  <action>
    Create internal/webserver/inject_test.go in package webserver. Stand up the webserver test
    server with a counting PTY writer (atomic.Int32 per Write) backing the hub. Mint a RO JWT via
    the capability package the same way existing webserver tests do, present it as ?cap=, dial the
    WS, and send a hand-crafted MsgSessionInject binary frame. Assert: (1) a frame whose first byte
    is relay.MsgInjectError is received within a timeout, and (2) the PTY write counter is exactly
    0. Reuse the relay package's MsgSessionInject / InjectPayload / MakeInjectErrorFrame symbols
    (imported via the relay package). If a writerFunc adapter is needed, define it locally.
  </action>
  <acceptance_criteria>
    - `go test -race -short -run TestInjectRO_WebPath ./internal/webserver/...` passes.
    - The test asserts BOTH a relay.MsgInjectError NAK is received AND the PTY write counter reads
      exactly 0 (zero PTY writes for the RO-JWT client).
    - The inject frame is hand-crafted and sent directly over the WS (not via any UI affordance),
      proving server-side enforcement per SEC-01 / D-04.
    - The RO status under test originates from claims.Perms == "read" on the signed JWT (web-path
      derivation), distinct from the relay path's query param (Pitfall 5).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -race -short -run TestInjectRO_WebPath ./internal/webserver/...</automated>
  </verify>
  <done>A hand-crafted MsgSessionInject from a RO-JWT web client is rejected with a NAK and causes zero PTY writes — SEC-01 proven on the web entry path.</done>
</task>

<task type="auto">
  <name>Task 3: Register new test files in TESTING.md and run the traceability check</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md §2 (Suite Manifest) — current Go test count (359) and the suite-group format for
      adding test files; §4 (Traceability) — the row format (Requirement | Path | … ) where the
      Path column is a repo-relative .go file path only; §5/§6 for the standing convention.
    - ./CLAUDE.md (root) Regression Test Convention — the four-step standing rule.
    - 153-RESEARCH.md "Validation Architecture" → "Wave 0 Gaps"/"TESTING.md must be updated" — Go
      count 359 → 362 (+3 files) and the four requirements to map.
  </read_first>
  <action>
    Update TESTING.md per the repo standing convention. (1) Suite Manifest §2: add the three new Go
    test files (internal/relay/sanitize_test.go, internal/relay/server_inject_test.go,
    internal/webserver/inject_test.go) to the appropriate suite group(s) and bump the Go count from
    359 to 362. (2) Traceability §4: add a row per new test file mapping to its requirement(s) —
    sanitize_test.go → SEC-02; server_inject_test.go → MENTION-02, MENTION-03, SEC-01 (relay);
    inject_test.go → SEC-01 (web). The Path column must be the repo-relative .go file path only (no
    test names). (3) If build.yml matrix entries are unchanged, no branch-protection update is
    needed (note this in the SUMMARY). Then run the traceability path checker.
  </action>
  <acceptance_criteria>
    - `bash tests/check-traceability-paths.sh` exits 0.
    - `grep -c 'sanitize_test.go\|server_inject_test.go\|inject_test.go' TESTING.md` returns 3 or
      more (all three new files referenced).
    - The Go test count in §2 reads 362 (was 359).
    - Each new test file has a Traceability §4 row mapping it to MENTION-02/MENTION-03/SEC-01/SEC-02
      as appropriate, with a repo-relative .go path in the Path column.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && bash tests/check-traceability-paths.sh</automated>
  </verify>
  <done>TESTING.md lists the three new Go test files in the Suite Manifest (count 362) and the Traceability map (per-requirement rows); the traceability path check passes.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03 portion)
- Read-pump case `relay.MsgSessionInject` — `internal/webserver/server.go`
- Test file `internal/webserver/inject_test.go`
- TESTING.md updates: Suite Manifest (+3 Go files, count 362) + Traceability rows (MENTION-02/03, SEC-01, SEC-02)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web-share WS client → handleWSSRelay read pump | RO status derives from a signed capability JWT (`claims.Perms == "read"`); a RO-JWT holder is untrusted for PTY writes and the gate must hold against a hand-crafted frame regardless of client-side suppression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-10 | Elevation of Privilege | RO-JWT web client sends hand-crafted MsgSessionInject to write PTY | high | mitigate | sub.ReadOnly from claims.Perms == "read"; HandleInject returns ErrReadOnly → MsgInjectError NAK; zero PTY writes proven by TestInjectRO_WebPath (adversarial frame). |
| T-153-11 | Elevation of Privilege | Web-path gate forgotten while relay path is gated (silent SEC-01 hole) | high | mitigate | Both read pumps route to the single hub.HandleInject; dedicated web-path adversarial test (Pitfall 5) proves parity. |
| T-153-12 | Spoofing | RO status falsified via URL param to bypass the gate | medium | mitigate | Web path ignores client-asserted URL params for RO status; sub.ReadOnly is derived from the verified signed JWT (D-24/SEC-04). |
| T-153-SC | Tampering | Supply chain (new dependencies) | low | accept | No new Go modules — stdlib + existing pinned deps. |
</threat_model>

<verification>
- `go build ./...` succeeds with the web-path inject case.
- `go test -race -short -run TestInjectRO_WebPath ./internal/webserver/...` passes (SEC-01 web path).
- `go test -race -short ./...` passes (full suite green before verify).
- `bash tests/check-traceability-paths.sh` exits 0.
- `go vet ./internal/webserver/...` and `gofmt -l internal/webserver/server.go internal/webserver/inject_test.go` report no issues.
</verification>

<success_criteria>
- A hand-crafted MsgSessionInject from a RO-JWT web client is rejected server-side with a NAK and zero PTY writes (SEC-01, web path).
- The web and relay paths share one gated inject implementation (cross-surface parity).
- TESTING.md correctly registers the three new Go test files with per-requirement traceability and the path check passes.
</success_criteria>

<output>
Create `.planning/phases/153-session-pty-bridge/153-03-SUMMARY.md` when done.
</output>
