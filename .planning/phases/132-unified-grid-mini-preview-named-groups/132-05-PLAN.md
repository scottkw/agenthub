---
phase: 132-unified-grid-mini-preview-named-groups
plan: 05
type: execute
wave: 4
depends_on: ["132-03", "132-04"]
files_modified:
  - frontend/src/components/Hub/HubPanel.tsx
  - frontend/src/components/Hub/HubPanel.test.tsx
  - frontend/src/App.tsx
  - frontend/src/style.css
autonomous: true
requirements: [CARD-07, GRID-03, GRID-07]
must_haves:
  truths:
    - "HubPanel runs ONE shared usePreviewPoller (3s interval) that batches GetSessionTailLines for local sessions only, pauses when Hub inactive, and excludes remote (hostname!='') sessions"
    - "HubPanel merges local sessions with the remoteSessions prop into one unified grid (GRID-07)"
    - "HubPanel renders a two-column hub__body: GroupSidebar (collapsed/expanded, localStorage) + the existing hub__grid-scroll"
    - "HubPanel owns named-group state (loadGroups on mount) and wires create/assign/remove through hubGroups CRUD with localStorage persistence"
    - "App.tsx polls remote sessions when EITHER the Remote OR the Hub tab is active and passes adaptAllRemoteSessions(remotePeers) + isActive into HubPanel"
    - "All new CSS tokens + rules are added; every new transition is inside @media (prefers-reduced-motion: no-preference)"
  artifacts:
    - path: "frontend/src/components/Hub/HubPanel.tsx"
      provides: "usePreviewPoller + GroupSidebar layout + group state + remote merge"
      contains: "usePreviewPoller"
    - path: "frontend/src/App.tsx"
      provides: "remote poll gate extension + HubPanel prop wiring"
      contains: "adaptAllRemoteSessions"
    - path: "frontend/src/style.css"
      provides: "Phase 132 --hub-* tokens + hub__body/sidebar/preview/menu rules"
      contains: "--hub-preview-bg"
  key_links:
    - from: "frontend/src/components/Hub/HubPanel.tsx"
      to: "frontend/src/wailsjs/go/main/App"
      via: "GetSessionTailLines in usePreviewPoller"
      pattern: "GetSessionTailLines"
    - from: "frontend/src/components/Hub/HubPanel.tsx"
      to: "frontend/src/components/Hub/GroupSidebar.tsx"
      via: "<GroupSidebar ...>"
      pattern: "GroupSidebar"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/lib/remoteAdapter"
      via: "adaptAllRemoteSessions(remotePeers)"
      pattern: "adaptAllRemoteSessions"
---

<objective>
Integrate everything into the live Hub surface: add the single shared `usePreviewPoller` (CARD-07 perf mandate), the two-column `hub__body` layout hosting `GroupSidebar` with named-group state + localStorage (GRID-03), the local+remote unified merge (GRID-07), the App.tsx remote-poll-gate extension + HubPanel prop wiring, and all Phase 132 CSS tokens/rules with `prefers-reduced-motion` discipline.

Purpose: This is the final wave — it connects the Wave-2/3 components (GroupSidebar, extended SessionCard/SessionCardGrid) and the Wave-1 libs (hubGroups, remoteAdapter) and the Wave-0 RPC (GetSessionTailLines) into a working Hub.
Output: Modified HubPanel + App.tsx + style.css + updated HubPanel tests, all green; full Hub vitest suite + go build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-unified-grid-mini-preview-named-groups/132-UI-SPEC.md
@.planning/phases/132-unified-grid-mini-preview-named-groups/132-PATTERNS.md

<interfaces>
<!-- HubPanel mods + full usePreviewPoller body in 132-PATTERNS.md §HubPanel.tsx (lines 701-789). App.tsx mods in §App.tsx (lines 793-834). CSS in §style.css (lines 838-980) + UI-SPEC §1/§2/§3. -->

usePreviewPoller (full body 132-PATTERNS.md lines 716-753): one 3s interval; STABLE dep is `sessions.map(s=>s.id).join(',')` (Pitfall 3 — do NOT depend on the sessions array directly); fetches only local sessions (filter `!s.hostname || s.hostname===''`, Pitfall 4); Promise.all over GetSessionTailLines(id,4) with .catch(()=>[]); returns Map<id,string[]>; early-return when !isActive || no sessions; cleanup clears interval. Import GetSessionTailLines from '../../wailsjs/go/main/App'.

HubPanel new props (132-PATTERNS.md lines 758-768): remoteSessions?: SessionInfo[]; isActive?: boolean.
HubPanel internal state to add: sidebarCollapsed (localStorage-backed, mirror Sidebar.tsx key e.g. 'hub-group-sidebar-collapsed'); activeGroupId: string|null; groupDefs (init via loadGroups()).
Merge: allSessions = [...sessions, ...(remoteSessions ?? [])]. Apply existing filterSessions to allSessions. Apply group filter: when activeGroupId is set, narrow to sessions whose memberKey ∈ that def's memberKeys (or Other when activeGroupId==='__other__'). Pass filtered+grouped set to SessionCardGrid with groupDefs, previewTails (from poller), onAssignGroup.
Group CRUD wiring: onCreateGroup(name) → setGroupDefs(createGroup(prev,name)); onDropOnGroup/onAssignGroup(memberKey, groupId) → if groupId==='__other__' use removeFromGroup, else assignToGroup. All via hubGroups.ts (which persists to localStorage internally).
Layout: wrap GroupSidebar + the existing .hub__grid-scroll in a new <div className="hub__body"> (UI-SPEC §2 layout). GroupSidebar gets allSessions for counts.

App.tsx changes (132-PATTERNS.md lines 793-834):
  - remote poll gate (currently line ~935 `if (activeId !== REMOTE_SESSIONS_TAB.id) return`) → `if (activeId !== REMOTE_SESSIONS_TAB.id && activeId !== HUB_TAB.id) return`; keep 30s cadence; add HUB_TAB.id to the useEffect deps if needed.
  - HubPanel render (~line 1355): add `remoteSessions={adaptAllRemoteSessions(remotePeers)}` and `isActive={activeId === HUB_TAB.id}`; import adaptAllRemoteSessions from './lib/remoteAdapter'. Preserve existing sessions/error/onNewSession/onRename/onOpenSession props.

CSS (132-PATTERNS.md §style.css lines 838-980 + UI-SPEC §1/§2/§3/§Motion Contract):
  - APPEND new --hub-* tokens to the EXISTING :root and [data-ui-theme="light"] blocks (do NOT create new :root blocks; do NOT duplicate Phase 131 tokens). Token tables: UI-SPEC §Design System.
  - Add .hub__body (flex row), .hub__group-sidebar (+ --collapsed), group item rules, .hub-card__drag-handle / .hub-card__menu-btn (absolute, opacity 0 → 1 on hover/focus-within), .hub-card__preview / -line, .hub-card__menu / -item / -divider, needs-input badge.
  - EVERY new transition (sidebar width 150ms, drag-handle/menu-btn opacity 100ms) MUST be inside @media (prefers-reduced-motion: no-preference) (Pitfall 7).

Source-level comments REQUIRED in style.css:
  /* CARD-07: mini preview is plain text snapshot — NO xterm instance; polling interval 3s shared across all cards */
  /* COLORBLIND-SAFE: needs-input badge dark hex #f59e0b ... */ /* ... light #b45309 ... */
  /* COLORBLIND-SAFE: drag-over border dark hex #7aa2f7 ... */ /* ... light #3d6fe8 ... */
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HubPanel — usePreviewPoller + GroupSidebar layout + group state + remote merge</name>
  <files>frontend/src/components/Hub/HubPanel.tsx, frontend/src/components/Hub/HubPanel.test.tsx</files>
  <read_first>
    frontend/src/components/Hub/HubPanel.tsx (FULL existing file, lines 1-158 — preserve filter/search/'/' shortcut/error/empty states),
    frontend/src/components/Hub/HubPanel.test.tsx (existing assertions to keep green),
    132-PATTERNS.md §HubPanel.tsx (lines 701-789, full usePreviewPoller body) + §Critical Implementation Notes (stable dep lines 1104-1109),
    frontend/src/components/Hub/GroupSidebar.tsx (Plan 03), frontend/src/components/Hub/SessionCardGrid.tsx (Plan 04),
    frontend/src/lib/hubGroups.ts (loadGroups/createGroup/assignToGroup/removeFromGroup/memberKey),
    frontend/src/components/Sidebar.tsx (localStorage collapsed pattern lines 12-43)
  </read_first>
  <behavior>
    - usePreviewPoller(sessions, isActive=true): mocking GetSessionTailLines, one tick fetches only local sessions and returns a Map keyed by id; remote (hostname set) sessions are excluded from the fetch
    - usePreviewPoller(sessions, isActive=false): does NOT start the interval / does not call GetSessionTailLines
    - HubPanel merges remoteSessions into the grid: a remote SessionInfo (hostname set) appears in the rendered grid
    - HubPanel renders a .hub__body wrapper containing the GroupSidebar and .hub__grid-scroll
    - creating a group via the sidebar callback adds it to state and persists (loadGroups() after returns it)
    - selecting a group narrows the grid to that group's sessions; selecting All clears the group filter
    - existing Phase 131 HubPanel behaviors (status filter, search, '/' shortcut, error state, empty state) remain green
  </behavior>
  <action>
    Modify frontend/src/components/Hub/HubPanel.tsx per 132-PATTERNS.md §HubPanel.tsx. Add the `usePreviewPoller` hook (verbatim body, with the stable `sessionIdKey` dep per Critical Implementation Notes — NOT the sessions array). Import GetSessionTailLines from '../../wailsjs/go/main/App'; GroupSidebar from './GroupSidebar'; loadGroups/createGroup/assignToGroup/removeFromGroup/memberKey + HubGroupDef from '../../lib/hubGroups'. Add `remoteSessions?` and `isActive?` to HubPanelProps. Add state: `groupDefs` (init `loadGroups()`), `activeGroupId` (null), `sidebarCollapsed` (localStorage init via key 'hub-group-sidebar-collapsed', mirror Sidebar.tsx). Compute `allSessions = [...sessions, ...(remoteSessions ?? [])]`; pass allSessions through the existing filterSessions; then narrow by activeGroupId (when set, keep sessions whose memberKey is in the active def's memberKeys, or the Other bucket when activeGroupId==='__other__'). Call `const previewTails = usePreviewPoller(allSessions, isActive ?? false)`. Wrap GroupSidebar + .hub__grid-scroll in `<div className="hub__body">`. Pass to GroupSidebar: groupDefs, sessions=allSessions, activeGroupId, collapsed=sidebarCollapsed, onToggle (toggle + persist), onGroupSelect=setActiveGroupId, onCreateGroup (setGroupDefs(createGroup(prev,name))), onDropOnGroup ((groupId,key)=> setGroupDefs(prev => groupId==='__other__' ? removeFromGroup(prev,key) : assignToGroup(prev,groupId,key))). Pass to SessionCardGrid: groupDefs, previewTails, onAssignGroup (same mapping as onDropOnGroup). Keep header/filter-bar/error/empty branches intact. Add the empty-groupDefs case so the grid still renders (SessionCardGrid falls back to workDir grouping — Plan 04). Account for Pitfall 5: when groupDefs is empty, sidebar/grid behave as Phase 131.

    Update frontend/src/components/Hub/HubPanel.test.tsx: KEEP existing assertions; mock '../../wailsjs/go/main/App' GetSessionTailLines (vi.mock); ADD cases for poller active vs inactive, remote merge into grid, hub__body presence, create-group persistence, and group-filter narrowing. filterSessions export is already covered — keep those tests.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/components/Hub/HubPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm vitest run src/components/Hub/HubPanel.test.tsx` passes ALL cases (existing Phase 131 + new)
    - `grep -c "usePreviewPoller" frontend/src/components/Hub/HubPanel.tsx` returns at least 2 (def + call)
    - `grep -c "setInterval" frontend/src/components/Hub/HubPanel.tsx` returns exactly 1 (single shared timer — per-card intervals forbidden)
    - `grep -c "sessionIdKey\|\.map((s) => s.id).join" frontend/src/components/Hub/HubPanel.tsx` returns at least 1 (stable dep, Pitfall 3)
    - `grep -c "hub__body" frontend/src/components/Hub/HubPanel.tsx` returns at least 1
    - `grep -c "GroupSidebar" frontend/src/components/Hub/HubPanel.tsx` returns at least 1
  </acceptance_criteria>
  <done>HubPanel runs one shared 3s poller (local-only, paused when inactive), merges remote sessions, hosts the GroupSidebar in hub__body, and manages persisted named-group state; all Phase 131 behavior preserved; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: App.tsx remote-poll-gate extension + HubPanel prop wiring</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    frontend/src/App.tsx (remote poll useEffect ~lines 933-960, Hub poll ~lines 907-923, HubPanel render ~lines 1355-1363; HUB_TAB / REMOTE_SESSIONS_TAB definitions; remotePeers state ~line 190),
    132-PATTERNS.md §App.tsx (lines 793-834),
    frontend/src/lib/remoteAdapter.ts (adaptAllRemoteSessions, Plan 02)
  </read_first>
  <action>
    Modify frontend/src/App.tsx per 132-PATTERNS.md §App.tsx. (1) Extend the remote-session poll gate: change the early-return condition from `if (activeId !== REMOTE_SESSIONS_TAB.id) return` to `if (activeId !== REMOTE_SESSIONS_TAB.id && activeId !== HUB_TAB.id) return`; keep the existing 30s interval; ensure HUB_TAB.id is reflected in that useEffect's dependency/active logic so switching to Hub triggers a remote fetch (Pitfall 4). (2) Import `adaptAllRemoteSessions` from './lib/remoteAdapter'. (3) In the HubPanel render block add props `remoteSessions={adaptAllRemoteSessions(remotePeers)}` and `isActive={activeId === HUB_TAB.id}` while preserving all existing props (sessions, error, onNewSession, onRename, onOpenSession). Do not alter the local Hub ListSessions poll.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/components/Hub/HubPanel.test.tsx && grep -c "adaptAllRemoteSessions" frontend/src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adaptAllRemoteSessions" frontend/src/App.tsx` returns at least 1
    - `grep -c "activeId !== REMOTE_SESSIONS_TAB.id && activeId !== HUB_TAB.id" frontend/src/App.tsx` returns 1 (extended gate)
    - `grep -c "isActive={activeId === HUB_TAB.id}" frontend/src/App.tsx` returns 1
    - `cd frontend && pnpm vitest run` (full suite) is green (App.tsx change does not break existing tests)
  </acceptance_criteria>
  <done>App.tsx polls remote sessions when Hub OR Remote tab is active and passes adapted remote sessions + isActive into HubPanel; no regressions.</done>
</task>

<task type="auto">
  <name>Task 3: style.css — Phase 132 tokens + hub__body/sidebar/preview/menu rules (reduced-motion safe)</name>
  <files>frontend/src/style.css</files>
  <read_first>
    frontend/src/style.css (existing :root block, [data-ui-theme="light"] block, and Phase 131 .hub-* rules — locate insertion points; confirm .hub-card already has position: relative),
    132-UI-SPEC.md §Design System token tables + §1/§2/§3 + §Motion Contract + §Source-Level Verification Checklist,
    132-PATTERNS.md §style.css (lines 838-980)
  </read_first>
  <action>
    Modify frontend/src/style.css. APPEND the new dark-theme --hub-* tokens to the EXISTING :root block (do NOT open a second :root) and the light-theme values to the EXISTING [data-ui-theme="light"] block — exact hex values from UI-SPEC §Design System token tables (preview bg/text/border, sidebar bg/width/collapsed-width/item-active/hover, needs-input badge bg/text, drag-over border/bg). Include the COLORBLIND-SAFE hex source comments for needs-input badge (dark #f59e0b / light #b45309) and drag-over border (dark #7aa2f7 / light #3d6fe8). Add new BEM rules (after existing .hub-* rules): .hub__body (flex row, flex:1, min-height:0), .hub__group-sidebar (width var, bg, flex column, overflow hidden), .hub__group-sidebar--collapsed (collapsed width), group toggle/heading/list/item/-item--active/-item__name/-item__count/-item__needs-input-badge/-new, .hub-card__drag-handle + .hub-card__menu-btn (position absolute, opacity 0, top/left|right 8px), hover/focus-within opacity:1 rule, .hub-card__preview (56px, border-top, bg, padding) + .hub-card__preview-line (11px mono, nowrap, ellipsis) + --empty/--loading centered, .hub-card__menu (surface-elevated, border, radius 6px, shadow, z-index 20, min-width 160px) + -item + -divider. EVERY transition (sidebar width 150ms ease; drag-handle/menu-btn opacity 100ms ease) MUST be wrapped in `@media (prefers-reduced-motion: no-preference)` (Pitfall 7). Add the CARD-07 source comment by the preview rules.
  </action>
  <verify>
    <automated>cd frontend && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm build` completes without CSS/TS errors (or the project's existing build script succeeds)
    - `grep -c "^  --hub-preview-bg" frontend/src/style.css` shows the token appears in both :root and the light block (count >= 2)
    - `grep -c "@media (prefers-reduced-motion: no-preference)" frontend/src/style.css` increased to cover the new sidebar-width and drag-handle/menu-btn opacity transitions (at least the 2 new blocks)
    - `grep -c "transition:" frontend/src/style.css` — verify NO new `transition:` on .hub__group-sidebar or .hub-card__drag-handle/.hub-card__menu-btn appears OUTSIDE a reduced-motion media block (manual scan of the appended section)
    - `grep -c "COLORBLIND-SAFE: needs-input badge" frontend/src/style.css` returns at least 2 (dark + light)
    - `grep -c "COLORBLIND-SAFE: drag-over border" frontend/src/style.css` returns at least 2 (dark + light)
  </acceptance_criteria>
  <done>All Phase 132 tokens + BEM rules present in both themes; new transitions are reduced-motion gated; colorblind hex source comments present; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GetSessionTailLines RPC → usePreviewPoller → MiniPreview DOM | Stripped server-side (Plan 01); rendered as React text in MiniPreview (Plan 03) |
| remotePeers (remote-supplied) → adaptAllRemoteSessions → Hub grid | Remote hostnames/names rendered via React text escaping |
| localStorage group state ↔ HubPanel | User-owned local data, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-12 | DoS (client) | usePreviewPoller rebinding into a polling storm | mitigate | Single setInterval; stable `sessionIdKey` dep (Pitfall 3); local-only fetch (Pitfall 4); paused when Hub inactive |
| T-132-13 | Injection (XSS) | remote hostname/name + tail in the unified grid | mitigate | React text rendering only; no dangerouslySetInnerHTML; server-side strip upstream |
| T-132-14 | Spoofing | unreachable peer phantom sessions | mitigate | adaptAllRemoteSessions (Plan 02) filters reachable===false before merge |
| T-132-SC | Tampering | npm installs | n/a | Zero new dependencies (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `cd frontend && pnpm vitest run` (full suite) green — no Hub or App regressions
- `cd frontend && pnpm build` clean
- `go build ./...` clean (App.d.ts binding referenced by frontend)
- Manual-only (per 132-VALIDATION.md): mini-preview perf at scale, drag-and-drop card→group, remote peer card in grid — recorded for /gsd:verify-work UAT
</verification>

<success_criteria>
- CARD-07: single shared 3s poller drives all card previews; never an xterm; paused when Hub inactive
- GRID-03: collapsible group sidebar with counts + needs-input badge, persisted collapsed state, group-filter narrowing
- GRID-07: local + remote sessions in one unified grid
- All new motion respects prefers-reduced-motion; colorblind hex comments present
- Every Phase 131 Hub behavior preserved
</success_criteria>

<output>
Create `.planning/phases/132-unified-grid-mini-preview-named-groups/132-05-SUMMARY.md` when done
</output>
