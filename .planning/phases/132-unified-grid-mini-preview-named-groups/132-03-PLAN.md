---
phase: 132-unified-grid-mini-preview-named-groups
plan: 03
type: execute
wave: 2
depends_on: ["132-01", "132-02"]
files_modified:
  - frontend/src/components/Hub/MiniPreview.tsx
  - frontend/src/components/Hub/MiniPreview.test.tsx
  - frontend/src/components/Hub/GroupSidebar.tsx
  - frontend/src/components/Hub/GroupSidebar.test.tsx
autonomous: true
requirements: [CARD-07, GRID-03, GROUP-01, GROUP-02]
must_haves:
  truths:
    - "MiniPreview renders Loading… when lines undefined, No output yet when empty, and up to N plain text lines otherwise — never an xterm instance"
    - "MiniPreview pane carries aria-hidden=true (decorative)"
    - "GroupSidebar renders an All item plus one row per group with a running/total count"
    - "A group containing a waiting session shows a needs-input badge with PauseCircleIcon (icon carries state, not color)"
    - "GroupSidebar collapses to 32px (icon strip) and expands to 200px via a toggle"
    - "New group inline input: Enter (non-empty) fires onCreateGroup, Escape cancels, empty Enter is a no-op"
    - "A group sidebar item is a drop target: onDrop fires onDropOnGroup(groupId, droppedMemberKey)"
  artifacts:
    - path: "frontend/src/components/Hub/MiniPreview.tsx"
      provides: "CARD-07 plain-text preview pane"
      contains: "aria-hidden"
    - path: "frontend/src/components/Hub/GroupSidebar.tsx"
      provides: "GRID-03 sidebar + GroupSidebarItem + create flow + drop target"
      contains: "PauseCircleIcon"
  key_links:
    - from: "frontend/src/components/Hub/GroupSidebar.tsx"
      to: "frontend/src/lib/hubGroups"
      via: "import HubGroupDef"
      pattern: "lib/hubGroups"
    - from: "frontend/src/components/Hub/GroupSidebar.tsx"
      to: "frontend/src/lib/hubStatus"
      via: "deriveHubStatus for counts"
      pattern: "deriveHubStatus"
---

<objective>
Build the two NEW Hub components: `MiniPreview` (CARD-07 — plain-text snapshot pane, never an xterm) and `GroupSidebar` + `GroupSidebarItem` (GRID-03 collapsible group list with running/total counts and colorblind-safe needs-input badge; GROUP-01 inline create flow; GROUP-02 drop-target half of drag-and-drop).

Purpose: These are self-contained presentational components consumed by Wave-3 SessionCard/SessionCardGrid (MiniPreview) and Wave-4 HubPanel (GroupSidebar). Building them standalone with their own tests keeps each downstream integration plan small.
Output: Two component files + two vitest suites, all green. No wiring into existing components yet (that is Plan 04/05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-unified-grid-mini-preview-named-groups/132-UI-SPEC.md
@.planning/phases/132-unified-grid-mini-preview-named-groups/132-PATTERNS.md

<interfaces>
<!-- MiniPreview full body in 132-PATTERNS.md §MiniPreview.tsx (lines 445-485). GroupSidebar structure spec in §GroupSidebar.tsx (lines 489-536) + UI-SPEC §2 + §3. -->

MiniPreview props: { lines: string[] | undefined; dimmed?: boolean }
  - lines === undefined → Loading… ; lines.length === 0 → No output yet ; else map to .hub-card__preview-line divs
  - whole pane aria-hidden="true"

GroupSidebar props (from 132-PATTERNS.md lines 533):
  groupDefs: HubGroupDef[]; sessions: SessionInfo[]; activeGroupId: string | null;
  collapsed: boolean; onToggle: () => void; onGroupSelect: (id: string | null) => void;
  onCreateGroup: (name: string) => void; onDropOnGroup: (groupId: string, memberKey: string) => void

Counts (UI-SPEC §2 "Counts computation"):
  running = sessions in group where deriveHubStatus(s) ∈ {running, idle, waiting}
  total   = all sessions in group
  needs-input = sessions where deriveHubStatus(s) === 'waiting'
  "All" item shows global running/total

Group membership for counts: a session belongs to a named group when memberKey(s.name, s.workDir) ∈ groupDef.memberKeys (import memberKey from lib/hubGroups). Sessions not in any named group count toward the built-in "Other"/All view per UI-SPEC §2 "Empty sidebar state".

Drop target pattern (VERIFIED FileBrowserTab.tsx lines 879-899): onDragOver{e.preventDefault(); setIsDragOver(true)}, onDragLeave{setIsDragOver(false)}, onDrop{e.preventDefault(); read e.dataTransfer.getData('text/plain'); call onDropOnGroup}. e.preventDefault on onDragOver is mandatory.

BEM classes (UI-SPEC §2): .hub__group-sidebar, --collapsed, -toggle, -heading, -list, -item, -item--active, -item__name, -item__count, -item__needs-input-badge, -new
MiniPreview BEM (UI-SPEC §1): .hub-card__preview, -line, --empty, --loading

Icons: ChevronLeftIcon/ChevronRightIcon (toggle), PauseCircleIcon (needs-input badge) — @heroicons/react/24/outline

Source-level comments REQUIRED:
  /* CARD-07: mini preview is plain text snapshot — NO xterm instance; polling interval 3s shared across all cards */
  /* COLORBLIND-SAFE: needs-input badge dark hex #f59e0b — reinforcement only; PauseCircleIcon carries the state */

Accessibility (UI-SPEC §Accessibility 2): sidebar list uses role="listbox" on the ul, role="option" + aria-selected on each li. Toggle button has aria-expanded + aria-controls. Needs-input badge aria-label per Copywriting Contract ("{N} session needs input" / "{N} sessions need input").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MiniPreview.tsx (CARD-07)</name>
  <files>frontend/src/components/Hub/MiniPreview.tsx, frontend/src/components/Hub/MiniPreview.test.tsx</files>
  <read_first>
    132-PATTERNS.md §MiniPreview.tsx (lines 445-485),
    132-UI-SPEC.md §1 Mini Preview Pane,
    frontend/src/components/Hub/SessionCard.tsx (ROW/BEM/import conventions, lines 1-16)
  </read_first>
  <behavior>
    - lines === undefined → renders text "Loading…" and class hub-card__preview--loading
    - lines === [] → renders text "No output yet" and class hub-card__preview--empty
    - lines === ['a','b','c','d'] → renders 4 .hub-card__preview-line elements with that text in order
    - the outer .hub-card__preview element has aria-hidden="true" in every state
    - an empty-string line renders as a non-collapsing line (renders ' ' so the row keeps height)
  </behavior>
  <action>
    Create frontend/src/components/Hub/MiniPreview.tsx following 132-PATTERNS.md §MiniPreview.tsx verbatim: export `interface MiniPreviewProps { lines: string[] | undefined; dimmed?: boolean }` and `function MiniPreview({ lines })`. Three render branches (loading / empty / data) per <behavior>. Outer div className `hub-card__preview` with state modifier class and `aria-hidden="true"`. Lines keyed by index. Include the CARD-07 source comment. NO xterm import, NO setInterval — purely presentational; data arrives via the `lines` prop only.

    Create frontend/src/components/Hub/MiniPreview.test.tsx with @testing-library/react render assertions for each <behavior> case (use getByText for copy, container query for the aria-hidden attribute and modifier classes).
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/components/Hub/MiniPreview.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm vitest run src/components/Hub/MiniPreview.test.tsx` passes all cases
    - `grep -c "xterm\|setInterval\|Terminal(" frontend/src/components/Hub/MiniPreview.tsx` returns 0
    - `grep -c "aria-hidden" frontend/src/components/Hub/MiniPreview.tsx` returns at least 1
    - `grep -c "CARD-07" frontend/src/components/Hub/MiniPreview.tsx` returns 1
  </acceptance_criteria>
  <done>MiniPreview renders loading/empty/data states as plain aria-hidden text; no xterm, no timer; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GroupSidebar.tsx + GroupSidebarItem (GRID-03, GROUP-01, GROUP-02 drop target)</name>
  <files>frontend/src/components/Hub/GroupSidebar.tsx, frontend/src/components/Hub/GroupSidebar.test.tsx</files>
  <read_first>
    132-PATTERNS.md §GroupSidebar.tsx (lines 489-536) + §Critical Implementation Notes (needs-input badge lines 1122-1131),
    132-UI-SPEC.md §2 Group Sidebar + §3 create flow + §Accessibility Contract + §Copywriting Contract,
    frontend/src/components/Sidebar.tsx (collapsed toggle pattern lines 24-79),
    frontend/src/components/FileBrowserTab.tsx (DnD drop handlers lines 879-899),
    frontend/src/lib/hubStatus.ts (deriveHubStatus), frontend/src/lib/hubGroups.ts (memberKey, HubGroupDef — from Plan 02)
  </read_first>
  <behavior>
    - renders an "All" option plus one option per groupDef.name
    - each item shows a "{running}/{total}" count: running counts deriveHubStatus ∈ {running,idle,waiting}, total = all-in-group
    - a group containing a waiting session renders a needs-input badge containing a PauseCircleIcon and the waiting count; badge aria-label reads "1 session needs input" for count 1 and "2 sessions need input" for count 2
    - clicking a group option fires onGroupSelect(groupId); clicking "All" fires onGroupSelect(null)
    - active group (activeGroupId match) has class hub__group-sidebar-item--active and aria-selected="true"
    - collapsed=true → root has hub__group-sidebar--collapsed and group labels/heading are not rendered; toggle aria-label = "Expand group sidebar"
    - collapsed=false → toggle aria-label = "Collapse group sidebar"; clicking toggle fires onToggle
    - clicking "New group" reveals an inline input (placeholder "Group name…"); Enter with non-empty trimmed value fires onCreateGroup(value); Escape restores the button without firing; Enter with empty/whitespace value does NOT fire onCreateGroup
    - dropping on a group item (onDrop with dataTransfer text/plain = a member key) calls onDropOnGroup(groupId, key); onDragOver sets a drag-over class
  </behavior>
  <action>
    Create frontend/src/components/Hub/GroupSidebar.tsx exporting `GroupSidebar` and the `GroupSidebarItem` sub-component (same file) with the prop signature in the interfaces block. Mirror Sidebar.tsx for the collapsed toggle (but collapsed/onToggle are controlled props here, NOT internal state — HubPanel owns the state in Plan 05). Use role="listbox" on the list and role="option" + aria-selected on each item per UI-SPEC §Accessibility 2. Compute counts with deriveHubStatus + memberKey (a session is in a named group when memberKey(s.name,s.workDir) is in that def's memberKeys). The "All" item uses global counts. Render the needs-input badge ONLY when the group's waiting count > 0, and it MUST include `<PauseCircleIcon aria-hidden="true" />` plus the count, with the badge aria-label from the Copywriting Contract and the COLORBLIND-SAFE source comment. Implement the inline create flow as local `creating` state + controlled input (Enter/Escape/blur handlers per UI-SPEC §Interaction Contract). Implement drop-target handlers per the FileBrowserTab DnD pattern (preventDefault on dragOver, read text/plain on drop). Include the GRID-03 and colorblind source comments. Do NOT implement the drag SOURCE (that is on SessionCard in Plan 04) — only the drop target here.

    Create frontend/src/components/Hub/GroupSidebar.test.tsx covering every <behavior> case. For the drop test, fire a `drop` event with a mock `dataTransfer.getData` returning a member key and assert onDropOnGroup is called with (groupId, key). For the create flow, use fireEvent.keyDown with key 'Enter'/'Escape'.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/components/Hub/GroupSidebar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm vitest run src/components/Hub/GroupSidebar.test.tsx` passes all cases including the needs-input-badge-has-PauseCircleIcon assertion and the empty-name no-op
    - `grep -c "PauseCircleIcon" frontend/src/components/Hub/GroupSidebar.tsx` returns at least 1
    - `grep -c "COLORBLIND-SAFE" frontend/src/components/Hub/GroupSidebar.tsx` returns at least 1
    - `grep -c "role=\"listbox\"\|role=\"option\"" frontend/src/components/Hub/GroupSidebar.tsx` returns at least 2
    - `grep -c "draggable" frontend/src/components/Hub/GroupSidebar.tsx` returns 0 (drop target only, no drag source here)
  </acceptance_criteria>
  <done>GroupSidebar renders All + group rows with counts + colorblind-safe needs-input badge; collapse/expand, inline create flow, and drop-target all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tail lines (untrusted bytes) → MiniPreview DOM | Already stripped server-side (Plan 01); rendered as React text |
| dropped dataTransfer payload → onDropOnGroup | Drag payload is a local member-key string from same-page card |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-07 | Injection (XSS) | MiniPreview rendering tail lines | mitigate | Lines rendered as React text children only — NO dangerouslySetInnerHTML. React escapes content. Server-side strip (Plan 01) is defense-in-depth. |
| T-132-08 | Input validation | new group name | mitigate | Trim + reject empty/whitespace before firing onCreateGroup (ASVS V5; mirrors InlineSessionName) |
| T-132-09 | Tampering | dropped member key | accept | Same-page drag source; key is a local string with no privileged effect beyond group membership in localStorage |
| T-132-SC | Tampering | npm installs | n/a | Zero new dependencies |
</threat_model>

<verification>
- `cd frontend && pnpm vitest run src/components/Hub/MiniPreview.test.tsx src/components/Hub/GroupSidebar.test.tsx` green
- `grep -rn "dangerouslySetInnerHTML" frontend/src/components/Hub/MiniPreview.tsx frontend/src/components/Hub/GroupSidebar.tsx` returns nothing
</verification>

<success_criteria>
- CARD-07 preview pane component complete (plain text, no xterm, aria-hidden)
- GRID-03 group sidebar complete with colorblind-safe needs-input badge
- GROUP-01 inline create flow + GROUP-02 drop-target complete
- Keyboard/ARIA roles per UI-SPEC §Accessibility (full hardening is Phase 135)
</success_criteria>

<output>
Create `.planning/phases/132-unified-grid-mini-preview-named-groups/132-03-SUMMARY.md` when done
</output>
