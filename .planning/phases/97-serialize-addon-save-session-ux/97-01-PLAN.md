---
phase: 97
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/pnpm-lock.yaml
  - web/vendor/xterm/addons/addon-serialize.js
  - web/vendor/xterm/VERSION
  - internal/webserver/vendor_drift_test.go
  - internal/daemon/plugin_settings_test.go
  - frontend/src/lib/__tests__/stripAnsi.test.ts
  - frontend/src/lib/__tests__/sanitizeFilename.test.ts
  - frontend/src/__tests__/App.saver.test.tsx
  - frontend/src/components/__tests__/TabBar.test.tsx
  - frontend/src/components/__tests__/TerminalPanel.test.tsx
  - frontend/src/components/__tests__/PluginsSection.test.tsx
  - internal/release/no_autosave_test.go
  - app_save_terminal_test.go
autonomous: true
requirements: [SER-01, SER-02, SER-03]
tags: [phase-97, serialize, scaffolding, vendor-unblock, wave-0]
must_haves:
  truths:
    - "@xterm/addon-serialize@0.14.0 is a regular runtime dependency in frontend/package.json (NOT devDep) and resolved at 0.14.0 in frontend/pnpm-lock.yaml with zero transitive deps"
    - "web/vendor/xterm/addons/addon-serialize.js is a byte-identical copy of frontend/node_modules/@xterm/addon-serialize/lib/addon-serialize.js (UMD CJS bundle, NOT the .mjs)"
    - "web/vendor/xterm/VERSION has @xterm/addon-serialize@0.14.0 as its 9th line; vendor_drift_test.go min-count guard bumped from 8 to 9 with addon-serialize appended to the in-line error-message package list"
    - "TestDefaultPluginSettings in internal/daemon/plugin_settings_test.go has a new assertion locking PluginSettings.Serialize == true (Phase 96 already shipped the field + default; this plan locks the default to defend against regression)"
    - "Every Wave 0 RED scaffold uses expect.fail(...) (TS) or t.Skip(...) (Go) with an explicit 'Plan 97-XX implements ...' marker; no it.skip / no xit / no it.todo"
    - "Frontend scaffolds compile (pnpm tsc --noEmit exits 0); Go scaffolds build (go build ./internal/release/... ./...) and report visible SKIP rows in `go test -v` output"
    - "SER-03 negative-grep test (internal/release/no_autosave_test.go) runs GREEN immediately — no auto-save patterns exist; the test locks the absence as forever-defense regression"
  artifacts:
    - path: "frontend/package.json"
      provides: "@xterm/addon-serialize@^0.14.0 in dependencies block (NOT devDependencies)"
      contains: "@xterm/addon-serialize"
    - path: "web/vendor/xterm/addons/addon-serialize.js"
      provides: "Vendored UMD CJS bundle of @xterm/addon-serialize@0.14.0 served same-origin via embed.FS (script tag + initTerminal wiring lands in Plan 97-06)"
      min_lines: 50
    - path: "web/vendor/xterm/VERSION"
      provides: "9th line: @xterm/addon-serialize@0.14.0"
      contains: "addon-serialize@0.14.0"
    - path: "internal/webserver/vendor_drift_test.go"
      provides: "min-count guard bumped 8 → 9; addon-serialize appended to in-line error-message package list"
      contains: "addon-serialize"
    - path: "internal/daemon/plugin_settings_test.go"
      provides: "TestDefaultPluginSettings extended with Serialize == true assertion (Phase 97 SER-02 default-lock)"
      contains: "Serialize"
    - path: "frontend/src/lib/__tests__/stripAnsi.test.ts"
      provides: "RED scaffold for stripAnsi() unit tests (Plan 97-02 implements)"
      min_lines: 15
    - path: "frontend/src/lib/__tests__/sanitizeFilename.test.ts"
      provides: "RED scaffold for sanitizeFilename() unit tests (Plan 97-02 implements)"
      min_lines: 15
    - path: "frontend/src/__tests__/App.saver.test.tsx"
      provides: "RED scaffold for App.tsx saver-registry source-scan (Plan 97-03 implements)"
      min_lines: 30
    - path: "frontend/src/components/__tests__/TabBar.test.tsx"
      provides: "EXTENDED with Save Terminal As… menu-item RED scaffolds (Plan 97-04 implements)"
      contains: "Save Terminal As"
    - path: "frontend/src/components/__tests__/TerminalPanel.test.tsx"
      provides: "EXTENDED with SerializeAddon hot-swap arm RED scaffolds (Plan 97-04 implements)"
      contains: "SerializeAddon"
    - path: "frontend/src/components/__tests__/PluginsSection.test.tsx"
      provides: "EXTENDED with verbatim SER-02 secrets-warning caption RED scaffold (Plan 97-05 implements)"
      contains: "Saved files include any secrets"
    - path: "internal/release/no_autosave_test.go"
      provides: "SER-03 negative-grep regression test — GREEN immediately; locks absence of setInterval/setTimeout/BeforeQuit/OnShutdown invocations of serialize() and absence of autoSave* PluginSettings fields"
      min_lines: 60
    - path: "app_save_terminal_test.go"
      provides: "RED scaffold for TestSaveTerminalSession table-driven tests (Plan 97-05 implements)"
      min_lines: 25
  key_links:
    - from: "frontend/package.json (dependencies)"
      to: "frontend/node_modules/@xterm/addon-serialize/lib/addon-serialize.js"
      via: "pnpm install"
      pattern: "@xterm/addon-serialize"
    - from: "web/vendor/xterm/VERSION (9th line)"
      to: "internal/webserver/vendor_drift_test.go (min-count >= 9)"
      via: "drift-test min-count guard"
      pattern: "addon-serialize@0.14.0"
    - from: "internal/release/no_autosave_test.go"
      to: "frontend/src/**/*.{ts,tsx} + **/*.go (filepath.Walk)"
      via: "regex scan — locks absence of auto-save patterns"
      pattern: "(?i)\\bauto.?save\\b"
---

<objective>
Lay the Wave 0 foundation for Phase 97: (1) promote `@xterm/addon-serialize@0.14.0` to a runtime dependency under `frontend/package.json#dependencies` (mirror Phase 96 IMG-01 promotion); (2) physically vendor the UMD CJS bundle to `web/vendor/xterm/addons/addon-serialize.js` byte-identically and append the version line to `web/vendor/xterm/VERSION` (this is a Rule-3 unblock — promoting addon-serialize to a runtime dep would otherwise immediately fail `vendor_drift_test.go`'s min-count guard and the parity check; embed.go + terminal.html `<script>` tag + initTerminal construction land in Plan 97-06); (3) bump `internal/webserver/vendor_drift_test.go` min-count from 8 to 9 in lockstep; (4) lock the existing `Serialize: true` default in `defaultPluginSettings()` via a single-line `TestDefaultPluginSettings` extension (Phase 96 already shipped the `Serialize bool` field at line 81 + default at line 109 — Phase 97 makes NO struct changes); (5) author every Wave 0 RED scaffold so Plans 97-02..97-06 each have at least one concrete `<automated>` verify target waiting to flip GREEN per VALIDATION.md sampling-continuity rule; (6) ship the SER-03 negative-grep regression test as the only GREEN-NOW Wave 0 artifact (the no-auto-save invariant is structurally true today; the test locks the absence as forever-defense).

Purpose: Phase 97 mirrors Phase 96's structural shape verbatim (vendoring lockstep + RED scaffolds + GREEN-now invariant lock + hand-edited Wails binding) but with a smaller surface (no daemon struct change, no CSP amendment, no new HTTP route). The two surprises are: (a) `vendor_drift_test.go` MUST be bumped IN THIS PLAN — moving `@xterm/addon-serialize` from absent → runtime-dep would immediately fail the drift test's parity check (pnpm-lock has 9 `@xterm/*` packages but VERSION has 8), so the vendored file + VERSION + min-count bump must land together in Wave 0 to avoid red CI; (b) SER-03 ships GREEN-now because the no-auto-save invariant is structurally already satisfied — the test exists purely as regression defense (mirror of Phase 96 IMG-04 byte-fidelity test which also shipped GREEN-now in Wave 0).

Output:
- `frontend/package.json` lists `@xterm/addon-serialize: "^0.14.0"` under `dependencies` (alphabetical between `addon-search` and `addon-unicode11`); `frontend/pnpm-lock.yaml` resolves it at `0.14.0` with zero transitive deps
- `web/vendor/xterm/addons/addon-serialize.js` exists byte-identical to `frontend/node_modules/@xterm/addon-serialize/lib/addon-serialize.js`
- `web/vendor/xterm/VERSION` has 9 lines, 9th = `@xterm/addon-serialize@0.14.0`
- `internal/webserver/vendor_drift_test.go` min-count guard `< 8` → `< 9`; error message updated
- `internal/daemon/plugin_settings_test.go` TestDefaultPluginSettings asserts `s.Serialize == true`
- 8 frontend test scaffolds (5 RED extensions, 1 RED new App.saver.test, 2 RED new lib helper tests)
- 1 Go RED scaffold (`app_save_terminal_test.go`)
- 1 Go GREEN-NOW SER-03 regression test (`internal/release/no_autosave_test.go`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/97-serialize-addon-save-session-ux/97-RESEARCH.md
@.planning/phases/97-serialize-addon-save-session-ux/97-PATTERNS.md
@.planning/phases/97-serialize-addon-save-session-ux/97-VALIDATION.md
@.planning/phases/96-image-addon-csp-audit/96-01-PLAN.md
@.planning/phases/96-image-addon-csp-audit/96-06-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Existing daemon contracts and test patterns the executor MUST preserve verbatim. -->

From internal/daemon/plugin_settings.go (current — Phase 96 shape, NO changes in Phase 97):
```go
type PluginSettings struct {
    // ... fields including:
    Image       bool        `json:"image"`        // line 79
    ImageConfig ImageConfig `json:"imageConfig"`  // line 80
    Serialize   bool        `json:"serialize"`    // line 81 — already exists; DO NOT touch
    Clipboard   bool        `json:"clipboard"`
    Progress    bool        `json:"progress"`
}

func defaultPluginSettings() PluginSettings {
    return PluginSettings{
        // ... including:
        Image:       true,                                  // line 107
        ImageConfig: ImageConfig{StorageLimit: 16},         // line 108
        Serialize:   true,                                  // line 109 — already exists; DO NOT touch
        // ...
    }
}
```

**Phase 97 makes ZERO changes to plugin_settings.go.** The `Serialize bool` field and `Serialize: true` default already exist (Phase 96 ship). Phase 97 only ADDS a single assertion line to the existing `TestDefaultPluginSettings` test.

From internal/webserver/vendor_drift_test.go (current — Phase 96 shape, line 34):
```go
if len(pnpmVersions) < 8 {
    t.Fatalf("failed to parse at least 8 @xterm/* packages (xterm, addon-fit, addon-webgl, addon-unicode11, addon-clipboard, addon-search, addon-web-links, addon-image) from pnpm-lock.yaml: found %v (Phase 95 SRC-95-06 — addon-web-links joined the manifest; Phase 96 IMG-03 — addon-image joined the manifest; T-96-06-01 mitigation)", pnpmVersions)
}
```

After this plan: `< 8` → `< 9`; `at least 8` → `at least 9`; append `, addon-serialize` to the in-line package list inside the parenthetical; append `; Phase 97 SER-03 — addon-serialize joined the manifest` to the citation chain.

From web/vendor/xterm/VERSION (current — 8 lines):
```
@xterm/xterm@6.0.0
@xterm/addon-fit@0.11.0
@xterm/addon-webgl@0.19.0
@xterm/addon-unicode11@0.9.0
@xterm/addon-clipboard@0.2.0
@xterm/addon-search@0.16.0
@xterm/addon-web-links@0.12.0
@xterm/addon-image@0.9.0
```

After this plan: 9 lines; 9th = `@xterm/addon-serialize@0.14.0` with trailing newline preserved.

From frontend/package.json (current — addon-image is a runtime dep at `@xterm/addon-image: "^0.9.0"`):
- `@xterm/addon-serialize` is **absent today** (NOT installed yet — verify before promotion via `pnpm view @xterm/addon-serialize` against the lockfile if pnpm-lock already shows it as a devDep, follow Phase 96 IMG-01 demote-then-promote pattern; otherwise fresh install).

After this plan: `@xterm/addon-serialize: "^0.14.0"` in `dependencies` block, alphabetical position **between `@xterm/addon-search` and `@xterm/addon-unicode11`**.
</interfaces>

<closely-related>
- Phase 96 Plan 01 (`.planning/phases/96-image-addon-csp-audit/96-01-PLAN.md`) is the verbatim structural mirror — same Wave 0 shape (install + scaffolds + GREEN-now invariant lock). Phase 97 differs ONLY in: (a) NO daemon struct change (Phase 96 already added `Serialize bool` and the field exists; Phase 97 only locks the default assertion); (b) the vendored file lands in Wave 0 (not deferred to Wave 3 like Phase 96 did) because the drift-test min-count gate would otherwise fail the moment the dep promotes.
- Phase 96 Plan 06 (`.planning/phases/96-image-addon-csp-audit/96-06-PLAN.md`) is the structural mirror for what NORMALLY lands in the last plan: vendor + embed.go + script tag + initTerminal + drift-test bump. Phase 97 SPLITS this — Wave 0 ships the file + VERSION + drift-test bump (the parity gate); Plan 97-06 ships the embed.go directive + script tag + initTerminal construction.
- 97-PATTERNS.md is the line-by-line analog map. Pattern Map confirms: every Phase 97 file has a Phase 92/93/95/96 analog; the only structurally-novel shape is the saver-registry callback chain in App.tsx (Plan 97-03).
- 97-VALIDATION.md "Wave 0 Requirements" enumerates all 11 scaffold/extension files this plan must produce; sampling-continuity requires every downstream plan have ≥1 named verify target before its implementation lands.
</closely-related>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote addon-serialize to runtime dep + vendor UMD bundle + bump VERSION + bump drift-test min-count + lock SER-02 default</name>
  <files>frontend/package.json, frontend/pnpm-lock.yaml, web/vendor/xterm/addons/addon-serialize.js, web/vendor/xterm/VERSION, internal/webserver/vendor_drift_test.go, internal/daemon/plugin_settings_test.go</files>
  <read_first>
    - frontend/package.json (full — confirm `@xterm/addon-serialize` is NOT yet in dependencies; verify alphabetical neighbors `@xterm/addon-search@^0.16.0` and `@xterm/addon-unicode11@^0.9.0`)
    - frontend/pnpm-lock.yaml (search for any existing `@xterm/addon-serialize` block; if present as devDep follow Phase 96 IMG-01 `pnpm remove` then `pnpm add` demote-then-promote pattern; if absent fresh install)
    - internal/webserver/vendor_drift_test.go (full — confirm current `< 8` guard at line 34 and the in-line package list inside the t.Fatalf format string)
    - internal/daemon/plugin_settings.go (lines 70-115 — confirm `Serialize bool` is at line 81 and `Serialize: true` is at line 109; confirm Phase 97 makes ZERO changes to this file)
    - internal/daemon/plugin_settings_test.go (full — find `TestDefaultPluginSettings`; identify the existing assertion block style used for `Image`/`Serialize`/etc; locate the appropriate insertion point for the new Serialize-default-lock assertion)
    - web/vendor/xterm/VERSION (full — confirm 8 entries; verify trailing newline)
    - .planning/phases/96-image-addon-csp-audit/96-01-PLAN.md Task 1 (the structural mirror — copy the dep-promotion + drift-test pattern verbatim and adapt to addon-serialize)
    - .planning/phases/96-image-addon-csp-audit/96-06-PLAN.md (the structural mirror for the vendor copy + VERSION append + drift-test min-count bump — adapt 7 → 8 logic to 8 → 9)
    - .planning/phases/97-serialize-addon-save-session-ux/97-RESEARCH.md §"Standard Stack" (confirms 0.14.0, zero transitive deps, lib/addon-serialize.js is the UMD CJS bundle)
    - .planning/phases/97-serialize-addon-save-session-ux/97-PATTERNS.md §"frontend/package.json", §"web/vendor/xterm/addons/addon-serialize.js", §"web/vendor/xterm/VERSION", §"internal/webserver/vendor_drift_test.go", §"internal/daemon/plugin_settings_test.go"
  </read_first>
  <behavior>
    - After this task: `pnpm list @xterm/addon-serialize --dir frontend --depth 0` reports `@xterm/addon-serialize 0.14.0` AND `frontend/package.json` `dependencies` block contains the entry alphabetically between `@xterm/addon-search` and `@xterm/addon-unicode11`. Entry is NOT under `devDependencies`.
    - After this task: `web/vendor/xterm/addons/addon-serialize.js` exists; `cmp -s frontend/node_modules/@xterm/addon-serialize/lib/addon-serialize.js web/vendor/xterm/addons/addon-serialize.js` exits 0 (byte-identical).
    - After this task: `web/vendor/xterm/VERSION` has exactly 9 non-empty lines; 9th line literal: `@xterm/addon-serialize@0.14.0`.
    - After this task: `internal/webserver/vendor_drift_test.go` line 34's `< 8` → `< 9`; the literal `at least 8` → `at least 9`; the in-line package list contains `addon-serialize`; the citation chain appends `; Phase 97 SER-03 — addon-serialize joined the manifest`.
    - After this task: `go test ./internal/webserver/ -run TestXtermVendorVersions -count=1` exits 0.
    - After this task: `internal/daemon/plugin_settings_test.go` TestDefaultPluginSettings asserts `s.Serialize == true`; existing assertions unchanged.
    - After this task: `go test ./internal/daemon/ -run TestDefaultPluginSettings -count=1` exits 0; `cd frontend && pnpm tsc --noEmit` exits 0.
  </behavior>
  <action>
    Step A — promote addon-serialize to runtime dep. The package is currently absent from frontend/package.json. Install as a runtime dep:

    ```bash
    cd frontend && pnpm add @xterm/addon-serialize@^0.14.0
    cd ..
    ```

    If `pnpm-lock.yaml` already shows `@xterm/addon-serialize` under `devDependencies` (rare — only if the user pre-installed during research), follow the Phase 96 IMG-01 demote-then-promote pattern instead: `cd frontend && pnpm remove @xterm/addon-serialize && pnpm add @xterm/addon-serialize@^0.14.0`. Verify alphabetical position: AFTER `@xterm/addon-search` and BEFORE `@xterm/addon-unicode11`. Do NOT bump unrelated packages. Do NOT install globally.

    Verify zero transitive deps:
    ```bash
    cd frontend && pnpm list @xterm/addon-serialize --depth 1
    ```
    Output should show no children. Per RESEARCH §"Standard Stack" line 203, `@xterm/addon-serialize` has zero runtime dependencies.

    Step B — vendor the UMD bundle byte-identically. The pnpm install in Step A places the addon at `frontend/node_modules/@xterm/addon-serialize/lib/addon-serialize.js` (the UMD CJS bundle, NOT the `.mjs` ESM file). Copy it:

    ```bash
    cp frontend/node_modules/@xterm/addon-serialize/lib/addon-serialize.js web/vendor/xterm/addons/addon-serialize.js
    ```

    Verify byte-identity:
    ```bash
    cmp -s frontend/node_modules/@xterm/addon-serialize/lib/addon-serialize.js web/vendor/xterm/addons/addon-serialize.js && echo OK
    ```

    Verify the file is the UMD build (assigns `global.SerializeAddon` in the IIFE preamble):
    ```bash
    head -3 web/vendor/xterm/addons/addon-serialize.js
    ```
    Expected: leading `(function (global, factory)` UMD wrapper. Do NOT use the `.mjs` file — terminal.html `<script>` tags load CJS UMD and require `window.SerializeAddon` to be assigned.

    Step C — append VERSION line. Edit `web/vendor/xterm/VERSION` to add a 9th line (preserving trailing newline):

    Append literally: `@xterm/addon-serialize@0.14.0` (followed by newline). The file should end with newline; do NOT remove existing entries; do NOT reorder.

    Verify:
    ```bash
    grep -c "^@xterm/" web/vendor/xterm/VERSION  # should be 9
    grep -q "^@xterm/addon-serialize@0.14.0$" web/vendor/xterm/VERSION  # exits 0
    ```

    Step D — bump `internal/webserver/vendor_drift_test.go` min-count guard. Locate the t.Fatalf at line 34. Change:

    BEFORE:
    ```go
    if len(pnpmVersions) < 8 {
        t.Fatalf("failed to parse at least 8 @xterm/* packages (xterm, addon-fit, addon-webgl, addon-unicode11, addon-clipboard, addon-search, addon-web-links, addon-image) from pnpm-lock.yaml: found %v (Phase 95 SRC-95-06 — addon-web-links joined the manifest; Phase 96 IMG-03 — addon-image joined the manifest; T-96-06-01 mitigation)", pnpmVersions)
    }
    ```

    AFTER:
    ```go
    if len(pnpmVersions) < 9 {
        t.Fatalf("failed to parse at least 9 @xterm/* packages (xterm, addon-fit, addon-webgl, addon-unicode11, addon-clipboard, addon-search, addon-web-links, addon-image, addon-serialize) from pnpm-lock.yaml: found %v (Phase 95 SRC-95-06 — addon-web-links joined the manifest; Phase 96 IMG-03 — addon-image joined the manifest; Phase 97 SER-03 — addon-serialize joined the manifest)", pnpmVersions)
    }
    ```

    DO NOT touch any other line in the file. The version-parity assertion logic (parsing pnpm-lock + comparing to VERSION) is automatic — it scans both files and asserts equality, so adding the 9th line in VERSION + adding the package via pnpm both auto-feed into the existing assertion logic.

    Step E — lock the existing `Serialize: true` default with Phase 97 SER-02 citation. The assertion `if !s.Serialize { t.Error(...) }` already EXISTS at `internal/daemon/plugin_settings_test.go` lines 28–29 from a prior phase (current message: `"expected Serialize=true (UI-SPEC default ON)"`). Make this step **idempotent**:

    1. **If the assertion already exists** (it does — confirmed at lines 28–29): UPDATE the existing `t.Error(...)` message to cite Phase 97 SER-02 lock-in. Use the Edit tool to replace ONLY the message string:

       BEFORE (line 29):
       ```go
       t.Error("expected Serialize=true (UI-SPEC default ON)")
       ```

       AFTER:
       ```go
       t.Error("expected Serialize=true (Phase 97 SER-02 lock-in)")
       ```

       Add a comment immediately above the assertion (lines 28–29) if not present:
       ```go
       // Phase 97 SER-02: Serialize defaults to true (Phase 96 set the default at
       // plugin_settings.go line 109; Phase 97 freezes the assertion as forever-
       // defense regression). Toggle is the addon-as-library boolean — when true,
       // SerializeAddon attaches to terminals; when false, the right-click "Save
       // Terminal As…" menu item still appears but invokes a banner instead of
       // the dialog (per RESEARCH §User Constraints / "Whether the Save menu
       // item appears when Serialize is toggled OFF").
       ```

    2. **If the assertion is somehow absent** (it shouldn't be, per the read_first verification): ADD the assertion block above plus the `if !s.Serialize { t.Error(...) }` body. Do NOT duplicate the assertion if Step 1 applies.

    DO NOT modify any other existing assertions. DO NOT touch `plugin_settings.go` (the field and default already exist from Phase 96). The end-state is: the existing assertion stays; only its `t.Error` message text and a Phase 97 SER-02 comment are updated.
  </action>
  <verify>
    <automated>
      grep -q '"@xterm/addon-serialize": "\^0.14' frontend/package.json \
        &amp;&amp; ! grep -A 30 'devDependencies' frontend/package.json | grep -q '@xterm/addon-serialize' \
        &amp;&amp; (cd frontend &amp;&amp; pnpm list @xterm/addon-serialize --depth 0 2&gt;/dev/null | grep -qE "@xterm/addon-serialize 0\.14\.[0-9]+") \
        &amp;&amp; cmp -s frontend/node_modules/@xterm/addon-serialize/lib/addon-serialize.js web/vendor/xterm/addons/addon-serialize.js \
        &amp;&amp; head -3 web/vendor/xterm/addons/addon-serialize.js | grep -qE "(global, factory|UMD)" \
        &amp;&amp; [ "$(grep -c '^@xterm/' web/vendor/xterm/VERSION)" = "9" ] \
        &amp;&amp; grep -q '^@xterm/addon-serialize@0.14.0$' web/vendor/xterm/VERSION \
        &amp;&amp; grep -q 'len(pnpmVersions) &lt; 9' internal/webserver/vendor_drift_test.go \
        &amp;&amp; grep -q 'addon-serialize' internal/webserver/vendor_drift_test.go \
        &amp;&amp; grep -q 'Phase 97 SER-03' internal/webserver/vendor_drift_test.go \
        &amp;&amp; go test ./internal/webserver/ -run TestXtermVendorVersions -count=1 \
        &amp;&amp; grep -q 'Serialize.*true' internal/daemon/plugin_settings_test.go \
        &amp;&amp; grep -q 'SER-02' internal/daemon/plugin_settings_test.go \
        &amp;&amp; go test ./internal/daemon/ -run TestDefaultPluginSettings -count=1 \
        &amp;&amp; gofmt -l internal/daemon/plugin_settings_test.go internal/webserver/vendor_drift_test.go | (! grep .) \
        &amp;&amp; (cd frontend &amp;&amp; pnpm tsc --noEmit)
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"@xterm/addon-serialize": "\^0.14' frontend/package.json` exits 0 (entry in dependencies)
    - `grep -A 30 'devDependencies' frontend/package.json | grep -c '@xterm/addon-serialize'` returns 0 (NOT in devDependencies)
    - `cd frontend && pnpm list @xterm/addon-serialize --depth 0 | grep -E "0\.14\.[0-9]+"` exits 0
    - `cmp -s frontend/node_modules/@xterm/addon-serialize/lib/addon-serialize.js web/vendor/xterm/addons/addon-serialize.js` exits 0 (byte-identical UMD)
    - `head -3 web/vendor/xterm/addons/addon-serialize.js | grep -qE "(global, factory|UMD)"` exits 0 (UMD wrapper present)
    - `grep -c '^@xterm/' web/vendor/xterm/VERSION` returns exactly `9`
    - `grep -q '^@xterm/addon-serialize@0.14.0$' web/vendor/xterm/VERSION` exits 0
    - `grep -q 'len(pnpmVersions) < 9' internal/webserver/vendor_drift_test.go` exits 0
    - `grep -q ', addon-serialize' internal/webserver/vendor_drift_test.go` exits 0 (in-line list extended)
    - `grep -q 'Phase 97 SER-03' internal/webserver/vendor_drift_test.go` exits 0 (citation chain extended)
    - `go test ./internal/webserver/ -run TestXtermVendorVersions -count=1` exits 0
    - `grep -q 'Serialize.*true' internal/daemon/plugin_settings_test.go` exits 0 (assertion present — already exists at lines 28–29)
    - `grep -q 'SER-02' internal/daemon/plugin_settings_test.go` exits 0 (Phase 97 SER-02 citation added — comment OR error message)
    - `go test ./internal/daemon/ -run TestDefaultPluginSettings -count=1` exits 0
    - `go test ./internal/daemon/... ./internal/webserver/... -count=1` exits 0 (no Phase 92-96 regression)
    - `gofmt -l internal/daemon/plugin_settings_test.go internal/webserver/vendor_drift_test.go` produces NO output
    - `cd frontend && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    `@xterm/addon-serialize@^0.14.0` is a runtime dep; UMD bundle vendored byte-identically; VERSION 9-line; drift-test min-count gate passes; Serialize default locked.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author Wave 0 RED+GREEN scaffolds for every downstream Phase 97 plan (8 frontend, 1 Go RED, 1 Go GREEN-now SER-03 regression test)</name>
  <files>frontend/src/lib/__tests__/stripAnsi.test.ts, frontend/src/lib/__tests__/sanitizeFilename.test.ts, frontend/src/__tests__/App.saver.test.tsx, frontend/src/components/__tests__/TabBar.test.tsx, frontend/src/components/__tests__/TerminalPanel.test.tsx, frontend/src/components/__tests__/PluginsSection.test.tsx, internal/release/no_autosave_test.go, app_save_terminal_test.go</files>
  <read_first>
    - frontend/src/lib/relayClient.test.ts (full — existing lib/ unit-test conventions; vitest import shape; describe/it style)
    - frontend/src/lib/urlSafety.ts and frontend/src/lib/urlSafety.test.ts if they exist (Phase 95 — pure helper test pattern; named export with table-driven tests)
    - frontend/src/components/__tests__/TabBar.test.tsx (full — existing source-scan test conventions; existing `Rename` menu-item assertions if any)
    - frontend/src/components/__tests__/TerminalPanel.test.tsx (full — existing hot-swap arm assertions for clipboard/webgl/search/webLinks; the source-scan style; how Phase 96 distinguished mount-vs-hot-swap-useEffect placement)
    - frontend/src/components/__tests__/PluginsSection.test.tsx (full — existing italic-caption assertions for unicode11 and image rows)
    - frontend/src/__tests__/App.plugin-event.test.tsx (full — App-level source-scan test pattern; the `import raw from '../App.tsx?raw'` convention)
    - app.go (lines 800-870 — verify OpenFileDialog at lines 815-829; identify insertion site for SaveTerminalSession between OpenFileDialog and the next method). The TestSER03_OnlySaveTerminalSessionInAppGo path-relative regex hits this file from `internal/release/` (two levels up); confirm the relative path resolves before authoring the scaffold.
    - app_test.go (lines 1-260 — full understanding of `testApp(t)` harness and `TestUpdateCLIPath` table-driven pattern; identify imports and test-file conventions)
    - internal/webserver/vendor_drift_test.go (full — Phase 93 filepath.Walk + regex pattern; this is the ARCHITECTURAL ANALOG for the new internal/release/no_autosave_test.go GREEN-now test)
    - .planning/phases/97-serialize-addon-save-session-ux/97-RESEARCH.md §"Wave 0 Gaps", §"ANSI Output Audit" (lines 139-167), §"User Constraints / Locked Decisions" (regex `/\x1b\[[?]?[0-9;]*[a-zA-Z]/g`)
    - .planning/phases/97-serialize-addon-save-session-ux/97-PATTERNS.md §`internal/release/no_autosave_test.go` (lines 232-269), §`app_save_terminal_test.go` (lines 187-230), §source-scan tests (lines 511-547)
    - .planning/phases/97-serialize-addon-save-session-ux/97-VALIDATION.md "Wave 0 Requirements" (full — the 11-item checklist this task satisfies)
    - .planning/phases/96-image-addon-csp-audit/96-01-PLAN.md Task 2 (the structural mirror — copy scaffold-style verbatim and adapt names to Phase 97)
  </read_first>
  <behavior>
    - `frontend/src/lib/__tests__/stripAnsi.test.ts` (NEW) — 6+ RED `expect.fail` tests covering SGR, ECH, CUF/CUB/CUU/CUD, DEC private modes, plain-text passthrough, fixture round-trip. Each test cites Plan 97-02 in the failure message.
    - `frontend/src/lib/__tests__/sanitizeFilename.test.ts` (NEW) — 6+ RED `expect.fail` tests covering path traversal, empty input, leading-dot, Windows reserved (CON/PRN/AUX/NUL/COM1/LPT1), whitespace collapse, allowed characters preserved. Each test cites Plan 97-02.
    - `frontend/src/__tests__/App.saver.test.tsx` (NEW) — 7+ RED `expect.fail` source-scan tests for App.tsx imports (stripAnsi, sanitizeFilename, SaveTerminalSession), state declaration (serializerRegistry or equivalent), handleRegisterSaver, handleRequestSave, TerminalPanel `onRegisterSaver` prop receipt, TabBar `onRequestSave` prop receipt. Each test cites Plan 97-03.
    - `frontend/src/components/__tests__/TabBar.test.tsx` EXTENDED — 3+ new RED `expect.fail` tests for: "Save Terminal As" string presence, `onRequestSave` prop in interface, click handler invokes `onRequestSave?.(contextMenu.tabId)`. Each test cites Plan 97-04.
    - `frontend/src/components/__tests__/TerminalPanel.test.tsx` EXTENDED — 7+ new RED `expect.fail` tests for: SerializeAddon import, serializeAddonRef declaration, `new SerializeAddon()` construction, `serialize({ excludeModes: true })` (Pitfall #1 regression guard), `pluginConfig?.serialize` toggle read, `onRegisterSaver` prop consumption, hot-swap-useEffect placement (NOT mount, distinguishing from Image), unregister-on-toggle-off, dep-array inclusion. Each test cites Plan 97-04.
    - `frontend/src/components/__tests__/PluginsSection.test.tsx` EXTENDED — 1 new RED `expect.fail` test asserting the verbatim string `'Saved files include any secrets, tokens, or sensitive data printed in the session.'` appears in raw source AND appears within ~200 chars of the `'Save terminal as text'` row label. Cites Plan 97-05.
    - `internal/release/no_autosave_test.go` (NEW, package `release_test`) — 2 GREEN-now tests + 1 RED-skip scaffold: `TestSER03_NoAutoSavePatterns` (filepath.Walk over the repo + regex scan for `setInterval/setTimeout/BeforeQuit/OnShutdown` calling `serialize`, plus `auto.?save` / `save.?on.?(quit|exit|close)` patterns) and `TestSER03_NoAutoSettingsField` (assert PluginSettings has no autoSave/autoExport/autoCapture/saveOnX field) — both GREEN immediately. **Plus** `TestSER03_OnlySaveTerminalSessionInAppGo` (RED-skip scaffold #9 in the Wave 0 list) — asserts that app.go has exactly one method matching the (?i)Save.*(Session|Terminal|Scrollback) shape and that its name is `SaveTerminalSession`. Stays SKIP until Plan 97-05 lands the method (Plan 97-05 unskips by removing the `t.Skip` line; the body then flips GREEN automatically — see VALIDATION.md row 12 SER-03 dialog-call-site assertion).
    - `app_save_terminal_test.go` (NEW, package `main`, repo root) — 1 RED-skip test `TestSaveTerminalSession` covering 4 sub-cases: cancel (empty path), normal write, write IO error (e.g. parent dir doesn't exist), dialog setup error. Cites Plan 97-05 implements via `saveFileDialogFunc` injection.
  </behavior>
  <action>
    Author 8 scaffold/extension files. ALL TS scaffolds use `expect.fail('RED scaffold — Plan 97-XX implements ... (97-VALIDATION row XX)')`. ALL Go RED scaffolds use `t.Skip("Pending until Plan 97-XX implements ...")` so they appear as visible SKIP rows. The two SER-03 regression tests are GREEN-now (no skip).

    File 1 — `frontend/src/lib/__tests__/stripAnsi.test.ts` (NEW):
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('Phase 97 SER-01: stripAnsi() — Plan 97-02 implements', () => {
      it('strips SGR (Select Graphic Rendition) sequences', () => {
        expect.fail('RED scaffold — Plan 97-02 implements src/lib/stripAnsi.ts (97-VALIDATION row SER-01 stripAnsi strips SGR/CUF/CUB/CUU/CUD/ECH/DEC modes)')
      })
      it('strips ECH (erase character) sequences', () => {
        expect.fail('RED scaffold — Plan 97-02 implements ECH \\x1b[NX stripping (97-RESEARCH §ANSI Output Audit row ECH)')
      })
      it('strips cursor-move sequences (CUF/CUB/CUU/CUD)', () => {
        expect.fail('RED scaffold — Plan 97-02 implements cursor-move stripping (97-RESEARCH §ANSI Output Audit rows CUF/CUB/CUU/CUD)')
      })
      it('strips DEC private mode sequences (\\x1b[?25h, \\x1b[?7l)', () => {
        expect.fail('RED scaffold — Plan 97-02 implements DEC private mode stripping (\\x1b[?…l/h optional ? prefix)')
      })
      it('preserves plain text without escape sequences', () => {
        expect.fail('RED scaffold — Plan 97-02 implements stripAnsi("plain text") === "plain text" identity case')
      })
      it('round-trips serializeAddon-style fixture (mixed SGR + cursor + ECH + plain)', () => {
        expect.fail('RED scaffold — Plan 97-02 implements full serializeAddon fixture round-trip (97-RESEARCH §ANSI Output Audit table)')
      })
    })
    ```

    File 2 — `frontend/src/lib/__tests__/sanitizeFilename.test.ts` (NEW):
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('Phase 97 SER-01: sanitizeFilename() — Plan 97-02 implements', () => {
      it('rejects path traversal (../../etc/passwd)', () => {
        expect.fail('RED scaffold — Plan 97-02 implements src/lib/sanitizeFilename.ts (97-VALIDATION row SER-01 sanitizeFilename handles path traversal)')
      })
      it('returns "session" for empty input', () => {
        expect.fail('RED scaffold — Plan 97-02 implements empty-input fallback (97-PATTERNS §sanitizeFilename Pattern 4)')
      })
      it('returns "session" for leading-dot hidden files (.bashrc)', () => {
        expect.fail('RED scaffold — Plan 97-02 implements leading-dot guard')
      })
      it('returns "session" for Windows reserved names (CON/PRN/AUX/NUL/COM1/LPT1)', () => {
        expect.fail('RED scaffold — Plan 97-02 implements Windows reserved-name guard')
      })
      it('collapses whitespace runs to underscore (my session  name → my_session_name)', () => {
        expect.fail('RED scaffold — Plan 97-02 implements whitespace collapse')
      })
      it('preserves allowed characters (agent-1.txt → agent-1.txt)', () => {
        expect.fail('RED scaffold — Plan 97-02 implements allowed-character preservation [\\w\\-.]')
      })
    })
    ```

    File 3 — `frontend/src/__tests__/App.saver.test.tsx` (NEW). Mirror Phase 92's `App.plugin-event.test.tsx` source-scan style (`import raw from '../App.tsx?raw'`):
    ```typescript
    import { describe, it, expect } from 'vitest'
    import raw from '../App.tsx?raw'
    import terminalPanelRaw from '../components/TerminalPanel.tsx?raw'
    import tabBarRaw from '../components/TabBar.tsx?raw'

    describe('Phase 97 SER-01: App.tsx saver-registry round-trip — Plan 97-03 implements', () => {
      it('App.tsx imports stripAnsi from ./lib/stripAnsi', () => {
        expect.fail('RED scaffold — Plan 97-03 implements App.tsx import { stripAnsi } from "./lib/stripAnsi"')
      })
      it('App.tsx imports sanitizeFilename from ./lib/sanitizeFilename', () => {
        expect.fail('RED scaffold — Plan 97-03 implements App.tsx import { sanitizeFilename } from "./lib/sanitizeFilename"')
      })
      it('App.tsx imports SaveTerminalSession from wailsjs binding', () => {
        expect.fail('RED scaffold — Plan 97-03 implements App.tsx import { SaveTerminalSession } from "./wailsjs/go/main/App"')
      })
      it('App.tsx declares saver-registry state (serializerRegistry or equivalent Map<sessionId, () => string>)', () => {
        expect.fail('RED scaffold — Plan 97-03 implements serializerRegistry useState (97-RESEARCH §Pattern 2: Saver Registry)')
      })
      it('App.tsx declares handleRegisterSaver useCallback', () => {
        expect.fail('RED scaffold — Plan 97-03 implements handleRegisterSaver(sessionId, fn|null)')
      })
      it('App.tsx declares handleRequestSave useCallback that calls SaveTerminalSession after stripAnsi + sanitizeFilename', () => {
        expect.fail('RED scaffold — Plan 97-03 implements handleRequestSave(tabId) → registry lookup → stripAnsi → sanitizeFilename → SaveTerminalSession')
      })
      it('handleRequestSave shows a banner when registry is empty (Serialize OFF)', () => {
        expect.fail('RED scaffold — Plan 97-03 implements no-saver-registered banner branch (97-RESEARCH §Whether the Save menu item appears when Serialize is toggled OFF)')
      })
      it('TerminalPanel.tsx accepts onRegisterSaver?: (sessionId, fn) => void prop', () => {
        // This assertion uses terminalPanelRaw — ensures Plan 97-04 wires through.
        expect.fail('RED scaffold — Plan 97-04 implements onRegisterSaver prop on TerminalPanel')
      })
      it('TabBar.tsx accepts onRequestSave?: (tabId: string) => void prop', () => {
        // This assertion uses tabBarRaw — ensures Plan 97-04 wires through.
        expect.fail('RED scaffold — Plan 97-04 implements onRequestSave prop on TabBar')
      })
    })
    ```

    File 4 — `frontend/src/components/__tests__/TabBar.test.tsx` EXTEND. Append to end of file (DO NOT modify existing tests):
    ```typescript
    describe('Phase 97 SER-01: TabBar Save Terminal As… menu item — Plan 97-04 implements', () => {
      it('Save Terminal As menu-item label appears in TabBar source', () => {
        expect.fail('RED scaffold — Plan 97-04 implements <button role="menuitem">Save Terminal As…</button> with U+2026 ellipsis')
      })
      it('TabBar.tsx props interface declares onRequestSave?: (tabId: string) => void', () => {
        expect.fail('RED scaffold — Plan 97-04 implements onRequestSave optional prop')
      })
      it('Save menu-item onClick invokes onRequestSave?.(contextMenu.tabId) and closes the menu', () => {
        expect.fail('RED scaffold — Plan 97-04 implements onClick handler that calls onRequestSave + setContextMenu(null)')
      })
    })
    ```

    File 5 — `frontend/src/components/__tests__/TerminalPanel.test.tsx` EXTEND. Append:
    ```typescript
    describe('Phase 97 SER-01: SerializeAddon hot-swap arm — Plan 97-04 implements', () => {
      it('imports SerializeAddon from @xterm/addon-serialize', () => {
        expect.fail('RED scaffold — Plan 97-04 implements import { SerializeAddon } from "@xterm/addon-serialize"')
      })
      it('declares serializeAddonRef parallel to other addon refs', () => {
        expect.fail('RED scaffold — Plan 97-04 implements useRef<SerializeAddon | null>(null)')
      })
      it('constructs new SerializeAddon() inside the hot-swap useEffect', () => {
        expect.fail('RED scaffold — Plan 97-04 implements new SerializeAddon() construction')
      })
      it('calls serialize({ excludeModes: true }) — Pitfall #1 regression guard against trailing mode-restore wrap', () => {
        expect.fail('RED scaffold — Plan 97-04 implements serialize({ excludeModes: true })')
      })
      it('reads pluginConfig?.serialize toggle to gate the arm', () => {
        expect.fail('RED scaffold — Plan 97-04 implements pluginConfig?.serialize gate')
      })
      it('consumes onRegisterSaver?: callback prop from App.tsx', () => {
        expect.fail('RED scaffold — Plan 97-04 implements onRegisterSaver prop consumption')
      })
      it('SerializeAddon construction lives in HOT-SWAP useEffect, NOT mount useEffect (distinct from Phase 96 ImageAddon)', () => {
        // Critical pattern-map distinction: Image is mount-only (buffer-state implications);
        // Serialize is hot-swap (pure buffer-walker). Plan 97-04 places it alongside Clipboard/WebGL,
        // NOT alongside Image/Unicode11. 97-PATTERNS.md §Hot-swap addon arm contract.
        expect.fail('RED scaffold — Plan 97-04 implements hot-swap-useEffect placement (NOT mount); 97-PATTERNS §Hot-swap arm contract')
      })
      it('unregisters saver on toggle-off (onRegisterSaver?.(sessionId, null) — Pitfall #6)', () => {
        expect.fail('RED scaffold — Plan 97-04 implements unregister-on-toggle-off + unregister-on-unmount')
      })
      it('hot-swap useEffect dep array includes pluginConfig?.serialize and onRegisterSaver', () => {
        expect.fail('RED scaffold — Plan 97-04 extends dep array to include pluginConfig?.serialize + onRegisterSaver')
      })
    })
    ```

    File 6 — `frontend/src/components/__tests__/PluginsSection.test.tsx` EXTEND. Append:
    ```typescript
    describe('Phase 97 SER-02: italic secrets-warning caption under Serialize row — Plan 97-05 implements', () => {
      it('Serialize renderRow carries the verbatim secrets warning as its 4th argument', () => {
        // Verbatim per REQUIREMENTS SER-02:
        // "Saved files include any secrets, tokens, or sensitive data printed in the session."
        expect.fail('RED scaffold — Plan 97-05 implements verbatim secrets-warning caption (97-PATTERNS §PluginsSection.tsx Serialize row 4th arg)')
      })
    })
    ```

    File 7 — `internal/release/no_autosave_test.go` (NEW directory `internal/release/`). This test runs GREEN-now (no t.Skip). Mirror the `vendor_drift_test.go` filepath.Walk + regex pattern:
    ```go
    package release_test

    import (
        "io/fs"
        "os"
        "path/filepath"
        "regexp"
        "strings"
        "testing"
    )

    // TestSER03_NoAutoSavePatterns enforces the SER-03 invariant: no on-disk
    // capture of session state occurs without an explicit user action in v3.2.
    // This is a static regex-grep regression test, mirror of Phase 88's
    // OriginPatterns: ["*"] reintroduction guard and Phase 93's
    // vendor_drift_test.go filepath.Walk pattern.
    //
    // The test scans both the Go tree and the frontend src/ tree for forbidden
    // patterns. It runs GREEN immediately because no auto-save patterns exist
    // today; the test exists as forever-defense against regression. Adding a
    // setInterval/setTimeout/BeforeQuit/OnShutdown hook that calls serialize()
    // — or a PluginSettings field whose JSON name matches autoSave/autoExport/
    // autoCapture/saveOnX — will cause this test to FAIL on next CI run.
    //
    // Phase 97 SER-03. Mirror of internal/webserver/vendor_drift_test.go and
    // Phase 88 origin_test.go negative-regression patterns.
    func TestSER03_NoAutoSavePatterns(t *testing.T) {
        // Forbidden patterns. Each entry: (regex, description).
        forbidden := []struct {
            re   *regexp.Regexp
            desc string
        }{
            // Long-delay scheduled serialize() invocation
            {regexp.MustCompile(`setInterval\([^)]*[Ss]eriali[zs]e`), "setInterval scheduling serialize()"},
            {regexp.MustCompile(`setTimeout\([^,]*[Ss]eriali[zs]e[^,]*,\s*[0-9]{4,}`), "setTimeout long-delay scheduled serialize()"},
            // Lifecycle-hook serialize() invocation
            {regexp.MustCompile(`(?i)BeforeQuit\b[^}]*[Ss]eriali[zs]e`), "BeforeQuit hook calling serialize()"},
            {regexp.MustCompile(`(?i)OnShutdown\b[^}]*[Ss]eriali[zs]e`), "OnShutdown hook calling serialize()"},
            // Auto-save settings vocabulary
            {regexp.MustCompile(`(?i)\bauto[._-]?save\b`), "auto-save vocabulary"},
            {regexp.MustCompile(`(?i)\bauto[._-]?export\b`), "auto-export vocabulary"},
            {regexp.MustCompile(`(?i)\bauto[._-]?capture\b`), "auto-capture vocabulary"},
            {regexp.MustCompile(`(?i)\bsave[._-]?on[._-]?(quit|exit|close)\b`), "save-on-quit/exit/close vocabulary"},
        }

        // Skip-list directories.
        skipDirs := map[string]bool{
            ".git":                     true,
            "node_modules":             true,
            "build":                    true,
            "dist":                     true,
            "vendor":                   true,
            "internal/release":         true, // self — regex literals would false-positive
            ".planning":                true, // research/plan docs cite the patterns by name
            "frontend/src/wailsjs":     true, // generated bindings
            "screenshots":              true,
        }

        // Repo root is two levels up from internal/release/.
        repoRoot, err := filepath.Abs(filepath.Join("..", ".."))
        if err != nil {
            t.Fatalf("filepath.Abs: %v", err)
        }

        var violations []string
        err = filepath.WalkDir(repoRoot, func(path string, d fs.DirEntry, err error) error {
            if err != nil {
                return err
            }
            relPath, _ := filepath.Rel(repoRoot, path)
            if d.IsDir() {
                for skip := range skipDirs {
                    if relPath == skip || strings.HasPrefix(relPath, skip+string(filepath.Separator)) {
                        return filepath.SkipDir
                    }
                }
                return nil
            }
            // Only scan source files.
            ext := filepath.Ext(path)
            if ext != ".go" && ext != ".ts" && ext != ".tsx" && ext != ".js" {
                return nil
            }
            // Self-skip (the test file itself).
            if strings.HasSuffix(path, "no_autosave_test.go") {
                return nil
            }
            data, err := os.ReadFile(path)
            if err != nil {
                return nil // unreadable files — best-effort scan
            }
            for _, pat := range forbidden {
                if loc := pat.re.FindIndex(data); loc != nil {
                    line := 1 + strings.Count(string(data[:loc[0]]), "\n")
                    violations = append(violations, relPath+":"+itoa(line)+" matches forbidden pattern: "+pat.desc)
                }
            }
            return nil
        })
        if err != nil {
            t.Fatalf("filepath.WalkDir: %v", err)
        }
        if len(violations) > 0 {
            t.Errorf("SER-03 invariant violated — auto-save patterns detected:\n  %s", strings.Join(violations, "\n  "))
        }
    }

    // TestSER03_NoAutoSettingsField asserts that PluginSettings (defined in
    // internal/daemon/plugin_settings.go) has no JSON field whose name matches
    // autoSave/autoExport/autoCapture/saveOnX — locking the SER-03 invariant
    // against a future settings-key regression.
    func TestSER03_NoAutoSettingsField(t *testing.T) {
        repoRoot, err := filepath.Abs(filepath.Join("..", ".."))
        if err != nil {
            t.Fatalf("filepath.Abs: %v", err)
        }
        data, err := os.ReadFile(filepath.Join(repoRoot, "internal", "daemon", "plugin_settings.go"))
        if err != nil {
            t.Fatalf("ReadFile plugin_settings.go: %v", err)
        }
        forbidden := []*regexp.Regexp{
            regexp.MustCompile("`json:\"auto[Ss]ave\""),
            regexp.MustCompile("`json:\"auto[Ee]xport\""),
            regexp.MustCompile("`json:\"auto[Cc]apture\""),
            regexp.MustCompile("`json:\"saveOn[A-Z][a-zA-Z]+\""),
        }
        for _, re := range forbidden {
            if re.Match(data) {
                t.Errorf("SER-03 invariant violated: PluginSettings contains forbidden auto-save field matching %s", re.String())
            }
        }
    }

    // TestSER03_OnlySaveTerminalSessionInAppGo asserts that app.go contains
    // EXACTLY ONE method matching the regex (?i)func\s+\([^)]*\*App\)\s+\w*[Ss]ave\w*(?:Session|Terminal|Scrollback)\w*
    // and that method's name is exactly SaveTerminalSession. This locks the
    // SER-03 dialog-call-site invariant: there is one and only one Save*
    // method in app.go that handles the terminal/session save gesture. Adding
    // a second (e.g., SaveSessionAuto) would cause this test to FAIL.
    //
    // Per VALIDATION.md row 12 (SER-03: "Only (*App).SaveTerminalSession
    // matches (?i)save.*(session|terminal|scrollback) in app.go").
    //
    // RED scaffold in Wave 0 — Plan 97-01 — because (*App).SaveTerminalSession
    // does NOT yet exist (Plan 97-05 lands it). Wave 0 ships this as a t.Skip
    // scaffold; Plan 97-05 unskips by removing the t.Skip line — at that point
    // len(names) == 1 && names[0] == "SaveTerminalSession" will hold and the
    // test flips GREEN automatically.
    func TestSER03_OnlySaveTerminalSessionInAppGo(t *testing.T) {
        t.Skip("Pending until Plan 97-05 lands (*App).SaveTerminalSession in app.go (97-VALIDATION row 12 SER-03 dialog-call-site assertion). When Plan 97-05 implements the method, remove this t.Skip line; the body below will run and flip GREEN.")

        repoRoot, err := filepath.Abs(filepath.Join("..", ".."))
        if err != nil {
            t.Fatalf("filepath.Abs: %v", err)
        }
        src, err := os.ReadFile(filepath.Join(repoRoot, "app.go"))
        if err != nil {
            t.Fatalf("read app.go: %v", err)
        }
        re := regexp.MustCompile(`(?i)func\s+\([^)]*\*App\)\s+(\w*[Ss]ave\w*(?:Session|Terminal|Scrollback)\w*)\s*\(`)
        matches := re.FindAllSubmatch(src, -1)
        var names []string
        for _, m := range matches {
            names = append(names, string(m[1]))
        }
        if len(names) != 1 || names[0] != "SaveTerminalSession" {
            t.Errorf("SER-03: expected exactly one Save*(Session|Terminal|Scrollback) method named SaveTerminalSession in app.go, got %v", names)
        }
    }

    // itoa formats an int as decimal — used to avoid pulling in strconv just
    // for one call site.
    func itoa(n int) string {
        if n == 0 {
            return "0"
        }
        neg := n < 0
        if neg {
            n = -n
        }
        var buf [20]byte
        i := len(buf)
        for n > 0 {
            i--
            buf[i] = byte('0' + n%10)
            n /= 10
        }
        if neg {
            i--
            buf[i] = '-'
        }
        return string(buf[i:])
    }
    ```

    File 8 — `app_save_terminal_test.go` (NEW, repo root, package `main`). RED-skip scaffold mirroring `app_test.go` `TestUpdateCLIPath`:
    ```go
    package main

    import "testing"

    // TestSaveTerminalSession — Plan 97-05 implements.
    //
    // Table-driven tests for (*App).SaveTerminalSession covering:
    //   1. Cancel path — saveFileDialogFunc returns ("", nil); assert no file
    //      written and method returns nil (silent success per
    //      OpenFileDialog precedent at app.go lines 815-829).
    //   2. Normal write — saveFileDialogFunc returns (tempdir/path, nil);
    //      assert file written with content matching the supplied bytes,
    //      file mode 0o644.
    //   3. Write IO error — saveFileDialogFunc returns a path under a
    //      non-existent parent dir; assert returned error wraps with
    //      "SaveTerminalSession: write:" prefix.
    //   4. Dialog setup error — saveFileDialogFunc returns ("", err);
    //      assert returned error wraps with "SaveTerminalSession: dialog:"
    //      prefix.
    //
    // Plan 97-05 introduces a saveFileDialogFunc field on *App (function-
    // injection pattern matching serviceControlFunc / statusFunc precedent
    // per PROJECT.md "Key Decisions") so the test can mock the Wails runtime
    // free function. See 97-PATTERNS.md §"Function-injection for testability".
    func TestSaveTerminalSession(t *testing.T) {
        t.Skip("Pending until Plan 97-05 implements (*App).SaveTerminalSession + saveFileDialogFunc injection (97-VALIDATION row SER-01 Wails RPC writes file via SaveFileDialog).")
    }
    ```

    DO NOT modify `frontend/src/components/__tests__/PluginsSection.test.tsx` existing tests. DO NOT modify `frontend/src/components/__tests__/TerminalPanel.test.tsx` existing tests (Phase 96 IMG tests must continue green). DO NOT modify `app_test.go` or its `testApp(t)` harness — Plan 97-05 will add the saveFileDialogFunc field to *App and adapt this test to use it.
  </action>
  <verify>
    <automated>
      test -f frontend/src/lib/__tests__/stripAnsi.test.ts \
        &amp;&amp; test -f frontend/src/lib/__tests__/sanitizeFilename.test.ts \
        &amp;&amp; test -f frontend/src/__tests__/App.saver.test.tsx \
        &amp;&amp; test -f internal/release/no_autosave_test.go \
        &amp;&amp; test -f app_save_terminal_test.go \
        &amp;&amp; grep -q "Save Terminal As" frontend/src/components/__tests__/TabBar.test.tsx \
        &amp;&amp; grep -q "SerializeAddon" frontend/src/components/__tests__/TerminalPanel.test.tsx \
        &amp;&amp; grep -q "Saved files include any secrets" frontend/src/components/__tests__/PluginsSection.test.tsx \
        &amp;&amp; grep -q "TestSER03_NoAutoSavePatterns" internal/release/no_autosave_test.go \
        &amp;&amp; grep -q "TestSER03_NoAutoSettingsField" internal/release/no_autosave_test.go \
        &amp;&amp; grep -q "TestSER03_OnlySaveTerminalSessionInAppGo" internal/release/no_autosave_test.go \
        &amp;&amp; grep -q "SaveTerminalSession" internal/release/no_autosave_test.go \
        &amp;&amp; grep -q "TestSaveTerminalSession" app_save_terminal_test.go \
        &amp;&amp; go build ./internal/release/... \
        &amp;&amp; go vet ./internal/release/... \
        &amp;&amp; go test ./internal/release/... -count=1 \
        &amp;&amp; go test . -run TestSaveTerminalSession -count=1 -v 2&gt;&amp;1 | grep -qE "^---? SKIP" \
        &amp;&amp; (cd frontend &amp;&amp; pnpm tsc --noEmit) \
        &amp;&amp; (! grep -E "(it\.skip\(|xit\(|it\.todo\()" frontend/src/lib/__tests__/stripAnsi.test.ts frontend/src/lib/__tests__/sanitizeFilename.test.ts frontend/src/__tests__/App.saver.test.tsx) \
        &amp;&amp; [ "$(grep -cE 'Plan 97-(02|03|04|05|06)' frontend/src/lib/__tests__/stripAnsi.test.ts frontend/src/lib/__tests__/sanitizeFilename.test.ts frontend/src/__tests__/App.saver.test.tsx frontend/src/components/__tests__/TabBar.test.tsx frontend/src/components/__tests__/TerminalPanel.test.tsx frontend/src/components/__tests__/PluginsSection.test.tsx)" -ge 20 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - All scaffold files exist on disk:
      - `test -f frontend/src/lib/__tests__/stripAnsi.test.ts` exits 0
      - `test -f frontend/src/lib/__tests__/sanitizeFilename.test.ts` exits 0
      - `test -f frontend/src/__tests__/App.saver.test.tsx` exits 0
      - `test -f internal/release/no_autosave_test.go` exits 0
      - `test -f app_save_terminal_test.go` exits 0
    - All Go scaffolds compile: `go build ./internal/release/... ./...` exits 0
    - `go vet ./internal/release/...` exits 0
    - **GREEN-NOW SER-03 tests pass immediately** (this is THE load-bearing assertion):
      - `go test ./internal/release/... -count=1` exits 0 with PASS for both `TestSER03_NoAutoSavePatterns` and `TestSER03_NoAutoSettingsField`
    - **RED-skip dialog-call-site scaffold visible** (per BLOCKER #3 / VALIDATION.md row 12):
      - `go test ./internal/release/... -run TestSER03_OnlySaveTerminalSessionInAppGo -count=1 -v 2>&1 | grep -E "^---? SKIP"` exits 0
      - `grep -q "TestSER03_OnlySaveTerminalSessionInAppGo" internal/release/no_autosave_test.go` exits 0
      - `grep -q "SaveTerminalSession" internal/release/no_autosave_test.go` exits 0 (test body literally references the canonical method name)
      - Plan 97-05 will unskip this test by removing the `t.Skip(...)` line; the body's regex assertion then flips GREEN with `len(names) == 1 && names[0] == "SaveTerminalSession"`
    - app_save_terminal_test.go is RED-SKIP visibly:
      - `go test . -run TestSaveTerminalSession -count=1 -v 2>&1 | grep -E "^---? SKIP"` exits 0
    - All TS scaffolds use explicit `expect.fail(...)` with literal `Plan 97-XX` markers (no it.skip/xit/it.todo):
      - `grep -E "(it\.skip|xit\(|it\.todo)" frontend/src/lib/__tests__/stripAnsi.test.ts frontend/src/lib/__tests__/sanitizeFilename.test.ts frontend/src/__tests__/App.saver.test.tsx` returns nothing
      - `grep -cE 'Plan 97-(02|03|04|05|06)'` across all 6 frontend test files returns ≥ 20 (every RED test cites a downstream plan)
    - Specific assertions:
      - `grep -q "Save Terminal As" frontend/src/components/__tests__/TabBar.test.tsx` exits 0
      - `grep -q "SerializeAddon" frontend/src/components/__tests__/TerminalPanel.test.tsx` exits 0
      - `grep -q "Saved files include any secrets" frontend/src/components/__tests__/PluginsSection.test.tsx` exits 0
      - `grep -q "excludeModes: true" frontend/src/components/__tests__/TerminalPanel.test.tsx` exits 0 (Pitfall #1 regression guard scaffolded)
      - `grep -q "hot-swap" frontend/src/components/__tests__/TerminalPanel.test.tsx` exits 0 (mount-vs-hot-swap distinction scaffolded)
    - `cd frontend && pnpm tsc --noEmit` exits 0
    - Phase 92-96 tests still green: `go test ./...` exits 0; `cd frontend && pnpm test --run` does not regress (RED tests are visibly red but scoped to Phase 97 namespace)
  </acceptance_criteria>
  <done>
    8 scaffolds on disk; SER-03 negative-grep test runs GREEN immediately as the only Wave 0 invariant lock; every downstream Phase 97 plan has ≥1 named verify target waiting to flip GREEN; TS scaffolds compile + RED visibly via explicit `expect.fail`; Go RED scaffold visibly SKIP-rows.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pnpm registry → frontend/node_modules | Supply-chain ingress for `@xterm/addon-serialize`. SLSA L2 attestation already covers the resulting binary (Phase 90). First-party `@xterm/*` scope. |
| Vendored UMD asset → web/embed.FS | Same-origin static asset; CSP `script-src 'self'` enforcement comes from Phase 89 middleware unchanged. addon-serialize has zero CSP-relevant constructs (97-RESEARCH §"Mandatory Pre-Phase CSP Audit" — zero WebAssembly/Worker/blob/eval). |
| internal/release/ test scope | Static regex scan over the repo; reads files only; no network or process execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01-01 | Tampering | npm package supply chain (`@xterm/addon-serialize`) | accept | First-party `@xterm/*` scope; Phase 90 SLSA L2 attestation covers release artifact; `vendor_drift_test.go` (bumped here) gates version parity in CI; addon has zero transitive runtime deps (verified per RESEARCH §"Standard Stack" line 203 via `pnpm view`). |
| T-97-01-02 | Information Disclosure | RED scaffold leaks implementation details to test output | accept | Skip messages cite Plan-XX targets only; no secrets, no internal API paths beyond what is already in RESEARCH.md (committed to repo). |
| T-97-01-03 | Tampering | future regression introducing auto-save (timer/lifecycle hook) | mitigate | `internal/release/no_autosave_test.go` GREEN-now regression test fails on next CI run if `setInterval/setTimeout/BeforeQuit/OnShutdown` is wired to `serialize()`, OR if `PluginSettings` adds an `autoSave*` field. Forever-defense at minimal cost. |
| T-97-01-04 | Tampering | future regression introducing auto-save settings vocabulary | mitigate | `TestSER03_NoAutoSettingsField` blocks adding `json:"autoSave"` etc to PluginSettings struct. |
| T-97-01-05 | Denial of Service | drift-test min-count gate fails before Wave 0 closes | accept | Wave 0 lands the vendored file + VERSION line + min-count bump in lockstep so `go test ./internal/webserver/...` is GREEN at end of plan; subsequent plans rely on this invariant remaining green. |
</threat_model>

<verification>
After both tasks complete:

```bash
# Vendor parity + drift gate
go test ./internal/webserver/ -run TestXtermVendorVersions -count=1
cmp -s frontend/node_modules/@xterm/addon-serialize/lib/addon-serialize.js web/vendor/xterm/addons/addon-serialize.js && echo "byte-identical OK"
grep -c '^@xterm/' web/vendor/xterm/VERSION  # expect 9

# SER-02 default lock
go test ./internal/daemon/ -run TestDefaultPluginSettings -count=1

# SER-03 negative-grep — GREEN-NOW
go test ./internal/release/... -count=1 -v

# Wave 0 RED scaffolds visible (Go)
go test . -run TestSaveTerminalSession -v 2>&1 | head -10

# Frontend compile + RED scaffolds visible (vitest expect.fail)
cd frontend && pnpm tsc --noEmit
pnpm test --run src/lib/__tests__/stripAnsi.test.ts 2>&1 | tail -20
pnpm test --run src/__tests__/App.saver.test.tsx 2>&1 | tail -20
```

## Truths Mapped to Tests

| Truth | Verified By |
|-------|-------------|
| addon-serialize@0.14.0 is a runtime dep | `pnpm list @xterm/addon-serialize --dir frontend` + grep package.json devDependencies absence |
| UMD bundle vendored byte-identical | `cmp -s` of node_modules/lib vs web/vendor/xterm/addons |
| VERSION 9-line + drift-test guard passes | `grep -c '^@xterm/' web/vendor/xterm/VERSION` == 9; TestXtermVendorVersionsMatchPnpmLock GREEN |
| Serialize default locked | TestDefaultPluginSettings GREEN with new assertion |
| SER-03 invariant locked GREEN-NOW | TestSER03_NoAutoSavePatterns + TestSER03_NoAutoSettingsField pass immediately |
| Every downstream plan has ≥1 RED target | 8 scaffold/extension files exist; explicit Plan-XX markers in each test |
</verification>

<success_criteria>
- All Task 1 acceptance criteria green
- All Task 2 acceptance criteria green
- `go test ./internal/daemon/... ./internal/webserver/... ./internal/release/... -count=1` exits 0
- `cd frontend && pnpm tsc --noEmit` exits 0
- Phase 92/93/94/95/96 tests still green: `go test ./...` and `cd frontend && pnpm test` (existing suites) unaffected
- SER-03 negative-grep test passes — locks the no-auto-save invariant as forever-defense regression
- Vendor lockstep partially landed (file + VERSION + drift-test min-count); embed.go + terminal.html + initTerminal land in Plan 97-06
</success_criteria>

<output>
After completion, create `.planning/phases/97-serialize-addon-save-session-ux/97-01-SUMMARY.md` covering:
- pnpm runtime-dep promotion + lockfile diff scope (zero transitive deps)
- Vendored UMD bundle byte-identical at web/vendor/xterm/addons/addon-serialize.js + VERSION 9-line + drift-test min-count 8→9 lockstep
- TestDefaultPluginSettings extended for SER-02 default lock (no struct change)
- 8 scaffolds/extensions and which Plan 97-XX each unblocks (cite VALIDATION.md row IDs)
- SER-03 negative-grep regression test runs GREEN-NOW (no Wave 1 implementation needed) — note this in the summary as the architectural invariant lock, mirror of Phase 96 IMG-04 byte-fidelity test
</output>
