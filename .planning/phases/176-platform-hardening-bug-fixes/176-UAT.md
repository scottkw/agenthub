---
status: complete
phase: 176-platform-hardening-bug-fixes
source: [176-VERIFICATION.md]
started: 2026-07-09T00:00:00Z
updated: 2026-07-09T18:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Linux/Wayland GUI launch — no segfault, working menu, no DMABUF freeze (M-52 / BUG-05 / #124)
expected: On a Linux/Wayland desktop, `AgentHub` launches without the macOS-role-menu (Wails GTK nil-SubMenu) segfault, shows a working menu bar (File / Help), and the webview renders/interacts with no DMABUF freeze. Source fix: `main.go` guards the three macOS role menus (`AppMenu`/`EditMenu`/`WindowMenu`) behind `goruntime.GOOS != "linux"` and sets `WEBKIT_DISABLE_DMABUF_RENDERER=1` on Linux in `runGUI()`.
result: pass
resolution: pass-by-acceptance (owner has no Linux/Wayland machine; the #124 reporter validated the fix from source in the exact environment — accepted 2026-07-09 as sufficient to ship)
source: reporter (from-source verification on the reporter's Pop!_OS / COSMIC / Wayland setup — the environment where BUG-05 / #124 was originally filed)
evidence: |
  Owner has no Linux/Wayland machine to reproduce; the bug and its fix were reported and validated
  from source by the friend who filed #124 on Pop!_OS/COSMIC/Wayland. The fix is a straightforward,
  fully source-reviewable platform guard in main.go (darwin/windows keep the role menus; linux is the
  only excluded platform — WR-01 code-review fix confirmed `GOOS != "linux"`, not `== "darwin"`).
  Decision (2026-07-09): ACCEPT the reporter's validation as sufficient to ship; no owner UAT planned.
  Not owner-reproducible — no further action.

### 2. Production /app/ CSP console sweep — no CSP violations on the guest SPA (M-53 / BUG-06 / #123)
expected: On a production build (`wails build -tags wailsassets`, so the daemon's `staticAppFS` is wired — `wails dev`/`go build` returns 503 "app bundle not configured"), share a session and load the shared `/app/` link in Chrome with DevTools → Console open. The strict CSP now served on `/app/` (`default-src 'none'`; `script-src 'self' 'wasm-unsafe-eval'`; `style-src 'self' 'unsafe-inline'`; `connect-src 'self' wss://…`; `img-src 'self' data:`; `font-src 'self'`; `base-uri 'none'`; `form-action 'self'`; `frame-ancestors 'none'`) must NOT block any of the SPA's own resources: terminal renders, chat works, the WebSocket connects, images/fonts/styles load. Pass = zero `Content-Security-Policy` violation errors in the console.
result: pass
source: live (dev-browser against a real production Wails build over Tailscale Funnel, 2026-07-09; join → /app/?session=…&cap=… guest SPA in Chromium with console + response listeners)
evidence: |
  Live prod-build Funnel sweep — zero CSP violations, zero page errors, zero failed/blocked requests.
  - The strict CSP header IS served on the /app/ document and byte-matches the expected policy:
    default-src 'none'; script-src 'self' 'wasm-unsafe-eval'; style-src 'self' 'unsafe-inline';
    connect-src 'self' wss://kens-personal-macbook-air.tail46d69a.ts.net:7443; img-src 'self' data:;
    font-src 'self'; base-uri 'none'; form-action 'self'; frame-ancestors 'none'
    (confirms we exercised the real strict policy, not its absence).
  - All SPA resources returned 200 and none were blocked: /app/assets/index-*.css, dist-*.js,
    index-*.js, and both PlusJakartaSans-*.woff2 fonts (all 'self').
  - Terminal renders: 3 xterm canvases, hasXterm=true, live Claude Code session streaming in the
    screenshot (m53-app.png) — proves the WebSocket connected (permitted by connect-src wss://…:7443).
  - Chat panel rendered ("No messages yet… Send").
  The strict policy permits every one of the app's own legitimate resources with no violations.
  Complements the automated coverage (TestCSPHeaderStrict_App header assertion +
  TestAppBundle_HashedAsset_CacheableNotNoStore WR-02 cacheability).

### 3. Hub mini-preview long-line wrapping (BUG-07 / #127)
expected: Hub session-card mini-preview renders each output line as one clipped horizontal row (not char-per-row stacking).
result: pass
source: automated (dev-browser live-repro harness during 176-03 — DOES-NOT-REPRODUCE verdict)
evidence: |
  176-03 live repro (screenshot 176-03-evidence-minipreview-repro.png + computed-style/geometry checks)
  showed all four preview lines render as clean single horizontal rows with ellipsis clipping; per-char
  spans within a line share one `top`, each line a distinct evenly-spaced `top`. Root cause eliminated by
  the Phase 172 Hub-card refinement — no code change needed (plan Branch B). No live human UAT required.

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

note: test 1 (M-52) is pass-by-acceptance — see its resolution field.

## Notes

- GitHub #123/#124/#127 remain OPEN — close at v4.2 ship.
- M-52 accepted-by-reporter (owner has no Linux box). M-53 PASSED live on a real prod-build Funnel
  session (2026-07-09) — no outstanding live items remain for this phase.
