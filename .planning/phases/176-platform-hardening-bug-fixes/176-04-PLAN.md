---
phase: 176-platform-hardening-bug-fixes
plan: 04
type: execute
wave: 2
depends_on: ["176-01", "176-02", "176-03"]
files_modified:
  - TESTING.md
autonomous: true
requirements: [BUG-05, BUG-06, BUG-07]
must_haves:
  truths:
    - "TESTING.md carries a new manual M-NN item for the BUG-05 Linux/Wayland GUI launch + menu-bar + no-freeze check."
    - "TESTING.md carries a new manual M-NN item for the BUG-06 production-build /app/ CSP console sweep."
    - "TESTING.md Section 4 has a traceability row for the BUG-06 CSP test, and for the BUG-07 test IFF plan 176-03 landed a fix (BRANCH A)."
    - "tests/check-traceability-paths.sh exits 0 and the Suite Manifest counts stay internally consistent."
  artifacts:
    - "TESTING.md — new M-NN manual items (BUG-05 Linux GUI, BUG-06 /app/ CSP sweep), Section 4 traceability rows, Suite Manifest reconciliation"
  key_links:
    - "check-traceability-paths.sh passes on the updated Section 4 (repo-relative .go/.tsx paths only, no test names in the path column)"
    - "Suite Manifest file counts adjust only for NEW test files (none this phase — BUG-06 extended csp_integration_test.go in place; BUG-07 BRANCH A extended MiniPreview.test.tsx in place)"
---

<objective>
Reconcile TESTING.md for Phase 176 per the repo Regression Test Convention (D-12) and CLAUDE.md's standing rule. This is the single serialized editor of TESTING.md for the phase (Wave 2), so the three parallel Wave-1 bug plans never conflict on it.

Purpose: keep TESTING.md — the canonical regression-suite home — truthful about Phase 176's manual checks and test artifacts.
Output: new manual M-NN items, Section 4 traceability rows, Suite Manifest reconciliation, and a green `check-traceability-paths.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/176-platform-hardening-bug-fixes/176-CONTEXT.md
@TESTING.md
@.planning/phases/176-platform-hardening-bug-fixes/176-01-SUMMARY.md
@.planning/phases/176-platform-hardening-bug-fixes/176-02-SUMMARY.md
@.planning/phases/176-platform-hardening-bug-fixes/176-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manual M-NN items (BUG-05 Linux GUI, BUG-06 /app/ CSP sweep) and Section 4 traceability rows</name>
  <read_first>TESTING.md (Section 5 manual checklist — the last category and highest existing M-NN is M-51 in "Category Z"; Section 4 traceability header lines 164-172 and existing BUG-01/02/04 rows lines 408-412), 176-01-SUMMARY.md (BUG-05 fix confirmation), 176-02-SUMMARY.md (BUG-06 test name TestCSPHeaderStrict_App), 176-03-SUMMARY.md (which branch A/B was taken)</read_first>
  <files>TESTING.md</files>
  <action>Append a new Section 5 category (e.g. "Category AA — Platform & Hardening Bug Fixes (BUG-05..07)") with two manual items (numbered after the current highest M-51, i.e. M-52 and M-53):
  - **M-52 (BUG-05, #124)** — Linux/Wayland GUI: on the reporter's environment class (Pop!_OS 24.04 / COSMIC / Wayland / x86_64 / WebKit2GTK 4.1 / `.deb`), the GUI launches with NO segfault, the File and Help menu bar are present and functional, and hamburger/UI interactions do NOT freeze (DMABUF fix). Include the "why not automatable" note (macOS dev box cannot exercise the Linux GTK/Wayland path; reporter's from-source verification accepted for ship per D-11) and mark it OPPORTUNISTIC / not-ship-blocking.
  - **M-53 (BUG-06, #123)** — production-build `/app/` CSP console sweep (the manual embodiment of D-06): build `wails build -tags "webkit2_41,wailsassets"`, load `/app/`, confirm the SPA renders and the browser console shows no CSP violation that breaks functionality (inline scripts, `wasm-unsafe-eval`, `connect-src` SSE + WS relay, `font-src`, `img-src data:`) per D-06; relax a directive ONLY for a concrete violation (D-07). Note `/app/` returns 503 under `wails dev`/plain `go build`, so a prod build is required (release-time, opportunistic).
  Then add Section 4 traceability rows (repo-relative paths only, test names in the Notes column):
  - `| BUG-06 | internal/webserver/csp_integration_test.go | Go | Phase 176-02 (v4.2): TestCSPHeaderStrict_App — GET /app/ (with stub staticAppFS) returns 200 with the strict CSP header via assertCSPHeaderStrict; proves the /app/ route now flows through ws.cspHeaders. |`
  - IFF 176-03 took BRANCH A (fix landed): `| BUG-07 | frontend/src/components/Hub/MiniPreview.test.tsx | vitest | Phase 176-03 (v4.2, extended in place): single-clipped-row structural assertion — each StyledSpan row renders as one .hub-card__preview-line, child spans not forced display:block/column. |`. IF 176-03 took BRANCH B (already-fixed, no code change): add NO BUG-07 traceability row; instead record in this task's notes that #127 was closed as already-fixed with live evidence (no test artifact).
  Follow the existing Section 5 M-NN entry format (numbered steps + "why not automatable" + "Source:" lines) and the Section 4 table format exactly.</action>
  <verify>
    <automated>grep -q 'M-52' TESTING.md && grep -q 'M-53' TESTING.md && grep -q 'TestCSPHeaderStrict_App' TESTING.md && grep -q 'csp_integration_test.go | Go' TESTING.md</automated>
  </verify>
  <acceptance_criteria>
    - M-52 (BUG-05 Linux GUI) and M-53 (BUG-06 /app/ CSP sweep) exist in Section 5 with the standard format and "why not automatable" notes.
    - A Section 4 BUG-06 row points to internal/webserver/csp_integration_test.go with TestCSPHeaderStrict_App in the Notes column.
    - A BUG-07 Section 4 row exists IFF 176-03 landed a fix; otherwise the already-fixed closure is documented and no BUG-07 row is added.
  </acceptance_criteria>
  <done>TESTING.md reflects Phase 176's two manual items and the BUG-06 (and conditional BUG-07) traceability rows in the correct sections/formats.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the Suite Manifest counts and run check-traceability-paths.sh</name>
  <read_first>TESTING.md (Section 2 Suite Manifest lines 22-50; the most recent dated correction note pattern from Phase 175-07), tests/check-traceability-paths.sh</read_first>
  <files>TESTING.md</files>
  <action>Reconcile the Suite Manifest (Section 2) file counts. Phase 176 added NO new test FILES — BUG-06's TestCSPHeaderStrict_App was added to the existing internal/webserver/csp_integration_test.go, and BUG-07 BRANCH A (if taken) extended the existing frontend/src/components/Hub/MiniPreview.test.tsx in place; BUG-05 added no test. So the Go and vitest FILE counts are unchanged. Using the established correction-note pattern (add a NEW dated note stating old→new counts rather than editing a prior note — precedent 171-04/173-07), add a short Phase 176 note confirming the counts are unchanged (net +0 files; +1 Go test function in an existing file, and conditionally +1 vitest test in an existing file). Do NOT invent a file-count bump. Then run `bash tests/check-traceability-paths.sh` and ensure it exits 0; if the macOS BSD-grep unreliability noted in Phase 173-07 applies, validate the new rows' paths manually (all end in .go/.ts/.tsx/.sh, no test names in the path column) and record that in the SUMMARY.</action>
  <verify>
    <automated>bash tests/check-traceability-paths.sh; echo "exit=$?" && grep -qE 'Phase 176' TESTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/check-traceability-paths.sh` exits 0 (or, if BSD-grep-unreliable per 173-07, manual path validation is recorded and the new rows demonstrably use repo-relative code paths only).
    - A dated Phase 176 Suite Manifest note states the file counts are unchanged (no invented bump).
    - No test names appear in any Section 4 path column.
  </acceptance_criteria>
  <done>The Suite Manifest is reconciled (unchanged file counts, documented) and check-traceability-paths.sh passes on the updated Section 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only change to TESTING.md; no code, no runtime surface, no input parsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-176-04-SC | Tampering | package installs | n/a | accept | No npm/pip/cargo installs; TESTING.md doc edits only, no new dependency. |

ASVS L1, block on high: no ASVS L1 high-severity threat introduced — this plan edits only the regression-test manifest document. Gate satisfied (nothing to block).
</threat_model>

<verification>
- `bash tests/check-traceability-paths.sh` exits 0 on the updated Section 4.
- M-52, M-53, and the BUG-06 traceability row are present; the BUG-07 row is present iff 176-03 landed a fix.
- The Suite Manifest carries a dated Phase 176 note; no file-count bump was invented.
</verification>

<success_criteria>
- TESTING.md truthfully records Phase 176's manual checks and test artifacts per the Regression Test Convention.
- check-traceability-paths.sh is green.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 176-04)
- **New manual TESTING.md items:** M-52 (BUG-05 Linux/Wayland GUI launch + menu bar + no-freeze) and M-53 (BUG-06 production-build /app/ CSP console sweep), in a new Section 5 "Category AA".
- **New Section 4 traceability rows:** BUG-06 → internal/webserver/csp_integration_test.go (TestCSPHeaderStrict_App); BUG-07 → frontend/src/components/Hub/MiniPreview.test.tsx (only if 176-03 BRANCH A).
- **Suite Manifest note:** dated Phase 176 entry confirming unchanged file counts (+0 new test files).
</artifacts>

<output>
Create `.planning/phases/176-platform-hardening-bug-fixes/176-04-SUMMARY.md` when done.
</output>
