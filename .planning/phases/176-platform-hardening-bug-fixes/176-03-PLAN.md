---
phase: 176-platform-hardening-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Hub/MiniPreview.tsx
  - frontend/src/components/Hub/MiniPreview.test.tsx
autonomous: true
requirements: [BUG-07]
must_haves:
  truths:
    - "The char-per-row stacking is reproduced or disproven LIVE on the current build (dev-browser component harness, long-line repro) BEFORE any code change."
    - "If reproduced: each mini-preview output line renders on ONE clipped horizontal row (not column-wrapped), and the true root cause is identified before the fix."
    - "If NOT reproduced: #127 is closed as already-fixed with recorded live evidence (screenshot + computed styles) and no code change."
  artifacts:
    - "Live evidence (screenshot + computed styles of .hub-card__preview-line and its child spans) captured via dev-browser"
    - "(conditional) frontend/src/components/Hub/MiniPreview.tsx + MiniPreview.test.tsx — only if a render-layer fix lands"
  key_links:
    - "dev-browser mounts MiniPreview alone in a throwaway vite harness (the Wails-coupled app shell will not render in plain Chrome)"
    - "single-clipped-row render assertion added to MiniPreview.test.tsx as a structural DOM check (jsdom computes no real layout)"
  prohibitions:
    - "Do NOT re-apply the white-space:nowrap / overflow:hidden / text-overflow:ellipsis CSS — it already exists on .hub-card__preview-line (style.css:6020-6028) (D-01)."
    - "Do NOT implement terminal-style column-wrap to multiple rows — target is a single clipped row per line (D-03)."
---

<objective>
Fix BUG-07 (#127): the Hub session-card mini-preview stacks long output lines one/two characters per row instead of rendering each line as one clipped horizontal row.

CRITICAL: the CSS the issue hypothesizes as the fix (`white-space: nowrap; overflow: hidden; text-overflow: ellipsis`) is ALREADY present on `.hub-card__preview-line` (style.css:6020-6028). So the issue's suspected fix is already patched. This plan MUST first reproduce the stacking LIVE, then branch:
- reproduces → find the TRUE root cause (D-02) and land a render-layer fix + test.
- does NOT reproduce → close #127 as already-fixed with live evidence, no code change (D-04).

Purpose: prove the actual current rendering behavior rather than re-applying already-present CSS — honoring the standing lesson that a "looks-fixed" state can certify a broken feature; drive it live.
Output: live evidence + a conditional render-layer fix with vitest coverage OR a documented already-fixed closure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/176-platform-hardening-bug-fixes/176-CONTEXT.md
@.planning/phases/176-platform-hardening-bug-fixes/176-PATTERNS.md
@frontend/src/components/Hub/MiniPreview.tsx
@frontend/src/components/Hub/MiniPreview.test.tsx
@frontend/src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce (or disprove) the char-per-row stacking LIVE via a dev-browser component harness</name>
  <read_first>frontend/src/components/Hub/MiniPreview.tsx (per-styled-run span rendering lines 40-64), frontend/src/style.css (.hub-card__preview / .hub-card__preview-line lines 6012-6038), 176-CONTEXT.md (D-01/D-02), the [[reference_browser_uat_help_harness.md]] component-harness memory (mount the component alone in a temp vite html/entry — the Wails app shell will not render in plain Chrome; dev-browser 0.2.8 has no addInitScript and route.fetch hangs)</read_first>
  <files>frontend/src/components/Hub/MiniPreview.tsx</files>
  <action>Using the dev-browser skill, mount `MiniPreview` ALONE in a throwaway vite harness (temp html + entry under the scratchpad or an untracked `frontend/uat-176-*.{html,tsx}` mirroring the existing uat-162/uat-173 harnesses — do NOT commit these). Feed it a `StyledSpan[][]` derived from the long-line repro from #127 (`sh -c 'i=0; while true; do i=$((i+1)); printf "%s  live tick #%d\n" "$(date +%T)" "$i"; sleep 1; done'` output — long unbroken lines that overflow the card width). Render inside a container sized to the real `.hub-card__preview` width. Observe and CAPTURE: (a) a screenshot, and (b) computed styles (`display`, `flex-direction`, `white-space`) of `.hub-card__preview-line` AND of its child `<span>` runs. Do NOT change any code in this task. Determine the verdict: does each line render as ONE clipped horizontal row (correct), or does it stack one/two characters per row (BUG-07 reproduces)? If it stacks, per D-02 identify the TRUE mechanism (a global/descendant rule forcing `span { display: block }`, a `flex-direction: column` leaking onto `.hub-card__preview`/`.hub-card__preview-line`, or the styled-tail data itself being one-char-per-run) — the `nowrap` on the line div is already present, so the cause is elsewhere. Record the verdict + evidence in the plan SUMMARY.</action>
  <verify>
    <automated>test -f frontend/src/components/Hub/MiniPreview.tsx && git diff --exit-code frontend/src/components/Hub/MiniPreview.tsx</automated>
  </verify>
  <acceptance_criteria>
    - A dev-browser screenshot AND computed-style readout (display/flex-direction/white-space of the line div and its child spans) are captured and recorded in the SUMMARY.
    - A clear verdict is stated: REPRODUCES (with the identified true mechanism per D-02) or DOES-NOT-REPRODUCE.
    - No code change was made in this task (git diff on MiniPreview.tsx is empty at the end of Task 1).
  </acceptance_criteria>
  <done>The current-build mini-preview rendering of long lines is observed live, evidence captured, and a REPRODUCES / DOES-NOT-REPRODUCE verdict (with root-cause mechanism if reproduced) is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Branch on the verdict — land a render-layer fix + test, OR close #127 as already-fixed with evidence</name>
  <read_first>Task 1 verdict + evidence (from the SUMMARY), frontend/src/components/Hub/MiniPreview.tsx, frontend/src/components/Hub/MiniPreview.test.tsx (existing "StyledSpan[][] rendering" describe block lines 117-169; renderPreviewStyled helper), 176-CONTEXT.md (D-03/D-04), 176-PATTERNS.md ("MiniPreview.test.tsx — BUG-07 test extension" section)</read_first>
  <files>frontend/src/components/Hub/MiniPreview.tsx, frontend/src/components/Hub/MiniPreview.test.tsx</files>
  <action>BRANCH on the Task 1 verdict.
  BRANCH A — REPRODUCES: apply the minimal render-layer fix at the TRUE cause identified in Task 1 (D-02) so each line renders on ONE clipped horizontal row (D-03), NOT column-wrap. The fix target depends on the mechanism: if a `display:block`/`flex-direction:column` leak, correct the offending rule (in style.css or the offending span styling) so the child spans flow inline within the `nowrap` line div; if the styled-tail data is one-char-per-run, coalesce runs in MiniPreview.tsx. Do NOT re-add the already-present nowrap/overflow/ellipsis CSS (D-01). Then extend MiniPreview.test.tsx's existing "StyledSpan[][] rendering" describe block with a STRUCTURAL single-row assertion (jsdom has no layout, so assert DOM structure: e.g. `.hub-card__preview-line` count equals the input row count and child spans are not forced to `display:block`/`column`) — mirror the existing renderPreviewStyled pattern.
  BRANCH B — DOES-NOT-REPRODUCE: make NO code change (D-04). Record the live evidence (screenshot + computed styles from Task 1) as the closure justification for #127 (already-fixed — a prior hub-card layout effort likely landed the nowrap CSS). Note the colorblind-safety is unaffected (layout, not color); if any color span were touched it would be verified at source per [[user_colorblind]], but no color code is touched here.</action>
  <verify>
    <automated>cd frontend && ( git -C .. diff --quiet -- src/components/Hub/MiniPreview.tsx src/components/Hub/MiniPreview.test.tsx src/style.css && echo "BRANCH B: no code change — verify evidence recorded in SUMMARY" ) || ( pnpm exec tsc --noEmit && pnpm vitest run src/components/Hub/MiniPreview.test.tsx )</automated>
  </verify>
  <acceptance_criteria>
    - BRANCH A: `tsc --noEmit` clean AND `pnpm vitest run src/components/Hub/MiniPreview.test.tsx` passes including the new single-clipped-row structural assertion; no re-added nowrap/overflow/ellipsis CSS; no column-wrap introduced.
    - BRANCH B: zero diff across MiniPreview.tsx, MiniPreview.test.tsx, and style.css; the SUMMARY records the live already-fixed evidence and states #127 is closed as already-fixed.
    - Exactly one branch is taken, matching the Task 1 verdict.
  </acceptance_criteria>
  <done>Either a render-layer fix + passing structural vitest coverage lands (reproduced), or #127 is documented closed as already-fixed with live evidence and no code change (not reproduced).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| styled-tail data → MiniPreview DOM | Session output (already sanitized upstream by the styled-tail pipeline) is rendered as span text content via React (auto-escaped); no dangerouslySetInnerHTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-176-07-01 | Tampering (XSS) | MiniPreview span rendering | low | accept | Content is set as React text children (auto-escaped); colors resolved via resolveColor (not raw CSS injection). No new sink introduced — this is a layout fix. |
| T-176-07-SC | Tampering | package installs | n/a | accept | No npm/pip/cargo installs; conditional CSS/TSX render-layer change only, no new dependency. |

ASVS L1, block on high: no high-severity threat is introduced. This is a pure client-side layout fix (or no code change at all in Branch B); it adds no new input sink, network surface, or dependency. Gate satisfied.
</threat_model>

<verification>
- Task 1 is verified by the captured dev-browser evidence (screenshot + computed styles) and the recorded verdict — the live observation IS the acceptance evidence (per the standing "drive it live" lesson).
- Task 2 automated verify branches on whether a code change exists: BRANCH A runs `tsc --noEmit` + the MiniPreview vitest suite; BRANCH B asserts a zero diff (no code change) and relies on the recorded live evidence for the already-fixed closure.
- Throwaway harness files (uat-176-*.html/.tsx or scratchpad equivalents) are NOT committed (precedent: existing untracked uat-162/uat-173 harnesses).
</verification>

<success_criteria>
- The current-build behavior is proven live before any change.
- Reproduced → single clipped row per line + structural vitest coverage; the true root cause (not the already-present CSS) is fixed.
- Not reproduced → #127 closed as already-fixed with live evidence and zero code change.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 176-03)
- **Live evidence:** dev-browser screenshot + computed-style readout of MiniPreview long-line rendering (recorded in SUMMARY); the verdict REPRODUCES / DOES-NOT-REPRODUCE.
- **(conditional, BRANCH A only) render-layer fix:** minimal change in frontend/src/components/Hub/MiniPreview.tsx and/or frontend/src/style.css at the true root cause.
- **(conditional, BRANCH A only) new vitest test:** a single-clipped-row structural assertion in frontend/src/components/Hub/MiniPreview.test.tsx (existing file — extended in place, no new file, no Suite Manifest file-count change).
- **(BRANCH B) no code artifact** — already-fixed closure evidence only.
- **Downstream:** plan 176-04 adds a Section 4 traceability row for MiniPreview.test.tsx ONLY IF BRANCH A landed a fix; BRANCH B adds no test row (records closure evidence instead) per D-12.
</artifacts>

<output>
Create `.planning/phases/176-platform-hardening-bug-fixes/176-03-SUMMARY.md` when done — MUST state which branch (A/B) was taken and embed the live verdict + evidence.
</output>
