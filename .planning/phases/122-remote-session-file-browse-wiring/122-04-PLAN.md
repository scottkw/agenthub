---
phase: 122
plan: 04
type: execute
wave: 2
depends_on: ["122-01", "122-02"]
files_modified:
  - internal/tui/remote_files_client.go
  - internal/tui/remote_files_client_test.go
  - internal/tui/files.go
  - internal/tui/files_test.go
  - internal/tui/update.go
  - internal/tui/update_test.go
  - internal/tui/joincode_prompt.go
  - internal/tui/joincode_prompt_test.go
  - internal/tui/model.go
  - internal/tui/keys.go
  - internal/tui/help.go
autonomous: true
requirements: [REMOTE-03, REMOTE-04, REMOTE-05]
tags: [tui, files, remote, cap, joincode, prompt, modal]

must_haves:
  truths:
    - "Pressing `f` on a remote-session entry in the TUI Sessions list opens the Files view fetching from the remote webserver over HTTPS+cap (NOT the local Unix socket)."
    - "The previous 'File browser not available for remote sessions in v3.4' toast is GONE."
    - "If no cap is cached for a remote session, the TUI prompts for a join code in a modal text-input (mirroring the desktop GUI's paste-join-code modal flow)."
    - "Once cached, subsequent `f` presses for the same session reuse the cap with no prompt (per D-03)."
    - "TUI behavior is identical between local and remote sessions for nav (Up/Down/PgUp/PgDn), preview (text/markdown/binary/over-cap), filter (`/`), status line, glamour rendering."
    - "If the remote responds 401 to a file fetch, the TUI surfaces 'Remote session must be web-shared to browse files. Ask the owner to enable sharing.' in the status line and offers a re-prompt path."
  artifacts:
    - path: "internal/tui/remote_files_client.go"
      provides: "RemoteFilesClient struct implementing FilesClient via HTTPS + ?cap=<token>; TLS 1.2+; mirrors RESEARCH §Pattern 2"
      contains: "type RemoteFilesClient struct"
    - path: "internal/tui/joincode_prompt.go"
      provides: "Bubble Tea modal sub-model: textinput for join code + status/error display + Enter to submit"
      contains: "joinCodePromptModel"
    - path: "internal/tui/model.go"
      provides: "Model.remoteCapStore map[sessionID]remoteCapEntry + Model.joinCodePrompt joinCodePromptModel + modalJoinCodePrompt modalState"
      contains: "remoteCapStore"
    - path: "internal/tui/update.go"
      provides: "FilesOpen handler branches on entryLocal vs entryRemote; remote branch: if cap cached → open with RemoteFilesClient; else → open join-code prompt modal"
      contains: "case entryRemote:"
  key_links:
    - from: "internal/tui/update.go (FilesOpen / entryRemote branch)"
      to: "Model.remoteCapStore + (cap absent) joinCodePromptModel"
      via: "modalJoinCodePrompt overlay with the remote-session context attached"
      pattern: "remoteCapStore"
    - from: "joinCodePromptModel (on Enter submit)"
      to: "internal/tui/remote_files_client.go (cap exchange + register flow)"
      via: "tea.Cmd that POSTs to remote /join/exchange, parses cap, stores in remoteCapStore, then dispatches loadDirCmd"
      pattern: "exchangeJoinCodeCmd"
    - from: "filesModel (m.files.client) for remote sessions"
      to: "RemoteFilesClient (HTTPS + cap)"
      via: "newFilesModelWithClient invoked with a constructed RemoteFilesClient at FilesOpen time"
      pattern: "NewRemoteFilesClient"
---

<objective>
Wire the TUI's Files view to open against remote tailnet sessions via HTTPS+cap, with a Bubble Tea join-code prompt modal as the cap-acquisition flow (mirroring the desktop GUI's modal). Closes REMOTE-03, REMOTE-04, and the TUI half of REMOTE-05.

Purpose: The TUI uses a Go HTTP client directly — no browser, no CORS — so it does NOT need the daemon proxy. RESEARCH §Architectural Responsibility Map line "TUI cross-network file fetch | API / Backend (Go HTTP client) | Standard net/http over Tailscale TLS — no CORS, no browser involved" applies. The TUI talks DIRECTLY to the remote webserver's `/api/files/*` routes via the new `RemoteFilesClient`. The join-code prompt is the TUI's parity-with-GUI path for cap acquisition per locked decision D-01.

Output: New `internal/tui/remote_files_client.go` + `_test.go`; new `internal/tui/joincode_prompt.go` + `_test.go`; Model extensions for the in-memory remote cap store + the join-code prompt modal state; update.go FilesOpen handler branched for entryRemote; v3.4 toast removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/122-remote-session-file-browse-wiring/122-CONTEXT.md
@.planning/phases/122-remote-session-file-browse-wiring/122-RESEARCH.md
@.planning/phases/121-tui-files-view/121-01-SUMMARY.md
@.planning/phases/121-tui-files-view/121-02-SUMMARY.md

@internal/tui/model.go
@internal/tui/update.go
@internal/tui/files.go
@internal/tui/files_cmds.go
@internal/tui/files_client.go
@internal/daemon/client.go
@internal/tailnet/sessions.go
@internal/webserver/server.go

<interfaces>
<!-- The contracts this plan consumes. -->

From Plan 02 (Wave 1):
- `FilesClient` interface in `internal/tui/files_client.go` with the four methods
- `filesModel.client FilesClient` field
- `newFilesModelWithClient(sid, client, ...)` constructor
- Cmd factories (`loadDirCmd`, `readFileCmd`, `headFileCmd`) take `FilesClient`

From Plan 01 (Wave 1):
- Remote webserver shape: `https://{fqdn}:{port}/api/files/{list,stat,read}?session=<sid>&path=<rel>&cap=<token>` returns the same `files.FileEntry` JSON shape as the local daemon
- Remote webserver shape: `POST https://{fqdn}:{port}/join/exchange` with form body `code=<code>` returns 303 to `/sessions/<sid>?cap=<token>` (the cap can be extracted from the Location header's query string)

From `internal/tui/model.go` (existing — extend, don't rewrite):
- `Model.modal modalState` — existing modal state machine; we add a new constant `modalJoinCodePrompt`
- `Model.files filesModel` — already carries `client FilesClient` after Plan 02
- `Model.unifiedList []listEntry` + `entryRemote` discriminator + `entry.remote *RemoteSessionEntry`
- `RemoteSessionEntry.URL string` — shape `https://{fqdn}:{port}/sessions/{id}` (drop `/sessions/{id}` to get the base URL)

From `internal/tui/keys.go`:
- `KeyMap` struct with `FilesOpen`, `PrevTab`, `NextTab`, etc. — modal-mode key handling cascades through `m.modal`-switch in update.go.

From `internal/tui/files_cmds.go` (Phase 121 invariants — PRESERVE):
- Every dispatch uses `gen = m.files.generation` so stale messages are discarded (T-121-04 / WR-03)
- `errNilClient` returned when a Cmd is dispatched with a nil client (must still work for `RemoteFilesClient(nil)`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RemoteFilesClient — HTTPS+cap implementation of FilesClient</name>
  <files>internal/tui/remote_files_client.go, internal/tui/remote_files_client_test.go</files>
  <read_first>
    @internal/tui/files_client.go (the FilesClient interface — RemoteFilesClient must satisfy it exactly)
    @internal/daemon/client.go (lines 362-484 — DaemonClient files methods — pattern source per RESEARCH §Pattern 2)
    @internal/tailnet/sessions.go (lines 38-72 — TLS 1.2+ HTTPS client pattern — copy verbatim)
    @internal/files/types.go (FileEntry + FileListResponse JSON shape)
  </read_first>
  <behavior>
    - Test 1: `NewRemoteFilesClient(baseURL, capToken)` returns a non-nil client; trailing slash on baseURL is stripped.
    - Test 2: `client.ListFiles(ctx, "sid", "rel")` against an `httptest.NewTLSServer` that serves `/api/files/list?session=sid&path=rel&cap=<token>` with the canonical FileListResponse JSON returns `(entries, truncated, nil)` matching the upstream payload.
    - Test 3: `client.StatFile(ctx, "sid", "rel")` round-trips the FileEntry shape (Test 2 mirror).
    - Test 4: `client.ReadFile(ctx, "sid", "rel")` returns `(data []byte, mime string, err error)` matching upstream Content-Type + body.
    - Test 5: `client.HeadFile(ctx, "sid", "rel")` returns `(size int64, mime string, mtime time.Time, err error)` parsing Content-Length + Content-Type + Last-Modified (mirror DaemonClient.HeadFile lines 454-484).
    - Test 6: Upstream 401 → error string contains `"401"` AND does NOT contain the cap token (T-122-CAPLEAK).
    - Test 7: Upstream 403 → error string contains `"403"`.
    - Test 8: Upstream 413 (over-cap) → error string contains `"413"` (preserves Phase 121 "Too large to preview" classification path).
    - Test 9: ctx cancelled mid-request → returns wrapped `context.Canceled` (preserves the Phase 121 generation-counter guard semantics — stale message via timeout is the same kind of error envelope).
    - Test 10: URL construction uses `net/url.Values{}.Encode()` (assert via source-grep — RESEARCH §Don't Hand-Roll).
    - Test 11: Empty `relPath` is normalized to `"."` before URL encoding (mirror DaemonClient.filesURL line 367-368).
    - Test 12: `var _ FilesClient = (*RemoteFilesClient)(nil)` compile-time guard present in the source file.
    - Test 13: TLS config uses `MinVersion: tls.VersionTLS12` (source-grep).
  </behavior>
  <action>
    Create `internal/tui/remote_files_client.go`:

    ```
    package tui

    import (
        "context"
        "crypto/tls"
        "encoding/json"
        "fmt"
        "io"
        "net/http"
        "net/url"
        "strings"
        "time"

        "github.com/scottkw/agenthub/internal/files"
    )

    // RemoteFilesClient satisfies FilesClient by talking to a remote AgentHub
    // peer's webserver over Tailscale HTTPS with a session-scoped cap token in
    // the query string. Mirror of *daemon.DaemonClient's files methods, but
    // pointed at https://{peer}:{port} instead of the local socket. Phase 122
    // / REMOTE-03.
    type RemoteFilesClient struct {
        baseURL  string
        capToken string
        http     *http.Client
    }

    // Compile-time guard: *RemoteFilesClient satisfies FilesClient.
    var _ FilesClient = (*RemoteFilesClient)(nil)

    // NewRemoteFilesClient constructs a client targeting baseURL with the given
    // cap token. baseURL of shape "https://{fqdn}:{port}" (no trailing path).
    // TLS 1.2+ enforced; pattern from internal/tailnet/sessions.go.
    func NewRemoteFilesClient(baseURL, capToken string) *RemoteFilesClient {
        return &RemoteFilesClient{
            baseURL:  strings.TrimRight(baseURL, "/"),
            capToken: capToken,
            http: &http.Client{
                Timeout: 15 * time.Second,  // longer than DaemonClient (5s) — tailnet RTT per RESEARCH Pitfall 7
                Transport: &http.Transport{
                    TLSClientConfig: &tls.Config{MinVersion: tls.VersionTLS12},
                },
            },
        }
    }

    func (c *RemoteFilesClient) filesURL(op, sessionID, relPath string) string {
        if relPath == "" {
            relPath = "."
        }
        q := url.Values{}
        q.Set("session", sessionID)
        q.Set("path", relPath)
        if c.capToken != "" {
            q.Set("cap", c.capToken)
        }
        return c.baseURL + "/api/files/" + op + "?" + q.Encode()
    }

    // ListFiles fetches a directory listing over HTTPS+cap.
    func (c *RemoteFilesClient) ListFiles(ctx context.Context, sid, rel string) ([]files.FileEntry, bool, error) {
        req, err := http.NewRequestWithContext(ctx, http.MethodGet, c.filesURL("list", sid, rel), nil)
        if err != nil {
            return nil, false, fmt.Errorf("remote files list: %w", err)
        }
        resp, err := c.http.Do(req)
        if err != nil {
            return nil, false, fmt.Errorf("remote files list: %w", err)
        }
        defer resp.Body.Close()
        if resp.StatusCode != http.StatusOK {
            body, _ := io.ReadAll(resp.Body)
            return nil, false, fmt.Errorf("remote files list: %d %s", resp.StatusCode, strings.TrimSpace(string(body)))
        }
        var out files.FileListResponse
        if err := json.NewDecoder(resp.Body).Decode(&out); err != nil {
            return nil, false, fmt.Errorf("remote files list: decode: %w", err)
        }
        return out.Entries, out.Truncated, nil
    }

    // StatFile / ReadFile / HeadFile follow the same pattern. Mirror
    // internal/daemon/client.go:402-484 substituting baseURL + cap path.
    // [Executor implements analogously — three short methods.]
    ```

    Implement `StatFile`, `ReadFile`, `HeadFile` mirroring `internal/daemon/client.go` lines 402-484 exactly with the HTTPS-base substitution. **CRITICAL**: every `fmt.Errorf` that interpolates the request URL or body MUST exclude the cap token. Use `%d %s` on (status, body) — body from a 5xx remote server should not contain the cap, but defense-in-depth: if any error path needs to mention the URL, strip the `cap=` query param first via `redactCapFromURL(u string) string` (small helper inside this file).

    Test approach in `remote_files_client_test.go`:
    - Use `httptest.NewTLSServer` exposing `/api/files/list`, `/stat`, `/read` (GET + HEAD). Hard-code responses keyed by `cap` query param.
    - Use `srv.Client()` as the test's HTTP client — BUT note that `RemoteFilesClient` constructs its OWN client internally. Either: (a) add a test-only constructor `newRemoteFilesClientWithHTTP(baseURL, cap, *http.Client)` accepting an injected client, OR (b) expose `client.http` as a package-internal field so the test can replace it post-construction. Use (a) — additive surface, no field leak.
    - Each test sets up the fixture server, points the client at `srv.URL`, exercises the method, asserts the response shape.
  </action>
  <verify>
    <automated>go test ./internal/tui/ -run 'TestRemoteFilesClient_' -race -count=1</automated>
  </verify>
  <done>
    `RemoteFilesClient` satisfies `FilesClient` (compile-time + 13 unit tests); cap-token never leaks into error strings; TLS 1.2+ enforced; URL construction via `url.Values{}.Encode()`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: joinCodePromptModel + Model state + exchange-and-cache Cmd</name>
  <files>internal/tui/joincode_prompt.go, internal/tui/joincode_prompt_test.go, internal/tui/model.go, internal/tui/keys.go</files>
  <read_first>
    @internal/tui/model.go (around lines 124-145 — existing modal state pattern: `modal modalState`, `editing` rename modal, kill confirmation; we add a third modal kind)
    @internal/tui/update.go (the modal-mode key dispatch — find the `switch m.modal { case modal...:` block; we add a `modalJoinCodePrompt` arm)
    @internal/webserver/server.go (lines 642-706 — handleJoinExchange shape; we mirror the form-POST + 303 parse on the client side)
    @internal/tui/files.go (the textinput pattern — `m.files.filterInput textinput.Model` is the model for a text input embedded in a Bubble Tea sub-model)
  </read_first>
  <behavior>
    - Test 1: `newJoinCodePromptModel(sessionID, sessionName, hostname, remoteBaseURL)` constructs a model with a focused, empty `textinput.Model`.
    - Test 2: Typing keys appends to the textinput value (verified by feeding `tea.KeyPressMsg{Code: 'A'}` and inspecting `.input.Value()`).
    - Test 3: Pressing Enter on a non-empty input returns a `tea.Cmd` that, when run, posts the code to `${remoteBaseURL}/join/exchange` and parses the 303 Location header for `?cap=...`.
    - Test 4: Pressing Enter on an empty input is a no-op (no Cmd dispatched, no state change).
    - Test 5: Pressing Esc returns a `cancelJoinCodeMsg` signaling the modal should be closed without further action.
    - Test 6: `exchangeJoinCodeCmd(remoteBaseURL, code)` returns a `joinCodeResultMsg{cap, err}`:
      - Upstream 303 with Location `/sessions/<sid>?cap=<token>` → cap = token, err = nil.
      - Upstream 303 with Location `/join?error=expired` → err includes "expired".
      - Upstream 303 with Location `/join?error=invalid` → err includes "invalid".
      - Upstream 303 with Location `/join?error=session-gone` → err includes "session-gone".
      - Any non-303 response → err includes the status code.
    - Test 7: When the cap-exchange completes successfully, the joinCodePromptModel transitions to a "submitting" state showing "Joining…" briefly before the parent FilesOpen flow takes over (state machine: idle → submitting → resolved/error).
    - Test 8: Render output includes the session name + hostname in the prompt body (parity with the desktop modal's contextual copy).
  </behavior>
  <action>
    ### internal/tui/model.go additions

    Add a new modal constant:
    ```
    const (
        modalNone modalState = iota
        modalKillConfirm
        modalNewSession
        modalEditName
        modalQR
        modalJoinCodePrompt  // NEW — Phase 122
    )
    ```

    Add to `Model` struct:
    ```
    // Phase 122 — Remote files cap acquisition.
    remoteCapStore   map[string]remoteCapEntry   // sessionID -> (baseURL, capToken)
    joinCodePrompt   joinCodePromptModel         // textinput-driven cap exchange flow
    ```

    Add the helper type alongside Model:
    ```
    type remoteCapEntry struct {
        baseURL  string
        capToken string
    }
    ```

    Initialize `remoteCapStore` in the model constructor (or zero-value-map check at lookup time).

    ### internal/tui/joincode_prompt.go (NEW)

    ```
    type joinCodePromptState int

    const (
        joinCodePromptIdle joinCodePromptState = iota
        joinCodePromptSubmitting
        joinCodePromptError
    )

    type joinCodePromptModel struct {
        sessionID     string
        sessionName   string
        hostname      string
        remoteBaseURL string  // e.g. "https://hub-a.tailnet.ts.net:9443"
        input         textinput.Model
        state         joinCodePromptState
        errMsg        string
    }

    func newJoinCodePromptModel(sessionID, sessionName, hostname, remoteBaseURL string) joinCodePromptModel
    func (m joinCodePromptModel) Update(msg tea.Msg) (joinCodePromptModel, tea.Cmd)
    func (m joinCodePromptModel) View(w, h int) string

    // exchangeJoinCodeCmd POSTs to ${remoteBaseURL}/join/exchange with
    // form-url-encoded `code=<code>`, configures CheckRedirect to return
    // http.ErrUseLastResponse, parses the resulting 303 Location header for
    // ?cap=<token>, and returns joinCodeResultMsg{cap, err}. Phase 122.
    func exchangeJoinCodeCmd(remoteBaseURL, code string) tea.Cmd

    type joinCodeResultMsg struct {
        sessionID string  // echo for staleness guard
        cap       string
        err       error
    }

    type cancelJoinCodeMsg struct{}
    ```

    The `Update` method:
    - Esc → returns `m, func() tea.Msg { return cancelJoinCodeMsg{} }`.
    - Enter on empty → returns `m, nil` (no-op).
    - Enter on non-empty → sets `m.state = joinCodePromptSubmitting`; returns `m, exchangeJoinCodeCmd(m.remoteBaseURL, m.input.Value())`.
    - Any other key → routes to `m.input.Update(msg)` and returns the resulting model.

    The `View` renders:
    - A bordered lipgloss box (mirror the existing modals' visual style).
    - Title: "Join Remote Session — Files"
    - Body: `fmt.Sprintf("Ask the owner of %q on %q for the 5-character join code.\n(Owner generates it via Daemon Manager.) Paste it below.", m.sessionName, m.hostname)`
    - The textinput rendered via `m.input.View()` with a `>` prompt prefix.
    - If `m.state == joinCodePromptSubmitting`: append "Joining…" line below the input.
    - If `m.state == joinCodePromptError`: append the friendly error message (translate per Plan 03 Task 2 mapping — "expired" → "Code expired. Ask the owner to generate a new code."; "invalid" / "not-found" → "Code invalid. Double-check the 5-character code."; "session-gone" → "Remote session is no longer web-shared.").
    - Hint line: "Enter Submit  Esc Cancel"

    The `exchangeJoinCodeCmd`:
    ```
    func exchangeJoinCodeCmd(remoteBaseURL, code string) tea.Cmd {
        return func() tea.Msg {
            client := &http.Client{
                Timeout: 10 * time.Second,
                Transport: &http.Transport{
                    TLSClientConfig: &tls.Config{MinVersion: tls.VersionTLS12},
                },
                CheckRedirect: func(req *http.Request, via []*http.Request) error {
                    return http.ErrUseLastResponse
                },
            }
            form := url.Values{"code": {code}}
            req, _ := http.NewRequest(http.MethodPost, remoteBaseURL+"/join/exchange", strings.NewReader(form.Encode()))
            req.Header.Set("Content-Type", "application/x-www-form-urlencoded")
            resp, err := client.Do(req)
            if err != nil {
                return joinCodeResultMsg{err: fmt.Errorf("join exchange: %w", err)}
            }
            defer resp.Body.Close()
            if resp.StatusCode != http.StatusSeeOther {
                return joinCodeResultMsg{err: fmt.Errorf("join exchange: unexpected status %d", resp.StatusCode)}
            }
            loc := resp.Header.Get("Location")
            if strings.Contains(loc, "/join?error=") {
                return joinCodeResultMsg{err: fmt.Errorf("join exchange: %s", strings.TrimPrefix(loc, "/join?error="))}
            }
            u, err := url.Parse(loc)
            if err != nil {
                return joinCodeResultMsg{err: fmt.Errorf("join exchange: bad location %q", loc)}
            }
            cap := u.Query().Get("cap")
            if cap == "" {
                return joinCodeResultMsg{err: fmt.Errorf("join exchange: no cap in location %q", loc)}
            }
            return joinCodeResultMsg{cap: cap}
        }
    }
    ```

    NOTE: `joinCodeResultMsg.sessionID` must be populated. Since `exchangeJoinCodeCmd` only takes `(remoteBaseURL, code)`, the sessionID echo must be wrapped by the caller. Adjust the signature: `exchangeJoinCodeCmd(sessionID, remoteBaseURL, code string) tea.Cmd` and stamp `sessionID` on the result.

    ### internal/tui/joincode_prompt_test.go

    Cover behaviors 1-8. Use `httptest.NewServer` (NOT TLS — the test exchanges against an HTTP server with appropriate Location headers; the redirect-following / form-POST logic is what's being tested, not the TLS handshake which Task 1 already covers).
  </action>
  <verify>
    <automated>go test ./internal/tui/ -run 'TestJoinCodePrompt_|TestExchangeJoinCodeCmd_' -race -count=1</automated>
  </verify>
  <done>
    `joinCodePromptModel` exists with Update/View; `exchangeJoinCodeCmd` parses 303 Location headers correctly across the four outcome cases; `Model.remoteCapStore` and `Model.joinCodePrompt` fields exist; `modalJoinCodePrompt` constant added; eight tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: update.go FilesOpen remote branch + modal dispatch + cap-cached fast path + remove v3.4 toast</name>
  <files>internal/tui/update.go, internal/tui/update_test.go, internal/tui/files_test.go, internal/tui/help.go</files>
  <read_first>
    @internal/tui/update.go (lines 381-411 — the existing FilesOpen handler; lines around the modal-switch dispatcher higher in the function)
    @internal/tui/files_cmds.go (loadDirCmd signature confirmed by Plan 02 — takes FilesClient)
    @internal/tui/help.go (the help overlay; we add a small mention of the join-code prompt in the Files-tab help group)
  </read_first>
  <behavior>
    - Test 1: Pressing `f` on a remote-session entry WITH a cap already in `m.remoteCapStore[sid]` → opens tabFiles directly with a `RemoteFilesClient(baseURL, cap)`. No modal opens. `loadDirCmd` dispatched against the remote client.
    - Test 2: Pressing `f` on a remote-session entry WITHOUT a cap in the store → opens `modalJoinCodePrompt` with the session context populated.
    - Test 3: In `modalJoinCodePrompt` mode, typing keys routes to `m.joinCodePrompt.input` (NOT to the underlying tab's `handleFilesKey` — modal mode preempts).
    - Test 4: In `modalJoinCodePrompt`, Esc closes the modal without opening a tab (`m.modal = modalNone`).
    - Test 5: In `modalJoinCodePrompt`, on successful `joinCodeResultMsg{cap: "xyz"}`: store the cap in `m.remoteCapStore[sid]`, close the modal, build a `RemoteFilesClient`, open tabFiles, dispatch `loadDirCmd`.
    - Test 6: In `modalJoinCodePrompt`, on `joinCodeResultMsg{err: ...}`: set `m.joinCodePrompt.state = joinCodePromptError` and `m.joinCodePrompt.errMsg = friendlyMessage(err)`. Modal stays open so the user can retry.
    - Test 7: When tabFiles is open against a remote session and the first `loadDirCmd` returns an error containing "401" (cap rejected upstream): the status line shows "Remote session must be web-shared to browse files. Ask the owner to enable sharing." AND `m.remoteCapStore[sid]` is DELETED (forces re-prompt on next `f`).
    - Test 8: Source-grep: `grep -c "File browser not available for remote sessions" internal/tui/update.go` == 0 (the v3.4 toast is gone).
    - Test 9: All existing Phase 121 update_test tests pass unchanged (local-session path preserved bit-for-bit).
  </behavior>
  <action>
    ### Helper: derive remote base URL

    Add (in `update.go` or a new small helper file `internal/tui/remote_helpers.go`):

    ```
    // remoteBaseURLFromSessionURL strips the /sessions/{id} suffix off a
    // RemoteSessionEntry.URL of shape "https://{fqdn}:{port}/sessions/{id}",
    // returning the bare https://{fqdn}:{port} base. Defensive: uses url.Parse
    // so query strings and ports are preserved correctly. Phase 122 RESEARCH
    // §Pattern 3 / §Don't Hand-Roll.
    func remoteBaseURLFromSessionURL(rawURL string) string {
        u, err := url.Parse(rawURL)
        if err != nil {
            return ""
        }
        return u.Scheme + "://" + u.Host
    }
    ```

    ### update.go FilesOpen handler replacement

    Replace the entire `case key.Matches(msg, m.keys.FilesOpen):` block (lines 381-411) with:

    ```
    case key.Matches(msg, m.keys.FilesOpen):
        if len(m.unifiedList) == 0 {
            return m, nil
        }
        entry := m.unifiedList[m.selected]

        // Compute dimensions once — both branches use them.
        cw := m.contentWidth()
        listW := cw * 40 / 100
        previewW := cw - listW - 1
        if previewW < 1 { previewW = 1 }
        paneH := m.height - 3
        if paneH < 1 { paneH = 1 }

        switch entry.kind {
        case entryLocal:
            if entry.session == nil { return m, nil }
            sid := entry.session.ID
            m.files = newFilesModelWithClient(sid, m.client, listW, paneH-2, previewW, paneH-2)
            m.openTab(tabFiles)
            return m, loadDirCmd(m.files.client, sid, ".", m.files.generation)

        case entryRemote:
            if entry.remote == nil { return m, nil }
            sid := entry.remote.ID

            // Cap-cached fast path — REMOTE-03 + D-03.
            if cached, ok := m.remoteCapStore[sid]; ok {
                client := NewRemoteFilesClient(cached.baseURL, cached.capToken)
                m.files = newFilesModelWithClient(sid, client, listW, paneH-2, previewW, paneH-2)
                m.openTab(tabFiles)
                return m, loadDirCmd(m.files.client, sid, ".", m.files.generation)
            }

            // Cap not cached — open join-code prompt modal (D-01 parity with GUI).
            baseURL := remoteBaseURLFromSessionURL(entry.remote.URL)
            if baseURL == "" {
                m.toast = "Cannot parse remote session URL"
                m.toastKind = toastError
                m.toastExp = time.Now().Add(3 * time.Second)
                return m, nil
            }
            m.joinCodePrompt = newJoinCodePromptModel(sid, entry.remote.Name, entry.remote.Hostname, baseURL)
            m.modal = modalJoinCodePrompt
            return m, nil
        }
        return m, nil
    ```

    ### Modal dispatch in Update

    Find the existing modal-switch in `update.go` (where `modalKillConfirm`, `modalNewSession`, etc. are handled). Add a new arm:

    ```
    case modalJoinCodePrompt:
        // Route keypress + joinCodeResultMsg + cancelJoinCodeMsg into the prompt sub-model.
        switch msg := msg.(type) {
        case tea.KeyPressMsg:
            var cmd tea.Cmd
            m.joinCodePrompt, cmd = m.joinCodePrompt.Update(msg)
            return m, cmd
        case joinCodeResultMsg:
            if msg.err != nil {
                m.joinCodePrompt.state = joinCodePromptError
                m.joinCodePrompt.errMsg = friendlyJoinCodeError(msg.err)
                return m, nil
            }
            // Success: store cap, close modal, open tabFiles with RemoteFilesClient.
            sid := m.joinCodePrompt.sessionID
            baseURL := m.joinCodePrompt.remoteBaseURL
            cap := msg.cap
            if m.remoteCapStore == nil {
                m.remoteCapStore = make(map[string]remoteCapEntry)
            }
            m.remoteCapStore[sid] = remoteCapEntry{baseURL: baseURL, capToken: cap}
            m.modal = modalNone

            // Recompute dimensions (window may have resized while modal was open).
            cw := m.contentWidth()
            listW := cw * 40 / 100
            previewW := cw - listW - 1
            if previewW < 1 { previewW = 1 }
            paneH := m.height - 3
            if paneH < 1 { paneH = 1 }

            client := NewRemoteFilesClient(baseURL, cap)
            m.files = newFilesModelWithClient(sid, client, listW, paneH-2, previewW, paneH-2)
            m.openTab(tabFiles)
            return m, loadDirCmd(m.files.client, sid, ".", m.files.generation)
        case cancelJoinCodeMsg:
            m.modal = modalNone
            return m, nil
        }
        return m, nil
    ```

    ### 401-upstream → forget cap + show "must be web-shared" message

    In `applyFilesListMsg` (and `applyFilesHeadMsg` / `applyFilesReadMsg`) inside `internal/tui/files.go`, extend the error-handling branch to detect `strings.Contains(err.Error(), "401")`. When detected AND the active client is a `*RemoteFilesClient` (use a type assertion on `m.files.client`):

    ```
    if rc, isRemote := m.files.client.(*RemoteFilesClient); isRemote && strings.Contains(msg.err.Error(), "401") {
        // Forget the cap — it has been rejected upstream. User must re-join.
        delete(m.remoteCapStore, msg.sessionID)
        m.files.err = errors.New("Remote session must be web-shared to browse files. Ask the owner to enable sharing.")
        _ = rc  // explicit no-use marker
        return m, nil
    }
    ```

    Note: the helper to detect "the active client is remote" lives more cleanly as a method on filesModel: `func (m filesModel) isRemoteClient() bool { _, ok := m.client.(*RemoteFilesClient); return ok }`. Use whichever is cleaner.

    ### help.go addition

    In the `tabFiles` help group, add (after the existing entries):
    ```
    f       Re-prompt for join code (after rejection)
    ```

    Actually — `f` is already taken to open the tab from Sessions; in tabFiles mode it doesn't currently do anything. Use a different key for "re-prompt": cleanest is to NOT bind a new key — instead, instruct the user to close the tab and press `f` again from Sessions, which will see the missing cap and re-prompt. Skip the help addition (delete this paragraph if the executor agrees) — the `errMsg` in the status line tells the user what to do.

    ### Remove v3.4 toast

    The replacement code above naturally removes the toast — verify by source-grep gate.
  </action>
  <verify>
    <automated>go test ./internal/tui/ -race -count=1</automated>
  </verify>
  <done>
    All nine tests pass; `grep -c "File browser not available for remote sessions" internal/tui/update.go` == 0; full Phase 121 test suite green; remote-session FilesOpen path opens either the modal (no cap) or a tabFiles against RemoteFilesClient (cap cached); 401 on remote forgets the cap.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI process → Local daemon (Unix socket / named pipe) | Trusted; existing `m.client *daemon.DaemonClient` continues to handle non-files ops |
| TUI process → Remote webserver (Tailscale HTTPS) | Untrusted transport; mutual auth via Tailscale TLS + cap token in URL query |
| Remote cap token | Lives ONLY in `Model.remoteCapStore` (in-process map); never written to disk; wiped on TUI exit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-04-01 | Information disclosure | Cap token leaking into error strings | mitigate | Task 1 Test 6 + `redactCapFromURL` helper; all `fmt.Errorf` paths use `%d %s` on (status, body) NOT the full URL. |
| T-122-04-02 | Information disclosure | Cap persisted to disk via Bubble Tea state-save | mitigate | TUI has no state-persistence layer (no `tea.WithState`-equivalent in use); `remoteCapStore` lives only on the Model struct, which is reconstructed on every TUI launch. Source-grep guard: `grep -c "os.WriteFile\\|ioutil.WriteFile" internal/tui/joincode_prompt.go internal/tui/remote_files_client.go` must be 0. |
| T-122-04-03 | Tampering | MITM on Tailscale HTTPS to remote peer | mitigate | TLS 1.2+ via `tls.Config{MinVersion: tls.VersionTLS12}` (Task 1 Test 13). Tailscale provides transport-layer mutual auth + pinned certs. |
| T-122-04-04 | Information disclosure | Join code typed into modal visible on shoulder-surf | accept | Join codes are intentionally 5-character ephemeral codes (5-minute TTL via `JoinCodeManager`); displaying them in plain text in the textinput is consistent with the desktop GUI modal and the existing `/join` browser flow. Threat model unchanged from Phase 87. |
| T-122-04-05 | Spoofing | Wrong cap stored under wrong sessionID | mitigate | `Model.remoteCapStore[sid]` keyed by the SAME sid that was on the unifiedList entry that triggered the prompt; the modal's session context is captured at open time, not at submit time, so the key cannot drift. If the join code was issued for a DIFFERENT session than the user is browsing, the proxy returns 403 on first request and the existing PermissionDenied surface lights up. |
| T-122-04-06 | Denial of service | Held-open HTTP request from a malicious remote | mitigate | `RemoteFilesClient.http.Timeout = 15 * time.Second` (Task 1, slightly longer than DaemonClient per RESEARCH Pitfall 7). Per-request ctx timeout in Cmd factories (5s for list/head, 10s for read) provides defense-in-depth. |
| T-122-04-07 | Tampering | Replay of expired cap | mitigate | Already mitigated by Phase 119 `requireCapability` middleware on the remote side (HMAC + TTL). TUI surfaces 401 → forgets cap → re-prompts. No new mitigation needed. |
| T-122-SC | Tampering | No new packages installed | mitigate | RESEARCH §Package Legitimacy Audit confirms — Phase 122 uses zero new Go packages. |
</threat_model>

<verification>
## Per-task automated

```
go test ./internal/tui/ -race -count=1
go vet ./internal/tui/...
go build -tags wailsassets ./...
```

## Whole-plan grep gates

| Check | Expected | Command |
|-------|----------|---------|
| v3.4 toast removed | 0 | `grep -c 'File browser not available for remote sessions' internal/tui/update.go` |
| RemoteFilesClient satisfies FilesClient (compile-time) | 1 | `grep -c 'var _ FilesClient = (\\*RemoteFilesClient)(nil)' internal/tui/remote_files_client.go` |
| TLS 1.2 minimum on RemoteFilesClient | 1 | `grep -c 'MinVersion: tls.VersionTLS12' internal/tui/remote_files_client.go` |
| TLS 1.2 minimum on joincode exchange | 1 | `grep -c 'MinVersion: tls.VersionTLS12' internal/tui/joincode_prompt.go` |
| modalJoinCodePrompt constant defined | 1 | `grep -c 'modalJoinCodePrompt' internal/tui/model.go` |
| remoteCapStore field on Model | 1 | `grep -c 'remoteCapStore.*map\\[string\\]remoteCapEntry' internal/tui/model.go` |
| No disk writes in joincode_prompt.go | 0 | `grep -v '^[[:space:]]*//' internal/tui/joincode_prompt.go \| grep -cE 'os\\.WriteFile\|ioutil\\.WriteFile\|os\\.Create'` |
| FilesOpen entryRemote branch | 1 | `grep -c 'case entryRemote:' internal/tui/update.go` |
| 'Remote session must be web-shared' verbatim | ≥1 | `grep -c 'Remote session must be web-shared to browse files' internal/tui/files.go` |
| url.Values.Encode used (not fmt.Sprintf for URL) | ≥1 | `grep -c 'url.Values' internal/tui/remote_files_client.go` |

## Regression

```
go test ./internal/tui/ ./internal/daemon/ ./internal/webserver/ ./internal/files/ -race -count=1
```

Phase 121 tests must remain 100% green. The local-session FilesOpen branch must produce bit-for-bit identical behavior (Task 3 Test 9).
</verification>

<success_criteria>
1. `RemoteFilesClient` satisfies `FilesClient` (compile-time guard) and round-trips ListFiles/StatFile/ReadFile/HeadFile against an `httptest.NewTLSServer` fixture.
2. `joinCodePromptModel` exists; Enter submits via `exchangeJoinCodeCmd`; Esc cancels; success populates `remoteCapStore` and opens tabFiles with the remote client.
3. `update.go` FilesOpen handler branches on entryLocal vs entryRemote; remote with cached cap takes the fast path; remote without cap opens `modalJoinCodePrompt`.
4. v3.4 toast is gone (source-grep gate).
5. 401 from remote → cap forgotten + "Remote session must be web-shared..." status (matches D-04 verbatim copy).
6. ALL Phase 121 tests pass unchanged; local-session behavior bit-for-bit preserved.
7. All grep gates above return expected counts.
8. `go build -tags wailsassets ./...` clean; `go vet ./internal/tui/...` clean.
</success_criteria>

<output>
Create `.planning/phases/122-remote-session-file-browse-wiring/122-04-SUMMARY.md` when done.
</output>
