---
phase: 168-bug-fix-settings-polish
plan: 07
type: execute
wave: 5
depends_on: [168-01, 168-02, 168-03, 168-04, 168-05, 168-06]
files_modified:
  - TESTING.md
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03, FIX-04, UX-01, UX-02]
must_haves:
  truths:
    - "Every new automated test file added in plans 01-06 is registered in TESTING.md Suite Manifest (§2) and the Requirement→Test Traceability map (§4) with a repo-relative path only."
    - "TESTING.md Category G / M-13 is REWORDED to describe FIX-03's new in-app-tab behavior (not the retired external-browser flow)."
    - "New manual M-NN items exist for the FIX-02 live two-viewer smoke test and the FIX-01 live browser-console CSP/hot-swap check."
    - "bash tests/check-traceability-paths.sh passes."
  artifacts:
    - "TESTING.md updated Suite Manifest + Traceability map + Manual Regression Checklist (Category G reword + new M-NN items)"
  key_links:
    - "Traceability §4 path column is a repo-relative .go/.ts/.tsx/.sh file path only — no test names in the path column (project standing convention)."
  prohibitions:
    - "MUST NOT add a NEW M-NN for FIX-03 — M-13 (Category G) is REWORDED in place (RESEARCH Pitfall 4)."
    - "MUST NOT put a test name in the Traceability §4 path column (check-traceability-paths.sh will fail)."
    - "MUST NOT blind-un-skip frontend/e2e/web-plugin-hot-swap.spec.ts — record it as a documented adaptation/reference item, not a re-enabled gate (RESEARCH Pitfall 5)."
---

<objective>
Wire the phase's new tests and manual checks into TESTING.md per the repo standing convention,
so the regression suite stays canonical (Suite Manifest + Traceability map) and the manual
checklist reflects the new in-app-tab and multi-viewer behaviors.

Purpose: The standing rule (./CLAUDE.md + TESTING.md §6) requires every phase that adds/renames
tests to update the Suite Manifest, Traceability map, and Manual checklist, and to pass
check-traceability-paths.sh. This plan is isolated so TESTING.md (a file every implementation
plan would otherwise contend on) is touched exactly once, after all test files exist.
Output: An updated TESTING.md with all new test files registered, M-13 reworded, and new manual
M-NN items, verified by the traceability path checker.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-bug-fix-settings-polish/168-RESEARCH.md
@.planning/phases/168-bug-fix-settings-polish/168-01-SUMMARY.md
@.planning/phases/168-bug-fix-settings-polish/168-02-SUMMARY.md
@.planning/phases/168-bug-fix-settings-polish/168-03-SUMMARY.md
@.planning/phases/168-bug-fix-settings-polish/168-04-SUMMARY.md
@.planning/phases/168-bug-fix-settings-polish/168-05-SUMMARY.md
@.planning/phases/168-bug-fix-settings-polish/168-06-SUMMARY.md
@TESTING.md
</context>

<artifacts_produced>
- TESTING.md §2 Suite Manifest: rows for every new test file from plans 01-06
- TESTING.md §4 Traceability map: FIX-01/FIX-02/FIX-03/FIX-04/UX-01/UX-02 → repo-relative test paths
- TESTING.md §5 Manual Regression Checklist: Category G / M-13 reworded (FIX-03 in-app tab); new M-NN items (FIX-02 live two-viewer, FIX-01 live browser hot-swap/CSP)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Register new test files in Suite Manifest + Traceability map</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md — §2 Suite Manifest (~22-67) for the row format, §4 Requirement→Test Traceability Map (~126) for the repo-relative-path-only column rule, and §6 Standing Convention
    - Each 168-0N-SUMMARY.md — for the exact final paths/names of the test files created (hub_test.go additions, engine_test.go / engine_stayonhub_test.go, api_stayonhub_test.go, api_disconnect_test.go, WebShareSessionView.plugin-config.test.tsx, App.open-remote.test.tsx, App.createTab.stayOnHub.test.tsx, SettingsTab.stay-on-hub-toggle.test.tsx, StatusBar.shareSession.test.tsx, SessionShareModal.disconnect.test.tsx)
  </read_first>
  <action>
    Add each new automated test file created in plans 01-06 to the appropriate group in the §2
    Suite Manifest. Add §4 Traceability rows mapping FIX-01, FIX-02, FIX-03, FIX-04, UX-01, UX-02
    to their covering test files, using a repo-relative .go/.tsx/.sh file path ONLY in the path
    column (no test-case names in that column). For the existing
    frontend/e2e/web-plugin-hot-swap.spec.ts, record it as a documented adaptation/reference item
    for FIX-01 (still skipped pending the addon-load-mechanism investigation, per RESEARCH
    Pitfall 5) — do not mark it as a passing gate.
  </action>
  <verify>
    <automated>bash tests/check-traceability-paths.sh</automated>
  </verify>
  <acceptance_criteria>
    All new test files appear in the Suite Manifest; §4 maps all six requirements to
    repo-relative test paths; check-traceability-paths.sh passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Reword M-13 and add manual M-NN items</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md — §5 Category G / M-13 (~387-395, the FIX-03 remote-open manual item currently describing the external-browser flow), and an existing two-Mac live-test item format for a model
  </read_first>
  <action>
    Reword M-13 (Category G) in place so its expected behavior describes FIX-03's new outcome:
    opening a remote tailnet session from the Hub opens an IN-APP web-session tab (streaming the
    relay) rather than an external browser window — do NOT add a separate new item for FIX-03.
    Add two new manual items: one for FIX-02 (live two-browser-viewer smoke test — two real
    browser clients on the same share link, confirm neither is kicked and the "Disconnect all
    viewers" button drops both, Hub viewer count returns to 0 within a poll cycle), and one for
    FIX-01 (open the /app/ share URL in a real browser, change plugin-config, confirm live
    hot-swap without reload and inspect DevTools Console for CSP errors — noting the documented
    /app/ no-CSP-header caveat from RESEARCH Pitfall 1).
  </action>
  <verify>
    <automated>grep -q "in-app" TESTING.md && grep -Eq "two-(browser|viewer)|two browser|Disconnect all viewers" TESTING.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    M-13 describes the in-app-tab behavior; new manual M-NN items exist for the FIX-02 live
    two-viewer and FIX-01 live hot-swap/CSP checks; no duplicate FIX-03 manual item added.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (documentation only) | This plan edits TESTING.md only; no runtime trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-10 | Repudiation | stale regression record (M-13 describing retired behavior) | low | mitigate | Reword M-13 in place so the manual regression record matches shipped FIX-03 behavior; run check-traceability-paths.sh to keep the map honest. |

No package installs in this plan — no supply-chain (T-168-SC) threat.
</threat_model>

<verification>
- `bash tests/check-traceability-paths.sh`
- Full suite (phase gate): `go test -race -short ./... && cd frontend && pnpm vitest run && pnpm exec tsc --noEmit`
</verification>

<success_criteria>
TESTING.md is the canonical regression record for this phase: every new test file is registered,
the traceability path checker passes, M-13 reflects the in-app-tab behavior, and manual M-NN
items cover the two live-only checks (FIX-02 multi-viewer, FIX-01 browser hot-swap/CSP).
</success_criteria>

<output>
Create `.planning/phases/168-bug-fix-settings-polish/168-07-SUMMARY.md` when done.
</output>
