---
phase: 168-bug-fix-settings-polish
plan: 09
type: execute
wave: 1
depends_on: ["168-03"]
files_modified:
  - frontend/src/components/Hub/WebShareSessionView.tsx
  - frontend/src/App.tsx
  - frontend/src/components/Hub/SessionCard.tsx
  - frontend/src/components/Hub/WebShareSessionView.test.tsx
  - frontend/src/components/__tests__/App.open-remote.test.tsx
  - frontend/src/components/__tests__/SessionCard.share.test.tsx
  - TESTING.md
autonomous: true
gap_closure: true
requirements: [FIX-03]

must_haves:
  truths:
    - "Opening a remote tailnet session via the card ⋯ menu opens an in-app tab whose terminal streams the remote PTY, routed through the local daemon proxy (ws://127.0.0.1:{relayPort}/api/relay/remote/{id}/ws) — NOT a direct cross-origin peer wss (FIX-03 RC-A)"
    - "The remote web-session tab fills its full pane height — no half-height terminal, no dead dark band below (FIX-03 RC-B)"
    - "Chat is not shown on remote web-session tabs (terminal-only); the native web-guest bootstrap tab (mode==='web') is unchanged — chat still present (FIX-03 RC-A follow-up)"
    - "The remote-card ⋯ menu item label matches the in-app-tab behavior — the stale 'Open in browser' wording is gone (FIX-03 RC-C / D-17)"
  artifacts:
    - frontend/src/components/Hub/WebShareSessionView.tsx
    - frontend/src/App.tsx
    - frontend/src/components/Hub/SessionCard.tsx
  key_links:
    - "WebShareSessionView `remote` prop → TerminalPanel `remote` (and NO wsURL) → RelayClient builds the daemon-proxy URL; cap looked up server-side from RemoteCapStore by sessionID"
    - "App __websession__ branch: isRemoteWebTab → WebShareSessionView `remote`; the branch wrapped in `.terminal-wrapper` supplies the full-height flex-column chain the modal classes need"
---

<objective>
Close three diagnosed FIX-03 gaps in the remote in-app web-session tab (168-UAT.md Test 3, all frontend-only, all introduced by 168-03). The tab currently opens BLANK (RC-A: a direct cross-origin `wss://<peer>/sessions/{id}/ws` that the peer's byte-exact Origin allowlist 403s before any bytes flow) AND renders at half-height with a dead band (RC-B: the modal flex classes mount under a block-context parent with no full-height wrapper). A cosmetic label (RC-C) still reads "Open in browser" though D-17 now opens an in-app tab.

Purpose: Make the FIX-03 in-app remote-open path actually stream the remote PTY at full pane size (the Phase 134 card-click modal already does this correctly — this plan mirrors that proven transport), and correct the misleading menu label.

Output: WebShareSessionView routes the remote tab's terminal through the same daemon proxy the working modal uses (dropping the doomed direct wss) and hides chat for remote tabs; App wraps the __websession__ branch in `.terminal-wrapper`; SessionCard's remote menu item is relabeled. Regression coverage added; TESTING.md updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The diagnosed gaps (root_cause / artifacts / missing per gap)
@.planning/phases/168-bug-fix-settings-polish/168-UAT.md
# Deep root-cause debug session — exact file:line evidence + fix recommendations
@.planning/debug/168-fix03-websession-tab-blank.md
# The plan that introduced the regression
@.planning/phases/168-bug-fix-settings-polish/168-03-SUMMARY.md

# Source under change
@frontend/src/components/Hub/WebShareSessionView.tsx
@frontend/src/components/Hub/HubInteractiveModal.tsx
@frontend/src/lib/relayClient.ts
@frontend/src/components/Hub/SessionCard.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route the remote web-session tab through the daemon proxy + hide chat for remote tabs (RC-A)</name>
  <files>frontend/src/components/Hub/WebShareSessionView.tsx, frontend/src/App.tsx, frontend/src/components/Hub/WebShareSessionView.test.tsx</files>
  <behavior>
    - Test 1 (proxy transport): mount WebShareSessionView with `remote={true}` and a real `relayPort` (e.g. 42) → the mocked TerminalPanel receives `remote === true` AND `wsURL === undefined` (so RelayClient will build `ws://127.0.0.1:{relayPort}/api/relay/remote/{id}/ws`, per relayClient.ts:247-250). It must NOT receive the direct `wss://<peer>/sessions/{id}/ws?cap=` string.
    - Test 2 (default/native path unchanged): mount WITHOUT `remote` (or `remote={false}`) → TerminalPanel still receives the direct `wsURL` (`{ws|wss}://{host}/sessions/{id}/ws?cap=`) exactly as today; the existing wsURL-construction suite must still pass unchanged.
    - Test 3 (chat hidden for remote): with `remote={true}`, the chat toggle button (`.hub-modal__chat-toggle`) and the ChatPanel (`[data-testid="mock-chat-panel"]`) are NOT rendered. Without `remote`, both are present (existing parity gates keep passing).
  </behavior>
  <action>
    Add a `remote?: boolean` prop to WebShareSessionView (WebShareSessionView.tsx). When `remote` is true, the tab is an in-app remote-peer tab and MUST use the daemon-proxy transport — the same path the working Phase 134 HubInteractiveModal uses (see HubInteractiveModal.tsx:80-89, which passes `remote` to TerminalPanel and NO wsURL).

    RC-A root cause (168-UAT.md gap 2, debug session Evidence blocks): today WebShareSessionView.tsx:77 builds a DIRECT cross-origin `wss://<peer>/sessions/{id}/ws?cap=<token>` and passes it as `wsURL` (relayClient.ts:244-245 uses it verbatim). The peer's /sessions/{id}/ws enforces a byte-exact Origin allowlist (internal/webserver/origin_mw.go + server.go OriginPatterns) accepting only the peer's own BaseURL()/FunnelBaseURL(); the webview cannot set the WS Origin header, so the upgrade is 403'd → blank terminal. Do NOT attempt to make the direct wss authenticate — it is architecturally impossible against the allowlist. The daemon proxy (internal/daemon/remote_ws_proxy.go:92) injects the byte-exact Origin server-side; the cap is looked up server-side from RemoteCapStore by sessionID (already deposited via RegisterRemoteCap in handleModalExchange / held-cap reuse — no new backend work, per 168-UAT.md gap 2 "missing" item 1).

    Implementation in WebShareSessionView.tsx:
    - Compute a transport switch, e.g. `const useProxy = remote === true`.
    - Pass to TerminalPanel: `remote={useProxy}` and `wsURL={useProxy ? undefined : wsURL}`. When useProxy is true, RelayClient builds `ws://127.0.0.1:{relayPort}/api/relay/remote/{sessionId}/ws` (relayClient.ts:247-250) — so `relayPort` MUST be the real daemon port for remote tabs. Update the `relayPort` prop doc comment: it is 0 only for the native web-guest path; for remote in-app tabs it is the desktop daemon relay port.
    - Hide chat for remote tabs: gate BOTH the `<ChatPanel …>` element and the `.hub-modal__chat-toggle` `<button>` behind `{!useProxy && ( … )}`. Rationale (168-UAT.md gap 2 "missing" item 2 — pick the lower-risk option): a functional remote chat would need an analogous daemon chat proxy (backend work, out of scope for this frontend-only gap closure); the working modal's ChatPanel has no remote proxy route either, so hiding chat matches the modal's effective behavior and keeps the terminal (the reported priority) working. Leave the web-guest plugin-config self-fetch effect as-is (it already .catch-swallows a cross-origin failure and falls back to TerminalPanel defaults).

    In App.tsx __websession__ render branch (App.tsx:1701-1724): `isRemoteWebTab` is already computed (line 1703). Thread it through as `remote={isRemoteWebTab}` on the `<WebShareSessionView … />` element. Do not change `relayPort={relayPort ?? 0}` — in the desktop app `relayPort` is the real daemon port, which is exactly what the proxy path needs.

    Then write the three RED behavior tests above in WebShareSessionView.test.tsx (extend the existing file — it already mocks TerminalPanel + ChatPanel and captures props via module-level `capturedTerminalProps`/`capturedChatPanelProps`; reuse that harness). Add a `remote` field to the CapturedTerminalProps interface. Then implement to GREEN.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit && pnpm vitest run WebShareSessionView.test</automated>
  </verify>
  <done>With `remote={true}`, TerminalPanel gets `remote:true` + no `wsURL` (daemon-proxy transport) and chat/chat-toggle are absent; without `remote`, the direct `wsURL` + chat behavior is byte-for-byte unchanged and all pre-existing WebShareSessionView.test.tsx assertions pass. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Full-height wrapper for the __websession__ branch (RC-B) + relabel remote menu item (RC-C) + TESTING.md</name>
  <files>frontend/src/App.tsx, frontend/src/components/Hub/SessionCard.tsx, frontend/src/components/__tests__/App.open-remote.test.tsx, frontend/src/components/__tests__/SessionCard.share.test.tsx, TESTING.md</files>
  <action>
    RC-B (168-UAT.md gap 2, debug Evidence 2026-07-02 App.tsx:1701-1724 / style.css:504-517): the __websession__ branch mounts WebShareSessionView (root class `.hub-modal__body--interactive`: flex:1) directly under the block-context `.terminal-container` (style.css:504 — no display:flex, no height:100%), so flex:1 is inert and the panel collapses to content height, exposing the `--hub-bg` dead band. Normal terminal tabs avoid this via a `<div className="terminal-wrapper">` (style.css:512 — height:100%; display:flex; flex-direction:column) at App.tsx:1899-1902.

    Fix: wrap the returned `<WebShareSessionView … />` in the __websession__ IIFE branch (App.tsx:1701-1724) with `<div className="terminal-wrapper" style={{ display: 'flex' }}> … </div>`. Prefer this wrapper over editing the shared `.hub-modal__*` classes (they must keep working in HubInteractiveModal). This branch renders for BOTH the remote tab AND the native web-guest bootstrap tab (mode==='web'); the wrapper is the same full-height flex-column chain the guest's modal classes want, so it fixes the remote tab without regressing the guest — call this out and verify.

    RC-C (168-UAT.md gap 3, D-17): in SessionCard.tsx the remote-only menu item (~line 408-416) reads "Open in browser" with an `ArrowTopRightOnSquareIcon` external-link glyph, but D-17/168-03 now opens an in-app tab via onOpenInBrowser → handleOpenRemoteSession → openWebSessionTab. Reword the visible menu text to "Open in tab" (drop the external-link connotation). Swap `ArrowTopRightOnSquareIcon` for an in-app glyph — use `WindowIcon` from `@heroicons/react/24/outline` (update the import at SessionCard.tsx:20-21 and the usage at line 414; keep the `hub-card__conn-icon` className). Leave the prop name `onOpenInBrowser` as-is (renaming the callback is a wider refactor out of scope; note it in the summary).

    Tests:
    - App.open-remote.test.tsx: add a source-inspection assertion in the existing `__websession__` render-branch describe block (the one at ~line 313 that slices `raw` from `activeId.startsWith('__websession__')`): the branch slice must contain `terminal-wrapper` AND `remote={isRemoteWebTab}` (proves RC-B wrapper present + RC-A remote flag threaded). Update the existing behavior tests that locate the menu item by `el.textContent?.includes('Open in browser')` (lines ~189, 203) to the new "Open in tab" text.
    - SessionCard.share.test.tsx: update the three tests asserting the "Open in browser" label (lines ~252-291) to the new "Open in tab" wording — the remote card menu textContent must contain "Open in tab" and must NOT contain "Open in browser"; the local card must not contain "Open in tab"; the item stays enabled without roJoinCode (D-03). These are rendered-textContent assertions, not source greps.
    - TESTING.md (standing rule — ./CLAUDE.md + TESTING.md Section 6): add a Section 2 Suite Manifest "no new file" note for Phase 168-09 (both test files extended in-place; state the vitest/Total counts stay unchanged); add/update Section 4 traceability rows for the two extended vitest files describing the FIX-03 gap-closure coverage (remote tab uses the daemon proxy not a direct peer wss; full-height wrapper; relabeled menu item). Path column = repo-relative file path only. Then run `bash tests/check-traceability-paths.sh` (must exit 0).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit && pnpm vitest run App.open-remote SessionCard.share && cd .. && bash tests/check-traceability-paths.sh</automated>
  </verify>
  <done>The __websession__ branch is wrapped in `.terminal-wrapper` and threads `remote={isRemoteWebTab}`; SessionCard's remote menu reads "Open in tab" with an in-app glyph; all updated/added vitest assertions pass; tsc clean; traceability check exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desktop webview → remote peer | The in-app remote tab's terminal socket crosses from this daemon's webview to a different tailnet peer's webserver |
| local daemon → remote peer | The daemon proxy (127.0.0.1) dials the peer with a server-injected byte-exact Origin + server-side cap lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-09-01 | Spoofing | remote WS Origin allowlist (origin_mw.go) | high | mitigate | Route the remote tab through the daemon proxy (remote_ws_proxy.go), which injects the byte-exact Origin the peer requires; the direct-wss path (which the webview cannot authenticate and which 403s) is removed. No client-supplied Origin is ever trusted. |
| T-168-09-02 | Information Disclosure | remote-peer capability token | medium | mitigate | The cap is looked up server-side from RemoteCapStore by sessionID (never carried in the proxy URL or React state); dropping the direct wsURL removes the `?cap=` token from a client-built cross-origin URL. |
| T-168-09-03 | Tampering | remote chat cross-origin route | low | accept | Chat is hidden for remote tabs rather than exposing a cross-origin ChatPanel that would 403; no chat data crosses the boundary from the remote tab. Terminal is the priority; a proxied chat route is deferred (out of scope, backend work). |
</threat_model>

<verification>
- `cd frontend && pnpm exec tsc --noEmit` passes (frontend gate — tsc must pass, not just vitest).
- `cd frontend && pnpm vitest run WebShareSessionView.test App.open-remote SessionCard.share` — all pass, including: remote-tab terminal targets the daemon proxy (remote:true, no wsURL); remote-tab chat hidden; native/web-guest path unchanged (direct wsURL + chat present); __websession__ branch wrapped in `.terminal-wrapper` + threads `remote={isRemoteWebTab}`; remote menu label is "Open in tab".
- Full frontend suite green: `cd frontend && pnpm vitest run`.
- `bash tests/check-traceability-paths.sh` exits 0.
</verification>

<success_criteria>
- RC-A closed: the remote in-app tab's terminal connects via `ws://127.0.0.1:{relayPort}/api/relay/remote/{id}/ws` (daemon proxy), not a direct cross-origin peer wss — no 403, PTY streams.
- RC-A follow-up closed: chat is hidden on remote web-session tabs; the native web-guest tab is unchanged.
- RC-B closed: the remote tab fills its full pane height (wrapped in `.terminal-wrapper`); no dead band; the web-guest tab is not regressed.
- RC-C closed: the remote-card ⋯ menu item reads "Open in tab" with an in-app glyph; no test asserts the stale "Open in browser" wording.
- TESTING.md updated (Suite Manifest note + Section 4 rows); traceability check green.
</success_criteria>

<output>
Create `.planning/phases/168-bug-fix-settings-polish/168-09-SUMMARY.md` when done.
</output>
