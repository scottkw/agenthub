---
status: complete
phase: 168-bug-fix-settings-polish
source: [168-VERIFICATION.md]
started: 2026-07-01T23:15:00Z
updated: 2026-07-02T14:23:47Z
---

## Current Test

[testing complete]

## Tests

### 1. FIX-01 live browser plugin-config hot-swap + CSP check (TESTING.md M-43)
expected: Open the /app/ share URL in a real (non-Wails) browser; change a plugin-config value server-side; confirm the terminal updates live without reload; inspect DevTools Console for no CSP violations.
result: pass
note: Verified with the "Find in scrollback" (search) plugin toggle — not the terminal theme (theme is a separate prop, never synced to guests). Disabling search in the desktop app made the guest browser's Cmd+F find bar stop responding immediately, with NO page reload; DevTools Console showed no CSP violations. First attempt used the terminal theme (out of scope for FIX-01) — false start, corrected. Share URL carries ?cap= token inline, so no separate share-code prompt is expected (by design).

### 2. FIX-02 live two-browser-viewer smoke test (TESTING.md M-42)
expected: Two real browser clients open the same share link; neither is kicked; click "Disconnect all viewers" and confirm both drop and the Hub card's viewer count returns to 0 within a poll cycle.
result: pass

### 3. FIX-03 live two-Mac remote-open in-app tab check (TESTING.md M-13, reworded)
expected: On a live two-Mac tailnet, open a remote session from the Hub; confirm an in-app terminal tab opens (no external browser window) and streams real PTY output; open a second different remote session and confirm two independent tabs with no cross-contamination.
result: issue
reported: "This is messed up. No join code (but I already entered one for the other test). It did open a tab, but it's not full window size and it's blank with a blinking cursor."
severity: major
note: |
  Retested live 2026-07-02 on a two-Mac tailnet (second Mac now available; production build).
  Sub-results:
  - The Phase 134 card-CLICK gesture (hub-modal intent) opens the interactive session modal and
    streams the remote PTY correctly (first screenshot) — that path WORKS (routes TerminalPanel
    through the daemon WS proxy via the `remote` prop). Not FIX-03, but confirms remote reachability.
  - The FIX-03 path (⋯ card menu → "Open in browser" → onOpenInBrowser → handleOpenRemoteSession →
    openWebSessionTab): a `web-session` tab DID open (tab-open mechanism works, D-17), and the
    join-code modal was correctly SKIPPED via held-cap reuse (D-03, remoteCapsCached seeded by the
    prior test) — that skip is expected, not a defect.
  - DEFECT: the opened `web-session` tab renders BLANK (blinking cursor, no PTY output) AND does not
    fill the pane (terminal occupies only the top ~half; large dead dark band below). The same remote
    session streams fine in the Phase 134 modal, so the remote PTY is reachable — the failure is in the
    `web-session` tab render/connect/sizing path, not the remote link.
  Likely-shared root cause hypothesis (to be confirmed by diagnosis): the web-session tab container
  is not sized to full height, so xterm.js fit() computes ~0 rows and the terminal cannot render —
  blank + short pane as two symptoms of one layout/mount bug. Alternative: the direct remote
  baseURL+cap connection used by the tab (vs. the daemon WS proxy the modal uses) never establishes.

### 4. UX-02 / CR-02 live-sync check — footer Share button from a non-Hub session tab (TESTING.md M-44)
expected: Create a new session (default Stay-on-Hub OFF, so the view switches to the new session tab). From that non-Hub tab, click the footer "Share Session" button — the Share modal opens for the active session. Enable Funnel and confirm the warm-up resolves live (does not appear stuck ~30s while Funnel is actually up), and that "Disconnect all viewers" visibility/count stays live while the modal is open.
result: pass
note: |
  Live re-check on a production build (wails build -tags wailsassets) after 168-08 fix — PASS
  (2026-07-02). Footer pill flips WEB ON on modal "Share the session" ON and returns WEB OFF on
  OFF, both toggle directions, from the footer "Share Session" button and the Hub-card modal
  paths, with the one-time shell-web-share warning already dismissed. No stale-label drift. This
  closes the prior "issue" (footer read WEB OFF while actively shared) reported on 2026-07-01.
prior_report: "Footer reads 'WEB OFF' while the Share modal shows the session actively shared (Share the session ON + Enable internet sharing ON, live Funnel URL). Footer status pill drifts from actual web-share state. — fixed by 168-08, re-verified live PASS above."
note_cr02: CR-02 sub-behavior PASSED — steps 1-3 (create session with Stay-on-Hub OFF → land on session tab → footer "Share Session" opens modal for active session) all confirmed; "Enable Funnel" is labeled "Enable internet sharing" (same mechanism, works, no 30s warm-up hang); CR-02 live-sync confirmed via controlled viewer join (Disconnect button + Hub-card count update live from the session tab, both directions). Out-of-scope findings surfaced during this test filed separately: #125 (no disconnect notice to remote viewer), #126 (shared-session tab doesn't auto-close on exit), comment on #107 (read-write option for internet share).

## Summary

total: 4
passed: 3
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Footer status pill reflects the active session's actual web-share state — no button↔modal/pill drift (UX-02 / #115)"
  status: resolved
  resolved_by: "168-08 (commits 2129a423, a1b609ff, 6ede69a2) — live re-checked PASS on a production build (wails build -tags wailsassets), 2026-07-02, Test 4"
  reason: "User reported: footer reads 'WEB OFF' while the Share modal shows the session actively shared (Share the session ON + Enable internet sharing ON, live Funnel URL present). Screenshot confirms shell 5 shared with footer showing WEB OFF."
  severity: major
  test: 4
  root_cause: "The footer StatusBar pill reads App-level `webEnabled[sessionId]` (App.tsx:1925). That record is only set true by (a) the mount/reconnect seed effects from SessionInfo.webEnabled (App.tsx:598-609 and 1495-1504) and (b) handleShellWebShareConfirm — the ONE-TIME shell-web-share warning-banner confirm path (App.tsx:968 sets setWebEnabled true). The modal's normal 'Share the session' toggle, once the user has dismissed the warning banner (shellWebShareWarned persisted true), falls through the guard at SessionShareModal.tsx:229 to line 234 `ToggleWebServing(session.id, next)` and updates ONLY the modal's local `shareEnabled` — it never notifies App. The CR-02 live poll (App.tsx:1049-1070) refreshes `shareModalSession` but does not reconcile `webEnabled`. Net: after the first warning acknowledgement, sharing via the modal leaves `webEnabled[sessionId]` stale-false and the footer pill stuck on 'WEB OFF'. UX-02/#115 fixed the button label + made the modal the toggle source of truth, but left the status pill reading a stale record."
  artifacts:
    - path: "frontend/src/components/Hub/SessionShareModal.tsx"
      issue: "handleShareToggle (line 222-244) calls ToggleWebServing without notifying App of the new web-share state — no onShareEnabledChange callback exists"
    - path: "frontend/src/App.tsx"
      issue: "webEnabled record only updated via the warning-confirm path (968) + mount/reconnect seeds (598-609, 1495-1504); SessionShareModal render (1945-1955) passes no share-state-change callback; CR-02 poll (1049-1070) does not reconcile webEnabled from ListSessions"
  missing:
    - "Add an onShareEnabledChange(sessionId, enabled) callback prop to SessionShareModal, invoked from handleShareToggle for BOTH ON and OFF, that calls setWebEnabled in App.tsx — OR have the ListSessions poll (CR-02 poll and/or Hub poll) reconcile webEnabled[sessionId] from server truth (SessionInfo.webEnabled)"
    - "Ensure the OFF-toggle also clears/sets webEnabled[sessionId] false so the pill returns to 'WEB OFF' when unshared"
    - "Add a regression test asserting the footer pill / App.webEnabled tracks a modal 'Share the session' toggle in the already-warned path (shellWebShareWarned=true)"
  debug_session: ""

- truth: "Opening a remote tailnet session via the card ⋯ menu 'Open in browser' opens a docked in-app web-session tab that STREAMS the remote PTY at full pane size (FIX-03 / SC3)"
  status: failed
  reason: "User reported (live two-Mac retest 2026-07-02): FIX-03 tab opens but 'it's not full window size and it's blank with a blinking cursor'. The Phase 134 card-click modal streams the same remote session correctly, so the remote link is fine; the web-session tab render/connect/sizing path fails."
  severity: major
  test: 3
  root_cause: ""     # Filled by diagnosis
  artifacts: []      # Filled by diagnosis
  missing: []        # Filled by diagnosis
  debug_session: ""  # Filled by diagnosis

- truth: "The remote-card ⋯ menu item that opens an in-app tab is labeled accurately (FIX-03 / D-17 UX polish)"
  status: failed
  reason: "User reported (live 2026-07-02): the ⋯ menu item reads 'Open in browser' but D-17/FIX-03 now opens an in-app tab, not an external browser — the label is stale and misleading and should be reworded (e.g. 'Open in tab' / 'Open session')."
  severity: cosmetic
  test: 3
  root_cause: "SessionCard.tsx remote-only menu item label text 'Open in browser' (~line 415) predates Phase 168-03 D-17, which changed onOpenInBrowser/handleOpenRemoteSession from BrowserOpenURL to openWebSessionTab (in-app tab). Label text was never updated to match the new behavior. Also note the prop name onOpenInBrowser and the ArrowTopRightOnSquareIcon (external-link glyph) are now semantically stale."
  artifacts:
    - path: "frontend/src/components/Hub/SessionCard.tsx"
      issue: "Menu item text 'Open in browser' (+ external-link icon) at the remote-only action block (~line 408-416) no longer matches the in-app-tab behavior wired via onOpenInBrowser -> handleOpenRemoteSession -> openWebSessionTab"
  missing:
    - "Reword the menu item to reflect in-app-tab behavior (e.g. 'Open in tab' or 'Open session'); optionally swap the external-link icon for a tab/window glyph"
    - "Update any test asserting the 'Open in browser' label text to the new wording"
  debug_session: ""
