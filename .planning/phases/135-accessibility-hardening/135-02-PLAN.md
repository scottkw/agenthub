---
phase: 135-accessibility-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Hub/HubFilterBar.tsx
  - frontend/src/components/Hub/HubFilterBar.test.tsx
  - frontend/src/components/Hub/GroupSidebar.tsx
  - frontend/src/components/Hub/GroupSidebar.test.tsx
autonomous: true
requirements: [A11Y-02]
tags: [accessibility, keyboard, aria, react]

must_haves:
  truths:
    - "A screen reader announces which status filter pill is active via aria-pressed"
    - "Group sidebar items are keyboard-focusable and activate on Enter/Space"
  artifacts:
    - path: "frontend/src/components/Hub/HubFilterBar.tsx"
      provides: "aria-pressed on each filter pill button"
      contains: "aria-pressed"
    - path: "frontend/src/components/Hub/GroupSidebar.tsx"
      provides: "tabIndex + onKeyDown Enter/Space on GroupSidebarItem li"
      contains: "tabIndex={0}"
  key_links:
    - from: "frontend/src/components/Hub/HubFilterBar.tsx"
      to: "activeFilter"
      via: "aria-pressed reflects active filter"
      pattern: "aria-pressed=\\{activeFilter"
    - from: "frontend/src/components/Hub/GroupSidebar.tsx"
      to: "onGroupSelect"
      via: "Enter/Space keydown invokes onGroupSelect"
      pattern: "onKeyDown"
---

<objective>
Close the two component-level A11Y-02 keyboard/screen-reader gaps on the Hub control surfaces: add `aria-pressed` to every status filter pill (GAP-135-B) so assistive tech can announce the active filter, and make `GroupSidebarItem` `<li>` elements keyboard-operable (RESEARCH Open Question 1) with `tabIndex={0}` + Enter/Space → `onGroupSelect`, matching the `:focus-visible` rule added in plan 135-01.

Purpose: Satisfies the A11Y-02 requirement that the entire Hub is navigable and operable by keyboard, and exposes filter state to screen readers. These are the two non-modal component gaps; the modal keyboard work (focus trap, Escape) is plan 135-03.
Output: Modified `HubFilterBar.tsx` and `GroupSidebar.tsx` plus DOM-render test assertions in their existing test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-accessibility-hardening/135-UI-SPEC.md
@.planning/phases/135-accessibility-hardening/135-RESEARCH.md
@.planning/phases/135-accessibility-hardening/135-PATTERNS.md

<interfaces>
<!-- Source-of-truth facts. Do NOT re-derive by exploring. -->

HubFilterBar pill button (HubFilterBar.tsx ~lines 104-113) currently:
  key, className (toggles hub-filter__pill--active), onClick={() => onFilterChange(key)}, type="button"
  ADD: aria-pressed={activeFilter === key ? 'true' : 'false'}
  Rationale (RESEARCH Pattern 4): pills are <button> in a role="group", so aria-pressed (toggle button),
  NOT aria-selected (which is for listbox/option). Container already has role="group"
  aria-label="Session status filter" — correct, do not change.

GroupSidebarItem <li> (GroupSidebar.tsx ~lines 127-131) currently:
  <li className={itemClass} role="option" aria-selected={...} onClick={() => onGroupSelect(id)} ...dnd handlers>
  ADD: tabIndex={0} and onKeyDown that, on 'Enter' or ' ', calls e.preventDefault() then onGroupSelect(id).
  Keyboard pattern to copy verbatim — SessionCard.tsx lines 250-257 (Enter/Space → preventDefault → handler).
  onGroupSelect signature: (id: string | null) => void  (GroupSidebar.tsx line 85).
  The matching .hub__group-sidebar-item:focus-visible CSS ring is added by plan 135-01.

Test harnesses (DOM-render, createRoot + act + getAttribute — jsdom-safe for ARIA/tabindex):
  HubFilterBar.test.tsx — has renderFilterBar(overrides) helper + afterEach cleanup; aria-label
    getAttribute pattern at lines 164-168 is the structural match for the new assertions.
  GroupSidebar.test.tsx — existing DOM-render harness; assert tabIndex + simulated keydown.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add aria-pressed to status filter pills (GAP-135-B)</name>
  <files>frontend/src/components/Hub/HubFilterBar.tsx, frontend/src/components/Hub/HubFilterBar.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/HubFilterBar.tsx (the FILTER_PILLS.map pill button block ~lines 104-113, and the role="group" container)
    - frontend/src/components/Hub/HubFilterBar.test.tsx lines 29-58 (renderFilterBar helper + afterEach) and 164-168 (getAttribute assertion pattern)
    - .planning/phases/135-accessibility-hardening/135-RESEARCH.md Pattern 4 (aria-pressed vs aria-selected rationale)
    - .planning/phases/135-accessibility-hardening/135-PATTERNS.md "HubFilterBar.tsx — GAP-135-B" section
  </read_first>
  <behavior>
    - Active pill (activeFilter === key) has attribute aria-pressed="true"
    - Every inactive pill has aria-pressed="false"
    - aria-pressed value tracks activeFilter prop (changing activeFilter re-renders with the new active pill pressed)
  </behavior>
  <action>
    Implement GAP-135-B. On each `.hub-filter__pill` `<button>` in the `FILTER_PILLS.map` of HubFilterBar.tsx, add `aria-pressed={activeFilter === key ? 'true' : 'false'}`. Use `aria-pressed` (toggle-button semantics), NOT `aria-selected` — the pills are buttons in a `role="group"`, per RESEARCH Pattern 4. Leave the container `role="group"` / `aria-label="Session status filter"` unchanged. Add the two behavior assertions (active pill `aria-pressed="true"`; all inactive pills `aria-pressed="false"`) to the existing `'HubFilterBar — filter pills'` describe block using the `renderFilterBar({ activeFilter: 'running' })` helper and `.getAttribute('aria-pressed')`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/Hub/HubFilterBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DOM behavior: `container.querySelector('.hub-filter__pill--active').getAttribute('aria-pressed')` === `'true'`.
    - DOM behavior: every non-active `.hub-filter__pill` has `getAttribute('aria-pressed')` === `'false'`.
    - Source: `aria-pressed` value is derived from `activeFilter === key` (not hardcoded).
    - HubFilterBar.test.tsx passes including the pre-existing aria-label and count tests.
  </acceptance_criteria>
  <done>Each filter pill exposes its toggle state via aria-pressed reflecting activeFilter; screen readers can announce the active filter. HubFilterBar.test.tsx green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make GroupSidebar items keyboard-operable (A11Y-02, RESEARCH Open Question 1)</name>
  <files>frontend/src/components/Hub/GroupSidebar.tsx, frontend/src/components/Hub/GroupSidebar.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/GroupSidebar.tsx lines 85-95 (onGroupSelect signature) and 126-161 (GroupSidebarItem li + role="option" + onClick + dnd handlers)
    - frontend/src/components/Hub/SessionCard.tsx lines 250-257 (the Enter/Space onKeyDown + tabIndex pattern to copy)
    - frontend/src/components/Hub/GroupSidebar.test.tsx (existing DOM-render harness)
    - .planning/phases/135-accessibility-hardening/135-RESEARCH.md Open Question 1
    - .planning/phases/135-accessibility-hardening/135-PATTERNS.md "GroupSidebar.tsx" section
  </read_first>
  <behavior>
    - A GroupSidebarItem <li> exposes tabIndex={0} (keyboard-focusable)
    - Pressing Enter on a focused item calls onGroupSelect with that item's id
    - Pressing Space on a focused item calls onGroupSelect with that item's id and prevents default (no page scroll)
    - Existing onClick → onGroupSelect behavior and drag-and-drop handlers are unchanged
  </behavior>
  <action>
    Implement the A11Y-02 keyboard gap for the group sidebar (RESEARCH Open Question 1 recommendation). On the `GroupSidebarItem` `<li>` (GroupSidebar.tsx ~lines 127-131) add `tabIndex={0}` and an `onKeyDown` that, on `e.key === 'Enter' || e.key === ' '`, calls `e.preventDefault()` then `onGroupSelect(id)` — copy the keyboard handler shape from SessionCard.tsx lines 250-257. Preserve the existing `role="option"`, `aria-selected`, `onClick`, and the `onDragOver`/`onDragLeave`/`onDrop` handlers exactly. The matching `.hub__group-sidebar-item:focus-visible` ring is supplied by plan 135-01 (do not add CSS here). Add DOM-render assertions to GroupSidebar.test.tsx: item has `tabIndex === 0`; dispatching a `keydown` with `key: 'Enter'` (and separately `' '`) on the item invokes the `onGroupSelect` spy with the correct id.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/Hub/GroupSidebar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DOM behavior: a `GroupSidebarItem` `<li>` has `tabIndex` 0 (keyboard-focusable).
    - DOM behavior: simulating `keydown` `Enter` on the item calls the `onGroupSelect` spy with that item's id.
    - DOM behavior: simulating `keydown` `Space` (`' '`) on the item calls `onGroupSelect` with the id AND `preventDefault` was invoked (no scroll).
    - `role="option"`, `aria-selected`, `onClick`, and drag handlers remain present (no regression to existing GroupSidebar tests).
  </acceptance_criteria>
  <done>Group sidebar items are reachable by Tab and activate on Enter/Space, consistent with the SessionCard keyboard pattern; drag-and-drop and click behavior preserved. GroupSidebar.test.tsx green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds DOM attributes (aria-pressed, tabIndex) and a keyboard handler to existing client-side React components. No network surface, no IPC, no capability, no new untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-02-01 | Tampering | filter/group keyboard handlers | accept | Handlers only invoke existing callbacks (onFilterChange, onGroupSelect) with the same id values already reachable via onClick. No new code path or privilege. |
| T-135-02-02 | Elevation of Privilege | keyboard activation of group/filter | accept | Keyboard activation reaches exactly the same client-side state changes as the existing mouse onClick. No backend call, no capability boundary touched. |
| T-135-02-SC | Tampering | npm/pip/cargo installs | N/A | No packages installed this plan (RESEARCH: zero new dependencies). Legitimacy gate not applicable. |

No high-severity threats. Keyboard/ARIA parity for already-clickable client-side controls; no new attack surface.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/Hub/HubFilterBar.test.tsx src/components/Hub/GroupSidebar.test.tsx` — green
- `cd frontend && npx vitest run` — full suite green (no regression)
- Keyboard parity: group sidebar items operable by Enter/Space identically to mouse click
</verification>

<success_criteria>
- A11Y-02 (component portion): filter pills expose `aria-pressed` reflecting the active filter; group sidebar items are keyboard-focusable and activate on Enter/Space.
- Existing onClick, drag-and-drop, and ARIA contracts are preserved (no behavioral regression).
</success_criteria>

<output>
Create `.planning/phases/135-accessibility-hardening/135-02-SUMMARY.md` when done
</output>
