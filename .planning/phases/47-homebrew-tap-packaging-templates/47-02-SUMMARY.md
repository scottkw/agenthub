---
phase: 47-homebrew-tap-packaging-templates
plan: "02"
subsystem: infra
tags: [homebrew, github-actions, distribute, tap, cask, PAT]

# Dependency graph
requires:
  - phase: 46-release-build-pipeline
    provides: release.yml that generates checksums.txt with SHA256 for all artifacts
provides:
  - .github/workflows/distribute.yml — auto-updates Homebrew tap on release:published
  - Homebrew tap formula update automation via TAP_DEPLOY_TOKEN PAT
affects: [47-03, 48-winget-distribution]

# Tech tracking
tech-stack:
  added: [nick-fields/retry@v3]
  patterns: [release:published trigger, sed-based formula update, cross-repo PAT push, checksums.txt SHA256 extraction]

key-files:
  created:
    - .github/workflows/distribute.yml
  modified: []

key-decisions:
  - "Trigger on release:published (not push:tags) — tag push fires before artifacts are uploaded; release:published fires only after publish job completes"
  - "Extract SHA256 from checksums.txt (not by re-downloading DMG) — faster, avoids 50MB+ binary, avoids notarization-staple timing issue"
  - "Use nick-fields/retry@v3 with 5 attempts and 60s wait — handles CDN propagation delay after notarization"
  - "Use sed [^\"]*  pattern for version replacement — avoids regex metacharacter issues with dots in version strings"
  - "Guard empty commit with git diff --cached --quiet — prevents workflow failure if formula already up to date"

patterns-established:
  - "Pattern: Cross-repo git push via classic PAT (TAP_DEPLOY_TOKEN) — GITHUB_TOKEN cannot push to other repos"
  - "Pattern: checksums.txt grep+awk SHA256 extraction — grep for exact filename, awk for first field (handles variable spacing)"

requirements-completed: [DIST-02]

# Metrics
duration: 1min
completed: 2026-04-05
---

# Phase 47 Plan 02: Distribute Workflow Summary

**distribute.yml GitHub Actions workflow that auto-updates scottkw/homebrew-agenthub cask formula on each release:published event using checksums.txt SHA256 extraction, nick-fields/retry, and TAP_DEPLOY_TOKEN PAT**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-04-05T00:25:09Z
- **Completed:** 2026-04-05T00:25:49Z
- **Tasks:** 1 of 2 (Task 2 is checkpoint:human-action — awaiting manual setup)
- **Files modified:** 1

## Accomplishments
- Created `.github/workflows/distribute.yml` that triggers on `release:published` (not tag push) to avoid artifact race condition
- Configured retry logic via `nick-fields/retry@v3` (5 attempts, 60s wait) for CDN propagation delay after notarization
- Extracts DMG SHA256 from `checksums.txt` via grep+awk — no re-download of the 50MB+ binary
- Checks out `scottkw/homebrew-agenthub` tap repo via `TAP_DEPLOY_TOKEN` PAT for cross-repo push
- Updates cask formula version and sha256 via sed with safe `[^"]*` pattern (handles dots in version strings)
- Guards against empty commits with `git diff --cached --quiet` to prevent workflow failure

## Task Commits

1. **Task 1: Create distribute.yml Homebrew tap auto-update workflow** - `8a3ba3c` (feat)

## Files Created/Modified
- `.github/workflows/distribute.yml` - Homebrew tap auto-update workflow; triggers on release:published, retries checksums.txt download, extracts SHA256, updates cask formula via PAT push

## Decisions Made
- Trigger: `release:published` over `push:tags` — tag push fires before artifacts exist in release; `release:published` fires after `softprops/action-gh-release` has uploaded all assets
- SHA256 source: parse `checksums.txt` (already generated by Phase 46's publish job) rather than re-downloading DMG — avoids binary download, avoids notarization timing window
- sed pattern: `[^"]*` instead of literal version string — dots are regex metacharacters; character class is safe
- Empty commit guard: `git diff --cached --quiet` exit 0 — prevents workflow failure if formula already reflects current version

## Deviations from Plan

None - plan executed exactly as written.

## User Setup Required

**Task 2 requires manual setup before the distribute.yml workflow can function:**

1. **Create tap repo:** Go to https://github.com/new and create `homebrew-agenthub` under the `scottkw` account (Public, initialize with README). The name MUST be `homebrew-agenthub` — Homebrew's `brew tap scottkw/agenthub` short form requires this prefix.

2. **Create Casks/agenthub.rb in the tap repo:** After creating the repo, create `Casks/agenthub.rb` with placeholder content. Use the GitHub UI or git clone. Content:
   ```ruby
   cask "agenthub" do
     version "0.0.0"
     sha256 "placeholder"

     url "https://github.com/scottkw/agenthub/releases/download/v#{version}/agenthub-v#{version}-darwin-universal.dmg"
     name "AgentHub"
     desc "AI coding session manager with tabbed terminals and Tailscale web access"
     homepage "https://github.com/scottkw/agenthub"

     depends_on macos: ">= :ventura"

     app "agenthub.app"

     zap trash: [
       "~/Library/Application Support/agenthub",
       "~/Library/Preferences/com.scottkw.agenthub.plist",
       "~/Library/Caches/com.scottkw.agenthub",
       "~/Library/Logs/agenthub",
     ]
   end
   ```

3. **Create TAP_DEPLOY_TOKEN secret:** Go to https://github.com/settings/tokens and generate a classic PAT (NOT fine-grained) with `repo` scope. Then go to https://github.com/scottkw/agenthub/settings/secrets/actions and add it as `TAP_DEPLOY_TOKEN`.

**Verification checklist:**
- https://github.com/scottkw/homebrew-agenthub exists and is public
- https://github.com/scottkw/homebrew-agenthub/blob/main/Casks/agenthub.rb contains `cask "agenthub" do`
- TAP_DEPLOY_TOKEN secret listed at https://github.com/scottkw/agenthub/settings/secrets/actions
- `brew tap scottkw/agenthub` succeeds locally

## Known Stubs

None. The distribute.yml is complete and functional. Task 2 (tap repo setup) is a manual prerequisite, not a code stub.

## Next Phase Readiness
- `.github/workflows/distribute.yml` is committed and ready — it will auto-trigger on the first `release:published` event after setup
- Task 2 must be completed manually (tap repo + TAP_DEPLOY_TOKEN) before distribute.yml can function
- Phase 47-03 (packaging templates) can proceed in parallel with Task 2 setup

---
*Phase: 47-homebrew-tap-packaging-templates*
*Completed: 2026-04-05*

## Self-Check: PASSED
- .github/workflows/distribute.yml: FOUND
- .planning/phases/47-homebrew-tap-packaging-templates/47-02-SUMMARY.md: FOUND
- Commit 8a3ba3c: FOUND
