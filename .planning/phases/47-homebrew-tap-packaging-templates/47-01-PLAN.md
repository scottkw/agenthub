---
phase: 47-homebrew-tap-packaging-templates
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packaging/homebrew/agenthub.rb.template
  - packaging/winget/manifests/scottkw.agenthub.yaml
  - packaging/winget/manifests/scottkw.agenthub.installer.yaml
  - packaging/winget/manifests/scottkw.agenthub.locale.en-US.yaml
autonomous: true
requirements: [DIST-01, DIST-04]
must_haves:
  truths:
    - "packaging/homebrew/agenthub.rb.template is a valid Ruby cask formula with {{VERSION}} and {{SHA256}} placeholders"
    - "packaging/winget/manifests/ contains three WinGet manifest files at schema 1.12.0"
    - "Homebrew template URL matches Phase 46 artifact naming convention exactly"
    - "WinGet installer manifest URL matches Phase 46 NSIS artifact naming exactly"
  artifacts:
    - path: "packaging/homebrew/agenthub.rb.template"
      provides: "Homebrew cask formula template with placeholder tokens"
      contains: "cask \"agenthub\""
    - path: "packaging/winget/manifests/scottkw.agenthub.yaml"
      provides: "WinGet version manifest"
      contains: "ManifestType: version"
    - path: "packaging/winget/manifests/scottkw.agenthub.installer.yaml"
      provides: "WinGet installer manifest"
      contains: "ManifestType: installer"
    - path: "packaging/winget/manifests/scottkw.agenthub.locale.en-US.yaml"
      provides: "WinGet default locale manifest"
      contains: "ManifestType: defaultLocale"
  key_links:
    - from: "packaging/homebrew/agenthub.rb.template"
      to: "release.yml artifact naming"
      via: "URL pattern in template"
      pattern: "agenthub-v\\{\\{VERSION\\}\\}-darwin-universal\\.dmg"
    - from: "packaging/winget/manifests/scottkw.agenthub.installer.yaml"
      to: "release.yml artifact naming"
      via: "InstallerUrl in manifest"
      pattern: "agenthub-v\\{\\{VERSION\\}\\}-windows-amd64-installer\\.exe"
---

<objective>
Create packaging templates for Homebrew cask and WinGet manifests in the main repo.

Purpose: Provides the source-of-truth templates that distribution workflows use to generate live package manager formulas. Homebrew template is used by distribute.yml (Plan 02) as reference; WinGet templates are consumed by Phase 48.
Output: Four template files in packaging/ directory tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-homebrew-tap-packaging-templates/47-RESEARCH.md
@.planning/phases/46-release-build-pipeline/46-01-SUMMARY.md

<interfaces>
<!-- Artifact naming convention from Phase 46 release.yml -->
<!-- These exact patterns MUST be reflected in template URLs -->

From .github/workflows/release.yml:
- macOS DMG: agenthub-${GITHUB_REF_NAME}-darwin-universal.dmg (e.g., agenthub-v1.8.0-darwin-universal.dmg)
- Windows installer: agenthub-${GITHUB_REF_NAME}-windows-amd64-installer.exe
- Windows EXE: agenthub-${GITHUB_REF_NAME}-windows-amd64.exe
- Linux tar.gz: agenthub-${GITHUB_REF_NAME}-linux-amd64.tar.gz
- Linux deb: agenthub-${GITHUB_REF_NAME}-linux-amd64.deb
- Checksums: checksums.txt (sha256sum output, format: "<hash>  <filename>")

From Phase 46 Summary:
- .app bundle name: agenthub.app (lowercase)
- Release download URL pattern: https://github.com/scottkw/agenthub/releases/download/v1.8.0/agenthub-v1.8.0-darwin-universal.dmg
- GITHUB_REF_NAME includes the "v" prefix (e.g., v1.8.0)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Homebrew cask template and WinGet manifest templates</name>
  <files>
    packaging/homebrew/agenthub.rb.template
    packaging/winget/manifests/scottkw.agenthub.yaml
    packaging/winget/manifests/scottkw.agenthub.installer.yaml
    packaging/winget/manifests/scottkw.agenthub.locale.en-US.yaml
  </files>
  <read_first>
    .github/workflows/release.yml
    .planning/phases/47-homebrew-tap-packaging-templates/47-RESEARCH.md
    .planning/phases/46-release-build-pipeline/46-01-SUMMARY.md
  </read_first>
  <action>
Create 4 files. Create parent directories first (`mkdir -p packaging/homebrew packaging/winget/manifests`).

**File 1: `packaging/homebrew/agenthub.rb.template`**

```ruby
cask "agenthub" do
  version "{{VERSION}}"
  sha256 "{{SHA256}}"

  url "https://github.com/scottkw/agenthub/releases/download/v#{version}/agenthub-v#{version}-darwin-universal.dmg"
  name "AgentHub"
  desc "AI coding session manager with tabbed terminals and Tailscale web access"
  homepage "https://github.com/scottkw/agenthub"

  depends_on macos: ">= :ventura"

  app "agenthub.app"

  zap trash: [
    "~/Library/Application Support/agenthub",
    "~/Library/Preferences/com.scottkw.agenthub.plist",
    "~/Library/Caches/com.scottkw.agenthub",
    "~/Library/Logs/agenthub",
  ]
end
```

Key details:
- `version` stores bare version (e.g., `1.8.0`) -- the `v` prefix is added in the URL via `v#{version}`
- `{{VERSION}}` and `{{SHA256}}` are sed-replaceable placeholder tokens
- URL pattern matches Phase 46 exactly: `agenthub-v1.8.0-darwin-universal.dmg`
- `.app` name is `agenthub.app` (lowercase, confirmed from build/bin/)
- `depends_on macos: ">= :ventura"` -- macOS 13.0 minimum (safe baseline per research)
- No `uninstall` stanza needed for `.app` artifacts (only for `pkg`/`installer`)

**File 2: `packaging/winget/manifests/scottkw.agenthub.yaml`**

```yaml
PackageIdentifier: scottkw.agenthub
PackageVersion: "{{VERSION}}"
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.12.0
```

**File 3: `packaging/winget/manifests/scottkw.agenthub.installer.yaml`**

```yaml
PackageIdentifier: scottkw.agenthub
PackageVersion: "{{VERSION}}"
InstallerType: nullsoft
InstallModes:
  - silent
  - silentWithProgress
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/scottkw/agenthub/releases/download/v{{VERSION}}/agenthub-v{{VERSION}}-windows-amd64-installer.exe
    InstallerSha256: "{{WINDOWS_SHA256}}"
ManifestType: installer
ManifestVersion: 1.12.0
```

Key details:
- `InstallerType: nullsoft` (not `exe`) -- Phase 46 uses `nsis: true` in wails-build-action, WinGet `nullsoft` enables automatic `/S` silent install
- `Architecture: x64` -- Phase 46 builds `windows/amd64` only
- URL pattern matches Phase 46 exactly: `agenthub-v1.8.0-windows-amd64-installer.exe`
- `{{WINDOWS_SHA256}}` is a separate placeholder from `{{SHA256}}` (Homebrew uses macOS DMG hash)

**File 4: `packaging/winget/manifests/scottkw.agenthub.locale.en-US.yaml`**

```yaml
PackageIdentifier: scottkw.agenthub
PackageVersion: "{{VERSION}}"
PackageLocale: en-US
Publisher: scottkw
PublisherUrl: https://github.com/scottkw
PackageName: AgentHub
PackageUrl: https://github.com/scottkw/agenthub
License: Proprietary
ShortDescription: AI coding session manager with tabbed terminals and Tailscale web access
Tags:
  - ai
  - terminal
  - claude-code
  - tailscale
  - developer-tools
ManifestType: defaultLocale
ManifestVersion: 1.12.0
```

Key details:
- `License: Proprietary` -- no LICENSE file exists in repo (verified 2026-04-04). Update to SPDX identifier if LICENSE added before Phase 48.
- `PackageIdentifier: scottkw.agenthub` follows WinGet `Publisher.PackageName` convention
- All three WinGet files use schema `ManifestVersion: 1.12.0`
  </action>
  <verify>
    <automated>ruby -c packaging/homebrew/agenthub.rb.template && test -f packaging/winget/manifests/scottkw.agenthub.yaml && test -f packaging/winget/manifests/scottkw.agenthub.installer.yaml && test -f packaging/winget/manifests/scottkw.agenthub.locale.en-US.yaml && echo "ALL FILES VALID"</automated>
  </verify>
  <acceptance_criteria>
    - packaging/homebrew/agenthub.rb.template contains `cask "agenthub" do`
    - packaging/homebrew/agenthub.rb.template contains `version "{{VERSION}}"`
    - packaging/homebrew/agenthub.rb.template contains `sha256 "{{SHA256}}"`
    - packaging/homebrew/agenthub.rb.template contains `agenthub-v#{version}-darwin-universal.dmg`
    - packaging/homebrew/agenthub.rb.template contains `app "agenthub.app"`
    - packaging/homebrew/agenthub.rb.template contains `depends_on macos: ">= :ventura"`
    - packaging/winget/manifests/scottkw.agenthub.yaml contains `ManifestType: version`
    - packaging/winget/manifests/scottkw.agenthub.yaml contains `ManifestVersion: 1.12.0`
    - packaging/winget/manifests/scottkw.agenthub.installer.yaml contains `InstallerType: nullsoft`
    - packaging/winget/manifests/scottkw.agenthub.installer.yaml contains `agenthub-v{{VERSION}}-windows-amd64-installer.exe`
    - packaging/winget/manifests/scottkw.agenthub.installer.yaml contains `ManifestType: installer`
    - packaging/winget/manifests/scottkw.agenthub.locale.en-US.yaml contains `License: Proprietary`
    - packaging/winget/manifests/scottkw.agenthub.locale.en-US.yaml contains `ManifestType: defaultLocale`
    - `ruby -c packaging/homebrew/agenthub.rb.template` exits 0 (valid Ruby syntax)
  </acceptance_criteria>
  <done>Four packaging template files exist with correct placeholder tokens, URL patterns matching Phase 46 artifact naming, and valid syntax.</done>
</task>

</tasks>

<verification>
- `ruby -c packaging/homebrew/agenthub.rb.template` exits 0
- All 4 files exist in correct directories
- Homebrew template URL contains `v#{version}` (not `#{version}` without v prefix)
- WinGet installer URL contains `v{{VERSION}}` (not `{{VERSION}}` without v prefix)
- WinGet manifests all specify `ManifestVersion: 1.12.0`
</verification>

<success_criteria>
- packaging/homebrew/agenthub.rb.template is a syntactically valid Ruby file with {{VERSION}} and {{SHA256}} placeholders
- packaging/winget/manifests/ contains 3 YAML files (version, installer, locale) at schema 1.12.0
- All URLs match Phase 46 artifact naming convention exactly
</success_criteria>

<output>
After completion, create `.planning/phases/47-homebrew-tap-packaging-templates/47-01-SUMMARY.md`
</output>
