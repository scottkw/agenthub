---
phase: 72
slug: ui-contrast-improvement
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-14
---

# Phase 72 — UI Design Contract

> Visual and interaction contract for Phase 72: UI Contrast Improvement.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — pure CSS (single `frontend/src/style.css`) |
| Icon library | none — inline SVG / Unicode glyphs in JSX |
| Font | "Cascadia Code", "MesloLGS NF", "Fira Code", monospace (system fallback) |

> Source: RESEARCH.md (Standard Stack), `style.css` line 53.
> No Tailwind, no shadcn, no CSS modules — all tokens are hardcoded hex values in one file.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon padding, tight inline gaps |
| sm | 8px | Compact element gaps, field group bottom margin, status bar padding |
| md | 16px | Default section padding, modal body horizontal gutter |
| lg | 24px | Section-level vertical rhythm, panel padding |
| xl | 32px | Welcome tab section breaks |
| 2xl | 48px | Welcome tab content top/bottom padding |
| 3xl | 64px | Not used in this phase |

Exceptions: Tab bar height is 42px (not a multiple of 8). Status bar height is 32px. These are pre-existing values — do not change them in Phase 72.

> Source: `style.css` direct inspection. This phase makes no spacing changes — all spacing values are carried forward as-is.

---

## Typography

All font sizes are pre-existing in `style.css`. This phase makes no typography changes — only `color:` property values change. Declared here for reference so executor does not accidentally alter sizes.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body / general UI | 13px | 400 (regular) | 1.5 (explicit where set, inherited otherwise) |
| Small / meta / labels | 12px | 400 (regular) | 1.5 |
| Uppercase section labels | 11px | 600 (semibold) | 1.2 |
| Modal title / panel title | 16px | 600 (semibold) | 1.2 |

> Source: RESEARCH.md contrast audit + `style.css` font-size extraction.
> Uppercase 11px labels (`.welcome-tab__heading`, `.tab-status-bar__state`, `.new-session-modal__section-label`, `.settings-panel__body h3`) are small-caps style — they do NOT qualify as WCAG "large text" and require the full 4.5:1 threshold.

---

## Color

### Surface Palette (no changes in this phase)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#1a1b26` | Main area background (terminal container, welcome tab, daemon panel) |
| Secondary (30%) | `#16161e` | Tab bar, sidebar, status bar, code blocks |
| Tertiary | `#1e2030` | Cards, modals, settings panel body, context menus |
| Border / separator | `#292e42` | All rule separators and borders |
| Hover surface | `#3b4261` | Interactive element hover backgrounds |

### Text Palette — BEFORE and AFTER

This is the core change for Phase 72. Only `color:` properties on text-content elements change.

| Color | Hex | Contrast on `#16161e` | Contrast on `#1a1b26` | Contrast on `#1e2030` | Status |
|-------|-----|-----------------------|-----------------------|-----------------------|--------|
| Bright primary | `#c0caf5` | 10.59:1 | — | — | PASS — no change |
| Primary labels | `#a9b1d6` | 8.52:1 | 8.10:1 | 7.63:1 | PASS — no change |
| Sidebar labels | `#9aa5ce` | 7.41:1 | 7.04:1 | 6.63:1 | PASS — PROMOTION TARGET |
| Muted (failing) | `#565f89` | 2.91:1 | 2.76:1 | 2.60:1 | **FAIL — replace on text `color:` only** |
| Accent blue | `#7aa2f7` | — | — | — | PASS — links, focus rings, CTAs |
| Success green | `#9ece6a` | — | — | — | PASS — status ON |
| Error red | `#f7768e` | — | — | — | PASS — destructive hover, error text |

### Exact Color Changes Required

Replace `#565f89` with `#9aa5ce` on the following CSS rules (`color:` property only):

| CSS Selector | Property | Before | After | Background | New Ratio |
|---|---|---|---|---|---|
| `.tab { color }` | color | `#565f89` | `#9aa5ce` | `#16161e` | 7.41:1 |
| `.tab__close { color }` | color | `#565f89` | `#9aa5ce` | `#16161e` | 7.41:1 |
| `.tab-status-bar { color }` | color | `#565f89` | `#9aa5ce` | `#16161e` | 7.41:1 |
| `.tab-status-bar__state--off { color }` | color | `#565f89` | `#9aa5ce` | `#16161e` | 7.41:1 |
| `.settings-panel__body h3 { color }` | color | `#565f89` | `#9aa5ce` | `#1e2030` | 6.63:1 |
| `.settings-panel__description { color }` | color | `#565f89` | `#9aa5ce` | `#1e2030` | 6.63:1 |
| `.settings-panel__empty { color }` | color | `#565f89` | `#9aa5ce` | `#1e2030` | 6.63:1 |
| `.settings-panel__table th { color }` | color | `#565f89` | `#9aa5ce` | `#1e2030` | 6.63:1 |
| `.settings-panel__url { color }` | color | `#565f89` | `#9aa5ce` | `#1e2030` | 6.63:1 |
| `.welcome-tab__version { color }` | color | `#565f89` | `#9aa5ce` | `#1a1b26` | 7.04:1 |
| `.welcome-tab__heading { color }` | color | `#565f89` | `#9aa5ce` | `#1a1b26` | 7.04:1 |
| `.new-session-modal__section-label { color }` | color | `#565f89` | `#9aa5ce` | `#1e2030` | 6.63:1 |

### Color Rules That Must NOT Change

The following selectors use `#565f89` as a decorative, border, or intentionally-muted value. Do not touch them:

| CSS Selector | Property | Reason |
|---|---|---|
| `border-right: 1px solid #292e42` | border | Not text — decorative separator |
| `.settings-panel__btn--cancel { color }` | color | Ghost button — user can see it; acceptable if hover fixes it. Include in fix. |
| `.new-session-modal__btn--close { color }` | color | Ghost button — Include in fix. |
| `.update-banner__btn--dismiss { color }` | color | Ghost button — Include in fix. |
| `.new-session-modal__folder-display { color }` | color | Placeholder/empty state — Include in fix (shows path). |
| `.daemon-panel__count { color }` | color | Count badge — Include in fix. |
| `.tab-status-bar__state--inactive { color: #414868 }` | color | Intentionally invisible "closed session" — 1.72:1, acceptable per design intent (session is gone, not a readable label). Do NOT change. |

> Source: RESEARCH.md "Pitfall 1: Replacing #565f89 Where It Is Not Text" + direct `style.css` inspection.

### Accent Reserved For

`#7aa2f7` (TokyoNight blue) is reserved for: active tab underline border, focus rings on inputs/selects, CLI name text in settings table, links in status bar and welcome tab, primary CTA button background, "Download" and "Create Session" button backgrounds.

`#9ece6a` (green) is reserved for: status-on badge, Tailscale connected dot, check mark icon.

`#f7768e` (red) is reserved for: error/destructive text, close-button hover color.

---

## Copywriting Contract

This phase has no new UI copy. All affected text is pre-existing. Defined here so executor does not accidentally change copy while editing color values.

| Element | Copy |
|---------|------|
| Primary CTA (New Session modal) | "Create Session" |
| Primary CTA (Settings) | "Save Paths" |
| Empty state (daemon panel) | Pre-existing copy — do not change |
| Error state (settings paths) | Pre-existing copy in `.settings-panel__error` — do not change |
| Destructive confirmation | No destructive actions introduced in this phase |

> Source: REQUIREMENTS.md (UI-01 scope), `style.css` direct inspection.
> Phase 72 makes zero copy changes. The contract is "preserve all existing copy exactly."

---

## Interaction Contract

No new interactions are introduced. Pre-existing hover states remain unchanged. The following hover-state color upgrades are already in the codebase and must be preserved:

| Element | Default color (after fix) | Hover color |
|---------|--------------------------|-------------|
| `.tab` | `#9aa5ce` | `#a9b1d6` |
| `.tab__close` | `#9aa5ce` | `#f7768e` |
| `.sidebar__item` | `#9aa5ce` (no change) | `#c0caf5` |
| `.settings-panel__btn--cancel` | `#9aa5ce` | `#c0caf5` |
| `.new-session-modal__btn--close` | `#9aa5ce` | `#c0caf5` |
| `.update-banner__btn--dismiss` | `#9aa5ce` | `#c0caf5` |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn |
| third-party | none | not applicable |

No new dependencies. No package installs. No registry components.

---

## Test Contract

Tests live in `frontend/src/components/__tests__/style.contrast.test.ts` (Wave 0 gap — must be created).

| Assertion | Method | Command |
|-----------|--------|---------|
| No failing `#565f89` text-color rule remains on any of the 12 target selectors | CSS source regex inspection | `cd /Users/ken/dev/agenthub/frontend && pnpm test -- style.contrast` |
| `#9aa5ce` achieves >= 4.5:1 on `#16161e`, `#1a1b26`, `#1e2030` | WCAG formula embedded in test | same |
| `.tab-status-bar__state--inactive` color is NOT `#9aa5ce` (must stay dim) | CSS source regex | same |

WCAG formula to embed in test file:

```typescript
function sRGB(c: number): number {
  c = c / 255
  return c <= 0.04045 ? c / 12.92 : Math.pow((c + 0.055) / 1.055, 2.4)
}
function relativeLuminance(hex: string): number {
  const r = parseInt(hex.slice(1, 3), 16)
  const g = parseInt(hex.slice(3, 5), 16)
  const b = parseInt(hex.slice(5, 7), 16)
  return 0.2126 * sRGB(r) + 0.7152 * sRGB(g) + 0.0722 * sRGB(b)
}
function contrastRatio(fg: string, bg: string): number {
  const L1 = relativeLuminance(fg)
  const L2 = relativeLuminance(bg)
  return (Math.max(L1, L2) + 0.05) / (Math.min(L1, L2) + 0.05)
}
```

> Source: RESEARCH.md "Pattern 2: WCAG Contrast Calculation" and "Validation Architecture".

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
