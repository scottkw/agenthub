---
phase: 72-ui-contrast-improvement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/__tests__/style.contrast.test.ts
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Contrast test file exists and runs in vitest"
    - "Tests assert that #9aa5ce passes 4.5:1 on all three app backgrounds"
    - "Tests assert that all 12 core failing selectors no longer use #565f89 as text color"
    - "Tests fail (RED) against current style.css because #565f89 is still present"
  artifacts:
    - path: "frontend/src/components/__tests__/style.contrast.test.ts"
      provides: "WCAG AA contrast regression tests for UI-01"
      min_lines: 80
  key_links:
    - from: "frontend/src/components/__tests__/style.contrast.test.ts"
      to: "frontend/src/style.css"
      via: "fs.readFileSync"
      pattern: "readFileSync.*style\\.css"
---

<objective>
Create the WCAG contrast regression test file that verifies all GUI text color replacements meet AA standards.

Purpose: Establishes the automated safety net before any CSS changes. Tests will initially fail (RED state) against current style.css, confirming they correctly detect the #565f89 contrast failures. Plan 02 will make them pass.

Output: `frontend/src/components/__tests__/style.contrast.test.ts` — a complete test file covering all UI-01 contrast assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-ui-contrast-improvement/72-RESEARCH.md
@.planning/phases/72-ui-contrast-improvement/72-PATTERNS.md
@.planning/phases/72-ui-contrast-improvement/72-UI-SPEC.md

<interfaces>
<!-- Analog test file pattern — executor must replicate this structure exactly -->

From frontend/src/components/__tests__/style.settings.test.ts:
```typescript
import { describe, it, expect } from 'vitest'
import { readFileSync } from 'fs'
import { resolve } from 'path'

const css = readFileSync(resolve(__dirname, '../../style.css'), 'utf-8')

describe('group name', () => {
  it('does NOT contain failing pattern', () => {
    expect(css).not.toContain('.some-class')
  })
  it('contains required pattern', () => {
    expect(css).toContain('.some-class')
  })
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WCAG contrast test file with all UI-01 assertions</name>
  <files>frontend/src/components/__tests__/style.contrast.test.ts</files>
  <read_first>
    - frontend/src/components/__tests__/style.settings.test.ts
    - frontend/src/style.css
  </read_first>
  <action>
Create `frontend/src/components/__tests__/style.contrast.test.ts` following the exact pattern from `style.settings.test.ts`.

**File structure (in this exact order):**

1. Imports: `import { describe, it, expect } from 'vitest'` and `import { readFileSync } from 'fs'` and `import { resolve } from 'path'`

2. Comment header: `// UI-01: WCAG AA contrast regression tests for non-terminal GUI text.`

3. CSS load: `const css = readFileSync(resolve(__dirname, '../../style.css'), 'utf-8')`

4. WCAG helper functions (embed directly, do NOT import):
```typescript
function sRGB(c: number): number {
  c = c / 255
  return c <= 0.04045 ? c / 12.92 : Math.pow((c + 0.055) / 1.055, 2.4)
}
function relativeLuminance(hex: string): number {
  const r = parseInt(hex.slice(1, 3), 16)
  const g = parseInt(hex.slice(3, 5), 16)
  const b = parseInt(hex.slice(5, 7), 16)
  return 0.2126 * sRGB(r) + 0.7152 * sRGB(g) + 0.0722 * sRGB(b)
}
function contrastRatio(fg: string, bg: string): number {
  const L1 = relativeLuminance(fg)
  const L2 = relativeLuminance(bg)
  return (Math.max(L1, L2) + 0.05) / (Math.min(L1, L2) + 0.05)
}
```

5. **Describe block: "UI-01: replacement color passes WCAG AA on all backgrounds"**
   - Test: `#9aa5ce` achieves >= 4.5 on `#16161e` (sidebar/tab-bar bg)
   - Test: `#9aa5ce` achieves >= 4.5 on `#1a1b26` (main area bg)
   - Test: `#9aa5ce` achieves >= 4.5 on `#1e2030` (settings panel bg)

6. **Describe block: "UI-01: tab bar contrast — no failing #565f89 text color"**
   Use `expect(css).not.toMatch(regex)` with selector-scoped regex (NOT plain `toContain`):
   - `.tab` rule does not use `#565f89` for color: regex `/\.tab\s*\{[^}]*color:\s*#565f89/`
   - `.tab__close` rule does not use `#565f89`: regex `/\.tab__close\s*\{[^}]*color:\s*#565f89/`
   - `.tab-status-bar` rule does not use `#565f89`: regex `/\.tab-status-bar\s*\{[^}]*color:\s*#565f89/`
   - `.tab-status-bar__state--off` does not use `#565f89`: regex `/\.tab-status-bar__state--off\s*\{[^}]*color:\s*#565f89/`

7. **Describe block: "UI-01: settings panel contrast — no failing #565f89 text color"**
   - `.settings-panel__body h3` does not use `#565f89`: regex `/\.settings-panel__body\s+h3\s*\{[^}]*color:\s*#565f89/`
   - `.settings-panel__description` does not use `#565f89`: regex `/\.settings-panel__description\s*\{[^}]*color:\s*#565f89/`
   - `.settings-panel__empty` does not use `#565f89`: regex `/\.settings-panel__empty\s*\{[^}]*color:\s*#565f89/`
   - `.settings-panel__table th` does not use `#565f89`: regex `/\.settings-panel__table\s+th\s*\{[^}]*color:\s*#565f89/`
   - `.settings-panel__url` does not use `#565f89`: regex `/\.settings-panel__url\s*\{[^}]*color:\s*#565f89/`

8. **Describe block: "UI-01: welcome tab contrast — no failing #565f89 text color"**
   - `.welcome-tab__version` does not use `#565f89`: regex `/\.welcome-tab__version\s*\{[^}]*color:\s*#565f89/`
   - `.welcome-tab__heading` does not use `#565f89`: regex `/\.welcome-tab__heading\s*\{[^}]*color:\s*#565f89/`

9. **Describe block: "UI-01: modal contrast — no failing #565f89 text color"**
   - `.new-session-modal__section-label` does not use `#565f89`: regex `/\.new-session-modal__section-label\s*\{[^}]*color:\s*#565f89/`

10. **Describe block: "UI-01: intentionally-dim elements preserved"**
    - `.tab-status-bar__state--inactive` STILL uses `#414868` (must NOT be changed to `#9aa5ce`): `expect(css).toMatch(/\.tab-status-bar__state--inactive\s*\{[^}]*color:\s*#414868/)`

**IMPORTANT:** The selector-scoped negative assertions (steps 6-9) WILL FAIL against the current style.css because `#565f89` is still present. This is correct (RED state). Plan 02 will fix the CSS and turn them green.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- style.contrast 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `style.contrast.test.ts` exists at `frontend/src/components/__tests__/style.contrast.test.ts`
    - File contains `import { describe, it, expect } from 'vitest'`
    - File contains `readFileSync(resolve(__dirname, '../../style.css'), 'utf-8')`
    - File contains `function contrastRatio`
    - File contains `function relativeLuminance`
    - File contains `function sRGB`
    - File contains `contrastRatio('#9aa5ce', '#16161e')`
    - File contains `contrastRatio('#9aa5ce', '#1a1b26')`
    - File contains `contrastRatio('#9aa5ce', '#1e2030')`
    - File contains `.toBeGreaterThanOrEqual(4.5)`
    - File contains regex patterns for `.tab`, `.tab__close`, `.tab-status-bar`, `.settings-panel__body`, `.settings-panel__description`, `.settings-panel__empty`, `.settings-panel__table`, `.settings-panel__url`, `.welcome-tab__version`, `.welcome-tab__heading`, `.new-session-modal__section-label`
    - File contains test for `.tab-status-bar__state--inactive` preserving `#414868`
    - The 3 contrast-ratio tests PASS (math is correct regardless of CSS state)
    - The 12 selector-scoped negative assertions FAIL (confirming RED state — #565f89 still in CSS)
    - The 1 inactive-state preservation test PASSES
  </acceptance_criteria>
  <done>Test file created, vitest recognizes it, contrast-ratio math tests pass, selector assertions correctly fail against unmodified style.css (RED state confirmed)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates a test file only — no user input, no network, no data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | N/A | style.contrast.test.ts | accept | Pure test file with fs.readFileSync on local CSS — no attack surface. No user input processed, no output rendered, no network calls. |
</threat_model>

<verification>
- `cd /Users/ken/dev/agenthub/frontend && pnpm test -- style.contrast` runs and produces output
- Contrast ratio math tests (3) pass
- Selector assertions (12) fail against current CSS (RED state)
- Inactive-state preservation test (1) passes
</verification>

<success_criteria>
- Test file exists and is recognized by vitest
- All WCAG formula assertions pass (proving the math is correct)
- All selector-scoped assertions fail against current style.css (proving they detect the problem)
- Total test count: 16 (3 contrast math + 12 selector checks + 1 preservation check)
</success_criteria>

<output>
After completion, create `.planning/phases/72-ui-contrast-improvement/72-01-SUMMARY.md`
</output>
