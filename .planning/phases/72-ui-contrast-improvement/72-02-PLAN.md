---
phase: 72-ui-contrast-improvement
plan: 02
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - frontend/src/style.css
autonomous: false
requirements:
  - UI-01

must_haves:
  truths:
    - "Inactive tab text is clearly legible against #16161e background"
    - "Tab close button is visible against #16161e background"
    - "Status bar text is readable against #16161e background"
    - "Settings section headers and descriptions are legible against #1e2030 background"
    - "Welcome tab version and heading text is readable against #1a1b26 background"
    - "New session modal labels are legible against #1e2030 background"
    - "All ghost buttons, empty states, and labels across daemon panel, remote panel, banners, and web server settings are legible"
    - "The intentionally-dim .tab-status-bar__state--inactive remains unchanged at #414868"
  artifacts:
    - path: "frontend/src/style.css"
      provides: "All GUI text color declarations meeting WCAG AA 4.5:1"
      contains: "color: #9aa5ce"
  key_links:
    - from: "frontend/src/components/__tests__/style.contrast.test.ts"
      to: "frontend/src/style.css"
      via: "fs.readFileSync regex assertions"
      pattern: "not\\.toMatch.*#565f89"
---

<objective>
Replace all failing `color: #565f89` text declarations in style.css with `color: #9aa5ce` to meet WCAG AA 4.5:1 contrast on every app background.

Purpose: Fixes GitHub #23 (Brighter UI). Every non-terminal, non-decorative text element that currently uses the failing muted color gets upgraded to a palette-consistent replacement that passes on all three backgrounds (#16161e, #1a1b26, #1e2030).

Output: Updated `frontend/src/style.css` with all 30 text-color declarations changed from `#565f89` to `#9aa5ce`. All contrast tests from Plan 01 turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-ui-contrast-improvement/72-RESEARCH.md
@.planning/phases/72-ui-contrast-improvement/72-PATTERNS.md
@.planning/phases/72-ui-contrast-improvement/72-UI-SPEC.md

<interfaces>
<!-- The 30 CSS selectors with `color: #565f89` that must change to `color: #9aa5ce` -->
<!-- The 1 selector that must NOT change: .tab-status-bar__state--inactive { color: #414868 } -->
<!-- All border-color, border, background-color, background uses of #565f89 must NOT change -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace #565f89 text color with #9aa5ce in tab bar and status bar selectors</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css
    - .planning/phases/72-ui-contrast-improvement/72-PATTERNS.md
  </read_first>
  <action>
In `frontend/src/style.css`, change `color: #565f89;` to `color: #9aa5ce;` on the following lines (line numbers are approximate — match by selector context, not line number):

**Tab bar group (background: #16161e):**
1. Line ~109: `.tab { ... color: #565f89; }` -> `color: #9aa5ce;`
2. Line ~152: `.tab__close { ... color: #565f89; }` -> `color: #9aa5ce;`
3. Line ~270: `.tab-status-bar { ... color: #565f89; }` -> `color: #9aa5ce;`
4. Line ~282: `.tab-status-bar__state--off { color: #565f89; }` -> `color: #9aa5ce;`

**DO NOT CHANGE:**
- `.tab-status-bar__state--inactive { color: #414868; }` — this is intentionally dim for closed sessions
- `.tab-status-bar__state--on { color: #9ece6a; }` — green, already passes
- Any `border-right`, `border-top`, `background-color`, or `background` property that uses `#565f89` or any other color

After editing, run: `cd /Users/ken/dev/agenthub/frontend && pnpm test -- style.contrast` to verify the tab-bar assertions flip from FAIL to PASS.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- style.contrast 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `style.css` contains `.tab {` block with `color: #9aa5ce` (not `#565f89`)
    - `style.css` contains `.tab__close {` block with `color: #9aa5ce` (not `#565f89`)
    - `style.css` contains `.tab-status-bar {` block with `color: #9aa5ce` (not `#565f89`)
    - `style.css` contains `.tab-status-bar__state--off` with `color: #9aa5ce` (not `#565f89`)
    - `style.css` still contains `.tab-status-bar__state--inactive` with `color: #414868` (unchanged)
    - Tab-bar contrast tests in style.contrast.test.ts pass
  </acceptance_criteria>
  <done>All 4 tab-bar/status-bar selectors updated, inactive state preserved, tab-bar contrast tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Replace #565f89 text color with #9aa5ce in settings panel, welcome tab, and all remaining selectors</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css
    - .planning/phases/72-ui-contrast-improvement/72-PATTERNS.md
  </read_first>
  <action>
In `frontend/src/style.css`, change `color: #565f89;` to `color: #9aa5ce;` on ALL of the following selectors. Match by selector name, not line number.

**Settings panel group (background: #1e2030):**
5. `.settings-panel__body h3 { ... color: #565f89; }` -> `color: #9aa5ce;`
6. `.settings-panel__empty { ... color: #565f89; }` -> `color: #9aa5ce;`
7. `.settings-panel__table th { ... color: #565f89; }` -> `color: #9aa5ce;`
8. `.settings-panel__description { ... color: #565f89; }` -> `color: #9aa5ce;`
9. `.settings-panel__url { ... color: #565f89; }` -> `color: #9aa5ce;`
10. `.settings-panel__btn--cancel { ... color: #565f89; }` -> `color: #9aa5ce;`

**Welcome tab group (background: #1a1b26):**
11. `.welcome-tab__version { ... color: #565f89; }` -> `color: #9aa5ce;`
12. `.welcome-tab__heading { ... color: #565f89; }` -> `color: #9aa5ce;`

**New session modal group (background: #1e2030):**
13. `.new-session-modal__section-label { ... color: #565f89; }` -> `color: #9aa5ce;`
14. `.new-session-modal__close { ... color: #565f89; }` -> `color: #9aa5ce;`
15. `.new-session-modal__folder-display { ... color: #565f89; }` -> `color: #9aa5ce;`
16. `.new-session-modal__btn--close { ... color: #565f89; }` -> `color: #9aa5ce;`

**Update banner group (background: #1e2030):**
17. `.update-banner__arrow { ... color: #565f89; }` -> `color: #9aa5ce;`
18. `.update-banner__btn--dismiss { ... color: #565f89; }` -> `color: #9aa5ce;`

**Daemon panel group (background: #1a1b26):**
19. `.daemon-panel__empty { ... color: #565f89; }` -> `color: #9aa5ce;`
20. `.daemon-panel__count { ... color: #565f89; }` -> `color: #9aa5ce;`
21. `.daemon-panel__cli { ... color: #565f89; }` -> `color: #9aa5ce;`
22. `.daemon-panel__hostname { ... color: #565f89; }` -> `color: #9aa5ce;`

**Remote panel group (background: #1a1b26):**
23. `.remote-panel__loading` (the centered flex container with `color: #565f89`) -> `color: #9aa5ce;`
24. `.remote-panel__empty-title { ... color: #565f89; }` -> `color: #9aa5ce;`
25. `.remote-panel__empty-body { ... color: #565f89; }` -> `color: #9aa5ce;`
26. `.remote-panel__peer-header { ... color: #565f89; }` -> `color: #9aa5ce;`
27. `.remote-panel__peer-meta { ... color: #565f89; }` -> `color: #9aa5ce;`
28. `.remote-panel__cli { ... color: #565f89; }` -> `color: #9aa5ce;`

**Local network banner (background: #1e2030):**
29. `.local-network-banner__sub { ... color: #565f89; }` -> `color: #9aa5ce;`

**Settings web server (background: #1e2030):**
30. `.settings-web-server__password-label { ... color: #565f89; }` -> `color: #9aa5ce;`
31. `.settings-web-server__credential-hint { ... color: #565f89; }` -> `color: #9aa5ce;`
32. `.settings-web-server__action-btn { ... color: #565f89; }` -> `color: #9aa5ce;`

**DO NOT CHANGE:**
- Any `border-color:`, `border:`, `background-color:`, or `background:` property using `#565f89`
- `.tab-status-bar__state--inactive { color: #414868; }` — must stay at `#414868`
- Any property that does NOT use `#565f89` as its value

**VERIFICATION after edit:** Run `grep -n 'color: #565f89' frontend/src/style.css` — the output should be EMPTY (zero remaining instances). Every single `color: #565f89` in the file should now be `color: #9aa5ce`.

Then run: `cd /Users/ken/dev/agenthub/frontend && pnpm test` to verify ALL tests pass (both existing and new contrast tests).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && grep -c 'color: #565f89' src/style.css && echo "FAIL: #565f89 text color still present" || echo "PASS: no #565f89 text color remaining" && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'color: #565f89' frontend/src/style.css` returns 0 (zero remaining instances)
    - `grep -c 'color: #9aa5ce' frontend/src/style.css` returns >= 30 (all replacements applied)
    - `style.css` still contains `color: #414868` on `.tab-status-bar__state--inactive`
    - `style.css` still contains all existing `border` and `background` rules unchanged
    - `cd /Users/ken/dev/agenthub/frontend && pnpm test` exits 0 (all tests pass including style.contrast)
    - `cd /Users/ken/dev/agenthub/frontend && pnpm test -- style.contrast` exits 0 (all 16 contrast tests pass)
  </acceptance_criteria>
  <done>All 30+ text-color declarations changed from #565f89 to #9aa5ce. Zero remaining `color: #565f89` instances in style.css. All contrast tests pass. All existing tests still pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of contrast improvements</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css
  </read_first>
  <action>
Present the user with a visual verification checklist. All automated CSS changes are complete. The user needs to launch the app and confirm that all text is comfortably legible across all app surfaces, and that no borders or backgrounds were inadvertently brightened.

Use dev-browser skill if available to perform automated visual checks. Otherwise, present the manual checklist below.
  </action>
  <what-built>All muted text across the app (tab bar, settings, welcome screen, modals, daemon panel, remote panel, status bar) has been brightened from #565f89 (2.6-2.9:1 failing contrast) to #9aa5ce (6.6-7.4:1 passing contrast). No border colors, backgrounds, or layout were changed.</what-built>
  <how-to-verify>
    1. Launch the app (or use dev-browser if available)
    2. Check the **tab bar**: inactive tab titles and the X close button should be clearly readable (not dim/muddy)
    3. Check the **status bar** at bottom of a terminal tab: text should be legible
    4. Open **Settings** (sidebar): section headers (uppercase text like "APPEARANCE", "PATHS"), descriptions below dropdowns, table headers, and URL labels should all be comfortable to read
    5. Check the **Welcome tab** (Home): version text and section headings should be clearly visible
    6. Click **New Session** in the sidebar: the modal section labels ("CLI", "WORKING DIRECTORY") and the close button should be readable
    7. Check the **Daemon panel** (if visible): count badge, CLI labels, hostname labels should be legible
    8. Check the **Remote panel**: peer headers, meta text, empty state text should be readable
    9. Confirm nothing looks TOO bright (the text should be a medium-bright blue-gray, not white)
    10. Confirm borders and separators still appear subtle (not brightened)
  </how-to-verify>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms all text is comfortably legible and no visual regressions exist</done>
  <resume-signal>Type "approved" if all text is comfortably legible, or describe any elements that look wrong</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure CSS color change — no user input, no data flow, no network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02 | N/A | style.css color changes | accept | CSS color property changes have zero attack surface. No input handling, no data processing, no authentication affected. Changes are purely visual — hex value replacement on static text color declarations. |
</threat_model>

<verification>
- `grep -c 'color: #565f89' frontend/src/style.css` returns 0
- `cd /Users/ken/dev/agenthub/frontend && pnpm test` exits 0 (all tests pass)
- `cd /Users/ken/dev/agenthub/frontend && pnpm test -- style.contrast` — all 16 tests pass
- Visual inspection confirms all text is legible and no borders/backgrounds were altered
</verification>

<success_criteria>
- Zero `color: #565f89` text declarations remain in style.css
- All 30+ text-color declarations now use `#9aa5ce` (WCAG AA 4.5:1+ on all backgrounds)
- `.tab-status-bar__state--inactive` preserved at `#414868`
- All vitest tests pass (both existing and new contrast tests)
- Human visual verification confirms comfortable legibility across all app surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/72-ui-contrast-improvement/72-02-SUMMARY.md`
</output>
