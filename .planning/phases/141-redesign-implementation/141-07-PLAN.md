---
phase: 141-redesign-implementation
plan: 07
type: execute
gap_closure: true
wave: 2
depends_on: [141-06]
files_modified:
  - frontend/src/style.css
autonomous: true
requirements: [RDS-02, RDS-03, RDS-04]
user_setup: []

must_haves:
  truths:
    - "Every surviving surface (Welcome/Home, Hub, terminal/tab/status bar chrome, File Browser, Editor chrome, Settings, Share Modal) applies --hub-font-ui for UI text and --hub-font-mono for code/path/credential text"
    - "Panels, cards, modals, inputs, and the share modal use the comp radii tokens (--hub-radius-sm/md/lg); filter chips/pills use --hub-radius-pill"
    - "Surface chrome consumes the re-valued --hub-* color tokens (no surface renders the old TokyoNight values), so the running app visually matches the comp per surface"
    - "The comp type scale (--hub-font-size-* + --hub-font-weight-emphasis) is applied to headings/body/small text on the restyled surfaces"
    - "prefers-reduced-motion guards remain intact; colorblind-safe D-03 fences (agent-badge, status-state hex) are untouched"
  artifacts:
    - path: "frontend/src/style.css"
      provides: "Surface selectors applying font/radii/type tokens + re-valued color tokens across all surfaces"
      contains: "var(--hub-font-ui)"
  key_links:
    - from: "surface selectors (.sidebar, .tab-bar, .welcome-tab, .file-browser, .settings-panel, .hub-share-modal)"
      to: "--hub-font-ui / --hub-font-mono / --hub-radius-* tokens"
      via: "var() reference"
      pattern: "font-family: var\\(--hub-font"
---

<objective>
Apply the comp design language — defined as tokens in 141-06 — across every surviving surface in
`frontend/src/style.css`. Swap each surface's font-family to `var(--hub-font-ui)` (UI) or
`var(--hub-font-mono)` (code/paths/credentials), apply the comp radii tokens to panels/cards/inputs/
pills, and apply the comp type scale to headings/body/small text. Because 141-06 already re-valued the
`--hub-*` color tokens, the surfaces that already consume those tokens repaint to the comp palette
automatically — this plan ensures fonts, radii, and type scale land everywhere the comp shows them.

Purpose: Fonts + radii are the most visible part of the comp the original phase missed. The color
re-value alone (141-06) is invisible until every surface also adopts the comp font and rounded shapes.

Output: All surface selectors restyled to the comp's typography + shape language, matching c-*.png.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-redesign-implementation/141-DESIGN-GAP.md
@.planning/phases/141-redesign-implementation/141-UI-SPEC.md
@.planning/phases/141-redesign-implementation/141-PATTERNS.md
@.planning/phases/141-redesign-implementation/141-06-SUMMARY.md
@./CLAUDE.md

<canonical_targets>
Per-surface render targets (match these): `agenthub-v4.0-redesign/AgentHub UI redesign/c-welcome.png`
(Welcome/Home), `c-session.png` (terminal + tab + tmux status bar), `c-filebrowser.png` (File Browser),
`c-settings.png` (Settings). Design source CSS: `AgentHub Redesign (standalone).html`.
</canonical_targets>

<scope_guard>
This plan touches ONLY app-chrome surface selectors in frontend/src/style.css (fonts/radii/type).
DO NOT change the terminal / xterm rendering theme (the agent-output color palette) or any of these
test files: TerminalPanel.test.tsx, vtColor.test.ts, MiniPreview.test.tsx, HubBriefingModal.test.tsx —
those are the TERMINAL'S xterm theme, not app-chrome --hub-* tokens. The terminal's monospace
font-family binding (if set in JS/TerminalPanel) is also out of scope unless it is a CSS chrome rule in
style.css. Only app-chrome `--hub-*` selectors and the chrome style.hub/style.contrast/style.redesign
gates are in scope.
</scope_guard>

<interfaces>
<!-- Surface selector blocks in frontend/src/style.css (line ranges from 141-PATTERNS.md; -->
<!-- they shifted slightly after 141-06 added @font-face — re-grep each selector before editing). -->

Surfaces and their selectors:
- Sidebar (Home/Hub/Settings rail): .sidebar, .sidebar__toggle, .sidebar__item, .sidebar__item--active
- S-01 Welcome/Home: .welcome-tab* (headings, body, install-grid code blocks, version string)
- S-03 Terminal/Session: .tab-bar, .tab, .tab--active, .tab__close, .tab-status-bar* (tmux footer)
- S-04 File Browser: .file-browser* (breadcrumb, list rows, column headers, resize dividers)
- S-05 Editor chrome: .editor* / breadcrumb + save controls (mirror File Browser per D-02; --cm-* OUT of scope)
- S-06 Settings: .settings-panel*, .settings-jump-bar*, .settings-search* (h3 headings, labels, toggles, buttons)
- S-07 Share Modal: .hub-share-modal, .hub-share-modal__* (added in 141-04; apply radii + fonts)
- Hub: .hub-card*, .hub__group-sidebar* (already tokenized for color; apply font + radii where comp shows rounding)

Tokens available (from 141-06, both themes):
  --hub-font-ui, --hub-font-mono
  --hub-radius-sm (8px), --hub-radius-md (10px), --hub-radius-lg (11px), --hub-radius-pill (999px)
  --hub-font-size-base (14px), --hub-font-size-sm (12.5px), --hub-font-size-heading (19px),
  --hub-font-weight-emphasis (600)

D-03 fences — DO NOT touch (semantic per-agent + colorblind status hex):
  .tab__agent-badge--*  ,  .tab-status-bar__state--on/off/inactive
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply UI + mono font tokens across all surviving surfaces</name>
  <read_first>
    - frontend/src/style.css (grep each surface selector listed in <interfaces>; note every current
      font-family declaration: `grep -n 'font-family' src/style.css`)
    - .planning/phases/141-redesign-implementation/141-06-SUMMARY.md (confirms --hub-font-ui/--hub-font-mono token names + values shipped)
    - .planning/phases/141-redesign-implementation/141-UI-SPEC.md §Typography (which surfaces are UI vs terminal/mono)
    - <scope_guard> above (xterm terminal theme + its test files are OFF LIMITS)
  </read_first>
  <action>
    In `frontend/src/style.css`, set the font-family on each surviving UI surface to
    `var(--hub-font-ui)` and each code/path/credential context to `var(--hub-font-mono)`.

    UI font (`var(--hub-font-ui)`) — apply to the text-bearing root of each chrome surface so children
    inherit: .sidebar, .welcome-tab (UI copy/headings, NOT the install-command code blocks),
    .tab-bar/.tab, .tab-status-bar (tmux footer labels), .file-browser (breadcrumb, column headers, row
    labels), editor chrome (breadcrumb + save controls), .settings-panel (h3 headings, labels,
    descriptions, buttons), .hub-share-modal (title, labels), .hub-card / .hub__group-sidebar text.

    Mono font (`var(--hub-font-mono)`) — apply to: install-command code blocks in .welcome-tab,
    file paths / breadcrumb path segments in .file-browser, the .hub-share-modal__lan-creds `code`
    elements, and any monospaced credential/URL display. Do NOT change the xterm terminal viewport
    font binding here if it is set in JS/TerminalPanel — only CSS chrome (per <scope_guard>). (If a
    `.xterm`/terminal CSS font-family exists in style.css and the comp shows JetBrains Mono in the
    terminal, set it to var(--hub-font-mono); otherwise leave terminal font to its existing JS theme path.)

    Replace existing literal stacks: the `system-ui, -apple-system, "Segoe UI", sans-serif` and
    `-apple-system, BlinkMacSystemFont, ...` UI stacks → `var(--hub-font-ui)`; the
    `ui-monospace, SFMono-Regular, Menlo, monospace` / `'Cascadia Code', 'Fira Code', monospace` chrome
    code stacks → `var(--hub-font-mono)`. Do NOT alter `font-family: inherit` declarations (they already
    inherit the new token from their styled ancestor).

    Do not touch the D-03 fenced selectors. Do not introduce new colors here (color tokens already
    re-valued in 141-06). Do NOT modify the xterm terminal theme colors or its test files (<scope_guard>).
  </action>
  <verify>
    <automated>cd frontend && grep -q 'font-family: var(--hub-font-ui)' src/style.css && grep -q 'font-family: var(--hub-font-mono)' src/style.css && [ "$(grep -c 'font-family: var(--hub-font' src/style.css)" -ge 8 ] && grep -q '.tab__agent-badge--claude' src/style.css && echo FONTS_APPLIED_OK</automated>
  </verify>
  <acceptance_criteria>
    - ≥8 surface selectors now use var(--hub-font-ui) or var(--hub-font-mono)
    - Welcome, sidebar, tab/status bar, file browser, settings, share modal all reference --hub-font-ui
    - Code blocks / paths / lan-creds reference --hub-font-mono
    - No remaining literal `system-ui, -apple-system` UI stack on a restyled chrome surface (grep clean except inherit)
    - D-03 fences (.tab__agent-badge--*, .tab-status-bar__state--*) still present and unchanged
    - No xterm terminal theme color or its test files touched
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Apply comp radii + type scale across panels, cards, inputs, pills, and headings</name>
  <read_first>
    - frontend/src/style.css (grep current radii + sizes on surfaces:
      `grep -n 'border-radius\|font-size' src/style.css` for the surface selectors)
    - .planning/phases/141-redesign-implementation/141-DESIGN-GAP.md §Shape/spacing (8–11px panels/cards/inputs; 999px pills)
    - agenthub-v4.0-redesign/AgentHub UI redesign/c-settings.png + c-filebrowser.png (rounded panels/inputs reference)
  </read_first>
  <action>
    In `frontend/src/style.css`, apply the comp radii + type scale tokens to the restyled surfaces:

    Radii:
    - Panels / cards / modals (.hub-card, .hub-share-modal, .settings-panel field-groups, .file-browser
      panels, welcome install-grid blocks): `border-radius: var(--hub-radius-md)` (10px) — use
      --hub-radius-lg (11px) for the largest modal/panel containers, --hub-radius-sm (8px) for smaller
      nested blocks, per what the comp shows on that element.
    - Inputs / selects / buttons (.settings-panel__path-input, .settings-panel__btn--*, input/textarea,
      .file-browser inputs): `border-radius: var(--hub-radius-sm)` (8px).
    - Filter chips / pills (.hub-filter__pill, group-sidebar count pills, any rounded status chip):
      `border-radius: var(--hub-radius-pill)` (999px).

    Type scale (apply where the comp differs from current; weight 600 for emphasis):
    - Section headings (.settings-panel h3, modal titles, .welcome-tab headings):
      `font-size: var(--hub-font-size-heading)` (19px), `font-weight: var(--hub-font-weight-emphasis)` (600).
    - Body / labels / button text on chrome surfaces: `font-size: var(--hub-font-size-base)` (14px).
    - Small text (descriptions, status bar labels, badges, version string, breadcrumb separators):
      `font-size: var(--hub-font-size-sm)` (12.5px).

    Keep structural exceptions from 141-UI-SPEC §Spacing (tab height 42px, status bar 32px, group item
    32px, filter pill 28px, 44px touch targets) — change radii/type, not those layout heights. Do not
    introduce new animations; keep all existing prefers-reduced-motion guards intact. Do not change
    color tokens or D-03 fences. Do NOT touch the xterm terminal theme or its test files (<scope_guard>).
  </action>
  <verify>
    <automated>cd frontend && grep -q 'border-radius: var(--hub-radius-pill)' src/style.css && grep -q 'border-radius: var(--hub-radius-md)' src/style.css && grep -q 'border-radius: var(--hub-radius-sm)' src/style.css && grep -q 'font-size: var(--hub-font-size-heading)' src/style.css && pnpm test -- --run src/components/__tests__/style.hub.test.ts && echo RADII_TYPE_OK</automated>
  </verify>
  <acceptance_criteria>
    - --hub-radius-pill applied to filter chips/pills; --hub-radius-md/lg on panels/cards/modals; --hub-radius-sm on inputs/buttons
    - --hub-font-size-heading + --hub-font-weight-emphasis applied to section headings/modal titles
    - --hub-font-size-sm applied to small/descriptive text
    - Structural layout heights (42/32/28/44px) unchanged
    - style.hub.test.ts still passes; no new animation introduced outside no-preference guards
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add a CSS-source gate test pinning font/radii/type adoption across surfaces</name>
  <read_first>
    - frontend/src/components/__tests__/style.hub.test.ts (CSS-raw gate test pattern — readFileSync + cssRaw.toContain; mirror this exactly)
    - frontend/src/style.css (the selectors just edited — assertion targets)
  </read_first>
  <action>
    Create `frontend/src/components/__tests__/style.redesign.test.ts` mirroring the style.hub.test.ts
    pattern (readFileSync style.css into `cssRaw`, assert with toContain / regex). Assert the comp
    design language is wired at source level (the gate the original false-pass lacked):
    - `@font-face` rules exist and reference `../assets/fonts/` (no fonts.googleapis/gstatic)
    - `--hub-font-ui` and `--hub-font-mono` declared in both :root and [data-ui-theme="light"]
    - Dark `--hub-bg: #14151b`, `--hub-surface: #16181f`, `--hub-surface-elevated: #1c1e28` (comp palette)
    - `--hub-accent: #7aa2f7` present AND `#7C8CFF` absent (locked blue, violet rejected)
    - `--hub-success: #4ade80`, `--hub-warning: #fbbf24` (comp semantics)
    - At least N surface selectors use `font-family: var(--hub-font-ui|mono)` (assert count ≥ 8 via regex match)
    - `--hub-radius-pill` and `--hub-radius-md` are consumed by at least one selector each
    - D-03 fences still present: `.tab__agent-badge--claude` and `.tab-status-bar__state--on`
    This test becomes the permanent regression guard so a future "tokens exist but values reverted"
    cannot silently pass again.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run src/components/__tests__/style.redesign.test.ts && echo REDESIGN_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - style.redesign.test.ts exists and all assertions pass
    - Test fails if any of: @font-face removed, font tokens missing from a theme block, comp bg/surface values reverted to TokyoNight, accent changed to violet, success/warning reverted, or font-family-on-surface count drops below 8
    - D-03 fence assertions present
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS-only surface restyle; no new input/output boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-07-C | Tampering | colorblind-safe semantics regression | mitigate | Task 3 source gate asserts D-03 fences (.tab__agent-badge--*, .tab-status-bar__state--*) remain hardcoded; no color is changed in this plan (only fonts/radii/type), so no semantic-color signal is weakened. |
| T-141-07-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages (CSS + one vitest file only); no package-legitimacy table required. |
</threat_model>

<verification>
- `pnpm test -- --run src/components/__tests__/style.redesign.test.ts` passes (new source gate)
- `pnpm test -- --run src/components/__tests__/style.hub.test.ts` still passes
- `cd frontend && pnpm build` succeeds (Vite bundles fonts + CSS without error)
- `grep -c 'font-family: var(--hub-font' frontend/src/style.css` ≥ 8
- D-03 fences intact (agent-badge + status-state hex unchanged); xterm terminal theme + its tests untouched
</verification>

<success_criteria>
- Every surviving surface applies --hub-font-ui (UI) / --hub-font-mono (code/paths/creds)
- Comp radii (8–11px panels/cards/inputs, 999px pills) and type scale applied per surface
- Surfaces repaint to the comp palette (via 141-06 token values) — no TokyoNight visuals remain
- New style.redesign.test.ts permanently guards against silent token-value reversion
- D-03 colorblind fences + reduced-motion guards untouched; xterm terminal theme out of scope and unchanged
</success_criteria>

<output>
Create `.planning/phases/141-redesign-implementation/141-07-SUMMARY.md` when done
</output>
</content>
