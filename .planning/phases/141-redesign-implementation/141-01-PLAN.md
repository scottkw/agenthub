---
phase: 141-redesign-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/style.css
  - frontend/src/components/Hub/GroupSidebar.test.tsx
  - frontend/src/components/__tests__/StatusBar.test.tsx
  - frontend/src/components/__tests__/SessionShareModal.test.tsx
autonomous: true
requirements: [RDS-02, RDS-04, CARRY-01, RDS-03]
must_haves:
  truths:
    - "A --hub-text-dim token resolves in both dark (:root) and light ([data-ui-theme=light]) blocks"
    - "GroupSidebar tests assert the post-CARRY-01 ARIA model (no role=listbox/option; aria-pressed on button) and RED against current component"
    - "StatusBar test asserts the new D-11 copy and is RED against current component"
    - "SessionShareModal test asserts .hub-share-modal__header/__body render (S-07 smoke) and is RED against current markup"
  artifacts:
    - path: "frontend/src/style.css"
      provides: "New --hub-text-dim token in both theme blocks (plus optional --hub-toggle-thumb-off)"
      contains: "--hub-text-dim"
    - path: "frontend/src/components/Hub/GroupSidebar.test.tsx"
      provides: "Updated CARRY-01 ARIA assertions"
    - path: "frontend/src/components/__tests__/StatusBar.test.tsx"
      provides: "Updated D-11 copy assertion"
    - path: "frontend/src/components/__tests__/SessionShareModal.test.tsx"
      provides: "S-07 hub-share-modal render smoke assertion"
  key_links:
    - from: "frontend/src/style.css :root block (line 3896)"
      to: "[data-ui-theme=light] block (line 3946)"
      via: "matching --hub-text-dim declaration in both"
      pattern: "--hub-text-dim"
---

<objective>
Establish the shared foundation every later plan in Phase 141 depends on: the new
`--hub-*` token(s) needed by the surface migrations, and the Wave 0 test-assertion
updates (RED tests) that define the contract for the CARRY-01 ARIA fix, the D-11
copy reword, and the S-07 Share Modal CSS gap.

Purpose: Surface-migration plans (02, 03, 04) consume `--hub-text-dim`; it must
exist in both theme blocks first. The component plans (04, 05) must be driven by
tests written first — these tests are intentionally RED until those plans land.

Output: New token declarations in `style.css`; updated assertions in three test
files. This plan does NOT change any component or non-token CSS — it only adds
tokens and rewrites test expectations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-redesign-implementation/141-UI-SPEC.md
@.planning/phases/141-redesign-implementation/141-RESEARCH.md
@.planning/phases/141-redesign-implementation/141-PATTERNS.md
@.planning/phases/141-redesign-implementation/141-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new --hub-* token(s) to both theme blocks</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css lines 3896–3995 (the `:root` dark block and the `[data-ui-theme="light"]` block — verified existing `--hub-scrollbar-hover` at 3918 dark / 3970 light)
    - 141-PATTERNS.md §"New token insertion pattern" (lines 80–87) and §"Shared Patterns → Token Resolution" (lines 683–703)
    - 141-RESEARCH.md §"New Tokens Required" (lines 742–771)
  </read_first>
  <action>
    Add `--hub-text-dim` to BOTH blocks in a single edit: in the `:root` dark block
    (after the existing `--hub-scrollbar-hover: #565f89;` declaration, line 3918)
    add `--hub-text-dim: #565f89;`, and in the `[data-ui-theme="light"]` block
    (after `--hub-scrollbar-hover: #9999b0;`, line 3970) add `--hub-text-dim: #9999b0;`.
    This consolidates the `#545c7e` status-bar hint and `#565f89` breadcrumb separator
    (per RESEARCH §New Tokens Required, assumption A2). Do NOT introduce a separate
    `--hub-toggle-thumb-off` token — per D-03 latitude and RESEARCH A3, reuse
    `--hub-scrollbar-hover` for the settings toggle thumb off-state in plan 03; one
    new token only. Do NOT change any existing token value (the token tables in
    141-UI-SPEC.md are marked DO NOT CHANGE).
  </action>
  <verify>
    <automated>test "$(grep -c -- '--hub-text-dim:' frontend/src/style.css)" -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n -- '--hub-text-dim:' frontend/src/style.css` returns exactly two lines: one inside the `:root` block (dark `#565f89`) and one inside `[data-ui-theme="light"]` (light `#9999b0`).
    - No new `--hub-toggle-thumb-off` declaration is added (`grep -c -- '--hub-toggle-thumb-off' frontend/src/style.css` returns 0).
    - No existing token value in lines 3896–3995 changed (diff touches only the two added lines).
  </acceptance_criteria>
  <done>--hub-text-dim resolves in both dark and light theme blocks; no other token altered.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite GroupSidebar.test.tsx to the CARRY-01 ARIA contract (RED)</name>
  <files>frontend/src/components/Hub/GroupSidebar.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/GroupSidebar.test.tsx (full file — current assertions at lines 206–221, 261–275, 380–413 assert the broken listbox/option pattern)
    - 141-PATTERNS.md §"GroupSidebar.test.tsx — CARRY-01 assertion updates" (lines 507–609) — exact before/after assertion blocks
    - 141-UI-SPEC.md §CARRY-01 (the locked ARIA contract)
  </read_first>
  <action>
    Update the assertions so they describe the target ARIA model from 141-UI-SPEC.md
    §CARRY-01: the `<ul>` has NO `role="listbox"` (assert `role` attribute is null) and
    has `aria-labelledby="hub-group-sidebar-heading"`; each item `<li>` contains an inner
    `<button type="button">` carrying `aria-pressed` (true for the active group, false
    otherwise) — assert against the inner button via `item.querySelector('button')`, not
    the `<li>`. Replace the `aria-selected` assertions with `aria-pressed`. Replace the
    `tabIndex` assertion on `<li>` with one on the inner button (native button default 0).
    Update Enter/Space activation to dispatch on / `.click()` the inner button. Add an
    assertion that `<aside>` exposes `aria-label="Session groups"`. Use the exact
    before/after blocks in 141-PATTERNS.md lines 507–609 as the source of truth. These
    tests are EXPECTED TO FAIL (RED) until plan 05 lands the component change — do not
    touch GroupSidebar.tsx in this plan.
  </action>
  <verify>
    <automated>grep -q "aria-pressed" frontend/src/components/Hub/GroupSidebar.test.tsx && ! grep -q "role').toBe('option'" frontend/src/components/Hub/GroupSidebar.test.tsx; echo $?</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aria-selected" frontend/src/components/Hub/GroupSidebar.test.tsx` returns 0 (all listbox-model assertions removed).
    - The file asserts `aria-pressed` on inner buttons and asserts the `<ul>` `role` is null.
    - The file asserts `<aside>` has `aria-label="Session groups"` and `<ul>` has `aria-labelledby="hub-group-sidebar-heading"`.
    - `cd frontend && pnpm test -- GroupSidebar` runs and the suite is RED (assertions fail against the still-old component) — failure is the expected state for this plan.
  </acceptance_criteria>
  <done>GroupSidebar.test.tsx encodes the CARRY-01 target contract; suite is RED pending the plan 05 component fix.</done>
</task>

<task type="auto">
  <name>Task 3: Update StatusBar + add S-07 Share Modal smoke assertions (RED)</name>
  <files>frontend/src/components/__tests__/StatusBar.test.tsx, frontend/src/components/__tests__/SessionShareModal.test.tsx</files>
  <read_first>
    - frontend/src/components/__tests__/StatusBar.test.tsx lines 60–75 (description at 66, assertion at 70)
    - frontend/src/components/__tests__/SessionShareModal.test.tsx (full file — existing render setup to extend with a header/body smoke assertion)
    - 141-PATTERNS.md §"StatusBar.test.tsx — D-11 assertion update" (lines 634–652) and §"SessionShareModal.tsx — inline style lift" (lines 656–677)
    - 141-RESEARCH.md §"D-11 Copy Fix — Exact Locations" (lines 813–828) and §"S-07: Share Modal" (lines 711–739)
  </read_first>
  <action>
    In StatusBar.test.tsx: change the line-66 test description to reference "Hub card"
    instead of "Sessions tab", and change the line-70 assertion to
    `expect(hint?.textContent).toBe('Share — open the Hub card')` (the exact new copy
    string plan 05 will render). In SessionShareModal.test.tsx: add a smoke test that
    renders the modal in a state where the panel mounts and asserts that an element with
    class `hub-share-modal__header` and one with class `hub-share-modal__body` are present
    in the DOM (these classes have no CSS yet but ARE referenced in the TSX — confirm the
    TSX renders them; if the existing TSX does not yet emit `__header`/`__body` wrappers,
    assert on the `.hub-share-modal` panel container and the `.hub-share-modal__lan-creds`
    block instead, matching what plan 04 will guarantee). Both updated/added assertions are
    EXPECTED RED until plans 05 (copy) and 04 (modal structure) land. Do not modify any
    component file in this plan.
  </action>
  <verify>
    <automated>grep -q "Share — open the Hub card" frontend/src/components/__tests__/StatusBar.test.tsx && grep -Eq "hub-share-modal__(header|body)|hub-share-modal__lan-creds" frontend/src/components/__tests__/SessionShareModal.test.tsx; echo $?</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Sessions tab" frontend/src/components/__tests__/StatusBar.test.tsx` returns 0.
    - StatusBar.test.tsx asserts `toBe('Share — open the Hub card')`.
    - SessionShareModal.test.tsx contains a render-smoke assertion referencing a `.hub-share-modal*` class.
    - `cd frontend && pnpm test -- StatusBar` and `cd frontend && pnpm test -- SessionShareModal` both run; the new assertions are RED pending plans 05/04.
  </acceptance_criteria>
  <done>D-11 copy assertion and S-07 render-smoke assertion are encoded and RED pending the component plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds CSS token declarations and rewrites test expectations only — no input, auth, or network surface is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-01 | Tampering/Disclosure | style.css token block + test files | accept | No data flow, no secrets, no auth/network change. Token values are public theme colors; test files contain no credentials. No new threat introduced. |

No new threats introduced — token-only CSS additions plus test-assertion rewrites; no change to data handling, auth, or network surfaces. No package installs (no supply-chain checkpoint needed).
</threat_model>

<verification>
- `grep -c -- '--hub-text-dim:' frontend/src/style.css` == 2
- The three test files compile under `cd frontend && pnpm exec tsc --noEmit` (RED test runtime failures are acceptable; TS type errors are NOT).
- Run `cd frontend && pnpm exec tsc --noEmit` — must pass (no type errors introduced by test edits).
</verification>

<success_criteria>
- New `--hub-text-dim` token present in both theme blocks.
- GroupSidebar/StatusBar/SessionShareModal tests encode the Phase 141 target contracts and are RED against current components.
- `tsc --noEmit` clean.
</success_criteria>

<output>
Create `.planning/phases/141-redesign-implementation/141-01-SUMMARY.md` when done.
</output>
