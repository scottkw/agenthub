---
phase: 141-redesign-implementation
plan: 04
type: execute
wave: 4
depends_on: ["141-01", "141-02", "141-03"]
files_modified:
  - frontend/src/style.css
  - frontend/src/components/Hub/SessionShareModal.tsx
autonomous: true
requirements: [RDS-02, RDS-04]
must_haves:
  truths:
    - ".hub-share-modal and .hub-share-modal__* CSS rules exist and consume --hub-* tokens (S-07 gap closed)"
    - "SessionShareModal no longer carries hardcoded inline hex colors (color #a9b1d6 / background #16161e lifted to CSS)"
    - "Share modal enter/exit animation is inside prefers-reduced-motion: no-preference with a reduce static fallback"
    - "SessionShareModal render-smoke test (from plan 01) passes"
  artifacts:
    - path: "frontend/src/style.css"
      provides: "New .hub-share-modal* rules (panel, header, title, body, lan-creds) + fade keyframes"
      contains: ".hub-share-modal"
    - path: "frontend/src/components/Hub/SessionShareModal.tsx"
      provides: "Inline color/background hex removed; classes carry the styling"
  key_links:
    - from: "SessionShareModal.tsx .hub-share-modal* class usages"
      to: "new .hub-share-modal* CSS rules"
      via: "className binding resolving to tokenized CSS"
      pattern: "hub-share-modal"
---

<objective>
Close the S-07 gap: add the missing `.hub-share-modal` and `.hub-share-modal__*` CSS
rules to `style.css` (modelled on the `.hub-modal` header/body layout, scaled to a
~480px share modal, fade-only animation), and lift the hardcoded inline hex colors out
of `SessionShareModal.tsx` into those rules so the light theme renders correctly.

Purpose: RDS-02 (Share modal is a surviving surface) and RDS-04 (light/dark parity +
reduced-motion). The interaction model (two toggles, LAN password, SessionSharePanel)
is preserved exactly (D-13 — recolor only). Inline styles must be lifted because they
override CSS and break the light theme (Pitfall 2).

Output: New tokenized `.hub-share-modal*` rules; inline hex removed from the TSX;
the plan-01 render-smoke test passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-redesign-implementation/141-UI-SPEC.md
@.planning/phases/141-redesign-implementation/141-RESEARCH.md
@.planning/phases/141-redesign-implementation/141-PATTERNS.md
@.planning/phases/141-redesign-implementation/141-VALIDATION.md

<interfaces>
From frontend/src/components/Hub/SessionShareModal.tsx (verified file:line):
- The panel uses className `hub-share-modal hub-share-modal--${phase}` where phase is the enter/exit/open state machine value.
- Overlay already uses `hub-modal-overlay` (CSS already exists at style.css 4988–4996; overlay keyframes at 5233–5241) — do NOT add overlay CSS.
- Line 292: `<div className="hub-share-modal__lan-creds" style={{ margin: '8px 0', fontSize: 12, color: '#a9b1d6' }}>` — remove the `color` key only; keep margin/fontSize (structural).
- Line 294: `<code style={{ background: '#16161e', padding: '2px 6px', borderRadius: 3, fontFamily: 'monospace' }}>` — remove all style props; the `.hub-share-modal__lan-creds code` CSS rule supplies them.
- LAN password (`{lanPassword}`) is already rendered today; this plan changes styling only — no new disclosure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .hub-share-modal* CSS rules + fade keyframes (S-07)</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css lines 4999–5100 (`.hub-modal` / `.hub-modal__header` / `.hub-modal__body` analog) and 5219–5273 (hub-modal motion guard) and 4988–4996 (existing hub-modal-overlay rules)
    - 141-PATTERNS.md §"S-07: New .hub-share-modal* CSS rules" (lines 290–390) — exact rule excerpts to copy
    - 141-RESEARCH.md §"S-07: Share Modal" (lines 711–739) and §Motion Contract (lines 842–889)
  </read_first>
  <action>
    Add new rules near the hub-modal block in style.css: `.hub-share-modal` panel
    (`width: min(480px, calc(100vw - 48px))`, `background: var(--hub-surface-elevated)`,
    `border: 1px solid var(--hub-border)`, `border-radius: 12px`, flex column, overflow
    hidden); `.hub-share-modal__header` (height 48px, flex, `border-bottom: 1px solid
    var(--hub-border)`); `.hub-share-modal__title` (14px/600, `color: var(--hub-text-primary)`,
    ellipsis); `.hub-share-modal__body` (flex column, padding 16px, gap 12px, overflow-y
    auto); `.hub-share-modal__lan-creds` (`font-size: 12px; color: var(--hub-text-secondary)`)
    and `.hub-share-modal__lan-creds code` (`background: var(--hub-surface); padding: 2px 6px;
    border-radius: 3px; font-family: ui-monospace, SFMono-Regular, Menlo, monospace`). Declare
    `@keyframes hub-share-modal-in`/`-out` (opacity fade) at root scope; assign
    `.hub-share-modal--entering`/`--exiting` animations INSIDE `@media (prefers-reduced-motion:
    no-preference)`; add a `@media (prefers-reduced-motion: reduce)` block setting
    `.hub-share-modal { animation: none; transition: none; opacity: 1; }`. Copy the exact
    excerpts from 141-PATTERNS.md lines 296–388. Use tokens throughout — NO hardcoded theme
    hex (the only permitted literal is the `rgba(0,0,0,0.6)` box-shadow opacity). Do NOT add
    overlay CSS (`hub-modal-overlay` already exists).
  </action>
  <verify>
    <automated>grep -q '\.hub-share-modal__body' frontend/src/style.css && grep -q '\.hub-share-modal__header' frontend/src/style.css && grep -q 'hub-share-modal-in' frontend/src/style.css; echo $?</automated>
  </verify>
  <acceptance_criteria>
    - `.hub-share-modal`, `.hub-share-modal__header`, `.hub-share-modal__title`, `.hub-share-modal__body`, `.hub-share-modal__lan-creds`, and `.hub-share-modal__lan-creds code` rules all exist.
    - Extracting the new `.hub-share-modal*` rule bodies and grepping for hex returns nothing except the `rgba(0,0,0,...)` box-shadow opacity (every color is a `var(--hub-*)`).
    - `--entering`/`--exiting` animation assignments are inside a `@media (prefers-reduced-motion: no-preference)` block, and a `@media (prefers-reduced-motion: reduce)` block zeroes the animation.
    - No `hub-modal-overlay` rule was added or modified (`grep -c 'hub-modal-overlay {' frontend/src/style.css` unchanged from baseline).
  </acceptance_criteria>
  <done>Tokenized .hub-share-modal* rules + motion-guarded fade exist; S-07 CSS gap closed.</done>
</task>

<task type="auto">
  <name>Task 2: Lift inline hex out of SessionShareModal.tsx</name>
  <files>frontend/src/components/Hub/SessionShareModal.tsx</files>
  <read_first>
    - frontend/src/components/Hub/SessionShareModal.tsx lines 290–298 (LAN creds div + code, verified inline hex at 292/294)
    - 141-PATTERNS.md §"SessionShareModal.tsx — inline style lift" (lines 656–677)
    - 141-RESEARCH.md §"Inline styles to lift" (lines 728–733) and §Pitfall 2 (lines 919–934)
  </read_first>
  <action>
    On line 292, remove the `color: '#a9b1d6'` key from the `style={{...}}` object,
    leaving `style={{ margin: '8px 0', fontSize: 12 }}` (margin/fontSize are structural,
    not theme color — keep them; the `.hub-share-modal__lan-creds` CSS rule from task 1
    supplies the color). On line 294, remove the entire `style={{ background: '#16161e',
    padding: '2px 6px', borderRadius: 3, fontFamily: 'monospace' }}` attribute from the
    `<code>` element so it reads `<code>` — the `.hub-share-modal__lan-creds code` CSS rule
    supplies background/padding/border-radius/font-family. Do NOT change the
    `style={{ cursor: ... }}` attributes on lines 273/315 (cursor is interaction, not color).
    Do NOT alter the modal's interaction logic, toggle handlers, or the `{lanPassword}` render
    condition (D-13 — recolor only; LAN password is already shown today, no new disclosure).
  </action>
  <verify>
    <automated>! grep -nE "#a9b1d6|#16161e" frontend/src/components/Hub/SessionShareModal.tsx; echo $?</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{6}' frontend/src/components/Hub/SessionShareModal.tsx` returns nothing (no inline hex remains).
    - The `.hub-share-modal__lan-creds` div retains its `margin`/`fontSize` inline props; the `<code>` element has no `style` attribute.
    - `cd frontend && pnpm test -- SessionShareModal` passes (the plan-01 render-smoke assertion is now GREEN).
    - The toggle handlers and `{lanPassword}` conditional are byte-identical except for the removed color/background props.
  </acceptance_criteria>
  <done>Inline hex removed from SessionShareModal.tsx; styling driven by the new CSS classes; share-modal test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Share Modal render (LAN password display) | The modal already renders `{lanPassword}` today in local mode; this plan only restyles it. No new value, link, or capability is exposed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-04 | Information Disclosure | SessionShareModal LAN creds block | accept | The LAN password is already displayed in the existing component (unchanged render condition `webServerMode === 'local' && webServerRunning && lanPassword`). This plan changes only CSS/inline-style — it does not widen visibility, add a copy-to-clipboard, or expose the value in any new context. No new disclosure surface. |
| T-141-05 | Tampering | style.css .hub-share-modal* rules | accept | Token-only CSS additions; no script, no data flow. No new threat. |

No new threats introduced — recolor/CSS-gap-fill only; share-modal interaction model and disclosure surface are unchanged (D-13). No package installs.
</threat_model>

<verification>
- `grep -nE '#[0-9a-fA-F]{6}' frontend/src/components/Hub/SessionShareModal.tsx` returns nothing.
- New `.hub-share-modal*` rules contain only `var(--hub-*)` colors (+ rgba shadow).
- `cd frontend && pnpm exec tsc --noEmit && pnpm test` passes; `pnpm test -- SessionShareModal` green.
</verification>

<success_criteria>
- S-07 CSS gap closed with tokenized, motion-guarded `.hub-share-modal*` rules.
- No inline theme hex remains in SessionShareModal.tsx.
- Share modal interaction model unchanged; full frontend gate green.
</success_criteria>

<output>
Create `.planning/phases/141-redesign-implementation/141-04-SUMMARY.md` when done.
</output>
