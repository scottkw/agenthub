---
phase: 141-redesign-implementation
plan: 09
type: execute
gap_closure: true
wave: 3
depends_on: [141-06, 141-07, 141-08]
files_modified:
  - .planning/phases/141-redesign-implementation/141-RENDER-COMPARE.md
autonomous: false
requirements: [RDS-02, RDS-03, RDS-04, CARRY-01]
user_setup: []

must_haves:
  truths:
    - "The running app (wails dev) is rendered and compared side-by-side against the canonical comp per surface (Welcome, Session, File Browser, Settings) — NOT a token-existence check"
    - "Each surface visibly uses Plus Jakarta Sans (UI) + JetBrains Mono (mono), the comp palette, and comp radii — matching c-*.png"
    - "The blue accent is verified at hex level (#7aa2f7 dark / #3d6fe8 light); violet #7C8CFF is absent from source"
    - "The light/dark toggle in Settings → Appearance flips every surface and persists across restart"
    - "vendor-drift / no-CDN gate stays green; colorblind D-03 fences + reduced-motion guards intact"
  artifacts:
    - path: ".planning/phases/141-redesign-implementation/141-RENDER-COMPARE.md"
      provides: "Per-surface app-vs-comp comparison record with captured screenshots and pass/fail per surface"
      contains: "Welcome"
  key_links:
    - from: "rendered app surfaces"
      to: "c-*.png comp targets"
      via: "side-by-side dev-browser capture against wails dev"
      pattern: "c-welcome|c-session|c-filebrowser|c-settings"
---

<objective>
Verify the redesign LANDED by rendering the running app and comparing it surface-by-surface to the
canonical comp — the exact step the original Phase 141 verification skipped (it checked tokens/ARIA at
the source level and never rendered the comp). Run automated source gates (vendor/no-CDN, hex accent,
comp palette, D-03 fences, reduced-motion) AND a human-verified visual match per surface, then record
the result.

Purpose: Close the false-pass. The phase only re-verifies green when a human confirms the app visually
matches c-welcome/c-session/c-filebrowser/c-settings AND the automated gates pass.

Output: 141-RENDER-COMPARE.md with per-surface evidence; phase ready for re-verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-redesign-implementation/141-DESIGN-GAP.md
@.planning/phases/141-redesign-implementation/141-UI-SPEC.md
@.planning/phases/141-redesign-implementation/141-06-SUMMARY.md
@.planning/phases/141-redesign-implementation/141-07-SUMMARY.md
@.planning/phases/141-redesign-implementation/141-08-SUMMARY.md
@./CLAUDE.md

<canonical_targets>
Comp render targets (compare against): `agenthub-v4.0-redesign/AgentHub UI redesign/c-welcome.png`,
`c-session.png`, `c-filebrowser.png`, `c-settings.png`. Design source: `AgentHub Redesign (standalone).html`.
Per CLAUDE.md UAT rule: use the dev-browser skill to drive the live app — do NOT ask the user to
manually navigate. User is colorblind: verify accent/color CORRECTNESS at hex level, ask the human only
about typography/shape/layout fidelity, not "is this the right blue".
</canonical_targets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated source gates — vendor/no-CDN, hex accent, comp palette, fences, motion</name>
  <read_first>
    - frontend/src/components/__tests__/style.redesign.test.ts (the gate added in 141-07 — run it)
    - frontend/src/components/__tests__/style.hub.test.ts (existing light-theme + fence gates)
    - internal/webserver/vendor_drift_test.go (xterm/CodeMirror vendor-drift gate — must stay green)
    - .planning/phases/141-redesign-implementation/141-DESIGN-GAP.md (locked accent + comp palette values to assert)
  </read_first>
  <action>
    Run the full automated gate set and capture results into the report (Task 3). Do not weaken any
    gate; if one fails, the redesign is incomplete — stop and report the specific failure.
    - `cd frontend && pnpm test -- --run src/components/__tests__/style.redesign.test.ts src/components/__tests__/style.hub.test.ts src/components/__tests__/SettingsTab.appearance-theme.test.tsx`
    - `cd frontend && pnpm exec tsc --noEmit -p . && pnpm build` (confirms fonts bundle + app compiles —
      per MEMORY: a green vitest is NOT proof the app builds; run tsc + build)
    - No-CDN / vendor checks: `! grep -rniE 'fonts\.googleapis|fonts\.gstatic|@import +url' frontend/index.html frontend/src/style.css` and `ls frontend/src/assets/fonts/*.woff2`
    - Go vendor-drift gate: `go test ./internal/webserver/ -run 'VendorVersions|VendorDrift'` (stays green)
    - Hex accent + palette greps: `--hub-accent: #7aa2f7` present, `#7C8CFF` absent, `--hub-bg: #14151b`,
      `--hub-success: #4ade80`, D-03 fences `.tab__agent-badge--claude` + `.tab-status-bar__state--on` present.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run src/components/__tests__/style.redesign.test.ts src/components/__tests__/style.hub.test.ts src/components/__tests__/SettingsTab.appearance-theme.test.tsx && pnpm exec tsc --noEmit -p . && ! grep -rniE 'fonts\.googleapis|fonts\.gstatic' index.html src/style.css && grep -q -- '--hub-accent: #7aa2f7' src/style.css && ! grep -q '#7C8CFF' src/style.css && echo GATES_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - All three vitest suites pass; tsc --noEmit + pnpm build succeed
    - No CDN font reference anywhere; ≥5 vendored woff2 present
    - go vendor-drift test green
    - Accent #7aa2f7 present, #7C8CFF absent, comp bg/success values present, D-03 fences intact
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render each surface in the live app and capture app-vs-comp screenshots</name>
  <read_first>
    - ./CLAUDE.md (UAT rule: drive the live app with the dev-browser skill; never delegate manual nav)
    - .planning/phases/141-redesign-implementation/141-UI-SPEC.md §Surface-by-Surface (which surface maps to which c-*.png)
    - frontend/src/components/__tests__ (how the app is launched in this repo — wails dev)
  </read_first>
  <action>
    Launch the app with `wails dev` (or `cd frontend && pnpm dev` if the Wails bridge is unnecessary for
    pure UI surfaces; terminal-bound surfaces may need native webview per MEMORY — note any surface that
    cannot be driven headless). Using the dev-browser skill, navigate to and screenshot each surface in
    DARK theme:
    - Home/Welcome → compare to c-welcome.png
    - A terminal session (tab bar + tmux status bar) → compare to c-session.png
    - File Browser → compare to c-filebrowser.png
    - Settings → compare to c-settings.png
    Then flip Settings → Appearance to LIGHT and screenshot the same four surfaces to confirm every
    surface repaints (no surface stuck dark). Restart the app (or reload) and confirm the light choice
    persisted (data-ui-theme=light re-applied on load).
    Save captured screenshots under
    `agenthub-v4.0-redesign/AgentHub UI redesign/screenshots/141-09/` (dir exists) with surface+theme
    names. For each surface, write an objective delta note: font family visibly Plus Jakarta Sans /
    JetBrains Mono? radii rounded per comp? palette matches the darker/cooler comp? Record findings for
    Task 3. Do NOT judge accent color by eye (user is colorblind) — that is covered by Task 1's hex gate.
  </action>
  <verify>
    <automated>ls "agenthub-v4.0-redesign/AgentHub UI redesign/screenshots/141-09/" | grep -qiE 'welcome|session|filebrowser|settings' && echo SCREENSHOTS_CAPTURED</automated>
  </verify>
  <acceptance_criteria>
    - Dark-theme screenshots captured for all 4 surfaces + their c-*.png counterparts identified
    - Light-theme screenshots confirm all 4 surfaces repaint
    - Persistence confirmed (light survives reload/restart)
    - Per-surface objective notes on font/radii/palette recorded for the report
    - Any surface that cannot be driven headless is explicitly flagged for the human checkpoint
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human visual match — app vs comp per surface</name>
  <action>Human reviews the Task 2 screenshots beside the canonical comp targets and confirms each surface (Welcome, Session, File Browser, Settings) matches the comp on typography, radii, palette, and that the light/dark toggle flips all surfaces and persists. Pauses for approval before the phase can re-verify.</action>
  <what-built>
    The redesign now applies the comp's fonts (Plus Jakarta Sans + JetBrains Mono), the comp color
    palette (darker/cooler), comp radii (rounded panels/cards/inputs, pill chips), the comp type scale,
    and a working Settings → Appearance light/dark toggle. Task 2 captured live app screenshots beside
    each canonical comp (c-welcome / c-session / c-filebrowser / c-settings).
  </what-built>
  <how-to-verify>
    1. Open the screenshots in `agenthub-v4.0-redesign/AgentHub UI redesign/screenshots/141-09/`
       alongside the comp targets `c-welcome.png`, `c-session.png`, `c-filebrowser.png`, `c-settings.png`.
    2. For each surface confirm the app now MATCHES the comp on: typography (Plus Jakarta Sans UI +
       JetBrains Mono code — clearly different from the old system font), rounded corners on panels/
       cards/inputs and pill-shaped filter chips, and the darker/cooler background family.
    3. In the live app, open Settings → Appearance and flip Light/Dark — confirm every surface repaints,
       then restart and confirm the choice persisted.
    4. (Accent/color hue is NOT for you to eyeball — it is hex-verified in Task 1. Judge fonts, shapes,
       spacing, and that light/dark both look right structurally.)
    If a surface still looks like the OLD app (same font, square corners, identical-to-before palette),
    that is a fail — describe which surface and what is off.
  </how-to-verify>
  <resume-signal>Type "approved" if all four surfaces match the comp and the toggle works, or describe the specific surface(s) and mismatch(es) to fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the per-surface comparison result in 141-RENDER-COMPARE.md</name>
  <read_first>
    - .planning/phases/141-redesign-implementation/141-VERIFICATION.md (the false-pass record this supersedes)
  </read_first>
  <action>
    Write `.planning/phases/141-redesign-implementation/141-RENDER-COMPARE.md` recording: the automated
    gate results (Task 1), the captured screenshot paths (Task 2), the human verdict (checkpoint), and a
    per-surface table (Welcome / Session / File Browser / Settings) with columns: font match, radii match,
    palette match, light/dark verified, comp target, pass/fail. Include the accent hex-verification line
    (#7aa2f7 / #3d6fe8 present, #7C8CFF absent) and confirm D-03 fences + reduced-motion guards intact.
    State explicitly that this comparison rendered the running app against the comp (the step the
    original verification skipped). If the human flagged any surface, list it as an open gap with the
    specific fix needed (a follow-up plan, not a silent pass).
  </action>
  <verify>
    <automated>grep -qiE 'Welcome' .planning/phases/141-redesign-implementation/141-RENDER-COMPARE.md && grep -qiE 'File Browser|filebrowser' .planning/phases/141-redesign-implementation/141-RENDER-COMPARE.md && grep -q '7aa2f7' .planning/phases/141-redesign-implementation/141-RENDER-COMPARE.md && echo REPORT_OK</automated>
  </verify>
  <acceptance_criteria>
    - 141-RENDER-COMPARE.md exists with a per-surface pass/fail table referencing the c-*.png targets
    - Automated gate results + screenshot paths + human verdict all recorded
    - Accent hex verification + D-03/motion intact statements present
    - Any human-flagged mismatch recorded as an explicit open gap (never silently passed)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan; no new runtime boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-09-C | Spoofing | false-pass recurrence | mitigate | This plan exists specifically to prevent a token-level false pass: it requires a RENDERED app-vs-comp comparison + a blocking human checkpoint, not just source greps. The original 4/4 cannot recur because the gate now includes screenshots vs c-*.png. |
| T-141-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs; verification + one markdown report only. |
</threat_model>

<verification>
- All automated gates green (Task 1): vitest suites, tsc, build, no-CDN, vendor-drift, hex accent/palette, D-03 fences
- Live screenshots captured for all 4 surfaces in both themes; persistence confirmed
- Human checkpoint approved (visual match to comp on fonts/shape/spacing; toggle works)
- 141-RENDER-COMPARE.md records per-surface pass/fail vs c-*.png
</verification>

<success_criteria>
- The running app is rendered and compared to the canonical comp per surface (the missing step)
- Fonts, palette, radii, type scale visibly match c-welcome/c-session/c-filebrowser/c-settings
- Blue accent hex-verified; violet absent; D-03 + reduced-motion intact; no-CDN vendor gate green
- Light/dark toggle flips all surfaces and persists
- Result recorded; any residual mismatch surfaced as an explicit gap, not a silent pass
</success_criteria>

<output>
Create `.planning/phases/141-redesign-implementation/141-09-SUMMARY.md` when done
</output>
