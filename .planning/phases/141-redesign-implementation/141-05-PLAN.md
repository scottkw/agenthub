---
phase: 141-redesign-implementation
plan: 05
type: execute
wave: 2
depends_on: ["141-01"]
files_modified:
  - frontend/src/components/Hub/GroupSidebar.tsx
  - frontend/src/components/StatusBar.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [CARRY-01, RDS-03]
must_haves:
  truths:
    - "GroupSidebar <ul> has no role=listbox and items have no role=option/aria-selected"
    - "Each GroupSidebar item is an inner <button type=button> with aria-pressed and a count-bearing aria-label; <aside> has aria-label=Session groups"
    - "No 'Sessions tab' copy remains in the frontend; StatusBar hint reads the new D-11 string"
    - "GroupSidebar and StatusBar test suites (updated in plan 01) pass GREEN"
  artifacts:
    - path: "frontend/src/components/Hub/GroupSidebar.tsx"
      provides: "CARRY-01 ARIA model (plain ul + li + button aria-pressed)"
      contains: "aria-pressed"
    - path: "frontend/src/components/StatusBar.tsx"
      provides: "D-11 reworded hint copy"
  key_links:
    - from: "GroupSidebar item button"
      to: "onGroupSelect handler"
      via: "native button onClick (no onKeyDown needed)"
      pattern: "onGroupSelect"
---

<objective>
Two independent component fixes that do not touch `style.css`:
1. CARRY-01 (#97): fix the GroupSidebar ARIA model — drop the mismatched
   `role="listbox"`/`role="option"` pattern in favor of a plain `<ul>` + `<li>` +
   `<button type="button" aria-pressed>` navigation-filter pattern per the locked
   141-UI-SPEC.md §CARRY-01 contract.
2. D-11 (RDS-03): remove/reword all "Sessions tab" copy in the frontend (functional
   string in StatusBar.tsx plus two editorial comments).

Purpose: CARRY-01 (internally consistent ARIA) and RDS-03 (Hub-first copy — the
Sessions page was removed; the copy must not reference it). The test contracts for
both were written in plan 01 (Wave 1) and are RED until this plan turns them GREEN.

Output: Updated GroupSidebar.tsx, StatusBar.tsx, App.tsx comment; suites GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-redesign-implementation/141-UI-SPEC.md
@.planning/phases/141-redesign-implementation/141-RESEARCH.md
@.planning/phases/141-redesign-implementation/141-PATTERNS.md
@.planning/phases/141-redesign-implementation/141-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix GroupSidebar ARIA per CARRY-01 contract</name>
  <files>frontend/src/components/Hub/GroupSidebar.tsx</files>
  <behavior>
    - The `<ul>` (id=SIDEBAR_LIST_ID) has no `role` attribute and carries `aria-labelledby="hub-group-sidebar-heading"`.
    - Each group item is a `<li>` containing an inner `<button type="button">`; the `<li>` is inert (no role, no tabIndex, no handlers).
    - The inner button has `aria-pressed={isActive}` and `aria-label` of the form `"{label} group, {running}/{total} sessions"`.
    - The `<aside>` wrapper has `aria-label="Session groups"`; the heading `<span>` has `id="hub-group-sidebar-heading"`.
    - Activating the button (click or native Enter/Space) calls `onGroupSelect(id)`; drag handlers (onDragOver/onDragLeave/onDrop) move to the button and still fire.
  </behavior>
  <read_first>
    - frontend/src/components/Hub/GroupSidebar.tsx (full file — current `GroupSidebarItem` render at lines 126–173, `<ul>` at 264–268, `<aside>` wrapper, heading `<span>` at ~260, toggle button analog at 235–256)
    - 141-PATTERNS.md §"CARRY-01: GroupSidebar.tsx ARIA fix" (lines 430–503) — exact before/after JSX
    - 141-UI-SPEC.md §CARRY-01 (lines 325–390) — the locked contract incl. the 7 key changes
    - 141-RESEARCH.md §"Pattern 4: CARRY-01 ARIA Contract" (lines 310–378)
  </read_first>
  <action>
    Apply the exact transformation in 141-PATTERNS.md lines 430–503: in `GroupSidebarItem`,
    make the `<li>` inert (remove `role="option"`, `aria-selected`, `tabIndex={0}`, the
    `onClick`, and the `onKeyDown` handler from the `<li>`) and wrap the existing children in
    an inner `<button type="button" className="hub__group-sidebar-item">` that carries
    `aria-pressed={isActive}`, `aria-label={`${label} group, ${counts.running}/${counts.total} sessions`}`,
    `onClick={() => onGroupSelect(id)}`, and the three drag handlers (onDragOver/onDragLeave/
    onDrop) moved from the `<li>`. Keep the `itemClass` active/state class on the appropriate
    element so the existing visuals are unchanged (recolor-only / D-13 — no visual change; the
    `.hub__group-sidebar-item--active` CSS already exists). On the `<ul>` (264–268) remove
    `role="listbox"` and add `aria-labelledby="hub-group-sidebar-heading"`. On the `<aside>`
    wrapper add `aria-label="Session groups"`. Ensure the heading `<span>` has
    `id="hub-group-sidebar-heading"`. Do NOT add roving-tabindex or aria-activedescendant
    (the chosen resolution is plain buttons, not listbox). The "New group" input/button are
    already correct — do not touch them.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- GroupSidebar</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "role=.(listbox|option)|aria-selected" frontend/src/components/Hub/GroupSidebar.tsx` returns nothing.
    - `grep -q 'aria-pressed' frontend/src/components/Hub/GroupSidebar.tsx` and `grep -q 'aria-label="Session groups"' frontend/src/components/Hub/GroupSidebar.tsx` both succeed.
    - `cd frontend && pnpm test -- GroupSidebar` is GREEN (the plan-01 assertions now pass).
    - `cd frontend && pnpm exec tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>GroupSidebar uses the plain-button navigation-filter ARIA model; CARRY-01 test suite green; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Reword D-11 "Sessions tab" copy (StatusBar + App comment)</name>
  <files>frontend/src/components/StatusBar.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/components/StatusBar.tsx lines 13–17 (JSDoc at 15) and 45–52 (functional hint at 49)
    - frontend/src/App.tsx line 710 (code comment referencing "Sessions tab")
    - 141-RESEARCH.md §"D-11 Copy Fix — Exact Locations" (lines 813–828)
    - 141-PATTERNS.md §"StatusBar.tsx — D-11 copy fix" (lines 613–630)
  </read_first>
  <action>
    In StatusBar.tsx: change the functional hint text on line 49 from
    `Share links are on the Sessions tab` to exactly `Share — open the Hub card`
    (must match the plan-01 StatusBar.test.tsx assertion byte-for-byte). Reword the JSDoc
    on line 15 to reference "the Hub card's job (SessionShareModal …)" instead of "the
    Sessions tab's job (SessionSharePanel …)" — editorial only. In App.tsx: reword the
    line-710 comment so it no longer says "Sessions tab" (e.g. "…the removed Sessions page
    showed bogus 'WEB ON' state…") — editorial, non-functional. After these edits, NO
    occurrence of the literal "Sessions tab" may remain anywhere under frontend/src.
  </action>
  <verify>
    <automated>! grep -rn "Sessions tab" frontend/src/ ; echo $?</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Sessions tab" frontend/src/` returns nothing (zero matches across components, tests, and App.tsx).
    - StatusBar.tsx line 49 renders exactly `Share — open the Hub card`.
    - `cd frontend && pnpm test -- StatusBar` is GREEN (the plan-01 assertion now passes).
    - `cd frontend && pnpm exec tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>No "Sessions tab" copy remains; StatusBar hint reworded; StatusBar suite green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | ARIA attribute changes and copy rewording only; no input, auth, network, or data-flow change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-06 | Tampering/Disclosure | GroupSidebar.tsx, StatusBar.tsx, App.tsx | accept | ARIA roles + a copy string + two comments change; no data handling, secret, link, or capability is added or exposed. The reworded hint points to an existing on-screen control (the Hub card), revealing nothing new. No new threat. |

No new threats introduced — ARIA/copy-only changes; no change to data handling, auth, or network surfaces. No package installs.
</threat_model>

<verification>
- `grep -rn "Sessions tab" frontend/src/` returns nothing.
- `cd frontend && pnpm exec tsc --noEmit && pnpm test` passes (GroupSidebar + StatusBar suites GREEN).
</verification>

<success_criteria>
- GroupSidebar ARIA model is internally consistent (plain ul/li/button + aria-pressed); CARRY-01 resolved.
- All "Sessions tab" copy removed/reworded; D-11 satisfied.
- Full frontend gate green.
</success_criteria>

<output>
Create `.planning/phases/141-redesign-implementation/141-05-SUMMARY.md` when done.
</output>
