---
phase: 141-redesign-implementation
plan: 03
type: execute
wave: 3
depends_on: ["141-01", "141-02"]
files_modified:
  - frontend/src/style.css
autonomous: true
requirements: [RDS-02, RDS-04]
must_haves:
  truths:
    - "File Browser + Editor chrome (S-04/S-05) consume --hub-* tokens so light/dark toggle repaints them"
    - "Settings panel/jump-bar/search (S-06) consume --hub-* tokens; save/destructive CTAs use accent/destructive tokens"
    - "Hover/focus transitions on migrated selectors are motion-guarded"
  artifacts:
    - path: "frontend/src/style.css"
      provides: "Tokenized file-browser (2646–3380) and settings (370–771) rules"
      contains: "var(--hub-accent)"
  key_links:
    - from: "file-browser + settings selectors"
      to: "--hub-* token declarations"
      via: "var() reference resolved in both theme blocks"
      pattern: "var\\(--hub-"
---

<objective>
Migrate the second batch of non-Hub surface CSS in `style.css` — the File Browser
and its Editor chrome (S-04/S-05: breadcrumb, status line, file list, preview/editor
chrome) and the Settings surface (S-06: panel, jump bar, search, toggles, buttons) —
from hardcoded hex to the `--hub-*` token system. Per D-02 the editor chrome derives
its restyle from the File Browser surface (same token migration); CodeMirror internal
`--cm-*` theme is out of scope.

Purpose: RDS-02 and RDS-04 for the File Browser, Editor chrome, and Settings.
Recolor-only (D-13).

Output: Tokenized file-browser and settings rules; motion-guarded transitions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-redesign-implementation/141-UI-SPEC.md
@.planning/phases/141-redesign-implementation/141-RESEARCH.md
@.planning/phases/141-redesign-implementation/141-PATTERNS.md
@.planning/phases/141-redesign-implementation/141-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate File Browser + Editor chrome (2646–3380) to tokens</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css lines 2646–3380 (`.file-browser*` breadcrumb, status, list, col, preview, btn rules)
    - 141-RESEARCH.md §"S-04 + S-05: File Browser + Editor Chrome" hex tables (lines 531–629)
    - 141-PATTERNS.md §"S-04 + S-05 File Browser + Editor Chrome block migration" (lines 265–276)
  </read_first>
  <action>
    Migrate every selector in the RESEARCH S-04/S-05 hex tables (lines 537–629)
    selector-by-selector to its `--hub-*` token. Chrome/surface: `#16161e`→
    `var(--hub-surface)`, `#1a1b26`→`var(--hub-bg)`, `#292e42`→`var(--hub-border)`,
    `#1e2030`→`var(--hub-surface-elevated)`; text: `#c0caf5`→`var(--hub-text-primary)`,
    `#a9b1d6`→`var(--hub-text-secondary)`, `#9aa5ce`→`var(--hub-text-muted)`; accent
    `#7aa2f7`→`var(--hub-accent)` (sort col active, selected-row left border, focus-visible
    outlines, divider:hover, breadcrumb-current/segment:hover, primary btn bg); warning
    `#f59e0b`→`var(--hub-warning)` on `.file-browser__truncated-banner` border-left;
    destructive `#f7768e`→`var(--hub-destructive)` on `.file-browser__row-icon-overlay`;
    primary btn text `#1a1b26`→`var(--hub-bg)`. The two breadcrumb-separator / dim values
    (`#565f89`) → `var(--hub-text-dim)` (token from plan 01). Wrap migrated hover/focus
    transitions in the motion guard (141-PATTERNS.md lines 402–426). Editor chrome shares
    the `.file-browser__*` selectors per D-02 — do NOT touch CodeMirror `--cm-*` (out of
    scope; confirmed absent from style.css).
  </action>
  <verify>
    <automated>sed -n '2646,3380p' frontend/src/style.css | grep -E '#[0-9a-fA-F]{3,6}' | grep -v 'rgba(0'; true</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '2646,3380p' frontend/src/style.css | grep -E '#[0-9a-fA-F]{3,6}'` returns only `rgba(0,0,0,...)` shadow opacity (no chrome/text/accent hex).
    - `grep -c 'var(--hub-text-dim)' frontend/src/style.css` increased (breadcrumb separator consumes it).
    - `.file-browser__btn--primary` uses `var(--hub-accent)` bg and `var(--hub-bg)` text.
    - Migrated `.file-browser__*` hover/focus transitions are inside `@media (prefers-reduced-motion: no-preference)` with a `reduce` fallback.
  </acceptance_criteria>
  <done>File Browser + Editor chrome fully tokenized; motion-guarded; no chrome hex remains in range.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Settings panel/jump-bar/search/toggles (370–771) to tokens</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css lines 370–771 (`.settings-panel*`, `.settings-jump-bar*`, `.settings-search*`)
    - 141-RESEARCH.md §"S-06: Settings" hex table (lines 633–707), incl. the toggle-thumb-off note (lines 704–707)
    - 141-PATTERNS.md §"S-06 Settings Panel block migration" (lines 278–287)
  </read_first>
  <action>
    Migrate every selector in the RESEARCH S-06 hex table (lines 637–702) selector-by-
    selector to its `--hub-*` token. Panel `#1e2030`→`var(--hub-surface-elevated)`,
    borders `#292e42`→`var(--hub-border)`; scrollbar `#3b4261`→`var(--hub-scrollbar)`,
    `#565f89`→`var(--hub-scrollbar-hover)`; text muted/secondary/primary as in the table;
    accent `#7aa2f7`→`var(--hub-accent)` for jump-bar links, focus borders, cli-name,
    details summary, save btn bg; `#89b4fa`→`var(--hub-accent-hover)`; destructive
    `#f7768e`→`var(--hub-destructive)` for `.settings-panel__error`; success
    `#9ece6a`→`var(--hub-success)` for `.settings-panel__check` and `--btn--saved` bg
    (these are UI states, migration allowed per D-03 — NOT semantic status identifiers).
    For `.settings-panel__toggle-thumb` off-state `#565f89`, REUSE
    `var(--hub-scrollbar-hover)` (no new token — plan 01 deliberately did not add
    `--hub-toggle-thumb-off`). Save/saved/toggle-checked button text `#1a1b26`→
    `var(--hub-bg)`. Wrap migrated button/link hover transitions in the motion guard.
    Preserve the existing toggle interaction model and `aria-checked` (D-13 — recolor only).
  </action>
  <verify>
    <automated>sed -n '370,771p' frontend/src/style.css | grep -E '#[0-9a-fA-F]{3,6}' | grep -v 'rgba(0'; true</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '370,771p' frontend/src/style.css | grep -E '#[0-9a-fA-F]{3,6}'` returns only `rgba(0,0,0,...)` shadow opacity (no settings chrome/text/accent hex).
    - `.settings-panel__toggle-thumb` off-state uses `var(--hub-scrollbar-hover)` (no `--hub-toggle-thumb-off` introduced: `grep -c -- '--hub-toggle-thumb-off' frontend/src/style.css` == 0).
    - `.settings-panel__btn--save` uses `var(--hub-accent)`; `.settings-panel__error` uses `var(--hub-destructive)`.
    - Settings hover transitions are inside `@media (prefers-reduced-motion: no-preference)` with `reduce` fallback; toggle markup/`aria-checked` unchanged.
  </acceptance_criteria>
  <done>Settings surface fully tokenized; toggle thumb reuses scrollbar-hover token; motion-guarded; no chrome hex in range.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS-only recolor of File Browser, Editor chrome, and Settings; no input, auth, network, or data-flow change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-03 | Tampering | style.css file-browser + settings selectors | accept | Token substitution changes only color resolution. Settings still render the same path inputs/links; no secret or path is newly exposed. No new threat. |

No new threats introduced — recolor/token migration only; no change to data handling, auth, or network surfaces. No package installs.
</threat_model>

<verification>
- Colorblind grep gate (VALIDATION.md): `sed -n '370,771p'` and `sed -n '2646,3380p'` over `style.css` show only `rgba(0,0,0,...)` residual.
- `cd frontend && pnpm exec tsc --noEmit && pnpm test` passes.
</verification>

<success_criteria>
- File Browser, Editor chrome, and Settings consume `--hub-*` tokens (light + dark).
- No new token introduced beyond plan 01's `--hub-text-dim`.
- Migrated transitions motion-guarded; full frontend gate green.
</success_criteria>

<output>
Create `.planning/phases/141-redesign-implementation/141-03-SUMMARY.md` when done.
</output>
