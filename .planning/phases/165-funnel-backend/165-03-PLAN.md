---
phase: 165-funnel-backend
plan: 03
type: execute
wave: 3
depends_on: [165-02]
files_modified:
  - app.go
  - internal/daemon/client.go
  - app_test.go
  - internal/daemon/client_test.go
autonomous: true
requirements: [FNL-01]
user_setup: []

must_haves:
  truths:
    - "App.SetSessionFunnel(id, enabled, expiresIn) delegates to a.client.SetSessionFunnel; when a.client is nil it returns the 'daemon not connected' error and never panics (mirrors ToggleWebServing/SetSessionBrowse)"
    - "DaemonClient.SetSessionFunnel POSTs to /sessions/{id}/funnel with a JSON body {enabled, expiresIn} matching SetSessionFunnelRequest; an httptest server observes the correct method, path, and body"
    - "App.ListSessions propagates FunnelActive from the daemon's SessionInfo into the Wails-bound SessionInfo so the frontend (Phase 166) can poll funnel state; false is preserved (no silent drop)"
  artifacts:
    - app.go
    - internal/daemon/client.go
  key_links:
    - "App.SetSessionFunnel → DaemonClient.SetSessionFunnel → POST /sessions/{id}/funnel → 165-02 handleSetSessionFunnel — this is the GUI on-ramp that completes the FNL-01 enable path end-to-end"
    - "app.go SessionInfo.FunnelActive is copied field-by-field from daemon.SessionInfo.FunnelActive in App.ListSessions (same pattern as HomeDir/BrowseEnabled); omission silently drops it to false"
---

<objective>
Add the Wails on-ramp for the Funnel lifecycle: the `App.SetSessionFunnel` bound method, the `DaemonClient.SetSessionFunnel` daemon-client method it delegates to, and the `SessionInfo.FunnelActive` mirror field + propagation in `App.ListSessions` so the frontend can both enable Funnel and poll its state.

Purpose: 165-02 made `POST /sessions/{id}/funnel` and `SessionInfo.funnelActive` exist on the daemon. This plan exposes them to the Wails frontend (Phase 166 builds the SessionShareModal toggle UI + risk dialog on top of these). This is pure thin delegation — no Funnel logic lives here (Architectural Responsibility Map: "Wails bound method … Thin bridge; no logic").

Locked decisions honored:
- `expiresIn` (seconds) is threaded through unchanged so Phase 166's FUI-02 expiry picker can drive FNL-07.
- `FunnelActive` is added to the app.go SessionInfo mirror WITHOUT omitempty (false must serialize so the frontend poll detects expiry) — same rule as HomeDir/BrowseEnabled.

Output: modified `app.go` (SetSessionFunnel bound method + SessionInfo.FunnelActive field + ListSessions copy-loop propagation), modified `internal/daemon/client.go` (DaemonClient.SetSessionFunnel), tests in `app_test.go` and `internal/daemon/client_test.go`.
</objective>

<artifacts_this_plan_produces>
NEW symbols (exclude from drift verification):
- `App.SetSessionFunnel(sessionID string, enabled bool, expiresIn int) error` (app.go bound method)
- `DaemonClient.SetSessionFunnel(sessionID string, enabled bool, expiresIn int) error` (client.go)
- `app.SessionInfo.FunnelActive bool` field (`json:"funnelActive"`, NOT omitempty) + propagation in App.ListSessions
</artifacts_this_plan_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-funnel-backend/165-PATTERNS.md
@.planning/phases/165-funnel-backend/165-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: App.SetSessionFunnel + DaemonClient.SetSessionFunnel + SessionInfo.FunnelActive propagation</name>
  <files>app.go, internal/daemon/client.go, app_test.go, internal/daemon/client_test.go</files>
  <read_first>
    - app.go lines 29-54 (App's SessionInfo struct — WebEnabled/HomeDir/BrowseEnabled tags + NOT-omitempty comments) and the App.ListSessions field-by-field copy loop (~lines 73-92 region, where HomeDir/BrowseEnabled are propagated)
    - app.go bound-method analogs ToggleWebServing + SetSessionBrowse (a.client nil-guard → delegate; near line 868-885 in the source map, search for `func (a *App) ToggleWebServing`)
    - internal/daemon/client.go lines 347-359 (DaemonClient.ToggleWebServing + SetSessionBrowse — doJSON POST idiom), 706+ (doJSON signature)
    - internal/daemon/types.go SetSessionFunnelRequest (added in 165-02 — the body this client serializes) and daemon SessionInfo.FunnelActive (added in 165-02 — the source field)
    - 165-PATTERNS.md "app.go — SetSessionFunnel Wails bound method" (exact thin-delegation shape + the `FunnelActive bool` SessionInfo note)
    - app_test.go lines 184-250 (TestListSessions_PropagatesHomeDirAndBrowseEnabled — the propagation-regression analog to extend/mirror) and internal/daemon/client_test.go httptest patterns (~line 145, server records method/path/body)
  </read_first>
  <behavior>
    - TestApp_SetSessionFunnel_NilClient: an App with a.client == nil returns the "daemon not connected" error (no panic), mirroring the other bound methods.
    - TestDaemonClient_SetSessionFunnel: an httptest server asserts the client issues POST /sessions/{id}/funnel with JSON body {"enabled":true,"expiresIn":N}; a 2xx response returns nil error; a non-2xx surfaces an error.
    - TestListSessions_PropagatesFunnelActive (extend or mirror the existing HomeDir/BrowseEnabled propagation test): when the daemon SessionInfo carries FunnelActive=true, App.ListSessions returns a SessionInfo with FunnelActive=true; when false, it stays false (not dropped).
  </behavior>
  <action>
    In internal/daemon/client.go add `func (c *DaemonClient) SetSessionFunnel(sessionID string, enabled bool, expiresIn int) error` that calls `c.doJSON(http.MethodPost, "/sessions/"+sessionID+"/funnel", SetSessionFunnelRequest{Enabled: enabled, ExpiresIn: expiresIn}, nil)` — exact mirror of ToggleWebServing/SetSessionBrowse, discarding the response body. In app.go add the bound method `func (a *App) SetSessionFunnel(sessionID string, enabled bool, expiresIn int) error` that nil-guards a.client (return fmt.Errorf("daemon not connected")) then returns a.client.SetSessionFunnel(sessionID, enabled, expiresIn) — copy the ToggleWebServing shape exactly; no Funnel logic in app.go. Add `FunnelActive bool` with tag `json:"funnelActive"` to the app.go SessionInfo struct (NOT omitempty — mirror the HomeDir/BrowseEnabled comment explaining that false must serialize so the frontend poll detects expiry) and add `FunnelActive: s.FunnelActive,` to the field-by-field copy in App.ListSessions (omitting it silently drops the flag to false — the documented HomeDir-class UAT bug). Add the three tests: the nil-client guard test in app_test.go, the httptest client test in internal/daemon/client_test.go (mirror the existing method/path/body-recording pattern), and the FunnelActive propagation assertion (extend TestListSessions_PropagatesHomeDirAndBrowseEnabled or add a sibling test).
  </action>
  <verify>
    <automated>go build ./... && go test . ./internal/daemon/... -run 'TestApp_SetSessionFunnel_NilClient|TestDaemonClient_SetSessionFunnel|TestListSessions_PropagatesFunnelActive|TestListSessions_PropagatesHomeDirAndBrowseEnabled' -count=1</automated>
  </verify>
  <done>App.SetSessionFunnel and DaemonClient.SetSessionFunnel exist and delegate cleanly (nil-guard returns "daemon not connected"); the client POSTs the correct {enabled, expiresIn} body; SessionInfo.FunnelActive propagates through App.ListSessions (FNL-01 enable path complete end-to-end).</done>
  <acceptance_criteria>
    - go build ./... succeeds; App.SetSessionFunnel and DaemonClient.SetSessionFunnel exist with the (sessionID, enabled, expiresIn) signature.
    - App.SetSessionFunnel returns "daemon not connected" when a.client is nil (no panic).
    - DaemonClient.SetSessionFunnel POSTs to /sessions/{id}/funnel with the correct {enabled, expiresIn} body (httptest-verified).
    - app.go SessionInfo carries FunnelActive (json:"funnelActive", not omitempty) and App.ListSessions propagates it (true stays true, false stays false).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wails frontend → App bound method | Renderer-originated enable/disable + expiry cross into the Go App shell |
| App shell → daemon (Unix socket) | DaemonClient.SetSessionFunnel POSTs the enable/disable request to the daemon |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-165-14 | Denial of Service | App.SetSessionFunnel panics on a nil daemon client (startup-failure state) | medium | mitigate | nil-guard returns "daemon not connected"; TestApp_SetSessionFunnel_NilClient (Task 1) |
| T-165-15 | Information Disclosure | SessionInfo.FunnelActive silently dropped (omitempty / missing copy) → frontend never sees expiry, leaves a stale public indicator | medium | mitigate | json tag without omitempty + explicit copy-loop propagation; TestListSessions_PropagatesFunnelActive (Task 1) |
| T-165-SC | Tampering | npm/go package installs | low | accept | No new packages this phase; legitimacy gate N/A |
</threat_model>

<verification>
- `go build ./...` passes with the bound method, client method, and SessionInfo.FunnelActive mirror.
- `go test . ./internal/daemon/... -count=1` is green.
- The bridge is pure delegation: app.go contains no Funnel state or SetServeConfig logic (all of that lives in 165-01/165-02).
</verification>

<success_criteria>
- The frontend can enable/disable Funnel for a session through App.SetSessionFunnel → DaemonClient → daemon, threading expiresIn for FNL-07 — completing the FNL-01 enable path end-to-end.
- SessionInfo.FunnelActive is exposed to the frontend for polling, with false preserved.
</success_criteria>

<output>
Create `.planning/phases/165-funnel-backend/165-03-SUMMARY.md` when done.
</output>
</content>
