---
status: partial
phase: 165-funnel-backend
source: [165-VERIFICATION.md]
started: 2026-06-30T15:57:01Z
updated: 2026-06-30T16:22:00Z
---

## Current Test

[testing paused — 2 items outstanding (require live Funnel-granted tailnet / external machine)]

## Tests

### 1. M-34 — External-tailnet HTTP request gets 200 on the no-port Funnel URL
expected: A machine with no Tailscale installed opens https://<hostname>.ts.net/sessions/<id>?cap=TOKEN and receives 200 (not 403), with the Origin: https://<hostname>.ts.net header present.
result: blocked
blocked_by: external-machine
reason: "Requires (a) a Funnel-granted, connected tailnet node — this host's BackendState is Stopped and the `funnel` node attribute is not set (CheckFunnelAccess returns 'Funnel not available'), so no real Funnel URL can be served — and (b) a second machine off the tailnet to originate the external request. Neither can be provided from this session; both are operator actions (Tailscale admin ACL grant + `tailscale up`, plus a phone/cellular device). Origin-check logic is unit-proven by TestRequireAllowedOrigin_FunnelOrigin."

### 2. M-35 — `tailscale serve status` shows empty config after each teardown trigger
expected: After each of the four production teardown paths (toggle-off, web-share-off, session natural end, daemon clean stop), running `tailscale serve status` on the host reports no serve entries / an empty config.
result: blocked
blocked_by: funnel-grant
reason: "The teardown-clears-config cycle can only be observed if Funnel is first ENABLED (SetServeConfig writes a real serve entry), then torn down. On this node Funnel cannot be enabled (node `funnel` attribute not set), so there is nothing to tear down. Verified the negative live: with the funnel-enable attempt blocked by the prereq check, `tailscale serve status` stays 'No serve config' (SetServeConfig is never reached). Full enable→teardown cycle needs a Funnel-granted, connected tailnet node. Teardown wiring is unit-proven by TestFunnelTeardown_AllTriggers (5 sub-tests) + TestWebServerStop_DisablesFunnel."

### 3. M-36 — Local-network fallback web-share continues unaffected with tailscaled stopped
expected: With tailscaled not running, an existing web-share session stays accessible on the local tailnet URL; funnelActive stays false; attempting to enable Funnel returns an error; the web-share guest connection is not disrupted.
result: pass
source: live
note: "Verified live against a freshly-built binary (HEAD 3487a955, branch v4.2-funnel-sharing) driving the REAL webserver local.Client against the REAL tailscaled — not the unit-test fake. State: Tailscale BackendState=Stopped; daemon web server running in local-network fallback mode (web status: running=true, mode=local, url=https://10.200.4.240:7443). Observed: (1) web-share session reachable on its local URL — HTTP 401 (no cap token; server is serving, not connection-refused) both BEFORE and AFTER the funnel-enable attempt → unaffected/continuity proven; (2) POST /sessions/{id}/funnel {enabled:true} returns a clean human-readable error 'Funnel not available; \"funnel\" node attribute not set' (HTTP 400) — not a panic, not a silent success; (3) funnelActive stays false in GET /sessions; (4) `tailscale serve status` stays 'No serve config' — SetServeConfig never called; (5) daemon stays alive, no panic. NUANCE: because tailscaled is running-but-Stopped (not fully killed), the error originates from the CheckFunnelAccess prereq branch (FNL-06) rather than the StatusWithoutPeers-error fallback branch (FNL-05) that TestEnableFunnel_FallbackModeSafe simulates. Both branches deliver the identical user-observable guarantee M-36 requires; the FNL-05 fully-killed-tailscaled branch was not forced because killing the operator's tailscaled service would disrupt their machine. Reported in TESTING.md M-36."

## Summary

total: 3
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 2

## Gaps

[none — no functional defects found; 2 items blocked on live Funnel-granted tailnet / external machine, not code issues]
