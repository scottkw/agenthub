---
phase: 57-quick-wins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Sidebar.tsx
  - frontend/src/components/__tests__/Sidebar.test.tsx
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "Sidebar displays 'New Session' label text when expanded"
    - "Sidebar button has aria-label='New Session' in both expanded and collapsed states"
    - "No occurrence of 'New Tab' remains in Sidebar.tsx"
  artifacts:
    - path: "frontend/src/components/Sidebar.tsx"
      provides: "New Session label and aria-label"
      contains: "New Session"
    - path: "frontend/src/components/__tests__/Sidebar.test.tsx"
      provides: "Test verifying New Session label"
      contains: "New Session"
  key_links:
    - from: "frontend/src/components/Sidebar.tsx"
      to: "aria-label attribute"
      via: "button aria-label prop"
      pattern: 'aria-label="New Session"'
---

<objective>
Rename the sidebar "New Tab" button label and aria-label to "New Session" to accurately reflect that the action creates a terminal session, not a browser-style tab.

Purpose: The sidebar button currently says "New Tab" which is misleading after the v1.10 sidebar navigation redesign. Users create sessions, not tabs.

Output: Updated Sidebar.tsx with renamed label, new test in Sidebar.test.tsx verifying the label.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-quick-wins/57-RESEARCH.md
@.planning/phases/57-quick-wins/57-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename "New Tab" to "New Session" in Sidebar and add test</name>
  <files>frontend/src/components/Sidebar.tsx, frontend/src/components/__tests__/Sidebar.test.tsx</files>
  <read_first>
    - frontend/src/components/Sidebar.tsx (current label text on lines 83-87: aria-label="New Tab" and span text "New Tab")
    - frontend/src/components/__tests__/Sidebar.test.tsx (existing test patterns — renderSidebar helper, describe blocks, aria-label queries)
    - .planning/phases/57-quick-wins/57-UI-SPEC.md (copywriting contract — exact strings to use)
  </read_first>
  <behavior>
    - Test: renders "New Session" label and aria-label for the add button — queries button[aria-label="New Session"], asserts it exists, asserts its .sidebar__label child has textContent "New Session"
    - Existing tests continue to pass (none assert "New Tab")
  </behavior>
  <action>
1. RED phase — Add the following test to frontend/src/components/__tests__/Sidebar.test.tsx inside the existing `describe('Sidebar component (SIDE-01)')` block, after the "renders a Sessions sidebar__item button" test:

```tsx
it('renders "New Session" label and aria-label for the add button (UI-01)', () => {
  ;({ container, root } = renderSidebar())
  const addBtn = container.querySelector('button[aria-label="New Session"]')
  expect(addBtn).not.toBeNull()
  const label = addBtn!.querySelector('.sidebar__label')
  expect(label).not.toBeNull()
  expect(label!.textContent).toBe('New Session')
})
```

Run: `cd /Users/ken/dev/agenthub/frontend && pnpm test -- --reporter=verbose` — expect FAIL (button has aria-label="New Tab", not "New Session").

2. GREEN phase — Edit frontend/src/components/Sidebar.tsx. Change lines 83 and 86:

Line 83 — change `aria-label="New Tab"` to `aria-label="New Session"`
Line 86 — change `<span className="sidebar__label">New Tab</span>` to `<span className="sidebar__label">New Session</span>`

The resulting button block (lines 80-87) should be:
```tsx
<button
  className="sidebar__item"
  onClick={onAdd}
  aria-label="New Session"
>
  <PlusIcon className="sidebar__icon" />
  {!collapsed && <span className="sidebar__label">New Session</span>}
</button>
```

Run: `cd /Users/ken/dev/agenthub/frontend && pnpm test -- --reporter=verbose` — expect all tests PASS.

3. Verify no "New Tab" string remains: `grep -r "New Tab" frontend/src/components/Sidebar.tsx` should return nothing.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/Sidebar.tsx contains: aria-label="New Session"
    - frontend/src/components/Sidebar.tsx contains: >New Session</span>
    - frontend/src/components/Sidebar.tsx does NOT contain: "New Tab"
    - frontend/src/components/__tests__/Sidebar.test.tsx contains: aria-label="New Session"
    - frontend/src/components/__tests__/Sidebar.test.tsx contains: textContent).toBe('New Session')
    - `cd frontend && pnpm test` exits 0
  </acceptance_criteria>
  <done>Sidebar shows "New Session" in expanded label, aria-label, and collapsed tooltip; new test verifies the label; no "New Tab" string remains in Sidebar.tsx; all frontend tests pass</done>
</task>

</tasks>

<verification>
- `cd /Users/ken/dev/agenthub/frontend && pnpm test -- --reporter=verbose` — all Sidebar tests pass
- `grep "New Tab" frontend/src/components/Sidebar.tsx` returns no matches
- `grep "New Session" frontend/src/components/Sidebar.tsx` shows both aria-label and span text
</verification>

<success_criteria>
- Sidebar displays "New Session" label when expanded
- Sidebar has aria-label="New Session" (serves as tooltip in collapsed state)
- No occurrence of "New Tab" in Sidebar.tsx
- New test verifies the label text and aria-label
- All existing frontend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/57-quick-wins/57-02-SUMMARY.md`
</output>
