---
phase: 175-web-share-remote-viewer-windowing-bug-fixes
plan: 07
type: execute
wave: 3
depends_on: ["175-03", "175-04", "175-05", "175-06"]
files_modified:
  - TESTING.md
autonomous: true
requirements: [BUG-01, BUG-02, BUG-03, BUG-04]
must_haves:
  truths:
    - "TESTING.md Suite Manifest counts match the actual on-disk test-file counts after this phase"
    - "Every new automated test file has a Section 4 traceability row (repo-relative .go/.ts/.tsx path only) mapped to its BUG-0x requirement"
    - "New M-NN manual items exist for the four bugs' live-UAT gates"
    - "Category P (M-27/M-28) no longer calls the dead web/assets/terminal.js 'the web viewer' — it is reconciled to the post-Phase-159 /app/ React surface"
  artifacts:
    - "TESTING.md"
  key_links:
    - "Regression Test Convention (repo CLAUDE.md) — every phase that adds/renames/removes tests updates Sections 2, 4, 5"
---

<objective>
Satisfy AgentHub's standing Regression Test Convention for Phase 175: register the new automated test files in the Suite Manifest (Section 2) and Traceability map (Section 4), add manual checklist items (Section 5) for the four bugs' live-UAT gates, and reconcile the now-stale Category P wording that still describes the dead `web/assets/terminal.js` as "the web viewer" (unreachable since Phase 159's redirect).

Purpose: TESTING.md is the canonical regression-suite home; it must stay in sync with reality (repo CLAUDE.md standing rule).
Output: an updated, self-consistent TESTING.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/175-web-share-remote-viewer-windowing-bug-fixes/175-RESEARCH.md
@TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile Suite Manifest counts + add Section 4 traceability rows</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md Section 2 (Suite Manifest — current: 376 Go / 147 vitest / 9 Playwright / 2 build / 534 total) and Section 4 (Traceability map; path column is a repo-relative file path only — no test names).
    - repo CLAUDE.md "Regression Test Convention" (steps 1-4) and the note that `bash tests/check-traceability-paths.sh` must exit 0.
    - The 175-02..06 SUMMARYs for the exact final test-file set (new: app_poll_test.go, internal/webserver/session_ended_test.go, internal/relay/scrollback_altscreen_test.go, frontend/src/components/__tests__/SessionEndedBanner.test.tsx; extended: frontend/src/lib/terminalScale.test.ts, frontend/src/components/__tests__/TerminalPanel.scale.test.tsx, frontend/src/lib/relayClient.test.ts).
  </read_first>
  <action>
    Recompute the Go and vitest file counts from the actual working tree (`find internal -name '*_test.go' | wc -l` for Go incl. repo-root `*_test.go`; `find frontend/src -name '*.test.ts*' | wc -l` for vitest) and update the Suite Manifest table + Total, adding a NEW dated reconciliation note stating the old→new counts (the 171-04/173-07 correction-note pattern — do not edit prior plans' historical notes). Add one Section 4 traceability row per NEW test file (path column = repo-relative file path only), mapping app_poll_test.go → BUG-03, session_ended_test.go → BUG-02, scrollback_altscreen_test.go → BUG-04, SessionEndedBanner.test.tsx → BUG-02, and add BUG-01 rows for the extended terminalScale.test.ts / TerminalPanel.scale.test.tsx. Then run the traceability path checker.
  </action>
  <verify>
    <automated>bash tests/check-traceability-paths.sh</automated>
  </verify>
  <acceptance_criteria>
    - Suite Manifest Go/vitest counts + Total match `find` output; a dated old→new reconciliation note is present.
    - Every new test file has a Section 4 row with a repo-relative .go/.tsx path (no test names in the path column) mapped to its BUG-0x id.
    - `bash tests/check-traceability-paths.sh` exits 0 (note: per project memory, macOS BSD grep can under-report in this script — if it passes trivially, spot-check the added paths resolve to real files).
  </acceptance_criteria>
  <done>Sections 2 and 4 reflect the phase's new tests and pass the path checker.</done>
</task>

<task type="auto">
  <name>Task 2: Add Section 5 manual M-NN items + reconcile stale Category P</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md Section 5 (manual checklist; find the current highest M-NN to assign the next four sequential numbers).
    - TESTING.md Category P (M-27/M-28) lines ~579-599 — the text still says the web `terminal.js` viewer is "the web viewer" and calls it the reachable surface; post-Phase-159 the real surface is `/app/` (WebShareSessionView → TerminalPanel.tsx) and terminal.js is dead code.
    - 175-VALIDATION.md "Manual-Only Verifications" (the four live-UAT behaviors) and each impl plan's deferred live gate.
  </read_first>
  <action>
    Add four new sequential M-NN manual checklist items (numbers after the current highest in Section 5), one per bug's live gate: (a) BUG-01 mobile-legibility — web-share a session, open the link at a phone-width viewport, confirm the 80-col grid stays readable (floor + horizontal scroll, no unbounded downscale, host PTY grid unchanged); (b) BUG-02 disconnect notice — owner stops a shared session, guest sees the accessible disconnect banner (text + aria-live), not a frozen terminal; (c) BUG-03 timed repro — shared session lived >5min then exited auto-closes its tab, matching unshared behavior; (d) BUG-04 live two-client reconnect — start a full-screen TUI agent, join as a guest after it enters alt-screen (and/or reconnect), confirm the guest sees the current screen not a blank/garbled window. Then reconcile Category P: update M-27/M-28's prose so the reachable web surface is the `/app/` React viewer (TerminalPanel.tsx), and `web/assets/terminal.js` is explicitly labeled dead code (unreachable since the Phase-159 `/sessions/{id}` → `/app/` redirect) rather than "the web viewer". Keep the M-27/M-28 numbers stable; only correct the stale wording.
  </action>
  <verify>
    <automated>grep -c "dead code\|/app/" TESTING.md | awk '{ if ($1+0 >= 1) print "category P reconciled"; else { print "MISSING reconciliation"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - Four new M-NN items exist (sequential after the current highest), one per BUG-0x live gate, with concrete steps + expected outcomes.
    - Category P M-27/M-28 prose no longer calls web/assets/terminal.js "the web viewer"; it labels terminal.js dead code and names `/app/`/TerminalPanel.tsx as the reachable surface.
    - M-27/M-28 numbers unchanged.
  </acceptance_criteria>
  <done>Section 5 documents the four new live gates and Category P is accurate for the post-Phase-159 architecture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (documentation only) | This plan edits TESTING.md only; no runtime code, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-07-01 | Repudiation | stale/incorrect regression manifest | low | mitigate | Counts are recomputed from the working tree and the path checker gates the traceability paths — the manifest cannot silently drift. |
| T-175-SC | Tampering | installs | low | accept | No packages installed this phase (RESEARCH: N/A). |
</threat_model>

<verification>
- `bash tests/check-traceability-paths.sh` exits 0.
- Suite Manifest counts match `find internal -name '*_test.go' | wc -l` (+ repo-root) and `find frontend/src -name '*.test.ts*' | wc -l`.
- Four new M-NN items present; Category P reconciled.
</verification>

<success_criteria>
TESTING.md is self-consistent with the phase's new tests and manual gates, the path checker passes, and the stale Category P terminal.js wording is corrected.
</success_criteria>

<output>
Create `.planning/phases/175-web-share-remote-viewer-windowing-bug-fixes/175-07-SUMMARY.md` when done.
</output>
