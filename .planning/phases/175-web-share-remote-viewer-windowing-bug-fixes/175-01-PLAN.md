---
phase: 175-web-share-remote-viewer-windowing-bug-fixes
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/175-web-share-remote-viewer-windowing-bug-fixes/175-01-DIAGNOSIS.md
autonomous: false
requirements: [BUG-03]
must_haves:
  truths:
    - "A timed live repro has confirmed OR disproved that app.go:386's fixed 300s exit-poll deadline is BUG-03's root cause"
    - "The diagnosis is written down with the observed timings so 175-05 can pick the correct fix branch"
  artifacts:
    - ".planning/phases/175-web-share-remote-viewer-windowing-bug-fixes/175-01-DIAGNOSIS.md"
  key_links:
    - "Diagnosis outcome gates which fix 175-05 (BUG-03) implements (remove-deadline vs fallback logging pass)"
---

<objective>
Run the mandatory live timed reproduction for BUG-03 (#126) BEFORE any BUG-03 fix is written. Per the phase's locked decision, BUG-03's root cause is UNCONFIRMED: static reading found no share-conditional branch in the exit-detection chain, and the strongest hypothesis (the fixed 5-minute exit-poll deadline at `app.go:386`) is code-grounded but not live-confirmed. This plan produces a written diagnosis that decides which fix 175-05 implements.

Purpose: Prevent shipping a fix for the wrong bug (RESEARCH Pitfall 4). A realistic "create → share → deliver join code → verify from a guest device → exit" workflow easily exceeds 5 minutes; an immediate "exit" does not — which would explain the observed correlation with sharing without sharing itself being causal.
Output: `175-01-DIAGNOSIS.md` recording the two timed runs and a CONFIRMED / DISPROVED verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/175-web-share-remote-viewer-windowing-bug-fixes/175-RESEARCH.md
@app.go
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Live timed BUG-03 repro — confirm or disprove the 5-minute deadline</name>
  <files>.planning/phases/175-web-share-remote-viewer-windowing-bug-fixes/175-01-DIAGNOSIS.md</files>
  <action>No code changes. A human runs the timed live repro described below and writes 175-01-DIAGNOSIS.md with a CONFIRMED/DISPROVED verdict that selects 175-05's fix branch.</action>
  <read_first>
    - app.go:383-449 (pollSessionStatus + emitExitEvent — note line 386's `deadline := time.Now().Add(300 * time.Second)`, a fixed non-renewing window measured from poll start; if it expires before the session exits, the loop returns silently and NO `session:exit` event is emitted).
    - frontend/src/App.tsx:711-750 (session:exit handler → handleCloseTab) and 886-920 (handleCloseTab) — confirmed identical for shared/unshared sessions.
    - 175-RESEARCH.md "Bug 3" section and "Pitfall 4" (the required diagnostic procedure).
  </read_first>
  <what-built>
    No code is changed in this task. This is a human-run live diagnostic. It requires a production/`wails dev` app + real daemon (the exit-detection poller lives in the Wails Go process, not the vitest/go-test surface) and real elapsed wall-clock timing.
  </what-built>
  <how-to-verify>
    Run BOTH cases and record wall-clock timings:
    1. BASELINE (unshared, immediate): Create a session, do NOT share it, exit it immediately (`exit` in the shell, or quit the agent). Expect: tab auto-closes within ~1-2s. Record elapsed time and whether the tab closed.
    2. SHARE-WORKFLOW (>5 minutes): Create a second session, enable web-share, and let it sit for MORE than 5 minutes (simulate delivering a join code + a guest connecting/verifying — actual guest connection optional; the deciding variable is elapsed time since creation). After >5 min, exit from inside the session. Record elapsed time and whether the tab auto-closes.
    3. (Optional tie-breaker) Repeat the shared case but exit at <5 minutes. If the tab closes at <5min shared but NOT at >5min shared, the deadline theory is strongly confirmed.
    Interpretation:
    - If the >5min case leaves the tab open AND baseline closes → CONFIRMED: app.go:386 deadline is the root cause. 175-05 removes/re-arms the deadline.
    - If a <5min shared exit ALSO fails to close the tab → DISPROVED: the deadline is not the mechanism. 175-05 instead adds a diagnostic-logging pass around handleCloseTab/ToggleWebServing/KillSession to locate the real stall (RESEARCH notes the un-timeout'd http.Client at internal/daemon/client.go:37 as a candidate hang site).
    Write `.planning/phases/175-web-share-remote-viewer-windowing-bug-fixes/175-01-DIAGNOSIS.md` capturing: both timings, tab-close outcomes, and a one-line VERDICT (CONFIRMED or DISPROVED) plus the chosen fix branch for 175-05.
  </how-to-verify>
  <acceptance_criteria>
    - 175-01-DIAGNOSIS.md exists and records both timed runs with explicit elapsed times.
    - The file states a clear VERDICT: CONFIRMED (deadline is root cause) or DISPROVED (needs fallback logging pass).
    - The verdict names the fix branch 175-05 must take.
  </acceptance_criteria>
  <resume-signal>Type "confirmed" (deadline is root cause) or "disproved" (needs fallback logging), and confirm 175-01-DIAGNOSIS.md is written.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (diagnostic only) | This plan changes no code and introduces no new runtime trust boundary. It runs an existing app against an existing daemon and writes a planning doc. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-01-01 | Information Disclosure | 175-01-DIAGNOSIS.md | low | accept | Diagnosis records only session timings/outcomes, never capability tokens or session content. |
| T-175-SC | Tampering | npm/pip/go installs | low | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A — all fixes reuse already-vendored libraries). |
</threat_model>

<verification>
- 175-01-DIAGNOSIS.md exists with both timed runs and a VERDICT.
- No source files were modified (this is a diagnostic-only plan).
</verification>

<success_criteria>
BUG-03's root cause is CONFIRMED or DISPROVED in writing, and 175-05's fix branch is selected before any BUG-03 code is written.
</success_criteria>

<output>
Create `.planning/phases/175-web-share-remote-viewer-windowing-bug-fixes/175-01-SUMMARY.md` when done.
</output>
