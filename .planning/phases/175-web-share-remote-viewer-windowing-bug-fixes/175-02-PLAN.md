---
phase: 175-web-share-remote-viewer-windowing-bug-fixes
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - app.go
  - app_poll_test.go
  - internal/webserver/session_ended_test.go
  - internal/relay/scrollback_altscreen_test.go
autonomous: true
requirements: [BUG-02, BUG-03, BUG-04]
must_haves:
  truths:
    - "app.go:386's exit-poll deadline math lives in a pure, injectable-clock helper that go test can exercise"
    - "A RED (skip-guarded) Go test exists asserting the web-guest WS close on hub.Done() carries a code + reason (BUG-02)"
    - "A RED (skip-guarded) Go test exists asserting reconnect after ring-wrap-past-alt-screen-enter reconstructs the correct buffer mode (BUG-04)"
  artifacts:
    - "app_poll_test.go"
    - "internal/webserver/session_ended_test.go"
    - "internal/relay/scrollback_altscreen_test.go"
  key_links:
    - "shouldContinuePolling helper is the seam 175-05 (BUG-03) rewires"
    - "session_ended_test.go is unskipped+greened by 175-06 (BUG-02); scrollback_altscreen_test.go by 175-04 (BUG-04)"
---

<objective>
Wave-0 test scaffolding for the three automatable bugs (BUG-02, BUG-03, BUG-04). Extract the untestable exit-poll deadline math into a pure helper, and create the RED backend tests that the Wave-1/Wave-2 implementations must turn GREEN. No production behavior changes here except the pure refactor extraction (behavior-preserving).

Purpose: Nyquist compliance — no fix ships without an automated test that fails before and passes after. app.go's poll logic is untestable until its deadline math is a pure function (RESEARCH Bug 3 Testability).
Output: one behavior-preserving refactor (+ GREEN test) and two skip-guarded RED tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/175-web-share-remote-viewer-windowing-bug-fixes/175-RESEARCH.md
@app.go
@internal/webserver/inject_test.go
@internal/relay/scrollback.go
@internal/relay/hub.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract app.go exit-poll deadline into a pure injectable-clock helper</name>
  <files>app.go, app_poll_test.go</files>
  <read_first>
    - app.go:383-426 (pollSessionStatus — line 386 `deadline := time.Now().Add(300 * time.Second)`, line 387 `for time.Now().Before(deadline)`).
    - 175-RESEARCH.md "Bug 3 Testability" (recommends a pure `shouldContinuePolling(createdAt, now, maxWindow) bool`).
  </read_first>
  <action>
    Add a pure, side-effect-free helper `shouldContinuePolling(pollStart, now time.Time, maxWindow time.Duration) bool` returning `now.Before(pollStart.Add(maxWindow))`. Refactor pollSessionStatus to compute `pollStart := time.Now()` and drive the loop condition through the helper with `maxWindow = 300 * time.Second` — this is a BEHAVIOR-PRESERVING extraction only (do NOT change the 300s value or exit conditions in this plan; the behavioral fix is 175-05, gated on the 175-01 diagnosis). Create `app_poll_test.go` (package main) asserting current behavior: helper returns true when now is within the window, false at/after the deadline, and false for a maxWindow of 0. This test is GREEN immediately.
  </action>
  <verify>
    <automated>go test ./... -run TestShouldContinuePolling -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `shouldContinuePolling` exists as a pure function with no clock/IO reads inside it (time is passed in).
    - pollSessionStatus drives its loop through the helper with no observable behavior change (still 300s window).
    - app_poll_test.go passes (GREEN) and covers in-window, at-deadline, and zero-window cases.
    - `go build ./...` and `go vet ./...` clean.
  </acceptance_criteria>
  <done>The deadline seam exists and is unit-tested; 175-05 can now change the semantics with test coverage.</done>
</task>

<task type="auto">
  <name>Task 2: RED scaffold — web-guest WS close-on-session-end carries code + reason (BUG-02)</name>
  <files>internal/webserver/session_ended_test.go</files>
  <read_first>
    - internal/webserver/inject_test.go:95-140 (harness: NewHubManager + io.Pipe PTY + selfSignedTLSForTest + NewWebServer + dialWebServerWS + issueCapFor).
    - internal/webserver/server.go:1590-1605 (write pump; `case <-hub.Done(): return` at 1599 — currently a bare return, no close reason).
    - 175-RESEARCH.md "Bug 2 Testability" (assert the client receives a websocket.CloseError with the expected code/reason after hub.Shutdown()).
  </read_first>
  <action>
    Create `internal/webserver/session_ended_test.go` mirroring the inject_test.go harness: stand up a WebServer over a HubManager, dial a WS client for a session, then call `manager.Get(sessionID)` → `hub.Shutdown()` mid-test. Assert the client's next `conn.Read(ctx)` returns a `*websocket.CloseError` whose `Code == websocket.StatusNormalClosure` and `Reason == "session ended"`. Guard the whole test body behind `t.Skip("RED until 175-06 sends a close reason on hub.Done() — BUG-02")` on the FIRST line of the test function so the suite stays green between waves. Do NOT reference the literal disconnect-banner copy anywhere in this file.
  </action>
  <verify>
    <automated>go test ./internal/webserver/... -run TestSessionEnd -count=1</automated>
  </verify>
  <acceptance_criteria>
    - session_ended_test.go compiles and the test is present but SKIPPED (skip message names 175-06 and BUG-02).
    - The assertion targets `websocket.StatusNormalClosure` + reason `"session ended"` (a fixed enum, not raw error text — the security contract for T-175-06-01).
    - `go build ./...` clean; suite is green (test skipped, not failing).
  </acceptance_criteria>
  <done>The BUG-02 server-side contract is encoded as a skip-guarded RED test awaiting 175-06.</done>
</task>

<task type="auto">
  <name>Task 3: RED scaffold — reconnect reconstructs alt-screen mode after ring-wrap (BUG-04)</name>
  <files>internal/relay/scrollback_altscreen_test.go</files>
  <read_first>
    - internal/relay/scrollback.go (256 KiB ring; Append drops oldest bytes at an arbitrary boundary).
    - internal/relay/hub.go:363-395 (Run/broadcast) and :767-799 (ScrollbackSnapshot, Cols, Rows, Done).
    - internal/daemon/engine.go:762-802 (GetSessionStyledTailLines — the xvt emulator + query-strip + 0x01 MsgOutput-strip precedent BUG-04 will mirror inside package relay).
    - 175-RESEARCH.md "Bug 4 Testability".
  </read_first>
  <action>
    Create `internal/relay/scrollback_altscreen_test.go` (package relay). The test feeds a Hub enough synthetic PTY output to wrap the 256 KiB ring PAST a single `ESC[?1049h` (alt-screen enter) followed by alt-screen content, subscribes a fresh client, and asserts the NEW emulator-derived reconnect preamble (the method 175-04 will add, e.g. `hub.RenderSnapshot()`) reconstructs alt-screen mode — i.e. the preamble re-establishes `?1049h` and the current screen content, which the raw `ScrollbackSnapshot()` alone loses after wrap. Guard the whole test body behind `t.Skip("RED until 175-04 adds the live per-hub VT emulator + RenderSnapshot — BUG-04")` on the first line. If the referenced method does not yet exist, keep the reference behind the skip (build must stay green) by writing the test against the method name 175-04 will introduce and ensuring the file still compiles — if a compile reference is unavoidable, assert against `ScrollbackSnapshot()` today and leave a `// 175-04: switch to hub.RenderSnapshot()` marker inside the skipped body.
  </action>
  <verify>
    <automated>go test ./internal/relay/... -run TestScrollbackAltScreenReplay -count=1</automated>
  </verify>
  <acceptance_criteria>
    - scrollback_altscreen_test.go compiles and the test is present but SKIPPED (skip message names 175-04 and BUG-04).
    - The synthetic fixture provably wraps the ring past the `ESC[?1049h` sequence (assert the raw snapshot no longer contains that byte sequence).
    - `go build ./internal/relay/...` clean; suite green (skipped, not failing).
  </acceptance_criteria>
  <done>The BUG-04 reconnect-replay contract is encoded as a skip-guarded RED test awaiting 175-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → in-process WS server | Tests dial a loopback TLS WS; no external network. No new production trust boundary is introduced by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-02-01 | Tampering | pure refactor of pollSessionStatus | low | mitigate | Extraction is behavior-preserving; app_poll_test.go pins the pre-existing 300s semantics so no accidental behavior change slips in. |
| T-175-02-02 | Information Disclosure | session_ended_test.go asserted reason string | low | accept | Test pins the fixed generic reason `"session ended"` — reinforces (does not weaken) the T-175-06-01 no-leak contract. |
| T-175-SC | Tampering | npm/pip/go installs | low | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `go build ./... && go vet ./...` clean.
- `go test ./... -run TestShouldContinuePolling -count=1` GREEN.
- `go test ./internal/webserver/... ./internal/relay/... -count=1` green (new tests SKIPPED, none failing).
</verification>

<success_criteria>
The deadline seam is testable and pinned; two skip-guarded RED backend tests exist encoding the BUG-02 and BUG-04 contracts, and the full suite stays green.
</success_criteria>

<output>
Create `.planning/phases/175-web-share-remote-viewer-windowing-bug-fixes/175-02-SUMMARY.md` when done.
</output>
