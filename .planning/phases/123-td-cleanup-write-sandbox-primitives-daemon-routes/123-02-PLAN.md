---
phase: 123-td-cleanup-write-sandbox-primitives-daemon-routes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/daemon/client_remote_files.go
  - internal/daemon/client_test.go
  - frontend/src/components/FileBrowserTab.tsx
  - frontend/src/components/FileBrowser/FileRow.tsx
  - frontend/src/lib/humanSize.ts
autonomous: true
requirements: [FSW-10, FSW-11]
must_haves:
  truths:
    - "ExchangeJoinCodeAtURL parses a 303 Location ?cap=<token> and returns the token instead of JSON-decoding an empty body"
    - "The expired/invalid/not-found/session-gone error-substring contract the modal pivots on is preserved"
    - "TD-4 WR-03/WR-04/WR-05 frontend hardening applied at the corrected paths; WR-01/WR-02 confirmed already-satisfied (verify-only, not re-touched)"
  artifacts:
    - path: "internal/daemon/client_remote_files.go"
      provides: "303-aware ExchangeJoinCodeAtURL with a dedicated CheckRedirect client"
      contains: "ErrUseLastResponse"
    - path: "frontend/src/components/FileBrowser/FileRow.tsx"
      provides: "formatRowMtime empty-mtime fallback (WR-04)"
    - path: "frontend/src/lib/humanSize.ts"
      provides: "WR-05 comment clarity"
  key_links:
    - from: "DaemonClient.ExchangeJoinCodeAtURL"
      to: "Location header ?cap= token"
      via: "dedicated http.Client with CheckRedirect: ErrUseLastResponse, detect 303"
      pattern: "StatusSeeOther"
---

<objective>
Close the two carried tech-debts while the file layer is open: TD-5 (FSW-10) fixes `DaemonClient.ExchangeJoinCodeAtURL` so the desktop GUI can acquire a remote session cap (today it JSON-decodes a bodyless 303 and silently fails — blocking all remote-write work in Phase 128); TD-4 (FSW-11) applies the Phase 120 WR-03/04/05 file-browser hardening at the corrected source paths and confirms WR-01/WR-02 are already satisfied.

Purpose: TD-5 is a hard prerequisite for the Phase 128 remote-write epic; landing it now unblocks downstream. TD-4 is small, file-adjacent hardening that belongs in the same sweep.
Output: 303-aware join-code exchange with the error contract preserved; WR-03/04/05 frontend fixes; WR-01/02 verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-td-cleanup-write-sandbox-primitives-daemon-routes/123-RESEARCH.md
@.planning/phases/123-td-cleanup-write-sandbox-primitives-daemon-routes/123-PATTERNS.md

<interfaces>
TD-5 reference implementation (PORT this logic) — internal/tui/joincode_prompt.go:153-208 exchangeJoinCodeCmd:
- Build a dedicated http.Client with Timeout 10s, Transport TLSClientConfig MinVersion TLS 1.2, and CheckRedirect returning http.ErrUseLastResponse (do NOT auto-follow the 303).
- POST form{"code": code} to remoteBaseURL + "/join/exchange".
- Expect resp.StatusCode == http.StatusSeeOther (303); any other status is the error path.
- loc := resp.Header.Get("Location"); if it contains "/join?error=" extract the kind (TrimPrefix then cut at first '&') and map onto the existing error substrings.
- url.Parse(loc); u.Query().Get("cap") is the token.
- friendlyJoinCodeError (joincode_prompt.go:214+) shows the substring-match contract.

Current buggy site — internal/daemon/client_remote_files.go:56-118 ExchangeJoinCodeAtURL — JSON-decodes resp.Body on the success path (lines 107-117) but the webserver returns 303 + Location with NO body.

Shared package-level client — internal/daemon/client_remote_files.go:37-42 remoteFilesHTTPClient — is ALSO used by RegisterRemoteCap (client_remote_files.go:124-144). DO NOT add CheckRedirect to it. Construct a dedicated client inside ExchangeJoinCodeAtURL.

Error-substring contract the modal UI pivots on (documented at client_remote_files.go:48-53): "expired", "invalid", "not-found", "session-gone". The existing 4xx-status mapping (client_remote_files.go:89-105) already produces these; map the new /join?error=<kind> Location onto the same substrings.

TD-4 corrected paths (PATTERNS.md TD-4 section — RESEARCH paths were partly stale):
- WR-01 (block /app/ dir listing): internal/webserver/server.go ~:583-588 — comment "Phase 120 WR-01" present → ALREADY DONE, verify-only.
- WR-02 (cache-control on /app/): internal/webserver/server.go ~:556-565 — WR-02 caching policy documented as implemented → ALREADY DONE, verify-only.
- WR-03 (joinPath name sanitize): frontend/src/components/FileBrowserTab.tsx ~:397-409 navigateInto — partial defence present citing "Phase 120 WR-03"; confirm coverage, extend if a code-path is missing.
- WR-04 (mtime fallback): frontend/src/components/FileBrowser/FileRow.tsx formatRowMtime — add empty-string mtime fallback.
- WR-05 (comment clarity): frontend/src/lib/humanSize.ts:7-22 — comment-only; tests at frontend/src/lib/__tests__/humanSize.test.ts.
WR-06 (BreadcrumbBar.tsx) → NO ACTION. WR-07 (PreviewPane.tsx) → already fixed in 120-04. Do not re-touch either (Chesterton's Fence).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TD-5 — 303-aware ExchangeJoinCodeAtURL (FSW-10)</name>
  <read_first>
    - internal/daemon/client_remote_files.go (the buggy ExchangeJoinCodeAtURL :56-118; shared client :37-42; error-contract doc :48-53; 4xx mapping :89-105)
    - internal/tui/joincode_prompt.go (exchangeJoinCodeCmd :153-208 — reference impl; friendlyJoinCodeError :214+)
    - internal/daemon/client_test.go (existing test conventions — httptest server setup)
  </read_first>
  <files>internal/daemon/client_remote_files.go, internal/daemon/client_test.go</files>
  <behavior>
    - TestExchangeJoinCode_303Success: an httptest server replies 303 with Location "/sessions/abc?cap=TOKEN123"; ExchangeJoinCodeAtURL returns "TOKEN123", nil.
    - TestExchangeJoinCode_NoAutoFollow: the test server records whether the redirect target ("/sessions/abc") was hit; it MUST NOT be — proving CheckRedirect: ErrUseLastResponse is in effect.
    - TestExchangeJoinCode_ErrorLocation: Location "/join?error=expired" → error whose message contains "expired"; repeat for invalid / not-found / session-gone substrings.
    - TestExchangeJoinCode_EmptyCap: 303 with Location lacking a cap query → error containing "invalid".
    - TestExchangeJoinCode_SharedClientUnchanged: assert remoteFilesHTTPClient.CheckRedirect is still nil after the call (no mutation of the shared client).
  </behavior>
  <action>
    Add the tests above to internal/daemon/client_test.go (table or per-case) using net/http/httptest. Then rewrite the success path of ExchangeJoinCodeAtURL in internal/daemon/client_remote_files.go by porting joincode_prompt.go:155-206: construct a dedicated http.Client inside the function (Timeout 10s, Transport with TLSClientConfig MinVersion TLS 1.2 and InsecureSkipVerify matching the existing tailnet self-signed precedent, CheckRedirect returning http.ErrUseLastResponse); POST the form to /join/exchange; on resp.StatusCode == http.StatusSeeOther read the Location header, handle the "/join?error=<kind>" shape first mapping kind onto the existing expired/invalid/not-found/session-gone substrings, otherwise url.Parse + Query().Get("cap"); empty cap → "invalid" error. Preserve the existing 4xx/410/404/408 status-to-substring mapping for non-303 responses. Do NOT touch the package-level remoteFilesHTTPClient.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -race ./internal/daemon/ -run 'TestExchangeJoinCode' 2>&1 | tail -20</automated>
  </verify>
  <done>All TestExchangeJoinCode_* cases pass under -race; the shared client is unmutated; the error-substring contract is preserved.</done>
  <acceptance_criteria>
    - `go test -race ./internal/daemon/ -run 'TestExchangeJoinCode'` exits 0.
    - grep shows ErrUseLastResponse and StatusSeeOther inside ExchangeJoinCodeAtURL.
    - remoteFilesHTTPClient.CheckRedirect remains nil (asserted by TestExchangeJoinCode_SharedClientUnchanged).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: TD-4 — WR-03/04/05 frontend hardening + WR-01/02 verify (FSW-11)</name>
  <read_first>
    - internal/webserver/server.go (the GET /app/ handler region ~:556-588 — confirm WR-01 dir-listing block and WR-02 cache policy)
    - frontend/src/components/FileBrowserTab.tsx (navigateInto / joinPath ~:397-409)
    - frontend/src/components/FileBrowser/FileRow.tsx (formatRowMtime)
    - frontend/src/lib/humanSize.ts (:7-22) and frontend/src/lib/__tests__/humanSize.test.ts
    - .planning/phases/123-td-cleanup-write-sandbox-primitives-daemon-routes/123-PATTERNS.md (TD-4 source-locations table)
  </read_first>
  <files>frontend/src/components/FileBrowserTab.tsx, frontend/src/components/FileBrowser/FileRow.tsx, frontend/src/lib/humanSize.ts, internal/webserver/server.go (verify-only)</files>
  <action>
    WR-01/WR-02 (verify-only): read the GET /app/ handler in internal/webserver/server.go and confirm the directory-index block and the index.html no-store + hashed-asset cache policy match the requirement. Do NOT modify if already satisfied; record the verification in the SUMMARY (Chesterton's Fence — explain why the existing code suffices).
    WR-03: in frontend/src/components/FileBrowserTab.tsx confirm navigateInto strips a leading slash from server-returned names before joining; if any name-join code-path lacks the sanitization, apply the same strip used at the existing "Phase 120 WR-03" site.
    WR-04: in frontend/src/components/FileBrowser/FileRow.tsx make formatRowMtime return a safe fallback (e.g. an em-dash or empty render) when the mtime string is empty, instead of producing an Invalid Date.
    WR-05: in frontend/src/lib/humanSize.ts clarify the unit-threshold comment (:7-22) — comment-only, no behavior change.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- --run humanSize 2>&1 | tail -15 && pnpm exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>WR-03/04/05 applied at the corrected paths; humanSize tests pass; tsc clean; WR-01/02 confirmed already-satisfied and recorded in SUMMARY without re-touching.</done>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` reports no new errors in the touched files.
    - humanSize test suite passes.
    - FileRow.formatRowMtime returns a non-"Invalid Date" fallback for an empty mtime (asserted or visually confirmed in code).
    - SUMMARY records WR-01/WR-02 as verified-already-satisfied with the line references and rationale.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI → remote webserver /join/exchange | the 303 Location carries the cap token in a query param over a self-signed tailnet TLS link |
| server-returned file names → frontend path join | listing names are untrusted; a leading-slash/`..` name could alter the joined navigation path |
| embedded /app/ bundle → browser | directory-index responses would leak bundle file names + mtimes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-07 | Information Disclosure | cap token in TD-5 error/log path | mitigate | dedicated client, no token logging; existing redactCapTokenFromError contract preserved; token only returned, never logged (Task 1) |
| T-123-08 | Spoofing / DoS | auto-following the 303 chases /sessions and loses the cap (silent failure) | mitigate | CheckRedirect: ErrUseLastResponse on the dedicated client; detect 303 explicitly (Task 1) |
| T-123-09 | Tampering | shared remoteFilesHTTPClient behavior change affecting RegisterRemoteCap | mitigate | dedicated client constructed inside the function; TestExchangeJoinCode_SharedClientUnchanged asserts no mutation (Task 1) |
| T-123-10 | Information Disclosure | /app/ directory-index listing leaks bundle file names | accept (verify) | WR-01 already blocks dir-index via SPA fallback (Phase 120); confirmed verify-only (Task 2) |
| T-123-11 | Tampering | server-returned name with leading slash alters frontend nav path | mitigate | WR-03 joinPath leading-slash strip (Task 2) — UI defense-in-depth atop the server sandbox |
</threat_model>

<verification>
- `go test -race ./internal/daemon/ -run 'TestExchangeJoinCode'` green.
- `gofmt -l internal/daemon/client_remote_files.go` prints nothing.
- frontend: `pnpm exec tsc --noEmit` clean; humanSize tests pass.
</verification>

<success_criteria>
- ExchangeJoinCodeAtURL parses a 303 Location ?cap=<token> and returns the token (maps to phase success criterion #4) with the error-substring contract intact.
- The shared remoteFilesHTTPClient is not mutated.
- TD-4 WR-03/04/05 applied; WR-01/02 verified already-satisfied (FSW-11).
</success_criteria>

<output>
Create `.planning/phases/123-td-cleanup-write-sandbox-primitives-daemon-routes/123-02-SUMMARY.md` when done.
</output>
