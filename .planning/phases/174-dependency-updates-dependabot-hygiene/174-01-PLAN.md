---
phase: 174-dependency-updates-dependabot-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/build.yml
  - .github/workflows/e2e.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [DEP-01]
must_haves:
  truths:
    - "The 4 low-risk CI-action Dependabot bumps (#114, #113, #103, #85) are applied on branch v4.2-funnel-sharing as SHA-pinned edits."
    - "Every occurrence of each bumped action across build.yml, e2e.yml, release.yml carries the new SHA — none left on the old SHA."
    - "All three workflow files still parse as valid YAML after the edits."
    - "Dependabot PRs #114, #113, #103, #85 are closed citing Phase 174."
  artifacts:
    - .github/workflows/build.yml
    - .github/workflows/e2e.yml
    - .github/workflows/release.yml
  key_links:
    - "setup-go (#113) and pnpm/action-setup (#85) SHAs must be updated in ALL 3 files (6 occurrences each), not just build.yml — a partial edit drifts CI."
    - "attest-build-provenance (#114) is a supply-chain integrity tool; its new SHA 0f67c3f4… must match upstream v4.1.1 exactly (copied from Dependabot's diff)."
---

<objective>
Apply the four low-risk CI-action version bumps from DEP-01 as SHA-pinned edits on the active v4.2 branch, then close the corresponding Dependabot PRs.

Bumps (each a pure `.github/workflows/*.yml` SHA swap — no Go/frontend/compiled-code impact):
- #114 actions/attest-build-provenance 4.1.0 → 4.1.1 (release.yml, 4 occurrences)
- #113 actions/setup-go 6.4.0 → 6.5.0 (build.yml×1, e2e.yml×1, release.yml×4 = 6 total)
- #103 softprops/action-gh-release 3.0.0 → 3.0.1 (release.yml, 1 occurrence)
- #85 pnpm/action-setup 6.0.8 → 6.0.9 (build.yml×1, e2e.yml×1, release.yml×4 = 6 total)

Purpose: keep pinned CI actions current without touching application code. These are the lowest-risk half of DEP-01 (CI runner/release-pipeline actions only).

Output: 3 edited workflow files + 4 closed Dependabot PRs.

Merge-target note (user-confirmed): these bumps land as commits on `v4.2-funnel-sharing` (behind the local gate) and ship as part of v4.2 — they are NOT merged into `main` via the Dependabot PRs. Each Dependabot PR is CLOSED citing this phase.

No-new-tests assertion: this plan adds ZERO automated test files (it edits pinned action SHAs only). Per the CLAUDE.md standing rule, no TESTING.md Suite-Manifest / traceability-map rows are required. These edits change only `uses:` SHA lines — they do NOT alter any `build.yml` matrix entry, job name, or branch-protection check-context name, so TESTING.md Section 3 (branch protection) and Section 4 need no update either.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/174-dependency-updates-dependabot-hygiene/174-RESEARCH.md
@.planning/phases/174-dependency-updates-dependabot-hygiene/174-VALIDATION.md
</context>

<artifacts_produced>
Files this plan creates or modifies (no new source symbols are created):
- `.github/workflows/build.yml` — setup-go + pnpm/action-setup SHA bumps (1 each)
- `.github/workflows/e2e.yml` — setup-go + pnpm/action-setup SHA bumps (1 each)
- `.github/workflows/release.yml` — attest-build-provenance (×4), setup-go (×4), action-gh-release (×1), pnpm/action-setup (×4) SHA bumps
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Bump the two multi-file CI actions (setup-go #113, pnpm/action-setup #85)</name>
  <files>.github/workflows/build.yml, .github/workflows/e2e.yml, .github/workflows/release.yml</files>
  <read_first>
    - .planning/phases/174-dependency-updates-dependabot-hygiene/174-RESEARCH.md — "Exact SHA changes for the 4 low-risk action bumps" block (the verbatim new SHAs) and Pitfall 3 (missing occurrences).
    - .github/workflows/build.yml (line ~45 setup-go, ~69 pnpm), e2e.yml (~26, ~31), release.yml (setup-go ×4, pnpm ×4).
  </read_first>
  <action>
    Replace every occurrence of the setup-go and pnpm/action-setup SHA pins across all three workflow files. Both actions appear 6 times each (build.yml×1, e2e.yml×1, release.yml×4). Use a replace-all edit per file — the old SHA string is identical at every occurrence, so a single replace-all per file catches them all. Keep the trailing version comment updated to match the new tag.

    setup-go (#113 per Phase 174): replace the pinned SHA `4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0` with `924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0` (copy the SHA verbatim from RESEARCH.md — do not hand-type).

    pnpm/action-setup (#85 per Phase 174): replace the pinned SHA `0e279bb959325dab635dd2c09392533439d90093 # v6.0.8` with `0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9`.

    Do NOT switch any action to a floating tag — preserve SHA-pinning (supply-chain control). Do NOT touch actions/setup-node, actions/upload-artifact, or actions/checkout.
  </action>
  <verify>
    <automated>python3 -c "import yaml; [yaml.safe_load(open(f)) for f in ['.github/workflows/build.yml','.github/workflows/e2e.yml','.github/workflows/release.yml']]; print('yaml-ok')" && test $(grep -rl '924ae3a1cded613372ab5595356fb5720e22ba16' .github/workflows/build.yml .github/workflows/e2e.yml .github/workflows/release.yml | wc -l) -eq 3 && test $(grep -rl '0ebf47130e4866e96fce0953f49152a61190b271' .github/workflows/build.yml .github/workflows/e2e.yml .github/workflows/release.yml | wc -l) -eq 3 && test $(grep -rho 'setup-go@924ae3a1cded613372ab5595356fb5720e22ba16' .github/workflows/*.yml | wc -l) -eq 6 && test $(grep -rho 'action-setup@0ebf47130e4866e96fce0953f49152a61190b271' .github/workflows/*.yml | wc -l) -eq 6</automated>
  </verify>
  <acceptance_criteria>
    New setup-go SHA (924ae3a1…) present in all 3 files, 6 total occurrences; new pnpm SHA (0ebf4713…) present in all 3 files, 6 total occurrences; all three YAML files still parse. `git diff` shows only SHA + version-comment lines changed.
  </acceptance_criteria>
  <done>
    setup-go and pnpm/action-setup are bumped everywhere they appear; no occurrence remains on the old SHA; YAML valid.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump the two release-only CI actions (attest-build-provenance #114, action-gh-release #103)</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .planning/phases/174-dependency-updates-dependabot-hygiene/174-RESEARCH.md — SHA block + Security Domain note (attest-build-provenance is itself a supply-chain integrity tool; confirm new SHA matches upstream v4.1.1).
    - .github/workflows/release.yml (attest at lines ~99, ~179, ~281, ~433; action-gh-release at ~438).
  </read_first>
  <action>
    In release.yml only, replace all attest-build-provenance and action-gh-release SHA pins.

    attest-build-provenance (#114 per Phase 174): replace `a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32 # v4.1.0` with `0f67c3f4856b2e3261c31976d6725780e5e4c373 # v4.1.1` (4 occurrences — use replace-all).

    action-gh-release (#103 per Phase 174): replace `b4309332981a82ec1c5618f44dd2e27cc8bfbfda # v3.0.0` with `718ea10b132b3b2eba29c1007bb80653f286566b # v3.0.1` (1 occurrence).

    Copy SHAs verbatim from RESEARCH.md. Preserve SHA-pinning; do not use floating tags.
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml')); print('yaml-ok')" && test $(grep -o 'attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373' .github/workflows/release.yml | wc -l) -eq 4 && test $(grep -o 'action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b' .github/workflows/release.yml | wc -l) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    New attest SHA (0f67c3f4…) appears 4× and new gh-release SHA (718ea10b…) appears 1× in release.yml; file parses as YAML; git diff shows only SHA + comment changes.
  </acceptance_criteria>
  <done>
    Both release-only actions bumped; release.yml valid YAML; no old SHA remains for these two actions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit the bumps and close Dependabot PRs #114/#113/#103/#85</name>
  <files>.github/workflows/build.yml, .github/workflows/e2e.yml, .github/workflows/release.yml</files>
  <read_first>
    - .planning/phases/174-dependency-updates-dependabot-hygiene/174-RESEARCH.md — "Merge / branch strategy" (apply on v4.2-funnel-sharing + close PRs, do NOT merge to main) and "Live Dependabot PR State" table (PR numbers + head SHAs for cross-check).
  </read_first>
  <action>
    Confirm the working branch is `v4.2-funnel-sharing` (git branch --show-current). Stage and commit the workflow edits from Tasks 1-2 as a single commit: `ci(174): bump low-risk pinned CI actions (setup-go, pnpm, attest-provenance, gh-release)`.

    Then close each Dependabot PR with a comment citing this phase and the merge-target decision. Use gh CLI:
    - `gh pr close 114 --comment "Applied in Phase 174 on v4.2-funnel-sharing (attest-build-provenance 4.1.0→4.1.1, SHA 0f67c3f4…). Bump ships with v4.2; not merged to main via this PR."`
    - `gh pr close 113 --comment "Applied in Phase 174 on v4.2-funnel-sharing (setup-go 6.4.0→6.5.0, all 6 occurrences). Bump ships with v4.2."`
    - `gh pr close 103 --comment "Applied in Phase 174 on v4.2-funnel-sharing (action-gh-release 3.0.0→3.0.1). Bump ships with v4.2."`
    - `gh pr close 85 --comment "Applied in Phase 174 on v4.2-funnel-sharing (pnpm/action-setup 6.0.8→6.0.9, all 6 occurrences). Bump ships with v4.2."`

    Do NOT run `gh pr merge` on any of these. If a `gh pr close` fails on auth, surface the error and stop (do not force).
  </action>
  <verify>
    <automated>test "$(git branch --show-current)" = "v4.2-funnel-sharing" && for n in 114 113 103 85; do test "$(gh pr view $n --json state -q .state)" = "CLOSED" || { echo "PR #$n not closed"; exit 1; }; done && echo "all-4-closed"</automated>
  </verify>
  <acceptance_criteria>
    Workflow edits committed on v4.2-funnel-sharing; `gh pr view <n> --json state` returns CLOSED for #114, #113, #103, #85; none merged.
  </acceptance_criteria>
  <done>
    All four low-risk CI-action PRs closed citing Phase 174; bumps committed on the v4.2 branch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependabot PR diff → repo workflow files | An action SHA pin is copied from an external PR into CI; a wrong/poisoned SHA would run attacker code in CI with repo permissions. |
| CI workflow → release signing (attest-build-provenance) | The provenance action signs release artifacts; a tampered version undermines supply-chain attestation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-01 | Tampering | Pinned action SHAs in workflow YAML | high | mitigate | Copy each new SHA verbatim from RESEARCH.md (resolved from Dependabot's own diff against the upstream tag); keep SHA-pinning, never switch to a floating tag; positive-count verify asserts the exact new SHA at every expected occurrence. |
| T-174-02 | Tampering | attest-build-provenance (release signing) | high | mitigate | New SHA 0f67c3f4… must match upstream v4.1.1; it is a supply-chain integrity tool, so verify before applying (RESEARCH.md Security Domain). |
| T-174-SC | Tampering | Action version bumps (no new packages) | low | accept | All four are incumbent, already-vetted SHA-pinned official/established actions (RESEARCH.md Package Legitimacy Audit — all OK). No new dependency introduced, so no [ASSUMED]/[SUS] legitimacy checkpoint applies. |
</threat_model>

<verification>
- All three workflow files parse as YAML (python3 yaml.safe_load).
- Positive SHA counts: setup-go 6×, pnpm 6×, attest 4×, gh-release 1× on the NEW SHAs.
- `git diff` on the workflow files shows only SHA + trailing version-comment lines changed (no matrix/job/name changes → branch-protection contexts unchanged → no TESTING.md Section 3/4 update needed).
- `gh pr view` returns CLOSED for #114/#113/#103/#85; none merged.
</verification>

<success_criteria>
Four low-risk CI-action bumps applied on v4.2-funnel-sharing with every occurrence updated, all workflows valid YAML, and the four Dependabot PRs closed citing Phase 174.
</success_criteria>

<output>
Create `.planning/phases/174-dependency-updates-dependabot-hygiene/174-01-SUMMARY.md` when done.
</output>
