---
phase: 174-dependency-updates-dependabot-hygiene
plan: 02
type: execute
wave: 2
depends_on: [174-01]
files_modified:
  - go.mod
  - go.sum
autonomous: true
requirements: [DEP-01]
must_haves:
  truths:
    - "coder/websocket is bumped 1.8.14→1.8.15 and the webserver + relay test suites stay green."
    - "golang.org/x/term is bumped 0.43.0→0.44.0 and the full Go build/vet/test suite stays green."
    - "goreleaser/nfpm/v2 is bumped 2.46.3→2.47.0 and the deb packaging step still produces a .deb."
    - "No bump transitively pulls go-webview2 to >=1.0.20 — it stays pinned at v1.0.19 (Windows-build guard)."
    - "Dependabot PRs #89, #106, #105 are closed citing Phase 174."
  artifacts:
    - go.mod
    - go.sum
  key_links:
    - "coder/websocket carries live PTY relay frames — the webserver+relay race tests are the real gate (#89)."
    - "nfpm is a go.mod-pinned CLI installed at release; the gate is that `nfpm package --packager deb` still emits a .deb (deb-only pipeline — no rpm)."
    - "`go mod tidy` must not raise go-webview2 — review the go.sum diff to confirm it stays v1.0.19."
---

<objective>
Apply the three low-risk Go-module bumps from DEP-01 on the v4.2 branch, each isolated behind its own build+test gate, then close the corresponding Dependabot PRs.

Bumps:
- #89 github.com/coder/websocket 1.8.14 → 1.8.15 (patch — gate on webserver + relay tests)
- #106 golang.org/x/term 0.43.0 → 0.44.0 (dep-refresh only — full build/test gate)
- #105 github.com/goreleaser/nfpm/v2 2.46.3 → 2.47.0 (release build tool — deb-packaging gate)

Purpose: keep incumbent Go deps current. Isolating each bump makes any regression attributable to a single dependency.

Output: edited go.mod + go.sum + 3 closed Dependabot PRs.

Merge-target note (user-confirmed): bumps land as commits on `v4.2-funnel-sharing` and ship with v4.2; the Dependabot PRs are CLOSED, not merged to main.

Flaky-CI caveat (RESEARCH.md Pitfall 1): the red badges on these PRs are pre-existing flaky tests (`playwright [e2e]`, `internal/daemon` chat-store / exit-event / alias-perms, `internal/files` two-writer race) — NOT the bumps. Verify LOCALLY. A local `go test -short ./...` that is green except for those known-flaky packages counts as pass; a NEW failure in a package the bump touches (internal/webserver, internal/relay, internal/attach, internal/statusbar) is a real regression.

Frontend gate intentionally omitted: DEP-01's gate wording includes `tsc && vite build`, but no task runs the frontend build because no frontend/node dependency is bumped in this phase (all three bumps are Go modules; pnpm/action-setup #85 is a CI-runner action handled in plan 01, not a frontend package). The `tsc && vite build` leg would verify nothing these bumps changed, so it is deliberately not run here — adding a frontend-build task would be a no-op gate. (RESEARCH.md confirms frontend is unaffected.)

No-new-tests assertion: this plan authors ZERO new automated test files — it re-runs the existing suite against bumped modules. No TESTING.md Suite-Manifest / traceability-map rows required. No `build.yml` matrix change, so no TESTING.md Section 3/4 update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/174-dependency-updates-dependabot-hygiene/174-RESEARCH.md
@.planning/phases/174-dependency-updates-dependabot-hygiene/174-VALIDATION.md
@.github/workflows/release.yml
</context>

<artifacts_produced>
Files this plan modifies (no new source symbols):
- `go.mod` — coder/websocket, golang.org/x/term, goreleaser/nfpm/v2 version bumps
- `go.sum` — updated checksums (review diff: go-webview2 must remain v1.0.19)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Bump coder/websocket 1.8.15 (#89) — gate on webserver + relay tests</name>
  <files>go.mod, go.sum</files>
  <read_first>
    - .planning/phases/174-dependency-updates-dependabot-hygiene/174-RESEARCH.md — "Go module bumps — how to apply", Changelog risk review (coder/websocket single-frame-compression is a no-op when permessage-deflate is off — the default), Pitfall 1 (flaky tests).
    - .planning/phases/174-dependency-updates-dependabot-hygiene/174-VALIDATION.md — task 174-02-01 gate command.
  </read_first>
  <action>
    On branch v4.2-funnel-sharing, bump coder/websocket (#89 per Phase 174): run `go get github.com/coder/websocket@v1.8.15` then `go mod tidy`. Review the go.sum diff and confirm no unexpected transitive bump — in particular go-webview2 MUST stay at v1.0.19 (Windows-build guard). Do not edit any Go source; this is a pure version bump.

    Gate: coder/websocket carries the live PTY relay frames, so the webserver + relay suites are the authoritative check. Run the full build/vet plus the targeted race tests. If a NEW failure appears in internal/webserver or internal/relay, treat it as a real regression and stop (RULE 0). Pre-existing flakes in unrelated packages (playwright, internal/daemon, internal/files) do not block.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -race -short ./internal/webserver/... ./internal/relay/... && test "$(go list -m -f '{{.Version}}' github.com/coder/websocket)" = "v1.8.15" && test "$(go list -m -f '{{.Version}}' github.com/wailsapp/go-webview2)" = "v1.0.19"</automated>
  </verify>
  <acceptance_criteria>
    go.mod shows coder/websocket v1.8.15; go build + go vet clean; webserver + relay race tests green; go-webview2 unchanged at v1.0.19.
  </acceptance_criteria>
  <done>
    coder/websocket bumped and gated green on webserver/relay; no transitive go-webview2 change.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump x/term 0.44.0 (#106) + nfpm/v2 2.47.0 (#105) — full build/test + deb-packaging gate</name>
  <files>go.mod, go.sum</files>
  <read_first>
    - .planning/phases/174-dependency-updates-dependabot-hygiene/174-RESEARCH.md — Changelog risk review (x/term is dep-refresh only; nfpm 2.47.0 minor, no break to `nfpm package --packager deb`) and "Packaging reality check" (pipeline produces DEB ONLY — no rpm; do not hunt for an rpm build).
    - .github/workflows/release.yml lines ~255-277 — the nfpm install + `nfpm package --packager deb` recipe to replicate the deb gate.
    - .planning/phases/174-dependency-updates-dependabot-hygiene/174-VALIDATION.md — tasks 174-02-02 / 174-02-03 gates.
  </read_first>
  <action>
    On branch v4.2-funnel-sharing, bump both low-risk modules (per Phase 174):
    - x/term (#106): `go get golang.org/x/term@v0.44.0`
    - nfpm/v2 (#105): `go get github.com/goreleaser/nfpm/v2@v2.47.0`
    Then `go mod tidy`. Review the go.sum diff (confirm go-webview2 stays v1.0.19; watch x/crypto+x/net transitive refreshes noted in the nfpm changelog — expected, benign).

    x/term gate: full `go build ./... && go vet ./... && go test -short ./...` (x/term feeds cmd_attach / statusbar / attach_unix). Discount only the known-flaky packages from RESEARCH.md Pitfall 1.

    nfpm deb gate (deb-only — no rpm): install the pinned nfpm CLI from go.mod and produce a .deb the way release.yml does. Install: `go install github.com/goreleaser/nfpm/v2/cmd/nfpm@$(go list -m -f '{{.Version}}' github.com/goreleaser/nfpm/v2)`. Then, in a fresh `mktemp -d` scratch dir, generate a minimal nfpm.yaml (follow the release.yml:255-277 recipe — name/arch/version/maintainer + a single dummy contents entry pointing at a placeholder binary in the same scratch dir) and run `nfpm package --packager deb --config <scratch>/nfpm.yaml --target <scratch>/agenthub.deb`. Success = a non-empty .deb is produced (`test -s <scratch>/agenthub.deb`). Remove the scratch dir after (do not commit anything). This deb-production step is run inside the automated verify below, so the "still emits a .deb" must_have is proven automatically, not just by inspection.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -short ./internal/attach/... ./internal/statusbar/... && test "$(go list -m -f '{{.Version}}' golang.org/x/term)" = "v0.44.0" && test "$(go list -m -f '{{.Version}}' github.com/goreleaser/nfpm/v2)" = "v2.47.0" && test "$(go list -m -f '{{.Version}}' github.com/wailsapp/go-webview2)" = "v1.0.19" && go install github.com/goreleaser/nfpm/v2/cmd/nfpm@$(go list -m -f '{{.Version}}' github.com/goreleaser/nfpm/v2) && $(go env GOPATH)/bin/nfpm --version && D=$(mktemp -d) && printf 'x' > "$D/agenthub" && printf 'name: agenthub\narch: amd64\nversion: 0.0.0\nmaintainer: test <test@example.com>\ncontents:\n  - src: %s/agenthub\n    dst: /usr/bin/agenthub\n' "$D" > "$D/nfpm.yaml" && $(go env GOPATH)/bin/nfpm package --packager deb --config "$D/nfpm.yaml" --target "$D/agenthub.deb" && test -s "$D/agenthub.deb" && rm -rf "$D"</automated>
  </verify>
  <acceptance_criteria>
    go.mod shows x/term v0.44.0 and nfpm/v2 v2.47.0; go build/vet clean; attach + statusbar tests green; go-webview2 unchanged at v1.0.19; nfpm 2.47.0 installs, reports its version, and `nfpm package --packager deb` emits a non-empty .deb — all asserted automatically in the verify block (`nfpm package ... && test -s .../agenthub.deb`).
  </acceptance_criteria>
  <done>
    x/term and nfpm bumped; full build/test green; a .deb is produced with nfpm 2.47.0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit the module bumps and close Dependabot PRs #89/#106/#105</name>
  <files>go.mod, go.sum</files>
  <read_first>
    - .planning/phases/174-dependency-updates-dependabot-hygiene/174-RESEARCH.md — "Merge / branch strategy" and "Live Dependabot PR State" (PR numbers).
  </read_first>
  <action>
    Confirm branch is v4.2-funnel-sharing. Commit go.mod + go.sum: `deps(174): bump coder/websocket 1.8.15, x/term 0.44.0, nfpm/v2 2.47.0`. (A single commit is fine — each bump was already isolated-verified in Tasks 1-2.)

    Close each Dependabot PR citing Phase 174 (do NOT `gh pr merge`):
    - `gh pr close 89 --comment "Applied in Phase 174 on v4.2-funnel-sharing (coder/websocket 1.8.14→1.8.15). Gated green on webserver+relay tests. Ships with v4.2."`
    - `gh pr close 106 --comment "Applied in Phase 174 on v4.2-funnel-sharing (golang.org/x/term 0.43.0→0.44.0). Full build/test green. Ships with v4.2."`
    - `gh pr close 105 --comment "Applied in Phase 174 on v4.2-funnel-sharing (goreleaser/nfpm/v2 2.46.3→2.47.0). Deb packaging verified. Ships with v4.2."`

    If any `gh pr close` fails on auth, surface the error and stop.
  </action>
  <verify>
    <automated>test "$(git branch --show-current)" = "v4.2-funnel-sharing" && for n in 89 106 105; do test "$(gh pr view $n --json state -q .state)" = "CLOSED" || { echo "PR #$n not closed"; exit 1; }; done && echo "all-3-closed"</automated>
  </verify>
  <acceptance_criteria>
    go.mod/go.sum committed on v4.2-funnel-sharing; #89, #106, #105 all CLOSED (not merged).
  </acceptance_criteria>
  <done>
    Three Go-module PRs closed citing Phase 174; bumps committed on the v4.2 branch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `go get` / `go mod tidy` → go.mod + go.sum | A module bump can transitively raise other modules; a raise of go-webview2 to >=1.0.20 would silently break the Windows build. |
| coder/websocket → live PTY relay frames | The websocket lib carries untrusted terminal bytes across the relay; a behavior change could alter framing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-02 | Tampering | Transitive go.sum bumps (esp. go-webview2) | high | mitigate | `go mod tidy` after each `go get`; explicit `go list -m go-webview2 == v1.0.19` assertion in every task's verify; review go.sum diff before commit. |
| T-174-03 | Denial of Service | coder/websocket relay framing change | medium | mitigate | Gate #89 on `go test -race -short ./internal/webserver/... ./internal/relay/...`; permessage-deflate is off by default so the single-frame-compression change is a no-op (RESEARCH.md A2). |
| T-174-SC | Tampering | Go module version bumps (no new packages) | low | accept | All three are incumbent, already-vetted direct deps (RESEARCH.md Package Legitimacy Audit — all OK); patch/minor/dep-refresh only; no new dependency, so no legitimacy checkpoint applies. |
</threat_model>

<verification>
- go build ./... and go vet ./... clean after every bump.
- coder/websocket gate: `go test -race -short ./internal/webserver/... ./internal/relay/...` green.
- x/term gate: full `go test -short ./...` green except the RESEARCH.md-documented flaky packages.
- go-webview2 stays v1.0.19 (asserted in each task's verify).
- nfpm 2.47.0 produces a .deb (deb-only pipeline).
- #89/#106/#105 CLOSED, none merged.
</verification>

<success_criteria>
Three Go-module bumps applied on v4.2-funnel-sharing, each gated green locally (discounting known flakes), go-webview2 untouched at v1.0.19, deb packaging still builds, and the three Dependabot PRs closed citing Phase 174.
</success_criteria>

<output>
Create `.planning/phases/174-dependency-updates-dependabot-hygiene/174-02-SUMMARY.md` when done.
</output>
