---
phase: 174-dependency-updates-dependabot-hygiene
plan: 03
type: execute
wave: 3
depends_on: [174-02]
files_modified:
  - .github/dependabot.yml
autonomous: true
requirements: [DEP-02]
must_haves:
  truths:
    - "dependabot.yml gomod block ignores the risky wails/v2 2.12.0 bump (>=2.11.0) without freezing future patch security fixes."
    - "dependabot.yml gomod block ignores the tailscale.com 1.100.0 bump (>=1.100.0) without freezing 1.98.x/1.99.x patches."
    - "dependabot.yml github-actions block ignores actions/checkout major bumps (v7+) while allowing v6 patch/minor security updates."
    - "Each ignore entry carries an inline comment stating the defer rationale + revisit condition."
    - "Dependabot PRs #104, #88, #102 are closed citing this phase's ignore rationale."
  artifacts:
    - .github/dependabot.yml
  key_links:
    - "wails goes under gomod, tailscale under gomod, checkout under github-actions — an ignore under the wrong ecosystem silently does nothing (RESEARCH.md Pitfall 4)."
    - "The wails ignore only stops Dependabot RE-OPENING the PR; the real Windows-build guard is the existing go-webview2 v1.0.19 pin + NOT running `go get wails@2.12.0`. The plan must not claim the ignore alone protects the build."
    - "dependabot.yml is read from the DEFAULT branch (main); these ignore entries take effect once v4.2 merges to main — until then a re-open is possible (expected, not a defect)."
---

<objective>
Formally defer the three high-risk upgrades (DEP-02) by adding surgical `ignore:` entries to `.github/dependabot.yml`, then close the three Dependabot PRs citing this phase's rationale.

Deferrals:
- #104 wailsapp/wails/v2 2.10.2 → 2.12.0 — requires go-webview2 v1.0.22, but the project pins v1.0.19 (>=1.0.20 breaks the Windows build, Phase 145). Ignore blocks re-opening; the Windows guard remains the go-webview2 pin.
- #88 tailscale.com 1.98.3 → 1.100.0 — the entire Funnel feature was built + live-UAT'd on 1.98.3; bumping requires a full off-tailnet Funnel re-UAT, out of scope pre-ship.
- #102 actions/checkout v6 → v7 — major bump (fork-PR checkout + ESM/runtime change); evaluate in a throwaway branch later.

Purpose: stop these PRs from re-opening every Monday while preserving future security-patch flow on all three lines (surgical `versions` / `update-types` ignores, not whole-package blocks).

Output: edited .github/dependabot.yml + 3 closed Dependabot PRs.

Honest-guard note: the wails ignore does NOT by itself protect the Windows build — an ignore only suppresses Dependabot-opened PRs; it cannot cap the transitive minimum-version requirement wails 2.12.0 imposes on go-webview2. The real guard is the existing go-webview2 v1.0.19 pin + NOT running `go get wails@v2.12.0`. The ignore's job is only to stop the #104 PR re-appearing (RESEARCH.md Pitfall 2).

Effective-branch note: Dependabot reads its config from the DEFAULT branch (main). Committing these entries on v4.2-funnel-sharing makes them effective once v4.2 merges to main; a transient re-open before that merge is expected, not a plan defect (VALIDATION.md manual-only row).

No-new-tests assertion: config-only edit; ZERO new automated test files; no TESTING.md Suite-Manifest / traceability-map rows; no `build.yml` matrix change → no TESTING.md Section 3/4 update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/174-dependency-updates-dependabot-hygiene/174-RESEARCH.md
@.planning/phases/174-dependency-updates-dependabot-hygiene/174-VALIDATION.md
@.github/dependabot.yml
</context>

<artifacts_produced>
Files this plan modifies (no new source symbols):
- `.github/dependabot.yml` — add wails/v2 + tailscale.com ignore entries under gomod; add an `ignore:` block with actions/checkout under github-actions
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add gomod ignore entries for wails/v2 (#104) and tailscale.com (#88)</name>
  <files>.github/dependabot.yml</files>
  <read_first>
    - .github/dependabot.yml (current: github-actions block has no ignore; gomod block already ignores go-webview2).
    - .planning/phases/174-dependency-updates-dependabot-hygiene/174-RESEARCH.md — "dependabot.yml ignore syntax" (exact entries), Pitfall 2 (ignore ≠ Windows guard) and Pitfall 4 (correct ecosystem placement).
  </read_first>
  <action>
    Extend the EXISTING gomod `ignore:` list (the one already holding go-webview2) with two new entries. Use the surgical `versions` form so future security patches on each line still flow (per Phase 174 / DEP-02):

    wails/v2 (#104): add `dependency-name: "github.com/wailsapp/wails/v2"` with `versions: [">=2.11.0"]`. Inline comment: defer 2.12.0 — it transitively requires go-webview2 v1.0.22 but the build pins v1.0.19 (>=1.0.20 breaks Windows, Phase 145); the go-webview2 pin is the actual guard, this entry only stops Dependabot re-opening the PR; remove when a coordinated, tested webview2 upgrade lands.

    tailscale.com (#88): add `dependency-name: "tailscale.com"` with `versions: [">=1.100.0"]`. Inline comment: defer 1.100.0 — the Funnel feature was built + live-UAT'd on 1.98.3; revisit post-ship with a full off-tailnet re-UAT; allows 1.98.x/1.99.x patches.

    Place BOTH under the gomod ecosystem's ignore list (NOT github-actions). Do not remove or alter the existing go-webview2 ignore.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/dependabot.yml')); gomod=[u for u in d['updates'] if u['package-ecosystem']=='gomod'][0]; names={i['dependency-name'] for i in gomod['ignore']}; assert 'github.com/wailsapp/wails/v2' in names, 'wails ignore missing'; assert 'tailscale.com' in names, 'tailscale ignore missing'; assert 'github.com/wailsapp/go-webview2' in names, 'go-webview2 ignore lost'; w=[i for i in gomod['ignore'] if i['dependency-name']=='github.com/wailsapp/wails/v2'][0]; t=[i for i in gomod['ignore'] if i['dependency-name']=='tailscale.com'][0]; assert w.get('versions')==['>=2.11.0'], w; assert t.get('versions')==['>=1.100.0'], t; print('gomod-ignores-ok')"</automated>
  </verify>
  <acceptance_criteria>
    gomod ignore list contains go-webview2 (unchanged), wails/v2 (>=2.11.0), and tailscale.com (>=1.100.0), each with a rationale comment; file parses as YAML.
  </acceptance_criteria>
  <done>
    Both gomod deferrals present under the correct ecosystem with surgical version ranges + rationale.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add github-actions ignore block for actions/checkout v7 (#102)</name>
  <files>.github/dependabot.yml</files>
  <read_first>
    - .github/dependabot.yml (github-actions block currently has NO ignore key).
    - .planning/phases/174-dependency-updates-dependabot-hygiene/174-RESEARCH.md — checkout ignore syntax ("pin a major, allow patches" via update-types semver-major) + Pitfall 4.
  </read_first>
  <action>
    Add a new `ignore:` key to the github-actions ecosystem block (it has none today). Add one entry (per Phase 174 / DEP-02): `dependency-name: "actions/checkout"` with `update-types: ["version-update:semver-major"]`. Inline comment: defer the v7 major (fork-PR checkout + ESM/runtime change); evaluate in a throwaway branch; v6 patch/minor security updates still flow.

    Place this under github-actions (NOT gomod). Do not touch the gomod block edited in Task 1.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/dependabot.yml')); gha=[u for u in d['updates'] if u['package-ecosystem']=='github-actions'][0]; ig=gha.get('ignore',[]); c=[i for i in ig if i['dependency-name']=='actions/checkout']; assert c, 'checkout ignore missing under github-actions'; assert c[0].get('update-types')==['version-update:semver-major'], c[0]; print('gha-ignore-ok')"</automated>
  </verify>
  <acceptance_criteria>
    github-actions block has an ignore entry for actions/checkout limited to semver-major, with a rationale comment; file parses as YAML.
  </acceptance_criteria>
  <done>
    checkout v7 major deferred under the github-actions ecosystem; patch/minor still allowed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit the ignore entries and close Dependabot PRs #104/#88/#102</name>
  <files>.github/dependabot.yml</files>
  <read_first>
    - .planning/phases/174-dependency-updates-dependabot-hygiene/174-RESEARCH.md — "Merge / branch strategy", Open Question 2 (add ignore entries FIRST, then close), Live PR State (numbers).
  </read_first>
  <action>
    Confirm branch is v4.2-funnel-sharing. Commit the config: `ci(174): defer wails/tailscale/checkout via surgical dependabot ignore entries`.

    Then close each deferred PR citing this phase's ignore rationale (do NOT merge):
    - `gh pr close 104 --comment "Deferred in Phase 174 (DEP-02). wails 2.12.0 requires go-webview2 v1.0.22 but the build pins v1.0.19 (>=1.0.20 breaks Windows, Phase 145). dependabot.yml now ignores wails/v2 >=2.11.0; reopen when a coordinated webview2 upgrade is tested."`
    - `gh pr close 88 --comment "Deferred in Phase 174 (DEP-02). Funnel feature built + live-UAT'd on tailscale 1.98.3; 1.100.0 needs a full off-tailnet re-UAT post-ship. dependabot.yml now ignores tailscale.com >=1.100.0."`
    - `gh pr close 102 --comment "Deferred in Phase 174 (DEP-02). actions/checkout v7 is a major (fork-PR + ESM/runtime change) to evaluate in a branch. dependabot.yml now ignores checkout semver-major; v6 patches still flow."`

    Note in the SUMMARY: these ignores only take effect once v4.2 merges to main (Dependabot reads config from the default branch); a transient re-open before that merge is expected. If a `gh pr close` fails on auth, surface the error and stop.
  </action>
  <verify>
    <automated>test "$(git branch --show-current)" = "v4.2-funnel-sharing" && for n in 104 88 102; do test "$(gh pr view $n --json state -q .state)" = "CLOSED" || { echo "PR #$n not closed"; exit 1; }; done && echo "all-3-closed"</automated>
  </verify>
  <acceptance_criteria>
    dependabot.yml committed on v4.2-funnel-sharing; #104, #88, #102 all CLOSED (not merged), each with a rationale comment linking Phase 174.
  </acceptance_criteria>
  <done>
    Three high-risk PRs deferred and closed citing this phase; ignore entries committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependabot.yml ignore rules → future Dependabot behavior | An over-broad ignore silently freezes ALL future updates (including security patches) on a deferred dependency. |
| Deferred wails bump → Windows build integrity | Believing the ignore protects the Windows build (it does not) could let a manual `go get wails@2.12.0` slip through and break go-webview2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-04 | Denial of Service | Over-broad dependabot ignore freezing security patches | high | mitigate | Use surgical `versions` (wails >=2.11.0, tailscale >=1.100.0) and `update-types: [semver-major]` (checkout) — each blocks only the named risky bump; verify asserts the exact range/type so a whole-package block cannot slip in. |
| T-174-05 | Spoofing | False belief that the wails ignore guards the Windows build | medium | mitigate | Objective + key_links + PR-close comment state explicitly the ignore only stops re-opening; the real guard is the go-webview2 v1.0.19 pin + not running `go get wails@2.12.0` (RESEARCH.md Pitfall 2). |
| T-174-SC | Tampering | Config-only change, no packages installed | low | accept | No dependency is added or upgraded in this plan; it only edits dependabot config + closes PRs. No legitimacy checkpoint applies. |
</threat_model>

<verification>
- dependabot.yml parses as YAML.
- gomod ignore list = { go-webview2 (unchanged), wails/v2 >=2.11.0, tailscale.com >=1.100.0 }.
- github-actions ignore = { actions/checkout semver-major }.
- Each new entry has a rationale comment (visible in git diff).
- Ecosystem placement correct (wails+tailscale under gomod, checkout under github-actions).
- #104/#88/#102 CLOSED, none merged.
</verification>

<success_criteria>
Three high-risk upgrades deferred via surgical, security-patch-preserving ignore entries under the correct ecosystems, committed on v4.2-funnel-sharing, and the three Dependabot PRs closed citing Phase 174 rationale.
</success_criteria>

<output>
Create `.planning/phases/174-dependency-updates-dependabot-hygiene/174-03-SUMMARY.md` when done.
</output>
