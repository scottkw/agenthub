---
phase: 145-windows-files-test-fixes
plan: 03
type: execute
wave: 3
depends_on: ["145-01", "145-02"]
files_modified:
  - TESTING.md
autonomous: false

requirements: [FIX-02]

must_haves:
  truths:
    - "TESTING.md Suite Manifest (Section 2) reflects the two new build-tagged helper files in the Go group count"
    - "TESTING.md Traceability Map (Section 4) has a FIX-02 row for each new helper file with a repo-relative .go path (no test names in the path column)"
    - "bash tests/check-traceability-paths.sh exits 0 after the TESTING.md edits"
    - "A manual M-NN checklist item records that Windows runtime correctness is verified only by the green windows-latest CI matrix job"
    - "FIX-02 is TRULY verified only when build (agenthub, windows/amd64, windows-latest) is green after pushing the branch"
  artifacts:
    - path: "TESTING.md"
      provides: "Suite Manifest + traceability rows + manual checklist item for the two new test files and the Windows CI gate"
      contains: "concurrent_read_windows_test.go"
  key_links:
    - from: "TESTING.md Section 4 traceability rows"
      to: "internal/files/concurrent_read_windows_test.go and internal/files/concurrent_read_unix_test.go"
      via: "FIX-02 → repo-relative .go path mapping validated by check-traceability-paths.sh"
      pattern: "concurrent_read_(windows|unix)_test\\.go"
---

<objective>
Honor the repo's standing TESTING.md convention for the two NEW test files added in plan 145-02, then establish the Windows CI green job as the phase's true verification gate.

Per ./CLAUDE.md and TESTING.md Section 6, every phase that adds tests MUST update: the Suite Manifest (Section 2), the Traceability Map (Section 4), and — for behavior that cannot be verified locally — the Manual Regression Checklist (Section 5). The Windows runtime correctness of these fixes is exactly such a behavior: the developer is on macOS and cannot reproduce, so the only ground truth is the `build (agenthub, windows/amd64, windows-latest)` GitHub Actions matrix job.

Purpose: Keep the regression-suite manifest authoritative and make the phase's completion contingent on a green Windows CI job, not on local passes.
Output: Updated `TESTING.md`; a human checkpoint confirming the Windows CI matrix job is green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-windows-files-test-fixes/145-RESEARCH.md
@.planning/phases/145-windows-files-test-fixes/145-VALIDATION.md
@TESTING.md
@CLAUDE.md

<!-- depends_on 145-01 + 145-02: the new test files must exist before they are added to the
     manifest and before check-traceability-paths.sh (which verifies every mapped path exists on disk)
     can pass. -->

<interfaces>
<!-- Exact TESTING.md conventions to follow (from a live read): -->

Section 2 Suite Manifest: Go group currently lists "344 *_test.go files" and Total "460".
Adding 2 new test files → Go group becomes 346, Total becomes 462. Update both cells and keep the
2026-06-21 scan note intact (or append a one-line note that 145 added 2 Windows portability helpers).

Section 4 Traceability Map: format is `| Requirement | Test File | Suite Group | Notes |`.
The Test File (path) column MUST be a repo-relative path ending in `.go` — NO test names, NO
describe suffixes. Test names go in Notes. The FIX-01 row (line 146) is the analog precedent:
  | FIX-01 | internal/daemon/engine_test.go | Go | Daemon styled-tail race fix (#100): ... |

Section 5 Manual Regression Checklist uses `M-NN` items grouped by category. Add an item recording
that the three target tests' Windows runtime pass is confirmed only by the green windows-latest job.

Validator: `bash tests/check-traceability-paths.sh` must exit 0 (it asserts every Section 4 path
exists on disk and ends in an allowed extension).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TESTING.md Suite Manifest, Traceability Map, and Manual Checklist</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md (Section 2 Suite Manifest around line 22-34; Section 4 Traceability Map header line 100 + FIX-01 analog row line 146; Section 5 Manual Regression Checklist line 150+; Section 6 Standing Convention line 210+)
    - ./CLAUDE.md (Regression Test Convention standing rule)
    - tests/check-traceability-paths.sh (understand exactly what it validates — path existence + allowed extension, no test names)
  </read_first>
  <action>
    Make three edits to TESTING.md:
    1. Section 2 Suite Manifest: bump the Go unit/integration count from 344 to 346 and the Total from 460 to 462 to account for `internal/files/concurrent_read_windows_test.go` and `internal/files/concurrent_read_unix_test.go`. Do not alter the other group counts.
    2. Section 4 Traceability Map: add two rows mapping FIX-02, following the FIX-01 analog row format exactly. Row A: `| FIX-02 | internal/files/concurrent_read_windows_test.go | Go | Windows portability (#101): readFilePlatformSafe opens with FILE_SHARE_DELETE so WriteFileAtomic POSIX-rename succeeds under concurrent read |`. Row B: `| FIX-02 | internal/files/concurrent_read_unix_test.go | Go | Non-Windows companion: readFilePlatformSafe delegates to os.ReadFile |`. The path column is the repo-relative `.go` file path ONLY — no test names there (test names belong in Notes). Optionally also note in a row's Notes that handler_test.go / write_test.go home-rooting fixes are covered, but do NOT add a path row whose path column contains a test name.
    3. Section 5 Manual Regression Checklist: add one `M-NN` item (next sequential number in the appropriate category, or a new short category for CI-gated Go portability) stating: "FIX-02 (#101): TestHandlerUpload_FilenameSanitized, TestDenylist_NonHomeRootedUnaffected, and TestWriteFileAtomic_ConcurrentReadNeverPartial pass on Windows — verify the `build (agenthub, windows/amd64, windows-latest)` job is green in GitHub Actions after pushing (no local Windows env)."
    Run the traceability validator after editing. Do not edit any code in this task.
  </action>
  <verify>
    <automated>bash tests/check-traceability-paths.sh && grep -c 'concurrent_read_windows_test.go' TESTING.md && grep -c 'concurrent_read_unix_test.go' TESTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/check-traceability-paths.sh` exits 0
    - `grep -c 'concurrent_read_windows_test.go' TESTING.md` returns >= 1 and `grep -c 'concurrent_read_unix_test.go' TESTING.md` returns >= 1
    - Both new Section 4 path-column entries are repo-relative paths ending in `.go` with no test-name text in the path column (validator enforces this; manual spot-check the two new rows)
    - Section 2 Go group count reads 346 and Total reads 462
    - A new `M-` item in Section 5 references the `build (agenthub, windows/amd64, windows-latest)` CI job as the Windows verification source
  </acceptance_criteria>
  <done>TESTING.md Suite Manifest counts, two FIX-02 traceability rows, and a Windows-CI manual checklist item are added; check-traceability-paths.sh exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the Windows CI matrix job is green</name>
  <files>(none — CI observation checkpoint)</files>
  <what-built>
    Plans 145-01 and 145-02 fixed the three internal/files tests for Windows path/share semantics (home-rooting redirect via setHomeEnv; FILE_SHARE_DELETE reader via build-tagged readFilePlatformSafe). All local pre-CI checks pass on macOS: full `internal/files` suite green with -race, and the package cross-compiles + vets clean for windows/amd64. TESTING.md is updated per the standing convention. Local checks give compile confidence ONLY — they cannot prove Windows runtime correctness. The Windows runner is the only ground truth.
  </what-built>
  <action>
    This is a human-verify checkpoint, not an automated task. Do not write code. Surface the verification steps in <how-to-verify> to the operator and pause for the resume-signal. The phase MUST NOT be marked verified while the Windows matrix job is red — a failing Windows job triggers a follow-up fix in plans 145-01 / 145-02.
  </action>
  <how-to-verify>
    1. Push the phase branch to GitHub (the developer/operator pushes; do not push without confirmation if on a protected branch).
    2. Open the GitHub Actions run for the pushed commit (Actions tab on scottkw/agenthub, or `gh run watch`).
    3. Confirm the matrix job `build (agenthub, windows/amd64, windows-latest)` completes GREEN (not skipped, not failed).
    4. Specifically confirm the `internal/files` Go tests pass within that Windows job — the three previously-failing tests (TestHandlerUpload_FilenameSanitized, TestDenylist_NonHomeRootedUnaffected, TestWriteFileAtomic_ConcurrentReadNeverPartial) must now pass on Windows.
    5. Confirm the Linux + macOS matrix jobs are also green (no regression introduced).
    Expected outcome: all build matrix jobs green; FIX-02 (#101) closed. If the Windows job is still red, capture the failing test output and return to plan 145-01 / 145-02 for a follow-up fix (do NOT mark the phase verified on a red Windows job).
  </how-to-verify>
  <verify>
    <human-check>`build (agenthub, windows/amd64, windows-latest)` matrix job is green in GitHub Actions for the pushed commit, and Linux + macOS matrix jobs remain green.</human-check>
  </verify>
  <resume-signal>Type "approved" once the Windows CI matrix job is confirmed green (and Linux/macOS jobs unbroken), or paste the failing Windows job output to trigger a follow-up fix.</resume-signal>
  <done>The Windows matrix job is confirmed green by the operator with Linux/macOS unbroken; FIX-02 (#101) is verified on the only Windows ground truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local macOS verification ↔ Windows runtime correctness | Local cross-compile/vet cannot observe Windows file-share semantics; the CI Windows runner is the only environment that exercises the real failure, so the manifest/checklist must route final verification there |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-04 | Repudiation | Phase verification provenance | mitigate | TESTING.md Section 4 traceability rows + a Section 5 manual checklist item make the Windows CI green job the auditable verification source for FIX-02, preventing a false "verified locally" close. check-traceability-paths.sh enforces the path mapping. |
| T-145-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; documentation-only edits to TESTING.md plus a CI observation checkpoint. |
</threat_model>

<verification>
- `bash tests/check-traceability-paths.sh` exits 0
- TESTING.md references both new test files and the Windows CI job
- `build (agenthub, windows/amd64, windows-latest)` matrix job GREEN after push (human-confirmed) — the true FIX-02 gate
- Linux + macOS matrix jobs remain green (no regression)
</verification>

<success_criteria>
- TESTING.md standing convention honored (Suite Manifest + traceability + manual checklist)
- check-traceability-paths.sh exits 0
- Windows CI matrix job confirmed green by human checkpoint — FIX-02 (#101) closed
</success_criteria>

<output>
Create `.planning/phases/145-windows-files-test-fixes/145-03-SUMMARY.md` when done.
</output>
