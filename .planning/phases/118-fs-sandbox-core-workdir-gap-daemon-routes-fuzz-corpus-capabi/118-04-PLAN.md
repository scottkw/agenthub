---
phase: 118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/daemon/engine.go
  - internal/daemon/plugin_settings.go
  - internal/daemon/engine_migration_test.go
  - tests/fixtures/settings_v3.2.json
autonomous: true
requirements: [FS-02, FS-14]
must_haves:
  truths:
    - "SessionEngine has a new sessionWorkDirs map[string]string field initialized in NewSessionEngine alongside tabNames and sessionCLIs"
    - "CreateSession populates sessionWorkDirs[id] with the filepath.EvalSymlinks-resolved absolute WorkDir AFTER $HOME substitution and AFTER backend.Create succeeds; the raw workDir is the fallback if EvalSymlinks errors so session creation never fails because of resolution"
    - "KillSession deletes sessionWorkDirs[id] alongside tabNames[id] and sessionCLIs[id] cleanups"
    - "Exported method GetSessionWorkDir(id string) string returns the resolved WorkDir or empty string for unknown session id (read-locked)"
    - "daemonSettings gains a FilesRead *bool field (pointer for tri-state: nil = legacy file pre-v3.4, *true = enabled, *false = explicitly disabled)"
    - "CurrentSchemaVersion is bumped from 2 to 3"
    - "loadSettingsFromDisk pre-populates FilesRead with a *true default BEFORE Unmarshal so v3.2 settings files (no filesRead key) load with FilesRead = *true (Pitfall 16 mitigation via the same defaults-merge pattern proven in v3.2)"
    - "TestSettingsMigration_FilesReadDefaultsTrue loads tests/fixtures/settings_v3.2.json (no filesRead key) and asserts the loaded engine reports FilesRead == true"
  artifacts:
    - path: "internal/daemon/engine.go"
      provides: "sessionWorkDirs map + GetSessionWorkDir + FilesRead default-merge in loadSettingsFromDisk"
      contains: "sessionWorkDirs"
      contains_2: "func (e *SessionEngine) GetSessionWorkDir"
    - path: "internal/daemon/plugin_settings.go"
      provides: "CurrentSchemaVersion bumped from 2 to 3"
      contains: "CurrentSchemaVersion = 3"
    - path: "tests/fixtures/settings_v3.2.json"
      provides: "pre-v3.4 fixture (schemaVersion: 2, no filesRead key) for migration test"
      contains: "\"schemaVersion\": 2"
    - path: "internal/daemon/engine_migration_test.go"
      provides: "TestSettingsMigration_FilesReadDefaultsTrue + new fixturePath helper for v3.2"
      contains: "TestSettingsMigration_FilesReadDefaultsTrue"
  key_links:
    - from: "internal/daemon/engine.go (CreateSession)"
      to: "filepath.EvalSymlinks(workDir)"
      via: "one-time resolution AFTER $HOME substitution; result cached in sessionWorkDirs[id]"
      pattern: "filepath\\.EvalSymlinks"
    - from: "internal/daemon/engine.go (loadSettingsFromDisk)"
      to: "FilesRead defaults-merge"
      via: "pre-populate s := daemonSettings{ FilesRead: ptrTrue(), Plugins: defaultPluginSettings() } BEFORE json.Unmarshal"
      pattern: "FilesRead"
---

<objective>
Patch the WorkDir gap and ship the v3.2→v3.3 settings migration that adds the daemon-wide `filesRead` default flag. Three additive changes to `internal/daemon`:

1. `SessionEngine.sessionWorkDirs map[string]string` populated at `CreateSession` via `filepath.EvalSymlinks` and cleared at `KillSession`, with `GetSessionWorkDir(id) string` exposed for Plan 05 to wire into the file handler's `sandboxResolver`. (FS-02 — fills the gap where the legacy `cmd.Dir` value was discarded after spawn.)

2. `daemonSettings.FilesRead *bool` field with the proven defaults-merge constructor pattern from v3.2 (pre-populate `*true` BEFORE Unmarshal). `CurrentSchemaVersion` bumps 2→3. (FS-14 — owner default-on, viewer default-off is controlled at token-issuance in Plan 05, not at settings time.)

3. A new `tests/fixtures/settings_v3.2.json` fixture without a `filesRead` key and a `TestSettingsMigration_FilesReadDefaultsTrue` test asserting the load path defaults to `true`. (Pitfall 16 / RESEARCH.md Pitfall 6 mitigation — Go's `encoding/json` leaves missing JSON keys at zero value; without defaults-merge a v3.3 user's upgrade would silently land FilesRead = false.)

Purpose: Plan 05 builds the file-handler `sandboxResolver` closure on top of `engine.GetSessionWorkDir`. Plan 05's token-issuance edit reads the `FilesRead` setting to decide whether to include `files.read` in owner Perms. This plan delivers BOTH preconditions without touching any HTTP route.

Output: Edited `engine.go`, `plugin_settings.go`, `engine_migration_test.go`; new `tests/fixtures/settings_v3.2.json`. Zero HTTP/handler surface area.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-RESEARCH.md
@.planning/research/PITFALLS.md
@internal/daemon/engine.go
@internal/daemon/plugin_settings.go
@internal/daemon/engine_migration_test.go
@tests/fixtures/settings_v3.1.json

<interfaces>
Existing SessionEngine fields (engine.go lines 25-60 verified):
- `tabNames    map[string]string`  — sessionID → display name
- `sessionCLIs map[string]string`  — sessionID → raw CLI name
- `cliPaths` map
- `startMinimized`, `shellWebShareWarned`, `shellPath`, `autoCloseSession`, `pluginSettings`
- `mu sync.RWMutex`

Existing NewSessionEngine pattern (lines 219-224):
- `e := &SessionEngine{ tabNames: make(map[string]string), sessionCLIs: make(map[string]string), ... }`
- `e.loadSettingsFromDisk(cfgDir)`

Existing CreateSession structure (lines 232-303 per RESEARCH.md):
- $HOME substitution for shell sessions lives lines ~250-266
- backend.Create returns sess line ~285
- e.mu.Lock() + maps populated lines ~300-303
- Pattern: `e.tabNames[id] = name; e.sessionCLIs[id] = cli`

Existing KillSession cleanup pattern (line ~433 per RESEARCH.md):
- `delete(e.tabNames, id); delete(e.sessionCLIs, id)`

Existing daemonSettings (engine.go lines 85-93):
```
type daemonSettings struct {
    CLIPaths            map[string]string `json:"cliPaths,omitempty"`
    StartMinimized      bool              `json:"startMinimized,omitempty"`
    ShellWebShareWarned bool              `json:"shellWebShareWarned,omitempty"`
    ShellPath           string            `json:"shellPath,omitempty"`
    AutoCloseSession    *bool             `json:"autoCloseSession,omitempty"`
    Plugins             PluginSettings    `json:"plugins"`
    SchemaVersion       int               `json:"schemaVersion"`
}
```

Existing CurrentSchemaVersion (plugin_settings.go lines 5-7):
- `const CurrentSchemaVersion = 2`

Existing defaults-merge pattern (engine.go lines 147-149 — the proven Pitfall #14 mitigation):
- `s := daemonSettings{ Plugins: defaultPluginSettings() }` BEFORE `json.Unmarshal(data, &s)`

Existing migration test (engine_migration_test.go line 16):
- `filepath.Join("..", "..", "tests", "fixtures", "settings_v3.1.json")` for the v3.1 fixture pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sessionWorkDirs map + EvalSymlinks-resolved population in CreateSession + GetSessionWorkDir method + KillSession cleanup</name>
  <files>internal/daemon/engine.go, internal/daemon/engine_test.go</files>
  <read_first>
    - /Users/ken/dev/agenthub/internal/daemon/engine.go (full file — focus on lines 25-60 SessionEngine struct, lines 215-225 NewSessionEngine, lines 232-303 CreateSession including the $HOME substitution block, lines 425-440 KillSession cleanup block)
    - /Users/ken/dev/agenthub/.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-RESEARCH.md (Pattern 5 "sessionWorkDirs parallel map" — full code example lines 421-477)
    - /Users/ken/dev/agenthub/.planning/research/ARCHITECTURE.md (Section 1.5 — WorkDir is NOT stored after session creation)
  </read_first>
  <behavior>
    - SessionEngine struct contains a new `sessionWorkDirs map[string]string` field declared adjacent to tabNames and sessionCLIs (per RESEARCH.md the field belongs in the existing mutex-protected block).
    - NewSessionEngine initializes the new map: `sessionWorkDirs: make(map[string]string)` adjacent to the existing tabNames/sessionCLIs make() calls.
    - When CreateSession is called with workDir == "", sessionWorkDirs[id] is set to "" (no resolution attempted).
    - When CreateSession is called with workDir == "/tmp/symlink-to-real" where symlink-to-real is a symlink to /tmp/real, sessionWorkDirs[id] equals "/tmp/real" (the EvalSymlinks-resolved absolute path).
    - When CreateSession is called with workDir == "/nonexistent/xyz" (EvalSymlinks fails), sessionWorkDirs[id] is set to the raw workDir as fallback so session creation still succeeds — the file browser will simply return 400 when looked up later.
    - Resolution happens AFTER the existing $HOME substitution block for shell sessions and BEFORE the e.mu.Lock() block that populates tabNames/sessionCLIs.
    - GetSessionWorkDir(id) is a public method on *SessionEngine, takes a session ID string, returns the cached workDir or "" if id is unknown. It acquires e.mu.RLock() and releases with deferred RUnlock.
    - KillSession deletes the entry: `delete(e.sessionWorkDirs, id)` — placed in the same lock-held block as the existing `delete(e.tabNames, id)` and `delete(e.sessionCLIs, id)` calls.
    - GetSessionWorkDir on a recently-created session returns the resolved path; after KillSession returns the empty string.
  </behavior>
  <action>
    Edit `internal/daemon/engine.go`:

    1. **Struct field**: Add `sessionWorkDirs map[string]string  // sessionID -> EvalSymlinks-resolved absolute WorkDir (Phase 118 / FS-02)` immediately after the `sessionCLIs map[string]string` field declaration in the SessionEngine struct (around line 35). Field-grouping comment block remains intact.

    2. **Constructor**: In NewSessionEngine (around line 219), add `sessionWorkDirs: make(map[string]string),` to the literal — placed adjacent to the existing `sessionCLIs: make(map[string]string),` line.

    3. **CreateSession population**: After the existing `$HOME` substitution block for shell sessions and after `backend.Create` returns sess (per RESEARCH.md Pattern 5), and BEFORE the existing `e.mu.Lock()` block that sets `e.tabNames[id] = name`, add a resolution block (no lock held during EvalSymlinks — it does I/O):
       ```
       resolvedWD := ""
       if workDir != "" {
           if r, err := filepath.EvalSymlinks(workDir); err == nil {
               resolvedWD = r
           } else {
               // Resolution failed (path may not exist on disk yet, or symlink chain
               // is broken). Fall back to raw workDir so session creation still
               // succeeds; file browser will surface a clear error later if the
               // user attempts to list this session's cwd.
               resolvedWD = workDir
           }
       }
       ```
       Then INSIDE the existing e.mu.Lock() block at lines ~300-303 (next to `e.tabNames[id] = name; e.sessionCLIs[id] = cli`), add: `e.sessionWorkDirs[id] = resolvedWD`.

    4. **KillSession cleanup**: In the existing delete block in KillSession (around line 433), add `delete(e.sessionWorkDirs, id)` adjacent to `delete(e.tabNames, id)` and `delete(e.sessionCLIs, id)`. If KillSession also handles a stale-session cleanup branch (line ~419 — `e.tabNames[id] = name` reassignment), ensure the new map is handled consistently there too (or document why it differs).

    5. **GetSessionWorkDir method**: Add a new exported method (placement: near the existing settings/state read methods at the bottom of engine.go):
       ```
       // GetSessionWorkDir returns the EvalSymlinks-resolved absolute WorkDir for
       // a session, or empty string if the session is unknown. Used by the file
       // browser handler in Phase 118 to construct a per-session Sandbox.
       func (e *SessionEngine) GetSessionWorkDir(id string) string {
           e.mu.RLock()
           defer e.mu.RUnlock()
           return e.sessionWorkDirs[id]
       }
       ```
       The `filepath` import is already present in engine.go (verified — the file uses it for the existing `filepath.Base/Ext/Join` calls in the knownShells path).

    6. Edit `internal/daemon/engine_test.go`: append at least three test functions:
       - `TestEngine_SessionWorkDirsPopulated`: Creates a session with workDir = a t.TempDir() resolved path; assert `engine.GetSessionWorkDir(id) == tempDir` (after EvalSymlinks normalization).
       - `TestEngine_SessionWorkDirsResolvesSymlink`: Create a real dir + a symlink pointing to it; CreateSession with workDir = symlinkPath; assert `engine.GetSessionWorkDir(id)` is the resolved (real, not symlink) absolute path.
       - `TestEngine_SessionWorkDirsClearedOnKill`: Create + kill a session; assert `engine.GetSessionWorkDir(id) == ""` after kill.
       - `TestEngine_SessionWorkDirsEmptyForUnknown`: assert `engine.GetSessionWorkDir("does-not-exist") == ""`.
       - `TestEngine_SessionWorkDirsFallbackOnEvalSymlinksError`: workDir = "/this/path/definitely/does/not/exist/xyz123"; CreateSession succeeds; GetSessionWorkDir returns that raw string (not "").

       Use the existing engine_test.go scaffolding for SessionEngine construction (look for the `newTestEngine` or equivalent helper; otherwise inline the NewSessionEngine + minimal backend mock setup that existing engine tests use).

    Do NOT add a `WorkDir` field to `pty.Session` (ARCHITECTURE.md Anti-Pattern 2). The map is the only authoritative store.
    Do NOT call `filepath.EvalSymlinks` per-request from any future handler — Plan 02's Sandbox already caches the resolved path via NewSandbox. This plan's resolution is the one-and-only-once at session creation per RESEARCH.md Pitfall 1 / Pattern 5.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -run '^TestEngine_SessionWorkDirs' ./internal/daemon/ -count=1 -v && go vet ./internal/daemon/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sessionWorkDirs *map\[string\]string' internal/daemon/engine.go` returns at least 1 (struct field declaration).
    - `grep -c 'sessionWorkDirs:.*make' internal/daemon/engine.go` returns at least 1 (constructor initialization).
    - `grep -c 'e\.sessionWorkDirs\[id\] = resolvedWD' internal/daemon/engine.go` returns at least 1 (CreateSession population).
    - `grep -c 'delete(e.sessionWorkDirs, id)' internal/daemon/engine.go` returns at least 1 (KillSession cleanup).
    - `grep -c '^func (e \*SessionEngine) GetSessionWorkDir' internal/daemon/engine.go` returns exactly 1.
    - `awk '/^func \(e \*SessionEngine\) GetSessionWorkDir/,/^}/{ print }' internal/daemon/engine.go | grep -c 'e.mu.RLock()'` returns at least 1 (read-locked).
    - `grep -c 'filepath.EvalSymlinks(workDir)' internal/daemon/engine.go` returns at least 1 (one-time resolution in CreateSession).
    - `go test -run '^TestEngine_SessionWorkDirs' ./internal/daemon/ -count=1 -v` exits 0 with all 5 subtests passing.
    - Full daemon package test suite passes with no new failures: `go test ./internal/daemon/ -count=1 -short` exits 0 (excluding any pre-existing skipped tests documented in STATE.md).
  </acceptance_criteria>
  <done>
    sessionWorkDirs map fully wired: declared, initialized, populated via EvalSymlinks resolution at session creation, cleaned up at session kill, and exposed via GetSessionWorkDir. The WorkDir gap (FS-02) is closed; Plan 05 can now build the sandboxResolver closure.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add FilesRead field to daemonSettings, bump CurrentSchemaVersion 2→3, fix defaults-merge in loadSettingsFromDisk, create v3.2 fixture + migration test</name>
  <files>internal/daemon/engine.go, internal/daemon/plugin_settings.go, tests/fixtures/settings_v3.2.json, internal/daemon/engine_migration_test.go</files>
  <read_first>
    - /Users/ken/dev/agenthub/internal/daemon/engine.go (lines 79-93 daemonSettings struct, lines 126-186 loadSettingsFromDisk full body, lines 188-205 saveSettingsToDisk full body)
    - /Users/ken/dev/agenthub/internal/daemon/plugin_settings.go (line 5-7 — CurrentSchemaVersion const)
    - /Users/ken/dev/agenthub/internal/daemon/engine_migration_test.go (existing v3.1→v3.2 migration test pattern — entire file)
    - /Users/ken/dev/agenthub/tests/fixtures/settings_v3.1.json (existing fixture pattern to model the new v3.2 fixture on)
    - /Users/ken/dev/agenthub/.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-RESEARCH.md (Pitfall 6 "Settings migration default-false trap")
    - /Users/ken/dev/agenthub/.planning/research/PITFALLS.md (Pitfall 16 "schemaVersion: 3 + filesRead default-true")
  </read_first>
  <behavior>
    - plugin_settings.go declares `const CurrentSchemaVersion = 3` (was 2).
    - daemonSettings has a new `FilesRead *bool` field with json tag `"filesRead,omitempty"`, placed between AutoCloseSession and Plugins so existing field ordering for Plugins + SchemaVersion is preserved at the end of the struct.
    - SessionEngine struct gains a new `filesRead *bool` lower-case mirror field next to autoCloseSession (the engine reads it into in-memory state the way other settings are mirrored).
    - loadSettingsFromDisk pre-populates `s := daemonSettings{ FilesRead: ptrBool(true), Plugins: defaultPluginSettings() }` BEFORE json.Unmarshal — this is the Pitfall 16 mitigation. A new helper `func ptrBool(v bool) *bool { return &v }` is added if not already present in the package; otherwise inline `var t = true; s.FilesRead = &t` style.
    - After Unmarshal, the loader copies `s.FilesRead` into `e.filesRead` adjacent to the existing `e.autoCloseSession = s.AutoCloseSession` line.
    - saveSettingsToDisk includes `FilesRead: e.filesRead` in the marshalled struct.
    - The needsUpgradeWrite logic (line 177 in engine.go) compares `s.SchemaVersion < CurrentSchemaVersion`; this comparison now triggers v3.2→v3.3 upgrade-rewrite because CurrentSchemaVersion is 3, while a v3.2 file has SchemaVersion=2. The existing logic continues to work without modification.
    - A new file `tests/fixtures/settings_v3.2.json` exists with schemaVersion: 2, a plugins block (matching the existing v3.2 settings shape), and NO filesRead key.
    - TestSettingsMigration_FilesReadDefaultsTrue loads tests/fixtures/settings_v3.2.json via the same fixturePath helper pattern used for v3.1 (relative path `filepath.Join("..", "..", "tests", "fixtures", "settings_v3.2.json")`); creates a SessionEngine that loads from a tempdir containing the copied fixture; asserts that the in-memory engine state reports `e.filesRead != nil && *e.filesRead == true`.
    - TestSettingsMigration_FilesReadExplicitFalse asserts that a fixture with `"filesRead": false` loads as `*e.filesRead == false` (defaults-merge does NOT clobber explicit user values).
    - TestSettingsMigration_FilesReadSchemaVersionRewrite asserts that after loading a schemaVersion-2 fixture, the engine re-saves settings.json with schemaVersion=3 (existing pattern from v3.1→v3.2 migration test).
  </behavior>
  <action>
    1. Edit `internal/daemon/plugin_settings.go`: change `const CurrentSchemaVersion = 2` to `const CurrentSchemaVersion = 3`. Update the doc-comment if it references "v3.2" to also include "v3.4 adds FilesRead".

    2. Edit `internal/daemon/engine.go`:
       - Add to the SessionEngine struct (near autoCloseSession): `filesRead          *bool   // v3.4 (Phase 118): nil for pre-v3.4 files (defaulted to true via loadSettingsFromDisk); *true = enabled; *false = explicitly disabled`.
       - Add to the daemonSettings struct between AutoCloseSession and Plugins:
         ```
         FilesRead           *bool             `json:"filesRead,omitempty"`
         ```
       - Update the doc-comment above daemonSettings to mention that FilesRead is the schemaVersion-3 addition (the existing block already discusses Plugins + SchemaVersion field-order being load-bearing — add a sentence about FilesRead being additive).
       - In loadSettingsFromDisk, change the defaults-merge literal (lines ~147-149) from:
         ```
         s := daemonSettings{
             Plugins: defaultPluginSettings(),
         }
         ```
         to:
         ```
         tr := true
         s := daemonSettings{
             FilesRead: &tr,                  // Phase 118 / FS-14: default ON for v3.2 files lacking the key (Pitfall 16 mitigation)
             Plugins:   defaultPluginSettings(),
         }
         ```
       - Immediately after `e.autoCloseSession = s.AutoCloseSession` (around line 170), add `e.filesRead = s.FilesRead`.
       - In saveSettingsToDisk, add to the marshalled literal: `FilesRead: e.filesRead,` between AutoCloseSession and Plugins.

    3. Create `tests/fixtures/settings_v3.2.json` with content modelled on the existing `tests/fixtures/settings_v3.1.json` but representing a v3.2 file (so it includes the v3.2 plugins block, but does NOT include filesRead). Concrete required keys:
       - "schemaVersion": 2
       - A "plugins" object matching the shape produced by `defaultPluginSettings()` in v3.2 (consult internal/daemon/plugin_settings.go for the keys — search, webLinks, image, etc.).
       - Any other keys present in the existing settings_v3.1.json that are still relevant in v3.2 (cliPaths can be present with an empty map; startMinimized, shellWebShareWarned, shellPath, autoCloseSession all optional).
       - MUST NOT include a "filesRead" key.
       Refer to `tests/fixtures/settings_v3.1.json` for the file structure (this fixture is committed and stable).

    4. Edit `internal/daemon/engine_migration_test.go`:
       - Add a new helper `func fixturePathV32() string` that returns `filepath.Join("..", "..", "tests", "fixtures", "settings_v3.2.json")` paralleling the existing v3.1 helper at line 16.
       - Append three test functions:
         - `TestSettingsMigration_FilesReadDefaultsTrue`: copy the v3.2 fixture into a t.TempDir() at the expected settings.json filename; construct a new SessionEngine that loads from that tempdir; assert `e.filesRead != nil && *e.filesRead == true`. Use the same setup scaffold as the existing v3.1 migration tests (NewSessionEngine constructor with the cfgDir argument override pattern — look at how existing tests construct an engine with a custom cfgDir).
         - `TestSettingsMigration_FilesReadExplicitFalse`: write a synthetic settings.json file directly (no fixture needed) containing `{"schemaVersion": 2, "filesRead": false, "plugins": {...minimal...}}`; load; assert `e.filesRead != nil && *e.filesRead == false`. This guards against the defaults-merge silently overriding explicit user choice.
         - `TestSettingsMigration_FilesReadSchemaVersionRewrite`: after loading the v3.2 fixture, read settings.json back from the tempdir and assert the on-disk schemaVersion is now 3 — this verifies the existing `needsUpgradeWrite` upgrade-rewrite path correctly re-saves the file at the new schema version (mirrors the existing v3.1→v3.2 migration test pattern).

    Do NOT add a `FilesRead` field to PluginSettings — it is a daemon-wide setting, not a plugin setting (RESEARCH.md Assumption A6).
    Do NOT bypass the defaults-merge pattern by adding migration logic that reads the raw JSON and mutates — the pre-Unmarshal literal IS the established pattern.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -run '^TestSettingsMigration_FilesRead' ./internal/daemon/ -count=1 -v && go vet ./internal/daemon/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^const CurrentSchemaVersion = 3' internal/daemon/plugin_settings.go` returns 1.
    - `grep -c '^const CurrentSchemaVersion = 2' internal/daemon/plugin_settings.go` returns 0.
    - `grep -c 'FilesRead.*\*bool' internal/daemon/engine.go` returns at least 2 (daemonSettings field + SessionEngine mirror field).
    - `grep -c '`json:"filesRead,omitempty"`' internal/daemon/engine.go` returns at least 1.
    - `awk '/^func \(e \*SessionEngine\) loadSettingsFromDisk/,/^}/{ print }' internal/daemon/engine.go | grep -c 'FilesRead: &tr'` returns at least 1 (defaults-merge sets *true BEFORE Unmarshal).
    - `awk '/^func \(e \*SessionEngine\) saveSettingsToDisk/,/^}/{ print }' internal/daemon/engine.go | grep -c 'FilesRead:'` returns at least 1 (save path serializes the field).
    - `ls tests/fixtures/settings_v3.2.json` exists.
    - `python3 -c "import json; d=json.load(open('tests/fixtures/settings_v3.2.json')); assert d['schemaVersion']==2; assert 'filesRead' not in d, 'fixture must not contain filesRead key'; print('ok')"` prints "ok".
    - `grep -c 'TestSettingsMigration_FilesReadDefaultsTrue' internal/daemon/engine_migration_test.go` returns at least 1.
    - `grep -c 'TestSettingsMigration_FilesReadExplicitFalse' internal/daemon/engine_migration_test.go` returns at least 1.
    - `grep -c 'TestSettingsMigration_FilesReadSchemaVersionRewrite' internal/daemon/engine_migration_test.go` returns at least 1.
    - `go test -run '^TestSettingsMigration_FilesRead' ./internal/daemon/ -count=1 -v` exits 0 with all three subtests passing.
    - Existing migration tests still pass: `go test -run '^TestSettingsMigration' ./internal/daemon/ -count=1` exits 0.
    - `go vet ./internal/daemon/` reports no diagnostics.
  </acceptance_criteria>
  <done>
    Schema bumped 2→3; FilesRead added with proven defaults-merge pattern; v3.2 fixture + three migration tests committed; v3.2 users upgrading to v3.4 will load with `filesRead = true` (Pitfall 16 closed). Plan 05's token-issuance edit can read `e.filesRead` to decide whether to include `files.read` in owner Perms.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → engine (settings.json) | Untrusted-on-disk file (user could hand-edit); JSON parser is the boundary; missing keys must not silently weaken posture |
| user-supplied workDir → SessionEngine | User chooses workDir at CreateSession; EvalSymlinks is informational (one-time normalization for sandbox root caching) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-17 | Elevation of Privilege | loadSettingsFromDisk + missing filesRead key | mitigate | Defaults-merge constructor pattern (v3.2-proven) pre-populates FilesRead to `*true` BEFORE Unmarshal. Mitigates Pitfall 16 (silent default-false would mean upgrading users lose access to their own sessions). TestSettingsMigration_FilesReadDefaultsTrue verifies. |
| T-118-18 | Tampering | hand-edited settings.json with filesRead=false | accept | An owner who edits their own settings.json to disable filesRead is exercising explicit user choice; TestSettingsMigration_FilesReadExplicitFalse confirms the defaults-merge does NOT clobber this. |
| T-118-19 | Information Disclosure | sessionWorkDirs containing absolute paths in memory | accept | Daemon process is a single-user-trust-domain; sessionWorkDirs never crosses process boundary except through GetSessionWorkDir, which Plan 05 wires only to the in-process file handler. No JSON serialization of the map. |
| T-118-20 | Denial of Service | filepath.EvalSymlinks on bad workDir at CreateSession | mitigate | Fallback to raw workDir on EvalSymlinks error — session creation never fails because of resolution. The file browser surfaces a 400 if/when the user attempts to list a non-existent cwd. Documented in code comment per the action spec. |
</threat_model>

<verification>
- `go build ./internal/daemon/` exits 0.
- `go vet ./internal/daemon/` reports no diagnostics.
- `go test -run '^TestEngine_SessionWorkDirs' ./internal/daemon/ -count=1` exits 0.
- `go test -run '^TestSettingsMigration_FilesRead' ./internal/daemon/ -count=1` exits 0.
- `go test ./internal/daemon/ -count=1 -short` exits 0 (full daemon-package suite passes excluding pre-existing skips documented in STATE.md / deferred-items.md).
- The existing v3.1→v3.2 migration test (TestSettingsMigration*) still passes with CurrentSchemaVersion now at 3 — the existing tests do NOT regress because the upgrade-write path is additive over schema deltas.
</verification>

<success_criteria>
- `SessionEngine.sessionWorkDirs map[string]string` exists, is populated at `CreateSession` via `filepath.EvalSymlinks(workDir)` (with raw-workDir fallback on resolution error), and is cleared at `KillSession` — FS-02 satisfied.
- `(*SessionEngine).GetSessionWorkDir(id string) string` exposed with RLock; Plan 05's sandboxResolver closure consumes this method.
- `daemonSettings.FilesRead *bool` field added with `json:"filesRead,omitempty"` tag.
- `CurrentSchemaVersion = 3` (bumped from 2).
- `loadSettingsFromDisk` pre-populates `FilesRead: &true` BEFORE `json.Unmarshal` so v3.2 settings.json files load with FilesRead=true (FS-14 / Pitfall 16 mitigation).
- `tests/fixtures/settings_v3.2.json` exists, contains `"schemaVersion": 2`, lacks any `"filesRead"` key.
- TestSettingsMigration_FilesReadDefaultsTrue, TestSettingsMigration_FilesReadExplicitFalse, TestSettingsMigration_FilesReadSchemaVersionRewrite all pass.
- Zero regressions to existing daemon/plugin-settings/migration tests.
- Pre-existing test failures listed in STATE.md (e.g., TestOpenCodeANSICapture data race, ShellWebShareWarned family) remain skipped — this plan does not attempt to fix them and they are not new failures.
</success_criteria>

<output>
Create `.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-04-SUMMARY.md` when done. Plan 05 (daemon routes + DaemonClient + token issuance) will (a) wire the file handler's sandboxResolver via `engine.GetSessionWorkDir` + `files.NewSandbox`; (b) edit `issueCapabilitiesForSession` to include `files.read` in the write-token Perms when `e.filesRead == nil || *e.filesRead == true`.
</output>
