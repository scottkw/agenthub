---
phase: 118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi
plan: 05
type: execute
wave: 3
depends_on: [118-01, 118-02, 118-03, 118-04]
files_modified:
  - internal/daemon/api.go
  - internal/daemon/api_test.go
  - internal/daemon/types.go
  - internal/daemon/client.go
  - internal/daemon/client_test.go
  - internal/daemon/engine.go
autonomous: true
requirements: [FS-03, FS-04, FS-05, FS-06, FS-12]
must_haves:
  truths:
    - "Daemon API registers GET /api/files/list, GET /api/files/stat, GET /api/files/read, and HEAD /api/files/read on the existing http.ServeMux — all method-prefixed per Go 1.22+ pattern, all unauthenticated (loopback socket is trusted)"
    - "POST /api/files/list and any non-GET/HEAD method on the file routes returns 405 (mux pattern enforces method via prefix; do not register fallback handlers that would mask this)"
    - "The handler is constructed once via files.NewHandler(resolver) where resolver closes over engine.GetSessionWorkDir + files.NewSandbox"
    - "DaemonClient gains ListFiles, StatFile, ReadFile methods using the existing client transport pattern"
    - "issueCapabilitiesForSession now mints the write (owner) token with Perms = \"read,write,files.read\" when daemonSettings.FilesRead is nil or *true; viewer (read) token Perms remains \"read\" (no files.read)"
    - "Integration test confirms a session-owner DaemonClient.ListFiles call against a real running daemon API returns the session cwd's directory entries"
    - "0-byte file read via daemon socket returns 200 (not 416) — FS-07 end-to-end verification through the daemon route layer"
  artifacts:
    - path: "internal/daemon/api.go"
      provides: "3 file route registrations + handler construction + issueCapabilitiesForSession edit"
      contains: "/api/files/list"
      contains_2: "/api/files/stat"
      contains_3: "/api/files/read"
      contains_4: "files.NewHandler"
    - path: "internal/daemon/types.go"
      provides: "Re-exported FileEntry/FileListResponse OR explicit doc-comment that internal/files exports are the wire-type source"
    - path: "internal/daemon/client.go"
      provides: "ListFiles + StatFile + ReadFile + HeadFile methods on *DaemonClient"
      contains: "func (c *DaemonClient) ListFiles"
      contains_2: "func (c *DaemonClient) StatFile"
      contains_3: "func (c *DaemonClient) ReadFile"
  key_links:
    - from: "internal/daemon/api.go (registerRoutes)"
      to: "internal/files.NewHandler"
      via: "files.NewHandler(func(sid string) (*files.Sandbox, error) { wd := a.engine.GetSessionWorkDir(sid); if wd == \"\" { return nil, errSessionNotFound }; return files.NewSandbox(wd) })"
      pattern: "files\\.NewHandler\\("
    - from: "internal/daemon/api.go (issueCapabilitiesForSession)"
      to: "Phase 118 token Perms edit"
      via: "owner token Perms = \"read,write,files.read\"; viewer = \"read\""
      pattern: "read,write,files\\.read"
    - from: "internal/daemon/client.go (ListFiles)"
      to: "DaemonClient HTTP transport"
      via: "existing http.Client over Unix socket / named pipe; same patterns as existing Sessions methods"
      pattern: "c\\.http\\.Get\\|c\\.do\\("
---

<objective>
Integration layer — wire Plans 01-04 outputs into the running daemon process:

1. Register `GET /api/files/list`, `GET /api/files/stat`, `GET /api/files/read`, `HEAD /api/files/read` on the daemon's `http.ServeMux` (the Unix socket / named pipe mux owned by `internal/daemon/api.go`). No auth — loopback transport is the trust boundary (per ARCHITECTURE.md Decision 2 + REQUIREMENTS.md WEB-01).

2. Construct the file `Handler` from Plan 02 by closing over `SessionEngine.GetSessionWorkDir` from Plan 04 and `files.NewSandbox` from Plan 01. The resolver indirection (RESEARCH.md Open Question 3) means Phase 119 will mount the same `Handler` on the webserver via `SetFilesHandlerProvider` without any logic duplication.

3. Add `DaemonClient.ListFiles / StatFile / ReadFile / HeadFile` methods so the GUI/CLI/TUI consumers (Phases 120/121 and any future consumer) have a typed Go API instead of crafting URLs ad-hoc.

4. Edit `issueCapabilitiesForSession` to include `files.read` in the **write (owner)** token's `Perms` field when `daemonSettings.FilesRead` is nil or `*true`. The **read (viewer)** token's Perms remains `"read"` — viewer sessions do NOT get `files.read` by default (FS-12). This is the token-issuance side of FS-13: the daemon-socket surface has no cap-token check, but the webserver surface (Phase 119) will gate on `requireFilesRead` consuming the Perms set here.

5. Land an end-to-end test that exercises the route registration + handler + sandbox + engine integration through `DaemonClient` — proving the seams between Plans 01/02/03/04 hold under real HTTP traffic, not just unit tests.

Output: Edits to `internal/daemon/api.go`, `internal/daemon/client.go`, additive wire-types in `internal/daemon/types.go` (or a doc-comment pointing to `internal/files.FileEntry`), and integration tests in `api_test.go` + `client_test.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-RESEARCH.md
@.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-01-SUMMARY.md
@.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-02-SUMMARY.md
@.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-03-SUMMARY.md
@.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-04-SUMMARY.md
@internal/daemon/api.go
@internal/daemon/client.go
@internal/daemon/types.go
@internal/files/handler.go
@internal/files/sandbox.go

<interfaces>
Available from Plan 01:
- `func files.NewSandbox(workDir string) (*files.Sandbox, error)` — empty-string and resolution failures already handled.

Available from Plan 02:
- `func files.NewHandler(resolve func(sessionID string) (*files.Sandbox, error)) *files.Handler`
- `func (h *files.Handler) List(w http.ResponseWriter, r *http.Request)`
- `func (h *files.Handler) Stat(w http.ResponseWriter, r *http.Request)`
- `func (h *files.Handler) Read(w http.ResponseWriter, r *http.Request)`
- `type files.FileEntry struct { ... }` / `type files.FileListResponse struct { ... }` — the wire types

Available from Plan 04:
- `func (e *SessionEngine) GetSessionWorkDir(id string) string` — returns "" for unknown session
- `e.filesRead *bool` lower-case field on SessionEngine (nil for legacy files; *true default; *false explicit user disable)

Existing daemon API surface (api.go lines 55-90 verified):
- `a.registerRoutes()` populates a.mux at startup
- `a.mux.HandleFunc("GET /sessions/{id}", a.handleGetSession)` is the existing route-registration pattern
- `func (a *API) issueCapabilitiesForSession(sessionID string) (readURL, writeURL, readCode, writeCode string, err error)` exists at line 927 with the locked statements:
  - `rClaims := capability.Claims{SID: sessionID, Perms: "read", IAT: now, GrantID: hex.EncodeToString(rgid[:]), V: 1}` (line 955)
  - `wClaims := capability.Claims{SID: sessionID, Perms: "read,write", IAT: now, GrantID: hex.EncodeToString(wgid[:]), V: 1}` (line 956)
- The `API` struct holds an `engine *SessionEngine` field (verified by reading api.go's a.engine.* call sites)
- The `API` struct already has the http.ServeMux as `a.mux`

Existing DaemonClient pattern (client.go):
- `type DaemonClient struct { http *http.Client; baseURL string }` (verified via grep)
- Methods follow the shape: `func (c *DaemonClient) Xxx(...) (...) { resp, err := c.http.Get(c.baseURL + "/path?..."); ... }`
- Body decoding follows: `var out T; json.NewDecoder(resp.Body).Decode(&out)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register the three file routes on daemon mux + wire files.NewHandler with engine-backed sandboxResolver</name>
  <files>internal/daemon/api.go, internal/daemon/api_test.go</files>
  <read_first>
    - /Users/ken/dev/agenthub/internal/daemon/api.go (lines 50-110 — API struct construction + registerRoutes layout)
    - /Users/ken/dev/agenthub/internal/files/handler.go (the Handler API your route handlers will delegate to)
    - /Users/ken/dev/agenthub/internal/files/sandbox.go (NewSandbox signature)
    - /Users/ken/dev/agenthub/.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-RESEARCH.md (Architectural Responsibility Map row "Daemon-socket routes"; Open Question 3 resolver recommendation)
    - /Users/ken/dev/agenthub/.planning/research/PITFALLS.md (Pitfall 8 — Mux routes need explicit method prefix; POST returns 405; missing cap = 401 not 404 — note that this plan owns DAEMON socket which is unauthenticated, so the 401 semantics live in Phase 119)
  </read_first>
  <behavior>
    - registerRoutes registers exactly these four NEW patterns (in addition to all existing patterns it already registers):
      - `"GET /api/files/list"` → file handler's List method
      - `"GET /api/files/stat"` → file handler's Stat method
      - `"GET /api/files/read"` → file handler's Read method
      - `"HEAD /api/files/read"` → file handler's Read method (same handler — http.ServeContent handles HEAD intrinsically)
    - The API struct gains a `filesHandler *files.Handler` field constructed once during API initialization (in the same constructor or initialization path that calls `a.registerRoutes()`).
    - The sandboxResolver closure encapsulates:
      ```
      func(sessionID string) (*files.Sandbox, error) {
          wd := a.engine.GetSessionWorkDir(sessionID)
          if wd == "" {
              return nil, errors.New("session not found or has no resolvable working directory")
          }
          return files.NewSandbox(wd)
      }
      ```
    - POST /api/files/list returns 405 Method Not Allowed (Go 1.22+ mux behavior — registering GET pattern implies non-GET methods return 405 automatically; no explicit POST handler is registered).
    - GET on a real running daemon API (test harness using httptest.NewServer with the same mux) with `?session=<id>&path=.` returns 200 with valid FileListResponse JSON.
    - GET on /api/files/list with no `session` query param returns 404 "session not found" (because the resolver returns nil from GetSessionWorkDir("")).
    - GET on /api/files/read on a 0-byte file returns 200 with empty body (FS-07 ROADMAP success criterion 3 — end-to-end via daemon socket).
    - GET on /api/files/read with `?path=../etc/passwd` returns 403 — ROADMAP success criterion 4 (sandbox rejects traversal end-to-end).
  </behavior>
  <action>
    Edit `internal/daemon/api.go`:

    1. Import `github.com/scottkw/agenthub/internal/files` (the Plan 01 + 02 package).

    2. Add a field to the API struct: `filesHandler *files.Handler  // Phase 118: handles /api/files/{list,stat,read}; resolver closes over engine.GetSessionWorkDir`. Place it adjacent to existing handler/state fields on the struct.

    3. In the API constructor (the function that returns a new *API and calls registerRoutes — likely named `New(...)`), construct the handler BEFORE calling registerRoutes:
       ```
       a.filesHandler = files.NewHandler(func(sessionID string) (*files.Sandbox, error) {
           if sessionID == "" {
               return nil, errors.New("missing session parameter")
           }
           wd := a.engine.GetSessionWorkDir(sessionID)
           if wd == "" {
               return nil, errors.New("session not found or has no working directory")
           }
           return files.NewSandbox(wd)
       })
       ```
       If the existing constructor does not have an `errors` import, add it.

    4. In registerRoutes, immediately after the existing `a.mux.HandleFunc` calls (the last registration before the close-brace), add:
       ```
       // Phase 118: read-only file API on the daemon-local socket. No auth here —
       // the loopback transport (Unix socket / Windows named pipe) is the
       // trust boundary. The webserver mounts the same handler under
       // requireFilesRead in Phase 119.
       a.mux.HandleFunc("GET /api/files/list", a.filesHandler.List)
       a.mux.HandleFunc("GET /api/files/stat", a.filesHandler.Stat)
       a.mux.HandleFunc("GET /api/files/read", a.filesHandler.Read)
       a.mux.HandleFunc("HEAD /api/files/read", a.filesHandler.Read)
       ```
       The four registrations are explicit and method-prefixed per Pitfall 8. Go 1.22+ mux automatically returns 405 for non-registered methods on a known path.

    5. Add integration tests in `internal/daemon/api_test.go`:
       - `TestAPI_FilesList_RealSession`: Set up a test engine + API with a tempdir-backed session; populate the session's workDir via CreateSession (or via test-only helper); start httptest.NewServer over a.mux; issue `GET /api/files/list?session=<id>&path=.`; assert 200 and parse JSON; assert entries non-empty when tempdir has files.
       - `TestAPI_FilesList_UnknownSessionReturns404`: missing/unknown session → 404 with body "session not found".
       - `TestAPI_FilesRead_ZeroByteReturns200`: tempdir contains a 0-byte file; `GET /api/files/read?session=<id>&path=empty.txt` returns 200 with Content-Length 0 — FS-07 end-to-end (ROADMAP success criterion 3 in literal form).
       - `TestAPI_FilesRead_TraversalReturns403`: `GET /api/files/read?session=<id>&path=../../etc/passwd` returns 403 — ROADMAP success criterion 4 in literal form.
       - `TestAPI_FilesList_POSTReturns405`: `POST /api/files/list?session=<id>&path=.` returns 405 (Pitfall 8 — registering GET-only pattern makes Go mux auto-reject other methods).
       - `TestAPI_FilesHeadRead_ReturnsContentLengthNoBody`: HEAD /api/files/read on a 100-byte file → 200, Content-Length 100, body length 0 — FS-06 end-to-end.
       - `TestAPI_FilesStat_ReturnsFileEntry`: GET /api/files/stat on a real file → JSON parses as files.FileEntry with the expected Name/Size/IsDir/MIME fields.

    Do NOT add `?download=1` query parameter handling — RESEARCH.md Assumption A7 locks the 5 MB cap as unconditional for v3.4.
    Do NOT add a separate POST handler that returns 405 — let the Go 1.22+ mux behavior do that automatically (Pitfall 8: registering an explicit catch-all would conflict with the mux's automatic method-rejection).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./internal/daemon/ && go test -run '^TestAPI_(Files|Files.*)' ./internal/daemon/ -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"GET /api/files/list"' internal/daemon/api.go` returns at least 1.
    - `grep -c '"GET /api/files/stat"' internal/daemon/api.go` returns at least 1.
    - `grep -c '"GET /api/files/read"' internal/daemon/api.go` returns at least 1.
    - `grep -c '"HEAD /api/files/read"' internal/daemon/api.go` returns at least 1.
    - `grep -c 'files.NewHandler' internal/daemon/api.go` returns at least 1.
    - `grep -c 'a.engine.GetSessionWorkDir' internal/daemon/api.go` returns at least 1 (resolver closure consumes engine method).
    - `grep -c 'files.NewSandbox' internal/daemon/api.go` returns at least 1 (resolver constructs sandbox per request).
    - `grep -c 'filesHandler.*\*files.Handler' internal/daemon/api.go` returns at least 1 (struct field).
    - `awk '/^func \(a \*API\) registerRoutes/,/^}/{ print }' internal/daemon/api.go | grep -c 'POST /api/files'` returns 0 (no explicit POST handlers — relying on mux 405 auto-behavior per Pitfall 8).
    - `go build ./internal/daemon/` exits 0.
    - `go test -run '^TestAPI_Files' ./internal/daemon/ -count=1 -v` exits 0 with all 7 subtests passing including ZeroByteReturns200, TraversalReturns403, POSTReturns405, HeadRead_ReturnsContentLengthNoBody.
  </acceptance_criteria>
  <done>
    Three GET routes + HEAD on read are wired to the file handler; resolver consumes engine.GetSessionWorkDir; integration tests prove the 0-byte / traversal / POST / HEAD behaviors end-to-end through the daemon mux. ROADMAP success criteria 2, 3, 4 satisfied at the daemon-socket layer.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add DaemonClient.ListFiles / StatFile / ReadFile / HeadFile methods + types.go wire-type re-exports</name>
  <files>internal/daemon/client.go, internal/daemon/client_test.go, internal/daemon/types.go</files>
  <read_first>
    - /Users/ken/dev/agenthub/internal/daemon/client.go (full file — existing method patterns including JSON decoding and error propagation)
    - /Users/ken/dev/agenthub/internal/daemon/client_test.go (existing test scaffolding)
    - /Users/ken/dev/agenthub/internal/daemon/types.go (existing wire-type conventions)
    - /Users/ken/dev/agenthub/internal/files/handler.go (the FileEntry and FileListResponse types — these are the source of truth)
    - /Users/ken/dev/agenthub/.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-RESEARCH.md (Pattern: DaemonClient methods returning files.FileEntry types; OQ-1 — HEAD support on daemon socket)
  </read_first>
  <behavior>
    - DaemonClient.ListFiles(ctx, sessionID, relPath string) returns (entries []files.FileEntry, truncated bool, err error).
    - On HTTP non-2xx, ListFiles returns an error containing the status code (e.g. "files list: 403 access denied: path traversal rejected").
    - DaemonClient.StatFile(ctx, sessionID, relPath string) returns (files.FileEntry, error).
    - DaemonClient.ReadFile(ctx, sessionID, relPath string) returns (data []byte, contentType string, err error). For files > 5 MB returns an error (the daemon already returns 413; client surfaces this as a typed error).
    - DaemonClient.HeadFile(ctx, sessionID, relPath string) returns (size int64, contentType string, modTime time.Time, err error). Used by Phase 120 for preflight (RESEARCH.md OQ-1).
    - All four methods use the existing DaemonClient transport (the `c.http *http.Client` field that already targets the Unix socket / named pipe) — no new HTTP plumbing.
    - Path encoding: relPath and sessionID go through `url.Values.Encode()` so special characters do not break the URL.
    - The types.go file either re-exports `FileEntry = files.FileEntry` and `FileListResponse = files.FileListResponse` (Go type aliases) OR adds a doc-comment block at the top of types.go pointing readers to `internal/files` as the source of truth for these wire types. Pick whichever the existing types.go conventions favor.
  </behavior>
  <action>
    1. Edit `internal/daemon/types.go`. Add either:
       - Type aliases at the bottom of the file:
         ```
         // Phase 118 (FS-03/FS-04): file browser wire types live in internal/files
         // — re-exported here so DaemonClient consumers do not need to import
         // both packages.
         type FileEntry = files.FileEntry
         type FileListResponse = files.FileListResponse
         ```
         Add the `internal/files` import if absent.
       - OR a doc-comment block referencing internal/files if the codebase favors no aliasing.

       Pick aliasing because it keeps caller imports clean (Phases 120/121 import only `internal/daemon` for the client + types).

    2. Edit `internal/daemon/client.go`. Add the four methods following the existing client method shape (look at e.g. how `GetSession` or similar existing methods construct the URL and decode the response). Concrete signatures and behavior:

       ```
       // ListFiles fetches a directory listing for the given session via the
       // daemon-local socket. Loopback transport — no auth needed.
       // Phase 118 / FS-03.
       func (c *DaemonClient) ListFiles(ctx context.Context, sessionID, relPath string) ([]files.FileEntry, bool, error) {
           if relPath == "" { relPath = "." }
           q := url.Values{}
           q.Set("session", sessionID)
           q.Set("path", relPath)
           req, _ := http.NewRequestWithContext(ctx, http.MethodGet, c.baseURL+"/api/files/list?"+q.Encode(), nil)
           resp, err := c.http.Do(req)
           if err != nil { return nil, false, err }
           defer resp.Body.Close()
           if resp.StatusCode != http.StatusOK {
               body, _ := io.ReadAll(resp.Body)
               return nil, false, fmt.Errorf("files list: %d %s", resp.StatusCode, strings.TrimSpace(string(body)))
           }
           var out files.FileListResponse
           if err := json.NewDecoder(resp.Body).Decode(&out); err != nil { return nil, false, err }
           return out.Entries, out.Truncated, nil
       }

       // StatFile fetches metadata for a single path. Phase 118 / FS-04.
       func (c *DaemonClient) StatFile(ctx context.Context, sessionID, relPath string) (files.FileEntry, error) { ... similar ... }

       // ReadFile fetches file bytes. Phase 118 / FS-05. The daemon enforces
       // the 5 MB preview cap server-side; a 413 surfaces here as an error.
       func (c *DaemonClient) ReadFile(ctx context.Context, sessionID, relPath string) ([]byte, string, error) { ... }

       // HeadFile preflights size + Content-Type without transferring the body.
       // Phase 118 / FS-06.
       func (c *DaemonClient) HeadFile(ctx context.Context, sessionID, relPath string) (int64, string, time.Time, error) { ... }
       ```

       Use the EXACT same `c.baseURL` / `c.http.Do` / `url.Values.Encode()` pattern that existing client methods use — do not invent a new transport.

    3. Add tests to `internal/daemon/client_test.go`:
       - `TestDaemonClient_ListFiles`: spin up an httptest.NewServer with a real *API + tempdir-backed session (reuse the harness from Plan 05 Task 1's TestAPI_FilesList); construct a DaemonClient pointing at httptest URL; call ListFiles; assert the returned entries match the directory.
       - `TestDaemonClient_StatFile`: similar; assert single FileEntry with matching Name/Size.
       - `TestDaemonClient_ReadFile`: assert returned bytes match file contents.
       - `TestDaemonClient_ReadFile_OverCapReturnsError`: create a 6 MB file; ReadFile returns a non-nil error containing "413" or "too large".
       - `TestDaemonClient_HeadFile`: assert Size + ContentType returned without body; no body fetched (this is the FS-06 preflight contract).
       - `TestDaemonClient_ListFiles_TraversalReturns403Error`: ?path=../escape — ListFiles returns error mentioning "403" and "traversal" (or "access denied").

    4. The existing daemon test infrastructure already has scaffolding for httptest-backed API tests; use it. If a helper named `newTestAPI(t)` or similar exists, reuse it; otherwise inline the construction following the patterns in api_test.go.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./internal/daemon/ && go test -run '^TestDaemonClient_(ListFiles|StatFile|ReadFile|HeadFile)' ./internal/daemon/ -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^func (c \*DaemonClient) ListFiles' internal/daemon/client.go` returns 1.
    - `grep -c '^func (c \*DaemonClient) StatFile' internal/daemon/client.go` returns 1.
    - `grep -c '^func (c \*DaemonClient) ReadFile' internal/daemon/client.go` returns 1.
    - `grep -c '^func (c \*DaemonClient) HeadFile' internal/daemon/client.go` returns 1.
    - `grep -c 'url.Values' internal/daemon/client.go` returns at least 4 (each method uses url.Values.Encode() — defense against path-quoting bugs).
    - `grep -c 'type FileEntry = files.FileEntry' internal/daemon/types.go` returns 1 (type alias OR an equivalent doc-comment pointer is acceptable; the grep gate accepts the alias form).
    - `go build ./internal/daemon/` exits 0.
    - `go test -run '^TestDaemonClient_(ListFiles|StatFile|ReadFile|HeadFile)' ./internal/daemon/ -count=1 -v` exits 0 with all subtests passing.
  </acceptance_criteria>
  <done>
    DaemonClient now has typed methods for List/Stat/Read/Head; Phases 120/121 consume these methods rather than crafting URLs by hand. Wire types are re-exported from internal/daemon so callers import only one package.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Edit issueCapabilitiesForSession to include files.read in owner Perms gated on daemonSettings.FilesRead</name>
  <files>internal/daemon/api.go, internal/daemon/api_test.go</files>
  <read_first>
    - /Users/ken/dev/agenthub/internal/daemon/api.go (lines 918-985 — issueCapabilitiesForSession including the rClaims/wClaims construction at lines 955-956)
    - /Users/ken/dev/agenthub/internal/capability/capability.go (the Claims struct + PermFilesRead constant from Plan 03)
    - /Users/ken/dev/agenthub/internal/daemon/engine.go (the e.filesRead field from Plan 04)
    - /Users/ken/dev/agenthub/.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-RESEARCH.md (Decision 4 + Validation Architecture FS-12 row)
    - /Users/ken/dev/agenthub/.planning/research/PITFALLS.md (Pitfall 4 — default-on for session owner, default-off for web-share viewer)
  </read_first>
  <behavior>
    - When daemonSettings.FilesRead is nil (legacy file) OR explicitly *true, issueCapabilitiesForSession mints the write (owner) token with `Perms = "read,write,files.read"`.
    - When daemonSettings.FilesRead is *false (operator explicitly disabled), the write token's Perms remains `"read,write"` — no files.read injected.
    - The read (viewer) token's Perms remains `"read"` in ALL cases — viewer never gets files.read by default (FS-12).
    - Token construction uses `capability.PermFilesRead` (the Plan 03 constant) — no hardcoded string literal.
    - No other claim fields change (SID, IAT, GrantID, V stay as-is). The HMAC signs the canonical JSON, which now includes the extended Perms — existing tokens issued before this edit still verify because they don't include files.read but Phase 119's requireFilesRead simply returns 403 for them (legacy viewer behavior preserved).
    - Test: TestIssueCapabilities_OwnerHasFilesRead asserts the decoded write-token Perms == "read,write,files.read" when FilesRead is nil (default-true case).
    - Test: TestIssueCapabilities_ViewerNoFilesRead asserts the decoded read-token Perms == "read" — no files.read present (false on capability.HasPerm(viewerClaims.Perms, "files.read")).
    - Test: TestIssueCapabilities_OwnerNoFilesReadWhenSettingFalse — manually set e.filesRead = ptrBool(false); assert write-token Perms == "read,write" (no files.read).
  </behavior>
  <action>
    Edit `internal/daemon/api.go` in `issueCapabilitiesForSession`:

    1. Just before the existing line `wClaims := capability.Claims{SID: sessionID, Perms: "read,write", IAT: now, GrantID: hex.EncodeToString(wgid[:]), V: 1}` (line ~956), compute the owner Perms string:
       ```
       // Phase 118 / FS-12: owner token includes files.read unless the operator
       // has explicitly disabled it via daemonSettings.FilesRead. nil (legacy
       // settings file pre-v3.4) defaults to true via the engine's loadSettingsFromDisk
       // defaults-merge — see Plan 04.
       ownerPerms := "read,write"
       if a.engine.filesReadEnabled() {
           ownerPerms = "read,write," + capability.PermFilesRead
       }
       wClaims := capability.Claims{SID: sessionID, Perms: ownerPerms, IAT: now, GrantID: hex.EncodeToString(wgid[:]), V: 1}
       ```
       Note: `capability.PermFilesRead` (the Plan 03 constant) substitutes for any hardcoded `"files.read"` string in this edit.

    2. The read (viewer) token line is UNCHANGED:
       `rClaims := capability.Claims{SID: sessionID, Perms: "read", IAT: now, GrantID: hex.EncodeToString(rgid[:]), V: 1}`

    3. Add a new helper on SessionEngine (in `internal/daemon/engine.go`):
       ```
       // filesReadEnabled returns true if file-browser capability issuance is
       // enabled for owner tokens. nil filesRead (pre-v3.4 settings) defaults
       // to true; explicit *true is true; explicit *false is false.
       // Phase 118 / FS-12.
       func (e *SessionEngine) filesReadEnabled() bool {
           e.mu.RLock()
           defer e.mu.RUnlock()
           return e.filesRead == nil || *e.filesRead
       }
       ```
       Lower-case (unexported) — only api.go in the same package consumes it.

    4. Add tests to `internal/daemon/api_test.go` (or extend existing capability-issuance tests if they live there):
       - `TestIssueCapabilities_OwnerHasFilesRead_WhenSettingNil`: engine constructed via NewSessionEngine (filesRead defaults to nil if no settings.json exists, OR to *true via the load-defaults). Call issueCapabilitiesForSession; capability.Verify the returned write token; assert the decoded Perms contains the string "files.read" via `capability.HasPerm(claims.Perms, capability.PermFilesRead) == true`.
       - `TestIssueCapabilities_ViewerNoFilesRead`: same setup; verify the read token; assert `capability.HasPerm(rClaims.Perms, capability.PermFilesRead) == false` AND `rClaims.Perms == "read"`.
       - `TestIssueCapabilities_OwnerNoFilesReadWhenDisabled`: construct engine; manually set `e.filesRead` to a pointer-to-false via a test helper or direct field write under the same package; call issueCapabilitiesForSession; assert the write token's Perms == "read,write" (the literal old value).
       - `TestIssueCapabilities_OwnerHasFilesReadWhenExplicitTrue`: similar but with `e.filesRead = ptrTrue()`; assert Perms == "read,write,files.read".

    Do NOT modify `requireCapability` middleware to check files.read (Plan 03 + Pitfall 4 — that would break terminal routes).
    Do NOT change rClaims (viewer token) — viewers default to no file-read access per FS-12.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./internal/daemon/ && go test -run '^TestIssueCapabilities_(OwnerHasFilesRead|ViewerNoFilesRead|OwnerNoFilesReadWhenDisabled|OwnerHasFilesReadWhenExplicitTrue)' ./internal/daemon/ -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^func \(a \*API\) issueCapabilitiesForSession/,/^}/{ print }' internal/daemon/api.go | grep -c 'capability.PermFilesRead'` returns at least 1 (owner Perms uses the constant from Plan 03, not a string literal).
    - `awk '/^func \(a \*API\) issueCapabilitiesForSession/,/^}/{ print }' internal/daemon/api.go | grep -c 'a.engine.filesReadEnabled()'` returns at least 1 (settings-gated path).
    - `awk '/^func \(a \*API\) issueCapabilitiesForSession/,/^}/{ print }' internal/daemon/api.go | grep -c '"read,write,files.read"'` returns 0 (constant composition only — `"read,write," + capability.PermFilesRead` — not a hardcoded literal).
    - `awk '/^func \(a \*API\) issueCapabilitiesForSession/,/^}/{ print }' internal/daemon/api.go | grep -c 'Perms: "read"'` returns at least 1 (viewer token unchanged).
    - `grep -c '^func (e \*SessionEngine) filesReadEnabled' internal/daemon/engine.go` returns 1.
    - `go test -run '^TestIssueCapabilities_(OwnerHasFilesRead_WhenSettingNil|ViewerNoFilesRead|OwnerNoFilesReadWhenDisabled|OwnerHasFilesReadWhenExplicitTrue)$' ./internal/daemon/ -count=1 -v` exits 0 with all four subtests passing.
    - Existing capability tests still pass: `go test ./internal/capability/ ./internal/webserver/ -count=1` exits 0 (no regressions to Sign/Verify or requireCapability / requireFilesRead from Plan 03).
  </acceptance_criteria>
  <done>
    Owner tokens now mint with `"read,write,files.read"` (settings-gated default-on); viewer tokens remain `"read"` (default-off per FS-12). ROADMAP success criterion 5 acceptance is in place at the issuance side; Phase 119 will mount `requireFilesRead` to enforce on the webserver.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unix socket / named pipe → daemon mux | Loopback OS-level transport; no auth gate on file routes (RESEARCH.md Decision 2 + WEB-01) |
| DaemonClient → daemon mux | Same loopback channel as above; client surfaces 4xx/5xx as typed errors |
| issueCapabilitiesForSession → minted token | Settings-gated decision: owner gets `files.read` only when daemonSettings.FilesRead is nil or *true |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-21 | Spoofing | daemon socket file routes | accept | Loopback transport is trusted (per ARCHITECTURE.md Decision 2 + REQUIREMENTS.md WEB-01); no auth gate. Webserver surface (Phase 119) adds requireFilesRead. |
| T-118-22 | Tampering | Mux POST returning 200 | mitigate | Routes registered with explicit GET / HEAD prefix; Go 1.22+ mux returns 405 for non-registered methods. No explicit POST handler exists (would mask 405). Verified by TestAPI_FilesList_POSTReturns405. |
| T-118-23 | Elevation of Privilege | Viewer token accidentally including files.read | mitigate | rClaims construction is unchanged — `Perms: "read"` literal preserved. TestIssueCapabilities_ViewerNoFilesRead asserts `capability.HasPerm(rClaims.Perms, PermFilesRead) == false`. FS-12 enforced. |
| T-118-24 | Elevation of Privilege | Owner token issued without files.read after Phase 118 ships | mitigate | Defaults-merge in Plan 04 ensures e.filesRead is *true for any settings.json lacking the key (Pitfall 16). Owner Perms = "read,write,files.read" in the default case. Operator can explicitly opt out by setting filesRead=false in settings.json (TestIssueCapabilities_OwnerNoFilesReadWhenDisabled). |
| T-118-25 | Information Disclosure | DaemonClient surfacing raw error bodies | accept | Error formatting includes status code + trimmed body; client callers (Phases 120/121) decide UX. Not a security boundary — these errors only surface to in-process consumers over loopback. |
</threat_model>

<verification>
- `go build ./internal/daemon/` exits 0.
- `go vet ./internal/daemon/` reports no diagnostics.
- `go test ./internal/files/ ./internal/capability/ ./internal/webserver/ ./internal/daemon/ -count=1 -short` exits 0 (all five plans' tests pass together — no integration regressions; pre-existing skips documented in STATE.md remain unchanged).
- Fuzz merge gate re-run (sanity): `go test -fuzz=FuzzSandboxPath -fuzztime=60s ./internal/files/` reports zero crashes.
- ROADMAP success criteria, verified end-to-end through daemon socket:
  - SC1 (fuzz corpus 60s clean) — Plan 01.
  - SC2 (daemon socket /list?session=&path=. returns FileEntry JSON for cwd) — TestAPI_FilesList_RealSession + TestDaemonClient_ListFiles.
  - SC3 (0-byte read returns 200 not 416) — TestAPI_FilesRead_ZeroByteReturns200.
  - SC4 (traversal returns 400/403) — TestAPI_FilesRead_TraversalReturns403.
  - SC5 (viewer without files.read = 403; owner token Perms contains "files.read"; HasPerm whole-token) — TestIssueCapabilities_ViewerNoFilesRead + TestIssueCapabilities_OwnerHasFilesRead_WhenSettingNil + Plan 03 TestHasPerm + TestRequireFilesRead.
</verification>

<success_criteria>
- Three GET routes + HEAD on /api/files/read registered on the daemon mux, all delegating to a single files.Handler constructed via files.NewHandler(resolver) where resolver closes over engine.GetSessionWorkDir + files.NewSandbox (FS-03, FS-04, FS-05, FS-06).
- POST /api/files/list returns 405 via Go 1.22+ mux auto-rejection (Pitfall 8).
- DaemonClient.ListFiles / StatFile / ReadFile / HeadFile methods consume the daemon socket using the existing transport and surface 413 / 403 / 404 as typed errors.
- types.go re-exports files.FileEntry and files.FileListResponse so Phase 120/121 consumers import only internal/daemon.
- issueCapabilitiesForSession owner token Perms = "read,write,files.read" when filesRead is enabled (nil or *true); == "read,write" when explicitly disabled.
- Viewer token Perms remains "read" — no files.read injected (FS-12).
- The plan_03 capability.PermFilesRead constant is used everywhere — no hardcoded "files.read" string literals in api.go's issuance code.
- All five ROADMAP Phase 118 success criteria have end-to-end test evidence at the daemon-socket layer.
</success_criteria>

<output>
Create `.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-05-SUMMARY.md` when done. With this plan merged, Phase 118 is complete: Phase 119 can begin (mount requireFilesRead on /api/files/* webserver routes via SetFilesHandlerProvider); Phase 120 (FileBrowserTab) and Phase 121 (TUI Files view) can begin once Phase 119 ships.
</output>
