---
phase: 118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/capability/capability.go
  - internal/capability/capability_test.go
  - internal/webserver/capability_mw.go
  - internal/webserver/capability_test.go
autonomous: true
requirements: [FS-10, FS-11, FS-13]
must_haves:
  truths:
    - "capability package exports PermFilesRead constant equal to the string \"files.read\""
    - "HasPerm(perms, perm) returns true only on whole-token comma-separated match; strings.Contains-style substring matches must NOT yield true"
    - "HasPerm(\"read,write\", \"files.read\") returns false"
    - "HasPerm(\"read,files.read\", \"files.read\") returns true"
    - "HasPerm(\"no-files.read,read\", \"files.read\") returns false (the critical Pitfall 4 false-positive case)"
    - "requireFilesRead middleware wrapper chains requireCapability then HasPerm(claims.Perms, PermFilesRead); on miss responds 403 with response body containing the literal string \"files.read\""
    - "requireFilesRead is a SEPARATE wrapper — internal/webserver/capability_mw.go's existing requireCapability function body is NOT modified"
  artifacts:
    - path: "internal/capability/capability.go"
      provides: "PermFilesRead constant + HasPerm helper"
      contains: "PermFilesRead = \"files.read\""
      contains_2: "func HasPerm"
    - path: "internal/capability/capability_test.go"
      provides: "TestHasPerm covering the 6 RESEARCH.md cases including the no-files.read false-positive guard"
      contains: "func TestHasPerm"
    - path: "internal/webserver/capability_mw.go"
      provides: "requireFilesRead wrapper that delegates to requireCapability and adds the HasPerm check"
      contains: "func (ws *WebServer) requireFilesRead"
    - path: "internal/webserver/capability_test.go"
      provides: "TestRequireFilesRead covering the 403-with-message path and the pass-through path"
      contains: "func TestRequireFilesRead"
  key_links:
    - from: "internal/webserver/capability_mw.go (requireFilesRead)"
      to: "internal/capability.HasPerm"
      via: "whole-token comma-split match — never strings.Contains"
      pattern: "capability\\.HasPerm"
    - from: "internal/webserver/capability_mw.go (requireFilesRead)"
      to: "internal/webserver/capability_mw.go (requireCapability)"
      via: "wrapper-of-wrapper composition; requireCapability runs first, then HasPerm"
      pattern: "ws\\.requireCapability\\("
---

<objective>
Add the `files.read` capability primitive: a `PermFilesRead` constant in `internal/capability`, a `HasPerm(perms, perm string) bool` helper with whole-token comma-split semantics (Pitfall 4 mitigation — `strings.Contains` is forbidden for this check), and a `requireFilesRead` middleware wrapper in `internal/webserver/capability_mw.go` that chains the existing `requireCapability` with the new `HasPerm` check. The wrapper body lives in Phase 118 per RESEARCH.md FS-11/FS-13 disambiguation; Phase 119 mounts it on webserver routes. Phase 118 unit-tests it standalone via synthetic `capability.WithClaims` context.

Purpose: All three file endpoints in Phase 119's webserver mount will gate on this single wrapper. Getting whole-token semantics right is a security-critical correctness property — `HasPerm("no-files.read", "files.read")` returning true would silently grant access. The test corpus must cover the false-positive case explicitly.

Output: Two source edits (`capability.go`, `capability_mw.go`) and two test additions (`capability_test.go`, `capability_test.go` in webserver pkg). No coupling to `internal/daemon` or `internal/files` — this plan operates entirely in the capability/webserver layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-RESEARCH.md
@.planning/research/PITFALLS.md
@internal/capability/capability.go
@internal/capability/context.go
@internal/webserver/capability_mw.go

<interfaces>
Existing capability package (read directly from source):
- `type Claims struct { SID string; Perms string; IAT int64; GrantID string; V int }` — Perms is a free-form comma-separated string. Current values in the field: "read" or "read,write" (defined in internal/daemon/api.go issueCapabilitiesForSession). Field order is load-bearing (HMAC signs declaration-order JSON bytes per the package doc-comment).
- `func WithClaims(ctx context.Context, c Claims) context.Context` (in context.go)
- `func FromContext(ctx context.Context) Claims` (in context.go)
- `func Sign(c Claims, key []byte) (string, error)`
- `func Verify(token string, key []byte) (Claims, error)`

Existing webserver middleware (capability_mw.go line 37):
- `func (ws *WebServer) requireCapability(next http.HandlerFunc) http.HandlerFunc` — 7-step gate that verifies HMAC, checks session ID, checks grant active, then attaches claims via `capability.WithClaims` and calls `next`.
- `ws` is `*WebServer` — the field this method hangs off.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PermFilesRead constant and HasPerm helper to internal/capability with whole-token comma-split semantics</name>
  <files>internal/capability/capability.go, internal/capability/capability_test.go</files>
  <read_first>
    - /Users/ken/dev/agenthub/internal/capability/capability.go (line 19-25 — Claims struct; the package doc-comment; the existing Sign/Verify functions)
    - /Users/ken/dev/agenthub/internal/capability/capability_test.go (existing test patterns to mirror)
    - /Users/ken/dev/agenthub/.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-RESEARCH.md (Pattern 4 "HasPerm whole-token match" at lines 384-410; Pitfall 4 references)
    - /Users/ken/dev/agenthub/.planning/research/PITFALLS.md (Pitfall 4 — Capability Token files.read Bit; Pitfall 3 — HasPerm false-positive via substring match)
  </read_first>
  <behavior>
    - PermFilesRead is exported and equals exactly the string "files.read".
    - HasPerm("", "files.read") returns false.
    - HasPerm("read,files.read", "") returns false (empty perm to check is rejected).
    - HasPerm("read,write", "files.read") returns false.
    - HasPerm("read,files.read", "files.read") returns true.
    - HasPerm("files.read,read", "files.read") returns true (any position in the list).
    - HasPerm("no-files.read", "files.read") returns false (the Pitfall 4 false-positive case — strings.Contains would return true here).
    - HasPerm("no-files.read,read", "files.read") returns false (Pitfall 4 case with additional tokens).
    - HasPerm("files.read.extra", "files.read") returns false (suffix attack).
    - HasPerm("read", "read") returns true (whole-token match works for existing perms).
    - HasPerm("read,write,files.read", "write") returns true.
    - HasPerm uses strings.Split on "," — NOT strings.Contains. Source-inspection grep must confirm the implementation does not use strings.Contains.
  </behavior>
  <action>
    Edit `internal/capability/capability.go`:

    1. Add a new exported const after the Claims struct declaration:
       ```
       // PermFilesRead is the capability bit token granting access to /api/files/* routes.
       // Added in v3.4 (Phase 118). Whole-token comma-separated semantics — never substring.
       const PermFilesRead = "files.read"
       ```
    2. Add the HasPerm helper (in capability.go, near Sign/Verify — it is part of the package's public surface):
       ```
       // HasPerm reports whether perm appears as a whole comma-separated token in perms.
       //
       // Whole-token semantics — NOT strings.Contains, which would allow "no-files.read"
       // to false-positive match "files.read" via substring inclusion (Pitfall 4 mitigation).
       //
       //   HasPerm("read,write",          "files.read") -> false
       //   HasPerm("read,files.read",     "files.read") -> true
       //   HasPerm("no-files.read,read",  "files.read") -> false  // critical
       //   HasPerm("files.read,read",     "files.read") -> true
       //   HasPerm("",                    "files.read") -> false
       func HasPerm(perms, perm string) bool {
           if perms == "" || perm == "" {
               return false
           }
           for _, t := range strings.Split(perms, ",") {
               if t == perm {
                   return true
               }
           }
           return false
       }
       ```
       Note: the `strings` import is already present in capability.go (used by Verify) — no new import needed.

    3. Do NOT modify the Claims struct field order — it is load-bearing for HMAC verification per the package doc-comment. The constant and the helper are additive, do not touch the struct.
    4. Do NOT add `files.read` to the existing `requireCapability` middleware switch or guard — that would break every existing terminal/relay route (Pitfall 4 anti-pattern). The middleware wrapper is Task 2.

    Edit `internal/capability/capability_test.go`:

    Append a `TestHasPerm` table-driven test with these subtests (one entry per row of the behavior table):
    - {"empty perms", "", "files.read", false}
    - {"empty target perm", "read,files.read", "", false}
    - {"read,write does not grant files.read", "read,write", "files.read", false}
    - {"read,files.read grants files.read", "read,files.read", "files.read", true}
    - {"position-independent — files.read first", "files.read,read", "files.read", true}
    - {"no-files.read false-positive guard", "no-files.read", "files.read", false}
    - {"no-files.read with siblings false-positive guard", "no-files.read,read", "files.read", false}
    - {"suffix attack", "files.read.extra", "files.read", false}
    - {"plain read whole-token", "read", "read", true}
    - {"write present alongside files.read", "read,write,files.read", "write", true}

    Each subtest: `t.Run(name, func(t *testing.T) { if got := HasPerm(perms, perm); got != want { t.Fatalf("HasPerm(%q, %q) = %v, want %v", perms, perm, got, want) } })`.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -run '^TestHasPerm' ./internal/capability/ -count=1 -v && go vet ./internal/capability/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^const PermFilesRead = "files.read"' internal/capability/capability.go` returns 1.
    - `grep -c '^func HasPerm' internal/capability/capability.go` returns 1.
    - `awk '/^func HasPerm/,/^}/{ print }' internal/capability/capability.go | grep -c 'strings.Split'` returns at least 1 (whole-token split present).
    - `awk '/^func HasPerm/,/^}/{ print }' internal/capability/capability.go | grep -c 'strings.Contains'` returns 0 (substring check is forbidden — Pitfall 4 mitigation).
    - The Claims struct field order is unchanged: `grep -A6 '^type Claims struct' internal/capability/capability.go | head -7` must still show SID, Perms, IAT, GrantID, V in that order.
    - `go test -run '^TestHasPerm$' ./internal/capability/ -count=1 -v` exits 0 and reports 10 subtest passes including the explicit "no-files.read false-positive guard" subtest.
    - `go vet ./internal/capability/` reports no diagnostics.
  </acceptance_criteria>
  <done>
    PermFilesRead and HasPerm exported from internal/capability with whole-token semantics; the substring false-positive guard explicitly tested; Claims struct untouched; existing capability tests still pass (no regressions to Sign/Verify path).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add requireFilesRead middleware wrapper to internal/webserver/capability_mw.go (body only — mount in Phase 119)</name>
  <files>internal/webserver/capability_mw.go, internal/webserver/capability_test.go</files>
  <read_first>
    - /Users/ken/dev/agenthub/internal/webserver/capability_mw.go (full file — the existing requireCapability is the wrapper to chain inside)
    - /Users/ken/dev/agenthub/internal/webserver/capability_test.go (existing test scaffolding patterns)
    - /Users/ken/dev/agenthub/internal/webserver/capability_test_helpers.go (test helper utilities such as token-signing scaffolding, if present)
    - /Users/ken/dev/agenthub/internal/capability/context.go (capability.FromContext signature)
    - /Users/ken/dev/agenthub/.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-RESEARCH.md (Validation Architecture FS-11/FS-13 note recommending option (a) — wrapper body in Phase 118)
    - /Users/ken/dev/agenthub/.planning/research/PITFALLS.md (Pitfall 4 — DO NOT add files.read check to shared requireCapability; use a SEPARATE wrapper)
  </read_first>
  <behavior>
    - requireFilesRead is a method on *WebServer with the same signature shape as requireCapability: `func (ws *WebServer) requireFilesRead(next http.HandlerFunc) http.HandlerFunc`.
    - The returned handler first runs ws.requireCapability(...) — meaning HMAC verification, session/grant checks all happen first.
    - After requireCapability attaches claims via WithClaims, the inner handler reads them via capability.FromContext, checks HasPerm(claims.Perms, capability.PermFilesRead), and on miss writes 403 with body containing the literal string "files.read".
    - The existing requireCapability function body is UNCHANGED — verify by diff: the line count and line content of requireCapability before and after this task are identical.
    - When the cap token has files.read in Perms: requireFilesRead delegates to next(w, r) and next runs.
    - When the cap token does not have files.read in Perms: response is 403 and body contains "files.read" (response.Body contains the substring "files.read"). next is NOT called.
    - Failure mode message includes a recognizable phrase such as "files.read capability required" so the frontend can surface a meaningful permission-denied UI in Phase 120.
  </behavior>
  <action>
    Edit `internal/webserver/capability_mw.go`:

    1. Do NOT modify the existing `requireCapability` function. Add to the bottom of the file (or immediately after requireCapability):
       ```
       // requireFilesRead wraps requireCapability and additionally enforces that
       // claims.Perms contains the files.read whole-token capability bit.
       //
       // ORDER MATTERS: requireCapability runs first (HMAC verify, session/grant
       // checks, claims attach via capability.WithClaims). Only after those succeed
       // does this wrapper extract claims via capability.FromContext and apply the
       // HasPerm check.
       //
       // On miss the response is 403 with body containing "files.read" so the
       // frontend (Phase 120) can surface a meaningful permission-denied message
       // (REQUIREMENTS.md FS-13, FS-11).
       //
       // This wrapper exists in Phase 118 but is not MOUNTED on any route until
       // Phase 119 attaches it to /api/files/list, /stat, /read via
       // SetFilesHandlerProvider. Phase 118 unit-tests it standalone using a
       // synthetic capability.WithClaims context.
       func (ws *WebServer) requireFilesRead(next http.HandlerFunc) http.HandlerFunc {
           return ws.requireCapability(func(w http.ResponseWriter, r *http.Request) {
               claims := capability.FromContext(r.Context())
               if !capability.HasPerm(claims.Perms, capability.PermFilesRead) {
                   http.Error(w, "files.read capability required", http.StatusForbidden)
                   return
               }
               next(w, r)
           })
       }
       ```
       The `capability` import is already present in the file — no new imports needed.

    2. Edit `internal/webserver/capability_test.go`. Append a `TestRequireFilesRead` test with at least these subtests:
       - **"pass-through when files.read present"**: Build a *WebServer test instance with a known signing key. Mint a token via capability.Sign with Perms = "read,write,files.read" and a valid grant ID registered with the WebServer (use the same scaffolding existing capability tests use — capability_test_helpers.go has the patterns). Issue a GET with `?cap=<token>` to a test mux that mounts a sentinel handler via `ws.requireFilesRead(sentinel)`. Expect status 200 and sentinel called.
       - **"403 when files.read missing"**: Same setup but token Perms = "read,write" (no files.read). Expect status 403 AND `strings.Contains(rec.Body.String(), "files.read")` is true. Sentinel NOT called (assert via flag).
       - **"401 path takes priority over 403"**: Same setup but token signed with WRONG key (Pitfall 8). requireCapability rejects first → 401 (or whatever existing requireCapability returns on signature failure). Confirms requireFilesRead does NOT run on auth-failed requests.
       - **"viewer token (read only) gets 403"**: Token Perms = "read". 403 + "files.read" in body.

       Use the existing test helpers in `capability_test_helpers.go` if they expose token-signing scaffolding; otherwise inline the construction following the existing TestRequireCapability_* patterns.

    3. CRITICAL: do NOT add `files.read` checking logic inside the existing `requireCapability` function body. Pitfall 4 explicitly forbids this — it would break every existing relay/terminal/plugin route. The wrapper composition is the only correct pattern.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -run '^TestRequireFilesRead' ./internal/webserver/ -count=1 -v && go vet ./internal/webserver/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^func (ws \*WebServer) requireFilesRead" internal/webserver/capability_mw.go` returns 1.
    - `grep -c "^func (ws \*WebServer) requireCapability" internal/webserver/capability_mw.go` returns 1 (unchanged — exactly one definition).
    - `awk '/^func \(ws \*WebServer\) requireFilesRead/,/^}/{ print }' internal/webserver/capability_mw.go | grep -c "ws.requireCapability("` returns at least 1 (wrapper-of-wrapper composition present).
    - `awk '/^func \(ws \*WebServer\) requireFilesRead/,/^}/{ print }' internal/webserver/capability_mw.go | grep -c "capability.HasPerm"` returns at least 1.
    - `awk '/^func \(ws \*WebServer\) requireFilesRead/,/^}/{ print }' internal/webserver/capability_mw.go | grep -c "capability.PermFilesRead"` returns at least 1.
    - `awk '/^func \(ws \*WebServer\) requireFilesRead/,/^}/{ print }' internal/webserver/capability_mw.go | grep -c '"files.read capability required"'` returns at least 1.
    - `awk '/^func \(ws \*WebServer\) requireCapability/,/^func [^(]|^func \(/{ print }' internal/webserver/capability_mw.go | grep -c "files.read"` returns 0 (the existing requireCapability is UNCHANGED — files.read does NOT appear inside its body).
    - `go test -run '^TestRequireFilesRead$' ./internal/webserver/ -count=1 -v` exits 0 with all four subtests passing.
    - The "403 when files.read missing" subtest passes the assertion `strings.Contains(rec.Body.String(), "files.read") == true` — verifies the FS-13 / ROADMAP success criterion 5 requirement that the response body contains the literal string `"files.read"`.
    - Existing webserver capability tests (`go test ./internal/webserver/ -run '^TestRequireCapability' -count=1`) still pass — no regression to the shared middleware.
  </acceptance_criteria>
  <done>
    requireFilesRead wrapper is defined, unit-tested, and ready for Phase 119 to mount on /api/files/list, /stat, /read. The existing requireCapability function body is unmodified; existing terminal/relay/plugin routes are unaffected. FS-11 (separate wrapper) and FS-13 (403 with "files.read" in body — daemon-socket surface does not use it, but the unit test proves the wrapper enforces it) acceptance evidence is in place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cap-token claims.Perms field | Free-form comma-separated string; sender controls token only with HMAC key (which is server-private). Risk is internal misinterpretation (false-positive grant). |
| requireCapability → requireFilesRead | The existing terminal-route gate must NOT change behavior; new gate is additive only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-13 | Elevation of Privilege | HasPerm substring false-positive | mitigate | Whole-token comma-split implementation (`strings.Split(perms, ",")` + exact equality). Source-inspection grep gate forbids `strings.Contains`. Explicit "no-files.read false-positive guard" subtest in TestHasPerm. Mitigates Pitfall 4 / RESEARCH.md Pitfall 3 false-positive class. |
| T-118-14 | Elevation of Privilege | adding files.read check to shared requireCapability | mitigate | requireFilesRead is a SEPARATE wrapper. Source-inspection grep gate confirms `files.read` does NOT appear inside requireCapability's body. Mitigates Pitfall 4 anti-pattern (would break all existing terminal/relay routes). |
| T-118-15 | Information Disclosure | requireFilesRead 403 message | mitigate | Response body contains the literal string "files.read" so the frontend can surface "files.read capability required" — never a generic "Forbidden" UX (PITFALLS.md UX Pitfalls table). |
| T-118-16 | Tampering | Claims field order change | mitigate | This plan does NOT modify the Claims struct. Capability HMAC verification is preserved across the v3.3→v3.4 upgrade. Verified by grep gate on field order. |
</threat_model>

<verification>
- `go build ./internal/capability/ ./internal/webserver/` exits 0.
- `go vet ./internal/capability/ ./internal/webserver/` reports no diagnostics.
- `go test -run '^TestHasPerm$' ./internal/capability/ -count=1` exits 0 with 10 subtests passing.
- `go test -run '^TestRequireFilesRead$' ./internal/webserver/ -count=1` exits 0 with at least 4 subtests passing.
- `go test ./internal/capability/ ./internal/webserver/ -count=1` exits 0 (no regressions to Sign/Verify, requireCapability, CSP, or other existing middleware tests).
</verification>

<success_criteria>
- `capability.PermFilesRead == "files.read"` (FS-10).
- `capability.HasPerm` whole-token semantics verified by 10 subtests including explicit `"no-files.read"` false-positive guard (FS-10 + Pitfall 4).
- `(*WebServer).requireFilesRead` exists as a SEPARATE wrapper composing `requireCapability` + `HasPerm`; never modifies the shared requireCapability body (FS-11).
- 403 response body from requireFilesRead contains the literal string `"files.read"` — verified by TestRequireFilesRead assertion `strings.Contains(rec.Body.String(), "files.read")` (FS-13 / ROADMAP success criterion 5).
- Claims struct field order unchanged (HMAC compatibility preserved across upgrade).
- Existing webserver and capability tests pass with zero regressions.
</success_criteria>

<output>
Create `.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-03-SUMMARY.md` when done. Plan 05 (token issuance edit) will set `Perms: "read,write,files.read"` on the owner token; Phase 119 will mount `requireFilesRead` on `/api/files/*` webserver routes.
</output>
