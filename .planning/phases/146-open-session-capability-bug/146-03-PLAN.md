---
phase: 146-open-session-capability-bug
plan: 03
type: execute
wave: 3
depends_on: ["146-00", "146-01", "146-02"]
files_modified:
  - TESTING.md
autonomous: true
requirements: [FIX-03]

must_haves:
  truths:
    - "TESTING.md Suite Manifest counts reflect the 2 new test files (Go embed test + frontend open-remote test)"
    - "A FIX-03 row exists in the Section 4 Traceability map with a repo-relative .go/.tsx path"
    - "A new M-NN manual item exists in Section 5 for the live two-Mac tailnet 'Open in browser' UAT"
    - "tests/check-traceability-paths.sh passes (path column is a bare repo-relative file path)"
  artifacts:
    - path: "TESTING.md"
      provides: "Suite Manifest + Traceability + Manual checklist updates for Phase 146"
      contains: "FIX-03"
  key_links:
    - from: "TESTING.md Section 4"
      to: "FIX-03 traceability rows"
      via: "repo-relative .go/.tsx paths, test names in Notes column"
      pattern: "FIX-03"
---

<objective>
Wave 3 (regression-convention compliance, standing rule per agenthub/CLAUDE.md): Update TESTING.md to register the new Phase 146 tests. Phase 146 added 2 new automated test files (`internal/webserver/sessions_meta_embed_test.go`, `frontend/src/components/__tests__/App.open-remote.test.tsx`), extended 2 existing files, and introduced a live-tailnet behavior that cannot be automated. The standing convention requires Suite Manifest counts, a Traceability row, a Manual checklist item, and the path-check script to pass.

Purpose: TESTING.md is the canonical regression-suite home; every test-adding phase MUST update it or the standing convention is violated.
Output: TESTING.md updated in Sections 2, 4, 5; `bash tests/check-traceability-paths.sh` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@TESTING.md
@agenthub/CLAUDE.md
@.planning/phases/146-open-session-capability-bug/146-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TESTING.md Suite Manifest, Traceability, and Manual checklist</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md (Section 2 Suite Manifest table ~L26-34 — Go unit count, vitest count, Total; Section 4 Traceability header ~L94-100 and an existing FIX/SHARE row for format; Section 5 Category B Manual items M-03..M-05 for the live-tailnet remote-peer format; Section 6 Standing Convention)
    - .planning/phases/146-open-session-capability-bug/146-VALIDATION.md (Manual-Only Verifications table — the live two-Mac UAT instructions to transcribe)
  </read_first>
  <action>
    Section 2 (Suite Manifest): increment the Go unit/integration count by 1 (new `sessions_meta_embed_test.go`) and the vitest count by 1 (new `App.open-remote.test.tsx`), and update the Total accordingly. (Extended-but-not-new files — `sessions_meta_test.go`, `remoteAdapter.test.ts`, `mint_join_codes_test.go` is also new so count that too: confirm exact +N new files at edit time by listing the Wave-0/Wave-1 additions; the new files are `sessions_meta_embed_test.go`, `mint_join_codes_test.go` (Go, +2) and `App.open-remote.test.tsx` (vitest, +1).) Adjust the Go count by +2, vitest by +1, and the Total by +3.
    Section 4 (Traceability): add FIX-03 rows. Path column must be a bare repo-relative path ending in .go/.tsx (no test names in the path column — names go in Notes). Add: `FIX-03 | internal/webserver/sessions_meta_embed_test.go | Go | TestSessionsMeta_EmbedJoinCodes / TestSessionsMeta_NilIssuer — meta embeds ro_join_code/rw_join_code, degraded when issuer nil`; `FIX-03 | internal/daemon/mint_join_codes_test.go | Go | TestMintSessionJoinCodes — mints RO+RW codes, registers grants`; `FIX-03 | frontend/src/components/__tests__/App.open-remote.test.tsx | vitest | handleOpenRemoteSession exchange-then-open, D-03/D-05/D-06 selection, expired-banner`.
    Section 5 (Manual checklist, Category B): add a new M-NN item (next sequential number) transcribing the VALIDATION.md live-tailnet UAT: on Mac A start a session + enable Share (RO and RW); on Mac B click the remote card "Open in browser"; expect the live session UI (terminal), NOT "capability required"; repeat for RO-only share (RO open) and owner re-attach (RW per D-06). Note why-not-automatable (two real Macs on one tailnet; :34115 bridge has no real peer, web-share WS blocks automated input) and Source (146-VALIDATION.md).
    Do NOT change build.yml matrix entries, so branch-protection check contexts are unaffected (no Section 3 re-apply needed).
  </action>
  <verify>
    <automated>bash tests/check-traceability-paths.sh && grep -q "FIX-03" TESTING.md && grep -q "App.open-remote.test.tsx" TESTING.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - TESTING.md Section 2 counts increased (Go +2, vitest +1, Total +3) and remain internally consistent
    - Section 4 contains 3 FIX-03 rows whose path columns are bare repo-relative `.go`/`.tsx` paths (test names only in Notes)
    - Section 5 contains a new sequential M-NN item describing the live two-Mac "Open in browser" UAT with why-not-automatable + source
    - `bash tests/check-traceability-paths.sh` exits 0
  </acceptance_criteria>
  <done>TESTING.md registers all Phase 146 test changes per the standing convention; the path-check script passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only change; introduces no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-09 | Repudiation | regression-suite drift | mitigate | TESTING.md updated per standing convention so the new FIX-03 tests are discoverable and traced; `check-traceability-paths.sh` enforces path format |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | No installs (documentation-only) — no slopcheck checkpoint |
</threat_model>

<verification>
- `bash tests/check-traceability-paths.sh` exits 0.
- TESTING.md contains FIX-03 traceability rows and a new M-NN manual item.
- Suite Manifest counts are internally consistent (group counts sum to Total).
</verification>

<success_criteria>
- Standing regression-test convention satisfied: Suite Manifest, Traceability, and Manual checklist all updated for Phase 146.
- Path-check script GREEN.
</success_criteria>

<output>
Create `.planning/phases/146-open-session-capability-bug/146-03-SUMMARY.md` when done.
</output>
