---
phase: 146-open-session-capability-bug
plan: 02
type: execute
wave: 2
depends_on: ["146-00", "146-01"]
files_modified:
  - frontend/src/wailsjs/go/main/App.d.ts
  - frontend/src/lib/remoteSession.ts
  - frontend/src/lib/remoteAdapter.ts
  - frontend/src/App.tsx
  - frontend/src/components/Hub/SessionCard.tsx
autonomous: true
requirements: [FIX-03]

must_haves:
  truths:
    - "roJoinCode/rwJoinCode flow from discovery JSON through RemoteSession → adaptRemoteSession → AdaptedRemoteSessionInfo"
    - "Clicking 'Open in browser' on a shared remote session exchanges the join code and opens baseURL/sessions/{id}?cap=TOKEN (not a bare cap-less URL → no 401)"
    - "When no join code is present, the user sees an informative 'enable sharing' message instead of a raw capability-required dead-end (D-03)"
    - "RW code is used only when the peer is the viewer's own node (D-06); otherwise RO (D-05, no silent escalation)"
    - "Exchange failure (expired/session-gone) surfaces an informative banner, not an unhandled rejection (Pitfall 4)"
    - "The local 'Open' button still calls onOpenSession, not onOpenInBrowser (D-08 — not separately broken)"
  artifacts:
    - path: "frontend/src/lib/remoteAdapter.ts"
      provides: "roJoinCode/rwJoinCode pass-through + extended AdaptedRemoteSessionInfo"
      contains: "roJoinCode"
    - path: "frontend/src/App.tsx"
      provides: "handleOpenRemoteSession exchange-then-open + isPeerSelf + D-03 banner"
      contains: "ExchangeJoinCodeAtURL"
    - path: "frontend/src/components/Hub/SessionCard.tsx"
      provides: "onOpenInBrowser(session) call + D-03 tooltip"
      contains: "onOpenInBrowser"
  key_links:
    - from: "frontend/src/App.tsx handleOpenRemoteSession"
      to: "ExchangeJoinCodeAtURL + BrowserOpenURL"
      via: "await exchange then open ?cap= URL"
      pattern: "ExchangeJoinCodeAtURL"
    - from: "frontend/src/components/Hub/SessionCard.tsx Open-in-browser item"
      to: "onOpenInBrowser(session)"
      via: "pass the session object, not the bare url"
      pattern: "onOpenInBrowser"
---

<objective>
Wave 2 (viewer-side, TypeScript): Consume the enriched discovery payload. Thread `roJoinCode`/`rwJoinCode` through the RemoteSession types and `adaptRemoteSession`, then rewrite `handleOpenRemoteSession` to exchange the appropriate join code via the existing `ExchangeJoinCodeAtURL` and open a cap-bearing URL via `BrowserOpenURL`. Handle the not-shared path (D-03) and exchange failures with informative banners instead of a raw 401 dead-end. Select RW vs RO per D-05/D-06. Cascade the `onOpenInBrowser` prop type change through HubPanel → SessionCardGrid → SessionCard.

Purpose: This is the actual #98 fix surface — the single shared handler covers both GUI and web (D-08). Makes the Wave 0 frontend tests GREEN.
Output: 5 frontend files modified; App.open-remote + remoteAdapter tests GREEN; SessionCard D-08 assertion GREEN; `pnpm tsc --noEmit` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-open-session-capability-bug/146-RESEARCH.md
@.planning/phases/146-open-session-capability-bug/146-PATTERNS.md

<interfaces>
<!-- Owner-side field-name contract locked by Wave 1. The frontend camelCase mirrors the snake_case JSON. -->

Discovery JSON (from Wave 1): each session carries `ro_join_code` / `rw_join_code` (omitempty).

From frontend/src/wailsjs/go/main/App.d.ts and frontend/src/lib/remoteSession.ts:
- interface RemoteSession { id, name, cliType, status, url: string }  // add roJoinCode?, rwJoinCode?
- remoteBaseURLFor(remote): string | undefined  // existing — builds peer base URL

From frontend/src/lib/remoteAdapter.ts:
- type AdaptedRemoteSessionInfo = SessionInfo & { url: string }  // extend with roJoinCode?/rwJoinCode?
- function adaptRemoteSession(peer, session): AdaptedRemoteSessionInfo  // url passed through at the CR-01 site

From frontend/src/App.tsx (already imported — no new imports):
- ExchangeJoinCodeAtURL(baseURL, code) → token  (from './wailsjs/go/main/App')
- BrowserOpenURL(url)  (from runtime; web mode falls back to window.open)
- remoteBaseURLFor (from './lib/remoteSession')
- setSaveBanner({ kind: 'error', text }) — banner pattern from handleModalExchange/handleBrowseFilesRemote
- tailscaleStatus state — already populated from GetTailscaleStatus() at startup; carries local node domain for isPeerSelf

Current handler to REPLACE (App.tsx L1062-1064):
  const handleOpenRemoteSession = useCallback((url: string) => { BrowserOpenURL(url) }, [])

D-08 target (SessionCard.tsx): local "Open" (L534) calls onOpenSession(id,name,cli); remote "Open in browser" (L399-419) calls onOpenInBrowser. Keep them distinct.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread roJoinCode/rwJoinCode through types + adaptRemoteSession</name>
  <files>frontend/src/wailsjs/go/main/App.d.ts, frontend/src/lib/remoteSession.ts, frontend/src/lib/remoteAdapter.ts</files>
  <read_first>
    - frontend/src/wailsjs/go/main/App.d.ts (RemoteSession interface L105-111)
    - frontend/src/lib/remoteSession.ts (RemoteSession interface L12-18 — canonical frontend type; must stay in sync with App.d.ts)
    - frontend/src/lib/remoteAdapter.ts (adaptRemoteSession L10-29; AdaptedRemoteSessionInfo type; the url pass-through at the CR-01 site L27)
    - frontend/src/lib/__tests__/remoteAdapter.test.ts (Wave 0 pass-through tests this satisfies)
  </read_first>
  <behavior>
    - adaptRemoteSession(peer, session) returns roJoinCode/rwJoinCode when present on the input session
    - adaptRemoteSession returns undefined roJoinCode when the input session has no join code (D-03 not-shared shape)
  </behavior>
  <action>
    Add `roJoinCode?: string` and `rwJoinCode?: string` to the RemoteSession interface in BOTH App.d.ts and remoteSession.ts (optional — un-upgraded peers omit them; absence = not shared yet). Extend `AdaptedRemoteSessionInfo` to `SessionInfo & { url: string; roJoinCode?: string; rwJoinCode?: string }`. In adaptRemoteSession, pass `roJoinCode: session.roJoinCode` and `rwJoinCode: session.rwJoinCode` through in the returned object literal, mirroring the existing `url: session.url` pass-through.
    Run `pnpm prettier` and `pnpm tsc --noEmit` after the change.
  </action>
  <verify>
    <automated>cd frontend && pnpm tsc --noEmit && pnpm test -- remoteAdapter --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Both RemoteSession interfaces (App.d.ts + remoteSession.ts) contain `roJoinCode?: string` and `rwJoinCode?: string`
    - AdaptedRemoteSessionInfo type includes `roJoinCode?: string; rwJoinCode?: string`
    - adaptRemoteSession returns those fields (pass-through); `pnpm test -- remoteAdapter --run` is GREEN
    - `pnpm tsc --noEmit` clean
  </acceptance_criteria>
  <done>Join codes flow from discovery through the adapter; remoteAdapter tests GREEN; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite handleOpenRemoteSession (exchange + D-03/D-05/D-06) + prop cascade</name>
  <files>frontend/src/App.tsx, frontend/src/components/Hub/SessionCard.tsx</files>
  <read_first>
    - frontend/src/App.tsx (handleOpenRemoteSession L1062-1064 to replace; analogs handleBrowseFilesRemote L1069-1103 and handleModalExchange L1111-1138 for the ExchangeJoinCodeAtURL + setSaveBanner pattern; imports L35-53 already present; tailscaleStatus state; HubPanel onOpenInBrowser wiring ~L1382)
    - frontend/src/components/Hub/SessionCard.tsx (Open-in-browser menu item L399-419, remoteUrl source L237, local Open button L534-543, SessionCardProps onOpenInBrowser type)
    - frontend/src/components/__tests__/App.open-remote.test.tsx (Wave 0 source-inspection contract this satisfies)
    - frontend/src/components/__tests__/SessionCard.share.test.tsx (D-08 onOpenSession-vs-onOpenInBrowser assertion to keep GREEN)
  </read_first>
  <behavior>
    - handleOpenRemoteSession with roJoinCode present: awaits ExchangeJoinCodeAtURL(baseURL, code) then BrowserOpenURL(baseURL + "/sessions/" + id + "?cap=" + token)
    - with no roJoinCode: setSaveBanner error "not shared / enable sharing" — no BrowserOpenURL, no 401 dead-end (D-03)
    - rwJoinCode chosen only when isPeerSelf(session.hostname, tailscaleStatus); else roJoinCode (D-05/D-06)
    - exchange throwing expired/session-gone → informative setSaveBanner; other errors → generic error banner (Pitfall 4)
    - local Open still calls onOpenSession, not onOpenInBrowser (D-08)
  </behavior>
  <action>
    Replace handleOpenRemoteSession to accept `(session: AdaptedRemoteSessionInfo): Promise<void>`. If `!session.roJoinCode` → setSaveBanner error pointing the user at the Share button (D-03) and return. Resolve baseURL via remoteBaseURLFor(session); if absent → error banner. Add an `isPeerSelf(peerHostname, tsStatus)` helper (inline or local util): compare peerHostname to the local node short hostname derived from tailscaleStatus (split domain on first dot); return false on missing data. Select `code = (session.rwJoinCode && isPeerSelf(session.hostname, tailscaleStatus)) ? session.rwJoinCode : session.roJoinCode` (D-06 prefers RW for owner re-attach; D-05 RO default — no silent escalation). Wrap `await ExchangeJoinCodeAtURL(baseURL, code)` in try/catch; on success `BrowserOpenURL(baseURL + '/sessions/' + session.id + '?cap=' + token)`; on catch map "expired"/"session-gone" substrings to an informative banner, else a generic error banner (PATTERNS Error-Handling). Add tailscaleStatus + setSaveBanner to the useCallback deps.
    Cascade the prop type: change `onOpenInBrowser` from `(url: string) => ...` to `(session: AdaptedRemoteSessionInfo) => ...` at every layer — App.tsx HubPanel wiring, HubPanel props, SessionCardGrid props, SessionCardProps in SessionCard.tsx — and change the menu-item call site (L399-419) from `onOpenInBrowser?.(remoteUrl)` to `onOpenInBrowser?.(session as AdaptedRemoteSessionInfo)`. D-03 card-level UX: when `!session.roJoinCode`, show a tooltip on the Open-in-browser item ("Session is not shared — share it from the owner's device first"). Do NOT touch the local Open button (L534) — it must keep calling onOpenSession (D-08).
    Per project rule, run `pnpm tsc --noEmit` after EACH file edit (vitest tolerates TS errors that the wails-dev `tsc && vite build` rejects). If isPeerSelf hostname formats mismatch at runtime (RESEARCH Open Question 1), fall back to roJoinCode for all remote opens (D-05 safe).
  </action>
  <verify>
    <automated>cd frontend && pnpm tsc --noEmit && pnpm test -- App.open-remote SessionCard --run 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx handleOpenRemoteSession signature is `(session: AdaptedRemoteSessionInfo)`, contains `ExchangeJoinCodeAtURL`, `isPeerSelf`, a `?cap=` URL build, and a setSaveBanner branch for `!session.roJoinCode` (D-03) and for `expired`/`session-gone`
    - `onOpenInBrowser` prop type is `(session: AdaptedRemoteSessionInfo)` across App.tsx, HubPanel, SessionCardGrid, SessionCard.tsx; the menu item passes the session object
    - The local Open button still calls `onOpenSession` (D-08) — SessionCard test GREEN
    - `pnpm test -- App.open-remote --run` GREEN; `pnpm test -- SessionCard --run` GREEN; `pnpm tsc --noEmit` clean
  </acceptance_criteria>
  <done>Open-in-browser exchanges a code and opens a cap-bearing URL on both surfaces; not-shared and exchange-failure paths show banners; D-05/D-06 selection correct; D-08 local Open untouched; all Wave 0 frontend tests GREEN; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer browser → owner webserver (`?cap=` URL) | cap token embedded in the opened URL; verified by requireCapability on first request |
| viewer daemon → owner `/join/exchange` (tailnet) | ExchangeJoinCodeAtURL validates the absolute Location host matches the request host (WR-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-05 | Elevation of privilege | RO→RW escalation | mitigate | `rwJoinCode` used ONLY when `isPeerSelf` confirms the peer is the viewer's own node (D-06); all other viewers get `roJoinCode` (D-05). On hostname-format ambiguity the code falls back to RO (safe default) |
| T-146-06 | Tampering | forged Location header in exchange | mitigate | Reuses existing `ExchangeJoinCodeAtURL` which validates absolute Location host == request host (WR-04, client_remote_files.go:155-158) — no new exchange path |
| T-146-07 | Spoofing (replay) | stale/expired join code | mitigate | Join code is single-use + 5-min TTL (owner side); on exchange failure the handler surfaces an "expired — click again" banner that triggers a fresh poll → fresh code (Pitfall 4) |
| T-146-08 | Denial of service / dead-end | not-shared / exchange error | mitigate | D-03: `!roJoinCode` and exchange failures route to informative banners, never to the raw 401 capability-required dead-end |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (RESEARCH §Package Legitimacy Audit) — no install task, no slopcheck checkpoint |
</threat_model>

<verification>
- `cd frontend && pnpm tsc --noEmit` clean (wails-dev build gate, not just vitest).
- `cd frontend && pnpm test -- App.open-remote remoteAdapter SessionCard --run` GREEN.
- No raw cap token logged or persisted; the cap lives only in the opened URL.
</verification>

<success_criteria>
- Open-in-browser on a shared remote session opens the live session (cap-bearing URL), not a 401.
- Not-shared and exchange-failure paths show informative banners (D-03), no dead-end.
- RW only for owner re-attach (D-05/D-06, no silent escalation).
- D-08 local Open path untouched and still asserted.
- GUI + web both covered by the single shared handler.
</success_criteria>

<output>
Create `.planning/phases/146-open-session-capability-bug/146-02-SUMMARY.md` when done.
</output>
