---
phase: 146-open-session-capability-bug
plan: 02
type: execute
wave: 1
depends_on: ["146-01"]
files_modified:
  - internal/webserver/server.go
  - internal/daemon/api.go
  - internal/tailnet/sessions.go
  - internal/webserver/sessions_meta_test.go
  - internal/daemon/mint_join_codes_test.go
autonomous: true
requirements: [FIX-03]
must_haves:
  truths:
    - "GET /api/sessions/meta returns no credential material (RB-03 restored)"
    - "No mintSessionJoinCodes / SetJoinCodeIssuer / joinCodeIssuer symbols remain in the Go codebase"
    - "The inverted RB-03 test (Plan 01) is GREEN"
  artifacts:
    - path: "internal/webserver/server.go"
      provides: "Cap-free sessionMetaItem + handleSessionsMeta (no join-code embed)"
    - path: "internal/daemon/api.go"
      provides: "Cap issuance via issueCapabilitiesForSession only; no broadcast mint helper"
    - path: "internal/tailnet/sessions.go"
      provides: "Cap-free ShareableSessionMeta"
  key_links:
    - from: "internal/webserver/server.go handleSessionsMeta"
      to: "sessionMetaItem JSON"
      via: "no ROJoinCode/RWJoinCode assignment"
      pattern: "ro_join_code"
---

<objective>
Remove the superseded broadcast on the Go side: delete the join-code issuer wiring and the
join-code fields from the discovery payload, restoring RB-03 (cap-free /api/sessions/meta).
Reconcile the Go tests so the inverted RB-03 test from Plan 01 goes GREEN and the broadcast-only
test (mint_join_codes_test.go) is deleted.

Purpose: The broadcast placed RW/RO codes on an unauthenticated payload every tailnet peer could
poll. D-10 mandates full removal; RB-03 must be restored as the discovery invariant.
Output: Go backend with no broadcast wiring; GET /api/sessions/meta carries id/name/cli_type/status/url only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-open-session-capability-bug/146-CONTEXT.md
@.planning/phases/146-open-session-capability-bug/146-RESEARCH.md
@.planning/phases/146-open-session-capability-bug/146-PATTERNS.md

<interfaces>
<!-- Exact symbols/lines to remove (from grep + PATTERNS.md). Surgical deletes only. -->

internal/webserver/server.go:
  - line ~49-50: comment block for ROJoinCode/RWJoinCode
  - line 59-60: ROJoinCode/RWJoinCode fields on sessionMetaItem
  - line ~109-113: joinCodeIssuer field + its comment
  - line ~170-178: SetJoinCodeIssuer setter (delete whole method)
  - line ~879-890: handleSessionsMeta join-code embed block (the `if ws.joinCodeIssuer != nil { ... }`)
  Restore handleSessionsMeta to build sessionMetaItem with ID/Name/CLIType/Status/URL only.

internal/daemon/api.go:
  - line 457-458: comment + ws.SetJoinCodeIssuer(a.mintSessionJoinCodes)
  - line 994-995: comment + ws.SetJoinCodeIssuer(a.mintSessionJoinCodes) (second wiring site)
  - line 1183-~1255: mintSessionJoinCodes method (delete entire function). issueCapabilitiesForSession (lines ~1098-1181) stays untouched.

internal/tailnet/sessions.go:
  - line ~117: comment block for ROJoinCode/RWJoinCode
  - line 128-129: ROJoinCode/RWJoinCode fields on ShareableSessionMeta

internal/webserver/sessions_meta_test.go:
  - line ~188-191: revert "Phase 146: ... intentionally allowed" comment
  - line 198-199: remove "ro_join_code": true and "rw_join_code": true from the allowed map

internal/daemon/mint_join_codes_test.go: DELETE entire file (tests the removed mintSessionJoinCodes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove broadcast wiring + restore cap-free discovery payload</name>
  <files>internal/webserver/server.go, internal/daemon/api.go, internal/tailnet/sessions.go</files>
  <read_first>
    - internal/webserver/server.go (sessionMetaItem ~L53-61, joinCodeIssuer field ~L109-113, SetJoinCodeIssuer ~L170-178, handleSessionsMeta ~L860-895)
    - internal/daemon/api.go (SetJoinCodeIssuer wiring L457-458 and L994-995, mintSessionJoinCodes L1183+, issueCapabilitiesForSession L1098-1181 — DO NOT touch)
    - internal/tailnet/sessions.go (ShareableSessionMeta struct ~L117-130, PeerSession analog ~L19-27)
    - .planning/phases/146-open-session-capability-bug/146-PATTERNS.md (server.go / api.go / tailnet/sessions.go restore sections with exact handler/struct shapes)
  </read_first>
  <action>
    In server.go: delete the ROJoinCode/RWJoinCode fields and their comment from sessionMetaItem;
    delete the joinCodeIssuer struct field and its comment; delete the entire SetJoinCodeIssuer method;
    delete the `if ws.joinCodeIssuer != nil { roCode, rwCode, err := ws.joinCodeIssuer(id) ... }` embed
    block in handleSessionsMeta so each item is built with ID/Name/CLIType/Status/URL only; restore the
    handler comment to state cap-free discovery (RB-03).
    In api.go: delete both `ws.SetJoinCodeIssuer(a.mintSessionJoinCodes)` wiring calls (L458, L995) and
    their Mechanism-B comments; delete the entire mintSessionJoinCodes method. Leave
    issueCapabilitiesForSession and IssueCapabilities completely untouched (owner-side minting stays —
    that is what the Share modal and the out-of-band code/link rely on).
    In tailnet/sessions.go: delete the ROJoinCode/RWJoinCode fields and comment from ShareableSessionMeta,
    restoring it to the cap-free metadata shape (model it on PeerSession — pure metadata, no credentials).
    Per D-10: app.go is NOT wired to carry codes — confirm no app.go change is needed (there are no codes).
  </action>
  <verify>
    <automated>go build ./... && grep -rn "SetJoinCodeIssuer\|mintSessionJoinCodes\|joinCodeIssuer\|ROJoinCode\|RWJoinCode" internal/ --include=*.go | grep -v '_test.go' | grep -v superseded | grep -vc '^#' ; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` succeeds (no dangling references to removed symbols)
    - Source absence: `grep -rn "SetJoinCodeIssuer\|mintSessionJoinCodes\|joinCodeIssuer\|ROJoinCode\|RWJoinCode" internal/ --include=*.go | grep -v '_test.go' | grep -v superseded` returns NO matches in non-test production files
    - issueCapabilitiesForSession and IssueCapabilities still exist (owner minting preserved): `grep -c "func.*issueCapabilitiesForSession" internal/daemon/api.go` == 1
    - No app.go modification (D-10): app.go unchanged in this plan's diff
  </acceptance_criteria>
  <done>All broadcast wiring removed from production Go; build passes; owner-side cap minting intact; discovery payload is cap-free.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile Go tests — green RB-03, delete broadcast-only test</name>
  <files>internal/webserver/sessions_meta_test.go, internal/daemon/mint_join_codes_test.go</files>
  <read_first>
    - internal/webserver/sessions_meta_test.go (allowed-key map ~L188-205 in TestSessionsMeta_NoCapInResponse)
    - internal/daemon/mint_join_codes_test.go (broadcast-only test to delete)
    - internal/webserver/sessions_meta_embed_test.go (Plan 01's inverted test — must be GREEN after this plan)
    - .planning/phases/146-open-session-capability-bug/146-PATTERNS.md (sessions_meta_test.go restore section + mint_join_codes_test.go removal note)
  </read_first>
  <action>
    In sessions_meta_test.go: revert the allowed-key map in TestSessionsMeta_NoCapInResponse to the
    cap-free key set (id, name, cli_type, status, url) — remove the "ro_join_code": true and
    "rw_join_code": true entries and revert the "Phase 146: ... intentionally allowed" comment so the
    test again enforces that no credential keys appear.
    Delete internal/daemon/mint_join_codes_test.go entirely — it exercises the removed mintSessionJoinCodes;
    cap issuance is already covered by api_test.go TestIssueCapabilities_* (no replacement needed).
  </action>
  <verify>
    <automated>rm -f internal/daemon/mint_join_codes_test.go; go test ./internal/webserver/ ./internal/daemon/ ./internal/tailnet/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - mint_join_codes_test.go no longer exists: `test ! -f internal/daemon/mint_join_codes_test.go`
    - Behavior assertion (Plan 01 contract now GREEN): `go test ./internal/webserver/ -run TestSessionsMeta_NoJoinCodesInResponse` PASSES
    - `go test ./internal/webserver/ ./internal/daemon/ ./internal/tailnet/` all PASS (no ro_join_code/rw_join_code allow-list entries remain; `grep -c 'ro_join_code' internal/webserver/sessions_meta_test.go` == 0)
  </acceptance_criteria>
  <done>RB-03 test enforces cap-free discovery and is GREEN; broadcast-only test deleted; Go package tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tailnet peer → /api/sessions/meta | Untrusted poll; payload must carry NO credentials after this plan (RB-03) |
| owner daemon → IssueCapabilities | Owner-only cap minting; preserved (the legitimate out-of-band source) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-01 | Information Disclosure | /api/sessions/meta + ShareableSessionMeta | mitigate | Delete ROJoinCode/RWJoinCode fields + joinCodeIssuer embed; RB-03 test (Plan 01) verifies absence — no credential material crosses the discovery boundary |
| T-146-03 | Elevation of Privilege | RW code broadcast to all peers | mitigate | Remove mintSessionJoinCodes + both SetJoinCodeIssuer wirings; RW access now requires an owner-delivered out-of-band code, not a tailnet-wide broadcast |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | No package installs; deletions and existing go test toolchain only |
</threat_model>

<verification>
- `go build ./...` passes.
- `go test ./internal/webserver/ ./internal/daemon/ ./internal/tailnet/` passes, including the inverted RB-03 test (now GREEN).
- No broadcast symbol remains in non-test production Go.
- app.go untouched (D-10).
</verification>

<success_criteria>
Broadcast removed on the Go side; GET /api/sessions/meta is cap-free (RB-03 restored and test-verified);
owner-side cap minting (issueCapabilitiesForSession/IssueCapabilities) intact for the out-of-band flow.
</success_criteria>

<output>
Create `.planning/phases/146-open-session-capability-bug/146-02-SUMMARY.md` when done
</output>
