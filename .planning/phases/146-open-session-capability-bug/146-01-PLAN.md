---
phase: 146-open-session-capability-bug
plan: 01
type: execute
wave: 1
depends_on: ["146-00"]
files_modified:
  - internal/webserver/server.go
  - internal/daemon/api.go
  - internal/tailnet/sessions.go
autonomous: true
requirements: [FIX-03]

must_haves:
  truths:
    - "/api/sessions/meta returns ro_join_code and rw_join_code for each web-enabled (shared) session"
    - "The codes are fresh single-use join codes minted per request, with both grants registered before issuance (cap is valid immediately)"
    - "When no issuer is wired, /api/sessions/meta still responds (degraded mode, empty codes) — no 500, no panic"
    - "No raw cap token appears in /api/sessions/meta (RB-03 preserved)"
  artifacts:
    - path: "internal/webserver/server.go"
      provides: "joinCodeIssuer field + SetJoinCodeIssuer + sessionMetaItem fields + handleSessionsMeta enrichment"
      contains: "SetJoinCodeIssuer"
    - path: "internal/daemon/api.go"
      provides: "mintSessionJoinCodes helper + SetJoinCodeIssuer wiring at startup"
      contains: "func (a *API) mintSessionJoinCodes("
    - path: "internal/tailnet/sessions.go"
      provides: "ShareableSessionMeta ro_join_code/rw_join_code fields"
      contains: "ro_join_code"
  key_links:
    - from: "internal/webserver/handleSessionsMeta"
      to: "ws.joinCodeIssuer"
      via: "per-session call, nil-guarded"
      pattern: "joinCodeIssuer"
    - from: "internal/daemon/api.go startup wiring"
      to: "ws.SetJoinCodeIssuer(a.mintSessionJoinCodes)"
      via: "callback wiring alongside SetJoinCodes"
      pattern: "SetJoinCodeIssuer"
    - from: "internal/daemon/mintSessionJoinCodes"
      to: "ws.AddGrant"
      via: "grant registration before code issuance"
      pattern: "AddGrant"
---

<objective>
Wave 1 (owner-side, Go): Enrich `/api/sessions/meta` so each web-enabled session carries a fresh per-session `ro_join_code` / `rw_join_code`. Wire a `joinCodeIssuer` callback into `WebServer` (same pattern as `sessionResolver`/`SetJoinCodes`), backed by a new `mintSessionJoinCodes` helper extracted from `issueCapabilitiesForSession`. This is Mechanism B from RESEARCH.md — no new endpoint, no new trust surface; the Share toggle (`webEnabledSessions()`) remains the gate (D-01).

Purpose: Deliver the cap to the viewer over the already-trusted discovery channel so the viewer can exchange a code and open a cap-bearing URL. Makes the Wave 0 Go tests GREEN.
Output: Modified server.go, api.go, sessions.go; TestSessionsMeta_EmbedJoinCodes, TestSessionsMeta_NilIssuer, TestMintSessionJoinCodes, TestSessionsMeta_NoCapInResponse all pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-open-session-capability-bug/146-RESEARCH.md
@.planning/phases/146-open-session-capability-bug/146-PATTERNS.md

<interfaces>
<!-- Extracted from codebase. Use these directly — no exploration needed. -->

From internal/webserver/server.go:
- type sessionMetaItem struct { ID,Name,CLIType,Status,URL string `json:...` }  (L42-54)
- func (ws *WebServer) SetSessionResolver(...)  — setter pattern, set once before Start()
- func (ws *WebServer) SetJoinCodes(jc *capability.JoinCodeManager) — mutex-guarded swap (L274-282)
- sessionResolver func(sessionID string) (name, cliType, status, hostname string)  — field decl pattern (L99-100)
- func (ws *WebServer) handleSessionsMeta(w,r) — iterates webEnabledSessions(), builds items (L831-859)
- func (ws *WebServer) AddGrant(sessionID, grantID string) — registers grant for isGrantActive

From internal/daemon/api.go:
- func (a *API) issueCapabilitiesForSession(sessionID string) (readURL, writeURL, readCode, writeCode string, err error)  (L1083-1175)
  // mints RO+RW HMAC tokens via capability.Sign, calls ws.AddGrant(sessionID, grantID) for BOTH, then a.joinCodes.Issue(token) per token
- a.joinCodes *capability.JoinCodeManager

From internal/tailnet/sessions.go:
- type ShareableSessionMeta struct { ID,Name,CLIType,Status,URL string `json:...` }  (L117-123)
  // decoded by doFetchSessionsMeta via stdlib json.Decode — new fields flow automatically
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract mintSessionJoinCodes helper + wire SetJoinCodeIssuer (daemon)</name>
  <files>internal/daemon/api.go</files>
  <read_first>
    - internal/daemon/api.go (issueCapabilitiesForSession L1083-1175 — copy the token-mint + AddGrant + joinCodes.Issue steps verbatim, drop the URL-build step; locate the existing SetJoinCodes wiring site at daemon startup)
    - internal/daemon/mint_join_codes_test.go (Wave 0 — the contract this implements)
    - internal/webserver/server.go (SetJoinCodes wiring target shape for the parallel SetJoinCodeIssuer call)
  </read_first>
  <behavior>
    - mintSessionJoinCodes("sess") returns two non-empty, distinct codes and a nil error for a web-enabled session
    - both grants are registered on the WebServer before return (a token exchanged from either code passes isGrantActive in requireCapability)
    - returns an error (not a panic) when signing key / web server / join-code manager is not bootstrapped
  </behavior>
  <action>
    Add `func (a *API) mintSessionJoinCodes(sessionID string) (roCode, rwCode string, err error)` to api.go. Body = the token-mint + grant-register + code-issue portion of issueCapabilitiesForSession (signing-key RLock, webServer/joinCodes nil-guards, generate rgid/wgid grant IDs, build rClaims/wClaims with the existing RO `"read"` and RW `"read,write"` perms INCLUDING the browse matrix exactly as issueCapabilitiesForSession does, capability.Sign both, call ws.AddGrant(sessionID, GrantID) for BOTH before issuing, then a.joinCodes.Issue(rTok) and a.joinCodes.Issue(wTok)). Omit the readURL/writeURL construction (the viewer builds the URL). Return (readCode, writeCode, nil).
    At the existing daemon startup site where `ws.SetJoinCodes(a.joinCodes)` is called, add `ws.SetJoinCodeIssuer(a.mintSessionJoinCodes)` immediately after (per PATTERNS.md wiring pattern). Run go fmt.
    Do NOT cache codes across calls (RESEARCH Anti-Pattern: single-use codes must be minted fresh per call). Do NOT skip either AddGrant (Pitfall 3).
  </action>
  <verify>
    <automated>go test -race -short ./internal/daemon/... -run TestMintSessionJoinCodes 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - api.go contains `func (a *API) mintSessionJoinCodes(sessionID string) (roCode, rwCode string, err error)`
    - The helper calls `ws.AddGrant(` exactly twice (RO + RW grant) before any `joinCodes.Issue`
    - Daemon startup contains `ws.SetJoinCodeIssuer(a.mintSessionJoinCodes)` adjacent to the existing `SetJoinCodes` wiring
    - `go test -race -short ./internal/daemon/... -run TestMintSessionJoinCodes` passes
    - `go fmt ./internal/daemon/...` produces no diff; `golangci-lint run ./internal/daemon/...` clean for the changed file
  </acceptance_criteria>
  <done>mintSessionJoinCodes exists, registers both grants before issuance, is wired as the issuer callback; TestMintSessionJoinCodes is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enrich sessionMetaItem + handleSessionsMeta; extend ShareableSessionMeta</name>
  <files>internal/webserver/server.go, internal/tailnet/sessions.go</files>
  <read_first>
    - internal/webserver/server.go (sessionMetaItem L42-54, sessionResolver field decl L99-100, SetSessionResolver/SetJoinCodes setters, handleSessionsMeta L831-859, webEnabledSessions gate)
    - internal/webserver/sessions_meta_embed_test.go (Wave 0 — TestSessionsMeta_EmbedJoinCodes + TestSessionsMeta_NilIssuer contract)
    - internal/webserver/sessions_meta_test.go (TestSessionsMeta_NoCapInResponse — must stay GREEN)
    - internal/tailnet/sessions.go (ShareableSessionMeta L117-123)
  </read_first>
  <behavior>
    - GET /api/sessions/meta with a wired issuer returns each item with populated ro_join_code/rw_join_code matching the issuer output
    - GET /api/sessions/meta with no issuer wired returns items with empty/omitted codes and HTTP 200 (no panic, no 500)
    - response JSON keys remain within the RB-03 allowed set (no cap/token/grant)
  </behavior>
  <action>
    In server.go: add `joinCodeIssuer func(sessionID string) (roCode, rwCode string, err error)` field to WebServer (follow the sessionResolver field-decl comment pattern). Add `func (ws *WebServer) SetJoinCodeIssuer(fn func(string) (string, string, error)) { ws.joinCodeIssuer = fn }` (set-once-before-Start, follow SetSessionResolver). Add `ROJoinCode string \`json:"ro_join_code,omitempty"\`` and `RWJoinCode string \`json:"rw_join_code,omitempty"\`` to sessionMetaItem. In handleSessionsMeta, after building sessionURL, if `ws.joinCodeIssuer != nil` call it per id; on success populate ROJoinCode/RWJoinCode; on error, leave them empty and continue (degraded mode per PATTERNS Error-Handling — do NOT return 500). Keep webEnabledSessions() as the only gate (D-01).
    In sessions.go: add `ROJoinCode string \`json:"ro_join_code,omitempty"\`` and `RWJoinCode string \`json:"rw_join_code,omitempty"\`` to ShareableSessionMeta (stdlib json.Decode maps them automatically — no decoder change). Run go fmt.
  </action>
  <verify>
    <automated>go test -race -short ./internal/webserver/... -run 'TestSessionsMeta_EmbedJoinCodes|TestSessionsMeta_NilIssuer|TestSessionsMeta_NoCapInResponse' && go test -race -short ./internal/tailnet/... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - server.go contains `func (ws *WebServer) SetJoinCodeIssuer(` and a nil-guarded `ws.joinCodeIssuer(id)` call inside handleSessionsMeta
    - sessionMetaItem and ShareableSessionMeta both contain `ro_join_code` and `rw_join_code` json tags
    - `go test -race -short ./internal/webserver/... -run 'TestSessionsMeta_EmbedJoinCodes|TestSessionsMeta_NilIssuer|TestSessionsMeta_NoCapInResponse'` all pass
    - `go test -race -short ./internal/tailnet/...` passes
    - `go fmt` no diff; golangci-lint clean for changed files
  </acceptance_criteria>
  <done>Meta response carries fresh per-session codes when an issuer is wired; degraded gracefully when not; RB-03 test stays GREEN; ShareableSessionMeta decodes the new fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer peer → owner `/api/sessions/meta` (tailnet) | network-layer-trusted discovery; carries join codes (not cap tokens) for shared sessions only |
| browser → owner webserver (`?cap=` request) | cap token verified by requireCapability on every request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-01 | Information disclosure | `sessionMetaItem` JSON / RB-03 | mitigate | Only `ro_join_code`/`rw_join_code` added (single-use pointers, not cap tokens); TestSessionsMeta_NoCapInResponse keeps the cap/token/grant blacklist — RB-03 preserved |
| T-146-02 | Spoofing (replay) | minted join codes | mitigate | `mintSessionJoinCodes` issues via `capability.JoinCodeManager` (single-use, 5-min TTL); fresh codes per meta request, never cached/shared across polls |
| T-146-03 | Information disclosure | unshared-session leak | mitigate | `webEnabledSessions()` is the sole gate; unshared sessions never appear in meta and emit no codes (D-01 Share toggle) |
| T-146-04 | Elevation of privilege | grant validity | mitigate | `ws.AddGrant` called for BOTH grants before code issuance so `isGrantActive` accepts the token; no orphan/over-broad grant created (RO perms = `read`, RW perms = `read,write` exactly as issueCapabilitiesForSession) |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (RESEARCH §Package Legitimacy Audit) — no install task, no slopcheck checkpoint |
</threat_model>

<verification>
- `go test -race -short ./internal/webserver/... ./internal/daemon/... ./internal/tailnet/...` GREEN.
- RB-03 guard (TestSessionsMeta_NoCapInResponse) GREEN — no raw cap token in meta.
- `go fmt ./...` no diff; `golangci-lint run` clean on changed files.
</verification>

<success_criteria>
- `/api/sessions/meta` carries fresh single-use ro_join_code/rw_join_code per web-enabled session.
- Both grants registered before code issuance; codes never cached across polls.
- Degraded mode (no issuer) returns 200 with empty codes.
- RB-03 preserved; no high-severity threat unmitigated.
</success_criteria>

<output>
Create `.planning/phases/146-open-session-capability-bug/146-01-SUMMARY.md` when done.
</output>
