---
phase: 146-open-session-capability-bug
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - internal/webserver/sessions_meta_embed_test.go
  - frontend/src/components/__tests__/App.open-remote.test.tsx
autonomous: true
requirements: [FIX-03]
must_haves:
  truths:
    - "A RED Go test asserts /api/sessions/meta contains NO ro_join_code/rw_join_code (RB-03 restored)"
    - "A RED frontend test asserts the remote Open button opens the join-code modal and the open-session exchange builds a /sessions/{id}?cap= URL"
    - "At least one assertion crosses the actual open path (behavior), not pure source-inspection"
  artifacts:
    - path: "internal/webserver/sessions_meta_embed_test.go"
      provides: "Inverted RB-03 cap-free assertion (was broadcast-embed assertion)"
      contains: "func TestSessionsMeta_NoJoinCodesInResponse"
    - path: "frontend/src/components/__tests__/App.open-remote.test.tsx"
      provides: "Out-of-band open behavior + source contract for handleOpenRemoteSession/handleModalExchange"
  key_links:
    - from: "internal/webserver/sessions_meta_embed_test.go"
      to: "GET /api/sessions/meta"
      via: "client.Get + JSON key-set absence assertion"
      pattern: "ro_join_code"
    - from: "frontend/src/components/__tests__/App.open-remote.test.tsx"
      to: "RemoteJoinCodeModal open-session intent + BrowserOpenURL ?cap= URL"
      via: "render + behavior assertion"
      pattern: "open-session"
---

<objective>
Write the failing test contract (RED) for the out-of-band open redesign of FIX-03 (#98).
This Wave-0 plan inverts the broadcast test contract to the restored RB-03 (cap-free
discovery) contract on the Go side, and replaces the broadcast-era frontend source tests
with an out-of-band open contract that includes a behavior-level assertion crossing the
actual open path — the blind spot that let the prior dead-on-arrival feature ship green.

Purpose: A green suite must NOT be able to certify a non-working open feature. The prior
suite validated each layer in isolation; this plan adds a behavior assertion that the
remote Open button actually drives the modal/exchange/BrowserOpenURL path.
Output: Two test files in RED state (assert behavior that does not exist until Waves 1-2),
ready for Plan 02 (Go) and Plan 03 (frontend) to turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-open-session-capability-bug/146-CONTEXT.md
@.planning/phases/146-open-session-capability-bug/146-RESEARCH.md
@.planning/phases/146-open-session-capability-bug/146-PATTERNS.md
@.planning/phases/146-open-session-capability-bug/146-VALIDATION.md

<interfaces>
<!-- Existing test patterns the executor reuses — no codebase exploration needed. -->

Go test helper (internal/webserver/server_test.go + sessions_meta_test.go):
  ws, client := testServer(t)
  ws.SetSigningKey(ssExtTestKey)
  ws.EnableSession("sess-id")
  resp, err := client.Get(ws.BaseURL() + "/api/sessions/meta")
  // decode into []map[string]any, then assert key absence on items[0]

Frontend source-inspection pattern (App.open-remote.test.tsx, App.fileBrowserMode.test.tsx):
  import raw from '../../App.tsx?raw'
  const slice = raw.slice(raw.indexOf('handleModalExchange'), raw.indexOf('handleModalExchange') + 1000)
  expect(slice).toContain('open-session')

Behavior render pattern (SessionCard.share.test.tsx):
  const { container } = renderCard({ session, isRemote: true, onOpenInBrowser })
  // click .hub-card__menu-btn then the "Open in browser" .hub-card__menu-item
  // assert onOpenInBrowser called with the session object
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invert the Go RB-03 contract test (cap-free /api/sessions/meta)</name>
  <files>internal/webserver/sessions_meta_embed_test.go</files>
  <read_first>
    - internal/webserver/sessions_meta_embed_test.go (current broadcast-embed assertions to invert)
    - internal/webserver/sessions_meta_test.go (TestSessionsMeta_NoCapInResponse decode + key-set pattern, lines ~150-221)
    - .planning/phases/146-open-session-capability-bug/146-PATTERNS.md (sessions_meta_embed_test.go section + Go JSON key-set assertion shared pattern)
  </read_first>
  <behavior>
    - GET /api/sessions/meta returns items that do NOT contain ro_join_code
    - GET /api/sessions/meta returns items that do NOT contain rw_join_code
    - The response still contains the cap-free fields (id, name, cli_type, status, url)
  </behavior>
  <action>
    Replace the file's broadcast-era tests (TestSessionsMeta_EmbedJoinCodes, TestSessionsMeta_NilIssuer
    — both assert/exercise SetJoinCodeIssuer and ro_join_code/rw_join_code presence) with a single
    inverted test TestSessionsMeta_NoJoinCodesInResponse using the testServer/SetSigningKey/EnableSession
    helper. Decode the response into []map[string]any, take items[0], and assert that the keys
    "ro_join_code" and "rw_join_code" are ABSENT (per RB-03 cap-free discovery), while asserting the
    cap-free keys are present. Do NOT reference SetJoinCodeIssuer or any joinCodeIssuer wiring — those
    symbols are being deleted in Plan 02 and must not appear here. This test is RED now only because the
    production code still embeds the codes; it goes GREEN when Plan 02 removes the broadcast.
    Per D-10/RB-03: discovery payload carries no credential material.
  </action>
  <verify>
    <automated>go test ./internal/webserver/ -run TestSessionsMeta_NoJoinCodesInResponse 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File contains func TestSessionsMeta_NoJoinCodesInResponse and no longer references SetJoinCodeIssuer or TestSessionsMeta_EmbedJoinCodes (source assertion: `grep -L 'SetJoinCodeIssuer' internal/webserver/sessions_meta_embed_test.go` returns the file)
    - Behavior assertion: the test asserts absence of both ro_join_code and rw_join_code keys via map index `_, ok := item["ro_join_code"]`
    - RED state now: `go test ./internal/webserver/ -run TestSessionsMeta_NoJoinCodesInResponse` FAILS (production still embeds codes) — this is the expected RED
  </acceptance_criteria>
  <done>Inverted RB-03 test exists, references no broadcast symbols, and is RED against current production code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite the frontend open-remote contract — source + behavior (open path)</name>
  <files>frontend/src/components/__tests__/App.open-remote.test.tsx</files>
  <read_first>
    - frontend/src/components/__tests__/App.open-remote.test.tsx (current broadcast-era source tests for isPeerSelf/rwJoinCode to replace)
    - frontend/src/App.tsx (handleOpenRemoteSession lines ~1082-1111, handleModalExchange lines ~1158-1186, joinModalForSession state lines ~211-216)
    - frontend/src/components/__tests__/SessionCard.share.test.tsx (render + menu-click behavior pattern for "Open in browser")
    - .planning/phases/146-open-session-capability-bug/146-PATTERNS.md (App.tsx section + App.open-remote.test.tsx rewrite section)
  </read_first>
  <behavior>
    - Source: handleOpenRemoteSession opens the join-code modal (setJoinModalForSession) for a remote session with no held cap — it does NOT call ExchangeJoinCodeAtURL directly
    - Source: handleModalExchange has an 'open-session' branch that builds a /sessions/{id}?cap= URL and calls BrowserOpenURL
    - Source: isPeerSelf / rwJoinCode / roJoinCode selection logic is GONE (dead code removed per WR-01/D-06)
    - Behavior: rendering the remote SessionCard and clicking "Open in browser" invokes the open handler with the session object (cross-path, not raw URL) — exercises the actual open entry point, not isolated source text
  </behavior>
  <action>
    Replace the broadcast-era assertions (which expect rwJoinCode, isPeerSelf, and a roJoinCode-gated
    setSaveBanner) with the out-of-band contract. Keep the App.tsx?raw source-inspection idiom for the
    handler-structure checks: assert handleOpenRemoteSession slice contains setJoinModalForSession and
    does NOT contain ExchangeJoinCodeAtURL; assert handleModalExchange slice contains 'open-session' and
    matches /\/sessions\/.*\?cap=/ and BrowserOpenURL; assert the raw file no longer contains 'isPeerSelf'
    or 'rwJoinCode'. THEN add at least one behavior-level test that renders the remote SessionCard
    (reuse the SessionCard.share.test.tsx render+menu-click idiom) and asserts the "Open in browser"
    menu item is present and clicking it calls onOpenInBrowser with the session object — this is the
    cross-path assertion the prior suite lacked. This file is RED until Plan 03 rewrites App.tsx and
    SessionCard.tsx. Do not assert any broadcast field. Per D-02/D-03/D-11.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- src/components/__tests__/App.open-remote.test.tsx --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file references 'open-session', 'setJoinModalForSession', 'BrowserOpenURL'; file does NOT reference 'isPeerSelf' or 'rwJoinCode' (grep both directions)
    - Behavior assertion present: at least one `it(...)` renders a remote SessionCard and asserts clicking "Open in browser" calls the open handler with the session object (not a bare URL string)
    - RED state now: `pnpm test -- src/components/__tests__/App.open-remote.test.tsx --run` FAILS (App.tsx still has isPeerSelf/rwJoinCode; no open-session branch) — expected RED
  </acceptance_criteria>
  <done>Frontend open contract test exists with a behavior-level open-path assertion, references no broadcast symbols, and is RED against current production code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tailnet peer → /api/sessions/meta | Any peer can poll discovery; payload must be untrusted-readable and carry NO credentials (RB-03) |
| viewer → /join/exchange | Possession of an out-of-band single-use code is the deliberate grant; no identity check exists (accepted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-01 | Information Disclosure | /api/sessions/meta payload | mitigate | This plan's RED Go test asserts ro_join_code/rw_join_code are ABSENT — locks RB-03 cap-free discovery so removal in Plan 02 is verifiable, not assumed |
| T-146-02 | Spoofing | open-path validation | mitigate | This plan's behavior test exercises the real open entry point (modal/exchange/BrowserOpenURL) so a green suite cannot certify a dead feature again (the prior blind spot) |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; existing test toolchain only (go test, vitest) |
</threat_model>

<verification>
- `go test ./internal/webserver/ -run TestSessionsMeta_NoJoinCodesInResponse` runs and FAILS (RED) against current code.
- `cd frontend && pnpm test -- src/components/__tests__/App.open-remote.test.tsx --run` runs and FAILS (RED) against current code.
- Neither test references any broadcast symbol (SetJoinCodeIssuer, isPeerSelf, rwJoinCode).
</verification>

<success_criteria>
Two RED tests exist that encode the restored RB-03 contract and the out-of-band open contract,
including one behavior-level assertion crossing the actual open path. Both fail against current
(broadcast) production code, establishing the contract Waves 1-2 must satisfy.
</success_criteria>

<output>
Create `.planning/phases/146-open-session-capability-bug/146-01-SUMMARY.md` when done
</output>
