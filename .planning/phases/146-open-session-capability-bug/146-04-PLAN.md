---
phase: 146-open-session-capability-bug
plan: 04
type: execute
wave: 2
depends_on: ["146-02", "146-03"]
files_modified:
  - TESTING.md
autonomous: true
requirements: [FIX-03]
must_haves:
  truths:
    - "TESTING.md no longer registers the deleted broadcast tests (mint_join_codes_test.go, broadcast embed assertions)"
    - "TESTING.md registers the out-of-band tests with accurate Notes (inverted RB-03 + out-of-band open)"
    - "The M-13 live-tailnet manual UAT reflects the out-of-band code-paste flow (not RO/RW client-side selection)"
    - "tests/check-traceability-paths.sh exits 0"
    - "Full Go + frontend suite is green and tsc is clean"
  artifacts:
    - path: "TESTING.md"
      provides: "Accurate FIX-03 traceability rows + out-of-band M-13 manual item + corrected counts"
  key_links:
    - from: "TESTING.md Section 4 traceability rows"
      to: "internal/webserver/sessions_meta_embed_test.go + frontend/src/components/__tests__/App.open-remote.test.tsx"
      via: "repo-relative path rows"
      pattern: "App.open-remote.test.tsx"
---

<objective>
Bring TESTING.md into compliance with the agenthub standing regression convention after the
out-of-band redesign: revert the superseded Phase 146 (broadcast) registrations, register the
new tests with accurate Notes, reword the M-13 live-tailnet manual UAT to the out-of-band
code-paste flow, run the traceability path checker, and verify the full automated suite is green.

Purpose: The standing rule (agenthub/CLAUDE.md, TESTING.md Section 6) requires every phase that
adds/renames/removes tests to update the canonical regression home. The first execution registered
broadcast tests that no longer exist; those rows are now stale and a deleted-file path would fail
the traceability checker.
Output: Compliant TESTING.md; passing traceability check; full-suite-green confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-open-session-capability-bug/146-RESEARCH.md
@.planning/phases/146-open-session-capability-bug/146-VALIDATION.md
@CLAUDE.md
@TESTING.md

<interfaces>
<!-- Exact TESTING.md edit sites (from grep). -->

Section 4 (Requirement->Test Traceability Map), FIX-03 rows:
  - L150: internal/webserver/sessions_meta_embed_test.go — Notes reference TestSessionsMeta_EmbedJoinCodes /
    TestSessionsMeta_NilIssuer (BROADCAST). Reword Notes to the inverted RB-03 test (TestSessionsMeta_NoJoinCodesInResponse —
    /api/sessions/meta carries no ro_join_code/rw_join_code). File path STAYS (file persists, content inverted by Plan 01/02).
  - L151: internal/daemon/mint_join_codes_test.go — DELETE this row entirely (file removed in Plan 02; a dangling
    path would fail check-traceability-paths.sh).
  - L152: frontend/src/components/__tests__/App.open-remote.test.tsx — reword Notes to the out-of-band contract
    (opens RemoteJoinCodeModal when no cap held; open-session exchange builds /sessions/{id}?cap= URL via BrowserOpenURL;
    behavior render+menu-click open-path assertion). File path STAYS.
  - L118 (CARD-04, frontend/src/lib/remoteAdapter.test.ts) — Notes still accurate (hostname/URL mapping); leave unless
    the Plan 03 edits changed what it covers (it should still be CARD-04 mapping only).

Section 5 Category G (Live Tailnet Remote Session Open), M-13 (L222):
  - Reword to the out-of-band flow: owner shares + copies the join code/link and delivers it OUT OF BAND;
    viewer clicks "Open in browser" -> RemoteJoinCodeModal -> pastes code -> live session opens with the
    permission of the code the owner sent (RO code -> RO, RW code -> RW). Remove the "RW only when peer=self
    (D-06 client-side selection)" language (that mechanism was removed).

L34 vitest-count note: no NET change to vitest file count in this redesign (App.open-remote.test.tsx already
  counted in the +1; no vitest files added/removed here — only rewritten). Leave the count unless a frontend
  test file was actually added/deleted across Plans 01/03 (none planned). If the count drifted, correct it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert/register TESTING.md rows + reword M-13; run traceability checker</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md Section 4 traceability rows L94-155, Section 5 Category G L220-224, Section 6 standing convention L228-238, vitest-count note L34
    - CLAUDE.md (agenthub regression-test convention standing rule)
    - tests/check-traceability-paths.sh (path-format rules it enforces)
    - .planning/phases/146-open-session-capability-bug/146-RESEARCH.md (Testing approach section)
    - .planning/phases/146-open-session-capability-bug/146-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <action>
    In Section 4: delete the mint_join_codes_test.go FIX-03 row (file removed in Plan 02). Reword the
    sessions_meta_embed_test.go FIX-03 row Notes to describe the inverted RB-03 assertion
    (TestSessionsMeta_NoJoinCodesInResponse — /api/sessions/meta carries no ro_join_code/rw_join_code;
    cap-free discovery restored), keeping the repo-relative .go path. Reword the App.open-remote.test.tsx
    FIX-03 row Notes to the out-of-band contract (opens RemoteJoinCodeModal when no cap held; open-session
    exchange builds a /sessions/{id}?cap= URL via BrowserOpenURL; includes a behavior render+menu-click
    open-path assertion), keeping the .tsx path. Verify the CARD-04 remoteAdapter.test.ts row Notes still
    accurately describe mapping coverage; adjust only if Plan 03 changed it.
    In Section 5 Category G: reword M-13 to the out-of-band flow — owner shares, copies the code/link, and
    delivers it OUT OF BAND; viewer clicks "Open in browser" -> RemoteJoinCodeModal -> pastes the code ->
    live session opens with the permission of the code the owner sent (RO code -> RO open, RW code -> RW
    open). Remove the client-side "RW when peer=self (D-06 selection)" language.
    Confirm the L34 vitest-count note is still accurate (no vitest file added/removed in this redesign,
    only rewrites); correct only if a file count actually drifted.
    In Section 2 (Suite Manifest): update the Go `*_test.go` count from 348 to 347 — Plan 02 deletes
    internal/daemon/mint_join_codes_test.go (net -1 Go test file; no Go test files added). Reconcile any
    dependent Total so the manifest stays internally consistent. Verify the final Go count against reality
    with `grep -rl --include='*_test.go' . internal/ | wc -l` (or the repo's existing count method) before committing.
    Then run the path checker.
  </action>
  <verify>
    <automated>bash tests/check-traceability-paths.sh 2>&1 | tail -5; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Stale row removed: `grep -c "mint_join_codes_test.go" TESTING.md` == 0
    - Broadcast Notes gone: `grep -c "TestSessionsMeta_EmbedJoinCodes\|TestMintSessionJoinCodes\|peer=self" TESTING.md` == 0
    - New contract registered: `grep -c "TestSessionsMeta_NoJoinCodesInResponse" TESTING.md` >= 1 and the App.open-remote.test.tsx row references the out-of-band open path
    - M-13 reworded: `grep -c "out of band\|out-of-band\|paste" TESTING.md` >= 1 in Category G; no "peer=self" selection language remains
    - Checker passes: `bash tests/check-traceability-paths.sh` exits 0
  </acceptance_criteria>
  <done>TESTING.md is convention-compliant for the out-of-band redesign; traceability path checker exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite + build-gate verification (cross-boundary green check)</name>
  <files>TESTING.md</files>
  <read_first>
    - .planning/phases/146-open-session-capability-bug/146-VALIDATION.md (Full suite command + sampling sign-off)
    - .planning/phases/146-open-session-capability-bug/146-RESEARCH.md (Validation Architecture — the green-suite-hid-dead-feature lesson)
  </read_first>
  <action>
    Run the full Go package suite and the full frontend suite plus the tsc build gate to confirm the
    redesign is green end-to-end — including the Plan 01 behavior test that crosses the open path (the
    prior blind spot). Record the results in this plan's SUMMARY. If any failure surfaces, STOP and
    report (do not paper over); the green suite must reflect a working out-of-band open path, not isolated
    layers. No source edits in this task beyond what verification requires; TESTING.md is listed only so
    a count correction (if the suite reveals drift) can land here.
  </action>
  <verify>
    <automated>go test ./internal/webserver/ ./internal/daemon/ ./internal/tailnet/ 2>&1 | tail -10 && cd frontend && pnpm test --run 2>&1 | tail -15 && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Go: `go test ./internal/webserver/ ./internal/daemon/ ./internal/tailnet/` PASSES (including TestSessionsMeta_NoJoinCodesInResponse)
    - Frontend: `cd frontend && pnpm test --run` PASSES (including App.open-remote.test.tsx behavior open-path test)
    - Build gate: `cd frontend && pnpm tsc --noEmit` is clean (vitest-green alone is not proof of build — standing rule)
    - No broadcast symbol anywhere in non-superseded source: `grep -rn "mintSessionJoinCodes\|SetJoinCodeIssuer\|isPeerSelf" internal/ frontend/src/ | grep -v superseded` returns NO matches
  </acceptance_criteria>
  <done>Full automated suite green and tsc clean; the open path is verified by a behavior test, not isolated source inspection; no broadcast remnants remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regression suite → release confidence | A green suite must reflect a working open path; the documented manual UAT covers the live-tailnet gap automation cannot reach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-02 | Spoofing | green-suite certifying a dead feature | mitigate | Task 2 runs the full suite including the Plan 01 cross-boundary behavior test; M-13 documents the live-tailnet UAT for what automation cannot reach |
| T-146-06 | Repudiation | stale/incorrect traceability | mitigate | Remove the deleted-file row and reword broadcast Notes; check-traceability-paths.sh enforces valid repo-relative paths |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | No package installs; docs + existing test toolchain only |
</threat_model>

<verification>
- `bash tests/check-traceability-paths.sh` exits 0.
- `go test ./internal/webserver/ ./internal/daemon/ ./internal/tailnet/` passes.
- `cd frontend && pnpm test --run && pnpm tsc --noEmit` passes/clean.
- No broadcast symbols remain in non-superseded source.
- M-13 reflects the out-of-band code-paste flow.
</verification>

<success_criteria>
TESTING.md is compliant with the standing regression convention for the out-of-band redesign;
the traceability path checker passes; the full automated suite is green and the build gate is clean,
with the open path verified by a cross-boundary behavior test; the live-tailnet open remains a
documented manual UAT (M-13, out-of-band flow).
</success_criteria>

<output>
Create `.planning/phases/146-open-session-capability-bug/146-04-SUMMARY.md` when done
</output>
