---
phase: 17-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/webserver/network.go
  - internal/webserver/network_test.go
  - app.go
  - app_test.go
autonomous: true
requirements: [CLEAN-01, CLEAN-02]

must_haves:
  truths:
    - "go build ./... passes with zero errors after network.go and network_test.go are deleted"
    - "No GetNetworkInterfaces method exists in app.go"
    - "No TestGetNetworkInterfaces test exists in app_test.go"
    - "go test ./... passes with all packages OK"
    - "No Go source file references NetworkInterface, IsTailscaleIP, or ListInterfaces"
  artifacts:
    - path: "internal/webserver/network.go"
      provides: "DELETED — file must not exist"
    - path: "internal/webserver/network_test.go"
      provides: "DELETED — file must not exist"
    - path: "app.go"
      provides: "GetNetworkInterfaces method removed, all other methods intact"
    - path: "app_test.go"
      provides: "TestGetNetworkInterfaces test removed, all other tests intact"
  key_links:
    - from: "app.go"
      to: "internal/webserver"
      via: "import still needed for TailscaleHealth, CheckHealth, NewWebServer, Config"
      pattern: "webserver\\."
---

<objective>
Delete all backend Go code supporting the generic VPN interface picker: the entire network.go and network_test.go files, and the GetNetworkInterfaces method from app.go plus its test.

Purpose: Fulfill CLEAN-01 (generic VPN interface binding code removed) and confirm CLEAN-02 (auth middleware absent — verified by existing regression tests staying green).
Output: Clean Go build with no dead VPN interface code.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-dead-code-cleanup/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete network.go and network_test.go files</name>
  <files>internal/webserver/network.go, internal/webserver/network_test.go</files>
  <read_first>
    - internal/webserver/network.go (confirm contents before deletion)
    - internal/webserver/network_test.go (confirm contents before deletion)
  </read_first>
  <action>
Delete both files entirely:
- `rm internal/webserver/network.go` — contains NetworkInterface struct, tailscaleCIDR var, init(), IsTailscaleIP(), ListInterfaces() — all dead after Phase 15 moved to local.Client direct IP query
- `rm internal/webserver/network_test.go` — contains tests exclusively for the above functions (TestIsTailscaleIP, TestListInterfaces, TestTailscaleIPDetectedInList, etc.)

Both files must be deleted together. Deleting network.go without network_test.go would cause compilation failure because network_test.go references symbols from network.go.

After deletion, run `go build ./...` to confirm no other Go code depends on these symbols. The research confirmed via grep that no remaining Go file references NetworkInterface, IsTailscaleIP, or ListInterfaces except app.go:GetNetworkInterfaces (deleted in Task 2).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && test ! -f internal/webserver/network.go && test ! -f internal/webserver/network_test.go && echo "FILES_DELETED_OK"</automated>
  </verify>
  <acceptance_criteria>
    - File internal/webserver/network.go does not exist
    - File internal/webserver/network_test.go does not exist
  </acceptance_criteria>
  <done>Both network.go and network_test.go are deleted from the filesystem</done>
</task>

<task type="auto">
  <name>Task 2: Remove GetNetworkInterfaces from app.go and app_test.go, verify build</name>
  <files>app.go, app_test.go</files>
  <read_first>
    - app.go (see current GetNetworkInterfaces method at line 301 and surrounding context)
    - app_test.go (see TestGetNetworkInterfaces at line 164 and surrounding context)
  </read_first>
  <action>
1. In `app.go`, delete the GetNetworkInterfaces method (lines 301-307):
```go
func (a *App) GetNetworkInterfaces() []webserver.NetworkInterface {
	ifaces, err := webserver.ListInterfaces()
	if err != nil {
		return []webserver.NetworkInterface{}
	}
	return ifaces
}
```
Do NOT remove the `webserver` package import — it is still used by: `webserver.TailscaleHealth`, `webserver.CheckHealth`, `webserver.Config`, `webserver.NewWebServer`.

2. In `app_test.go`, delete TestGetNetworkInterfaces (lines 164-172):
```go
func TestGetNetworkInterfaces(t *testing.T) {
	app := testApp(t)
	ifaces := app.GetNetworkInterfaces()
	// GetNetworkInterfaces may return empty on minimal CI environments.
	// The important thing is it does NOT return nil and does not panic.
	if ifaces == nil {
		t.Error("GetNetworkInterfaces returned nil, want non-nil slice")
	}
}
```

3. Run `go build ./...` — must pass with zero errors.
4. Run `go test ./...` — must pass all packages. The existing regression tests TestLoginRouteNotRegistered, TestTokenRouteNotRegistered, and TestSessionAccessWithoutAuth must remain and pass (they guard against auth re-introduction per CLEAN-02).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./... && go test ./... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app.go does NOT contain the string "GetNetworkInterfaces"
    - app_test.go does NOT contain the string "TestGetNetworkInterfaces"
    - app.go still contains "webserver." (import is preserved)
    - `go build ./...` exits 0
    - `go test ./...` exits 0 with all packages ok
    - grep -r "GetNetworkInterfaces" --include="*.go" /Users/ken/dev/agenthub returns only zero matches
  </acceptance_criteria>
  <done>GetNetworkInterfaces method and test removed; Go build and full test suite pass cleanly</done>
</task>

</tasks>

<verification>
After both tasks:
1. `go build ./...` passes with exit code 0
2. `go test ./...` passes all packages
3. `grep -r "NetworkInterface\|IsTailscaleIP\|ListInterfaces\|GetNetworkInterfaces" --include="*.go" /Users/ken/dev/agenthub` returns zero matches
4. `test ! -f internal/webserver/network.go && test ! -f internal/webserver/network_test.go` succeeds
</verification>

<success_criteria>
- All generic VPN interface Go code is deleted (network.go, network_test.go, GetNetworkInterfaces method and test)
- Go build passes cleanly
- All Go tests pass (including auth-absence regression tests)
- No Go source file references the deleted symbols
</success_criteria>

<output>
After completion, create `.planning/phases/17-dead-code-cleanup/17-01-SUMMARY.md`
</output>
