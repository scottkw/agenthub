---
status: testing
phase: 171-public-full-access-read-write-internet-sharing-behind-a-hard
source: [171-VERIFICATION.md]
started: 2026-07-07T23:56:00Z
updated: 2026-07-07T23:56:00Z
---

## Current Test

number: 1
name: M-47 live off-tailnet public-write UAT — the RCE-severity acceptance gate (FNL-09)
expected: |
  On a signed production build on a real Tailscale-Funnel-granted machine:
  1. Enable internet (Funnel) sharing for a session; in the Share modal's Danger section
     complete the ≥3s hold-to-confirm. Public write URL + single-use write code + live
     countdown appear; FULL ACCESS badge/tab icon light up.
  2. From an OFF-TAILNET device, redeem the write code ONCE and confirm terminal input reaches
     the PTY (type + run a command). The SAME code cannot be redeemed a second time (single-use).
  3. No other public write path exists: the public URL without the gate / without a registered
     grant is rejected (403/401) — the hard consent gate is the ONLY way in.
  4. With a second off-tailnet device on a READ spectator link, click "Disable public write":
     the writer's next input is rejected (401) immediately while the read spectator keeps
     viewing uninterrupted (independent read/write teardown).
  5. Shorter expiry fires: a 15m write grant auto-revokes the writer at expiry without touching
     the read share.
awaiting: user response

## Tests

### 1. M-47 live off-tailnet public-write UAT — the RCE-severity acceptance gate (FNL-09)
expected: |
  Hard consent gate (≥3s hold) is the ONLY public write path; single-use write code redeems
  exactly once from an off-tailnet device and reaches the PTY; disabling immediately revokes the
  writer while a read spectator keeps viewing; read/write surfaces teardown independently; shorter
  expiry auto-revokes the writer. A leaked link before the gate grants no write access.
result: [pending]

## Summary

total: 1
passed: 0
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps
