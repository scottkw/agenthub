---
phase: 171-public-full-access-read-write-internet-sharing-behind-a-hard
plan: 04
type: execute
wave: 4
depends_on: [171-01, 171-02, 171-03]
files_modified:
  - TESTING.md
  - frontend/src/content/help/sharing-guide.md
  - .planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-SECURITY.md
autonomous: true
requirements: [FNL-09]

must_haves:
  truths:
    - "TESTING.md Suite Manifest counts reflect the new Phase-171 test files (internal/webserver/rwgate_test.go is a new Go file; joincode_test.go / funnel_test.go / SessionSharePanel.test.tsx / SessionCard.share.test.tsx are extended in place); the Total is reconciled and internally consistent."
    - "The Traceability map (Section 4) has FNL-09 rows for every new/extended Phase-171 test file, each Path column a repo-relative .go/.ts/.tsx/.sh path only (no test names); `bash tests/check-traceability-paths.sh` exits 0."
    - "A Section-5 manual M-NN item captures the live off-tailnet public-write UAT (gate → redeem write code from an off-tailnet device → terminal input works → owner disables → writer's next input 401s while a read spectator keeps viewing) — the RCE-severity live acceptance gate that cannot be automated (no tailnet in CI)."
    - "171-SECURITY.md exists asserting no public write path except the consent gate, synthesizing the four plans' STRIDE registers, mapping each threat to its in-code mitigation + verifying test, and stating /gsd-secure-phase 171 re-verifies post-execution (SPEC R8)."
  artifacts:
    - "TESTING.md — Suite Manifest counts, FNL-09 traceability rows, new manual M-NN live-UAT item"
    - "frontend/src/content/help/sharing-guide.md — short public-write (FULL ACCESS) section"
    - ".planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-SECURITY.md — synthesized Phase-171 threat model asserting the closed write perimeter (SPEC R8)"
  key_links:
    - "The live M-NN item is the real acceptance gate for R4/R6 given the RCE severity — the automated WS-relay integration test (Plan 01) proves pre-gate rejection at unit level; only an off-tailnet device proves the closed perimeter end-to-end."
  prohibitions:
    - statement: "The Traceability Path column MUST NOT contain test names — repo-relative source paths only (repo standing rule)."
      status: resolved
      verification: "test — bash tests/check-traceability-paths.sh exits 0."
---

<objective>
Reconcile the regression-suite documentation (TESTING.md) with the Phase-171 test additions per the repo standing rule, and add the off-tailnet live-UAT manual item that is the true acceptance gate for an internet-RCE surface. Add a short public-write section to the in-app Sharing Guide.

Purpose: The repo standing convention (CLAUDE.md) requires every phase that adds/renames tests to update the Suite Manifest, Traceability map, and (for non-automatable live behavior) the Section-5 manual checklist, with `check-traceability-paths.sh` exiting 0. This phase's core R4/R6 guarantees can only be fully proven off-tailnet on a production build.

Output: reconciled TESTING.md (counts + traceability + M-NN) and a Sharing Guide RW section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-01-SUMMARY.md
@.planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-02-SUMMARY.md
@.planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TESTING.md reconcile — Suite Manifest, Traceability, M-NN live item</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md (Section 2 Suite Manifest table + Total row at :32; the Phase-170 note convention rows ~:34-38 to mirror; Section 4 Traceability map format; Section 5 manual checklist + the latest M-46 item added by Phase 170 for the M-NN numbering)
    - The three plan SUMMARYs (which test files were created/extended and their exact counts)
  </read_first>
  <action>
    Update Section 2 Suite Manifest: increment the Go file count for the new internal/webserver/rwgate_test.go (joincode_test.go, funnel_test.go, api_test.go were extended in place — no count change); increment the vitest count only if a new frontend test FILE was created (SessionSharePanel.test.tsx + SessionCard.share.test.tsx already exist and are extended in place — no count change; confirm against the SUMMARYs). Reconcile the Total to stay internally consistent (currently 528). Add a Phase-171 note row in the manifest-note style mirroring the Phase-170 rows, describing the FNL-09 additions (IssueSingleUseWithTTL, RemoveGrant/SetRWGate, TestHandleWSSRelay_WriteCap_RequiresGate, terminal-only-scope, expiry-clamp, RevokesGrantOnly, Danger-section/badge tests). Add Section-4 Traceability rows mapping FNL-09 to each new/extended file — Path column repo-relative .go/.ts/.tsx paths ONLY, no test names. Add a Section-5 manual item M-47 (or the next free number — verify against the current highest) for the live off-tailnet public-write UAT: hold-gate → redeem the single-use write code from an off-tailnet device → terminal input executes → owner disables RW → writer's next input 401s while a separate read spectator (reusable read code) keeps viewing → confirm single-use (2nd redemption of the write code fails closed). Note it requires a signed production build on a real Funnel tailnet + an off-tailnet device.
  </action>
  <verify>
    <automated>bash tests/check-traceability-paths.sh</automated>
  </verify>
  <done>Suite Manifest counts + Total reconciled and internally consistent; FNL-09 traceability rows added with repo-relative source paths only; M-47 (next free) live off-tailnet public-write item added; check-traceability-paths.sh exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Sharing Guide — public-write (FULL ACCESS) section</name>
  <files>frontend/src/content/help/sharing-guide.md</files>
  <read_first>
    - frontend/src/content/help/sharing-guide.md (existing Funnel + device-share structure from Phase 166 — match tone/heading style)
  </read_first>
  <action>
    Add a short "Public write access (FULL ACCESS)" section covering: what it grants (terminal command execution to anyone with the link + single-use write code — RCE risk stated plainly, not softened), how to enable it (the 3-second hold-to-confirm gate in the Share modal's Danger section), that the write code is single-use / one writer / short-lived (15m default, 1h max), that disabling is one click and revokes the writer immediately, and that the read share (reusable read code) is unaffected by disabling write. Keep it consistent with the risk-forward consent copy — do not understate the grant.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit && grep -qi 'FULL ACCESS' src/content/help/sharing-guide.md</automated>
  </verify>
  <done>Sharing Guide has a public-write section stating the RCE risk plainly, the hold-gate flow, single-use/short-lived semantics, and read/write independence. Frontend tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Synthesize 171-SECURITY.md — closed-write-perimeter threat model (SPEC R8)</name>
  <files>.planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-SECURITY.md</files>
  <read_first>
    - .planning/phases/171-.../171-01-PLAN.md `<threat_model>` (T-171-01..05: grant-gating at WS upgrade, surgical RemoveGrant, single-use code atomicity, custom-TTL, gate-aware originAllowedForWrite)
    - .planning/phases/171-.../171-02-PLAN.md `<threat_model>` (T-171-06..10: terminal-only perms, D-04 rebase removal, stale-cap revocation, expiry clamp, read-share survives RW disable; and the Trust Boundaries table — the loopback funnel-write endpoint reuses the existing Funnel-toggle loopback access-control model)
    - .planning/phases/171-.../171-03-PLAN.md `<threat_model>` (T-171-11..13: consent copy, hold-friction, colorblind indicator)
    - .planning/phases/171-.../171-04-PLAN.md `<threat_model>` (T-171-14: live off-tailnet acceptance gate)
    - .planning/phases/171-.../171-SPEC.md R8 (:59-62) + Acceptance Criteria (:92-103) — the exact assertion required
  </read_first>
  <action>
    Write .planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-SECURITY.md as a standalone Phase-171 threat model. It MUST:
    (1) State the load-bearing invariant verbatim as a top-level assertion: no public write path exists except through the new hard consent gate. Explain the enforcement mechanics precisely — grant-gating (D-01) via isGrantActive at the real GET /sessions/{id}/ws upgrade is the PRIMARY and sufficient terminal-write barrier reaching the PTY-write frames (MsgInput/MsgSessionInject); the write grant is registered ONLY by the gate handler (ws.AddGrant + ws.SetRWGate + IssueSingleUseWithTTL as one atomic post-hold action); surgical RemoveGrant tears it down. State explicitly that originAllowedForWrite (D-02) is defense-in-depth for the files.write HTTP routes ONLY and does NOT reach MsgInput/MsgSessionInject — so it is a secondary barrier, never the primary one.
    (2) Synthesize the four plans' STRIDE registers into one consolidated table: reproduce every threat row (T-171-01 through T-171-14 plus the T-171-SC supply-chain row) with columns Threat ID | Category | Severity | Disposition | In-code Mitigation | Verifying Test/Assertion. Map each threat to the concrete test that proves it (e.g. T-171-01 → TestHandleWSSRelay_WriteCap_RequiresGate; T-171-06 → TestFunnelWriteGate_TerminalOnlyScope; T-171-08 → the RW-disable/expiry revoke path; T-171-09 → TestHandleSetSessionFunnelWrite_ExpiryClamp; T-171-10 → TestDisableFunnelWrite_RevokesGrantOnly; T-171-05 → TestOriginAllowedForWrite_RWGate; T-171-03 → TestJoinCodeManager_IssueSingleUseWithTTL). Note the live M-item (T-171-14) as the off-tailnet end-to-end acceptance gate CI cannot cover.
    (3) Record the loopback trust-boundary provenance: the POST /sessions/{id}/funnel-write route reuses the SAME loopback owner-only access-control model as the existing Phase-165/170 Funnel-toggle loopback endpoints (POST /sessions/{id}/funnel) — so "owner-only, no auth" is inherited enforcement, not a newly assumed boundary.
    (4) Confirm no OPEN write-path threat remains (every write-path threat disposition is mitigate, with a verifying test), and state that /gsd-secure-phase 171 will be run post-execution to authoritatively re-verify each mitigation against the implemented code.
    Do NOT restate implementation code; reference identifiers, tests, and the SPEC R8 acceptance line.
  </action>
  <verify>
    <automated>test -f .planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-SECURITY.md && grep -q 'no public write path' .planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-SECURITY.md && grep -q 'TestHandleWSSRelay_WriteCap_RequiresGate' .planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-SECURITY.md && grep -q 'gsd-secure-phase 171' .planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - 171-SECURITY.md exists at the phase directory.
    - It contains the literal invariant phrase "no public write path" asserting the closed perimeter (SPEC R8).
    - The consolidated STRIDE table maps each threat (T-171-01..14 + T-171-SC) to an in-code mitigation AND a verifying test/assertion (threat→mitigation→test rows present).
    - It records that originAllowedForWrite is defense-in-depth for files.write only and does NOT reach MsgInput/MsgSessionInject.
    - It records the loopback funnel-write endpoint reusing the existing Phase-165/170 Funnel-toggle loopback access-control model.
    - It states /gsd-secure-phase 171 runs post-execution to re-verify mitigations.
    - No OPEN write-path threat remains (all write-path dispositions are mitigate).
  </acceptance_criteria>
  <done>171-SECURITY.md exists, asserts no public write path except the consent gate, consolidates all four STRIDE registers with threat→mitigation→verifying-test rows, records the loopback-endpoint provenance and the defense-in-depth scoping of originAllowedForWrite, and states /gsd-secure-phase 171 re-verifies post-execution. Grep gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator | The live M-NN item is the human gate that catches an end-to-end perimeter break CI cannot see |
| threat model (171-SECURITY.md) → auditor/`/gsd-secure-phase` | The synthesized closed-perimeter assertion is the artifact `/gsd-secure-phase 171` re-verifies against implemented code (SPEC R8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-171-14 | Repudiation (untested claim) | Green CI certifying an unproven perimeter | high | mitigate | Section-5 M-47 mandates an off-tailnet production-build UAT proving pre-gate rejection, single-use, immediate revoke, and read-survives — the RCE-severity acceptance gate (per project standing pattern for Funnel phases). |
| T-171-15 | Repudiation (undocumented perimeter) | No threat model asserting the closed write perimeter (SPEC R8) | high | mitigate | Task 3 writes 171-SECURITY.md synthesizing all four STRIDE registers, asserting no public write path except the consent gate, mapping each threat → in-code mitigation → verifying test; `/gsd-secure-phase 171` re-verifies post-execution against implemented code. |
| T-171-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new packages; docs-only plan. |
</threat_model>

<verification>
- `bash tests/check-traceability-paths.sh` exits 0
- TESTING.md Total row is internally consistent with the per-group counts
- `cd frontend && pnpm exec tsc --noEmit` clean
</verification>

<success_criteria>
TESTING.md reflects all Phase-171 test additions (manifest + traceability) with the traceability path-check passing, a live off-tailnet public-write M-item recorded, and the Sharing Guide covers public write honestly.
</success_criteria>

<output>
Create `.planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 04 slice)

- TESTING.md — Suite Manifest count reconcile + Phase-171 note row, Section-4 FNL-09 traceability rows, Section-5 M-47 live off-tailnet public-write UAT item
- frontend/src/content/help/sharing-guide.md — public-write (FULL ACCESS) help section
- .planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-SECURITY.md — synthesized Phase-171 threat model asserting no public write path except the consent gate, mapping T-171-01..14 + T-171-SC to in-code mitigations + verifying tests, recording the loopback-endpoint provenance, and stating /gsd-secure-phase 171 re-verifies post-execution (SPEC R8)
