---
phase: 171-public-full-access-read-write-internet-sharing-behind-a-hard
plan: 03
type: execute
wave: 3
depends_on: [171-02]
files_modified:
  - frontend/src/style.css
  - frontend/src/components/SessionSharePanel.tsx
  - frontend/src/components/Hub/SessionShareModal.tsx
  - frontend/src/components/Hub/SessionCard.tsx
  - frontend/src/components/TabBar.tsx
  - frontend/src/App.tsx
  - frontend/src/components/__tests__/SessionSharePanel.test.tsx
  - frontend/src/components/__tests__/SessionCard.share.test.tsx
autonomous: true
requirements: [FNL-09]

must_haves:
  truths:
    - "Releasing the hold before 3s issues nothing — zero SetSessionFunnelWrite calls, no write URL/code/grant (R1 gate-completeness edge); the fill resets to 0% and the label reverts to 'Hold 3s to confirm'."
    - "Completing the ≥3s hold fires exactly one SetSessionFunnelWrite(sessionId, expirySeconds) and reveals the public write URL + single-use write code (CodeDisplay) + live countdown + 'Disable public write' button (R1/D-13)."
    - "The RW indicator differs from the read indicator in label ('FULL ACCESS' vs 'INTERNET'), icon (LockOpenIcon vs GlobeAltIcon), AND shape (.hub-fullaccess-badge clip-path notch vs .hub-internet-badge border-radius pill) — verifiable at the source/DOM level, never by color alone (R7/D-09)."
    - "The FULL ACCESS badge (SessionCard) and full-access tab icon (TabBar) render when funnelWriteActive, coexisting with the read INTERNET badge/icon (read-then-write order); they clear when funnelWriteActive flips false independent of funnelActive (D-10; prohibition: RW teardown keeps the read badge)."
    - "The RW expiry selector offers only 15m/30m/1h (default 15m) — no 'Until I disable' option (D-11/R5)."
    - "The hold-to-confirm control is a real <button> with a keyboard equivalent (Space/Enter drives the same 3000ms hold; early key-release = early release) and is disabled until funnelActive && !warmingUp."
  artifacts:
    - "frontend/src/style.css — --hub-fullaccess-badge-bg/-text tokens (dark+light), .hub-funnel-write-gate* , .hub-fullaccess-badge* , .tab__fullaccess-icon"
    - "frontend/src/components/SessionSharePanel.tsx — Danger section (.hub-funnel-write-gate): warning banner, expiry selector, hold-to-confirm control, post-gate result/used states, disable button"
    - "frontend/src/components/Hub/SessionShareModal.tsx — hold-completion → SetSessionFunnelWrite wiring; writeURL/writeCode/expiresAt/funnelWriteActive/used state threaded into SessionSharePanel"
    - "frontend/src/components/Hub/SessionCard.tsx — FULL ACCESS badge; frontend/src/components/TabBar.tsx — full-access tab icon; frontend/src/App.tsx — funnelWriteActiveSessions derivation"
    - "SessionSharePanel.test.tsx + SessionCard.share.test.tsx — new RW assertions"
  key_links:
    - "SessionShareModal calls the Wails-bound SetSessionFunnelWrite (from Plan 02) only on hold completion; funnelWriteActive comes from the existing hubSessions 3s poll (App.tsx funnelWriteActiveSessions, mirroring funnelActiveSessions), NOT a new interval."
  prohibitions:
    - statement: "The consent-gate copy MUST NOT understate the grant — the warning body MUST contain the literal phrases 'command execution' and 'anyone with the link' and MUST NOT use softened/euphemistic wording."
      status: resolved
      verification: "judgment + test — SessionSharePanel.test.tsx asserts the rendered warning body literally contains 'command execution' and 'anyone with the link'; copy review against SPEC Prohibition #4."
---

<objective>
Add the desktop Share-modal Danger section (hold-to-confirm public-write consent gate) and the colorblind-safe FULL ACCESS indicators, per the approved 171-UI-SPEC.md. This is the owner-facing surface that turns the deliberate backend RW-gate lifecycle into an unmistakable, high-friction, abortable opt-in.

Purpose: The 3-second hold IS the consent mechanism (R1) — deliberately effortful, distinct from and physically separated below the benign Phase-166 read section (D-06). The FULL ACCESS badge must be distinguishable from the read INTERNET badge by label + icon + shape (never color), because the reviewer is colorblind — verify at source, not by eye.

Output: `.hub-funnel-write-gate` Danger section + hold control + post-gate/used states, `.hub-fullaccess-badge` on SessionCard, `.tab__fullaccess-icon` on TabBar, the modal wiring, and the CSS tokens/classes — with tests for early-release-issues-nothing, distinct-indicator, and consent-copy compliance.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-UI-SPEC.md
@.planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-PATTERNS.md
@.planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS tokens + Danger-section + FULL ACCESS badge/icon styles</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css (--hub-internet-badge-bg/-text at ~4707 dark / ~4766 light; .hub-internet-badge* rules at ~7181-7208; .hub-funnel-risk-panel* expand/collapse + prefers-reduced-motion precedent; --hub-destructive, --hub-surface-elevated, --hub-border, --hub-text-* tokens)
    - .planning/phases/171-.../171-UI-SPEC.md (Color, Component Inventory, Motion sections — the exact token hex values and class list)
  </read_first>
  <action>
    Add a new "/* ===== Phase 171 (FNL-09) — public write consent gate + FULL ACCESS badge ===== */" block after the Phase 166 block. Add tokens --hub-fullaccess-badge-bg / --hub-fullaccess-badge-text to BOTH the dark :root and [data-ui-theme="light"] blocks, adjacent to the --hub-internet-badge-* pair, using the UI-SPEC hex values (dark text #f7768e, light text #c0394f — an alias-by-value of --hub-destructive; include the mandated COLORBLIND-SAFE comment with the hex values called out for source-level verification, per the standing convention). Add .hub-funnel-write-gate and its BEM children (__heading, __warning, __icon, __warning-heading, __warning-body, __expiry, __hold-btn, __hold-fill, __hold-label, __result, __used, __disable) per the UI-SPEC Component Inventory: outer container border-top 2px solid var(--hub-destructive) (structurally distinct from the read section's 1px var(--hub-border)); heading 700/uppercase/destructive; warning left-border 3px destructive; disable button destructive border/text. Add .hub-fullaccess-badge with clip-path: polygon(...) notch geometry (NOT border-radius: var(--hub-radius-pill) — this is the load-bearing shape distinction from .hub-internet-badge), plus __icon/__label (label 700/uppercase). Add .tab__fullaccess-icon. Add the hold-fill motion rules under prefers-reduced-motion (functional geometry preserved, easing removed) per the UI-SPEC Motion section.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit && grep -q 'clip-path' src/style.css && grep -q 'hub-fullaccess-badge-text' src/style.css</automated>
  </verify>
  <done>All Phase-171 tokens and classes exist; .hub-fullaccess-badge uses clip-path (not the pill's border-radius); --hub-fullaccess-badge-text resolves to a different hex than --hub-internet-badge-text in both theme blocks; frontend tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Danger section + hold-to-confirm gate + modal wiring</name>
  <files>frontend/src/components/SessionSharePanel.tsx, frontend/src/components/Hub/SessionShareModal.tsx, frontend/src/components/__tests__/SessionSharePanel.test.tsx</files>
  <read_first>
    - frontend/src/components/SessionSharePanel.tsx (the Phase-166 .hub-share-internet-section block ~:344-395: funnelEngaged gating, publicReadCode row, CodeDisplay at :8; place the Danger section physically below it)
    - frontend/src/components/Hub/SessionShareModal.tsx (funnelOn/warmingUp/publicReadCode state + SetSessionFunnel/IssueCapabilities wiring ~:187-356 — add sibling write state, wire the new SetSessionFunnelWrite RPC)
    - .planning/phases/171-.../171-UI-SPEC.md (Interaction Contract state machine + Copywriting Contract — the exact copy strings are load-bearing)
  </read_first>
  <behavior>
    - Test (R1 early release): pointerdown then pointerup before 3000ms (fake timers) → zero SetSessionFunnelWrite calls; fill resets to 0%; label back to "Hold 3s to confirm".
    - Test (R1 complete): pointerdown held ≥3000ms → exactly one SetSessionFunnelWrite(sessionId, expirySeconds) call with the selected expiry (default 900); result block renders the write URL row + CodeDisplay "Single-use write code:" + countdown + "Disable public write".
    - Test (keyboard equivalent): Space/Enter keydown→keyup drives the same hold; early keyup issues nothing.
    - Test (used state): when the poll reports the write cap consumed, the URL/code rows collapse to "Write code used — one writer connected".
    - Test (consent copy compliance — SPEC Prohibition #4): the rendered warning body literally contains "command execution" AND "anyone with the link".
    - Test (warm-up gating): hold control is disabled (aria-disabled) until funnelActive && !warmingUp.
  </behavior>
  <action>
    In SessionSharePanel.tsx add the .hub-funnel-write-gate Danger section BELOW the existing .hub-share-internet-section, rendered only when funnelEngaged (matches the read section). Render the warning heading "⚠ You are exposing a terminal to the internet", the risk-forward body (verbatim from the UI-SPEC Copywriting Contract, containing "command execution" and "anyone with the link" — do NOT soften), the expiry <select> (15m/30m/1h, default 15m; no "Until I disable"), and the hold-to-confirm control as a real <button> (not a div). Drive the hold via pointerdown/pointerup/pointerleave AND keydown/keyup(Space,Enter) with a 3000ms timer + requestAnimationFrame/interval advancing .hub-funnel-write-gate__hold-fill width 0→100%; early release resets to 0% and makes NO RPC call; completion invokes an onGateConfirm(expirySeconds) callback exactly once. Disable the control (disabled + aria-disabled) until funnelActive && !warmingUp, showing the "Waiting for internet share to finish starting up…" note. After confirm, render the result block (public write URL row reusing the existing url-row/actions Copy/Open/QR pattern, <CodeDisplay label="Single-use write code:" code={writeCode} />, live countdown "Expires in mm:ss", and the "Disable public write" button). On the poll-detected redemption, collapse to the .hub-funnel-write-gate__used "Write code used — one writer connected" state. In SessionShareModal.tsx add sibling state (writeUrl/writeCode/expiresAt/writeUsed) and wire onGateConfirm → SetSessionFunnelWrite(session.id, expirySeconds); wire the Disable button → the RW-disable RPC; clear write state on disable/expiry. Use funnelWriteActive from the existing poll as authoritative for collapsing back to Idle (client-side setInterval only for the visual countdown, per RESEARCH Open Question 2). Add SessionSharePanel.test.tsx cases per <behavior> using fake timers.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- SessionSharePanel</automated>
  </verify>
  <done>Early release issues nothing; a completed hold fires exactly one SetSessionFunnelWrite and reveals URL+code+countdown+disable; keyboard hold works; used-state collapses; the warning body literally contains "command execution" and "anyone with the link"; hold disabled during warm-up. SessionSharePanel vitest green.</done>
</task>

<task type="auto">
  <name>Task 3: FULL ACCESS badge (SessionCard) + tab icon (TabBar) + App poll derivation</name>
  <files>frontend/src/components/Hub/SessionCard.tsx, frontend/src/components/TabBar.tsx, frontend/src/App.tsx, frontend/src/components/__tests__/SessionCard.share.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/SessionCard.tsx (the .hub-internet-badge render block :538-542, GlobeAltIcon import :13 — add LockOpenIcon import + sibling block gated on session.funnelWriteActive)
    - frontend/src/components/TabBar.tsx (.tab__internet-icon render :247-250 gated on funnelActiveSessions — add .tab__fullaccess-icon gated on funnelWriteActiveSessions)
    - frontend/src/App.tsx (funnelActiveSessions derivation :1634-1637 from the hubSessions poll — add funnelWriteActiveSessions the same way)
  </read_first>
  <behavior>
    - Test (R7 distinct indicator): when session.funnelWriteActive is true, SessionCard renders .hub-fullaccess-badge with the LockOpenIcon and literal label "FULL ACCESS" — a different class, icon-import name, and label string than .hub-internet-badge/GlobeAltIcon/"INTERNET" (assert at source/DOM level).
    - Test (coexistence): with BOTH funnelActive and funnelWriteActive true, both badges render (read INTERNET first, FULL ACCESS second); with funnelWriteActive false but funnelActive true, only the read badge renders (RW teardown keeps the read badge).
  </behavior>
  <action>
    In SessionCard.tsx import LockOpenIcon from @heroicons/react/24/outline and add a sibling render block gated on session.funnelWriteActive: <span className="hub-fullaccess-badge"><LockOpenIcon className="hub-fullaccess-badge__icon" aria-hidden="true"/><span className="hub-fullaccess-badge__label">FULL ACCESS</span></span>, placed AFTER the existing .hub-internet-badge block (read-then-write order). Include the mandated COLORBLIND-SAFE comment noting the hex + that shape+icon+text carry state. In TabBar.tsx add a .tab__fullaccess-icon (LockOpenIcon, aria-label/title "Full access exposed — command execution") gated on funnelWriteActiveSessions?.[tab.sessionId], rendered after .tab__internet-icon; add the funnelWriteActiveSessions?: Record<string,boolean> prop mirroring funnelActiveSessions. In App.tsx derive funnelWriteActiveSessions from the same hubSessions poll (reduce over s.funnelWriteActive) and pass it to TabBar — no new interval. Ensure the frontend session type carries funnelWriteActive (from the Plan 02 Wails binding). Add SessionCard.share.test.tsx cases per <behavior>.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- SessionCard.share && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>The FULL ACCESS badge/tab-icon render on funnelWriteActive with distinct class/icon/label; both badges coexist in read-then-write order; the read badge survives when only funnelWriteActive clears. SessionCard.share vitest + frontend tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (human) → Danger-section hold control | The 3s hold is the consent affordance; a too-easy or mislabeled control risks accidental internet-RCE opt-in |
| owner GUI → daemon RPC (SetSessionFunnelWrite) | Only invoked on hold completion; early release must never call it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-171-11 | Repudiation / social-engineering | Consent copy understating the grant | high | mitigate | Locked risk-forward copy contains "command execution" + "anyone with the link" verbatim (SPEC Prohibition #4); asserted by a literal-string test, not paraphrase. |
| T-171-12 | Elevation of Privilege (accidental) | One-click / low-friction enable | high | mitigate | Real ≥3s press-and-hold with early-release-issues-nothing (R1); zero RPC on early release, asserted by test. |
| T-171-13 | Information (colorblind mis-read) | RW link confused with read link | medium | mitigate | Label + icon + shape distinction (D-09), source-verifiable; color is reinforcement only. Distinct-indicator test at DOM/source level. |
| T-171-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new packages; LockOpenIcon ships in the already-installed @heroicons/react 2.2.0. |
</threat_model>

<verification>
- `cd frontend && pnpm test -- SessionSharePanel SessionCard.share` green
- `cd frontend && pnpm exec tsc --noEmit && pnpm build` clean (wails-dev parity — vitest alone does not prove the app builds)
- Source-level colorblind checks: `grep` confirms two different icon imports (LockOpenIcon vs GlobeAltIcon), two different labels ("FULL ACCESS" vs "INTERNET"), and `.hub-fullaccess-badge` uses clip-path not the pill radius
- Consent-copy literal check: the warning body string contains "command execution" and "anyone with the link"
</verification>

<success_criteria>
The Danger section gates public write behind a real, abortable ≥3s hold with keyboard parity and warm-up gating; the post-gate/used states render per the UI-SPEC; the FULL ACCESS indicators are source-verifiably distinct from the read INTERNET indicators and coexist correctly; consent copy is compliant. Frontend vitest + tsc + build green.
</success_criteria>

<output>
Create `.planning/phases/171-public-full-access-read-write-internet-sharing-behind-a-hard/171-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 03 slice)

- CSS tokens `--hub-fullaccess-badge-bg`, `--hub-fullaccess-badge-text` (dark + light) — frontend/src/style.css
- CSS classes `.hub-funnel-write-gate` (+ __heading/__warning/__icon/__warning-heading/__warning-body/__expiry/__hold-btn/__hold-fill/__hold-label/__result/__used/__disable), `.hub-fullaccess-badge` (+ __icon/__label), `.tab__fullaccess-icon` — frontend/src/style.css
- Danger section (public write consent gate) — frontend/src/components/SessionSharePanel.tsx
- Hold-completion → SetSessionFunnelWrite wiring + write state — frontend/src/components/Hub/SessionShareModal.tsx
- FULL ACCESS badge — frontend/src/components/Hub/SessionCard.tsx
- `.tab__fullaccess-icon` render + funnelWriteActiveSessions prop — frontend/src/components/TabBar.tsx
- `funnelWriteActiveSessions` poll derivation — frontend/src/App.tsx
- SessionSharePanel.test.tsx (early-release, complete-hold, keyboard, used-state, consent-copy) + SessionCard.share.test.tsx (distinct indicator, coexistence)
