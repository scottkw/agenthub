---
phase: 152-relay-protocol-identity-presence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/daemon/alias_store.go
  - internal/daemon/alias_store_test.go
autonomous: true
requirements: [IDENT-02]
must_haves:
  truths:
    - "An alias set via AliasStore.Set survives a reload (constructing a fresh AliasStore over the same dir returns the persisted alias) — satisfies D-02 daemon-persisted-across-restart"
    - "AliasStore is keyed by the composite personKey string (TailnetID + ':' + origin), so owner (local:local) and a same-machine browser (nodekey:web) keep separate persisted aliases"
    - "AliasStore.Set validates input via relay.ValidateAlias and rejects invalid aliases without persisting them"
  artifacts:
    - internal/daemon/alias_store.go
    - internal/daemon/alias_store_test.go
  key_links:
    - "aliases.json lives at a fixed hardcoded path under the supplied configDir — no user-controlled path component (T-152-06)"
    - "AliasStore.Set is the daemon-owned persistence sink wired into both WS paths via the Subscriber.AliasSetFn callback (Plans 05/06)"
---

<objective>
Create internal/daemon/alias_store.go: a small daemon-owned JSON-file store mapping
composite personKey → alias, persisted to aliases.json under the daemon config dir.
This is the storage layer for D-02 (alias is daemon-persisted, keyed by composite
TailnetID+origin from D-04, survives reconnect/late-join/daemon-restart). It is global,
not per-session, because alias identity is per-person across all of a user's sessions.

Purpose: provide the durable, restart-surviving alias map that both WS handlers read at
subscribe time and write on MsgAliasSet.
Output: AliasStore type (Get/Set/GetOrDefault + JSON load/persist) and its test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-relay-protocol-identity-presence/152-RESEARCH.md
@.planning/phases/152-relay-protocol-identity-presence/152-PATTERNS.md
@internal/daemon/chat.go
</context>

<artifacts_this_phase_produces>
This plan creates:
- File internal/daemon/alias_store.go + type AliasStore
- Functions: NewAliasStore(configDir) (*AliasStore, error), (*AliasStore).Get, (*AliasStore).Set, (*AliasStore).GetOrDefault, internal loadFromDisk + persist
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AliasStore with JSON persistence</name>
  <files>internal/daemon/alias_store.go, internal/daemon/alias_store_test.go</files>
  <read_first>
    - internal/daemon/chat.go (ChatStore struct lines ~84-89; NewChatStore constructor lines ~99-130; Delete lines ~347-356 — mu+filePath+map idiom + containment + 0600 write)
    - internal/daemon/engine.go (loadSettingsFromDisk lines 173-193; saveSettingsToDisk lines 242-258 — os.ReadFile/IsNotExist + json.Marshal + os.WriteFile 0600)
    - .planning/phases/152-relay-protocol-identity-presence/152-PATTERNS.md (section "internal/daemon/alias_store.go" lines 349-414 — exact analog mapping)
    - .planning/phases/152-relay-protocol-identity-presence/152-RESEARCH.md (Pattern 5 lines 499-526 — AliasStore API and path)
  </read_first>
  <behavior>
    - NewAliasStore(t.TempDir()) returns a usable store with an empty map on first run (no file)
    - Set("local:local","ken") then Get("local:local") returns ("ken", true)
    - After Set, constructing a fresh NewAliasStore(sameDir) and Get("local:local") returns ("ken", true) — persisted across reload (D-02)
    - Get("absent:web") returns ("", false)
    - GetOrDefault("absent:web","laptop") returns "laptop" (default used when no persisted alias)
    - GetOrDefault("local:local","laptop") returns "ken" after Set (persisted wins over default)
    - Set with an invalid alias (e.g. 40 runes, or containing U+0007) returns a non-nil error and does NOT persist
  </behavior>
  <action>
    Create internal/daemon/alias_store.go in package daemon. Define type AliasStore{ mu sync.RWMutex; filePath string; aliases map[string]string } (RWMutex because Get runs on every subscribe). NewAliasStore(configDir string) (*AliasStore, error): set filePath = filepath.Join(configDir, "aliases.json") (fixed hardcoded basename — no user-controlled path component, mitigating T-152-06), os.MkdirAll the configDir with 0700, init aliases as an empty map, call loadFromDisk, return the store. loadFromDisk: os.ReadFile(filePath); if os.IsNotExist return nil (first run is not an error); json.Unmarshal into the map (on unmarshal error, leave map empty and return the error). Get(personKey) (string,bool): RLock; map lookup. GetOrDefault(personKey, def string) string: if Get found return it, else return def. Set(personKey, alias string) error: validate via relay.ValidateAlias(alias) — if it returns "" return an error and do NOT mutate or persist; otherwise Lock, store the validated value, call persist (under the same lock), return persist's error. persist(): json.Marshal(a.aliases) then os.WriteFile(a.filePath, data, 0600) (single writer serialized by mu). Add alias_store_test.go using t.TempDir() as configDir covering every behavior case above, including the reload-persistence assertion and the invalid-alias rejection. Mirror the existing chat_test.go TempDir test idiom.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -race ./internal/daemon/ -run 'TestAliasStore'</automated>
  </verify>
  <acceptance_criteria>
    - internal/daemon/alias_store.go defines type AliasStore and NewAliasStore/Get/Set/GetOrDefault
    - aliases.json basename is hardcoded (grep -n 'aliases.json' internal/daemon/alias_store.go exits 0; no path component comes from personKey)
    - go test -race ./internal/daemon/ -run TestAliasStore exits 0 including a reload-after-restart persistence assertion and an invalid-alias-rejected assertion
    - Set delegates validation to relay.ValidateAlias (grep -n 'relay.ValidateAlias' internal/daemon/alias_store.go exits 0)
  </acceptance_criteria>
  <done>AliasStore persists composite-keyed aliases to a fixed-path aliases.json, survives reload, rejects invalid aliases via the shared validator, and is fully unit-tested with t.TempDir isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-process → filesystem | The personKey (derived from Tailscale identity) is used as a JSON map key, and the store writes a file under the daemon config dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-06 | Tampering | aliases.json path | low | mitigate | filePath is filepath.Join(configDir, "aliases.json") with a hardcoded basename; the personKey is only ever a JSON map key, never a path component — no path traversal is possible from user-influenced input. |
| T-152-01 | Tampering | AliasStore.Set | high | mitigate | Set delegates to relay.ValidateAlias before persisting, so the persisted store can never contain a control-char or over-length alias even if a future caller forgets to pre-validate. |
| T-152-SC | Tampering | go module installs | low | accept | No packages added — stdlib only (encoding/json, os, path/filepath, sync). |
</threat_model>

<verification>
- go test -race ./internal/daemon/ -run TestAliasStore green
- aliases.json written with 0600 perms at the fixed config-dir path
- No import cycle introduced (daemon already imports relay)
</verification>

<success_criteria>
- AliasStore.Set + reload returns the persisted alias (D-02 restart survival)
- Composite personKey keying keeps owner vs same-machine-browser aliases separate
- Invalid aliases never reach disk (T-152-01 defense in depth; T-152-06 path fixed)
</success_criteria>

<output>
Create `.planning/phases/152-relay-protocol-identity-presence/152-02-SUMMARY.md` when done
</output>
