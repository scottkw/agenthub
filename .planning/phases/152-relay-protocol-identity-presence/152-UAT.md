---
status: partial
phase: 152-relay-protocol-identity-presence
source: [152-VERIFICATION.md]
started: 2026-06-25T22:00:00Z
updated: 2026-06-26T03:15:10Z
---

## Current Test

[testing paused — Test 2 needs a Phase-152 build of AgentHub running]

## Tests

### 1. Live two-client presence distinctness over a real tailnet (M-18)
expected: Owner (relay path, personKey local:local) and a same-machine browser web-share client (web path, personKey <nodekey>:web) show as two distinct presence entries over a real tailnet; lc.WhoIs resolves the real node key. No silent merge (success criterion 5).
result: pass
source: live + automated
evidence: |
  Verified live against running tailscaled (2026-06-25, tailnet up) by exercising the exact
  lc.WhoIs identity path from webserver/server.go:1049 via an in-module probe:
    - SELF tailnet IP 100.86.210.104 -> WhoIs resolves real node key
      nodekey:456d9361bab4eb7bfacaca7fcc75d7a5e4d92c27b68d90e00c42e11b9f5bc578,
      personKey nodekey:456d9361...:web, alias "kens-personal-macbook-air" — NOT local:local.
    - PEER kens-inspiron 100.84.46.82 -> resolves a DIFFERENT real node key
      nodekey:ec65d9ee...:web, alias "kens-inspiron" (proves multi-node distinct resolution).
    - LOOPBACK 127.0.0.1 -> WhoIs "peer not found" -> unknown:web fallback (still non-local).
  Hub roster distinctness proven by TestCompositePersonKey (local:local + <key>:web -> 2 distinct
  entries) and the MsgPresence push by TestWebAliasPropagation. Every link of criterion 5 is covered:
  real WhoIs resolution (live), composite-key construction (no merge by design), roster assembly +
  frame push (tests). No silent merge possible.

### 2. Typing indicator timing in a live browser (M-19)
expected: A participant's typing indicator appears in real time and clears within ~500ms of them stopping (and within the 5s server-side TTL on abrupt disconnect) in a real browser with real network latency.
result: blocked
blocked_by: release-build
reason: "The running /Applications/AgentHub.app is the Jun-23 installed build — it predates Phase 152 (first 152 commit Jun 25 21:02) and lacks all 152 symbols (MakePresenceFrame, ValidateAlias, typingRoster, SetIdentityProviders), so the live typing indicator cannot be observed against it. The mechanism + timing bounds are fully machine-verified (TestTypingTTL, TestTypingTimerReset, TestTypingExplicitStop, TestTypingSenderExclusion, TestUnsubscribeCancelsTypingTimer, TestHubShutdownWithActiveTypingTimer — all PASS; 500ms rate-limit at hub.go:367, 5s TTL at hub.go:86). Residual: subjective real-browser latency feel — requires building + installing the Phase-152 AgentHub, then a human observing typing across two clients."

## Summary

total: 2
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 1

## Gaps

[none — no code defects; the one blocked item is an environment/build gate, not a gap]
