---
status: partial
phase: 152-relay-protocol-identity-presence
source: [152-VERIFICATION.md]
started: 2026-06-25T22:00:00Z
updated: 2026-06-26T03:11:55Z
---

## Current Test

[testing paused — live residual blocked on environment]

## Tests

### 1. Live two-client presence distinctness over a real tailnet (M-18)
expected: Owner (relay path, personKey local:local) and a same-machine browser web-share client (web path, personKey <nodekey>:web) show as two distinct presence entries over a real tailnet; lc.WhoIs resolves the real node key. No silent merge (success criterion 5).
result: blocked
blocked_by: physical-device
reason: "Tailscale is stopped on this host and no second tailnet node / running GUI is available, so a real lc.WhoIs node-key resolution cannot be observed here. Automatable substrate machine-verified 2026-06-25: TestCompositePersonKey (local:local + k:web -> 2 distinct entries), TestWebIdentity_WhoIsFailureFallback (web -> unknown:web, never local:local), TestPresenceCollapse, TestPresenceRefCount, TestEmptyPersonKeyNoPresenceEntry — all PASS. Residual requiring a live tailnet + second node: confirm WhoIs resolves the actual node key (vs the unknown fallback)."

### 2. Typing indicator timing in a live browser (M-19)
expected: A participant's typing indicator appears in real time and clears within ~500ms of them stopping (and within the 5s server-side TTL on abrupt disconnect) in a real browser with real network latency.
result: blocked
blocked_by: release-build
reason: "Requires the running app + a real browser with a human observing latency; no GUI/web-share server is running on this host. Mechanism + timing bounds machine-verified 2026-06-25: TestTypingTTL (auto typing:false within TTL), TestTypingTimerReset (no early fire on keystroke), TestTypingExplicitStop, TestTypingSenderExclusion, TestUnsubscribeCancelsTypingTimer, TestHubShutdownWithActiveTypingTimer — all PASS; 500ms rate-limit constant at hub.go:367, 5s default TTL at hub.go:86. Residual requiring a live browser: subjective real-network latency feel."

## Summary

total: 2
passed: 0
issues: 0
pending: 0
skipped: 0
blocked: 2

## Gaps

[none — no code defects; blocked items are environment/prerequisite gates, not gaps]
