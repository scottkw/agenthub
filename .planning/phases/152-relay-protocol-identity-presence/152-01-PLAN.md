---
phase: 152-relay-protocol-identity-presence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/relay/protocol.go
  - internal/relay/protocol_presence_test.go
autonomous: true
requirements: [IDENT-01, IDENT-02, PRESENCE-01, PRESENCE-02]
must_haves:
  truths:
    - "internal/relay/protocol.go defines the five chat/presence frame constants 0x30-0x34 with MsgPresence=0x32, MsgTyping=0x33, MsgAliasSet=0x34"
    - "PresencePayload, TypingPayload, AliasPayload round-trip through JSON marshal/unmarshal preserving every field"
    - "ValidateAlias rejects empty/all-whitespace input, input over 32 runes, and any rune containing a C0 (<U+0020) or C1 (U+007F-U+009F) control character; accepts trimmed printable Unicode unchanged"
  artifacts:
    - internal/relay/protocol.go
    - internal/relay/protocol_presence_test.go
  key_links:
    - "MsgPresence/MsgTyping/MsgAliasSet constant byte values are the canonical wire contract consumed by hub.go (Plan 03), both read pumps (Plans 05/06), and relayClient.ts (Plan 04)"
    - "ValidateAlias is exported so both the relay and webserver read pumps validate alias input identically (Pitfall 3 — consistent validation across surfaces)"
---

<objective>
Add the Phase 152 wire-protocol foundation to internal/relay/protocol.go: the five
chat/presence frame-type constants (0x30-0x34), the JSON payload structs that ride
them, their Make*Frame encoders, and the exported ValidateAlias function. Define all
five constants now to lock the wire protocol; this phase only dispatches 0x32/0x33/0x34
(MsgChat 0x30 / MsgChatSend 0x31 are reserved stubs for Phase 154).

This is the foundation every other Plan 152 task builds on — the Hub roster (Plan 03),
both WS read pumps (Plans 05/06), and the TypeScript parser (Plan 04) all import these
symbols. Implements the wire shape for D-04 (composite PersonKey carried in PresenceEntry)
and D-05 (TypingPayload carries the typer's alias, not just a count).

Purpose: lock the protocol contract before any consumer is written, so waves 2-4 build
against fixed symbols.
Output: extended protocol.go + new protocol_presence_test.go covering frame encode/decode
round-trip and alias validation bounds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-relay-protocol-identity-presence/152-RESEARCH.md
@.planning/phases/152-relay-protocol-identity-presence/152-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates (Phase 152 wire-protocol surface):
- Frame constants: MsgChat 0x30, MsgChatSend 0x31, MsgPresence 0x32, MsgTyping 0x33, MsgAliasSet 0x34
- Structs: PresenceEntry, PresencePayload, TypingPayload, AliasPayload
- Functions: MakePresenceFrame, MakeTypingFrame, MakeAliasSetFrame, ValidateAlias
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add frame constants, payload structs, and Make*Frame encoders</name>
  <files>internal/relay/protocol.go, internal/relay/protocol_presence_test.go</files>
  <read_first>
    - internal/relay/protocol.go (existing constant block lines 11-22; MetaPayload + MakeMeta lines 60-73; ParseFrame line 53)
    - .planning/phases/152-relay-protocol-identity-presence/152-PATTERNS.md (section "internal/relay/protocol.go" lines 26-76 — exact constant block + MakeMeta template to mirror)
    - .planning/phases/152-relay-protocol-identity-presence/152-RESEARCH.md (Pattern 1, lines 181-242 — constant values, struct field names, json tags)
  </read_first>
  <behavior>
    - MakePresenceFrame(PresencePayload{Participants:[{PersonKey:"local:local",Alias:"ken",ConnCount:1}]}) returns a frame whose frame[0]==MsgPresence (0x32) and whose frame[1:] json-unmarshals back to an equal PresencePayload
    - MakeTypingFrame(TypingPayload{PersonKey:"k:web",Alias:"sam",Typing:true}) returns frame[0]==MsgTyping (0x33); round-trips
    - MakeAliasSetFrame(AliasPayload{Alias:"ken"}) returns frame[0]==MsgAliasSet (0x34); round-trips
    - PresenceEntry has json tags personKey, tailnetID, origin, alias, connCount; TypingPayload has personKey/alias as omitempty and typing as non-omitempty bool
  </behavior>
  <action>
    Append a new const block to protocol.go immediately after the MsgMeta block, mirroring the existing 0x01-0x20 style: MsgChat byte = 0x30, MsgChatSend byte = 0x31, MsgPresence byte = 0x32, MsgTyping byte = 0x33, MsgAliasSet byte = 0x34, with a leading comment that 0x30-0x3F is reserved for chat/presence and that 0x30/0x31 are Phase 154 dispatch stubs. Add structs PresenceEntry{PersonKey,TailnetID,Origin,Alias string; ConnCount int} with json tags personKey/tailnetID/origin/alias/connCount; PresencePayload{Participants []PresenceEntry `json:"participants"`}; TypingPayload{PersonKey string `json:"personKey,omitempty"`; Alias string `json:"alias,omitempty"`; Typing bool `json:"typing"`}; AliasPayload{Alias string `json:"alias"`}. Add encoders MakePresenceFrame(p PresencePayload) []byte, MakeTypingFrame(p TypingPayload) []byte, MakeAliasSetFrame(p AliasPayload) []byte — each identical in shape to MakeMeta: json.Marshal the payload, allocate a 1+len(b) byte frame, set frame[0] to the matching constant, copy the body. Create protocol_presence_test.go with one round-trip test per payload type asserting the type byte and field equality (mirror the PATTERNS.md round-trip pattern at lines 450-459). Do NOT add a read-pump dispatch case here — that is Plans 05/06.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test ./internal/relay/ -run 'TestPresencePayloadRoundTrip|TestTypingPayloadRoundTrip|TestAliasPayloadRoundTrip' && grep -nE 'MsgPresence +byte = 0x32' internal/relay/protocol.go && grep -nE 'MsgTyping +byte = 0x33' internal/relay/protocol.go && grep -nE 'MsgAliasSet +byte = 0x34' internal/relay/protocol.go</automated>
  </verify>
  <acceptance_criteria>
    - internal/relay/protocol.go contains MsgPresence byte = 0x32, MsgTyping byte = 0x33, MsgAliasSet byte = 0x34 (and reserved MsgChat 0x30, MsgChatSend 0x31)
    - PresenceEntry, PresencePayload, TypingPayload, AliasPayload structs exist with the json tags above
    - MakePresenceFrame, MakeTypingFrame, MakeAliasSetFrame exist and set the correct leading type byte
    - go test ./internal/relay/ -run round-trip tests exits 0
  </acceptance_criteria>
  <done>The three new payload types encode and decode losslessly; the five frame constants are defined with the exact byte values; round-trip tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add exported ValidateAlias with 32-rune printable bounds</name>
  <files>internal/relay/protocol.go, internal/relay/protocol_presence_test.go</files>
  <read_first>
    - internal/relay/protocol.go (place ValidateAlias alongside the new constants, per Pitfall 3)
    - .planning/phases/152-relay-protocol-identity-presence/152-RESEARCH.md (Pattern 5 lines 528-556 — exact validation logic and rationale; 32-rune cap, trim-then-reject, C0/C1 rejection)
  </read_first>
  <behavior>
    - ValidateAlias("  ken  ") returns "ken" (trimmed)
    - ValidateAlias("") and ValidateAlias("   ") return "" (rejected)
    - ValidateAlias(string of 33 runes) returns "" (rejected, not truncated); 32 runes accepted
    - ValidateAlias("a\x07b") returns "" (C0 control rejected); ValidateAlias("ab") returns "" (C1 control rejected)
    - ValidateAlias("José 日本語") returns "José 日本語" unchanged (printable Unicode accepted)
  </behavior>
  <action>
    Add exported func ValidateAlias(raw string) string to protocol.go. Trim leading/trailing whitespace with strings.TrimSpace; return "" if empty after trim. Convert to []rune; return "" if len > 32 (reject, do not truncate — caller informs sender, per D-02 alias-is-a-label semantics). Iterate runes; return "" if any rune r < 0x0020 (C0 controls) or r >= 0x007F && r <= 0x009F (DEL + C1 controls). Otherwise return the trimmed string. Export it (capital V) because internal/webserver imports internal/relay and both read pumps must validate identically (Pitfall 3 — single source of truth). Add table-driven tests to protocol_presence_test.go covering: trim, empty/whitespace reject, 32-accept/33-reject boundary, C0 reject, C1 reject, multibyte-printable accept. Mitigates T-152-01 (control-char/injection tampering) at the validation source.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test ./internal/relay/ -run TestValidateAlias && grep -nE 'func ValidateAlias' internal/relay/protocol.go</automated>
  </verify>
  <acceptance_criteria>
    - internal/relay/protocol.go contains func ValidateAlias(raw string) string (exported)
    - go test ./internal/relay/ -run TestValidateAlias exits 0 covering trim, empty, 32/33-rune boundary, C0, C1, multibyte cases
    - 33-rune input returns "" (rejected, not truncated)
  </acceptance_criteria>
  <done>ValidateAlias enforces the 32-rune printable-Unicode bound, rejecting control characters and over-length input; table tests cover every documented case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server (WS frame) | Untrusted alias text and typing payloads arrive as JSON over the relay WebSocket and are decoded here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-01 | Tampering | ValidateAlias / AliasPayload | high | mitigate | ValidateAlias rejects any rune <U+0020 or in U+007F-U+009F and any input >32 runes; exported so both read pumps (Plans 05/06) apply it identically. Alias is rendered in other clients' rosters in Phases 154/155, so control-char/markup stripping at ingress is the primary XSS defense. |
| T-152-SC | Tampering | go module installs | low | accept | No packages added this plan — stdlib only (encoding/json, strings, unicode). No legitimacy gate required. |
</threat_model>

<verification>
- go test ./internal/relay/ exits 0 (new tests + existing protocol tests unaffected)
- The five frame constants and ValidateAlias are present and exported as specified
- No read-pump dispatch added in this plan (deferred to Plans 05/06)
</verification>

<success_criteria>
- All five frame constants 0x30-0x34 defined with MsgPresence=0x32, MsgTyping=0x33, MsgAliasSet=0x34
- Three payload structs + three Make*Frame encoders round-trip losslessly
- ValidateAlias enforces 32-rune printable bound (T-152-01 mitigated at source)
- go test -race ./internal/relay/... green
</success_criteria>

<output>
Create `.planning/phases/152-relay-protocol-identity-presence/152-01-SUMMARY.md` when done
</output>
