---
phase: 152-relay-protocol-identity-presence
plan: 05
type: execute
wave: 3
depends_on: ["152-01", "152-02", "152-03"]
files_modified:
  - internal/daemon/engine.go
  - internal/relay/server.go
  - internal/daemon/api.go
  - internal/relay/server_identity_test.go
autonomous: true
requirements: [IDENT-01, IDENT-02, PRESENCE-01, PRESENCE-02]
must_haves:
  truths:
    - "The daemon owns one global AliasStore (constructed in NewSessionEngine over daemonConfigDir, overridable for tests) reachable by both WS paths"
    - "The relay/loopback path (handleSession) stamps every owner connection with TailnetID='local', Origin='local', PersonKey='local:local' and an alias resolved from the AliasStore (default = engine hostname) before hub.Subscribe — satisfying IDENT-01 for the owner and the 'local' half of D-04"
    - "After Subscribe the handler calls NotifyPresence, and on disconnect it calls NotifyPresence only when Unsubscribe reports the last connection dropped (PRESENCE-01 real-time join/leave)"
    - "A MsgAliasSet frame over the relay validates+persists the alias and propagates a fresh MsgPresence roster to all clients within one round-trip (IDENT-02); a MsgTyping frame drives hub.UpdateTyping (PRESENCE-02)"
    - "Read-only (RO-cap) connections are NOT blocked from MsgAliasSet or MsgTyping — only MsgInput remains ReadOnly-gated (D-06)"
  artifacts:
    - internal/daemon/engine.go
    - internal/relay/server.go
    - internal/daemon/api.go
    - internal/relay/server_identity_test.go
  key_links:
    - "relay.Server gains identity-provider closures (owner default alias + AliasStore get/set), wired by the daemon in RelayHandler — same import-cycle-avoiding callback pattern as setChatProviders"
    - "The Unsubscribe call site is updated for the new (bool) signature; alias callback is wired BEFORE Subscribe so a persisted alias is used on (re)connect (Pitfall 6)"
---

<objective>
Wire the AliasStore into the daemon and complete the relay/loopback WS path. Add the
global AliasStore to SessionEngine, expose it, thread its get/set closures plus the owner
default alias into relay.Server, and extend handleSession to stamp owner identity
(D-01/D-04 'local' half), broadcast presence on subscribe/unsubscribe (PRESENCE-01), and
dispatch MsgAliasSet (IDENT-02) and MsgTyping (PRESENCE-02) in the read pump. RO-cap
connections stay full chat participants — only MsgInput remains gated (D-06).

Depends on Plan 01 (frame types/ValidateAlias), Plan 02 (AliasStore), Plan 03 (Hub
presence/typing methods + new Unsubscribe signature).

Purpose: deliver the owner-side end-to-end identity/presence/typing path with real
persistence wiring.
Output: AliasStore on the engine, relay.Server identity providers, completed
handleSession, and a relay integration test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-relay-protocol-identity-presence/152-RESEARCH.md
@.planning/phases/152-relay-protocol-identity-presence/152-PATTERNS.md
@internal/relay/server.go
@internal/daemon/engine.go
@internal/daemon/api.go
</context>

<artifacts_this_phase_produces>
This plan creates:
- SessionEngine.aliasStore field + construction in NewSessionEngine + Aliases() accessor (+ test override)
- relay.Server identity-provider fields + SetIdentityProviders setter
- handleSession identity stamping (local) + NotifyPresence on subscribe/unsubscribe + MsgTyping/MsgAliasSet read-pump cases
- internal/relay/server_identity_test.go integration test
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the global AliasStore to SessionEngine</name>
  <files>internal/daemon/engine.go</files>
  <read_first>
    - internal/daemon/engine.go (SessionEngine struct fields incl hostname line 30, configDir line 32, chatStores line 43; daemonConfigDir lines 67-75; NewSessionEngine lines 295-313; ConfigDirForTest lines 261-268)
    - internal/daemon/alias_store.go (NewAliasStore signature — from Plan 02)
    - .planning/phases/152-relay-protocol-identity-presence/152-PATTERNS.md (lines 412-414 — chatStores map wiring analog)
  </read_first>
  <action>
    Add field aliasStore *AliasStore to SessionEngine alongside chatStores. In NewSessionEngine, after cfgDir is derived, construct it: store, _ := NewAliasStore(cfgDir); assign to the struct (tolerate the error like loadSettingsFromDisk does — a failed load must not crash daemon startup; log if a logger is handy). Add accessor func (e *SessionEngine) Aliases() *AliasStore returning e.aliasStore. So that ConfigDirForTest stays meaningful, have it (or a new test helper) reconstruct the aliasStore over the new dir if tests rely on isolated persistence — mirror how ConfigDirForTest already overrides configDir. Do NOT change CreateSession/KillSession: the AliasStore is global (per-person), not per-session, so it is created once and never torn down with a session.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./... && go test -race ./internal/daemon/ -run 'TestNewSessionEngine|TestAliasStore'</automated>
  </verify>
  <acceptance_criteria>
    - SessionEngine has an aliasStore *AliasStore field constructed in NewSessionEngine (grep -nE 'aliasStore' internal/daemon/engine.go exits 0)
    - func (e *SessionEngine) Aliases() *AliasStore exists
    - go build ./... succeeds; daemon tests still pass
  </acceptance_criteria>
  <done>The engine constructs and exposes one global AliasStore over the daemon config dir, with a test-override path consistent with ConfigDirForTest.</done>
</task>

<task type="auto">
  <name>Task 2: Identity stamping + presence + alias/typing dispatch in the relay read pump</name>
  <files>internal/relay/server.go, internal/daemon/api.go</files>
  <read_first>
    - internal/relay/server.go (handleSession 177-302: Subscriber creation 218-225, Subscribe+NotifyViewerCount+defer 229-234, read-pump switch 267-284, NotifyViewerCount 365-372; Server struct 14-18)
    - internal/daemon/api.go (RelayHandler 260-275; setChatProviders 492 — the callback-wiring analog)
    - .planning/phases/152-relay-protocol-identity-presence/152-PATTERNS.md (section "internal/relay/server.go" lines 226-313 — exact extend points)
    - .planning/phases/152-relay-protocol-identity-presence/152-RESEARCH.md (Pattern 4 relay block lines 487-497; read-pump additions lines 425-451)
  </read_first>
  <action>
    On relay.Server add three identity-provider fields and a setter SetIdentityProviders(ownerDefaultAlias string, getAlias func(personKey, def string) string, setAlias func(personKey, alias string)); store them on the struct (nil-safe). In RelayHandler (api.go), after relay.NewServer(...), call server.SetIdentityProviders(a.engine.Hostname-or-equivalent, a.engine.Aliases().GetOrDefault, a.engine.Aliases().Set) — use the engine hostname as the owner default and the AliasStore methods as the closures (guard if Aliases() is nil). In handleSession, before hub.Subscribe: set sub.TailnetID="local", sub.Origin="local", sub.PersonKey="local:local"; compute alias := getAlias("local:local", ownerDefault) if the provider is set else ownerDefault; set sub.Alias=alias and sub.AliasSetFn=func(key,a string){ if setAlias != nil { setAlias(key,a) } }. After hub.Subscribe(sub); NotifyViewerCount(hub) add NotifyPresence(hub). Update the defer to capture presenceChanged := hub.Unsubscribe(sub) (new bool signature), call NotifyViewerCount(hub), and call NotifyPresence(hub) only if presenceChanged. In the read-pump switch add two cases after MsgPing: case MsgTyping: json.Unmarshal payload into TypingPayload; on success call hub.UpdateTyping(sub, tp.Typing). case MsgAliasSet: json.Unmarshal into AliasPayload; if newAlias := ValidateAlias(ap.Alias); newAlias != "" { sub.Alias = newAlias; if sub.AliasSetFn != nil { sub.AliasSetFn(sub.PersonKey, newAlias) }; hub.UpdateAlias(sub.PersonKey, newAlias); NotifyPresence(hub) }. Add func NotifyPresence(hub *Hub) here only if Plan 03 placed it in hub.go — otherwise it already exists; do not duplicate. CRITICAL (D-06): place both new cases OUTSIDE any ReadOnly guard — only MsgInput stays gated on sub.ReadOnly; RO clients must be able to set alias and type.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./... && go vet ./internal/relay/... ./internal/daemon/...</automated>
  </verify>
  <acceptance_criteria>
    - relay.Server has SetIdentityProviders; api.go RelayHandler wires engine hostname + AliasStore Get/Set
    - handleSession stamps PersonKey="local:local" and calls NotifyPresence after Subscribe and (conditionally) on Unsubscribe
    - read-pump has case MsgTyping and case MsgAliasSet; neither is inside the ReadOnly/MsgInput gate (grep context shows MsgAliasSet/MsgTyping not guarded by sub.ReadOnly)
    - go build ./... and go vet succeed
  </acceptance_criteria>
  <done>The relay path stamps owner identity, broadcasts presence on join/leave, and dispatches alias-set and typing frames — with RO clients unrestricted for chat actions (D-06).</done>
</task>

<task type="auto">
  <name>Task 3: Relay integration test — alias propagation + typing + presence over a real WS</name>
  <files>internal/relay/server_identity_test.go</files>
  <read_first>
    - internal/relay/server_test.go (setupTestServer harness lines 21-49; websocket.Dial + frame read patterns; TestHub_TwoClientsFanOut lines 204+)
    - internal/relay/server.go (SetIdentityProviders + handleSession from Task 2)
  </read_first>
  <behavior>
    - Two WS clients connect to the same test session; each receives a MsgPresence (0x32) frame listing the owner entry (PersonKey "local:local")
    - Client A sends a MsgAliasSet frame {"alias":"ken"}; both clients receive a MsgPresence frame whose roster entry alias == "ken" (propagation within one round-trip, IDENT-02)
    - Client A sends a MsgTyping {"typing":true}; client B receives a MsgTyping (0x33) frame with typing:true; client A does NOT receive its own typing frame (sender-exclusion)
    - A read-only client (?readonly=1) can still send MsgAliasSet and see it accepted (D-06)
  </behavior>
  <action>
    Create internal/relay/server_identity_test.go. Extend the setupTestServer pattern (or add a sibling helper) to call srv := httptest.NewServer of a relay.Server that has SetIdentityProviders wired to an in-memory map (ownerDefault "host", get/set against a map[string]string under a mutex) — this stands in for the daemon AliasStore without importing daemon. Dial two websockets, drain initial presence frames, then exercise: alias-set propagation (assert both clients see alias "ken"), typing broadcast with sender-exclusion (assert B receives typing:true, A times out / receives nothing within a short window), and a readonly client successfully setting an alias. Use parseServerFrame-equivalent Go-side decoding (ParseFrame + json.Unmarshal into PresencePayload/TypingPayload). Keep timeouts short (sub-second) to stay under the 60s budget.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -race ./internal/relay/ -run 'TestRelayIdentity|TestRelayAliasPropagation|TestRelayTypingExcludesSender|TestRelayReadOnlyCanChat'</automated>
  </verify>
  <acceptance_criteria>
    - server_identity_test.go exists and exercises handleSession over real websockets
    - alias-set propagation, typing sender-exclusion, and RO-can-chat (D-06) are each asserted
    - go test -race for the listed tests exits 0
  </acceptance_criteria>
  <done>An integration test proves alias changes propagate to all relay clients in one round-trip, typing excludes the sender, and read-only clients are full chat participants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner webview→relay (loopback) | The Wails owner connects over 127.0.0.1; trusted as 'local' but its alias/typing payloads still pass through validation |
| RO-cap client→relay | A read-only client may emit MsgAliasSet/MsgTyping; must be allowed for chat but never for PTY input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-01 | Tampering | handleSession MsgAliasSet | high | mitigate | The read-pump case calls relay.ValidateAlias before persisting/broadcasting; control-char/over-length aliases are rejected on the relay surface identically to the web surface (Pitfall 3). |
| T-152-05 | Elevation of Privilege | RO-cap chat vs PTY | low | accept | D-06: MsgAliasSet/MsgTyping are deliberately NOT ReadOnly-gated (benign chat actions); only MsgInput remains gated on sub.ReadOnly. The integration test asserts RO clients can chat but the existing read-only-input-discarded test still passes, proving PTY input stays gated. |
| T-152-11 | Spoofing | owner identity 'local' | low | accept | The loopback path is reachable only on 127.0.0.1; stamping a fixed 'local' sentinel is correct and never passed through WhoIs. |
| T-152-SC | Tampering | go module installs | low | accept | No packages added — stdlib + existing relay/daemon code only. |
</threat_model>

<verification>
- go build ./... and go vet clean
- go test -race ./internal/relay/... ./internal/daemon/... green (new integration test + existing read-only-input-discarded test both pass — D-06 without PTY escalation)
- Unsubscribe new signature consumed correctly at the relay call site
</verification>

<success_criteria>
- Owner connections stamped local:local with a persisted/default alias before any Subscribe (IDENT-01)
- Alias-set propagates to all relay clients in one round-trip (IDENT-02); presence updates on join/leave (PRESENCE-01); typing dispatches with sender-exclusion (PRESENCE-02)
- RO clients are full chat participants; PTY input remains ReadOnly-gated (D-06)
</success_criteria>

<output>
Create `.planning/phases/152-relay-protocol-identity-presence/152-05-SUMMARY.md` when done
</output>
