---
phase: 133-attention-pulse
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/style.css
autonomous: true
requirements: [ATTN-01, ATTN-02, ATTN-03, ATTN-06, A11Y-01, A11Y-03]
must_haves:
  truths:
    - "Attention cards show an amber-gold border that pulses (box-shadow glow) over 2s — only when prefers-reduced-motion is no-preference"
    - "Under prefers-reduced-motion: reduce the attention card shows a static border, no animation, no glow"
    - "An attention card keeps its amber border on hover (hover does not override the attention signal)"
    - "BellAlertIcon in the card and in the collapsed-group badge render at explicit pixel sizes (no Tailwind)"
  artifacts:
    - path: "frontend/src/style.css"
      provides: "Phase 133 attention tokens, .hub-card--attention modifier, hub-attn-pulse keyframe, reduced-motion guards, attn icon + badge sizing rules"
      contains: ".hub-card--attention"
  key_links:
    - from: ".hub-card--attention animation"
      to: "@media (prefers-reduced-motion: no-preference)"
      via: "animation declaration gated inside the no-preference guard"
      pattern: "prefers-reduced-motion: no-preference"
    - from: ".hub-card__attn-icon svg"
      to: "explicit 16px sizing"
      via: "CSS width/height (no Tailwind)"
      pattern: "width: 16px"
---

<objective>
Add all Phase 133 CSS to `style.css`: the `--hub-attn-*` tokens (dark + light themes), the `.hub-card--attention` modifier, the `hub-attn-pulse` keyframe, the reduced-motion guards (animated under no-preference, static under reduce), the hover override, the 400ms clear transition, the `.hub-card__attn-icon` wrapper + svg sizing, and the `.hub__group-sidebar-item__attn-badge` + svg sizing.

Purpose: CSS is independent of the component code (no imports), so it runs in Wave 1 in parallel with the predicate (Plan 01). Defining the classes/tokens first means the Wave 2/3 components attach class names that already have styling.

Output: Appended CSS blocks in `style.css`. No TS changes.

This is the colorblind-safe + reduced-motion enforcement layer. Every color rule MUST carry the COLORBLIND-SAFE source comment (the user is colorblind — verification is at source level against hex constants, never by eye). Every animation MUST be gated by the reduced-motion guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-attention-pulse/133-UI-SPEC.md
@.planning/phases/133-attention-pulse/133-RESEARCH.md
@.planning/phases/133-attention-pulse/133-PATTERNS.md

<interfaces>
<!-- Exact style.css anchor line numbers — extracted from codebase. Do NOT re-grep; use these. -->

style.css anchors (frontend/src/style.css):
- `:root` dark token block — `--hub-needs-input-badge-*` tokens at lines 4130-4131 (append new attn tokens to this block)
- `[data-ui-theme="light"]` block — `--hub-needs-input-badge-*` at lines 4174-4175 (append light attn tokens to this block)
- `.hub-card` base transition `transition: border-color 100ms ease, background 100ms ease;` at line 4288
- `.hub-card:hover` at line 4291 (sets border-color: var(--hub-border-hover) — this is the rule that would override the attention border)
- `@media (prefers-reduced-motion: no-preference)` spin guard at line 4359; `@keyframes hub-spin` at line 4367 (keyframe declared OUTSIDE any media query — copy this gating structure)
- `.hub__group-sidebar-toggle svg` icon-sizing analog at line 4710
- `.hub__group-sidebar-item__needs-input-badge` at line 4794; its `svg { width:12px; height:12px }` rule at line 4716
- `.hub-card--dragging` modifier at line 4849 (append `.hub-card--attention` near the other modifiers)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add attention tokens (dark + light)</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css lines 4120-4180 (the :root and [data-ui-theme="light"] token blocks where --hub-needs-input-badge-* live — the exact analog for appending new tokens)
    - .planning/phases/133-attention-pulse/133-UI-SPEC.md (Design System tables — locked hex values)
    - .planning/phases/133-attention-pulse/133-PATTERNS.md (style.css Analog 1 — token block pattern + comment format)
  </read_first>
  <action>
    Append the six dark-theme tokens to the `:root` block (near line 4131, before the block's closing brace): `--hub-attn-border: #e0af68`, `--hub-attn-border-glow: rgba(224,175,104,0.35)`, `--hub-attn-icon-color: #e0af68`, `--hub-attn-badge-bg: rgba(224,175,104,0.18)`, `--hub-attn-badge-text: #e0af68`, `--hub-attn-static-border: #e0af68`. Lead the block with `/* === Phase 133: Attention + Pulse tokens === */` and the COLORBLIND-SAFE comment: `/* COLORBLIND-SAFE: attn border dark hex #e0af68 — reinforcement only; BellAlertIcon + pulse shape carries state */`.

    Append the six light-theme overrides to the `[data-ui-theme="light"]` block (near line 4175): same variable names with values `#b45309`, `rgba(180,83,9,0.25)`, `#b45309`, `rgba(180,83,9,0.14)`, `#b45309`, `#b45309`. Lead with `/* COLORBLIND-SAFE: attn border light hex #b45309 — WCAG AA 7.6:1 on white; icon carries state */`.

    Use the exact hex values from the UI-SPEC Design System tables — do not substitute or "improve" them. The amber-gold #e0af68 is deliberately distinct from --hub-accent (blue) and --hub-warning (#f59e0b).
  </action>
  <verify>
    <automated>cd frontend && grep -v '^[[:space:]]*/\*' src/style.css | grep -c -- '--hub-attn-border:'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--hub-attn-border:' frontend/src/style.css` returns 2 (one dark, one light)
    - All six --hub-attn-* tokens present in BOTH the :root and the light-theme blocks
    - Dark values are #e0af68 family; light values are #b45309 family (per UI-SPEC, byte-exact)
    - Each token block carries a COLORBLIND-SAFE source comment
  </acceptance_criteria>
  <done>Both theme blocks define all six --hub-attn-* tokens with the locked hex values and colorblind-safe comments.</done>
</task>

<task type="auto">
  <name>Task 2: Add attention modifier, pulse keyframe, reduced-motion guards, hover override, clear transition</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css lines 4288-4294 (.hub-card base transition + .hub-card:hover — the hover rule that must be overridden)
    - frontend/src/style.css lines 4358-4370 (the hub-spin no-preference guard + @keyframes hub-spin — exact gating structure to copy)
    - frontend/src/style.css lines 4845-4852 (.hub-card--dragging modifier — where to place .hub-card--attention)
    - .planning/phases/133-attention-pulse/133-UI-SPEC.md (ATTN-01 Pulsing Border + Reduced-Motion Fallback + Clear Animation — locked CSS)
    - .planning/phases/133-attention-pulse/133-PATTERNS.md (style.css Analog 2/3/4/5)
  </read_first>
  <action>
    Add `.hub-card--attention { border-color: var(--hub-attn-border); }` near `.hub-card--dragging` (line 4849). Prefix with the three source comments: `/* ATTN-01: attention card — pulsing border + glow (colorblind-safe: BellAlertIcon carries state) */`, `/* COLORBLIND-SAFE: attn border dark hex #e0af68 — reinforcement only; BellAlertIcon + pulse shape carries state */`, `/* COLORBLIND-SAFE: attn border light hex #b45309 — WCAG AA 7.6:1 on white; icon carries state */`.

    Add a `@media (prefers-reduced-motion: no-preference)` block (mirroring the line 4359 hub-spin guard) containing three rules: (1) `.hub-card--attention { animation: hub-attn-pulse 2s ease-in-out infinite; }`; (2) override `.hub-card { transition: border-color 400ms ease, box-shadow 400ms ease, background 100ms ease; }` (this overrides the base 100ms transition for motion-enabled users only — ATTN-03 clear); (3) `.hub-card--attention:hover { border-color: var(--hub-attn-border); }` (keeps amber on hover, defeating the line 4291 hover rule). Lead with `/* A11Y-03: pulse wrapped in @media (prefers-reduced-motion: no-preference); static border fallback */` and `/* ATTN-03: attention clear — override 100ms base transition for motion-enabled users only */`.

    Declare `@keyframes hub-attn-pulse` OUTSIDE any media query (mirroring @keyframes hub-spin at line 4367): three stops at 0%/50%/100%, all keeping `border-color: var(--hub-attn-border)`, with `box-shadow: 0 0 0 0 var(--hub-attn-border-glow)` at 0% and 100%, `box-shadow: 0 0 0 4px var(--hub-attn-border-glow)` at 50%.

    Add a `@media (prefers-reduced-motion: reduce)` block: `.hub-card--attention { border-color: var(--hub-attn-static-border); box-shadow: none; animation: none; }`. Lead with `/* Reduced-motion: static attention border only — no animation, no glow */`.

    The `animation:` declaration MUST live inside the no-preference guard — never outside it. The keyframe declaration itself goes outside (unused keyframes do not run). Do NOT add the 400ms transition outside the guard (reduced-motion users keep the instant base behavior).
  </action>
  <verify>
    <automated>cd frontend && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `.hub-card--attention { border-color: var(--hub-attn-border) }` exists as a base rule (outside any media query)
    - The `animation: hub-attn-pulse` declaration appears ONLY inside a `@media (prefers-reduced-motion: no-preference)` block — `grep -B3 'animation: hub-attn-pulse' frontend/src/style.css` shows the no-preference guard above it
    - `@keyframes hub-attn-pulse` is declared outside any media query
    - A `@media (prefers-reduced-motion: reduce)` block sets `.hub-card--attention { animation: none; box-shadow: none }`
    - `.hub-card--attention:hover { border-color: var(--hub-attn-border) }` exists (hover override)
    - `cd frontend && pnpm build` succeeds (CSS parses)
  </acceptance_criteria>
  <done>The attention modifier, gated pulse animation, static reduced-motion fallback, hover override, and 400ms clear transition are all present and the bundle builds.</done>
</task>

<task type="auto">
  <name>Task 3: Add attn icon wrapper + collapsed-group attn badge CSS (no Tailwind sizing)</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css lines 4707-4720 (.hub__group-sidebar-toggle svg and needs-input-badge svg sizing — the no-Tailwind sizing analog)
    - frontend/src/style.css lines 4794-4805 (.hub__group-sidebar-item__needs-input-badge — exact shape to copy for the attn badge)
    - .planning/phases/133-attention-pulse/133-UI-SPEC.md (ATTN-06 Badge Visual Spec + ATTN-01 attn icon)
    - .planning/phases/133-attention-pulse/133-PATTERNS.md (style.css Analog 6/7 + "No-Tailwind Icon Sizing Rule")
  </read_first>
  <action>
    Add `.hub-card__attn-icon` wrapper: `display: inline-flex; align-items: center; flex-shrink: 0; color: var(--hub-attn-icon-color);`. Add `.hub-card__attn-icon svg { width: 16px; height: 16px; flex-shrink: 0; }`. Prefix the svg rule with `/* CRITICAL: size the icon with explicit CSS — no Tailwind in this project */` (Phase 132 UAT confirmed w-N h-N are no-ops).

    Add `.hub__group-sidebar-item__attn-badge`, copying the dimensions of `.hub__group-sidebar-item__needs-input-badge` (line 4794): `display: inline-flex; align-items: center; gap: 4px; padding: 0 4px; height: 16px; border-radius: 8px; background: var(--hub-attn-badge-bg); color: var(--hub-attn-badge-text); font-size: 11px; line-height: 1.0; font-family: system-ui, -apple-system, "Segoe UI", sans-serif; flex-shrink: 0;`. Prefix with `/* ATTN-06: collapsed-group attention badge */` and both COLORBLIND-SAFE comments (dark #e0af68, light #b45309). Note: gap is 4px (badge) vs the 2px on needs-input, per UI-SPEC.

    Add `.hub__group-sidebar-item__attn-badge svg { width: 12px; height: 12px; flex-shrink: 0; }` prefixed with `/* CRITICAL: explicit CSS sizing — no Tailwind in this project */`. The inner count span class `.hub__group-sidebar-item__attn-badge--count` needs no extra rule (inherits font-size from parent) unless a tweak is needed — leave unstyled.
  </action>
  <verify>
    <automated>cd frontend && grep -c 'hub-card__attn-icon svg' src/style.css && grep -c 'hub__group-sidebar-item__attn-badge' src/style.css</automated>
  </verify>
  <acceptance_criteria>
    - `.hub-card__attn-icon svg` rule sets explicit `width: 16px; height: 16px` (NOT a Tailwind class)
    - `.hub__group-sidebar-item__attn-badge svg` rule sets explicit `width: 12px; height: 12px`
    - `.hub__group-sidebar-item__attn-badge` matches the needs-input badge dimensions (16px height, 8px radius) with attn token colors
    - No `w-4`, `w-3`, `h-4`, `h-3` appears in any new Phase 133 CSS
    - `cd frontend && pnpm build` succeeds
  </acceptance_criteria>
  <done>The attn icon wrapper and collapsed-group attn badge are styled with explicit pixel sizing and attn token colors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS-only change; no data ingress, RPC, input, or storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-02 | Denial of Service | hub-attn-pulse infinite animation | accept | GPU-accelerated box-shadow on at most the visible attention cards; gated off entirely under prefers-reduced-motion; negligible cost vs existing hub-spin |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase; no install step |
</threat_model>

<verification>
- `cd frontend && pnpm build` succeeds (CSS parses, bundle emits)
- `grep` confirms: 2 `--hub-attn-border:` declarations, `animation: hub-attn-pulse` only inside a no-preference guard, explicit-px svg sizing rules present, no Tailwind utility classes in new CSS
</verification>

<success_criteria>
- All six --hub-attn-* tokens defined in both themes
- Pulse animation gated by prefers-reduced-motion: no-preference; static fallback under reduce
- Hover does not override the attention border
- 400ms clear transition active only for motion-enabled users
- Icon and badge sized with explicit CSS, never Tailwind
</success_criteria>

<output>
Create `.planning/phases/133-attention-pulse/133-02-SUMMARY.md` when done
</output>
