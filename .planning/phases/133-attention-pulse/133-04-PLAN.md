---
phase: 133-attention-pulse
plan: 04
type: execute
wave: 2
depends_on:
  - 133-01
  - 133-02
files_modified:
  - frontend/src/components/Hub/GroupSidebar.tsx
  - frontend/src/components/Hub/GroupSidebar.test.tsx
autonomous: true
requirements: [ATTN-06]
must_haves:
  truths:
    - "computeCounts and computeGlobalCounts return an attention count that is the count of sessions where isAttentionStatus is true (superset of waiting)"
    - "A COLLAPSED group sidebar item with attention > 0 shows the attention badge (BellAlertIcon + count)"
    - "A COLLAPSED group sidebar item with attention === 0 and waiting > 0 shows the needs-input badge (not the attention badge)"
    - "An EXPANDED group sidebar item shows neither the attention badge nor the needs-input badge (cards carry the signal)"
  artifacts:
    - path: "frontend/src/components/Hub/GroupSidebar.tsx"
      provides: "attention field on GroupCounts, attention badge render on collapsed items, inverted (collapsed) badge condition for both badges"
      contains: "hub__group-sidebar-item__attn-badge"
    - path: "frontend/src/components/Hub/GroupSidebar.test.tsx"
      provides: "unit coverage for attention count, collapsed/expanded badge rules, attention-over-needs-input priority"
      contains: "attn-badge"
  key_links:
    - from: "frontend/src/components/Hub/GroupSidebar.tsx"
      to: "frontend/src/lib/hubStatus.ts isAttentionStatus"
      via: "computeCounts/computeGlobalCounts call isAttentionStatus(deriveHubStatus(s))"
      pattern: "isAttentionStatus"
    - from: ".hub__group-sidebar-item__attn-badge"
      to: "frontend/src/style.css attn-badge rule"
      via: "class name on the collapsed-item badge span"
      pattern: "hub__group-sidebar-item__attn-badge"
---

<objective>
Add the collapsed-group attention badge to `GroupSidebar` (ATTN-06): extend `GroupCounts` with an `attention` field, compute it in both `computeCounts` and `computeGlobalCounts` via the canonical `isAttentionStatus` predicate, and render a `BellAlertIcon + count` badge (styled by Plan 02) on a COLLAPSED sidebar item when `attention > 0`. The attention badge REPLACES the needs-input badge when `attention > 0` (attention is a superset of waiting).

This plan also fixes the confirmed Phase 132 badge-condition inversion at `GroupSidebar.tsx` line 137: the existing needs-input badge renders under `!collapsed` (expanded) but the UI-SPEC requires badges to show when COLLAPSED (cards hidden → badge is the only signal). Both badges (needs-input and attention) must render under the `collapsed` condition.

Purpose: GroupSidebar is the second Wave 2 consumer of the foundation. It touches a different file than SessionCard (Plan 03), so the two run in parallel. It consumes the predicate (Plan 01) and the badge CSS (Plan 02).

Output: Updated `GroupSidebar.tsx` (GroupCounts.attention, both count functions, BellAlertIcon import, badge render + inverted condition) and extended `GroupSidebar.test.tsx`.

NO backend work. PRESERVE every Phase 132 feature: group list, "All" item, running/total counts, drag-over drop targets, create-group input, collapse toggle, and the existing `NeedsInputBadge` component (its render condition is corrected, the component itself is reused). The only condition change is `!collapsed` → `collapsed` for badge visibility; the name/count rows keep their existing `!collapsed` (expanded-only) behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-attention-pulse/133-UI-SPEC.md
@.planning/phases/133-attention-pulse/133-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — use directly. -->

From frontend/src/lib/hubStatus.ts (Plan 01 — shipped):
```typescript
export function deriveHubStatus(s: SessionInfo): HubStatus
/* ATTN-01 */ export function isAttentionStatus(status: HubStatus): boolean
```

Current GroupCounts (GroupSidebar.tsx lines 17-21): `{ running: number; total: number; waiting: number }`.

Current computeCounts (lines 23-36) and computeGlobalCounts (lines 38-47): loop sessions, increment running/waiting; computeGlobalCounts uses `sessions.length` for total.

Current badge render (GroupSidebar.tsx line 137 — CONFIRMED INVERSION BUG):
```tsx
{!collapsed && counts.waiting > 0 && (
  <NeedsInputBadge count={counts.waiting} />
)}
```
The name (line 129-131) and count (132-135) rows correctly render under `!collapsed` and stay as-is. Only the BADGE condition is wrong.

CSS classes provided by Plan 02 (already in style.css): `.hub__group-sidebar-item__attn-badge`, `.hub__group-sidebar-item__attn-badge svg { width: 12px; height: 12px }`. The needs-input badge already uses `PauseCircleIcon className="w-3 h-3"` (a no-op size today, but sized via the existing `.hub__group-sidebar-item__needs-input-badge svg` rule).

Test harness (GroupSidebar.test.tsx, already present): use its existing session factory + render helper (createRoot + act pattern, querySelector assertions). Reuse, do not add @testing-library.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add attention-count + badge-rule tests (RED)</name>
  <files>frontend/src/components/Hub/GroupSidebar.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/GroupSidebar.test.tsx (full file — reuse the existing session factory + render helper and querySelector style; do NOT introduce @testing-library)
    - .planning/phases/133-attention-pulse/133-UI-SPEC.md (ATTN-06 Badge Display Rules table; Counts Computation; superset note)
    - .planning/phases/133-attention-pulse/133-PATTERNS.md (GroupSidebar analog — computeCounts/computeGlobalCounts extension, badge condition inversion at line 137)
  </read_first>
  <behavior>
    - Test: computeCounts attention equals the number of member sessions whose status is waiting, errored, or stopped-err; and attention >= waiting for the same input (superset)
    - Test: a COLLAPSED GroupSidebarItem with attention > 0 renders `.hub__group-sidebar-item__attn-badge` (with the count) and renders NO `.hub__group-sidebar-item__needs-input-badge`
    - Test: a COLLAPSED GroupSidebarItem with attention === 0 and waiting > 0 renders `.hub__group-sidebar-item__needs-input-badge` and NO attn-badge
    - Test: an EXPANDED GroupSidebarItem (collapsed=false) with attention > 0 renders NEITHER badge
    - Test: the attn-badge has an aria-label matching "session needs attention" / "sessions need attention" with the correct count
  </behavior>
  <action>
    If `computeCounts` / `computeGlobalCounts` are not exported, either export them for direct unit testing OR assert the attention count indirectly by rendering a collapsed `GroupSidebar` with a known session mix and checking the rendered badge count text — prefer exporting the pure functions if the file already exports other helpers; otherwise render. Build session fixtures covering `waiting`, `errored`, `stopped` with non-zero `exitCode` (→ stopped-err), and a non-attention status (e.g. `running`) to prove the superset relationship.

    Add a `describe('GroupSidebar attention badge (ATTN-06)')` block with the behavior cases. For collapsed/expanded rendering, render `GroupSidebarItem` (or `GroupSidebar` with `collapsed` prop) and assert with `container.querySelector('.hub__group-sidebar-item__attn-badge')` and `.hub__group-sidebar-item__needs-input-badge`.

    Run the suite and confirm RED (attention field missing; attn-badge not rendered; badge still gated on `!collapsed`).
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run src/components/Hub/GroupSidebar.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - GroupSidebar.test.tsx contains a `describe` block for the attention badge with the five behavior cases above
    - The new cases FAIL before Task 2 (RED observed)
    - No new test framework imported
  </acceptance_criteria>
  <done>The ATTN-06 badge + count tests exist and fail because the attention count, attn-badge, and corrected collapsed condition do not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend counts, add attention badge, fix collapsed condition (GREEN)</name>
  <files>frontend/src/components/Hub/GroupSidebar.tsx</files>
  <read_first>
    - frontend/src/components/Hub/GroupSidebar.tsx (full file — 291 lines; GroupCounts 17-21, computeCounts 23-36, computeGlobalCounts 38-47, NeedsInputBadge 49-68, badge render 137-139, import block 4-13)
    - .planning/phases/133-attention-pulse/133-PATTERNS.md (GroupSidebar analog — exact import addition, GroupCounts extension, computeCounts/computeGlobalCounts additions, badge condition fix at line 137 with attention priority)
    - .planning/phases/133-attention-pulse/133-UI-SPEC.md (ATTN-06 Badge Display Rules — collapsed-only; attention replaces needs-input when attnCount>0; aria-label copy)
  </read_first>
  <action>
    Add `BellAlertIcon` to the existing `@heroicons/react/24/outline` import block. Extend the existing `import { deriveHubStatus } from '../../lib/hubStatus'` to `import { deriveHubStatus, isAttentionStatus } from '../../lib/hubStatus'`.

    Add `attention: number` to the `GroupCounts` interface with the comment `// ATTN-06: superset of waiting (waiting | errored | stopped-err)`.

    In `computeCounts`: initialize `attention = 0`; inside the member loop, after computing `st = deriveHubStatus(s)`, add `if (isAttentionStatus(st)) attention++`; return `attention` in the result object. Keep all existing running/waiting/total logic unchanged.

    In `computeGlobalCounts`: same — add `attention` accumulator over all sessions, `if (isAttentionStatus(st)) attention++`, and return it alongside the existing fields.

    Fix the badge render at line 137. Replace the single `!collapsed && counts.waiting > 0` needs-input block with TWO collapsed-gated blocks:
    1. Attention badge (priority): `{collapsed && counts.attention > 0 && ( <span className="hub__group-sidebar-item__attn-badge" aria-label={counts.attention === 1 ? '1 session needs attention' : `${counts.attention} sessions need attention`}> … </span> )}` — inside, render `<BellAlertIcon aria-hidden="true" />` (the parent span aria-label carries meaning per Accessibility Contract item 4) and `<span className="hub__group-sidebar-item__attn-badge--count">{counts.attention}</span>`.
    2. Needs-input fallback: `{collapsed && counts.attention === 0 && counts.waiting > 0 && ( <NeedsInputBadge count={counts.waiting} /> )}`.

    Prefix the attention block with `/* ATTN-06: collapsed-group attention badge — replaces needs-input when attnCount>0 */` and `/* COLORBLIND-SAFE: attn badge — reinforcement only; BellAlertIcon carries state */`.

    CRITICAL — NO Tailwind: do NOT put `w-3 h-3` or inline size on the new BellAlertIcon; sizing is the `.hub__group-sidebar-item__attn-badge svg` rule from Plan 02. (The existing NeedsInputBadge keeps its current `w-3 h-3` — out of scope to touch; its real sizing is the existing CSS rule.)

    Leave the name row (line 129-131) and count row (132-135) under `!collapsed` unchanged — only the badge moves to `collapsed`. Do NOT change `NeedsInputBadge`'s internal markup.

    Run the suite (GREEN) and `pnpm tsc --noEmit`.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run src/components/Hub/GroupSidebar.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "attention" frontend/src/components/Hub/GroupSidebar.tsx` is at least 5 (interface, two functions, two render conditions)
    - `grep -c "isAttentionStatus" frontend/src/components/Hub/GroupSidebar.tsx` is at least 2 (computeCounts + computeGlobalCounts)
    - `grep -c "hub__group-sidebar-item__attn-badge" frontend/src/components/Hub/GroupSidebar.tsx` is at least 2 (badge span + count span class)
    - `grep -c "collapsed && counts.attention" frontend/src/components/Hub/GroupSidebar.tsx` returns 2 (attention block + needs-input-fallback guard)
    - No `!collapsed && counts.waiting` remains (the inverted condition is gone): `grep -c '!collapsed && counts.waiting' frontend/src/components/Hub/GroupSidebar.tsx` returns 0
    - No `w-3`/`h-4`/`w-4` added to the new BellAlertIcon
    - `cd frontend && pnpm test -- --run src/components/Hub/GroupSidebar.test.tsx` exits 0
    - `cd frontend && pnpm tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>GroupCounts carries an attention count from the canonical predicate; collapsed items show the attention badge when attention>0 (replacing needs-input), the needs-input badge when only waiting>0; expanded items show no badge; the line-137 inversion is fixed; all Phase 132 features preserved; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational change — counts derived from existing session status; renders an SVG badge; no new data ingress, RPC, input, or storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-04 | Information Disclosure | collapsed-group attention badge | accept | Surfaces only an aggregate count of attention sessions already derivable from the visible cards; no new data |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: none installed); no install step |
</threat_model>

<verification>
- `cd frontend && pnpm test -- --run src/components/Hub/GroupSidebar.test.tsx` passes (existing + new cases)
- `cd frontend && pnpm tsc --noEmit` shows no new type errors
- `grep` confirms attention count in both functions, attn-badge render, and zero remaining `!collapsed && counts.waiting`
</verification>

<success_criteria>
- GroupCounts.attention computed via isAttentionStatus in both computeCounts and computeGlobalCounts
- Collapsed item: attention badge when attention>0 (replaces needs-input); needs-input when only waiting>0; expanded item shows no badge
- Line-137 inversion fixed — badges show when COLLAPSED, per UI-SPEC
- All Phase 132 sidebar features preserved
</success_criteria>

<output>
Create `.planning/phases/133-attention-pulse/133-04-SUMMARY.md` when done
</output>
