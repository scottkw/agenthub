---
phase: 133-attention-pulse
plan: 03
type: execute
wave: 2
depends_on:
  - 133-01
  - 133-02
files_modified:
  - frontend/src/components/Hub/SessionCard.tsx
  - frontend/src/components/Hub/SessionCard.test.tsx
autonomous: true
requirements: [ATTN-01]
must_haves:
  truths:
    - "A SessionCard whose status is in the attention set renders BellAlertIcon in ROW 1 with aria-label 'Needs attention'"
    - "A SessionCard whose status is in the attention set carries the .hub-card--attention class on its article element"
    - "A SessionCard whose status is in the attention set has ', needs attention' appended to its aria-label"
    - "A non-attention SessionCard renders no BellAlertIcon, no .hub-card--attention class, and no ', needs attention' suffix"
    - "The border/icon treatment is recomputed from the live session prop on each render (no internal debounce)"
  artifacts:
    - path: "frontend/src/components/Hub/SessionCard.tsx"
      provides: "isAttention prop, BellAlertIcon in ROW 1, .hub-card--attention modifier, aria-label suffix"
      contains: "hub-card--attention"
    - path: "frontend/src/components/Hub/SessionCard.test.tsx"
      provides: "unit coverage for attention rendering on/off and aria-label suffix"
      contains: "hub-card--attention"
  key_links:
    - from: "frontend/src/components/Hub/SessionCard.tsx"
      to: "frontend/src/lib/hubStatus.ts isAttentionStatus"
      via: "import of isAttentionStatus (NOT inline status checks)"
      pattern: "isAttentionStatus"
    - from: ".hub-card--attention class"
      to: "frontend/src/style.css .hub-card--attention rule"
      via: "class name attached when isAttention is true"
      pattern: "hub-card--attention"
---

<objective>
Add the attention treatment to `SessionCard`: an `isAttention` prop that, when true, attaches the `.hub-card--attention` modifier class (styled by Plan 02), renders `BellAlertIcon` in ROW 1 to the LEFT of the status icon, and appends `, needs attention` to the card's `aria-label` (ATTN-01).

Purpose: SessionCard is the leaf presentational consumer of the Wave 1 foundation. It binds the predicate result (passed down as `isAttention`) to the class name and icon that Plan 02's CSS animates. This is Wave 2 because it consumes both the predicate (Plan 01) and the CSS classes (Plan 02), but it is independent of GroupSidebar (Plan 04 — different file) and can run in parallel with it.

Output: Updated `SessionCard.tsx` (new `isAttention` prop, `BellAlertIcon` import, ROW 1 icon, className + aria-label changes) and extended `SessionCard.test.tsx`.

NO backend work. The predicate already exists from Plan 01. The CSS classes already exist from Plan 02. PRESERVE every existing Phase 131/132 feature: ROWs 1-6, the Open button, drag handle, overflow group menu, MiniPreview, the `hub-card--dim` / `hub-card--dragging` modifiers, and all existing aria attributes.

The border + BellAlertIcon update LIVE on every render from the `session` prop / `isAttention` prop — there is NO debounce inside SessionCard. Only POSITION (Plan 05, in the grid) is debounced. Do not add any timer or memoization that delays the icon/border appearing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-attention-pulse/133-UI-SPEC.md
@.planning/phases/133-attention-pulse/133-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — use directly, no exploration needed. -->

From frontend/src/lib/hubStatus.ts (Plan 01 — already shipped):
```typescript
export type HubStatus = 'running' | 'idle' | 'waiting' | 'errored' | 'stopped-ok' | 'stopped-err'
export function deriveHubStatus(s: SessionInfo): HubStatus
/* ATTN-01 */ export function isAttentionStatus(status: HubStatus): boolean
```

Current SessionCard className (SessionCard.tsx line 216):
```
className={`hub-card${hubStatus === 'stopped-ok' ? ' hub-card--dim' : ''}${isDragging ? ' hub-card--dragging' : ''}`}
```

Current aria-label (SessionCard.tsx line 161):
```
const cardAriaLabel = `${name}, ${displayLabel}, ${cli}, ${originText}`
```

Current ROW 1 (SessionCard.tsx lines 285-304): opens with `<div className="hub-card__row1">` then `<span className="hub-card__status-indicator">…</span>`.

CSS classes provided by Plan 02 (already in style.css): `.hub-card--attention`, `.hub-card__attn-icon`, `.hub-card__attn-icon svg { width: 16px; height: 16px }`.

Test harness (SessionCard.test.tsx, already present): `makeSession(overrides)` factory, `renderCard(session, onRename?)` via `createRoot` + `act`; query the DOM with `container.querySelector(...)`. Mock is `vi.mock('../../wailsjs/go/main/App', …)` at top of file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add attention rendering tests (RED)</name>
  <files>frontend/src/components/Hub/SessionCard.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/SessionCard.test.tsx (lines 1-60 — makeSession factory, renderCard helper, describe block; reuse exactly, do NOT add a new render harness)
    - .planning/phases/133-attention-pulse/133-UI-SPEC.md (Accessibility Contract items 2-3; ATTN-01 attention icon placement)
    - .planning/phases/133-attention-pulse/133-PATTERNS.md (SessionCard analog — className array-join at line 216, aria-label at 161, ROW 1 insertion at 285-304)
  </read_first>
  <behavior>
    - Test: a card rendered with isAttention=true has an element matching `.hub-card--attention`
    - Test: a card rendered with isAttention=true contains an element matching `.hub-card__attn-icon` whose aria-label is "Needs attention"
    - Test: a card rendered with isAttention=true has an article aria-label ending in ", needs attention"
    - Test: a card rendered with isAttention=false (or omitted) has NO `.hub-card--attention` element, NO `.hub-card__attn-icon`, and an aria-label NOT containing "needs attention"
    - Test: a card rendered with isAttention=true still renders the existing status-indicator (`.hub-card__status-indicator`) and the Open button when live (regression guard for Phase 131/132)
  </behavior>
  <action>
    Extend `renderCard` (or add a sibling helper) so it can pass `isAttention` — match the existing helper's `createRoot` + `act` style; do NOT introduce @testing-library. Add a `describe('SessionCard attention (ATTN-01)')` block with the cases listed under behavior. Use `container.querySelector('.hub-card--attention')`, `container.querySelector('.hub-card__attn-icon')`, and read `getAttribute('aria-label')` for assertions. For the "live" status, build the session with `makeSession({ status: 'waiting' })`; the `isAttention` prop is passed explicitly by the test (the grid computes it in Plan 05 — SessionCard itself just consumes the prop).

    Run the suite and confirm the new cases FAIL (RED) because the prop, icon, class, and aria suffix do not exist yet.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run src/components/Hub/SessionCard.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SessionCard.test.tsx contains a `describe` block referencing attention with at least the five behavior cases above
    - The new cases FAIL before Task 2 (RED state observed in the run output)
    - No new test framework imported (still vitest + createRoot + act)
  </acceptance_criteria>
  <done>The attention test cases exist and fail because SessionCard does not yet implement the prop, icon, class, or aria suffix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement isAttention prop, BellAlertIcon, modifier class, aria suffix (GREEN)</name>
  <files>frontend/src/components/Hub/SessionCard.tsx</files>
  <read_first>
    - frontend/src/components/Hub/SessionCard.tsx (full file — 369 lines; note the heroicons import block lines 3-14, the SessionCardProps interface lines 83-99, the destructure lines 119-126, the className at 216, aria-label at 161, ROW 1 at 285-304)
    - .planning/phases/133-attention-pulse/133-PATTERNS.md (SessionCard analog — exact import addition, props addition, className array-join conversion, aria-label extension, ROW 1 BellAlertIcon insertion)
    - .planning/phases/133-attention-pulse/133-UI-SPEC.md (ATTN-01 ROW 1 structure: BellAlertIcon left of status icon; Accessibility Contract item 2 — icon is NOT aria-hidden, carries aria-label "Needs attention")
  </read_first>
  <action>
    Add `BellAlertIcon` to the existing `@heroicons/react/24/outline` import block (do not create a second import statement).

    Add an optional `isAttention?: boolean` field to `SessionCardProps` with a doc comment: `/** ATTN-01: true when isAttentionStatus(deriveHubStatus(session)) is true */`. Add `isAttention` to the destructured props in the component signature. Do NOT compute attention inside SessionCard — it receives the prop from the grid (Plan 05). Do not import `isAttentionStatus` here unless you also use it; the canonical consumer is the grid. (If a default is needed, treat `undefined` as false.)

    Convert the article `className` (line 216) from the template-literal form to the array-filter-join form so three+ modifiers stay readable, appending the attention modifier: include `'hub-card'`, `hubStatus === 'stopped-ok' ? 'hub-card--dim' : ''`, `isDragging ? 'hub-card--dragging' : ''`, and `isAttention ? 'hub-card--attention' : ''`, then `.filter(Boolean).join(' ')`. Preserve the existing dim and dragging behavior byte-for-byte in meaning.

    Extend `cardAriaLabel` (line 161) to append `${isAttention ? ', needs attention' : ''}` to the existing string.

    Insert the attention icon as the FIRST child of `<div className="hub-card__row1">` (before `.hub-card__status-indicator`): when `isAttention` is true, render `<span className="hub-card__attn-icon" aria-label="Needs attention"><BellAlertIcon aria-hidden="true" /></span>`. The wrapper span carries the aria-label (it is NOT aria-hidden — Accessibility Contract item 2); the inner SVG is aria-hidden. Prefix the JSX with the comment `/* ATTN-01: attention icon — inline left of status icon; COLORBLIND-SAFE: BellAlertIcon carries state */`.

    CRITICAL — NO Tailwind: do NOT add `className="w-4 h-4"` or any `w-N h-N` to BellAlertIcon, and do NOT add an inline `style={{ width: '16px' }}`. Sizing comes exclusively from the `.hub-card__attn-icon svg` CSS rule shipped by Plan 02. Add the comment `{/* CRITICAL: NO Tailwind w-4 h-4 — size via .hub-card__attn-icon svg in style.css (Plan 02) */}` above the icon.

    Run the suite and confirm GREEN. Run `pnpm tsc --noEmit` to confirm no type regression.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run src/components/Hub/SessionCard.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isAttention" frontend/src/components/Hub/SessionCard.tsx` is at least 4 (prop type, destructure, className, aria-label, ROW 1 condition)
    - `grep -c "hub-card--attention" frontend/src/components/Hub/SessionCard.tsx` returns 1
    - `grep -c "hub-card__attn-icon" frontend/src/components/Hub/SessionCard.tsx` returns 1
    - `grep -c "BellAlertIcon" frontend/src/components/Hub/SessionCard.tsx` is at least 2 (import + render)
    - No `w-4`, `w-3`, `h-4`, `h-3`, or inline `style={{ width` appears on the BellAlertIcon element
    - The article className still includes `hub-card--dim` and `hub-card--dragging` logic (regression preserved)
    - `cd frontend && pnpm test -- --run src/components/Hub/SessionCard.test.tsx` exits 0 (all cases GREEN)
    - `cd frontend && pnpm tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>SessionCard renders the attention icon + modifier class + aria suffix when isAttention is true, leaves them off when false, preserves all Phase 131/132 features, and the dedicated tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational change — toggles a CSS class and renders an SVG from existing session status; no new data ingress, RPC, input, or storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-03 | Information Disclosure | BellAlertIcon + aria-label | accept | Exposes only the attention state already derivable from the rendered status label; no new data surfaced |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: none installed); no install step |
</threat_model>

<verification>
- `cd frontend && pnpm test -- --run src/components/Hub/SessionCard.test.tsx` passes (existing + new cases)
- `cd frontend && pnpm tsc --noEmit` shows no new type errors
- `grep` confirms `.hub-card--attention`, `.hub-card__attn-icon`, and `BellAlertIcon` present; no Tailwind utility classes on the icon
</verification>

<success_criteria>
- `isAttention` prop threads into SessionCard and drives the `.hub-card--attention` class, the BellAlertIcon in ROW 1, and the aria-label suffix
- BellAlertIcon wrapper has aria-label "Needs attention" and is not aria-hidden; the inner SVG is aria-hidden
- Icon sizing relies on Plan 02 CSS only — no Tailwind, no inline size
- All Phase 131/132 features (ROWs 1-6, Open, drag, group menu, MiniPreview, dim/dragging modifiers) preserved
</success_criteria>

<output>
Create `.planning/phases/133-attention-pulse/133-03-SUMMARY.md` when done
</output>
