---
phase: 108-tui-cli-shell-entry-collapse-parity-with-phase-107-gui-shell
plan: "108-03"
type: execute
wave: 2
depends_on:
  - "108-02" # also modifies cmd_cli.go (help block); 108-02 must land first
files_modified:
  - cmd_cli.go
  - README.md
autonomous: true
requirements:
  spec:
    - PARITY-DOCS-01
  roadmap:
    - PARITY-04
estimated_complexity: light
goal: >
  Update user-facing documentation to reflect the single-Shell-entry contract
  on all three surfaces: the CLI usage() help block (cmd_cli.go:29-30) and a
  new "Shell sessions" section in README.md that points users to Settings →
  Paths for binary selection. All references to per-surface --shell=X
  selection are removed.

must_haves:
  truths:
    - "`agenthub --help` (usage()) lists `new shell [<path>]` and does NOT list `--shell=bash|zsh|pwsh|powershell`"
    - "README has a 'Shell sessions' section describing single-Shell behaviour on GUI/TUI/CLI"
    - "README references Settings → Paths for shell binary selection"
    - "No user-facing reference to per-surface bash|zsh|pwsh shell selection remains"
  artifacts:
    - path: cmd_cli.go
      provides: "Updated usage() help block — `new shell [<path>]` with no --shell modifier line"
      absent: "--shell=bash|zsh|pwsh|powershell"
    - path: README.md
      provides: "'Shell sessions' section describing single-Shell parity across GUI/TUI/CLI"
      contains: "Shell sessions"
  key_links:
    - from: README.md (Shell sessions section)
      to: GUI Settings → Paths (Phase 107 SHELL-11)
      via: "Documentation cross-reference: users set the spawned binary via Settings → Paths; the choice applies uniformly to GUI, TUI, and CLI new-session flows"
      pattern: "Settings"
---

<objective>
With the TUI picker collapsed (108-01) and the CLI --shell flag removed
(108-02), the last remaining work is to align user-facing documentation:

1. cmd_cli.go usage() (lines 29-30): the trailing
   `--shell=bash|zsh|pwsh|powershell` modifier line is deleted; the `new
   shell [<path>]` line stays as the single source-of-truth for the
   subcommand.

2. README.md gets a new "Shell sessions" section (placed alongside the
   existing CLI / TUI Mode sections) that documents the single-Shell entry
   on all three surfaces (GUI new-session modal, TUI agent picker, CLI `new
   shell` subcommand) and points users to Settings → Paths for the binary
   choice.

This plan is small (light complexity, two file touches, no test churn) but
must run AFTER 108-02 — both modify cmd_cli.go (108-02 modifies the
cmdNewShell body and its doc comment; 108-03 modifies the usage() help
block above it). Sequential wiring avoids merge conflicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-tui-cli-shell-entry-collapse-parity-with-phase-107-gui-shell/108-SPEC.md
@.planning/phases/108-tui-cli-shell-entry-collapse-parity-with-phase-107-gui-shell/108-CONTEXT.md
@.planning/phases/108-tui-cli-shell-entry-collapse-parity-with-phase-107-gui-shell/108-02-SUMMARY.md
@cmd_cli.go
@README.md

<interfaces>
Current state of cmd_cli.go usage() help block (cmd_cli.go:22-57):

Lines 28-30 currently read:
```
  new <agent> <path> [-- <extra-args>...]     Create a new terminal session
  new shell [<path>]                          Create a new raw shell session
    --shell=bash|zsh|pwsh|powershell           Pick a specific shell (default: system default)
```

After this plan, line 30 (the `--shell=...` modifier) is deleted. Lines 28-29
remain. The `new shell [<path>]` line is unchanged (matches the needle in
TestUsage_IncludesNewShell which Plan 108-02 leaves intact).

README.md (current state): no "Shell sessions" heading exists. The CLI
section (README.md:94-100) lists `agenthub new` and related commands but
does not describe the shell variant or shell-binary selection. The TUI Mode
section (README.md:49-63) does not mention shells. The Settings section
(README.md:122-133) describes the Paths sub-section but does not mention
shellPath specifically.

This plan adds a focused "Shell sessions" section. Recommended placement:
between Features → CLI (line 94) and Features → Web Serving (line 102), as a
new ### subheading. Alternative: under Features → Terminal & Sessions. The
executor chooses; the SPEC accepts either location as long as the section
exists and is discoverable from the table of contents (if the README has
one — confirm during implementation).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove --shell modifier line from cmd_cli.go usage() help block</name>
  <files>cmd_cli.go</files>
  <action>
    Edit cmd_cli.go usage() function (lines 22-57). Delete line 30 (the
    `    --shell=bash|zsh|pwsh|powershell           Pick a specific shell
    (default: system default)` line) entirely. Lines 28 and 29 remain
    unchanged:

      new <agent> <path> [-- <extra-args>...]     Create a new terminal session
      new shell [<path>]                          Create a new raw shell session

    Confirm the rest of the usage() string is unaffected (no other line
    references --shell). Run `agenthub --help` (or inspect the captured
    output via `go run . --help 2>&1`) to verify visually.

    Per D-08 in CONTEXT.md (PARITY-DOCS-01 locked).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./... && ! grep -nE '\-\-shell=bash\|zsh\|pwsh\|powershell' cmd_cli.go</automated>
  </verify>
  <done>
    The `--shell=bash|zsh|pwsh|powershell` line no longer appears in cmd_cli.go. The package still compiles. `agenthub --help` shows `new shell [<path>]` with no modifier line beneath it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add "Shell sessions" section to README describing single-Shell parity</name>
  <files>README.md</files>
  <action>
    Edit README.md (PARITY-DOCS-01). Add a new `### Shell sessions`
    subsection under `## Features`, placed between `### CLI` (line 94) and
    `### Web Serving` (line 102) — or, at the executor's discretion, after
    the Plugin Suite section. Section body should cover:

    1. The big idea: AgentHub supports raw PTY shell sessions alongside AI
       CLI sessions (Claude Code, OpenCode, Codex, Gemini). All three
       surfaces (GUI new-session modal, TUI new-session picker, CLI `new
       shell` subcommand) expose this as exactly one entry labelled "Shell".

    2. Per-surface UX bullet:
       - **GUI** — New-session modal shows a single "Shell" row; pressing
         Create launches a raw PTY using the binary configured in Settings →
         Paths.
       - **TUI** — `agenthub tui`, press `n` to open the new-session modal,
         cycle the agent picker to "Shell", confirm. Same binary as GUI.
       - **CLI** — `agenthub new shell [<path>]`. No --shell flag — the
         spawned binary comes from Settings → Paths (same value used by GUI
         and TUI). Optional positional path sets the working directory; omit
         to launch in `$HOME`.

    3. Binary selection: a one-sentence pointer to Settings → Paths
       (described in the Settings section of the README). Mention that if
       no shellPath is set, the daemon falls back to `$SHELL` / platform
       default (`zsh` on macOS, `bash` on Linux, `powershell.exe` on
       Windows).

    4. Cross-surface parity callout: "All three surfaces use the same
       shellPath value — change it once in Settings → Paths and the new
       choice applies to every new shell session regardless of which
       surface launched it."

    5. (Optional but recommended) brief mention that extra arguments after
       `--` are NOT forwarded to shell sessions (matches the existing
       cmd_cli.go ExtraArgsWarning behaviour; helps anyone reading the
       README to understand the warning if they hit it).

    Keep the section concise — 4-6 short paragraphs or a brief intro plus a
    bulleted per-surface block. Do NOT mention --shell=bash|zsh|pwsh
    anywhere; that flag is gone. Do NOT mention `pty.DiscoverShells` or any
    internal API name. Match the README's existing voice (terse,
    feature-bullet-oriented).

    Per D-08 in CONTEXT.md (PARITY-DOCS-01 locked).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && grep -nE 'Shell sessions' README.md && ! grep -nE '\-\-shell=' README.md && grep -nE 'Settings' README.md | grep -i 'shell\|path' | head -1</automated>
  </verify>
  <done>
    README.md contains a `### Shell sessions` heading (or similar — the exact
    heading text is at executor discretion as long as it's discoverable and
    describes the feature). The section describes single-Shell behaviour on
    GUI, TUI, and CLI; references Settings → Paths for binary selection;
    contains zero `--shell=` references.
  </done>
</task>

<task type="auto">
  <name>Task 3: Confirm all SPEC acceptance grep gates pass and full suite is green</name>
  <files>(verification only)</files>
  <action>
    Run the SPEC's full acceptance grep matrix one final time to confirm
    Phase 108 is complete after all three plans land. Confirm:

    1. `go test ./... -count=1` is green (includes both 108-01 and 108-02
       changes — internal/tui + cmd_cli).
    2. `grep -nE "Shell —|sortShellsForPicker" internal/tui/modal.go` → 0.
    3. `grep -nE "detectedShells|DiscoverShells" internal/tui/*.go` excluding
       `*_test.go` → 0.
    4. `grep -nE '"shell".*flag|shellFlag|allowed.*shell|unknown shell' cmd_cli.go` → 0.
    5. `grep -nE '\-\-shell=' cmd_cli.go README.md` → 0 (test fixtures in
       `*_test.go` are excluded by file selection).
    6. `agenthub --help` does NOT mention --shell (visual check via `go run .
       --help 2>&1 | grep -- --shell` returns empty).
    7. `agenthub new shell --shell=zsh 2>&1` exits 1 with stderr containing
       `flag provided but not defined: -shell` (acceptance smoke).
    8. README has a "Shell sessions" section (visual confirmation).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -count=1 ./... && ! grep -nE 'Shell —|sortShellsForPicker' internal/tui/modal.go && ! grep -nE 'detectedShells|DiscoverShells' internal/tui/model.go internal/tui/modal.go internal/tui/tui.go internal/tui/update.go && ! grep -nE '"shell".*flag|shellFlag|allowed.*shell|unknown shell' cmd_cli.go && ! grep -nE '\-\-shell=' cmd_cli.go README.md</automated>
  </verify>
  <done>
    All eight SPEC acceptance criteria pass. Phase 108 is code-complete and ready for /gsd-verify-work or release sign-off.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Documentation | One-way (no input crosses); docs influence user mental model only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-DOCS-01 | I (Information Disclosure) | README Shell sessions section | accept | Documents only public CLI/GUI behaviour; no secrets, paths, or internal API names. Points to Settings → Paths (already a documented surface from Phase 107). |
| T-108-DOCS-02 | T (Tampering) | usage() help string | accept | Help text is a const string compiled into the binary; no user input crosses this boundary. |

No new threats. This plan is documentation-only; the actual attack-surface
reduction was performed by 108-01 (TUI shell discovery removal) and 108-02
(--shell flag removal).
</threat_model>

<verification>
1. `go test ./... -count=1` passes (whole module — Phase 108 end-state).
2. SPEC acceptance grep matrix (all eight criteria from 108-SPEC §Acceptance Criteria).
3. Manual: `go build . && ./agenthub --help` shows the `new shell [<path>]` line with no `--shell=...` modifier underneath.
4. Manual: open README in a Markdown viewer; confirm "Shell sessions" heading is present, discoverable, and reads naturally alongside neighbouring sections.
</verification>

<success_criteria>
- cmd_cli.go usage() block no longer mentions --shell.
- README contains a clearly-labelled "Shell sessions" section that describes single-Shell parity across GUI/TUI/CLI and points to Settings → Paths.
- All eight SPEC acceptance grep gates pass.
- `go test ./... -count=1` is green.
- Phase 108 is code-complete and ready for milestone sign-off.
</success_criteria>

<output>
After completion, create `.planning/phases/108-tui-cli-shell-entry-collapse-parity-with-phase-107-gui-shell/108-03-SUMMARY.md` documenting:
- The cmd_cli.go edit (one line deleted from usage()).
- The README addition (location of new section, summary of content).
- Confirmation that all eight SPEC acceptance grep gates pass and `go test ./...` is green.
- Hand-off to milestone closure: Phase 108 is the last release-blocking work item for v3.3 cross-surface shell-entry parity; the operator follow-ups (Phase 105 UATs, Phase 106 secrets) remain before `/gsd-complete-milestone v3.3`.
</output>
