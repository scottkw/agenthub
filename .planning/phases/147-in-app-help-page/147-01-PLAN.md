---
phase: 147-in-app-help-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/test-setup.ts
  - frontend/package.json
  - frontend/pnpm-lock.yaml
  - frontend/src/content/help/getting-started.md
  - frontend/src/content/help/faq.md
  - frontend/src/components/__tests__/Sidebar.test.tsx
  - frontend/src/components/__tests__/HelpTab.test.tsx
  - frontend/src/components/__tests__/HelpSearch.test.tsx
  - frontend/src/components/__tests__/HelpSectionNav.test.tsx
  - frontend/src/components/__tests__/HelpContent.test.tsx
autonomous: false
requirements: [HELP-01]

must_haves:
  truths:
    - "IntersectionObserver is polyfilled in jsdom so Help components mount in tests without throwing"
    - "rehype-sanitize is installed for defense-in-depth Markdown sanitization"
    - "Seeded Help content (Getting Started + FAQ) exists as importable Markdown"
    - "Failing test stubs exist for all four Help components (RED state) plus updated Sidebar tests"
  artifacts:
    - path: "frontend/src/test-setup.ts"
      provides: "IntersectionObserver no-op polyfill alongside ResizeObserver"
      contains: "IntersectionObserver"
    - path: "frontend/src/content/help/getting-started.md"
      provides: "Getting Started doc content"
      min_lines: 10
    - path: "frontend/src/content/help/faq.md"
      provides: "FAQ content — 6 seed questions from UI-SPEC Copywriting Contract"
      min_lines: 12
    - path: "frontend/src/components/__tests__/HelpSearch.test.tsx"
      provides: "Search debounce/highlight/empty-state/clear-button assertions (RED until Plan 03)"
    - path: "frontend/src/components/__tests__/HelpSectionNav.test.tsx"
      provides: "Section-nav render + aria-current assertions (RED until Plan 03)"
    - path: "frontend/src/components/__tests__/HelpContent.test.tsx"
      provides: "react-markdown source gate + BrowserOpenURL call assertions (RED until Plan 03)"
    - path: "frontend/src/components/__tests__/HelpTab.test.tsx"
      provides: "HELP_TAB / handleOpenHelp source gates + CSS token source gate (RED until Plans 02/03)"
  key_links:
    - from: "frontend/src/test-setup.ts"
      to: "globalThis.IntersectionObserver"
      via: "typeof-guarded class assignment"
      pattern: "IntersectionObserver"
---

<objective>
Establish the Wave 0 test/content infrastructure for the Help page: the IntersectionObserver jsdom polyfill, the rehype-sanitize dependency, the seeded Markdown content, and failing test stubs for every Help component (plus the Sidebar test count updates). This lands the RED state that the implementation plans (02 nav wiring, 03 components) turn GREEN.

Purpose: Per the Nyquist rule and VALIDATION.md, every behavior must have an automated test that exists before implementation. This plan creates that scaffold so executors implement against real failing tests, not after-the-fact.
Output: Polyfill, one new npm package, two Markdown content files, four new test files, one updated test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/147-in-app-help-page/147-CONTEXT.md
@.planning/phases/147-in-app-help-page/147-RESEARCH.md
@.planning/phases/147-in-app-help-page/147-PATTERNS.md
@.planning/phases/147-in-app-help-page/147-VALIDATION.md
@.planning/phases/147-in-app-help-page/147-UI-SPEC.md

<interfaces>
<!-- Contracts the test stubs assert against. Plans 02/03 implement these exactly. -->
HelpTab.tsx:     export function HelpTab(): React.ReactElement   (no props; owns query + activeSection state)
HelpSearch.tsx:  export function HelpSearch(props: { query: string; results: ReadonlyArray<SearchEntry>; onQueryChange: (raw: string) => void; onJumpToSection: (id: string) => void }): React.ReactElement
HelpSectionNav.tsx: export function HelpSectionNav(props: { activeSection: string; onSectionChange: (id: string) => void; contentPaneRef: React.RefObject<HTMLDivElement> }): React.ReactElement
HelpContent.tsx: export function HelpContent(props: { markdown: string }): React.ReactElement
SearchEntry:     { sectionId: string; sectionLabel: string; text: string }
App.tsx:         const HELP_TAB: Tab = { id: '__help__', name: 'Help', sessionId: '', cli: '', type: 'help' }; const handleOpenHelp = useCallback(...)
Sidebar.tsx:     new prop onOpenHelp: () => void; Help button aria-label="Help"; active via activePanel === '__help__'
CSS token:       --hub-search-highlight-bg (declared in :root and [data-ui-theme="light"])
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify rehype-sanitize package legitimacy before install</name>
  <read_first>
    - .planning/phases/147-in-app-help-page/147-RESEARCH.md (§Package Legitimacy Audit — rehype-sanitize row tagged [ASSUMED] because slopcheck was unavailable)
  </read_first>
  <what-built>
    Nothing automated yet — this is the pre-install legitimacy gate required because RESEARCH.md tagged rehype-sanitize as [ASSUMED] (slopcheck unavailable at research time).
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/rehype-sanitize
    2. Confirm the source repository is github.com/rehypejs/rehype-sanitize (the official `rehypejs` org — same ecosystem as already-installed remark-gfm).
    3. Confirm maintainer `wooorm` (Titus Wormer), version 6.0.0, MIT license, no postinstall scripts.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `pnpm add rehype-sanitize`, or describe the concern.</resume-signal>
  <acceptance_criteria>
    - Human confirms rehype-sanitize is from the rehypejs org at github.com/rehypejs/rehype-sanitize.
    - This checkpoint is NOT auto-approvable (T-147-SC supply-chain threat); workflow.auto_advance is ignored here.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install rehype-sanitize + add IntersectionObserver polyfill</name>
  <read_first>
    - frontend/src/test-setup.ts (lines 12-22 — existing ResizeObserver no-op polyfill is the exact pattern to mirror)
    - frontend/package.json (confirm react-markdown 10.1.0 + remark-gfm 4.0.1 already present; rehype-sanitize absent)
    - .planning/phases/147-in-app-help-page/147-PATTERNS.md (§frontend/src/test-setup.ts — IntersectionObserver polyfill with `as unknown as typeof IntersectionObserver` cast)
  </read_first>
  <action>
    Run `cd frontend && pnpm add rehype-sanitize` (only after Task 1 approval) — adds rehype-sanitize@6.0.0 to dependencies and updates pnpm-lock.yaml. Then append an IntersectionObserver no-op polyfill to frontend/src/test-setup.ts immediately after the existing ResizeObserver polyfill block: a `typeof globalThis.IntersectionObserver === 'undefined'` guard assigning a class with no-op `observe()`, `unobserve()`, `disconnect()` methods, cast `as unknown as typeof IntersectionObserver` (the cast is required because the stub omits the callback+options constructor signature). Mirror the ResizeObserver comment style and reference Phase 147.
  </action>
  <verify>
    <automated>cd frontend && grep -q "IntersectionObserver" src/test-setup.ts && grep -q '"rehype-sanitize"' package.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` dependencies include `rehype-sanitize` (version ^6.0.0); pnpm-lock.yaml resolves it.
    - `frontend/src/test-setup.ts` contains a `typeof globalThis.IntersectionObserver === 'undefined'` guard with no-op observe/unobserve/disconnect and the `as unknown as typeof IntersectionObserver` cast.
    - `cd frontend && pnpm test` still runs (existing suite green — no regression from the polyfill addition).
  </acceptance_criteria>
  <done>rehype-sanitize installed; IntersectionObserver polyfill present; existing vitest suite unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: Create seeded Help Markdown content</name>
  <read_first>
    - .planning/phases/147-in-app-help-page/147-UI-SPEC.md (§Copywriting Contract — the FAQ seed table with exact questions + answer directions; section headings "Getting Started" / "Frequently Asked Questions"; heading hierarchy h1→h2→h3)
    - .planning/phases/147-in-app-help-page/147-CONTEXT.md (§Specific Ideas — FAQ seed set; DevTools answer must match the known Wails-DevTools-disabled-in-prod behavior)
    - .planning/phases/147-in-app-help-page/147-RESEARCH.md (§Pattern 5 — paragraphs split on double-newline; per-paragraph indexing; A2 — omit Keyboard Shortcuts section, no documented shortcuts)
  </read_first>
  <action>
    Create `frontend/src/content/help/getting-started.md` and `frontend/src/content/help/faq.md`. getting-started.md: an h2 "Getting Started" section under an anchor heading whose rendered id resolves to `help-getting-started`, with short scannable paragraphs covering session creation, shells, session switching, file browser, web share, and settings overview (one paragraph each, separated by blank lines so the per-paragraph search index splits cleanly; each indexable paragraph > 20 chars). faq.md: an h2 "Frequently Asked Questions" section (anchor id `help-faq`) with the EXACT six seed questions from the UI-SPEC Copywriting Contract FAQ table, each as a question + 1–3 sentence answer following the stated answer direction. The DevTools answer must say DevTools is disabled in production and recommend `wails dev` or web-share to Chrome. Use inline code spans (backticks) for `wails dev` and the macOS logs path `~/Library/Application Support/agenthub/`. Do NOT create keyboard-shortcuts.md (omitted for v1 per A2). External-links copy is rendered by HelpContent (Plan 03) — do not duplicate link buttons in Markdown unless using GitHub-issues link in the FAQ "report a bug" answer.
  </action>
  <verify>
    <automated>cd frontend && grep -q "Frequently Asked Questions" src/content/help/faq.md && grep -qi "wails dev" src/content/help/faq.md && grep -q "Getting Started" src/content/help/getting-started.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `faq.md` contains all six seed questions (DevTools-in-prod, network sharing, remote file browse, sessions/logs location, updating, reporting bugs) with answers matching the UI-SPEC answer directions.
    - DevTools FAQ answer references `wails dev` / web-share and states DevTools is disabled in production (matches project behavior).
    - `getting-started.md` paragraphs are blank-line separated so `md.split(/\n\n+/)` yields per-paragraph entries each > 20 chars.
    - No `keyboard-shortcuts.md` file is created (A2).
    - No raw `<a href>` external links in content (external links are rendered as BrowserOpenURL buttons by HelpContent; the report-a-bug GitHub link is the only allowed Markdown link, rendered via the `a` component override).
  </acceptance_criteria>
  <done>Two Markdown files exist; FAQ has the six seed questions; content is paragraph-split-friendly; no keyboard-shortcuts file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Update Sidebar tests + create four failing Help component test stubs</name>
  <read_first>
    - frontend/src/components/__tests__/Sidebar.test.tsx (lines 92-96, 237-243, AND 379-389 — THREE `button.sidebar__item` count assertions currently asserting 3; lines 263-308 Hub-button test block to mirror for Help; renderSidebar/defaultProps helper)
    - frontend/src/components/SettingsSearch.tsx (snippet/result list ARIA — listbox/option pattern HelpSearch mirrors)
    - .planning/phases/147-in-app-help-page/147-PATTERNS.md (§Sidebar.test.tsx and §New test files — readFileSync source-gate pattern, vi.mock for Wails runtime, renderHelper idiom)
    - .planning/phases/147-in-app-help-page/147-VALIDATION.md (§Per-Task Verification Map — every assertion these stubs must cover)
    - .planning/phases/147-in-app-help-page/147-UI-SPEC.md (§Copywriting Contract + §CSS Naming Convention — exact strings: "Search help…", aria-label "Clear search", `No results for "{query}"`, "View on GitHub", aria-label "View AgentHub on GitHub (opens in browser)"; class names .help-search__mark, .help-search__clear, .help-search__empty)
  </read_first>
  <behavior>
    Sidebar.test.tsx (UPDATE — these must stay GREEN after Plan 02):
    - Change the THREE count assertions at lines ~95, ~243, and ~389 from `toBe(3)` to `toBe(4)` and update their describe text to include Help. (Line ~468 group-list `li` count stays 3 — that is the All+2-groups group-item count, NOT a sidebar__item count.)
    - Add `onOpenHelp: vi.fn()` to renderSidebar defaultProps.
    - Add a "Sidebar Help item (Phase 147)" describe block mirroring the Hub block: renders Help button with aria-label="Help"; fires onOpenHelp on click; NO sidebar__item--active when activePanel !== '__help__'; HAS sidebar__item--active when activePanel === '__help__'.
    HelpSearch.test.tsx (NEW — RED until Plan 03): renders visible label "Search help…"; renders clear button with aria-label "Clear search"; shows empty-state element (.help-search__empty) with `No results for "{query}"` when query non-empty AND results empty; does NOT show empty-state for empty query; HighlightedSnippet wraps the matched substring in `<mark class="help-search__mark">`; debounce — query change reflects in results only after ~200ms (use fake timers; assert behavior at the HelpTab integration level if debounce lives in HelpTab, otherwise source-gate the 200ms timeout).
    HelpSectionNav.test.tsx (NEW — RED): renders a `<button>` per section; the button matching activeSection prop carries `aria-current="true"` and class `help-nav__link--active`; clicking a nav button calls onSectionChange with that section id.
    HelpContent.test.tsx (NEW — RED): source-gate that HelpContent.tsx imports `react-markdown` and renders `<Markdown>`; with a vi.mock of '../../wailsjs/wailsjs/runtime/runtime' exposing BrowserOpenURL: vi.fn(), clicking a rendered external-link button calls BrowserOpenURL; source-gate that HelpContent contains no `<a href` literal (links are buttons).
    HelpTab.test.tsx (NEW — RED until Plans 02/03): source-gate that App.tsx declares `HELP_TAB` with `id: '__help__'` and `type: 'help'` and a `handleOpenHelp`; CSS source-gate that style.css declares `--hub-search-highlight-bg` in both `:root` and `[data-ui-theme="light"]`.
  </behavior>
  <action>
    Update Sidebar.test.tsx per the behavior list. Create the four new test files under frontend/src/components/__tests__/ using the Sidebar.test.tsx header idiom (vitest imports, act from react, createRoot, readFileSync+resolve for source/CSS gates). Mock the Wails runtime in HelpContent.test.tsx via `vi.mock('../../wailsjs/wailsjs/runtime/runtime', () => ({ BrowserOpenURL: vi.fn() }))`. Pull EXACT copy strings and class names from the UI-SPEC Copywriting Contract and CSS Naming Convention. The four new files MUST fail now (components/CSS/App changes do not yet exist) and turn GREEN in Plans 02/03 — this is the intended RED state. The Sidebar.test.tsx updates will fail until Plan 02 adds the 4th item; that is also intended RED for the count assertions, but the file must be syntactically valid and run.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && ls src/components/__tests__/HelpSearch.test.tsx src/components/__tests__/HelpSectionNav.test.tsx src/components/__tests__/HelpContent.test.tsx src/components/__tests__/HelpTab.test.tsx && echo STUBS_OK</automated>
  </verify>
  <acceptance_criteria>
    - All three `toBe(3)` sidebar__item count assertions (lines ~95, ~243, ~389) are changed to `toBe(4)`; the line ~468 group-`li` count remains 3.
    - renderSidebar defaultProps includes `onOpenHelp: vi.fn()`.
    - A "Sidebar Help item (Phase 147)" describe block exists with the four Hub-mirrored assertions.
    - Four new test files exist and are valid TypeScript (`npx tsc --noEmit` passes — stubs compile even though assertions fail at runtime).
    - HelpSearch test asserts label "Search help…", clear aria-label "Clear search", empty-state copy `No results for "{query}"`, and `<mark class="help-search__mark">` highlighting.
    - HelpContent test mocks BrowserOpenURL and asserts it is called on external-link click; source-gates absence of `<a href`.
    - HelpTab test source-gates `HELP_TAB`/`__help__`/`handleOpenHelp` in App.tsx and `--hub-search-highlight-bg` in style.css for both `:root` and `[data-ui-theme="light"]`.
    - Running `cd frontend && pnpm test` shows the new Help assertions FAILING (RED) — this is correct for Wave 0.
  </acceptance_criteria>
  <done>Sidebar tests updated to 4 items + Help block; four new Help test files compile and fail as intended RED scaffolds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Supply-chain: a new package (rehype-sanitize) is installed |
| (no runtime input boundary in this plan) | Test infra/content only; no user input processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-SC | Tampering | `pnpm add rehype-sanitize` | mitigate | Blocking-human checkpoint (Task 1) verifies provenance at npmjs.com/rehypejs org before install; package is [ASSUMED] per RESEARCH.md (slopcheck unavailable). Not auto-approvable. |
| T-147-01 | Tampering | bundled Markdown content | accept | Content is maintainer-authored and committed in-repo; no remote fetch; rendered later via react-markdown virtual DOM + rehype-sanitize (Plan 03). No user-supplied content path. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && pnpm test` runs; new Help component assertions are RED (intended); existing suite otherwise green.
- IntersectionObserver and rehype-sanitize present.
- Two Markdown content files present with the six FAQ seeds.
</verification>

<success_criteria>
- IntersectionObserver polyfill + rehype-sanitize installed.
- Seeded Getting Started + FAQ Markdown content created.
- Sidebar tests updated to 4 items with a Help describe block.
- Four failing Help component test stubs created and compiling.
</success_criteria>

<output>
Create `.planning/phases/147-in-app-help-page/147-01-SUMMARY.md` when done.
</output>
