---
phase: 147-in-app-help-page
plan: 04
type: execute
wave: 4
depends_on: ["147-01", "147-02", "147-03"]
files_modified:
  - TESTING.md
autonomous: true
requirements: [HELP-01]

must_haves:
  truths:
    - "Every new Help test file is registered in the TESTING.md Suite Manifest count and Traceability Map"
    - "The Wails-native live UAT for the Help page is recorded as a manual checklist item (M-14)"
    - "tests/check-traceability-paths.sh exits 0 (every mapped path resolves on disk, no test names in path column)"
  artifacts:
    - path: "TESTING.md"
      provides: "Updated vitest count, HELP-01 traceability rows, M-14 manual UAT item"
      contains: "HELP-01"
  key_links:
    - from: "TESTING.md §4 Traceability"
      to: "frontend/src/components/__tests__/Help*.test.tsx"
      via: "repo-relative path rows"
      pattern: "HelpSearch.test.tsx"
---

<objective>
Honor the AgentHub standing regression-test convention (repo CLAUDE.md / TESTING.md §6): register every new Help test file in the Suite Manifest (§2) and Traceability Map (§4), add the M-14 manual checklist item (§5) for the Wails-native-webview UAT that cannot be automated, and confirm `bash tests/check-traceability-paths.sh` exits 0.

Purpose: Keep the canonical regression suite authoritative — un-registered tests violate the standing convention and break traceability auditing.
Output: One modified file (TESTING.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/147-in-app-help-page/147-VALIDATION.md
@TESTING.md
@CLAUDE.md

<interfaces>
New test files to register (created in Plan 01 / made green in Plans 02-03):
  frontend/src/components/__tests__/HelpTab.test.tsx
  frontend/src/components/__tests__/HelpSearch.test.tsx
  frontend/src/components/__tests__/HelpSectionNav.test.tsx
  frontend/src/components/__tests__/HelpContent.test.tsx
(Sidebar.test.tsx is UPDATED not new — it is already registered; add a HELP-01 Notes mention only if a new row is warranted, otherwise leave its existing rows intact.)

TESTING.md anchors:
  §2 Suite Manifest — vitest row currently "**112** *.test.ts/tsx files"; +4 new Help files → 116. Update the count and the Note line.
  §4 Traceability Map — header "| Requirement | Test File | Suite Group | Notes |"; path column MUST be a repo-relative path ending .tsx (no test/describe names — those go in Notes). Highest existing manual item is M-13.
  §5 Manual Regression Checklist — add a new category or item for the Wails-native Help UAT as M-14.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register Help test files in Suite Manifest + Traceability Map</name>
  <read_first>
    - TESTING.md (§2 Suite Manifest table — vitest count 112 + the trailing Note line; §4 Traceability Map header + format rule "path column must end .go/.ts/.tsx/.sh, no test names"; §6 Standing Convention)
    - .planning/phases/147-in-app-help-page/147-VALIDATION.md (§Per-Task Verification Map — which test file covers which behavior, for the Notes column)
    - tests/check-traceability-paths.sh (confirm it validates that each path column entry resolves on disk and is path-only)
  </read_first>
  <action>
    In TESTING.md §2, bump the vitest group Count from 112 to 116 (four new Help test files) and the Total accordingly, and append a Note explaining the +4 (Phase 147 Help page: HelpTab, HelpSearch, HelpSectionNav, HelpContent). In §4, add one Traceability row per new file under Requirement HELP-01: path column = the repo-relative `.tsx` path ONLY (e.g. `frontend/src/components/__tests__/HelpSearch.test.tsx`), Suite Group = vitest, Notes = the behavior covered (e.g. "debounce, `<mark>` highlight, empty-state, clear button") — test/describe names live ONLY in Notes, never the path column. Do not alter existing Sidebar.test.tsx rows beyond optionally noting the new 4-item/Help-button coverage in their Notes.
  </action>
  <verify>
    <automated>grep -c "Help.*test.tsx\|HelpSearch.test.tsx" TESTING.md | grep -qE "[4-9]|[0-9]{2}" && grep -q "116" TESTING.md && bash tests/check-traceability-paths.sh && echo TRACE_OK</automated>
  </verify>
  <acceptance_criteria>
    - §2 vitest Count reads 116 (was 112) with a Note documenting the +4 Help files; Total updated to match.
    - §4 has one HELP-01 row per new Help test file; each path column entry is a repo-relative `.tsx` path with NO test/describe name appended.
    - `bash tests/check-traceability-paths.sh` exits 0 (every mapped path resolves; path-only format honored).
  </acceptance_criteria>
  <done>Four Help test files registered in §2 and §4; traceability path-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add M-14 manual UAT item for the live Wails Help page</name>
  <read_first>
    - TESTING.md (§5 Manual Regression Checklist — existing M-NN item format/categories; highest current id is M-13)
    - .planning/phases/147-in-app-help-page/147-VALIDATION.md (§Manual-Only Verifications — the exact live-UAT steps and rationale for HELP-01)
    - .planning/phases/147-in-app-help-page/147-UI-SPEC.md (§Interaction Contract — scroll-spy active state, debounced highlight + jump, external links open system browser)
  </read_first>
  <action>
    Add a manual checklist item **M-14** under an appropriate §5 category (new "Help Page (HELP-01)" category if none fits) covering the behaviors that cannot be automated in the Wails native webview: clicking Help in the sidebar opens the Help tab; full Markdown renders (headings, paragraphs, inline code spans); the left section-nav active item tracks as the content pane scrolls (IntersectionObserver scroll-spy); typing a query produces debounced highlighted snippets and the "Go to section →" jump works; the GitHub and Website links open the system default browser (not the in-app webview). State the rationale: Wails native webview is inaccessible to Playwright/headless automation (DevTools disabled in prod). Tag the source as Phase 147 / HELP-01.
  </action>
  <verify>
    <automated>grep -q "M-14" TESTING.md && grep -qi "help" TESTING.md && bash tests/check-traceability-paths.sh && echo M14_OK</automated>
  </verify>
  <acceptance_criteria>
    - §5 contains an **M-14** item describing the live Help-page UAT (open, render, scroll-spy active state, debounced search highlight + jump, external links open system browser).
    - The item records the manual rationale (Wails native webview not automatable) and is sourced to Phase 147 / HELP-01.
    - `bash tests/check-traceability-paths.sh` still exits 0.
  </acceptance_criteria>
  <done>M-14 manual UAT item added for the Help page; path-check still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation/registration only; no code, no runtime input, no package install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-06 | Repudiation | regression-suite registration | accept | Registering tests in TESTING.md is documentation; no security boundary. The path-check script (`tests/check-traceability-paths.sh`) provides integrity verification that mapped paths exist. |
</threat_model>

<verification>
- `bash tests/check-traceability-paths.sh` exits 0.
- §2 vitest count = 116; §4 has four HELP-01 rows (path-only); §5 has M-14.
</verification>

<success_criteria>
- All four Help test files registered in the Suite Manifest + Traceability Map.
- M-14 manual UAT item added.
- Traceability path-check passes.
</success_criteria>

<output>
Create `.planning/phases/147-in-app-help-page/147-04-SUMMARY.md` when done.
</output>
