---
phase: 147-in-app-help-page
plan: 03
type: execute
wave: 3
depends_on: ["147-01", "147-02"]
files_modified:
  - frontend/src/components/TabBar.tsx
  - frontend/src/components/Sidebar.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [HELP-01]

must_haves:
  truths:
    - "The sidebar shows four items in order Home / Hub / Settings / Help"
    - "Clicking the Help sidebar item opens (or focuses) a single __help__ special tab"
    - "The Help tab stays mounted across tab switches (state preserved), like Settings"
    - "The Tab type union includes 'help' so tsc and the production build pass"
  artifacts:
    - path: "frontend/src/components/TabBar.tsx"
      provides: "Tab.type union extended with 'help'"
      contains: "'help'"
    - path: "frontend/src/components/Sidebar.tsx"
      provides: "4th sidebar item (Help) + onOpenHelp prop + active state"
      contains: "onOpenHelp"
    - path: "frontend/src/App.tsx"
      provides: "HELP_TAB constant, handleOpenHelp, display-toggle HelpTab render, tab-type exclusion, Sidebar wiring"
      contains: "HELP_TAB"
  key_links:
    - from: "frontend/src/components/Sidebar.tsx"
      to: "App.tsx handleOpenHelp"
      via: "onOpenHelp prop"
      pattern: "onOpenHelp"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/HelpTab.tsx"
      via: "display-toggle render block"
      pattern: "HelpTab"
---

<objective>
Wire the Help page into the app shell: extend the Tab type union with 'help', add the 4th sidebar item (Help) with its active state and onOpenHelp prop, and in App.tsx add HELP_TAB, handleOpenHelp, the display-toggle HelpTab render block, the tab-type exclusion, and the Sidebar onOpenHelp wiring. This turns the Sidebar 4-item assertions, the Help-button describe block, and the HelpTab App-routing source gate GREEN, and makes the page reachable.

Purpose: Make the implemented Help components actually reachable from navigation (D-01, D-02) without regressing the existing Settings/Hub re-attach behavior.
Output: Three modified shell files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/147-in-app-help-page/147-PATTERNS.md
@.planning/phases/147-in-app-help-page/147-RESEARCH.md
@.planning/phases/147-in-app-help-page/147-UI-SPEC.md

<interfaces>
TabBar.tsx:12   type?: 'terminal' | 'welcome' | 'settings' | 'file-browser' | 'hub'  → add '| 'help''
App.tsx:96-98   SETTINGS_TAB / HUB_TAB constants → add HELP_TAB { id:'__help__', name:'Help', sessionId:'', cli:'', type:'help' }
App.tsx:780     handleOpenSettings (find-or-add) → mirror as handleOpenHelp (t.type === 'help')
App.tsx:1375    Sidebar usage (onSettings/onOpenHub) → add onOpenHelp={handleOpenHelp}
App.tsx:1533    SettingsTab display-toggle render → mirror HelpTab render (display flex/none on activeId === HELP_TAB.id)
App.tsx:1597    tab-type exclusion list (welcome/settings/file-browser/hub) → add 'help'
Sidebar.tsx     sidebar__bottom: Settings button → add Help button below it; new prop onOpenHelp; active via activePanel === '__help__'; import QuestionMarkCircleIcon
HelpTab import: import { HelpTab } from './components/HelpTab'  (no props)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Tab type union + add Help sidebar item</name>
  <read_first>
    - frontend/src/components/TabBar.tsx (line 12 — the `type?:` discriminant union to extend with 'help')
    - frontend/src/components/Sidebar.tsx (lines 2-7 icon imports; lines 15-28 SidebarProps; line 225 Hub active-state pattern; lines 286-295 sidebar__bottom Settings button to extend)
    - frontend/src/components/__tests__/Sidebar.test.tsx (the 4-item count assertions + "Sidebar Help item (Phase 147)" describe block created in Plan 01 — this task turns them green)
    - .planning/phases/147-in-app-help-page/147-PATTERNS.md (§Sidebar.tsx — onOpenHelp prop, QuestionMarkCircleIcon import, Settings-above-Help order; §Tab Type Discriminant Union)
    - .planning/phases/147-in-app-help-page/147-UI-SPEC.md (§Copywriting Contract sidebar label "Help"/aria-label "Help"; §Layout Contract — Settings above Help in sidebar__bottom)
  </read_first>
  <behavior>
    - TabBar.tsx Tab.type union gains `'help'`.
    - Sidebar.tsx: SidebarProps gains `onOpenHelp: () => void`; QuestionMarkCircleIcon imported from '@heroicons/react/24/outline'; a Help `<button className={`sidebar__item${activePanel === '__help__' ? ' sidebar__item--active' : ''}`} onClick={onOpenHelp} aria-label="Help">` with QuestionMarkCircleIcon + (when not collapsed) `<span className="sidebar__label">Help</span>` is added INSIDE sidebar__bottom, directly BELOW the existing Settings button.
  </behavior>
  <action>
    Add `'help'` to the TabBar.tsx `type?:` union (otherwise tsc rejects HELP_TAB). In Sidebar.tsx add the onOpenHelp prop, the QuestionMarkCircleIcon import, and the Help button below Settings using the line-225 active-state pattern with `activePanel === '__help__'`. Do not reorder or remove the existing Home/Hub/Settings items.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && pnpm test -- Sidebar && echo SIDEBAR_OK</automated>
  </verify>
  <acceptance_criteria>
    - TabBar.tsx Tab.type union includes `'help'`.
    - Sidebar.test.tsx 4-item count assertions (lines ~95, ~243, ~389) pass; the "Sidebar Help item (Phase 147)" block passes (renders aria-label="Help", fires onOpenHelp, active only when activePanel === '__help__').
    - Help button sits below Settings in `sidebar__bottom`; Home/Hub/Settings unchanged.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Tab union extended; Help is the 4th sidebar item; Sidebar tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire HELP_TAB + handleOpenHelp + render block in App.tsx</name>
  <read_first>
    - frontend/src/App.tsx (lines 96-98 tab constants; lines 780-788 handleOpenSettings; lines 1373-1385 Sidebar usage; lines 1532-1556 SettingsTab display-toggle render; line 1597 tab-type exclusion list)
    - frontend/src/components/__tests__/HelpTab.test.tsx (App-routing source gates: HELP_TAB / '__help__' / type 'help' / handleOpenHelp — this task turns them green)
    - .planning/phases/147-in-app-help-page/147-PATTERNS.md (§App.tsx — every anchor: constant, handleOpenHelp, display-toggle mount (NOT conditional), exclusion list, Sidebar wiring)
    - .planning/phases/147-in-app-help-page/147-RESEARCH.md (§Pattern 1 — display:none mount strategy preserves scroll/activeSection; Pitfall 6 — tsc union)
  </read_first>
  <behavior>
    - Add `const HELP_TAB: Tab = { id: '__help__', name: 'Help', sessionId: '', cli: '', type: 'help' }` alongside SETTINGS_TAB/HUB_TAB.
    - Add `handleOpenHelp` useCallback mirroring handleOpenSettings exactly: find tab with `t.type === 'help'`, focus it if present, else append HELP_TAB and set active. deps `[tabs]`.
    - Import HelpTab from './components/HelpTab'.
    - Render HelpTab inside the terminal-container using the SettingsTab display-toggle pattern: `{mode !== 'web' && (<div style={{ display: activeId === HELP_TAB.id ? 'flex' : 'none', flexDirection: 'column', height: '100%' }}><HelpTab /></div>)}`. MUST be display-toggle, NOT `{activeId === HELP_TAB.id && <HelpTab/>}` (conditional mount loses scroll/activeSection state).
    - Add `'help'` to the line-1597 tab-type exclusion list so help tabs skip terminal rendering.
    - Add `onOpenHelp={handleOpenHelp}` to the Sidebar usage.
  </behavior>
  <action>
    Apply the five App.tsx edits per the interfaces. Keep handleOpenHelp's find-or-add idempotent (re-clicking Help focuses the existing tab, never duplicates). Use the display-toggle mount strategy. Verify the existing Settings/Hub re-attach Open behavior is untouched (do not regress the Phase-134 re-attach button or Settings state preservation).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && pnpm test && echo APP_WIRE_OK</automated>
  </verify>
  <acceptance_criteria>
    - HelpTab.test App-routing source gates pass: App.tsx contains HELP_TAB with `id: '__help__'` and `type: 'help'`, and a handleOpenHelp.
    - handleOpenHelp focuses an existing help tab if present (no duplicate __help__ tabs) and appends otherwise.
    - HelpTab render uses display flex/none toggle (NOT conditional mount); `'help'` is in the line-1597 exclusion list; Sidebar receives onOpenHelp.
    - `cd frontend && npx tsc --noEmit && pnpm test` is FULLY GREEN (entire frontend suite, all Help tests included).
    - No regression to existing welcome/settings/hub/file-browser tab routing.
  </acceptance_criteria>
  <done>Help reachable from sidebar as a single mounted special tab; full frontend suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Phase build gate — tsc + vite build</name>
  <read_first>
    - .planning/phases/147-in-app-help-page/147-VALIDATION.md (§Test Infrastructure build gate `cd frontend && npx tsc && vite build`)
    - CLAUDE.md memory rule: vitest tolerates TS errors the real Wails build rejects — the build gate is mandatory, not optional.
  </read_first>
  <action>
    Run the full build gate `cd frontend && npx tsc && vite build` to prove the app actually builds (not just that vitest passes). The `?raw` Markdown imports must inline into the bundle (offline; survives `-tags wailsassets`). Fix any tsc/build error surfaced (e.g. a missed union member or import path) before completing.
  </action>
  <verify>
    <automated>cd frontend && npx tsc && vite build && echo BUILD_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc && vite build` exits 0.
    - The built bundle inlines the `?raw` Markdown content (no separate `.md` network request at runtime).
    - No TypeScript errors anywhere in the frontend.
  </acceptance_criteria>
  <done>Production frontend build succeeds with the Help page wired in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidebar click → tab state | UI event toggles in-memory tab state; no external input |
| (no new runtime input boundary) | Wiring only; the search-input boundary is owned by Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-05 | Tampering | Tab type union / routing | accept | Pure client-side React state; adding `'help'` to a closed union is type-safe and inert; no privilege or data boundary crossed. |
| T-147-04 | Spoofing | external links (inherited) | mitigate | Verified end-to-end here: BrowserOpenURL buttons render, no `<a href>` reaches the webview; covered by HelpContent.test (Plan 02) and the M-14 live UAT (Plan 04). |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && pnpm test` — entire frontend suite GREEN.
- `cd frontend && npx tsc && vite build` — production build succeeds.
- Sidebar reads Home / Hub / Settings / Help; clicking Help opens a single mounted __help__ tab.
</verification>

<success_criteria>
- Tab union includes 'help'; Help is the 4th sidebar item; HELP_TAB + handleOpenHelp wired with display-toggle mount.
- Full frontend vitest suite green; `tsc && vite build` succeeds.
</success_criteria>

<output>
Create `.planning/phases/147-in-app-help-page/147-03-SUMMARY.md` when done.
</output>
