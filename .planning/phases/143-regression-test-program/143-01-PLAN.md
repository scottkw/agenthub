---
phase: 143-regression-test-program
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/hubGroupCounts.test.ts
  - frontend/src/lib/agentBadge.test.ts
  - frontend/src/components/__tests__/Sidebar.test.tsx
  - frontend/src/components/__tests__/style.hub.test.ts
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "computeCounts and computeGlobalCounts have automated tests asserting concrete counts — GAP-01 closure (D-08, D-09)"
    - "agentBadgeModifier has an automated test asserting its cli→modifier mapping including the null fallback — GAP-02 closure (D-08, D-09)"
    - "The 3-item sidebar contract is proven to hold even when group defs are supplied (groups render as sub-list items, not top-level nav items) — GAP-03 closure (D-09)"
    - "Phase 142 comp-fidelity CSS tokens (data-agent spine, hub-card__badge chip, 16px radius, 150px preview height) are pinned at source so they cannot silently regress — GAP-04 closure (D-09)"
    - "Cross-surface coverage is closed at the unit/component/contract seam, not via full 3-surface UI e2e (D-10)"
  artifacts:
    - path: "frontend/src/lib/hubGroupCounts.test.ts"
      provides: "GAP-01 coverage — computeCounts / computeGlobalCounts"
      contains: "computeGlobalCounts"
    - path: "frontend/src/lib/agentBadge.test.ts"
      provides: "GAP-02 coverage — agentBadgeModifier"
      contains: "agentBadgeModifier"
    - path: "frontend/src/components/__tests__/Sidebar.test.tsx"
      provides: "GAP-03 coverage — 3-item contract with groups present"
      contains: "sidebar__group-item"
    - path: "frontend/src/components/__tests__/style.hub.test.ts"
      provides: "GAP-04 coverage — Phase 142 comp-fidelity tokens"
      contains: "border-radius: 16px"
  key_links:
    - from: "frontend/src/lib/hubGroupCounts.test.ts"
      to: "frontend/src/lib/hubGroupCounts.ts"
      via: "import computeCounts, computeGlobalCounts"
      pattern: "from './hubGroupCounts'"
    - from: "frontend/src/lib/agentBadge.test.ts"
      to: "frontend/src/lib/agentBadge.ts"
      via: "import agentBadgeModifier"
      pattern: "from './agentBadge'"
---

<objective>
Close the four v4.0 release-critical automated coverage gaps (GAP-01..04) identified by the RESEARCH.md gap-analysis (D-08), each with ≥1 automated vitest test recorded against the traceability map (D-09). No new Go tests, no new Playwright specs, no production-code changes — these tests pin existing stable logic.

Purpose: The v4.0 surface (Hub group counts, session-type badge, 3-item sidebar, Phase 142 comp-fidelity tokens) currently has logic with zero automated coverage. Closing these gaps lets the regression suite test the v4.0 surface, not just pre-v4.0 code paths (TEST-03).
Output: Two new vitest files and two extended vitest files, all green under `pnpm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-regression-test-program/143-RESEARCH.md
@.planning/phases/143-regression-test-program/143-PATTERNS.md

<interfaces>
<!-- Exact source signatures verified from the codebase 2026-06-21. -->
<!-- IMPORTANT: RESEARCH.md/PATTERNS.md referenced agentColor/agentLabel and -->
<!-- .hub-card__spine/.hub-card__origin-chip — those identifiers DO NOT EXIST. -->
<!-- Use the real identifiers below. -->

From frontend/src/lib/hubGroupCounts.ts:
```typescript
export interface GroupCounts { running: number; total: number; waiting: number; attention: number }
export function computeCounts(sessions: SessionInfo[], memberKeys: Set<string>): GroupCounts
export function computeGlobalCounts(sessions: SessionInfo[]): GroupCounts
// memberKey(s.name, s.workDir) decides membership for computeCounts.
// running counter increments for running|idle|waiting; waiting for waiting only;
// attention via isAttentionStatus (waiting|errored|stopped-err).
```

From frontend/src/lib/agentBadge.ts:
```typescript
export function agentBadgeModifier(cli: string): string | null
// 'claude'|'opencode'|'codex'|'gemini'|'cursor'|'aider' → returns cli unchanged
// 'shell'|'bash'|'zsh'|'pwsh'|'powershell' → returns 'shell'
// anything else → returns null
```

From frontend/src/lib/hubStatus.ts (helpers the count logic depends on — for building fixtures):
```typescript
export function deriveHubStatus(s: SessionInfo): HubStatus
// state==='stopped' + exitCode!==0 → 'stopped-err'; state==='stopped' + exitCode 0/undefined → 'stopped-ok';
// otherwise s.status cast to HubStatus.
```

SessionInfo (frontend/src/wailsjs/go/main/App.d.ts) relevant fields:
`cli: string`, `name: string`, `status: string`, `state: string`, `exitCode?: number`, `workDir: string`, `hostname: string`.

Real Phase 142 comp-fidelity CSS tokens (frontend/src/style.css, verified line numbers 2026-06-21):
- `.hub-card { ... border-radius: 16px; ... }` (line ~4423)
- `.hub-card[data-agent="claude"] { border-left: 3px solid #7aa2f7; }` (spine, line ~4437; one rule per agent + `unknown`)
- `.hub-card__badge { ... }` (line ~4639) with `.hub-card[data-agent="claude"] .hub-card__badge { color: #7aa2f7; ... }` (chip tint, line ~4654)
- `height: 150px;` mini-preview window (line ~5367) — NOTE: `height`, not `min-height`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-01 + GAP-02 — pure-function lib tests (hubGroupCounts, agentBadge)</name>
  <files>frontend/src/lib/hubGroupCounts.test.ts, frontend/src/lib/agentBadge.test.ts</files>
  <read_first>
    - frontend/src/lib/hubGroupCounts.ts (function under test)
    - frontend/src/lib/agentBadge.ts (function under test — real export is agentBadgeModifier, NOT agentColor/agentLabel)
    - frontend/src/lib/hubStatus.ts (deriveHubStatus / isAttentionStatus — needed to build correct SessionInfo fixtures)
    - frontend/src/lib/hubStatus.test.ts (EXACT analog from PATTERNS.md — pure-function vitest pattern: import only {describe,it,expect}, one describe per function, one it per input class, direct toBe/toEqual)
    - frontend/src/lib/hubGroups.test.ts (secondary analog — multi-input fixture style, memberKey usage)
    - .planning/phases/143-regression-test-program/143-RESEARCH.md (Gap Analysis: GAP-01, GAP-02 rows)
  </read_first>
  <behavior>
    hubGroupCounts.test.ts:
    - computeGlobalCounts on [] returns {running:0,total:0,waiting:0,attention:0}
    - computeGlobalCounts counts running|idle|waiting toward `running`, waiting toward `waiting`, total = sessions.length
    - computeGlobalCounts counts a waiting session toward attention AND a stopped-err session toward attention (attention is the superset)
    - computeCounts only counts sessions whose memberKey(name,workDir) is in the supplied Set; sessions outside the Set are excluded from total
    - computeCounts on a member Set that matches 2 of 3 sessions returns total:2
    agentBadge.test.ts:
    - agentBadgeModifier('claude') === 'claude' (one it per known agent: claude, opencode, codex, gemini, cursor, aider)
    - agentBadgeModifier('bash') === 'shell' and agentBadgeModifier('zsh'|'pwsh'|'powershell'|'shell') === 'shell'
    - agentBadgeModifier('totally-unknown') === null (fallback)
  </behavior>
  <action>Create frontend/src/lib/hubGroupCounts.test.ts and frontend/src/lib/agentBadge.test.ts following the hubStatus.test.ts analog exactly: import only `{ describe, it, expect }` from 'vitest', import the functions under test from './hubGroupCounts' and './agentBadge'. One `describe` block per exported function. For hubGroupCounts, build SessionInfo fixtures as minimal object literals cast to the SessionInfo type (set name, workDir, status, state, exitCode as needed to drive deriveHubStatus); use `memberKey` from './hubGroups' to construct the Set passed to computeCounts. Assert concrete numeric values with `toBe`/`toEqual`. For agentBadge, one `it()` per cli input class per the behavior block, asserting the exact string or `null`. No mocks, no DOM, no beforeEach. This closes GAP-01 (D-08) and GAP-02 (D-08); the colorblind-safe hex check for the badge palette lives in Task 3's style gate, not here (agentBadgeModifier returns a modifier string, not a color).</action>
  <verify>
    <automated>cd frontend && pnpm test -- hubGroupCounts agentBadge</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/hubGroupCounts.test.ts` exists and imports `computeCounts` and `computeGlobalCounts` from './hubGroupCounts'
    - `frontend/src/lib/agentBadge.test.ts` exists and imports `agentBadgeModifier` from './agentBadge'
    - `cd frontend && pnpm test -- hubGroupCounts agentBadge` exits 0 with both files reporting passing tests
    - agentBadge test includes an assertion that `agentBadgeModifier` returns `null` for an unknown cli
  </acceptance_criteria>
  <done>Both new vitest files exist and pass; GAP-01 and GAP-02 closed with ≥1 automated test each.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-03 — sidebar 3-item contract holds with groups present</name>
  <files>frontend/src/components/__tests__/Sidebar.test.tsx</files>
  <read_first>
    - frontend/src/components/__tests__/Sidebar.test.tsx (SELF — the file being extended; reuse the existing `renderSidebar(overrides)` helper at lines ~40-61 and the existing "exactly 3 sidebar__item" test at line ~92)
    - frontend/src/components/Sidebar.tsx (render logic — group defs render as `.sidebar__group-item` inside `ul.sidebar__group-list`, NOT as `.sidebar__item`; `showGroupList = effectiveExpanded && groupDefs.length > 0`)
    - frontend/src/lib/hubGroups.ts (HubGroupDef shape for building group fixtures)
    - .planning/phases/143-regression-test-program/143-PATTERNS.md (Component render + cleanup pattern section)
  </read_first>
  <behavior>
    - With one or more groupDefs supplied AND the sidebar expanded, `container.querySelectorAll('button.sidebar__item').length` still equals exactly 3 (Home, Hub, Settings) — group entries do NOT inflate the top-level nav item count
    - With the same groupDefs, group entries render separately as `.sidebar__group-item` (count matches groupDefs length)
    - No element with aria-label "Sessions" or "Remote" appears regardless of group defs
  </behavior>
  <action>Extend frontend/src/components/__tests__/Sidebar.test.tsx with a new `describe('NAV-05 positive render contract — 3 items with groups present (GAP-03)')` block. Reuse the existing `renderSidebar` helper, passing `groupDefs` (one or two HubGroupDef objects) and whatever override forces the expanded state so `showGroupList` is true (inspect Sidebar.tsx for the expansion prop/condition). Assert `container.querySelectorAll('button.sidebar__item').length === 3` and `container.querySelectorAll('.sidebar__group-item').length === groupDefs.length`. Reuse the existing `afterEach` cleanup. Do NOT duplicate the existing no-groups "exactly 3" test (line ~92) — this task's value is proving the contract holds specifically WHEN groups are present, which the existing test does not cover.</action>
  <verify>
    <automated>cd frontend && pnpm test -- Sidebar</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/__tests__/Sidebar.test.tsx` contains a new describe block referencing GAP-03 / "3 items with groups present"
    - The new test queries both `button.sidebar__item` (expects 3) and `.sidebar__group-item` (expects groupDefs length)
    - `cd frontend && pnpm test -- Sidebar` exits 0
  </acceptance_criteria>
  <done>GAP-03 closed: the 3-item sidebar contract is proven to survive group rendering.</done>
</task>

<task type="auto">
  <name>Task 3: GAP-04 — pin Phase 142 comp-fidelity CSS tokens (anti-regression)</name>
  <files>frontend/src/components/__tests__/style.hub.test.ts</files>
  <read_first>
    - frontend/src/components/__tests__/style.hub.test.ts (SELF — the file being extended; reuse the file-scope `cssRaw = readFileSync(...style.css)` declaration at line ~9 and the `cssRaw.toContain(literal)` / block-scoped `indexOf`+`slice` patterns)
    - frontend/src/style.css (VERIFY the real token text before asserting — confirm at the lines noted in the interfaces block; do not assert a token you have not located in source)
    - .planning/phases/143-regression-test-program/143-RESEARCH.md (Gap Analysis GAP-04 row + Assumptions Log A4)
    - .planning/phases/143-regression-test-program/143-PATTERNS.md (CSS source-gate pattern section)
  </read_first>
  <action>Extend frontend/src/components/__tests__/style.hub.test.ts with a new `describe('Phase 142 comp-fidelity CSS tokens (GAP-04 anti-regression)')` block at the bottom of the file, reusing the existing file-scope `cssRaw`. Assert the REAL tokens located in style.css (NOT the placeholder names `.hub-card__spine`/`.hub-card__origin-chip` from PATTERNS.md, which do not exist): (a) `.hub-card` block contains `border-radius: 16px`; (b) the session-type spine exists via `cssRaw` containing `.hub-card[data-agent="claude"]` with `border-left: 3px solid #7aa2f7` (pin the exact hex per the colorblind-safe source-gate convention); (c) the color-coded chip exists via `.hub-card__badge` and `.hub-card[data-agent="claude"] .hub-card__badge` containing `color: #7aa2f7`; (d) the mini-preview window declares `height: 150px` (note: `height`, not `min-height`). Before writing each assertion, confirm the exact literal string is present in style.css by reading the relevant lines — if any token differs from this description, assert the actual source token and note the correction in the test comment. Use the block-scoped `indexOf`+`slice` pattern for multi-property rule assertions.</action>
  <verify>
    <automated>cd frontend && pnpm test -- style.hub</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/__tests__/style.hub.test.ts` contains a `GAP-04` describe block asserting `border-radius: 16px`, a `data-agent` spine hex, a `.hub-card__badge` chip token, and the `150px` preview height
    - Every asserted literal is verified present in `frontend/src/style.css` (no assertion references a non-existent class such as `.hub-card__spine`)
    - `cd frontend && pnpm test -- style.hub` exits 0
  </acceptance_criteria>
  <done>GAP-04 closed: the Phase 142 comp-fidelity tokens are pinned at source and cannot silently regress.</done>
</task>

</tasks>

<verification>
- All four gaps (GAP-01..04) have ≥1 automated vitest test each (D-09 bar).
- `cd frontend && pnpm test` runs the full vitest suite green (no regression from the new/extended files).
- The four target paths are stable repo-relative paths that Plan 03's traceability table will reference and Plan 02's path-check will validate.
</verification>

<success_criteria>
- `frontend/src/lib/hubGroupCounts.test.ts` and `frontend/src/lib/agentBadge.test.ts` exist and pass.
- `Sidebar.test.tsx` proves the 3-item contract with groups present; `style.hub.test.ts` pins Phase 142 tokens.
- `cd frontend && pnpm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/143-regression-test-program/143-01-SUMMARY.md` when done, listing the four test file paths (repo-relative) so Plan 03 can cite them in the traceability table.
</output>
