---
phase: 143-regression-test-program
plan: 03
type: execute
wave: 2
depends_on: ["143-01", "143-02"]
files_modified:
  - TESTING.md
  - CLAUDE.md
autonomous: true
requirements: [TEST-01, TEST-04, TEST-05]
must_haves:
  truths:
    - "A single TESTING.md at repo root holds the suite manifest naming each group with no build tags or file moves (D-01), the hand-maintained Markdown v4.0-scoped requirement→test traceability map (D-02, D-04), the recorded gh api branch-protection command for reproducibility (D-07), the manual regression checklist, and the standing convention"
    - "Every test path in the traceability table is repo-relative and resolves on disk so the path-check script exits 0 against the committed TESTING.md"
    - "A new repo-level CLAUDE.md points to TESTING.md and the standing convention so the rule surfaces to Claude every session"
    - "The manual checklist holds the 11 human-intervention items mined from the UAT logs (D-12), and the existing per-phase UAT logs are left in place as historical record (D-13)"
  artifacts:
    - path: "TESTING.md"
      provides: "Manifest + traceability map + gh api command + manual checklist + convention (TEST-01/02/04/05)"
      contains: "Standing Convention"
      min_lines: 80
    - path: "CLAUDE.md"
      provides: "Repo-level pointer to the convention (D-14)"
      contains: "TESTING.md"
  key_links:
    - from: "TESTING.md"
      to: "tests/check-traceability-paths.sh"
      via: "traceability table uses the path-column convention the script parses"
      pattern: "frontend/src/lib/hubGroupCounts.test.ts"
    - from: "CLAUDE.md"
      to: "TESTING.md"
      via: "pointer line referencing the standing convention"
      pattern: "TESTING.md"
---

<objective>
Author the single canonical TESTING.md at repo root (D-11) and the new repo-level CLAUDE.md pointer (D-14). TESTING.md consolidates: the suite manifest (D-01), the v4.0-scoped requirement→test traceability map (D-02/D-04), the exact gh api branch-protection command for reproducibility (D-07), the 11-item manual regression checklist (D-12), and the standing convention (D-14). CLAUDE.md is a short pointer making the convention visible to Claude every session.

Purpose: TEST-01 (label + map), TEST-04 (single manual checklist), TEST-05 (standing convention) all land here. The traceability map references the four new gap-closure test paths from Plan 01 and uses the column convention from Plan 02 so the path-check (now wired into CI) exits 0 against the committed file.
Output: TESTING.md and CLAUDE.md at repo root.

CRITICAL: Every path in the traceability table MUST be a real repo-relative path ending in .go/.ts/.tsx/.sh, surrounded by `| path |` so `tests/check-traceability-paths.sh` parses and validates it. Verify each path exists before committing by running the script locally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/143-regression-test-program/143-RESEARCH.md
@.planning/phases/143-regression-test-program/143-CONTEXT.md
@.planning/phases/143-regression-test-program/143-01-SUMMARY.md
@.planning/phases/143-regression-test-program/143-02-SUMMARY.md

<interfaces>
<!-- Source-of-truth content TESTING.md must encode (all from RESEARCH.md, verified 2026-06-21). -->

Suite Manifest counts (RESEARCH.md Suite Inventory — use the VERIFIED live counts, NOT CONTEXT.md's stale 115/459):
- Go unit/integration: 344 `*_test.go` — `go test -race -short ./...`
- vitest (frontend): 108 `*.test.ts/tsx` — `pnpm test` (vitest run)
- Playwright e2e: 7 `*.spec.ts` in frontend/e2e — `pnpm exec playwright test`
- build-script: 1 `tests/build-script.test.sh` — `bash tests/build-script.test.sh`
- Total: 460

Recommended section order (RESEARCH.md): 1 Overview · 2 Suite Manifest · 3 Merge Gate: How to Apply Branch Protection · 4 Requirement→Test Traceability Map · 5 Manual Regression Checklist · 6 Standing Convention.

Traceability table format the path-check requires: `| Requirement | repo/relative/test/path.ext | Suite Group | Notes |` — path-only in the path column (test/describe name goes in Notes), repo-relative, ending in .go/.ts/.tsx/.sh.

The 11 manual checklist items (M-01..M-11) are enumerated in RESEARCH.md "Manual Regression Checklist Items (Mined from UAT Logs)" across five categories (Share modal native GUI, remote peer live tests, terminal repaint, signed build, deferred file/editor UATs).

The exact gh api PUT command + rollback DELETE command are in RESEARCH.md "GitHub Branch Protection: Exact API Command" — copy verbatim into TESTING.md section 3 for reproducibility (5 checks: 4 `build (agenthub, ...)` contexts + `playwright`, app_id 15368, enforce_admins:false, required_pull_request_reviews:null, restrictions:null).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write TESTING.md (manifest + traceability map + gh api command + manual checklist + convention)</name>
  <files>TESTING.md</files>
  <read_first>
    - .planning/phases/143-regression-test-program/143-RESEARCH.md (Suite Inventory, TESTING.md Section Ordering, Traceability Table Structure, Manual Regression Checklist Items, GitHub Branch Protection Exact API Command, Path-Existence parsing convention, Common Pitfalls 1/5)
    - .planning/phases/143-regression-test-program/143-CONTEXT.md (D-01, D-02, D-04, D-07, D-11, D-12, D-13, D-14 — locked scope)
    - .planning/phases/143-regression-test-program/143-01-SUMMARY.md (the four new gap-closure test paths to add to the map)
    - .planning/phases/143-regression-test-program/143-02-SUMMARY.md (the path-column convention the table must follow)
    - .planning/REQUIREMENTS.md (v4.0 requirement IDs for the traceability map scope — D-04)
    - .github/workflows/build.yml and .github/workflows/e2e.yml (to name the suite-run commands accurately in the manifest)
  </read_first>
  <action>Create TESTING.md at repo root with the six sections in RESEARCH.md's recommended order. (1) Overview: what this doc is, that it is canonical and supersedes the scattered per-phase UAT logs (per D-13, add the one-line note that per-phase logs remain as historical record). (2) Suite Manifest: a table of the four groups with the VERIFIED counts (344 Go / 108 vitest / 7 Playwright / 1 build-script = 460), each group's location and run command, and one line on what each guards (per D-01 — no build tags, no file moves). (3) Merge Gate — How to Apply Branch Protection: paste the exact gh api PUT command AND the rollback DELETE command verbatim from RESEARCH.md, plus the Pitfall-1 note that any change to build.yml matrix entries requires re-applying protection with updated check names. (4) Requirement→Test Traceability Map: a Markdown table `| Requirement | path | Suite Group | Notes |` scoped to v4.0 requirement IDs (D-04 — NAV/SHARE/CARD/TAB/RDS/POL/CARRY/TEST), using the "Already Covered" + "Coverage Gaps (now closed)" rows from RESEARCH.md's Gap Analysis, INCLUDING the four new paths from the 143-01 summary (hubGroupCounts.test.ts, agentBadge.test.ts, Sidebar.test.tsx, style.hub.test.ts). Every path repo-relative, path-only in the path column, test/describe name in Notes. (5) Manual Regression Checklist: the 11 items M-01..M-11 from RESEARCH.md across the five categories, each with behavior + why-not-automatable + source. (6) Standing Convention: the per-phase rule (every future phase adds its regression tests to the appropriate group — automated vs human-intervention — and updates this traceability map) per D-14. Use accurate counts; do NOT write 115/459 (Pitfall 5).</action>
  <verify>
    <automated>test -f TESTING.md && grep -q 'Suite Manifest' TESTING.md && grep -q 'Standing Convention' TESTING.md && grep -q 'Manual Regression Checklist' TESTING.md && [ "$(grep -c 'M-[0-9]' TESTING.md)" -ge 11 ] && bash tests/check-traceability-paths.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `TESTING.md` exists at repo root containing the headings "Suite Manifest", "Traceability Map", "Manual Regression Checklist", and "Standing Convention"
    - `grep -c 'M-[0-9]' TESTING.md` ≥ 11 (manual checklist items)
    - The traceability map includes rows for `frontend/src/lib/hubGroupCounts.test.ts`, `frontend/src/lib/agentBadge.test.ts`, `frontend/src/components/__tests__/Sidebar.test.tsx`, `frontend/src/components/__tests__/style.hub.test.ts`
    - `bash tests/check-traceability-paths.sh` exits 0 against the committed TESTING.md (every mapped path resolves on disk)
    - TESTING.md contains the verbatim `gh api repos/scottkw/agenthub/branches/main/protection --method PUT` command and the `--method DELETE` rollback
    - The manifest shows 108 vitest and 460 total (NOT 115 / 459)
  </acceptance_criteria>
  <done>TESTING.md is the single canonical home for manifest + map + gate command + manual checklist + convention, and the path-check passes against it.</done>
</task>

<task type="auto">
  <name>Task 2: Write repo-level CLAUDE.md pointer (D-14)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - .planning/phases/143-regression-test-program/143-CONTEXT.md (D-14 — pointer line; today no repo-level CLAUDE.md exists, only the shared /Users/ken/dev/CLAUDE.md which is the wrong home for agenthub-specific rules)
    - TESTING.md (the file being pointed to — write this pointer AFTER Task 1 so the reference is accurate)
  </read_first>
  <action>Create CLAUDE.md at the agenthub repo root (this is a NEW file — none exists today; do not confuse with the shared /Users/ken/dev/CLAUDE.md). Keep it short: a pointer line stating that TESTING.md at repo root is the canonical regression-suite home, and the standing convention that every phase must add its regression tests to the appropriate group (automated vs human-intervention) and update the TESTING.md traceability map. Reference TESTING.md by name so the rule surfaces to Claude every session. Do not duplicate the full convention text — point to TESTING.md's Standing Convention section.</action>
  <verify>
    <automated>test -f CLAUDE.md && grep -q 'TESTING.md' CLAUDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` exists at the agenthub repo root
    - It contains a pointer line referencing `TESTING.md` and the standing convention
    - It does not redefine or fork the convention — it points to TESTING.md's Standing Convention section
  </acceptance_criteria>
  <done>Repo-level CLAUDE.md surfaces the standing convention and points to TESTING.md every session (D-14).</done>
</task>

</tasks>

<verification>
- `bash tests/check-traceability-paths.sh` exits 0 against committed TESTING.md (all mapped paths resolve — this is now the live CI gate from Plan 02).
- TESTING.md has all six sections; manual checklist ≥ 11 items; manifest uses 108/460.
- CLAUDE.md points to TESTING.md.
</verification>

<success_criteria>
- TESTING.md is the single canonical doc (TEST-01 manifest+map, TEST-04 checklist, TEST-05 convention) and the gh api command is recorded for Plan 04.
- CLAUDE.md pointer exists (D-14).
- Path-check passes.
</success_criteria>

<output>
Create `.planning/phases/143-regression-test-program/143-03-SUMMARY.md` when done. Record that TESTING.md section 3 holds the exact gh api PUT command Plan 04 will execute, and confirm the path-check passes against the committed map.
</output>
