---
phase: 143-regression-test-program
plan: 04
type: execute
wave: 3
depends_on: ["143-03"]
files_modified: []
autonomous: false
requirements: [TEST-02]
user_setup:
  - service: github
    why: "Apply branch protection on scottkw/agenthub main (gh is authed as scottkw, admin)"
    dashboard_config:
      - task: "Confirm the branch-protection mutation at the checkpoint before it is applied"
        location: "Repo Settings → Branches → main (or via the gh api command)"
must_haves:
  truths:
    - "main on scottkw/agenthub requires all 5 CI status check contexts (4 build matrix jobs + playwright) to pass"
    - "enforce_admins is false and required_pull_request_reviews is null — admin direct push and the GSD .planning/ doc-commit flow are preserved"
    - "A failing CI check on a PR blocks the merge gate"
  artifacts: []
  key_links:
    - from: "GitHub repo settings (branch protection on main)"
      to: ".github/workflows/build.yml + e2e.yml check contexts"
      via: "required_status_checks.checks[] pins the exact context names + app_id 15368"
      pattern: "build (agenthub, .*)|playwright"
---

<objective>
Apply branch protection on `scottkw/agenthub` `main` via `gh api` (D-05/D-06/D-07), requiring the five CI status check contexts (four `build (agenthub, ...)` matrix jobs + `playwright`) while keeping `enforce_admins: false`, `required_pull_request_reviews: null`, and `restrictions: null` so admin direct push and the GSD `.planning/` doc-commit flow are preserved. This is the merge gate (TEST-02).

Purpose: The existing CI already runs on every push and PR; what is missing is the required-check enforcement. This plan attaches it. Because this mutates a live GitHub repo security-relevant control, it pauses at a confirmation checkpoint BEFORE the mutation (D-07, autonomous:false).
Output: Branch protection active on main; verified via `gh api` GET; the exact command already recorded in TESTING.md (Plan 03) for reproducibility.

This plan modifies NO repo files — branch protection is a GitHub repo setting that lives outside the repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-regression-test-program/143-RESEARCH.md
@.planning/phases/143-regression-test-program/143-CONTEXT.md
@TESTING.md

<interfaces>
<!-- Exact gh api command from RESEARCH.md (live-verified check names + app_id 15368, 2026-06-21). -->
<!-- It is also recorded verbatim in TESTING.md section 3 (Plan 03). -->
PUT payload requires these 5 contexts (all app_id 15368):
- "build (agenthub, linux/amd64, ubuntu-latest, webkit2_41, libwebkit2gtk-4.1-dev)"
- "build (agenthub, linux/amd64, ubuntu-22.04, libwebkit2gtk-4.0-dev)"
- "build (agenthub, darwin/universal, macos-latest)"
- "build (agenthub, windows/amd64, windows-latest)"
- "playwright"
With: strict:false, enforce_admins:false, required_pull_request_reviews:null, restrictions:null.

Verify GET commands (RESEARCH.md Validation Architecture):
- `gh api repos/scottkw/agenthub/branches/main/protection --jq '.required_status_checks.checks | length'` → expect 5
- `gh api repos/scottkw/agenthub/branches/main/protection --jq '.enforce_admins.enabled'` → expect false

Rollback: `gh api repos/scottkw/agenthub/branches/main/protection --method DELETE`
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm branch-protection mutation before applying</name>
  <decision>Apply branch protection to scottkw/agenthub main now?</decision>
  <context>This mutates a live GitHub repo security-relevant control. It is reversible via the recorded DELETE rollback. The locked design (D-06) keeps enforce_admins:false and required_pull_request_reviews:null specifically so the GSD direct-to-main .planning/ doc-commit flow keeps working — admin pushes bypass the gate; only PR merges are gated on the 5 checks. Confirm the exact command (recorded in TESTING.md section 3) and that CI is currently green on main before applying.</context>
  <options>
    <option id="apply">
      <name>Apply now</name>
      <pros>Merge gate becomes active immediately; TEST-02 satisfied</pros>
      <cons>Live repo setting changes (reversible via DELETE rollback)</cons>
    </option>
    <option id="defer">
      <name>Defer</name>
      <pros>No live mutation yet</pros>
      <cons>TEST-02 not satisfied; phase cannot complete</cons>
    </option>
  </options>
  <resume-signal>Select: apply or defer</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply branch protection via gh api and verify</name>
  <files></files>
  <read_first>
    - .planning/phases/143-regression-test-program/143-RESEARCH.md ("GitHub Branch Protection: Exact API Command" — the verbatim PUT payload, key-field rationale, rollback, and non-TTY heredoc note; Common Pitfalls 2 and 4 on enforce_admins/strict)
    - TESTING.md (section 3 — the recorded reproducible command must match what is executed here)
    - .planning/phases/143-regression-test-program/143-CONTEXT.md (D-05/D-06/D-07 — required-check model, admin-push-allowed, no PR requirement)
  </read_first>
  <action>Run the exact `gh api repos/scottkw/agenthub/branches/main/protection --method PUT` command from RESEARCH.md (the `--input - <<'EOF' ... EOF` heredoc form, which works non-TTY) with the 5 check contexts (app_id 15368), strict:false, enforce_admins:false, required_pull_request_reviews:null, restrictions:null. Then verify with the two GET --jq commands: `.required_status_checks.checks | length` must equal 5, and `.enforce_admins.enabled` must be false. Do NOT alter the check context strings — they must match the live build.yml matrix job names exactly (a typo silently requires a nonexistent check, making the gate pass nothing — the path-check + correct names mitigate this per the threat model). If the PUT returns an error, STOP and report (per RULE 0) — do not retry blindly or fall back to deprecated `contexts[]` without confirming the cause.</action>
  <verify>
    <automated>[ "$(gh api repos/scottkw/agenthub/branches/main/protection --jq '.required_status_checks.checks | length')" = "5" ] && [ "$(gh api repos/scottkw/agenthub/branches/main/protection --jq '.enforce_admins.enabled')" = "false" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `gh api repos/scottkw/agenthub/branches/main/protection --jq '.required_status_checks.checks | length'` returns `5`
    - `gh api repos/scottkw/agenthub/branches/main/protection --jq '.enforce_admins.enabled'` returns `false`
    - The five required contexts match the build.yml matrix job names + `playwright` exactly (no drift)
    - A direct `.planning/` doc commit to main still succeeds (admin push not blocked) — confirmed by this phase's own subsequent doc commits landing on main
  </acceptance_criteria>
  <done>main requires the 5 CI checks; admin direct push preserved; TEST-02 satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the gate actually blocks a failing PR</name>
  <what-built>Branch protection requiring 5 CI checks on main, with admin direct push preserved.</what-built>
  <how-to-verify>
    Smoke-test the gate (RESEARCH.md Validation Architecture, TEST-02 row):
    1. Open a draft PR from a throwaway branch that introduces a deliberately failing vitest (e.g. a one-line `expect(true).toBe(false)`).
    2. Wait for CI to run; confirm the `build (agenthub, ...)` ubuntu-latest check goes red.
    3. Confirm the PR's merge button is blocked ("Required statuses must pass").
    4. Close the draft PR and delete the throwaway branch (do not delete test artifacts on main).
    5. Separately confirm a normal `.planning/` doc commit pushed directly to main by admin still lands (it bypasses the gate by design, D-06).
  </how-to-verify>
  <resume-signal>Type "approved" once the failing PR is blocked and the admin doc-push still works, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GSD admin push → main | Trusted admin (scottkw) commits .planning/ docs directly; must NOT be blocked by the gate |
| Contributor PR → main | Code lands via PR; must be gated on the 5 CI checks |
| build.yml matrix config → required-check names | The check-context strings are derived from matrix job names; drift here silently weakens the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Denial of Service (availability/process) | Branch protection misconfigured to block GSD admin doc-push | mitigate | Lock enforce_admins:false + required_pull_request_reviews:null (D-06); Task 2 verifies `.enforce_admins.enabled` is false; Task 3 confirms a live admin doc-push still lands. Rollback DELETE recorded in TESTING.md. |
| T-143-02 | Tampering / Spoofing | Required-check context names drift from build.yml matrix job names, so the gate requires a nonexistent check and silently passes nothing | mitigate | Pin exact context strings + app_id 15368 from RESEARCH.md live query; Task 2 asserts `checks | length == 5`; TESTING.md Pitfall-1 note requires re-applying protection on any matrix edit; the path-check (Plan 02) guards the related traceability-map drift. |
| T-143-03 | Elevation / Bypass | strict:true would force branches up-to-date even for admin, adding friction and false confidence | accept | strict:false is the locked design (D-06, Pitfall 4); admin direct push is intentional for the solo+agent workflow. No new auth surface introduced. |
| T-143-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero new packages (RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `gh api ... --jq '.required_status_checks.checks | length'` returns 5.
- `gh api ... --jq '.enforce_admins.enabled'` returns false.
- A failing-CI PR is blocked; an admin `.planning/` doc push to main still lands.
</verification>

<success_criteria>
- Branch protection on main requires the 5 CI check contexts (TEST-02).
- enforce_admins:false and required_pull_request_reviews:null preserve the GSD admin doc-commit flow.
- The gate demonstrably blocks a failing PR.
</success_criteria>

<output>
Create `.planning/phases/143-regression-test-program/143-04-SUMMARY.md` when done. Record the verified check count (5), enforce_admins:false, and the smoke-test result. Note the rollback command location (TESTING.md section 3) for future reference.
</output>
