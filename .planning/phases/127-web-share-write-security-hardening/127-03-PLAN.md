---
phase: 127-web-share-write-security-hardening
plan: 03
type: execute
wave: 2
depends_on: ["127-01"]
files_modified:
  - internal/files/write_test.go
  - .planning/phases/127-web-share-write-security-hardening/127-SECURITY.md
autonomous: true
requirements: [SEC-04, SEC-05]
must_haves:
  truths:
    - "Two concurrent writers with the same If-Match validator produce exactly one success and one ErrPreconditionFailed; the file ends as one writer's complete content, never interleaved"
    - "An interrupted/failed WriteFileAtomic leaves the original file content intact and no .agenthub-tmp-* sibling behind"
    - "127-SECURITY.md exists and is committed under .planning/, documenting the per-surface capability-escalation enforcement matrix, the denylist threat model, and the residual-risk register"
    - "The SECURITY artifact records the daemon loopback socket as an ACCEPTED, DOCUMENTED risk (not a finding)"
  artifacts:
    - path: ".planning/phases/127-web-share-write-security-hardening/127-SECURITY.md"
      provides: "Capability-escalation audit + per-surface enforcement matrix + residual-risk register"
      contains: "Capability-Escalation Audit"
      min_lines: 60
    - path: "internal/files/write_test.go"
      provides: "TestWrite_TwoWritersIfMatchRace + interrupted-write/original-preserved test"
      contains: "TestWrite_TwoWritersIfMatchRace"
  key_links:
    - from: "127-SECURITY.md per-surface matrix"
      to: "requireFilesWrite / HasPerm / requireCapability SID scope / proxyRemoteFiles cap-strip"
      via: "documented enforcement path per surface (webserver, daemon socket, remote proxy, cross-session)"
      pattern: "requireFilesWrite"
    - from: "TestWrite_TwoWritersIfMatchRace"
      to: "WriteFileAtomic validator re-check (sandbox.go:256-276)"
      via: "two goroutines, same If-Match validator → exactly one success"
      pattern: "WriteFileAtomic"
---

<objective>
Produce the committed SECURITY artifact for the capability-escalation audit (SEC-04) and add the data-integrity tests SEC-05 requires (two-writer If-Match race + interrupted-write/original-preserved).

Purpose: SEC-04's deliverable IS the documented escalation audit — every enforcement path was verified correct during research (no token lacking files.write reaches any write endpoint on any surface; files.write does not leak across sessions). The artifact transcribes the finite, fully-enumerated audit table into a committed `.planning/` file following the 83/74/60-SECURITY.md precedent. SEC-05 requires explicit tests that concurrent writes and failed writes never leave a corrupt/partial file — the atomic temp+rename + validator re-check already guarantee this; these are confirming tests.

Output: 127-SECURITY.md (committed); TestWrite_TwoWritersIfMatchRace + interrupted-write test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-web-share-write-security-hardening/127-RESEARCH.md
@.planning/phases/127-web-share-write-security-hardening/127-PATTERNS.md
@.planning/milestones/v3.0-phases/83-settings-ui-alignment/83-SECURITY.md

<interfaces>
<!-- SECURITY artifact content is fully enumerated — transcribe, do not re-audit. -->
Capability-Escalation Audit table: 127-RESEARCH.md lines 131-143. Per-surface enforcement detail: 127-PATTERNS.md "Capability enforcement" section (lines 244-272). Source anchors to cite:
- Webserver write gate: requireFilesWrite then requireCapability (HMAC+SID+grant+session-enabled) then HasPerm(claims.Perms, PermFilesWrite) then originAllowedForWrite (capability_mw.go); HasPerm is whole-token strings.Split, NOT strings.Contains (capability.go:51)
- CSRF Origin inversion: originAllowedForWrite — absent Origin passes vacuously (desktop Wails), present must byte-match ws.BaseURL(), empty BaseURL fails closed (capability_mw.go)
- Cross-session scoping: requireCapability rejects claims.SID != pathID → 403
- Remote proxy: proxyRemoteFiles strips caller ?cap, force-sets session from path, injects stored cap, forwards body; remote peer's requireFilesWrite enforces (remote_files.go:175-226)
- Daemon Unix-socket/named-pipe: NO cap enforcement BY DESIGN (loopback trust, WEB-01) → ACCEPTED DOCUMENTED risk, not a finding
- Owner opt-in: files.write added to owner token only when filesWriteEnabledFor(sid); viewer token bare "read" (api.go:1070-1079)
Residual-risk register: microscopic stat→rename TOCTOU window in If-Match re-check (documented sandbox.go:198-203, cannot be eliminated without kernel renameat2 — accepted A3); NFC/NFD denylist residual LOW (all protected names ASCII — A1 / Plan 01 decision).

Prior SECURITY artifact frontmatter shape (83-SECURITY.md): phase, slug, status, threats_open, asvs_level, created. Sections: Trust Boundaries / Threat Register / Accepted Risks Log / Security Audit Trail.

Existing data-integrity tests (extend, do not duplicate): TestWriteFileAtomic_ConcurrentReadNeverPartial (write_test.go:145), TestWriteFileAtomic_ValidatorRecheck (write_test.go:757), TestWrite_IfMatch_* (write_test.go:644-707). WriteFileAtomic signature: WriteFileAtomic(relPath string, content []byte, expectedValidator ...string) error; returns ErrPreconditionFailed when validator mismatches. Validator format: mtime-UnixNano + size, quoted.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author and commit 127-SECURITY.md (capability-escalation audit)</name>
  <read_first>
    - .planning/milestones/v3.0-phases/83-settings-ui-alignment/83-SECURITY.md (heading shape, frontmatter, table format to follow)
    - 127-RESEARCH.md lines 131-143 (Capability-Escalation Audit table — the verified findings to transcribe)
    - 127-PATTERNS.md lines 244-272 (per-surface enforcement detail with source anchors)
  </read_first>
  <action>
    Create `.planning/phases/127-web-share-write-security-hardening/127-SECURITY.md` following the 83-SECURITY.md structure (frontmatter: phase 127, slug web-share-write-security-hardening, status verified, threats_open 0, asvs_level 1, created 2026-06-15). Include:

    1. Trust Boundaries — home-rooted session to host FS; tailnet browser to web-share write routes; caller to daemon loopback socket; local daemon to remote peer proxy.
    2. Per-surface capability-escalation enforcement matrix (transcribe RESEARCH lines 133-141): for each surface (webserver PUT/POST/DELETE write routes, webserver HEAD canWrite probe, daemon Unix socket / named pipe, remote proxy, cross-session, token tampering, viewer token default) record "reachable without files.write?" + enforcement path + verdict, citing source anchors (capability_mw.go requireFilesWrite/HasPerm/originAllowedForWrite, capability.go:51, remote_files.go:175-226, api.go:1070-1079).
    3. Denylist threat model — the denylistCheck chokepoint covers all four write methods; the two Plan 01 fixes (macOS config-dir hole + case-fold) and what each closes.
    4. Threat Register table (STRIDE) aligned with the phase threat IDs (T-127-01..08) and dispositions.
    5. Accepted Risks Log — explicitly record the daemon loopback socket as ACCEPTED/DOCUMENTED (WEB-01 loopback trust, not a web-share surface); the microscopic stat-to-rename TOCTOU window in If-Match re-check (sandbox.go:198-203, requires kernel renameat2, not eliminable in Go stdlib); NFC/NFD denylist residual LOW (all protected names ASCII).
    6. ASVS L1 mapping (V1/V2/V4/V5/V12/V13 per RESEARCH lines 213-221).

    State the CSRF Origin inversion explicitly (originAllowedForWrite is the INVERSE of the WS-upgrade requireAllowedOrigin: absent Origin passes for write). Do NOT spawn a separate auditor agent — the audit set is finite and verified (RESEARCH Open Q2). This documents already-correct code, not new findings.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && test -f .planning/phases/127-web-share-write-security-hardening/127-SECURITY.md && grep -qi "capability-escalation\|capability escalation" .planning/phases/127-web-share-write-security-hardening/127-SECURITY.md && grep -qi "accepted" .planning/phases/127-web-share-write-security-hardening/127-SECURITY.md && grep -qi "loopback" .planning/phases/127-web-share-write-security-hardening/127-SECURITY.md</automated>
  </verify>
  <done>127-SECURITY.md exists with the per-surface enforcement matrix, STRIDE threat register, accepted-risks log (loopback socket explicitly accepted), residual-risk register, and ASVS mapping. It is committed under .planning/.</done>
  <acceptance_criteria>
    - `test -f .planning/phases/127-web-share-write-security-hardening/127-SECURITY.md` exits 0
    - File contains a per-surface enforcement matrix covering webserver, daemon socket, remote proxy, and cross-session
    - File records the daemon loopback socket as an accepted/documented risk (grep "loopback" matches)
    - File documents the stat-to-rename TOCTOU residual and the NFC/NFD LOW residual
    - File ≥ 60 lines (`grep -v '^#' file | grep -c . ` ≥ 40 substantive lines)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SEC-05 data-integrity tests — two-writer If-Match race + interrupted-write</name>
  <read_first>
    - internal/files/write_test.go:142-200 (TestWriteFileAtomic_ConcurrentReadNeverPartial — concurrency harness pattern)
    - internal/files/write_test.go:753-840 (TestWriteFileAtomic_ValidatorRecheck — validator re-check + ErrPreconditionFailed assertion pattern)
    - internal/files/sandbox.go:214-285 (WriteFileAtomic — temp+Sync+rename, validator re-check at 256-276, temp suffix .agenthub-tmp-)
  </read_first>
  <action>
    Add to write_test.go:

    TestWrite_TwoWritersIfMatchRace — seed a target file with known content; compute its current validator (reuse the same validator-derivation the existing TestWriteFileAtomic_ValidatorRecheck uses). Launch two goroutines that both call WriteFileAtomic(target, distinct-content, sameValidator) concurrently, collecting each return error via a buffered channel + sync.WaitGroup. Assert exactly one returns nil and exactly one returns ErrPreconditionFailed (errors.Is). After both complete, read the target and assert its content equals exactly one of the two writers' payloads (never a byte-interleaved mix), and that no .agenthub-tmp-* sibling remains in the directory (filepath.Glob the temp pattern → empty).

    TestWrite_InterruptedWritePreservesOriginal — seed a target with known original content; trigger a WriteFileAtomic failure path that occurs AFTER the temp file is created but BEFORE the rename commits (e.g. pass a validator that will mismatch so the re-check returns ErrPreconditionFailed and removes the temp, OR force a write error via an unwritable target name). Assert: the call returns a non-nil error, the original file content is unchanged (re-read equals original), and no .agenthub-tmp-* sibling is left behind (Glob empty). If a fully deterministic mid-write interruption is not reachable through the public API, drive it via the validator-mismatch path (which exercises the same "temp created, rename skipped, temp removed, original preserved" guarantee) and document that choice in a comment.

    Run with -race.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -race ./internal/files/ -run 'TestWrite_TwoWritersIfMatchRace|TestWrite_InterruptedWritePreservesOriginal' -count=1 -v</automated>
  </verify>
  <done>Both tests pass under -race: the two-writer race yields exactly one success + one ErrPreconditionFailed with non-interleaved final content; the interrupted write leaves the original intact and no temp sibling.</done>
  <acceptance_criteria>
    - `go test -race ./internal/files/ -run 'TestWrite_TwoWritersIfMatchRace|TestWrite_InterruptedWritePreservesOriginal' -count=1` exits 0
    - `grep -c "TestWrite_TwoWritersIfMatchRace" internal/files/write_test.go` == 1
    - `grep -c "TestWrite_InterruptedWritePreservesOriginal" internal/files/write_test.go` == 1
    - Race test asserts exactly one nil + one ErrPreconditionFailed (errors.Is)
    - Both tests assert no .agenthub-tmp-* sibling remains (filepath.Glob empty)
    - No production source modified
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tailnet browser → web-share write routes | Untrusted browser must present a valid files.write cap + matching Origin to reach any write endpoint |
| caller → daemon loopback socket | In-process GUI/TUI only; loopback trust (WEB-01) — no cap enforcement by design |
| local daemon → remote peer proxy | Proxy strips caller cap and injects stored cap; remote peer is the enforcer |
| two concurrent writers → same target file | Optimistic-concurrency boundary; If-Match + atomic temp+rename prevent torn writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-07 | Elevation | capability escalation (no-perm / cross-session / token tamper) across all write surfaces | mitigate (verify + document) | HasPerm whole-token + SID-scoped tokens + opt-in issuance + remote-proxy cap-strip; enumerated and verified in 127-SECURITY.md per-surface matrix |
| T-127-08 | Tampering/data-integrity | concurrent-write lost update / torn file / leftover temp | mitigate (verify) | If-Match 412 + atomic temp+Sync+rename + validator re-check; TestWrite_TwoWritersIfMatchRace + TestWrite_InterruptedWritePreservesOriginal assert exactly-one-winner and original-preserved |
| T-127-09 | Information Disclosure / Elevation | daemon loopback socket reaches write routes without a cap | accept | Loopback trust boundary (WEB-01); only in-process GUI/TUI reach it; not a web-share surface — documented in Accepted Risks Log |
| T-127-10 | Tampering | stat→rename TOCTOU residual in If-Match re-check | accept | Microscopic local-FS window; not eliminable without kernel renameat2 (unavailable in Go stdlib) — documented residual (sandbox.go:198-203) |
</threat_model>

<verification>
- `go test -race ./internal/files/ -count=1` green
- 127-SECURITY.md committed with per-surface matrix + accepted risks + residual register
</verification>

<success_criteria>
SEC-04 (escalation audit artifact) and SEC-05 (data-integrity tests) closed: the committed SECURITY artifact documents every verified enforcement path and accepted risk, and the new tests prove concurrent/interrupted writes never leave a corrupt, partial, or orphaned file.
</success_criteria>

<output>
Create `.planning/phases/127-web-share-write-security-hardening/127-03-SUMMARY.md` when done
</output>
