---
phase: 127-web-share-write-security-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/files-write.spec.ts
autonomous: true
requirements: [SEC-07]
must_haves:
  truths:
    - "A web-share PUT /api/files/write with a VALID files.write cap but a mismatched Origin header (https://evil.example.com) is rejected with HTTP 403"
    - "Existing scenarios — viewer-with-cap writes 200, viewer-without-cap gets 403 — remain green cross-browser"
  artifacts:
    - path: "frontend/e2e/files-write.spec.ts"
      provides: "CSRF Origin-mismatch e2e cell using standalone APIRequestContext"
      contains: "evil.example.com"
  key_links:
    - from: "Origin-mismatch e2e cell"
      to: "originAllowedForWrite (capability_mw.go) running AFTER the cap check"
      via: "PUT with valid cap + mismatched Origin → 403"
      pattern: "Origin.*evil"
---

<objective>
Add the missing CSRF Origin-mismatch web-share e2e scenario (SEC-07). The Origin-mismatch defense is comprehensively unit-tested for all five write routes (capability_test.go:690), and files-write.spec.ts already covers viewer-with-cap write-OK (scenario 1), web-share mount (scenario 2), and 403-without-cap (scenario 3). The only gap is the browser-level proof that a fully-capable token is STILL rejected when the Origin does not match.

Purpose: SEC-07 explicitly asks for the e2e cell. This proves the cap check and the Origin check are both enforced end-to-end on the web-share surface and that order is load-bearing (a valid cap does not bypass CSRF). No new middleware — the check exists; this is the missing test cell only.

Output: one new Origin-mismatch scenario in files-write.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-web-share-write-security-hardening/127-RESEARCH.md
@.planning/phases/127-web-share-write-security-hardening/127-PATTERNS.md

<interfaces>
<!-- Request-shape analog: standalone APIRequestContext smoke, files-write.spec.ts:570-588 -->
Imports already present (spec line 31-32): `import { test, expect, request as playwrightRequest } from '@playwright/test'`; `import { loadFixtureEnv, ... } from './fixture-env'`.
Fixture env: `const env = loadFixtureEnv()` → `env.writeCap` (valid files.write cap), `env.baseURL`. Session param is the literal string `'playwright-test-session'`.
Standalone request pattern (spec:570-588): `const ctx = await playwrightRequest.newContext({ ignoreHTTPSErrors: true })`; build URLSearchParams `{ session, path, cap }`; `await ctx.put(url, { headers, data })`; `finally { await ctx.dispose() }`.
Behavior anchor: originAllowedForWrite (capability_mw.go) returns false when a PRESENT Origin != ws.BaseURL(); requireFilesWrite runs the Origin check AFTER the cap check, so a 403 with a valid writeCap proves CSRF rejection, not a cap failure.
Spec runs serial: `test.describe.configure({ mode: 'serial' })` (line 34) — keep the new cell consistent (no parallel-only assumptions).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSRF Origin-mismatch e2e scenario to files-write.spec.ts</name>
  <read_first>
    - frontend/e2e/files-write.spec.ts:570-588 (standalone APIRequestContext smoke — the request-shape analog to copy)
    - frontend/e2e/files-write.spec.ts:31-34 (imports + serial config — reuse, no new imports needed)
    - 127-PATTERNS.md "files-write.spec.ts — Origin-mismatch scenario" section (lines 193-224) — the exact request shape and behavior anchor
  </read_first>
  <action>
    Add a new `test(...)` cell to files-write.spec.ts mirroring the standalone APIRequestContext smoke at line 570. Use `loadFixtureEnv()`, create a standalone `playwrightRequest.newContext({ ignoreHTTPSErrors: true })`, build URLSearchParams with `session: 'playwright-test-session'`, a unique `path` (e.g. `origin-test-${Date.now()}.txt`), and `cap: env.writeCap` (the VALID write cap). Issue `ctx.put(url, { headers: { 'Content-Type': 'application/octet-stream', Origin: 'https://evil.example.com' }, data: 'csrf attempt' })`. Assert `resp.status()` === 403 with a message like 'mismatched Origin must 403 even with valid write cap'. Dispose the context in a finally block.

    Do NOT touch the originAllowedForWrite middleware or any Go source — the check is implemented and unit-tested; this is the e2e cell only. Keep the cell consistent with the existing serial describe config; do not introduce parallel-only patterns. Reuse existing imports (no new import lines needed).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm exec playwright test files-write --reporter=line</automated>
  </verify>
  <done>The new Origin-mismatch cell asserts 403 with a valid write cap and passes; scenarios 1-3 (write-OK / mount / 403-no-cap) and the standalone smoke remain green across the configured browser engines.</done>
  <acceptance_criteria>
    - `pnpm exec playwright test files-write` exits 0 (all cells green, including the new one)
    - `grep -c "evil.example.com" frontend/e2e/files-write.spec.ts` ≥ 1
    - The new cell uses env.writeCap (a VALID cap) so the 403 proves Origin rejection, not a cap failure
    - No Go source file modified by this plan
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cross-origin browser → web-share write routes | A page on an attacker origin (https://evil.example.com) attempts a state-changing PUT against the web-share surface with a leaked/valid cap; the Origin check must reject it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-11 | Spoofing/Tampering | CSRF from a tailnet/cross-origin browser on PUT/POST/DELETE write routes | mitigate (verify) | originAllowedForWrite byte-matches a present Origin against ws.BaseURL(), runs after the cap check; Playwright Origin-mismatch cell proves a valid cap is still 403'd cross-origin |
</threat_model>

<verification>
- `pnpm exec playwright test files-write` green (new Origin-mismatch cell + scenarios 1-3 + smoke)
- No Go source modified
</verification>

<success_criteria>
SEC-07 closed: the web-share write e2e now proves viewer-with-cap writes 200, viewer-without-cap gets 403, AND a CSRF Origin-mismatch PUT is rejected with 403 even with a valid files.write cap.
</success_criteria>

<output>
Create `.planning/phases/127-web-share-write-security-hardening/127-04-SUMMARY.md` when done
</output>
