---
phase: 166-funnel-frontend-help-guide
plan: 02
type: execute
wave: 1
depends_on: [166-01]
files_modified:
  - frontend/src/components/Hub/FunnelRiskPanel.tsx
  - frontend/src/components/__tests__/FunnelRiskPanel.test.tsx
  - frontend/src/components/Hub/SessionShareModal.tsx
  - frontend/src/components/__tests__/SessionShareModal.test.tsx
  - frontend/src/components/Hub/HubPanel.tsx
autonomous: true
requirements: [FUI-01, FUI-02, FUI-06]
must_haves:
  truths:
    - "Flipping the Funnel toggle reveals the inline risk panel on EVERY enable (no don't-show-again) with risk statement, auto-expiry selector, and a Help cross-link"
    - "Clicking 'Enable internet share' calls SetSessionFunnel(id, true, expirySeconds) with the selected preset value (1800/3600/14400/28800/0, default 3600)"
    - "When webServerMode is not 'tailscale', the Funnel toggle is disabled with the note 'Internet sharing requires Tailscale' and never calls SetSessionFunnel"
    - "The Share modal session prop stays in sync with the 3s Hub poll so funnelActive flips are observed"
  artifacts:
    - "frontend/src/components/Hub/FunnelRiskPanel.tsx (extracted component)"
    - "FunnelRiskPanel + SessionShareModal tests"
    - "HubPanel shareModalSession sync effect"
  key_links:
    - "Toggle ON does NOT call SetSessionFunnel — it opens the risk panel; only the explicit 'Enable internet share' button commits (D-02)"
    - "HubPanel shareModalSession is a snapshot; without a sync useEffect the warm-up flow in Plan 05 never completes (RESEARCH Pitfall 3)"
---

<objective>
Wire the Funnel enable flow: an inline risk-acknowledgment panel inside the existing Share modal (shown on every enable), an auto-expiry selector, a Help cross-link, and the local-fallback disabled state. On confirm, call the Phase-165 SetSessionFunnel binding. Also add the HubPanel sync effect that keeps the modal's session prop current with the 3s poll (prerequisite for Plan 05's warm-up).

Purpose: FUI-01 (risk dialog every enable), FUI-02 (auto-expiry selector), FUI-06 (Help cross-link). Implements D-01/D-02/D-03/D-04/D-05/D-06/D-15.
Output: FunnelRiskPanel component, modal wiring, HubPanel sync effect, tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-funnel-frontend-help-guide/166-CONTEXT.md
@.planning/phases/166-funnel-frontend-help-guide/166-PATTERNS.md
@.planning/phases/166-funnel-frontend-help-guide/166-UI-SPEC.md
@frontend/src/components/Hub/SessionShareModal.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)
- New component `frontend/src/components/Hub/FunnelRiskPanel.tsx` with props: `expirySeconds`, `onExpiryChange`, `onEnable`, `onCancel`, `onOpenHelp`
- `ShareSession` interface gains `funnelActive: boolean` (SessionShareModal.tsx)
- New modal state: `riskPanelOpen` and the Funnel toggle row (`.hub-funnel-risk-panel` mount + local-fallback disable)
- HubPanel `shareModalSession` sync `useEffect`
- New tests: `FunnelRiskPanel.test.tsx`, additions to `SessionShareModal.test.tsx`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FunnelRiskPanel component (risk statement + expiry selector + Help cross-link)</name>
  <files>frontend/src/components/Hub/FunnelRiskPanel.tsx, frontend/src/components/__tests__/FunnelRiskPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/SessionShareModal.tsx (host modal — ShellWebShareBanner inline-conditional pattern ~lines 302-317, motion guard ~lines 81-84)
    - frontend/src/components/__tests__/ShellWebShareBanner.test.tsx (analog test for an inline modal sub-component)
    - 166-UI-SPEC.md "Copywriting Contract" (exact risk statement, CTA labels, expiry option labels) and "Interaction Contract" (Tab order)
    - 166-PATTERNS.md "Wave 1: SessionShareModal" (risk panel conditional render, lines 145-206)
  </read_first>
  <behavior>
    - Renders the verbatim risk statement copy: "This makes the session reachable from the public internet. The join code is the only access gate for its lifetime. Prefer short-lived, read-only shares."
    - Renders an "Auto-expire:" &lt;select&gt; with options 30 minutes=1800, 1 hour=3600 (default selected), 4 hours=14400, 8 hours=28800, Until I disable=0
    - Renders a Help cross-link button labeled "Want tighter containment? See the Sharing Guide →" that invokes onOpenHelp
    - Renders "Keep local only" (calls onCancel) and "Enable internet share" (calls onEnable) buttons
    - Selecting an expiry option invokes onExpiryChange with the numeric preset value
  </behavior>
  <action>
    Create FunnelRiskPanel.tsx as a presentational component (no Wails calls). Root element uses `.hub-funnel-risk-panel` plus `.hub-funnel-risk-panel--open` when open; children use `__warning` (left-bordered ⚠ block; ExclamationTriangleIcon from @heroicons/react/24/outline as `__icon`), `__text`, `__expiry` (label + select), `__help-link`, `__actions` (Keep local only / Enable internet share). The expiry select value set is exactly {1800, 3600, 14400, 28800, 0}, default 3600 per D-05; 0 is the "Until I disable" no-expiry sentinel per D-07 — do not add a custom-minutes input (deferred). Guard the expand transition with the inline matchMedia `(prefers-reduced-motion: reduce)` check (same pattern as the host modal). Tab order: expiry select → help link → Keep local only → Enable internet share.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- FunnelRiskPanel</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts the risk statement text, all five expiry option values (1800/3600/14400/28800/0), and default 3600
    - Test asserts the Help cross-link fires onOpenHelp and "Enable internet share" fires onEnable
    - Component contains no `SetSessionFunnel` call (commit happens in the modal, not the panel)
    - `pnpm test -- FunnelRiskPanel` green
  </acceptance_criteria>
  <done>A testable, standalone risk panel renders the mandated copy, expiry presets, and Help link.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Funnel toggle + risk-panel wiring + local-fallback disable in SessionShareModal</name>
  <files>frontend/src/components/Hub/SessionShareModal.tsx, frontend/src/components/__tests__/SessionShareModal.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/SessionShareModal.tsx (file being modified — SetSessionBrowse handler lines 232-256, disabled-toggle pattern lines 357-380, ShareSession interface lines 18-25, webServerMode prop line 36/70)
    - frontend/src/components/Hub/FunnelRiskPanel.tsx (Task 1 output — the component being mounted)
    - frontend/src/components/__tests__/SessionShareModal.test.tsx (file being modified — existing modal test scaffold)
    - 166-UI-SPEC.md "Interaction Contract" state machine + "Local fallback state" section
  </read_first>
  <behavior>
    - Flipping the Funnel toggle ON opens the risk panel and does NOT call SetSessionFunnel (every enable, no skip — FUI-01/D-03)
    - Clicking "Enable internet share" calls SetSessionFunnel(session.id, true, expirySeconds) with the currently-selected preset, then collapses the panel
    - Clicking "Keep local only" collapses the panel and reverts the toggle to OFF with no API call
    - When webServerMode !== 'tailscale', the toggle is disabled (disabled attr + aria-disabled + pointer-events none) and shows "Internet sharing requires Tailscale"; no risk panel, no SetSessionFunnel
    - The Help cross-link triggers navigation to the help-sharing section (via the modal's existing nav/onClose + a help-open callback)
  </behavior>
  <action>
    Add `SetSessionFunnel` to the wailsjs import block. Extend the `ShareSession` interface with `funnelActive: boolean` (seeds Internet-section state consumed in Plan 05). Add a Funnel toggle row labeled "Enable internet sharing" following the existing browse-toggle markup. Add `riskPanelOpen` state and an `expirySeconds` state (default 3600). Toggle ON → `setRiskPanelOpen(true)` (NOT a SetSessionFunnel call). Mount `<FunnelRiskPanel open={riskPanelOpen} .../>` inline in the modal body during the 'open' phase. handleFunnelEnable → `await SetSessionFunnel(session.id, true, expirySeconds)` in a try/catch (on failure show inline `--hub-destructive` error "Failed to enable internet sharing. Please try again." and leave the toggle OFF); on success collapse the panel and set a `warmingUp` flag placeholder that Plan 05 consumes. Gate the whole toggle with the local-fallback pattern: when `webServerMode !== 'tailscale'`, wrap with `aria-disabled` + opacity 0.6 + pointerEvents none and render the muted note. Return focus to the toggle on cancel (toggle stays mounted). Do NOT implement the warm-up polling / IssueCapabilities re-issue here — that is Plan 05 (this plan only sets the enable path + state seams).
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- SessionShareModal</automated>
  </verify>
  <acceptance_criteria>
    - Test: toggling ON opens the risk panel and asserts SetSessionFunnel is NOT yet called
    - Test: "Enable internet share" calls SetSessionFunnel with (id, true, 3600) for the default, and with the selected value when changed
    - Test: with webServerMode='local', the toggle is disabled, the note "Internet sharing requires Tailscale" renders, and SetSessionFunnel is never called
    - Test: "Keep local only" collapses the panel with no API call
    - `pnpm test -- SessionShareModal` green; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The modal shows the risk panel on every enable, commits only on the explicit CTA, and fails closed in local-fallback mode.</done>
</task>

<task type="auto">
  <name>Task 3: HubPanel shareModalSession sync effect</name>
  <files>frontend/src/components/Hub/HubPanel.tsx</files>
  <read_first>
    - frontend/src/components/Hub/HubPanel.tsx (file being modified — shareModalSession state line 277, modal render line 566-568)
    - 166-RESEARCH.md "Pitfall 3: shareModalSession stale" (lines 363-365) and warm-up flow step 4 (line 308)
  </read_first>
  <action>
    Add a `useEffect` in HubPanel that, when `shareModalSession` is non-null, finds the matching entry in the `sessions` prop (updated by App.tsx's 3s poll) and calls `setShareModalSession(updated)` when it differs. Guard with `if (!shareModalSession) return` so it only runs while the modal is open. Depend on `[sessions, shareModalSession?.id]`. This propagates funnelActive flips into the modal so Plan 05's warm-up can complete (Pitfall 3). Do not change the snapshot-on-open behavior; only add the sync.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - HubPanel contains a useEffect keyed on `[sessions, shareModalSession?.id]` that re-sets shareModalSession from the sessions prop
    - The effect early-returns when shareModalSession is null (no work when modal closed)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The Share modal's session prop tracks the live poll, unblocking warm-up detection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture → Funnel enable | The risk panel's explicit CTA is the ONLY path that exposes a session to the public internet |
| webServerMode → toggle availability | Local-fallback state must fail closed (no Funnel when Tailscale is not the web-server mode) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-03 | Elevation of Privilege | Funnel toggle → enable | high | mitigate | Toggle ON never calls SetSessionFunnel; only the explicit "Enable internet share" button commits, shown on every enable with no don't-show-again (FUI-01/D-03); test asserts the two-step gesture |
| T-166-04 | Elevation of Privilege | local-fallback state | high | mitigate | webServerMode !== 'tailscale' disables the toggle and blocks SetSessionFunnel (D-15) mirroring the backend fail-closed; test asserts no call in local mode |
| T-166-05 | Tampering | auto-expiry select | low | mitigate | expiresIn comes from a fixed preset enum {1800,3600,14400,28800,0}; UI cannot submit an arbitrary integer (RESEARCH V5) |
</threat_model>

<verification>
- `cd frontend && pnpm test -- FunnelRiskPanel SessionShareModal` green.
- `cd frontend && npx tsc --noEmit` clean.
- Manual (deferred to Plan 05 gate M-40): toggle disabled when Tailscale not running.
</verification>

<success_criteria>
Toggling Funnel shows the inline risk panel on every enable with risk statement + expiry selector + Help link; the explicit CTA commits SetSessionFunnel with the chosen preset; local-fallback disables the toggle and blocks the call.
</success_criteria>

<output>
Create `.planning/phases/166-funnel-frontend-help-guide/166-02-SUMMARY.md` when done
</output>