---
phase: 166-funnel-frontend-help-guide
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/wailsjs/go/main/App.d.ts
  - frontend/src/wailsjs/go/main/App.js
  - frontend/src/style.css
  - frontend/src/components/__tests__/funnelBinding.contract.test.tsx
autonomous: true
requirements: [FUI-01, FUI-03, FUI-04, FUI-05]
must_haves:
  truths:
    - "Every frontend module that imports from ../../wailsjs/go/main/App can call SetSessionFunnel and read SessionInfo.funnelActive with no TypeScript error"
    - "The internet-exposure indicator color tokens exist as hex constants in style.css (dark #43ddb2 / light #0d7a5c) carrying a COLORBLIND-SAFE reinforcement-only comment"
  artifacts:
    - "frontend/src/wailsjs/go/main/App.d.ts with funnelActive on SessionInfo + SetSessionFunnel export"
    - "frontend/src/wailsjs/go/main/App.js with SetSessionFunnel Call wrapper"
    - "frontend/src/style.css with --hub-internet-badge-* tokens and all Phase-166 component CSS classes"
    - "frontend/src/components/__tests__/funnelBinding.contract.test.tsx"
  key_links:
    - "The hand-authored stub (wailsjs/go/main/App), NOT the generated wailsjs/wailsjs/go tree, is the single import source for the whole frontend"
---

<objective>
Lay the compile-against foundation for Phase 166: add SetSessionFunnel and SessionInfo.funnelActive to the HAND-AUTHORED Wails stubs the app actually imports, and add every Phase-166 CSS token + component class to style.css so no later wave has to touch style.css.

Purpose: RESEARCH.md's Critical Finding #1 — nothing in Wave 1+ typechecks until the hand-authored stubs (frontend/src/wailsjs/go/main/App.d.ts + App.js) gain SetSessionFunnel and funnelActive. The generated copies under wailsjs/wailsjs/go/ already have them but are NOT the app's import source. This is the hard blocker; it ships alone in Wave 0.
Output: Updated stubs, an import-contract test, and the complete Phase-166 CSS (tokens + all component classes) so Waves 1-2 only touch component files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-funnel-frontend-help-guide/166-RESEARCH.md
@.planning/phases/166-funnel-frontend-help-guide/166-PATTERNS.md
@.planning/phases/166-funnel-frontend-help-guide/166-UI-SPEC.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)
- Stub additions: `SetSessionFunnel(sessionID: string, enabled: boolean, expiresIn: number): Promise<void>` (App.d.ts + App.js) and `funnelActive: boolean` on the `SessionInfo` interface (App.d.ts)
- New CSS tokens: `--hub-internet-badge-bg`, `--hub-internet-badge-text` (dark + light `:root`)
- New CSS classes: `.hub-internet-badge`, `.hub-internet-badge__icon`, `.hub-internet-badge__label`, `.tab__internet-icon`, `.hub-funnel-risk-panel` (+ `--open`, `__warning`, `__icon`, `__text`, `__expiry`, `__help-link`, `__actions`), `.hub-share-internet-section` (+ `__heading`, `__warmup`, `__url-row`, `__disable`, `__error`)
- New test file: `frontend/src/components/__tests__/funnelBinding.contract.test.tsx`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add SetSessionFunnel + funnelActive to the hand-authored Wails stubs</name>
  <files>frontend/src/wailsjs/go/main/App.d.ts, frontend/src/wailsjs/go/main/App.js</files>
  <read_first>
    - frontend/src/wailsjs/go/main/App.d.ts (file being modified — SessionInfo interface + export block)
    - frontend/src/wailsjs/go/main/App.js (file being modified — Call wrapper block)
    - 166-PATTERNS.md "Wave 0: App.d.ts" and "Wave 0: App.js" sections (exact analog text, lines 29-91)
    - 166-RESEARCH.md "CRITICAL BINDING GAP" section (lines 184-208)
  </read_first>
  <action>
    In App.d.ts, add `funnelActive: boolean` to the `SessionInfo` interface immediately after `browseEnabled: boolean`, carrying the JSDoc `/** Phase 165 / FNL-01: true when Tailscale Funnel is active. NOT omitempty — false must serialize. */`. Then add the export `SetSessionFunnel(sessionID: string, enabled: boolean, expiresIn: number): Promise<void>` at the end of the export list, matching the existing typed-args + phase-tag comment style of SetSessionBrowse/GetCapabilityQRCode. In App.js, add `SetSessionFunnel = (sessionID, enabled, expiresIn) => Call('main.App.SetSessionFunnel', [sessionID, enabled, expiresIn])` at the end, matching the positional-arg arrow style of the existing SetSessionBrowse export. Do NOT touch the generated wailsjs/wailsjs/go tree — it already has these (RESEARCH Pitfall 1/2). expiresIn=0 is the daemon's confirmed no-expiry sentinel per D-07 — do not invent another.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'funnelActive: boolean' frontend/src/wailsjs/go/main/App.d.ts` returns >= 1
    - App.d.ts export list contains `SetSessionFunnel(sessionID: string, enabled: boolean, expiresIn: number): Promise<void>`
    - App.js contains `Call('main.App.SetSessionFunnel', [sessionID, enabled, expiresIn])`
    - `cd frontend && npx tsc --noEmit` reports no new errors attributable to these files
  </acceptance_criteria>
  <done>The app's import source exposes SetSessionFunnel and SessionInfo.funnelActive; tsc is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Import-contract test for the Funnel binding gap</name>
  <files>frontend/src/components/__tests__/funnelBinding.contract.test.tsx</files>
  <read_first>
    - frontend/src/components/__tests__/App.test.tsx (analog — `?raw` source-string import-contract pattern, lines 1-40)
    - frontend/src/wailsjs/go/main/App.d.ts (the file under contract, after Task 1)
  </read_first>
  <behavior>
    - Test: App.d.ts raw source contains `funnelActive: boolean` on SessionInfo (guards Pitfall 2 — must be on the stub interface, not only models.ts)
    - Test: App.d.ts raw source exports `SetSessionFunnel` with the `(sessionID: string, enabled: boolean, expiresIn: number)` signature
    - Test: App.js raw source registers the `main.App.SetSessionFunnel` Call
  </behavior>
  <action>
    Create frontend/src/components/__tests__/funnelBinding.contract.test.tsx mirroring App.test.tsx: import the two stub files via the vitest `?raw` suffix and assert with `expect(raw).toContain(...)` on the three strings above. This is the automated guard for RESEARCH Pitfalls 1 and 2 (updating only the generated stub, or only models.ts, leaves the app broken at runtime while components compile in isolation).
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- funnelBinding.contract</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm test -- funnelBinding.contract` passes with 3+ assertions
    - Test fails if `funnelActive` is removed from App.d.ts or the SetSessionFunnel export is deleted (verified by construction)
  </acceptance_criteria>
  <done>A red-on-regression test pins the hand-authored stub contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add all Phase-166 CSS tokens + component classes to style.css</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css (file being modified — existing `--hub-attn-*` token group ~line 4644 and the light `[data-ui-theme="light"]` block)
    - 166-PATTERNS.md "Wave 0 + all waves: style.css" section (lines 503-647 — full class list with values)
    - 166-UI-SPEC.md "Color" + "Component Inventory" sections (token hexes + BEM class names)
  </read_first>
  <action>
    Add the two new tokens to BOTH the dark `:root` and the light `[data-ui-theme="light"]` blocks, adjacent to `--hub-attn-*`. Dark: `--hub-internet-badge-bg: rgba(67, 221, 178, 0.15)` and `--hub-internet-badge-text: #43ddb2`, prefixed with the COLORBLIND-SAFE reinforcement-only comment naming the globe shape + INTERNET text as the state carriers. Light: `--hub-internet-badge-bg: rgba(13, 122, 92, 0.13)` and `--hub-internet-badge-text: #0d7a5c` with the matching COLORBLIND-SAFE comment noting ~5.2:1 AA on white. Then add all Phase-166 component classes exactly per PATTERNS.md: `.hub-internet-badge` (+ `__icon` 12x12, `__label` 11px/600/uppercase/letter-spacing 0.05em), `.tab__internet-icon` (14x14, flex-shrink 0), `.hub-funnel-risk-panel` (max-height 0, overflow hidden, `--hub-surface-elevated` bg, `--hub-border`, radius-sm, 16px pad) + `.hub-funnel-risk-panel--open` (max-height 400px) + the `prefers-reduced-motion` guard pair (transition max-height 200ms ease-out only under no-preference; none under reduce) + `__warning` (left-border 3px `--hub-warning`), `__icon`, `__text`, `__expiry`, `__help-link` (hover → `--hub-accent`), `__actions`, and `.hub-share-internet-section` (+ `__heading` uppercase/letter-spacing 0.08em, `__warmup` italic muted, `__url-row`, `__disable` neutral outlined with `:focus-visible` 2px `--hub-accent` ring, `__error` `--hub-destructive`). Reference only existing `--hub-*` tokens for radius/spacing/fonts — introduce no new spacing steps.
  </action>
  <verify>
    <automated>cd frontend && grep -c 'COLORBLIND-SAFE' src/style.css && grep -c 'hub-internet-badge' src/style.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'COLORBLIND-SAFE' src/style.css` increases by >= 2 (one per new token block)
    - Dark hex `#43ddb2` and light hex `#0d7a5c` both present as `--hub-internet-badge-text` values (colorblind verified at hex/source, not by eye)
    - All classes present: `.hub-internet-badge`, `.hub-internet-badge__icon`, `.hub-internet-badge__label`, `.tab__internet-icon`, `.hub-funnel-risk-panel`, `.hub-funnel-risk-panel--open`, `.hub-share-internet-section`, `.hub-share-internet-section__disable`, `.hub-share-internet-section__error`
    - `.hub-funnel-risk-panel` transition appears only inside `prefers-reduced-motion: no-preference`; `reduce` block sets `transition: none`
    - `cd frontend && npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>All Phase-166 tokens + component classes exist in style.css so Waves 1-2 touch no CSS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend build → Wails Go RPC | The stub is the only typed contract for `main.App.SetSessionFunnel`; a wrong signature silently mis-marshals args to the daemon |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-01 | Tampering | hand-authored App stub | medium | mitigate | Import-contract test (Task 2) pins signature + funnelActive field; tsc gate rejects arg-type drift |
| T-166-02 | Information Disclosure | internet-badge color token | low | mitigate | State carried by globe shape + text, never color alone (COLORBLIND-SAFE comment enforced at source); token is reinforcement only |
| T-166-SC | Tampering | npm/pnpm installs | low | accept | No new packages introduced this phase (RESEARCH Standard Stack); nothing to install |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` is clean (stubs typecheck against every importer).
- `cd frontend && pnpm test -- funnelBinding.contract` green.
- Both internet-badge hex constants present in style.css; COLORBLIND-SAFE comments present.
</verification>

<success_criteria>
Wave 1 plans can import SetSessionFunnel and read SessionInfo.funnelActive with zero TypeScript errors, and reference every Phase-166 CSS class without adding CSS.
</success_criteria>

<output>
Create `.planning/phases/166-funnel-frontend-help-guide/166-01-SUMMARY.md` when done
</output>