---
phase: 166-funnel-frontend-help-guide
plan: 03
type: execute
wave: 1
depends_on: [166-01]
files_modified:
  - frontend/src/components/Hub/SessionCard.tsx
  - frontend/src/components/Hub/SessionCard.test.tsx
  - frontend/src/components/TabBar.tsx
  - frontend/src/components/__tests__/TabBar.test.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [FUI-03]
must_haves:
  truths:
    - "While a session is internet-exposed, its Hub card shows a globe icon + 'INTERNET' text badge, and its session tab shows a globe icon carrying aria-label/title 'Internet exposed'"
    - "The indicator conveys state via icon shape + text, never color alone, verified at hex/source level"
    - "The indicator appears when funnelActive is true and is removed the moment it is false"
  artifacts:
    - "SessionCard .hub-internet-badge render"
    - "TabBar .tab__internet-icon render + funnelActiveSessions prop"
    - "App.tsx funnelActiveSessions derived map passed to TabBar"
  key_links:
    - "SessionCard reads session.funnelActive directly; TabBar has no SessionInfo so App.tsx must derive a funnelActiveSessions Record<id,boolean> from hubSessions (mirrors sessionStatuses)"
---

<objective>
Add the persistent, colorblind-safe internet-exposure indicator to both the Hub card (globe + "INTERNET" badge) and the session tab (globe icon only, with aria-label/title "Internet exposed"). Drive both from SessionInfo.funnelActive.

Purpose: FUI-03 — persistent non-color exposure indicator. Implements D-08/D-09/D-10/D-11. Colorblind-safety is release-blocking and verified at hex/source, not by eye (user is colorblind).
Output: SessionCard badge, TabBar icon + new prop, App.tsx derivation, tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-funnel-frontend-help-guide/166-PATTERNS.md
@.planning/phases/166-funnel-frontend-help-guide/166-UI-SPEC.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)
- SessionCard: `.hub-internet-badge` (GlobeAltIcon + "INTERNET") conditional render with COLORBLIND-SAFE source comment
- TabBar: new `funnelActiveSessions?: Record<string, boolean>` prop + `.tab__internet-icon` (GlobeAltIcon, aria-label/title "Internet exposed"); GlobeAltIcon import added
- App.tsx: `funnelActiveSessions` derived from `hubSessions`, passed to `<TabBar>`
- Tests: SessionCard badge test + TabBar icon test
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hub card internet badge (globe + INTERNET)</name>
  <files>frontend/src/components/Hub/SessionCard.tsx, frontend/src/components/Hub/SessionCard.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/SessionCard.tsx (file being modified — STATUS_CONFIG colorblind-safe pattern lines 36-61, GlobeAltIcon already imported line 13, card header row)
    - frontend/src/components/Hub/SessionCard.test.tsx (file being modified — existing card test)
    - 166-PATTERNS.md "Wave 2: SessionCard" (badge render, lines 324-352)
    - 166-UI-SPEC.md "Component Inventory #2" (badge classes + placement)
  </read_first>
  <behavior>
    - When session.funnelActive is true, the card renders `.hub-internet-badge` containing GlobeAltIcon (`.hub-internet-badge__icon`, aria-hidden) + "INTERNET" text (`.hub-internet-badge__label`)
    - When session.funnelActive is false, no badge renders
    - The GlobeAltIcon shape + "INTERNET" text are the state carriers; color is reinforcement only
  </behavior>
  <action>
    Render the internet badge in the SessionCard header row, adjacent to the existing status badge (after session status, before controls), gated on `session.funnelActive`. Use GlobeAltIcon (already imported) as `.hub-internet-badge__icon` with aria-hidden="true", and a "INTERNET" span as `.hub-internet-badge__label`. Precede the JSX with the COLORBLIND-SAFE comment naming the globe shape + INTERNET text as state carriers and the dark #43ddb2 / light #0d7a5c reinforcement hexes (matching the existing STATUS_CONFIG comment convention). Do not encode state by color alone.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- SessionCard</automated>
  </verify>
  <acceptance_criteria>
    - Test renders SessionCard with funnelActive=true and asserts the "INTERNET" text and the badge element are present
    - Test renders with funnelActive=false and asserts no `.hub-internet-badge`
    - `grep -c 'COLORBLIND-SAFE' src/components/Hub/SessionCard.tsx` increases by >= 1 (source-level colorblind verification)
    - `pnpm test -- SessionCard` green
  </acceptance_criteria>
  <done>The Hub card shows a shape+text internet badge tied to funnelActive with a source-level colorblind guarantee.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Session tab internet icon + funnelActiveSessions prop</name>
  <files>frontend/src/components/TabBar.tsx, frontend/src/components/__tests__/TabBar.test.tsx</files>
  <read_first>
    - frontend/src/components/TabBar.tsx (file being modified — sessionStatuses prop line 52, per-session render lines 224-228, Tab interface lines 7-13)
    - frontend/src/components/__tests__/TabBar.test.tsx (file being modified)
    - 166-PATTERNS.md "Wave 2: TabBar" (globe render + App.tsx derivation, lines 356-401)
    - 166-UI-SPEC.md "Component Inventory #3" (tab icon, aria-label requirement D-09)
  </read_first>
  <behavior>
    - TabBar accepts a new `funnelActiveSessions?: Record<string, boolean>` prop
    - When funnelActiveSessions[tab.sessionId] is true, the tab renders GlobeAltIcon with class `.tab__internet-icon`, aria-label="Internet exposed" and title="Internet exposed" (the text label lives in aria/title, not visible chrome — D-09)
    - When false/absent, no icon renders
  </behavior>
  <action>
    Import GlobeAltIcon from @heroicons/react/24/outline (not yet imported in TabBar). Add the `funnelActiveSessions?: Record<string, boolean>` prop parallel to `sessionStatuses`. In the tab render block, after the agent badge dot and before the tab name, conditionally render the GlobeAltIcon as `.tab__internet-icon` with aria-label and title both "Internet exposed" and aria-hidden={false} so the icon is exposed to assistive tech. Precede with the COLORBLIND-SAFE comment (globe shape carries state; text label is in aria-label, not rendered visually per D-09). Ensure the 24x24 minimum hit area from the UI-SPEC spacing exceptions is respected via the class.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- TabBar</automated>
  </verify>
  <acceptance_criteria>
    - Test: with funnelActiveSessions={{ [id]: true }}, the tab exposes an element with aria-label "Internet exposed"
    - Test: with the map empty/false, no `.tab__internet-icon` renders
    - `grep -c 'COLORBLIND-SAFE' src/components/TabBar.tsx` increases by >= 1
    - `pnpm test -- TabBar` green
  </acceptance_criteria>
  <done>The session tab carries a globe indicator with an accessible "Internet exposed" label when exposed.</done>
</task>

<task type="auto">
  <name>Task 3: Derive funnelActiveSessions in App.tsx and pass to TabBar</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (file being modified — sessionStatuses state line 158 + TabBar prop pass line 1495, hubSessions state + 3s poll line 984)
    - 166-PATTERNS.md "Wave 2: TabBar" App.tsx derivation snippet (lines 377-384)
  </read_first>
  <action>
    Derive `funnelActiveSessions` from `hubSessions` as a `Record<string, boolean>` keyed by session id (value = s.funnelActive), computed near where `sessionStatuses` is passed. Pass it as `<TabBar funnelActiveSessions={funnelActiveSessions} ... />`. This rides the existing 3s poll (App.tsx:984) — no new poll loop. Removal is automatic: when funnelActive flips false on the next poll, the map value flips and the tab icon disappears.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx contains a `funnelActiveSessions` map derived from `hubSessions` and passes it to `<TabBar`
    - The derivation reads `funnelActive` from each session (no new poll interval added)
    - `npx tsc --noEmit` clean; `pnpm test -- App` green
  </acceptance_criteria>
  <done>App.tsx feeds live funnelActive state to the tab bar via the existing poll.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon poll → indicator | The indicator is a read-only reflection of daemon-owned funnelActive; it must never assert exposure the daemon does not report |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-06 | Repudiation | exposure indicator | medium | mitigate | Indicator is driven solely by SessionInfo.funnelActive from the 3s poll; it appears while exposed and clears on the next poll when false, so a user can always see the true exposure state |
| T-166-07 | Information Disclosure | color-only encoding | low | mitigate | Globe shape + "INTERNET"/"Internet exposed" text carry state; color is reinforcement only; COLORBLIND-SAFE source comments verified at hex level (user is colorblind) |
</threat_model>

<verification>
- `cd frontend && pnpm test -- SessionCard TabBar App` green.
- `cd frontend && npx tsc --noEmit` clean.
- Source contains COLORBLIND-SAFE comments in SessionCard.tsx and TabBar.tsx.
- Manual (Plan 05 gate M-39): badge + tab icon visible while Funnel active, absent after disable.
</verification>

<success_criteria>
A funnelActive session shows a globe+"INTERNET" badge on its Hub card and a globe icon with "Internet exposed" aria-label on its tab; both disappear immediately when funnelActive goes false; state is shape+text, never color alone.
</success_criteria>

<output>
Create `.planning/phases/166-funnel-frontend-help-guide/166-03-SUMMARY.md` when done
</output>