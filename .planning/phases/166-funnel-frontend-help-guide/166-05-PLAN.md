---
phase: 166-funnel-frontend-help-guide
plan: 05
type: execute
wave: 2
depends_on: [166-02]
files_modified:
  - frontend/src/components/Hub/SessionShareModal.tsx
  - frontend/src/components/SessionSharePanel.tsx
  - frontend/src/components/__tests__/SessionSharePanel.test.tsx
  - TESTING.md
autonomous: true
requirements: [FUI-04, FUI-05]
must_haves:
  truths:
    - "Immediately after enabling Funnel, the Share modal shows a 'Starting up… (TLS warming up)' state with the URL/QR muted, and reveals a working public URL only once funnelActive flips true"
    - "The revealed 'Internet (public)' section shows a single read-only Funnel URL with copy-to-clipboard, open-in-browser, and QR code"
    - "A single click on 'Disable internet share' calls SetSessionFunnel(id, false, 0), tearing down the Funnel and clearing the indicator on the next poll"
    - "If warm-up does not complete within 30s, a timeout error is shown"
  artifacts:
    - "SessionShareModal warm-up state machine + IssueCapabilities re-issue + onDisableFunnel"
    - "SessionSharePanel Internet (public) section"
    - "TESTING.md manifest + traceability rows + M-37..M-40 manual items"
  key_links:
    - "SetSessionFunnel returns void — the Funnel URL is obtained by re-issuing IssueCapabilities AFTER funnelActive flips true (RESEARCH: daemon swaps to Funnel base)"
    - "Warm-up rides the existing 3s Hub poll via HubPanel's shareModalSession sync (Plan 02 Task 3); no new 2s poll"
---

<objective>
Complete the Funnel share surface: the warm-up UX after enable, the "Internet (public)" URL section (copy + open + QR) revealed once TLS is live, and one-click disable. Then update TESTING.md for all Phase-166 test files and manual UAT items.

Purpose: FUI-05 (public URL + copy + QR + "starting up" warm-up state), FUI-04 (one-click disable that fully tears down and clears the indicator). Implements D-12/D-13/D-14. Shares SessionShareModal.tsx with Plan 02, so this is Wave 2.
Output: modal warm-up wiring, SessionSharePanel Internet section, TESTING.md updates, and the phase build gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-funnel-frontend-help-guide/166-RESEARCH.md
@.planning/phases/166-funnel-frontend-help-guide/166-PATTERNS.md
@.planning/phases/166-funnel-frontend-help-guide/166-UI-SPEC.md
@frontend/src/components/SessionSharePanel.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 05)
- SessionShareModal: `warmingUp`, `warmupTimedOut`, `warmStartAt` state + 30s timeout `useRef`; reaction to `session.funnelActive` flip → `IssueCapabilities(session.id)` re-issue → `funnelUrl`; `onDisableFunnel` → `SetSessionFunnel(id, false, 0)`; new props passed to SessionSharePanel
- SessionSharePanel: new props `funnelActive?`, `funnelUrl?`, `warmingUp?`, `warmupTimedOut?`, `onDisableFunnel?`; new `.hub-share-internet-section` (heading, warmup, URL row with Copy URL / Open / QR, disable button, error)
- TESTING.md: Suite Manifest rows + Traceability rows (repo-relative paths) for all new Phase-166 test files; manual items M-37..M-40 in Section 5
- Test: SessionSharePanel Internet-section test
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Warm-up state machine + IssueCapabilities re-issue + disable in SessionShareModal</name>
  <files>frontend/src/components/Hub/SessionShareModal.tsx, frontend/src/components/__tests__/SessionShareModal.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/SessionShareModal.tsx (file being modified — Plan 02 enable path + warmingUp seam; seeding effect / cancelled-async pattern lines 156-199; SetSessionBrowse re-issue pattern lines 232-256)
    - frontend/src/components/__tests__/SessionShareModal.test.tsx (file being modified)
    - 166-RESEARCH.md "Warm-up polling mechanics" (lines 294-315) + "Funnel URL derivation" (lines 317-337) + "Pitfall 4" timer cleanup
    - 166-UI-SPEC.md "Interaction Contract" warm-up + disable states
  </read_first>
  <behavior>
    - On successful enable, warmingUp=true and warmStartAt=Date.now() are set immediately
    - When session.funnelActive flips true (via HubPanel sync from the 3s poll), the modal calls IssueCapabilities(session.id), stores the returned Funnel-base readUrl as funnelUrl, and sets warmingUp=false
    - If 30s elapse without funnelActive, warmupTimedOut=true
    - onDisableFunnel calls SetSessionFunnel(session.id, false, 0), clears funnelUrl and warm-up state; the 30s timeout ref is cleared on both success and disable
  </behavior>
  <action>
    Add `warmingUp`, `warmupTimedOut` state, a `warmStartAt` value, and a `useRef` holding the 30s timeout handle. In handleFunnelEnable (from Plan 02), after SetSessionFunnel resolves, set warmingUp=true, record warmStartAt, and arm a setTimeout(30000) that sets warmupTimedOut=true. Add an effect keyed on `session.funnelActive` that, when it becomes true and warmingUp is active, runs the cancelled-async IssueCapabilities(session.id) pattern to fetch the Funnel-base readUrl (the daemon swaps the base once Funnel is active — RESEARCH), stores it as `funnelUrl`, sets warmingUp=false, and clears the timeout ref. Add `onDisableFunnel` calling `SetSessionFunnel(session.id, false, 0)` then clearing funnelUrl + warm-up state + the timeout ref (Pitfall 4). Pass `funnelActive={session.funnelActive}`, `funnelUrl`, `warmingUp`, `warmupTimedOut`, and `onDisableFunnel` to the rendered SessionSharePanel. Do NOT add a separate 2s poll — ride the existing 3s poll via the HubPanel sync added in Plan 02.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- SessionShareModal</automated>
  </verify>
  <acceptance_criteria>
    - Test (fake timers): after enable, warmingUp is set immediately; when session.funnelActive flips true, IssueCapabilities is called once and warmingUp clears
    - Test: at 30s without funnelActive, warmupTimedOut becomes true
    - Test: onDisableFunnel calls SetSessionFunnel with (id, false, 0) and clears funnelUrl
    - Test: the 30s timeout ref is cleared on disable (no late timeout fire)
    - `pnpm test -- SessionShareModal` green; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The modal drives warm-up → live URL → disable off the existing poll with correct timer cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Internet (public) section in SessionSharePanel (URL + copy + open + QR + disable + warm-up/error)</name>
  <files>frontend/src/components/SessionSharePanel.tsx, frontend/src/components/__tests__/SessionSharePanel.test.tsx</files>
  <read_first>
    - frontend/src/components/SessionSharePanel.tsx (file being modified — CodeDisplay + QR pattern lines 1-57/115-175, ClipboardSetText copy lines 115-124, BrowserOpenURL usage, props interface lines 70-83)
    - frontend/src/components/__tests__/SessionSharePanel.test.tsx (file being modified)
    - 166-PATTERNS.md "Wave 2: SessionSharePanel" (Internet section JSX + props, lines 231-320)
    - 166-UI-SPEC.md "Component Inventory #4" + Copywriting Contract (labels)
  </read_first>
  <behavior>
    - When warmingUp is true, renders `.hub-share-internet-section__warmup` "Starting up… (TLS warming up)" with URL/QR muted (opacity 0.4, pointer-events none)
    - When funnelActive && funnelUrl && !warmingUp, renders "Public URL (read-only):" with the funnelUrl, a "Copy URL" button (ClipboardSetText), an "Open" button (BrowserOpenURL), and a QR button (GetCapabilityQRCode)
    - Renders a single "Disable internet share" button that calls onDisableFunnel (no confirm dialog)
    - When warmupTimedOut, renders `.hub-share-internet-section__error` "Connection timed out. Try disabling and re-enabling."
  </behavior>
  <action>
    Extend the props interface with `funnelActive?`, `funnelUrl?`, `warmingUp?`, `warmupTimedOut?`, `onDisableFunnel?`. Below the existing tailnet link rows, render `.hub-share-internet-section` with `__heading` "Internet (public)". Show the warm-up message when warmingUp; show the URL row (label "Public URL (read-only):", the funnelUrl, and Copy URL / Open / QR actions) when funnelActive && funnelUrl && !warmingUp; show the timeout error when warmupTimedOut. Use the existing ClipboardSetText copy handler with the 1500ms reset (NOT navigator.clipboard), BrowserOpenURL for Open, and GetCapabilityQRCode(funnelUrl) for the QR (reuse the existing QR img pattern). Add the single `.hub-share-internet-section__disable` button wired to onDisableFunnel (single click, no confirmation — the persistent indicator is the compensating control per D-13). Only the read-only Funnel URL is shown — never a public write link (D-12).
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- SessionSharePanel</automated>
  </verify>
  <acceptance_criteria>
    - Test: warmingUp=true renders the "Starting up… (TLS warming up)" text and no live URL actions
    - Test: funnelActive+funnelUrl renders the funnelUrl, and Copy uses ClipboardSetText (mocked), Open uses BrowserOpenURL (mocked)
    - Test: clicking "Disable internet share" invokes onDisableFunnel
    - Test: warmupTimedOut renders the timeout error copy
    - No public write URL is rendered in the Internet section (source assertion)
    - `pnpm test -- SessionSharePanel` green
  </acceptance_criteria>
  <done>The Internet section shows a working read-only public URL with copy/open/QR after warm-up, and a one-click disable.</done>
</task>

<task type="auto">
  <name>Task 3: TESTING.md manifest + traceability + M-37..M-40; run phase build gate</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md (file being modified — Section 2 Suite Manifest ~line 22, Section 4 Traceability ~line 116, Section 5 Manual Checklist ~line 287, Section 6 Standing Convention ~line 568)
    - ./CLAUDE.md (AgentHub regression-test convention — repo-relative path column, run check-traceability-paths.sh)
    - 166-RESEARCH.md "Manual Checklist Items (M-NN)" (M-37..M-40) and "Phase Gate"
    - tests/check-traceability-paths.sh
  </read_first>
  <action>
    Register every new Phase-166 test file in the Section 2 Suite Manifest frontend group: funnelBinding.contract.test.tsx, FunnelRiskPanel.test.tsx, SessionSharePanel.test.tsx (new coverage), and the additions to SessionShareModal.test.tsx / SessionCard.test.tsx / TabBar.test.tsx / HelpTab.integration.test.tsx. Add Section 4 Traceability rows mapping FUI-01..06 + HLP-01/02 to their test files using repo-relative `.tsx`/`.md` paths ONLY (no test names in the path column). Add Section 5 manual items M-37 (live Funnel enable: TLS warm-up completes + public URL opens off-tailnet — requires a PRODUCTION build per STATE.md), M-38 (live auto-expiry teardown at chosen duration), M-39 (Hub card globe badge + tab globe icon visible while Funnel active, absent after disable), M-40 (local-fallback: Funnel toggle disabled when Tailscale not running). Then run `bash tests/check-traceability-paths.sh` and fix any path issues before finishing.
  </action>
  <verify>
    <automated>bash tests/check-traceability-paths.sh && cd frontend && npx tsc --noEmit && pnpm run build >/dev/null 2>&1; echo "build-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/check-traceability-paths.sh` exits 0
    - TESTING.md contains rows for every new/changed Phase-166 test file (Section 2 + Section 4)
    - TESTING.md Section 5 contains M-37, M-38, M-39, M-40
    - `cd frontend && npx tsc --noEmit && pnpm run build` succeeds (the real build gate — vitest alone is insufficient per standing rule)
  </acceptance_criteria>
  <done>Regression convention satisfied and the app compiles + builds (tsc && vite build), not just vitest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon Funnel base → displayed URL | The public URL shown must be the read-only Funnel-base cap, never a write cap |
| user click → Funnel teardown | Disable must fully tear down the daemon Funnel config, not just hide the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-10 | Information Disclosure | Internet (public) URL | high | mitigate | Section displays only the read-only Funnel readUrl (D-12); no public write link is ever rendered; test asserts absence of a write URL in the section |
| T-166-11 | Denial of Service | warm-up polling | low | mitigate | Warm-up rides the existing 3s poll with a 30s timeout + error state; no busy-loop; timeout ref cleared on success/disable (Pitfall 4) |
| T-166-12 | Tampering | one-click disable | medium | mitigate | Disable calls SetSessionFunnel(id,false,0) which triggers the Phase-165 four-path teardown (FNL-05); indicator clears on next poll, keeping UI truthful about exposure |
</threat_model>

<verification>
- `cd frontend && pnpm test -- SessionShareModal SessionSharePanel` green.
- `cd frontend && npx tsc --noEmit && pnpm run build` succeeds.
- `bash tests/check-traceability-paths.sh` exits 0; TESTING.md has M-37..M-40.
- Manual (M-37..M-40, live, production build): warm-up completes, expiry tears down, indicators appear/clear, local-fallback disables the toggle.
</verification>

<success_criteria>
After enabling Funnel the user sees "Starting up…", then a working read-only public URL with copy + open + QR; one click disables and clears the indicator; TESTING.md and the tsc && vite build gate both pass.
</success_criteria>

<output>
Create `.planning/phases/166-funnel-frontend-help-guide/166-05-SUMMARY.md` when done
</output>