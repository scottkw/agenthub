---
phase: 166-funnel-frontend-help-guide
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/content/help/sharing-guide.md
  - frontend/src/components/HelpTab.tsx
  - frontend/src/components/HelpSectionNav.tsx
  - frontend/src/components/__tests__/HelpTab.integration.test.tsx
autonomous: true
requirements: [HLP-01, HLP-02]
must_haves:
  truths:
    - "The Help tab shows a 'Sharing Outside Your Tailnet' section between Chat and FAQ in both the content area and the left nav"
    - "The article documents both the Funnel path (Option 1) and the device-share + ACL alternative (Option 2)"
    - "The article contains a copy-pasteable ACL grant block targeting tag:agenthub:tcp:7443 and explicitly calls out the wildcard-default (*→*) gotcha, plus links to Tailscale docs"
  artifacts:
    - "frontend/src/content/help/sharing-guide.md"
    - "help-sharing entry in HelpTab SECTION_META and HelpSectionNav SECTIONS (same position)"
  key_links:
    - "SECTION_META (HelpTab) AND SECTIONS (HelpSectionNav) are SEPARATE arrays; both must gain help-sharing in the same position or the section renders with no nav link (RESEARCH Pitfall 5)"
    - "HelpContent auto-converts markdown links to BrowserOpenURL buttons — use plain markdown link syntax, no raw <a> (Wails CSP)"
---

<objective>
Add the in-app "Sharing Outside Your Tailnet" Help article covering both sharing paths, registered in both Help nav arrays. The article gives a copy-pasteable ACL grant, the wildcard-default gotcha, and Tailscale doc links.

Purpose: HLP-01 (article covering both paths), HLP-02 (ACL block + wildcard gotcha + doc links). Implements D-16/D-17/D-18. Independent of the Wails stub — no dependency on Plan 01.
Output: sharing-guide.md, both nav-array updates, integration test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-funnel-frontend-help-guide/166-CONTEXT.md
@.planning/phases/166-funnel-frontend-help-guide/166-PATTERNS.md
@.planning/phases/166-funnel-frontend-help-guide/166-UI-SPEC.md
@frontend/src/content/help/chat.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)
- New file `frontend/src/content/help/sharing-guide.md` (id `help-sharing`)
- HelpTab.tsx: `import sharingMd from '../content/help/sharing-guide.md?raw'` + SECTION_META entry `{ id: 'help-sharing', label: 'Sharing Outside Your Tailnet', markdown: sharingMd }` after help-chat
- HelpSectionNav.tsx: SECTIONS entry `{ id: 'help-sharing', label: 'Sharing Outside Your Tailnet' }` in the same position
- HelpTab.integration.test.tsx: section-count + help-sharing presence assertions
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author sharing-guide.md (both paths, ACL block, wildcard gotcha, doc links)</name>
  <files>frontend/src/content/help/sharing-guide.md</files>
  <read_first>
    - frontend/src/content/help/chat.md (analog — prose style, H3 sub-topics, code blocks, markdown link syntax)
    - frontend/src/components/HelpContent.tsx (lines 65-85 — markdown links auto-become BrowserOpenURL buttons; SAFE_LINK_SCHEME allowlist)
    - 166-UI-SPEC.md "Help guide content requirements" (mandated section order + verbatim wildcard callout)
    - 166-RESEARCH.md "Tailscale documentation URLs" (the four [ASSUMED] URLs) and "HLP-02 web-share port" (7443 confirmed)
  </read_first>
  <action>
    Create sharing-guide.md following chat.md conventions (H2 title provided by the article itself per the existing pattern; H3 for sub-topics; concrete step-by-step; standard markdown links only — HelpContent converts them to safe BrowserOpenURL buttons, so no raw &lt;a&gt; per the Wails CSP constraint). Required structure in order: H2 "Sharing Outside Your Tailnet" intro paragraph naming the two paths; H3 "Option 1 — Tailscale Funnel (public internet)" with the Share-modal step-by-step (toggle → risk dialog → choose expiry → copy the public URL), noting the join code is the only gate and it expires; H3 "Option 2 — Device Share + ACL (contained)" with steps (share the device in the Tailscale admin console → tag it tag:agenthub → restrict shared users to the app port) and a copy-pasteable JSON ACL grant block with action accept, src ["autogroup:shared"], dst ["tag:agenthub:tcp:7443"]. Use port 7443 (confirmed DefaultProbePort). Explicitly call out the wildcard-default gotcha with the verbatim UI-SPEC wording ("Tailscale ACLs default to `*→*` (allow all). Adding a `tag:agenthub` restriction will narrow this — verify your full ACL after editing."). Add markdown links to the four Tailscale KB URLs (Funnel 1223, ACL syntax 1337, sharing 1084, tags 1068) — these are [ASSUMED] from research; keep them as plain markdown links so a stale URL is a non-functional link only. Close Option 2 with a pointer back to Option 1 for fully-public sharing.
  </action>
  <verify>
    <automated>cd frontend && grep -c 'tag:agenthub:tcp:7443' src/content/help/sharing-guide.md && grep -c 'autogroup:shared' src/content/help/sharing-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains an H3 for Option 1 (Funnel) and an H3 for Option 2 (Device Share + ACL)
    - File contains `tag:agenthub:tcp:7443` inside a JSON block and `autogroup:shared`
    - File contains the literal `*→*` wildcard-default gotcha callout
    - File contains at least the four Tailscale KB links as plain markdown links (no raw `<a`)
    - `grep -c '<a ' src/content/help/sharing-guide.md` returns 0
  </acceptance_criteria>
  <done>The Sharing Guide article documents both paths with a correct, copy-pasteable ACL grant and the wildcard caveat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register help-sharing in BOTH Help nav arrays + integration test</name>
  <files>frontend/src/components/HelpTab.tsx, frontend/src/components/HelpSectionNav.tsx, frontend/src/components/__tests__/HelpTab.integration.test.tsx</files>
  <read_first>
    - frontend/src/components/HelpTab.tsx (file being modified — imports lines 12-14, SECTION_META lines 61-65)
    - frontend/src/components/HelpSectionNav.tsx (file being modified — SECTIONS lines 10-14)
    - frontend/src/components/__tests__/HelpTab.integration.test.tsx (file being modified — existing #help-chat / #help-faq assertions and section-count)
    - 166-RESEARCH.md "Pitfall 5" (both arrays must update together) and "HelpTab + HelpSectionNav" (lines 270-290)
  </read_first>
  <behavior>
    - HelpTab renders a section element with id `help-sharing` positioned after `help-chat` and before `help-faq`
    - HelpSectionNav lists a "Sharing Outside Your Tailnet" nav button for `help-sharing` in the same position
    - The section count assertion in the integration test increments to include the new section
  </behavior>
  <action>
    In HelpTab.tsx add `import sharingMd from '../content/help/sharing-guide.md?raw'` alongside the other `?raw` imports, and insert `{ id: 'help-sharing', label: 'Sharing Outside Your Tailnet', markdown: sharingMd }` into SECTION_META after the help-chat entry and before help-faq. In HelpSectionNav.tsx insert the matching `{ id: 'help-sharing', label: 'Sharing Outside Your Tailnet' }` into SECTIONS in the same position (both arrays must stay in sync — Pitfall 5). Update HelpTab.integration.test.tsx to assert a `#help-sharing` section element exists and that clicking its nav button scrolls to it (mirror the existing #help-faq test), and bump the section-count expectation.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- HelpTab.integration</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "help-sharing" src/components/HelpTab.tsx` >= 1 AND `grep -c "help-sharing" src/components/HelpSectionNav.tsx` >= 1
    - Integration test asserts `document.getElementById('help-sharing')` is non-null and ordered between chat and faq
    - `pnpm test -- HelpTab.integration` green; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The article is reachable from the Help left-nav and renders in-order between Chat and FAQ.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown → BrowserOpenURL | External doc links in the article cross into the system browser via the Wails runtime |
| Help copy → user ACL edit | The ACL block is copy-pasted by users into their live Tailscale policy; an over-broad grant self-inflicts exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-08 | Spoofing | external doc links | low | mitigate | HelpContent.tsx validates SAFE_LINK_SCHEME (https/mailto) before BrowserOpenURL; article uses plain markdown links, no raw <a> |
| T-166-09 | Information Disclosure | copy-paste ACL grant | high | mitigate | Article ships the narrow `autogroup:shared → tag:agenthub:tcp:7443` grant AND explicitly calls out the `*→*` wildcard-default gotcha with a "verify your full ACL after editing" caveat so users do not leave an over-broad allow-all in place |
</threat_model>

<verification>
- `cd frontend && pnpm test -- HelpTab.integration` green.
- `cd frontend && npx tsc --noEmit` clean.
- Article contains the ACL block, :7443, `*→*` gotcha, and Tailscale doc links; no raw `<a>`.
</verification>

<success_criteria>
The Help tab includes a "Sharing Outside Your Tailnet" article (reachable from the left nav, ordered between Chat and FAQ) covering both the Funnel and device-share+ACL paths, with a copy-pasteable :7443 ACL grant and the wildcard-default gotcha called out.
</success_criteria>

<output>
Create `.planning/phases/166-funnel-frontend-help-guide/166-04-SUMMARY.md` when done
</output>