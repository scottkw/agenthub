---
status: partial
phase: 166-funnel-frontend-help-guide
source: [166-01-SUMMARY.md, 166-02-SUMMARY.md, 166-03-SUMMARY.md, 166-04-SUMMARY.md, 166-05-SUMMARY.md]
started: 2026-07-01T04:45:25Z
updated: 2026-07-01T05:28:54Z
---

## Current Test

[testing paused — 4 live items (M-37..M-40) blocked on a production build + real tailnet + off-tailnet device]

## Tests

### 1. Funnel enable flow — two-step gesture + expiry
expected: Toggle ON opens the risk panel (does not enable directly); expiry dropdown defaults to 1h with 5 presets; explicit "Enable internet share" CTA commits, "Keep local only" cancels.
result: pass
note: "Two-step gesture + expiry worked from the start. Reported typography (Arial controls / mis-sized+colored 'Auto-expire:') and layout (34px clipped strip with toggle OFF) defects were diagnosed, fixed in style.css (commit f3c8d848), and user-confirmed live in `wails dev`."

### 2. Internet-exposure indicators appear/clear (M-39)
expected: While Funnel is active, the Hub card shows a globe + "INTERNET" badge and the session tab shows a globe icon (aria-label/title "Internet exposed"). After single-click "Disable internet share", both clear within ~3s (next poll). State is carried by shape+text, not color alone.
result: blocked
blocked_by: release-build
reason: "Needs a live daemon with real funnelActive transitions through the 3s poll (production build on a tailnet with Funnel granted). Not reproducible in wails dev."

### 3. Help cross-link navigates to Sharing Guide (FUI-06)
expected: Clicking "See the Sharing Guide →" in the risk panel closes the Share modal and navigates to the Help tab, scrolled to the "Sharing Outside Your Tailnet" section.
result: pass
note: "Confirmed live in `wails dev` — closes modal, lands on Help → Sharing Outside Your Tailnet. This is the FUI-06 gap the verifier flagged + closed (commit 9db19410)."

### 4. Sharing Guide article content (HLP-01/HLP-02)
expected: The Help tab's "Sharing Outside Your Tailnet" article renders both paths (Tailscale Funnel + device-share/ACL), a copy-pasteable ACL grant (autogroup:shared → tag:agenthub:tcp:7443), and the wildcard-default (*→*) gotcha caveat. External links open in the system browser.
result: pass
note: "Content + typography confirmed live in `wails dev` after the list/code-block fixes (commit f3c8d848). External-link open-in-browser not separately re-tested (unchanged Phase-147 HelpContent path)."

### 5. Live Funnel warm-up + off-tailnet URL (M-37)
expected: In a PRODUCTION build on a Tailscale-connected machine with Funnel granted, enabling internet share shows "Starting up… (TLS warming up)", then within ~30s reveals a single read-only "Public URL (read-only):" with Copy URL / Open / QR. The URL opens the session from a device NOT on the tailnet.
result: blocked
blocked_by: release-build
reason: "Requires production build + live tailnet + Funnel grant + a second off-tailnet device."

### 6. Live auto-expiry teardown (M-38)
expected: Enable Funnel with a short expiry. After the chosen duration elapses, the Funnel tears down automatically (tailscale serve status empty), the public URL stops responding, and the internet indicators clear on next poll — with no manual disable.
result: blocked
blocked_by: release-build
reason: "Requires a live running Funnel + wall-clock expiry on a real tailnet."

### 7. Local-fallback toggle disabled (M-40)
expected: With Tailscale not the web-server mode (tailscaled stopped or local mode), the "Enable internet sharing" toggle is greyed/non-interactive with the note "Internet sharing requires Tailscale", and no risk panel can open. Reconnecting Tailscale re-enables it.
result: blocked
blocked_by: release-build
reason: "Requires a running app in local web-server mode (or tailscaled stopped) to exercise the D-15 fail-closed path live."

### A1. Automated-verified requirements (8/8, gsd-verifier PASSED)
expected: FUI-01..06 + HLP-01/02 verified via passing vitest suites (2253 tests), tsc --noEmit clean, and pnpm run build OK, per 166-VERIFICATION.md (2026-06-30, 8/8). FUI-06 integration gap closed (HubPanel threads onOpenHelp).
result: pass
source: automated

## Summary

total: 8
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 4

## Gaps

- truth: "Risk-panel typography matches UI-SPEC (expiry selector 14px/400 Plus Jakarta Sans; controls in app font)"
  status: failed
  reason: "User reported: fonts all over the place / inconsistent / out of spec in the risk panel and Help sharing section"
  severity: major
  test: 1
  root_cause: "CSS omissions in Phase-166 style.css. Confirmed via rendered harness + computed styles: (1) the expiry <select> has NO styling rule -> native control renders font-family:Arial, color:rgb(0,0,0) on a white native background (spec: 14px/400 Plus Jakarta Sans, dark token surface). (2) <button>s .hub-funnel-risk-panel__help-link, .hub-share-internet-section__disable (Keep local only), and .link-confirm-popover__btn base (CTA) set no `font-family: inherit` -> all render font-family:Arial (form controls do not inherit font-family and there is no global form-control reset). (3) .hub-funnel-risk-panel__expiry label sets no explicit font-size (inherits 16px, spec 14px) and no color (inherits hardcoded body color #c0caf5 -> reads blue/off) -> the 'different serif-ish blue Auto-expire:' impression is actually correct family at wrong size + wrong color. The app already has the correct pattern in .settings-panel__select (font-family:inherit + token bg/border/color)."
  artifacts:
    - path: "frontend/src/style.css"
      issue: ".hub-funnel-risk-panel__expiry (no font-size/color); no rule for its <select>; .hub-funnel-risk-panel__help-link + .hub-share-internet-section__disable + .link-confirm-popover__btn missing font-family:inherit"
  missing:
    - "Add font-family:inherit to .link-confirm-popover__btn (fixes CTA + latent Arial in existing link-confirm popover)"
    - "Style .hub-funnel-risk-panel__expiry select mirroring .settings-panel__select (font-family:inherit, --hub-font-size-base, token bg/border/color, padding, radius)"
    - "Add explicit font-size:var(--hub-font-size-base) + color:var(--hub-text-secondary) to .hub-funnel-risk-panel__expiry"
    - "Add font-family:inherit to .hub-funnel-risk-panel__help-link and .hub-share-internet-section__disable"
    - "Scope CTA to spec: .hub-funnel-risk-panel__actions .link-confirm-popover__btn--continue font-size 14px / weight 600"
  debug_session: "inline (rendered harness frontend/uat-166-harness.{html,tsx} + Playwright computed-style probe)"
  resolution: "FIXED (root-cause, user chose global reset over scoped). style.css: (1) added global form-control reset `button,input,select,textarea { font-family: inherit }` after the body rule — repairs ALL Arial fallbacks app-wide (also confirmed-fixed: .help-nav__link ×4 + .help-search__input, which were the Help-page complaint, same root cause and pre-existing across all help sections). (2) .hub-funnel-risk-panel__expiry: added font-size:var(--hub-font-size-base) + color:var(--hub-text-secondary) (was inherited 16px/#c0caf5). (3) new .hub-funnel-risk-panel__expiry select rule: dark token bg/border/color + 14px (was white native Arial/black). (4) CTA scope rule: 14px/600 per spec. Verified via harness re-probe: risk panel + help nav + search + content ALL now Plus Jakarta Sans at spec sizes/weights. tsc clean; pnpm run build OK; 72/72 tests pass (FunnelRiskPanel, SessionShareModal, SessionSharePanel, HelpTab.integration)."
- truth: "Risk panel is fully collapsed when the Funnel toggle is off (no clipped strip on modal open)"
  status: failed
  reason: "User confirmed: toggle was OFF on modal open, yet a clipped warning strip showed (Image #1)."
  severity: minor
  test: 1
  root_cause: "Confirmed via harness (open=false): .hub-funnel-risk-panel is box-sizing:border-box with max-height:0 BUT padding:16px + 1px border — a border-box element cannot clip below its own padding+border, so the 'collapsed' panel still renders a 34px strip (16+16+1+1) and overflow:hidden clips the warning text into it. Panel is always mounted (SessionShareModal:553 open={riskPanelOpen}, initial false)."
  resolution: "FIXED. style.css .hub-funnel-risk-panel: collapsed state now zeros vertical padding (padding:0 16px) + border-top/bottom-width:0; .--open restores padding:16px + 1px borders; padding added to the transition. Verified: closed renderedHeight 0px (was 34px); open state unchanged. tsc/build/tests green."
- truth: "Help article lists + code blocks match the article's body typography (Sharing Guide)"
  status: failed
  reason: "User reported (Image #4): the Option-1 numbered list is a different size + color and sits outside the text bounds — 'sticks out like a sore thumb'."
  severity: major
  test: 4
  root_cause: "Confirmed via harness computed styles: .help-tab__content p is explicitly 14px / --hub-text-secondary, but <ol>/<li> had NO rule -> inherited leftover body defaults (16px / #c0caf5 lavender) and padding-left:0 pushed the '1.2.3.' markers outside the text column. Same unstyled-element class as the risk-panel label. Separately, fenced code blocks (<pre>) rendered in generic `monospace` while inline .help__kbd code uses JetBrains Mono. Phase-147 help-content gap surfaced by the Sharing Guide's heavy list + ACL-code use."
  resolution: "FIXED. style.css: added .help-tab__content ul,ol,li rules (14px / --hub-text-secondary / line-height 1.6 / ol,ul padding-left:24px so markers sit inside) mirroring the p rule; added .help-tab__content pre + pre code rules (var(--hub-font-mono) + block chrome, neutralized inline pill on inner code). Verified: li/ol now 14px/#c7cad6 w/ 24px indent; code block now JetBrains Mono. tsc/build/tests green (78/78)."
