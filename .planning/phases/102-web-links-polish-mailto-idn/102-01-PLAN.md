---
phase: 102-web-links-polish-mailto-idn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/TerminalPanel.tsx
  - frontend/src/components/__tests__/TerminalPanel.web-links.test.tsx
  - web/assets/terminal.js
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02
user_setup: []

must_haves:
  truths:
    - "WebLinksAddon is constructed with an explicit urlRegex that matches http(s) AND mailto: URLs"
    - "Cmd/Ctrl-clicking 'mailto:user@example.com' in terminal output invokes openLink with the mailto URL"
    - "Cmd/Ctrl-clicking a URL with non-ASCII Cyrillic hostname (e.g. 'https://пример.рф/') routes through LinkConfirmPopover (existing getRisk → idn path)"
    - "web/assets/terminal.js mirrors the desktop urlRegex addition (web parity, UI-SPEC §web-parity-mandate from Phase 95)"
  tests:
    - "vitest: TerminalPanel.web-links.test.tsx adds a case asserting modifier-click on mailto link calls openLink('mailto:...')"
    - "vitest: existing IDN-popover test still passes (sanity — POLISH-02 is already implemented at lib level)"

tasks:
  - id: 1
    name: "RED — add mailto + IDN tests"
    type: test
    file: frontend/src/components/__tests__/TerminalPanel.web-links.test.tsx
    description: |
      Add two new test cases:
      (a) modifier-click on mailto link → expect openLink called with 'mailto:user@example.com'
      (b) (sanity) modifier-click on Cyrillic hostname → expect LinkConfirmPopover renders with the URL
  - id: 2
    name: "GREEN — pass custom urlRegex to WebLinksAddon"
    type: feat
    files:
      - frontend/src/components/TerminalPanel.tsx
      - web/assets/terminal.js
    description: |
      Replace the default urlRegex with an explicit regex that matches http(s) AND mailto:
      Regex: /(https?:\/\/[^\s$.?#].[^\s]*|mailto:[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,})/
      Mirror the change in web/assets/terminal.js. Document with LNK / POLISH-01 comment.
  - id: 3
    name: "docs — SUMMARY"
    type: docs
    description: Write 102-01-SUMMARY.md
