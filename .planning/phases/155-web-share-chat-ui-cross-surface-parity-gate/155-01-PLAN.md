---
phase: 155-web-share-chat-ui-cross-surface-parity-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/daemon/chat.go
  - internal/daemon/chat_test.go
  - internal/daemon/chat_routes_test.go
  - internal/webserver/chat_test.go
  - TESTING.md
autonomous: true
requirements: [EXPORT-01]
must_haves:
  truths:
    - "ChatStore.Export() returns a Markdown document that opens with a `---` YAML frontmatter block containing session, exported_at, and participants keys (EXPORT-01, SC-2)."
    - "Every message round-trips: alias, tailnetID, ISO-8601 timestamp, body, and the injected-into-terminal marker are present without data loss."
    - "An alias containing a YAML special char (`:` or `#`) is double-quoted in the participants list and does not corrupt the frontmatter."
    - "GET /api/chat/{id}/export returns the YAML-frontmatter document on the relay loopback and (cap-gated) on the webserver; a request without a valid cap on the webserver is rejected."
  artifacts:
    - internal/daemon/chat.go (Export rewritten)
    - internal/daemon/chat_test.go (TestChatStore_Export)
    - internal/daemon/chat_routes_test.go (TestChatRoutes_Export)
    - internal/webserver/chat_test.go (TestChatExport)
    - TESTING.md (Section 2 + Section 4 EXPORT-01 rows)
  key_links:
    - "ChatStore.Export() is the single serializer both surfaces call (relay handleChatExport + webserver SetChatExportProvider) — one change covers both surfaces."
    - "Content-Disposition: attachment header is already set server-side (chat_routes.go:82, webserver/chat.go:101) — the browser download trigger depends on it; do not remove it."
---

<objective>
Rewrite `ChatStore.Export()` to emit GitHub-compatible Markdown with a YAML frontmatter block (session, exported_at, participants), satisfying EXPORT-01 success criterion 2. Add Go unit coverage for the serializer and for both export routes (relay loopback + cap-gated webserver). Register the new Go test files in TESTING.md.

Purpose: The export format is the wire contract the Playwright download assertion (Plan 155-04) and the desktop/web Export button (Plan 155-02) both depend on. It must land first and be proven by Go tests before the e2e gate can assert frontmatter.

Output: Updated `Export()` serializer, three Go test functions, TESTING.md traceability rows for EXPORT-01.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-RESEARCH.md
@.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-PATTERNS.md
@.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-UI-SPEC.md
</context>

<artifacts>
Symbols/files this plan produces (for downstream executors and the parity gate):
- `internal/daemon/chat.go` — `ChatStore.Export()` rewritten to emit YAML frontmatter (`session`, `exported_at`, `participants`) + per-message `## {alias} ({tailnetID}) — {ts}` headers + `_injected into terminal_` marker.
- `internal/daemon/chat_test.go` — `TestChatStore_Export` (frontmatter fields, quoting, round-trip).
- `internal/daemon/chat_routes_test.go` — `TestChatRoutes_Export` (relay loopback export route serves frontmatter).
- `internal/webserver/chat_test.go` — `TestChatExport` (cap-gated webserver export serves frontmatter; missing cap rejected).
- TESTING.md — Section 2 entries for the three Go test files; Section 4 EXPORT-01 traceability rows.
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite ChatStore.Export() to emit YAML frontmatter</name>
  <files>internal/daemon/chat.go, internal/daemon/chat_test.go</files>
  <read_first>
    - internal/daemon/chat.go:321-341 (current Export() — keep the lock/copy preamble verbatim)
    - internal/relay/protocol.go:89-97 (ChatMessage / PresenceEntry fields: AuthorAlias, AuthorID, TimestampMs, Content, SessionInject)
    - .planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-UI-SPEC.md — "Export File Format Contract (EXPORT-01)" (authoritative output shape)
    - .planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-RESEARCH.md:695-741 (full Go body reference)
  </read_first>
  <behavior>
    - Empty thread: returns frontmatter-only document (`---` / session / exported_at / empty participants / `---`) + `# Chat: {sessionID}` heading, nil error.
    - Single message: frontmatter participants has exactly one `"alias (authorID)"` entry; body has one `## alias (authorID) — {RFC3339 ts}` block followed by the content.
    - Two messages from the same AuthorID: participants list deduplicates to one entry (order of first appearance).
    - Message with SessionInject==true: emits `_injected into terminal_` on its own line after the content.
    - Alias containing `:` (e.g. `ops:lead`): participant entry is double-quoted `"ops:lead (id)"` so the YAML stays valid; an embedded `"` in an alias is escaped as `\"`.
  </behavior>
  <action>
    Keep the existing `s.mu.Lock()` / copy `s.messages` into `msgs` / read `sessionID` / `s.mu.Unlock()` preamble unchanged (chat.go:322-326). Replace the body builder (chat.go:328-339) with the YAML-frontmatter emitter described in the Export File Format Contract.

    Emit, via `strings.Builder` + `fmt.Fprintf`, in order: `---` line; `session: {sessionID}` (bare — session IDs are `[A-Za-z0-9_-]` only, no quoting); `exported_at: {time.Now().UTC().Format(time.RFC3339)}`; `participants:` followed by one `  - "{alias} ({authorID})"` line per deduplicated participant. Build participants by iterating `msgs` once, tracking a `map[string]bool` keyed on `AuthorID`, appending on first sight, order-of-appearance. Quote every participant value and escape embedded `"` via `strings.ReplaceAll(alias, "\"", "\\\"")` (Pitfall 4 — aliases may legally contain `:` and `#`). Close with `---`, blank line, then `# Chat: {sessionID}`, blank line.

    Per message emit `## {AuthorAlias} ({AuthorID}) — {RFC3339 ts}` (ts from `time.UnixMilli(msg.TimestampMs).UTC().Format(time.RFC3339)`), blank line, `{Content}`, blank line, then if `msg.SessionInject` emit `_injected into terminal_` + blank line, then the `---` section separator + blank line. Do NOT keep the old `**Author ID:**` line — the authorID now lives in the message header.

    Write `TestChatStore_Export` in chat_test.go covering the five behaviors above. Construct the store with `NewChatStore(t.TempDir(), "sess-1")`, `AppendMessage` a few `relay.ChatMessage` values (one with `SessionInject: true`, one alias containing `:`), call `Export()`, and assert on the returned string with `strings.Contains` / line checks.
  </action>
  <verify>
    <automated>go test ./internal/daemon/... -run TestChatStore_Export -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/daemon/... -run TestChatStore_Export -count=1` passes.
    - Exported string starts with `---\n` and contains `session: `, `exported_at: `, and `participants:`.
    - An alias containing `:` appears double-quoted in the participants block.
    - A SessionInject message produces the `_injected into terminal_` marker line.
  </acceptance_criteria>
  <done>Export() emits valid YAML frontmatter + per-message blocks; TestChatStore_Export green.</done>
</task>

<task type="auto">
  <name>Task 2: Export-route Go tests (relay loopback + cap-gated webserver)</name>
  <files>internal/daemon/chat_routes_test.go, internal/webserver/chat_test.go</files>
  <read_first>
    - internal/daemon/chat_routes.go:58-103 (handleChatExport + wrapRelayWithChat mount + Content-Disposition at :82)
    - internal/webserver/chat.go:35-101 (SetChatExportProvider + handleChatExport + Content-Disposition at :101)
    - internal/webserver/server.go:654-655 (GET /api/chat/{id}/export wrapped in requireCapability)
    - internal/webserver/capability_mw.go:39 (cap read from ?cap= query)
    - cmd/playwright-fixture/main.go:180-202 (capability.Claims / capability.Sign / AddGrant pattern for minting a test cap)
  </read_first>
  <action>
    Add `TestChatRoutes_Export` in internal/daemon/chat_routes_test.go: construct a ChatStore seeded with one message, wire it via the existing relay export provider path used by `wrapRelayWithChat`, issue a `GET /api/chat/{id}/export` request through the wrapped handler (httptest), and assert the response body starts with `---` (YAML frontmatter), the `Content-Disposition` header equals `attachment; filename="chat-{id}.md"`, and status 200. Follow the existing test style already present in chat_routes_test.go for the history route.

    Add `TestChatExport` in internal/webserver/chat_test.go: build a WebServer with `SetChatExportProvider` returning a seeded `store.Export()`, mint a valid cap with `capability.Sign` + `AddGrant` (mirror fixture pattern), and assert: (a) `GET /api/chat/{id}/export?cap={valid}` returns 200 with a body containing `session:` and `exported_at:` and the `attachment` Content-Disposition; (b) the SAME request WITHOUT `?cap=` is rejected (401/403) and serves no thread body. Reuse the existing webserver chat_test.go harness/setup for history if present.
  </action>
  <verify>
    <automated>go test ./internal/daemon/... -run TestChatRoutes_Export -count=1 && go test ./internal/webserver/... -run TestChatExport -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Both new test functions pass.
    - The webserver test proves a missing/invalid cap yields a non-200 and no thread body (export endpoint authorization — threat T-155-03).
    - Both tests assert the `attachment; filename="chat-{id}.md"` Content-Disposition header.
  </acceptance_criteria>
  <done>Relay loopback and cap-gated webserver export routes both serve the YAML-frontmatter document; unauthorized webserver request rejected.</done>
</task>

<task type="auto">
  <name>Task 3: Register Go export tests in TESTING.md</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md:22-35 (Section 2 Suite Manifest format)
    - TESTING.md:94-201 (Section 4 Requirement→Test Traceability Map format; path column = repo-relative file path only)
    - TESTING.md:339 (Section 6 Standing Convention)
    - CLAUDE.md (AgentHub regression-test convention — Section 2/4 update rule + traceability-path script)
  </read_first>
  <action>
    Add the three new/extended Go test files to the appropriate Go suite group in TESTING.md Section 2 (Suite Manifest): `internal/daemon/chat_test.go`, `internal/daemon/chat_routes_test.go`, `internal/webserver/chat_test.go` (note them as extended if they already exist). Add Section 4 traceability rows mapping EXPORT-01 to each of these three `.go` files (path column is the repo-relative `.go` path only — no test names in the path column). Do not add the Playwright/vitest rows here — those land in Plans 155-03 and 155-04.

    After editing, run `bash tests/check-traceability-paths.sh` and ensure it exits 0.
  </action>
  <verify>
    <automated>bash tests/check-traceability-paths.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/check-traceability-paths.sh` exits 0.
    - Section 4 contains EXPORT-01 rows for the three Go test files with repo-relative `.go` paths only.
  </acceptance_criteria>
  <done>EXPORT-01 Go tests are registered in TESTING.md Sections 2 and 4; traceability-path check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/web client → webserver export route | Untrusted `?cap=` query token crosses here; must be HMAC-verified before any thread bytes are served |
| message author alias → export YAML frontmatter | Author-controlled alias text crosses into a structured YAML document |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-02 | Tampering | `ChatStore.Export()` participants frontmatter | medium | mitigate | Always double-quote participant values and escape embedded `"`; `ValidateAlias` already limits aliases to printable ASCII. TestChatStore_Export asserts an alias with `:` stays valid YAML. |
| T-155-03 | Information Disclosure / Spoofing | webserver `GET /api/chat/{id}/export` | high | mitigate | Route is wrapped in `requireCapability` (HMAC-verified `?cap=`). TestChatExport asserts a request with no/invalid cap is rejected (non-200) and serves no thread body. No change to the middleware — verified by test. |
| T-155-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new packages introduced in this plan (Go-only; RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `go test ./internal/daemon/... -run Chat -count=1` green (Export + routes).
- `go test ./internal/webserver/... -run TestChatExport -count=1` green.
- `bash tests/check-traceability-paths.sh` exits 0.
- Per VALIDATION sampling: after the plan, `go test ./... ` green.
</verification>

<success_criteria>
- `ChatStore.Export()` emits the exact YAML-frontmatter format in the UI-SPEC Export File Format Contract (EXPORT-01, SC-2).
- Both export routes serve the new format; webserver route rejects unauthorized requests.
- TESTING.md Section 2 + Section 4 register EXPORT-01 Go coverage; traceability-path check passes.
</success_criteria>

<output>
Create `.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-01-SUMMARY.md` when done.
</output>
