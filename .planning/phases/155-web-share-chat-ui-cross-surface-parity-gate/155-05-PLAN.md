---
phase: 155-web-share-chat-ui-cross-surface-parity-gate
plan: 05
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/chat-parity.spec.ts
  - internal/relay/hub.go
  - internal/webserver/server.go
  - frontend/src/components/Hub/ChatPanel.tsx
  - frontend/src/lib/relayClient.ts
autonomous: true
requirements: [PARITY-01]

must_haves:
  truths:
    - "The root cause of broadcast non-delivery is identified from captured /__test__/hub-status JSON + page2 WebSocket frame evidence and recorded as one of: subscriber-registration race, slow-subscriber channel drop, write-pump delivery failure, or client-side render/virtualizer suppression (PARITY-01 SC-1)."
    - "A message sent by web-share RW client A appears in RW client B's thread within the 10s window on chromium, firefox, AND webkit."
    - "Page2 shows the .chat-badge unread badge when Page1 sends while Page2's chat drawer is closed, on chromium, firefox, AND webkit."
  artifacts:
    - "frontend/e2e/chat-parity.spec.ts (broadcast + unread-badge tests passing on all 3 browsers)"
    - "The fix in whichever file the diagnosis implicates (internal/relay/hub.go, internal/webserver/server.go, frontend/src/components/Hub/ChatPanel.tsx, or frontend/src/lib/relayClient.ts)"
  key_links:
    - "hub.HandleChatSend → hub.BroadcastChat → sub.Msgs (buffered 256) → webserver write pump (server.go:1209) → page2 WebSocket → RelayClient onmessage MSG_CHAT (0x30) → onChat → ChatPanel handleChat → setMessages → @tanstack/react-virtual render + auto-scroll"
---

<objective>
Close BLOCKER 1 (PARITY-01 SC-1): a chat message sent by web-share RW client A never reaches RW client B within the 10s window. The broadcast path is fully WIRED in both Go (`HandleChatSend → chatAppendFn → BroadcastChat → sub.Msgs`) and TypeScript (`RelayClient onChat → ChatPanel handleChat → setMessages`), and the 155-04 auto-scroll/virtualizer fix is already in place — yet the live Playwright e2e proves delivery fails on all 3 browsers (6 of 8 failures: SC-1 broadcast ×3 + SC-1 unread badge ×3, which shares the same root cause because the badge depends on broadcast delivery).

Because the root cause is NOT yet diagnosed, this plan is structured DIAGNOSE → FIX → PROVE. Task 1 runs the failing test and captures the `/__test__/hub-status` diagnostic plus the page2 WebSocket frame log, then classifies the root cause from that evidence. Task 2 implements the targeted fix for the identified cause only. Task 3 re-runs the broadcast and unread-badge tests on all 3 browsers and confirms green.

Purpose: PARITY-01 is release-blocking per the standing cross-surface parity rule; the authoritative gate is the live Playwright run being green, not source inspection.
Output: Diagnosed root cause, a targeted fix, and the SC-1 broadcast + unread-badge tests passing on chromium/firefox/webkit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-VERIFICATION.md
@.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DIAGNOSE — run the failing broadcast test and capture hub-status + page2 WS frame evidence</name>
  <files>frontend/e2e/chat-parity.spec.ts (run only; instrumentation already present), cmd/playwright-fixture/main.go (read), internal/relay/hub.go (read), internal/webserver/server.go (read)</files>
  <read_first>
    - frontend/e2e/chat-parity.spec.ts (the 'PARITY-01 SC-1 — message broadcast between two RW web-share clients' test: it already instruments `page2WsFrames` via `page2.on('websocket', …framereceived)` and fetches `${env.adminURL}/__test__/hub-status` before send and after page1 echo)
    - cmd/playwright-fixture/main.go (the `/__test__/hub-status` admin handler at line 359 reports `subscriberCount`, `chatAppendFnWired`, `hubFound`; `SetChatAppendFn` is wired at line 254 before `ws.Start()`)
    - internal/relay/hub.go (Subscribe at line 141, SubscriberCount at line 188, BroadcastChat at line 477 — non-blocking send with `go sub.CloseSlow()` on full channel, HandleChatSend at line 580)
    - internal/webserver/server.go (the WS handler: Subscribe at line 1090, MsgChatSend dispatch at line 1164, write pump at line 1209)
    - frontend/src/components/Hub/ChatPanel.tsx (handleChat at line 396, the RelayClient useEffect at line 404, the auto-scroll virtualizer useEffect at line 652)
  </read_first>
  <action>
    Run ONLY the broadcast test against the fixture with diagnostics surfaced, capturing stdout to a file: `pnpm -C frontend exec playwright test chat-parity --project=chromium -g "message broadcast between two RW" --reporter=list 2>&1 | tee /tmp/155-05-broadcast-diag.txt` (the fixture webServer boots automatically from playwright.config.ts). The test emits `[broadcast-diag]` console lines (hub status before send, hub status after page1 echo, and on failure the page2 WS frame log + closed events + chat-panel aria-hidden).
    From the captured output, extract and record THREE concrete observations into the plan SUMMARY:
      (a) the integer `subscriberCount` reported by `/__test__/hub-status` before the send — the expected value reflects every WS connection: each page mounts TerminalPanel + an always-mounted ChatPanel (D-09), so two browser contexts produce roughly 4 subscribers; a lower count points at a subscriber-registration race;
      (b) whether a chat frame with first byte 48 (0x30 = MSG_CHAT) appears in `page2WsFrames` — if present, the frame reached page2's WebSocket and the failure is client-side render/virtualizer; if absent, the frame never left the server (channel drop or write-pump);
      (c) whether `page2WsClosed` recorded a close (a `CloseSlow` "too slow" policy-violation close implies BroadcastChat's non-blocking send hit a full `Msgs` channel for page2).
    If chromium reproduces, also capture firefox and webkit (`--project=firefox`, `--project=webkit`) since the failure is reported on all 3 — note any per-browser divergence in the frame log.
    Classify the root cause as exactly ONE of: (1) subscriber-registration race, (2) slow-subscriber channel drop, (3) write-pump delivery failure, (4) client-side render/virtualizer suppression — and cite which of observations (a)/(b)/(c) supports it. This classification drives Task 2; do NOT implement any fix in this task.
    Do NOT weaken or remove the existing diagnostic instrumentation in the spec — it stays until Task 3 proves green.
  </action>
  <verify>
    <automated>test -s /tmp/155-05-broadcast-diag.txt && grep -q "broadcast-diag" /tmp/155-05-broadcast-diag.txt</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/155-05-broadcast-diag.txt exists and contains the `[broadcast-diag] hub status before send` line with a concrete `subscriberCount` integer (not null).
    - The SUMMARY records observations (a) the subscriberCount value, (b) present/absent of a byte=48 frame in page2WsFrames, (c) present/absent of a page2 WS close event.
    - The SUMMARY states a single chosen root-cause class (1/2/3/4) with the specific captured evidence (a/b/c) that supports it. "Understand the bug" alone is NOT acceptable — the classification must cite a concrete captured value.
  </acceptance_criteria>
  <done>Root cause of broadcast non-delivery classified into exactly one of the four hypotheses, backed by the captured subscriberCount + WS-frame evidence, recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: FIX — implement the targeted fix for the diagnosed root cause</name>
  <files>ONE of: internal/relay/hub.go, internal/webserver/server.go, frontend/src/components/Hub/ChatPanel.tsx, frontend/src/lib/relayClient.ts (whichever Task 1 implicates)</files>
  <read_first>
    - The Task 1 SUMMARY root-cause classification (drives which file is edited)
    - internal/relay/hub.go (Subscribe line 141, BroadcastChat line 477, HandleChatSend line 580 — note BroadcastChat's non-blocking `select { case sub.Msgs <- frame: default: go sub.CloseSlow() }`)
    - internal/webserver/server.go (write pump at line 1209: `case frame := <-sub.Msgs: conn.Write(ctx, MessageBinary, frame)`)
    - frontend/src/components/Hub/ChatPanel.tsx (handleChat line 396, RelayClient useEffect line 404, auto-scroll useEffect line 652 — `el.scrollHeight - el.scrollTop - el.clientHeight < 150` near-bottom guard)
    - frontend/src/lib/relayClient.ts (onmessage MSG_CHAT parse at line 167/241, onChat dispatch line 254)
  </read_first>
  <action>
    Implement the SMALLEST fix that resolves the root cause identified in Task 1, in the implicated file only:
      - If (1) subscriber-registration race: the race is in test/server timing — ensure the send only fires after both page1 AND page2 are confirmed subscribers. Prefer a fix that makes the system correct (e.g. server registers the subscriber before any client-visible readiness signal) over merely sleeping in the test; if the spec must gate on readiness, gate on the `/__test__/hub-status` subscriberCount reaching the expected value rather than a fixed sleep.
      - If (2) slow-subscriber channel drop: BroadcastChat (hub.go:477) drops the frame for a full `Msgs` channel and schedules CloseSlow. If page2's channel is transiently full at send time, address the drop for chat frames (e.g. avoid spuriously closing a healthy-but-busy chat subscriber, or ensure the 256-buffer is not exhausted by scrollback replay racing the chat frame). Do NOT make the send block the broadcast fan-out (preserve the unlock-before-IO and non-blocking-fan-out discipline; never hold hub.mu across IO).
      - If (3) write-pump delivery failure: the webserver write pump (server.go:1209) is not draining `sub.Msgs` for page2 (e.g. a select arm — ctx.Done/readDone/hub.Done — fires prematurely and returns before the chat frame is written). Fix the pump so a live subscriber's chat frame is written before any premature return.
      - If (4) client render/virtualizer suppression: the frame reaches page2 but ChatPanel does not surface it to the DOM. Strengthen the auto-scroll/virtualizer so a broadcast message received on an idle (not-near-bottom) page2 is rendered — extend the line-652 effect so a newly received message is reliably brought into the virtualizer's render window (mirror the 155-04 first-load scrollToIndex path).
    Honor project conventions: Go via `go fmt` + `golangci-lint` clean, context-aware; TS via camelCase + ESLint/Prettier. Add NO new external dependencies. Do NOT alter the RO gate (HandleChatSend's `sub.ReadOnly → ErrChatReadOnly` at hub.go:584) and do NOT change cross-session/cross-cap isolation — the fix must not let a frame reach a subscriber of a different session.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/relay/... ./internal/webserver/... -run 'Chat|Broadcast' -count=1 && pnpm -C frontend exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The edit is confined to the file(s) implicated by Task 1's classification; the change is the minimal fix for that cause.
    - `go build ./...` succeeds; existing Go chat/broadcast unit tests still pass; `pnpm -C frontend exec tsc --noEmit` passes.
    - The server-side RO gate (hub.go:584 `ErrChatReadOnly`) and BroadcastChat's per-session fan-out (only `h.subscribers` of THIS hub) are unchanged — no cross-session/cross-cap leak introduced.
    - No new package added to go.mod or frontend/package.json.
  </acceptance_criteria>
  <done>The targeted fix for the diagnosed root cause is implemented, builds clean, passes existing unit tests + tsc, and leaves the RO gate and per-session isolation intact.</done>
</task>

<task type="auto">
  <name>Task 3: PROVE — re-run SC-1 broadcast + unread-badge tests on all 3 browsers</name>
  <files>frontend/e2e/chat-parity.spec.ts</files>
  <read_first>
    - frontend/e2e/chat-parity.spec.ts (the two broadcast-dependent tests: 'message broadcast between two RW web-share clients' and 'unread badge appears on Page2 when Page1 sends while chat is closed')
    - frontend/playwright.config.ts (projects: chromium line 33, firefox line 37, webkit line 41)
  </read_first>
  <action>
    Run the two broadcast-dependent tests across all three browser projects: `pnpm -C frontend exec playwright test chat-parity -g "broadcast between two RW|unread badge" --reporter=list`. All 6 instances (2 tests × 3 browsers) must pass. If any browser still fails, return to Task 1's evidence — do NOT loosen the assertions (the 10s `toBeVisible` window and the `.chat-badge` visibility check are the contract; widening them to mask a real delivery failure is prohibited). Once green, remove only TEMPORARY diagnostic-capture scaffolding you may have added in Task 1 if it pollutes output; the test's existing `page2WsFrames`/hub-status instrumentation may remain as it is part of the committed spec.
  </action>
  <verify>
    <automated>pnpm -C frontend exec playwright test chat-parity -g "broadcast between two RW|unread badge" --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -C frontend exec playwright test chat-parity -g "broadcast between two RW|unread badge"` reports all 6 instances passed (chromium/firefox/webkit × 2 tests), 0 failed.
    - The 10s broadcast `toBeVisible` window and the `.chat-badge` visibility assertion are unchanged (not loosened to mask delivery failure).
  </acceptance_criteria>
  <done>SC-1 broadcast and SC-1 unread-badge tests pass on chromium, firefox, and webkit.</done>
</task>

</tasks>

<artifacts_produced>
This plan modifies (no net-new files):
- `frontend/e2e/chat-parity.spec.ts` — broadcast + unread-badge tests now green (possibly with a readiness gate on hub-status subscriberCount instead of a fixed wait).
- ONE of: `internal/relay/hub.go`, `internal/webserver/server.go`, `frontend/src/components/Hub/ChatPanel.tsx`, or `frontend/src/lib/relayClient.ts` — the targeted broadcast-delivery fix.
No new symbols, packages, or test files are introduced (the chat-parity spec already exists and is counted in TESTING.md §2/§4; no traceability-path change).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web-share browser → webserver WS (`/sessions/{id}/ws?cap=`) | Untrusted client frames cross here; cap token authorizes read vs write |
| hub.BroadcastChat → sub.Msgs (per session) | Frame fan-out must stay scoped to subscribers of the same session/hub |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-05-01 | Information Disclosure | BroadcastChat fan-out (hub.go:477) | high | mitigate | Fix must keep BroadcastChat iterating only `h.subscribers` of the message's own Hub; Task 2 acceptance forbids any cross-session/cross-cap delivery — a broadcast-delivery fix must not widen the recipient set beyond the session. |
| T-155-05-02 | Elevation of Privilege | HandleChatSend RO gate (hub.go:584) | high | mitigate | The broadcast fix must not touch the `sub.ReadOnly → ErrChatReadOnly` gate; Task 2 acceptance pins it unchanged so an RO cap still cannot post. |
| T-155-05-03 | Denial of Service | sub.Msgs full-channel CloseSlow (hub.go:485) | low | accept | A genuinely slow subscriber is still closed via CloseSlow; the fix only avoids spuriously closing a healthy-but-busy chat subscriber, not removing the slow-client protection. |
| T-155-05-SC | Tampering | npm/go installs | high | mitigate | No new packages are added (Task 2 acceptance forbids go.mod / package.json additions); no supply-chain surface introduced. |
</threat_model>

<verification>
- `go build ./...` succeeds.
- `pnpm -C frontend exec tsc --noEmit` passes.
- `pnpm -C frontend exec playwright test chat-parity -g "broadcast between two RW|unread badge"` — 6/6 instances pass across chromium/firefox/webkit.
- The SUMMARY documents the diagnosed root cause with cited hub-status/WS-frame evidence.
</verification>

<success_criteria>
BLOCKER 1 closed: the SC-1 broadcast and SC-1 unread-badge Playwright tests pass on all three browsers, the fix is confined to the diagnosed file, the RO gate and per-session isolation are intact, and no new dependency is added.
</success_criteria>

<output>
Create `.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-05-SUMMARY.md` when done. Record the diagnosed root cause (with captured subscriberCount + WS-frame evidence), the file changed, and the green test result.
</output>
