---
phase: 155-web-share-chat-ui-cross-surface-parity-gate
plan: 06
type: execute
gap_closure: true
wave: 2
depends_on: [155-05]
files_modified:
  - frontend/e2e/chat-parity.spec.ts
  - TESTING.md
autonomous: true
requirements: [PARITY-01]

must_haves:
  truths:
    - "The SC-3 RO-gate test reaches its adversarial send step on firefox AND webkit — the seeded history message 'Hello from the fixture (RW)' renders before the timer expires, confirming the WS+TLS handshake + history fetch completed (PARITY-01 SC-3)."
    - "The RO gate still holds on all 3 browsers: after the adversarial RO send attempt the message count is unchanged (server-side ErrReadOnly enforcement is NOT weakened)."
    - "The full chat-parity Playwright suite passes ALL tests on chromium, firefox, AND webkit (the authoritative release-blocking cross-surface parity gate is fully green)."
  artifacts:
    - "frontend/e2e/chat-parity.spec.ts (SC-3 warm-up applied; all 8 tests green on 3 browsers)"
  key_links:
    - "ChatPanel RelayClient onOpen → loadChatHistory(/api/chat/{id}/history?cap=) → setMessages → .chat-msg render → SC-3 timer satisfied → adversarial send → server HandleChatSend ErrReadOnly → afterCount == beforeCount"
---

<objective>
Close BLOCKER 2 (PARITY-01 SC-3) and run the final authoritative parity gate. The RO-gate test waits up to 8s for the seeded history message `Hello from the fixture (RW)` to render (to confirm the WS is established) before its adversarial send step; on firefox and webkit that history never renders in time, so the test times out before it can exercise the gate. The gate code itself is CORRECT and passes on chromium — server-side `HandleChatSend`/`HandleInject` return `ErrReadOnly` for RO caps, and client-side `isReadOnly` suppression disables the composer. The failure is purely connection/handshake timing against the fixture's self-signed cert on slower browsers.

The fix is a connection warm-up / longer wait so the WSS handshake + history fetch + React render completes on firefox and webkit — analogous to the unread-badge test's explicit open-then-close warm-up pattern (chat-parity.spec.ts lines 181-186). After the fix, run the FULL chat-parity suite on all 3 browsers as the final release-blocking gate (covers both blockers and guards against regression of the 16 already-passing instances).

This plan depends on 155-05 (both modify chat-parity.spec.ts, and the final full-suite gate must run after the broadcast fix lands).

Purpose: PARITY-01 is release-blocking; the authoritative evidence is the live Playwright run being fully green.
Output: SC-3 passing on all 3 browsers, the full chat-parity suite green (24/24), and TESTING.md verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-VERIFICATION.md
@.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIX — warm up the WS connection in the SC-3 RO-gate test for firefox/webkit</name>
  <files>frontend/e2e/chat-parity.spec.ts</files>
  <read_first>
    - frontend/e2e/chat-parity.spec.ts (the 'PARITY-01 SC-3 — RO viewer Send button is disabled and server gate holds' test, lines 247-279: it opens chat, asserts `[data-chat-send]` disabled, then waits up to 8s for `.chat-msg` filtered to 'Hello from the fixture (RW)' before counting and attempting the adversarial send)
    - frontend/e2e/chat-parity.spec.ts (the unread-badge test, lines 181-186: its open-then-close warm-up — `click toggle` → wait for composer textarea → wait for `.chat-msg` first → `click toggle` — is the analog pattern to copy)
    - frontend/src/components/Hub/ChatPanel.tsx (loadChatHistory at line 234, called inside the RelayClient `onOpen` callback at line 432-438 — history fetch starts only AFTER the WS handshake completes; on slow WSS this stacks WS-open + TLS + fetch + render inside one window)
    - internal/relay/hub.go (HandleChatSend RO gate at line 580/584 returns ErrChatReadOnly; HandleInject RO gate at line 505/508 returns ErrReadOnly — these stay UNCHANGED)
  </read_first>
  <action>
    Edit ONLY the SC-3 test in frontend/e2e/chat-parity.spec.ts so firefox and webkit have enough time for the WSS handshake + history fetch before the adversarial step. Apply the connection warm-up so the seeded history is reliably present:
      - Increase the seeded-history wait (`expect(...filter({ hasText: 'Hello from the fixture (RW)' })).toBeVisible({ timeout: ... })`) from 8_000 to 15_000 ms, AND/OR add an explicit warm-up before the timer modeled on the unread-badge test (lines 181-186): after opening chat and confirming the composer textarea is visible, wait for `.chat-msg` first() to be visible to confirm the WS is established and history rendered, before the `beforeCount` snapshot.
    Do NOT change the adversarial assertion semantics: the test must still fill the composer, press Enter, wait briefly, and assert `afterCount === beforeCount` (no new message from the RO client). Do NOT relax the server gate, do NOT change `[data-chat-send]` disabled expectation, and do NOT modify any Go file or ChatPanel `isReadOnly` logic — BLOCKER 2 is a test-timing fix only, and weakening the RO gate to make the test pass is explicitly forbidden.
    Follow TS conventions (camelCase, ESLint/Prettier). Because this only edits assertions/timing inside an EXISTING spec (no test file added/renamed/removed and no test-count change), TESTING.md §2 counts and §4 traceability rows do not change — Task 2 still runs the traceability-path guard to confirm.
  </action>
  <verify>
    <automated>pnpm -C frontend exec playwright test chat-parity -g "RO viewer Send button is disabled" --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - The SC-3 test passes on chromium, firefox, AND webkit (3/3 instances).
    - The change is confined to the SC-3 test block in chat-parity.spec.ts; no Go file and no ChatPanel `isReadOnly`/composer-disable logic is modified.
    - The adversarial assertion `expect(afterCount).toBe(beforeCount)` and the `expect(sendBtn).toBeDisabled()` assertion remain present and unchanged in intent.
  </acceptance_criteria>
  <done>SC-3 RO-gate test reaches its adversarial step and passes on all 3 browsers; the server-side RO gate is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: PROVE — full chat-parity suite green on 3 browsers + TESTING.md guard</name>
  <files>frontend/e2e/chat-parity.spec.ts (run), TESTING.md (conditional)</files>
  <read_first>
    - frontend/e2e/chat-parity.spec.ts (all 8 tests)
    - frontend/playwright.config.ts (projects chromium/firefox/webkit at lines 33/37/41)
    - TESTING.md (§2 Suite Manifest line 30 Playwright row; §4 traceability rows for PARITY-01/EXPORT-01 at lines 202/204/205)
    - tests/check-traceability-paths.sh (the traceability-path guard required by the standing TESTING.md convention)
  </read_first>
  <action>
    Run the FULL chat-parity suite across all three browser projects as the authoritative release-blocking gate: `pnpm -C frontend exec playwright test chat-parity --reporter=list`. All 24 instances (8 tests × 3 browsers) must pass — this covers BLOCKER 1's broadcast + unread tests (fixed in 155-05), BLOCKER 2's SC-3 (fixed in Task 1), and the 16 instances that were already green (guards against regression).
    Then run `bash tests/check-traceability-paths.sh` and confirm it exits 0. If 155-05 or Task 1 added/renamed/removed any test FILE (they should not — both only modify the existing chat-parity.spec.ts), update TESTING.md §2 (Suite Manifest counts) and §4 (traceability rows, repo-relative `.ts`/`.go`/`.tsx`/`.sh` paths only) accordingly and add a §5 M-NN manual item only if a new non-automatable behavior was introduced (none expected). If no test file was added/renamed/removed, leave TESTING.md counts unchanged — the existing chat-parity rows already cover this spec.
    If any of the 24 instances fail, do NOT loosen assertions; treat it as an unresolved blocker and return to 155-05 Task 1 (broadcast) or this plan's Task 1 (SC-3) evidence.
  </action>
  <verify>
    <automated>pnpm -C frontend exec playwright test chat-parity --reporter=list && bash tests/check-traceability-paths.sh</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -C frontend exec playwright test chat-parity` reports 24/24 passed (8 tests × chromium/firefox/webkit), 0 failed.
    - `bash tests/check-traceability-paths.sh` exits 0.
    - TESTING.md §2/§4 are consistent with reality (unchanged if no test file was added/renamed/removed; otherwise updated with repo-relative paths).
  </acceptance_criteria>
  <done>The full chat-parity cross-surface parity gate is green on all 3 browsers and the TESTING.md traceability guard passes — PARITY-01 is satisfied by live e2e evidence.</done>
</task>

</tasks>

<artifacts_produced>
This plan modifies (no net-new files):
- `frontend/e2e/chat-parity.spec.ts` — SC-3 test warm-up/timeout applied; all 8 tests green on 3 browsers.
- `TESTING.md` — only if a test file was added/renamed/removed by the gap closure (not expected); otherwise unchanged. The traceability-path guard (`tests/check-traceability-paths.sh`) is run regardless.
No new symbols, packages, or test files are introduced.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RO-cap web-share viewer → webserver WS / chat-send + inject | An RO viewer must never post a chat message or inject to the PTY; server enforces regardless of client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-06-01 | Elevation of Privilege | SC-3 RO gate (hub.go HandleChatSend:584 / HandleInject:508) | high | mitigate | BLOCKER 2 fix changes ONLY Playwright connection timing; Task 1 acceptance forbids editing any Go file or ChatPanel `isReadOnly`/composer-disable logic, so `ErrReadOnly`/`ErrChatReadOnly` enforcement is unchanged — an RO cap still cannot post or inject. |
| T-155-06-02 | Spoofing | self-signed fixture TLS cert | low | accept | The longer wait accommodates the slow WSS handshake against the fixture's self-signed cert; `ignoreHTTPSErrors` is fixture-only (build-tagged `playwrightfixture`, never in release builds) — no production trust change. |
| T-155-06-SC | Tampering | npm/go installs | high | mitigate | No new packages added; test-only timing edit. No supply-chain surface introduced. |
</threat_model>

<verification>
- `pnpm -C frontend exec playwright test chat-parity -g "RO viewer Send button is disabled"` — 3/3 pass (SC-3 on chromium/firefox/webkit).
- `pnpm -C frontend exec playwright test chat-parity` — 24/24 pass across all 3 browsers (full authoritative gate).
- `bash tests/check-traceability-paths.sh` exits 0.
</verification>

<success_criteria>
BLOCKER 2 closed and the phase re-verifiable: SC-3 passes on all 3 browsers via a connection-timing fix that leaves the server-side RO gate intact, and the full chat-parity suite (all 8 tests × 3 browsers) is green — the release-blocking PARITY-01 cross-surface parity gate is satisfied by live e2e evidence.
</success_criteria>

<output>
Create `.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-06-SUMMARY.md` when done. Record the SC-3 warm-up applied, the full 24/24 green result, and the traceability-guard exit status.
</output>
