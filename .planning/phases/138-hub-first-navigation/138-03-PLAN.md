---
phase: 138-hub-first-navigation
plan: 03
type: execute
wave: 2
depends_on: ["138-02"]
files_modified:
  - frontend/src/components/Hub/SessionCard.tsx
  - frontend/src/components/Hub/HubPanel.tsx
  - frontend/src/style.css
autonomous: true
requirements: [CARD-01, CARD-04]
must_haves:
  truths:
    - "The card overflow menu offers Kill session (two-step inline confirm) on live sessions, guarded against the card-click modal"
    - "Remote cards additionally offer Open in browser and Browse files overflow items; local cards offer neither"
    - "All new menu items call e.stopPropagation() so they never trigger the Phase 134 card-click modal"
    - "The .hub__header block (Hub title + duplicate New session button) is removed from HubPanel"
    - "HubFilterBar's New Session button is the sole GUI session-creation entry point"
    - "Per-peer unreachable / no-shared-sessions hint renders below the filter bar when such peers exist"
  artifacts:
    - path: "frontend/src/components/Hub/SessionCard.tsx"
      provides: "Kill/Open-browser/Browse overflow items + KillConfirmItem"
      contains: "Kill session"
    - path: "frontend/src/components/Hub/HubPanel.tsx"
      provides: "Header removed + peer hint"
      contains: "hub__peer-hint"
    - path: "frontend/src/style.css"
      provides: "Destructive menu + peer-hint CSS"
      contains: ".hub-card__menu-item--destructive"
  key_links:
    - from: "SessionCard overflow menu"
      to: "card-click guard"
      via: "e.stopPropagation + .closest('.hub-card__menu')"
      pattern: "stopPropagation"
    - from: "HubPanel"
      to: "remotePeers unreachable hint"
      via: "filter !reachable || sessions.length===0"
      pattern: "hub__peer-hint"
---

<objective>
The card-affordance and header-removal layer. Add the parity-migrated controls to the card overflow
menu (Kill — decision 1; Open in browser — decision 2; Browse files — decision 3), render the per-peer
unreachable/empty hint (decision 4), and delete the duplicate `.hub__header` New Session button so the
HubFilterBar button is the sole entry point (CARD-01). All decisions are LOCKED in CONTEXT.md —
nothing is deferred.

Purpose: Closes the three CONTEXT.md parity gaps on the Hub before the Sessions/Remote pages are
deleted in Plan 04 (cross-surface parity is release-blocking), and satisfies CARD-01 + the CARD-04
affordance/preservation contract.
Output: Card overflow menu with guarded Kill/Open-browser/Browse items, peer hint, header-less Hub,
matching CSS — full vitest suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-hub-first-navigation/138-CONTEXT.md
@.planning/phases/138-hub-first-navigation/138-RESEARCH.md
@.planning/phases/138-hub-first-navigation/138-PATTERNS.md
@.planning/phases/138-hub-first-navigation/138-UI-SPEC.md
@.planning/phases/138-hub-first-navigation/138-02-SUMMARY.md

<interfaces>
SessionCard already has (after Plan 02): isRemote, isConnected, onKill, onOpenInBrowser, onBrowseFiles props.
Overflow menu: opened via `.hub-card__menu-btn` (sets menuOpen); markup inside `{menuOpen && (<div className="hub-card__menu" role="menu">...)}` (~291-320). Existing items use role="menuitem" + className="hub-card__menu-item".
Card-click guard (article onClick ~248-257) already returns early on `target.closest('.hub-card__menu')` — covers new menu items.
Each new interactive child MUST use onClick={(e) => { e.stopPropagation(); ...; setMenuOpen(false) }}.
Session liveness: render Kill only when session.state !== 'stopped'.
onOpenInBrowser receives session.url; onBrowseFiles receives (id, name).
HubPanel header to delete (~462-467): <div className="hub__header"><span className="hub__title">Hub</span><button className="hub__new-session-btn" onClick={onNewSession}>New session</button></div>
HubFilterBar New Session button (~135-141) is the surviving entry point — do NOT touch it.
remotePeers prop (HubPanelProps, added in Plan 02) shape: RemotePeerSessions[] { hostname, reachable, sessions[] }.
Copywriting (UI-SPEC): "Kill session" → "Confirm kill" + "This will stop the session"; "Open in browser"; "Browse files"; hint "{hostname} is unreachable" / "{hostname} has no shared sessions".
ArrowTopRightOnSquareIcon is the Open-in-browser icon (confirmed in node_modules); text label is the primary signal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Kill / Open-in-browser / Browse-files items to the card overflow menu</name>
  <files>frontend/src/components/Hub/SessionCard.tsx, frontend/src/style.css</files>
  <read_first>
    - frontend/src/components/Hub/SessionCard.tsx (overflow menu block ~291-320; card-click guard ~248-257; Share button stopPropagation pattern ~421; useState import; menuOpen/setMenuOpen)
    - frontend/src/style.css (locate .hub-card__menu-item rules for the destructive modifier insertion point; confirm --hub-destructive custom property exists)
    - .planning/phases/138-hub-first-navigation/138-PATTERNS.md (SessionCard overflow additions + KillConfirmItem pattern; Pattern B stopPropagation guard)
    - .planning/phases/138-hub-first-navigation/138-UI-SPEC.md (overflow menu structure; Kill two-step contract; copywriting)
    - frontend/src/components/__tests__/SessionCard.share.test.tsx (CARD-04 RED tests — the green target)
  </read_first>
  <behavior>
    - Live local card: overflow menu shows "Kill session"; clicking it once flips label to "Confirm kill" + subtext "This will stop the session"; second click calls onKill(id)
    - Remote card: overflow menu also shows "Open in browser" and "Browse files" (above the Kill divider)
    - Local card: overflow menu shows neither "Open in browser" nor "Browse files"
    - Stopped session: no "Kill session" item
    - Clicking any new menu item never calls onCardClick (stopPropagation + existing .closest guard)
  </behavior>
  <action>
    Add `ArrowTopRightOnSquareIcon` to the SessionCard heroicons import. Inside the existing
    `{menuOpen && (...)}` overflow-menu block, after the group items, add a remote-only section guarded
    by `isRemote &&`: a `<hr className="hub-card__menu-divider" />` then two `role="menuitem"`
    `className="hub-card__menu-item"` buttons — "Open in browser" (onClick e.stopPropagation();
    onOpenInBrowser?.(session.url ?? ''); setMenuOpen(false)) with ArrowTopRightOnSquareIcon, and
    "Browse files" (onClick e.stopPropagation(); onBrowseFiles?.(id, name); setMenuOpen(false)). Then,
    guarded by `session.state !== 'stopped'`, add a `<hr className="hub-card__menu-divider" />` and a
    local `KillConfirmItem` component instance (onKill={() => { onKill?.(id); setMenuOpen(false) }}).
    Define `KillConfirmItem` as a small local component in SessionCard.tsx: holds a `confirming`
    useState; renders a button `className="hub-card__menu-item hub-card__menu-item--destructive"`
    role="menuitem"; first click (e.stopPropagation()) sets confirming=true and returns; second click
    calls onKill(); label is "Kill session" when not confirming, else "Confirm kill" with a
    `<span className="hub-card__menu-item-sub">This will stop the session</span>`.
    In style.css add `.hub-card__menu-item--destructive { color: var(--hub-destructive) }`,
    `.hub-card__menu-item-sub` (12px muted secondary line), and `.hub-card__menu-divider` if not already
    present (1px border, margin 4px 0). Color is reinforcement only — the word "Kill" is the signal.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/__tests__/SessionCard.share.test.tsx src/components/__tests__/style.hub.test.ts 2>&1 | tail -25 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SessionCard.tsx imports `ArrowTopRightOnSquareIcon`; overflow menu contains "Open in browser" and "Browse files" buttons inside an `isRemote &&` guard and a KillConfirmItem inside a `session.state !== 'stopped'` guard
    - Every new menu-item onClick calls `e.stopPropagation()` (grep: at least 3 new stopPropagation call sites in the menu region)
    - KillConfirmItem flips "Kill session" → "Confirm kill" + "This will stop the session" and only calls onKill on the second click
    - style.css contains `.hub-card__menu-item--destructive` using `var(--hub-destructive)`, plus `.hub-card__menu-item-sub` and `.hub-card__menu-divider`
    - All CARD-04 affordance tests in SessionCard.share.test.tsx pass GREEN; destructive-menu CSS tests in style.hub.test.ts pass GREEN; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Card overflow menu offers Kill (two-step), Open in browser, and Browse files with full card-click-modal guards; destructive CSS uses the custom property; CARD-04 affordance tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove .hub__header (CARD-01) and render the per-peer unreachable/empty hint</name>
  <files>frontend/src/components/Hub/HubPanel.tsx, frontend/src/style.css</files>
  <read_first>
    - frontend/src/components/Hub/HubPanel.tsx (.hub__header block ~462-467; HubFilterBar usage; remotePeers prop added in Plan 02; insertion point below the filter bar, above the card grid)
    - frontend/src/style.css (locate .hub__header / .hub__title / .hub__new-session-btn rules to remove; insertion point for .hub__peer-hint)
    - .planning/phases/138-hub-first-navigation/138-PATTERNS.md (HubPanel hub__header DELETE block + peer-hint pattern)
    - .planning/phases/138-hub-first-navigation/138-UI-SPEC.md (CARD-01 contract; Per-peer hint contract + copy)
    - frontend/src/components/__tests__/App.hub.test.tsx + style.hub.test.ts (Plan 01 assertions: no hub__header — the green target)
  </read_first>
  <behavior>
    - HubPanel source no longer contains the `.hub__header` block; the only New Session trigger is HubFilterBar's button
    - When remotePeers contains a peer with reachable===false, a muted line "{hostname} is unreachable" renders below the filter bar
    - When a peer is reachable but has zero sessions, "{hostname} has no shared sessions" renders
    - When all peers are reachable with sessions, no hint lines render
  </behavior>
  <action>
    Delete the entire `<div className="hub__header">...</div>` block (Hub title span + hub__new-session-btn
    button) from HubPanel.tsx. Verify `onNewSession` is still referenced only by HubFilterBar (do not
    remove the prop if HubFilterBar consumes it). Below the HubFilterBar render and above the card grid,
    add a hint block: map over `(remotePeers ?? []).filter(p => !p.reachable || p.sessions.length === 0)`
    and render one `<p key={p.hostname} className="hub__peer-hint">` per peer with text
    `${p.hostname} is unreachable` when `!p.reachable`, else `${p.hostname} has no shared sessions`.
    In style.css: remove (or comment as dead) the `.hub__header`, `.hub__title`, and
    `.hub__new-session-btn` rules; add `.hub__peer-hint` (color var(--hub-text-muted), font-size 12px,
    margin per spacing scale). Use `grep -n "hub__header\|hub__title\|hub__new-session-btn"
    frontend/src/style.css` to locate the rules first.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/__tests__/App.hub.test.tsx src/components/__tests__/style.hub.test.ts 2>&1 | tail -20 && grep -c "hub__header" frontend/src/components/Hub/HubPanel.tsx</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hub__header" frontend/src/components/Hub/HubPanel.tsx` returns 0
    - HubPanel.tsx contains a `.hub__peer-hint` render guarded by a filter on `!p.reachable || p.sessions.length === 0`; copy matches "is unreachable" and "has no shared sessions"
    - style.css contains `.hub__peer-hint` and does NOT contain a live `.hub__header` rule (commented-out is acceptable; an active selector is not)
    - App.hub.test.tsx hub__header-absence test passes GREEN; style.hub.test.ts (no .hub__header; CARD-04 anti-regression intact) passes GREEN
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The duplicate Hub header New Session button is gone (HubFilterBar is sole entry); per-peer unreachable/empty hints render from remotePeers; header CSS removed and peer-hint CSS added; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| card overflow action → App handler | Kill/open/browse invoke App callbacks that act on a session id/url |
| remote peer metadata → peer hint text | hostname rendered as text in the hint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-05 | Elevation of Privilege | Kill on remote cards | mitigate | KillConfirmItem calls onKill(id) → handleCloseTab, which kills LOCAL daemon tabs; remote sessions are not locally owned. Kill renders for live sessions regardless of origin, but the underlying handler only affects locally-managed sessions — matches the Sessions-page Kill it replaces. Remote-only re-share remains blocked (Share disabled on remote, SHARE-06, preserved in Plan 02). |
| T-138-06 | Tampering (XSS) | peer hint hostname text | accept | hostname rendered as a React text child (auto-escaped); no dangerouslySetInnerHTML. |
| T-138-07 | UI redress (accidental destructive action) | Kill session | mitigate | Two-step inline confirm ("Kill session" → "Confirm kill") prevents single-click accidental session termination; matches UI-SPEC low-friction destructive pattern. |
| T-138-SC | Tampering | npm/pip/cargo installs | accept | No package installs — edits against existing @heroicons + vitest only. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0.
- `cd frontend && npx vitest run` — full suite green (panels still present; all CARD-01/03/04 tests now green; Plan 04 will handle deletion).
- `grep -c "hub__header" frontend/src/components/Hub/HubPanel.tsx` === 0.
- Colorblind source check: Kill uses "Kill session" text + `var(--hub-destructive)`; peer hint is plain text; no color-only signals.
</verification>

<success_criteria>
The three CONTEXT.md parity affordances (Kill, Open-in-browser, Browse-files) live on the card overflow
menu fully guarded against the card-click modal; the per-peer hint preserves the Remote page's
unreachable/empty messaging; the duplicate `.hub__header` New Session button is removed leaving
HubFilterBar as sole entry; CSS updated; tsc and full vitest suite green.
</success_criteria>

<output>
Create `.planning/phases/138-hub-first-navigation/138-03-SUMMARY.md` when done. Confirm all card
affordances + header removal are green so Plan 04 can safely delete the panels and sidebar items.
</output>
