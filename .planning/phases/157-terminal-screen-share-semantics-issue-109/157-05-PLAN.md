---
phase: 157-terminal-screen-share-semantics-issue-109
plan: 05
type: execute
wave: 3
depends_on: ["157-01", "157-02", "157-03", "157-04"]
files_modified:
  - TESTING.md
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03, VIEW-04, VIEW-05]
must_haves:
  truths:
    - "TESTING.md Section 2 suite-manifest counts reflect every new/changed test file from Plans 01-04 (success criterion 6)."
    - "TESTING.md Section 4 traceability map has a row per VIEW requirement pointing at a repo-relative .go/.ts/.tsx path (test names in Notes only)."
    - "TESTING.md Section 5 has a new manual checklist item for the Issue #109 two-surface garble scenario (not unit-automatable)."
    - "bash tests/check-traceability-paths.sh exits 0 (every mapped path exists on disk)."
  artifacts:
    - "TESTING.md: updated Suite Manifest counts, VIEW-01..05 traceability rows, new manual Category P item"
  key_links:
    - "The traceability path column must be a repo-relative file path only (.go/.ts/.tsx/.sh); test/describe names live in the Notes column (enforced by check-traceability-paths.sh)."
  prohibitions:
    - "The path column MUST NOT contain a test/describe name (only a file path) — it would fail the path-check script."
    - "No phase test artifact may be left out of the manifest counts or traceability map (standing convention)."
---

<objective>
Honor the AgentHub standing regression-test convention (repo CLAUDE.md + TESTING.md Section 6): update
TESTING.md for every test file Plans 01-04 added or changed. Update Section 2 suite-manifest counts,
add Section 4 traceability rows for VIEW-01..05 (repo-relative paths only), add a Section 5 manual
item for the Issue #109 two-surface garble scenario, and run `bash tests/check-traceability-paths.sh`.

Purpose: This is release-relevant, not optional — the traceability path-check runs in CI and fails the
build if any mapped path is stale. ROADMAP success criterion 6 explicitly requires the TESTING.md
manifest + traceability map to be updated for every new/changed test file.
Output: Updated TESTING.md (Sections 2, 4, 5) with a green `check-traceability-paths.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/157-terminal-screen-share-semantics-issue-109/157-01-SUMMARY.md
@.planning/phases/157-terminal-screen-share-semantics-issue-109/157-02-SUMMARY.md
@.planning/phases/157-terminal-screen-share-semantics-issue-109/157-03-SUMMARY.md
@.planning/phases/157-terminal-screen-share-semantics-issue-109/157-04-SUMMARY.md
@TESTING.md
</context>

<artifacts_this_phase_produces>
- TESTING.md Section 2: updated Go + vitest counts and a phase-157 delta note.
- TESTING.md Section 4: VIEW-01..05 traceability rows.
- TESTING.md Section 5: new "Category P — Terminal Screen-Share Garble (Issue #109)" manual item(s).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Update TESTING.md manifest + traceability + manual checklist</name>
  <files>TESTING.md</files>
  <read_first>
TESTING.md Section 2 (Suite Manifest counts + delta-note style),
TESTING.md Section 4 (Traceability Map — path-only column rule at line ~98),
TESTING.md Section 5 (Manual Regression Checklist — Category format),
The four SUMMARY files (157-01..04) for the authoritative list of new/changed test files,
tests/check-traceability-paths.sh (what it validates).
  </read_first>
  <action>
From the Plan 01-04 SUMMARYs, gather the exact list of new/changed test files. Expected set (confirm
against SUMMARYs — they are authoritative if names differ):
- internal/relay/hub_test.go (rewritten MC-06 trio → host-authority/min/freeze/web-ignore/broadcast/Rows)
- internal/relay/server_test.go + internal/webserver/server_test.go (join-push order + web-origin drop)
- frontend/src/lib/relayClient.test.ts (0x02 → onResize)
- frontend/src/lib/terminalScale.test.ts (computeGuestScale cap math)
- frontend/src/components/__tests__/TerminalPanel.scale.test.tsx (guest honor + cap; host invariance)
(Section 2) Update the Go `*_test.go` and vitest counts by the net file delta (new files only; rewritten
files are not a count change) and append a Phase 157 delta note in the established running-note style.
(Section 4) Add one traceability row per VIEW requirement. Path column = repo-relative file path ONLY
(e.g. `internal/relay/hub_test.go`); put the test/describe name in the Notes column. Map:
VIEW-01 + VIEW-02 → internal/relay/hub_test.go; VIEW-02 + VIEW-03 → internal/webserver/server_test.go
and internal/relay/server_test.go; VIEW-04 → frontend/src/lib/relayClient.test.ts +
frontend/src/components/__tests__/TerminalPanel.scale.test.tsx; VIEW-05 → frontend/src/lib/terminalScale.test.ts
+ frontend/src/components/__tests__/TerminalPanel.scale.test.tsx.
(Section 5) Add a new "Category P — Terminal Screen-Share Garble (Issue #109)" with manual items: the
two-surface host+smaller-guest no-garble scenario (VIEW-01..05, criterion 1), the downscale-fit + cap
s<=1.0 visual check (VIEW-05), and the desktop-guest cross-surface parity check (criterion 5) — using
the instructions in 157-VALIDATION.md's Manual-Only Verifications table. Web `terminal.js` viewer
behavior is structurally gated + manual-UAT verified (vendored asset, outside vitest), so note its
proof lives in this Category P item.
Run `bash tests/check-traceability-paths.sh` and fix any path that does not resolve.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && bash tests/check-traceability-paths.sh && grep -c 'VIEW-0' TESTING.md</automated>
  </verify>
  <acceptance_criteria>
`check-traceability-paths.sh` exits 0. TESTING.md has a traceability row for each of VIEW-01..05 with a
repo-relative path-only column, updated Section 2 counts + Phase 157 delta note, and a Category P manual
item set covering the Issue #109 garble / scale / parity scenarios.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — documentation-only) | This plan edits TESTING.md; no runtime trust boundary is crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-157-08 | Repudiation (coverage gap) | TESTING.md traceability map | low | mitigate | `check-traceability-paths.sh` in CI fails the build if any mapped path is stale; the standing convention forces every test file into the manifest. |
</threat_model>

<verification>
- `bash tests/check-traceability-paths.sh` exits 0.
- Every VIEW-01..05 ID appears in Section 4 with a path-only column.
- Section 2 counts match the on-disk new test-file count; Section 5 Category P present.
</verification>

<success_criteria>
ROADMAP criterion 6 satisfied: MC-06 tests replaced (Plan 01) AND the TESTING.md suite manifest +
traceability map updated for every new/changed test file, with the path-check green.
</success_criteria>

<output>
Create `.planning/phases/157-terminal-screen-share-semantics-issue-109/157-05-SUMMARY.md` when done.
</output>
