---
status: complete
phase: 157-terminal-screen-share-semantics-issue-109
source: [157-VERIFICATION.md]
started: 2026-06-27T13:55:44Z
updated: 2026-06-27T14:18:53Z
---

## Current Test

[testing complete]

## Tests

### 1. Issue #109 two-surface garble check (M-27)
expected: Host PTY + smaller-windowed web guest: no garble (no overlapping/doubled chars); guest grid matches host; downscale CSS transform applied (s <= 1.0); no client-driven 0x11 resize frame sent. Live multi-window xterm.js render required (web terminal.js is a vendored asset outside vitest).
result: pass
source: automated
verified_by: |
  dev-browser harness (isolated, real Chromium) mounting the byte-identical
  web/assets/terminal.js (sha 96a8177a) over the real vendored xterm.js +
  addon-fit.js, with a mock WebSocket pushing a server-style 0x02 (MsgResize)
  host-grid frame (120x40) before 0x01 scrollback, served from /sessions/HARNESS/
  with a fail-safe write-cap perms stub. Measured live:
    - term grid honored 0x02 exactly: 120x40 == host 120x40 (no independent fit)
    - .xterm inline transform = scale(0.46875), s <= 1.0 (height-bound downscale)
    - VIEW-05 cap: viewport ENLARGED beyond grid -> scale capped at exactly 1.0
      (no upscale); shrunk smaller -> downscaled to 0.395648
    - frames sent by guest = 0 (0x11 count = 0) across onopen + multiple window
      resizes, even with a WRITE capability -> no client-driven resize ever
    - screenshot: 120-col ruler + box-drawing line render flush as single lines
      (no wrap/doubled-char garble), whole grid downscaled inside the guest
      viewport bounds with no clipping (right-side letterbox padding only)
  Evidence: scratchpad/h157/m27-guest.png; covers VALIDATION Manual-Only rows 1+2.

### 2. Desktop guest cross-surface parity check (M-28)
expected: Open a session as a remote guest in the desktop app via HubModal. Confirm the guest panel shows the host grid (no sendResize sent back to the server), CSS scale fits the viewport, and there is no garble — visual parity with the web guest. Requires the native Wails WebView (Playwright cannot access it). The isGuest gate and sendResize suppression are unit-proven.
result: pass
source: automated
surface_correction: |
  The verifier assumed the "desktop guest" renders in the native Wails WebView
  (Playwright-inaccessible). Source read disproves this: the React guest path
  (TerminalPanel with isGuest = remote || !!wsURL) is mounted ONLY by
  WebShareSessionView, which mounts ONLY when detectMode()==='web' (the SPA
  served at /app/ in a regular browser). The native app's sole TerminalPanel
  callsite (App.tsx:1711) passes neither remote nor wsURL -> always host; and
  "open remote session" calls BrowserOpenURL (default browser, not a native
  panel). So the M-28 "desktop guest" surface IS a browser-rendered React SPA
  and IS drivable by dev-browser.
verified_by: |
  Live end-to-end against the running daemon (NOT a mock). Built the desktop app
  fresh from HEAD 01d58ba7 (wails build -tags wailsassets; both pre-existing
  builds predated Phase 157), launched it, ran a host shell session (`top`),
  web-shared it, and drove the served React guest SPA at
  /app/?session=736f55f2...&cap=<read,write> in dev-browser (Chromium) at a
  760x440 viewport (smaller-windowed guest). Measured live:
    - WebShareSessionView mounted the real frontend/src/components/TerminalPanel
      (.xterm present) and streamed the host's live `top` output
    - guest rendered the host grid: 44 rows, UNCHANGED across three guest-window
      resizes (1200x800, 520x320, 900x600) -> host-authority, guest never
      re-grids itself
    - .xterm inline transform = scale(0.467836) at 760x440, recomputed to
      scale(0.584795) at 900x600 -> downscale applied, always s <= 1
    - frames sent by guest on resize = 0 (0x11 count = 0) across all three
      resizes, with a read,write capability -> no client-driven resize ever
    - screenshot: `top` process table columns render flush and aligned
      (no garble / no doubled chars), full grid downscaled inside the panel
      with no clipping -> visual parity with the M-27 web guest
  Evidence: scratchpad/h157/m28-desktop-guest.png; covers VALIDATION Manual-Only
  row 3 (cross-surface parity, release-blocking).

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — both manual live-render items verified via dev-browser]
