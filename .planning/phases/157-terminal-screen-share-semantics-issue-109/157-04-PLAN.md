---
phase: 157-terminal-screen-share-semantics-issue-109
plan: 04
type: execute
wave: 2
depends_on: ["157-01"]
files_modified:
  - frontend/src/lib/relayClient.ts
  - frontend/src/lib/terminalScale.ts
  - frontend/src/components/TerminalPanel.tsx
  - frontend/src/style.css
  - frontend/src/lib/relayClient.test.ts
  - frontend/src/lib/terminalScale.test.ts
  - frontend/src/components/__tests__/TerminalPanel.scale.test.tsx
autonomous: true
requirements: [VIEW-04, VIEW-05]
must_haves:
  truths:
    - "The desktop guest viewer (remote || wsURL) honors a server-pushed 0x02 → term.resize(cols,rows) and CSS-downscales to fit, achieving cross-surface parity with the web guest (VIEW-04/05, success criterion 5 — release-blocking)."
    - "The desktop HOST path (no remote, no wsURL) is unchanged: it keeps fitTerminal + client-driven 0x11 send and never honors 0x02 or scales (host view never disturbed)."
    - "computeGuestScale returns s = min(cw/gridW, ch/gridH) capped at 1.0 (downscale-only), shared by web and desktop math (VIEW-05)."
  artifacts:
    - "frontend/src/lib/relayClient.ts: new optional onResize callback wired where 0x02 was dropped"
    - "frontend/src/lib/terminalScale.ts: new pure computeGuestScale(containerW,containerH,gridW,gridH) helper"
    - "frontend/src/components/TerminalPanel.tsx: isGuest gating (honor onResize, disable fit/ResizeObserver-refit, no sendResize, apply transform)"
    - "frontend/src/style.css: guest .xterm transform-origin rule"
    - "vitest: relayClient.test.ts (0x02 → onResize), terminalScale.test.ts (cap math), TerminalPanel.scale.test.tsx (guest honors + caps; host path unchanged)"
  key_links:
    - "relayClient already parses 0x02 into {type:'resize',cols,rows} at relayClient.ts:140; the change un-drops it at relayClient.ts:259 by invoking callbacks.onResize."
    - "isGuest = remote || !!wsURL gates ALL guest behavior so the host path stays pixel-pristine."
  prohibitions:
    - "The desktop GUEST MUST NOT call client.sendResize (on open or term.onResize) and MUST NOT attach the ResizeObserver→fitTerminal refit; it observes the container only to recomputeScale."
    - "The desktop HOST path MUST NOT honor 0x02 or apply a scale transform (it renders natively)."
    - "The scale factor MUST NOT exceed 1.0."
---

<objective>
Bring the desktop terminal viewer to cross-surface parity with the web guest. Add an `onResize`
callback to `RelayClient` and stop dropping the already-parsed 0x02 frame. In `TerminalPanel`, gate
guest behavior on `isGuest = remote || !!wsURL`: guests honor 0x02 → `term.resize` + CSS-downscale,
disable FitAddon/ResizeObserver re-fit, and never send client-driven resize. The host path
(no remote, no wsURL) is left entirely unchanged. Extract the scale math into a shared pure helper so
VIEW-05 is unit-tested once and reused across surfaces.

Purpose: Success criterion 5 — cross-surface parity is a release-blocking project rule (a single
desktop build is host for local sessions and guest for remote/web-share sessions). It cannot be
waived.
Output: `onResize` on RelayClient, `computeGuestScale` helper, isGuest-gated TerminalPanel, desktop
CSS, and vitest coverage proving guest honor+cap and host-path invariance.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/157-terminal-screen-share-semantics-issue-109/157-RESEARCH.md
@frontend/src/lib/relayClient.ts
@frontend/src/components/TerminalPanel.tsx
@frontend/src/style.css
</context>

<artifacts_this_phase_produces>
- `RelayClientCallbacks.onResize?: (cols, rows) => void` and its dispatch wiring in `relayClient.ts`.
- `frontend/src/lib/terminalScale.ts`: `computeGuestScale(containerW, containerH, gridW, gridH): number` (capped at 1.0).
- `TerminalPanel` `isGuest` gating + guest-path onResize/recomputeScale + container ResizeObserver-for-scale.
- New test files: `frontend/src/lib/terminalScale.test.ts`, `frontend/src/components/__tests__/TerminalPanel.scale.test.tsx`; extended `frontend/src/lib/relayClient.test.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add onResize callback to RelayClient + shared computeGuestScale helper</name>
  <files>frontend/src/lib/relayClient.ts, frontend/src/lib/terminalScale.ts, frontend/src/lib/terminalScale.test.ts, frontend/src/lib/relayClient.test.ts</files>
  <read_first>
frontend/src/lib/relayClient.ts:3-5 (MSG_RESIZE/MSG_RESIZE2 consts),
frontend/src/lib/relayClient.ts:140-147 (0x02 decode to {type:'resize',cols,rows}),
frontend/src/lib/relayClient.ts:193-201 (RelayClientCallbacks),
frontend/src/lib/relayClient.ts:259 (the "informational" drop site),
frontend/src/lib/relayClient.test.ts (existing test patterns),
RESEARCH Change-Layer 6 step 1.
  </read_first>
  <behavior>
    - computeGuestScale(800,600,400,300) → 1 (would-be 2.0 capped to 1.0; never upscale)
    - computeGuestScale(400,300,800,600) → 0.5 (min(0.5,0.5))
    - computeGuestScale(400,600,800,300) → 0.5 (min(0.5,2.0) = 0.5; width-bound)
    - computeGuestScale with zero gridW or gridH → returns 1 (safe no-op, no divide-by-zero)
    - RelayClient: a 0x02 frame in the message dispatch invokes callbacks.onResize?.(cols,rows) with decoded big-endian values
  </behavior>
  <action>
(1) Add `onResize?: (cols: number, rows: number) => void` to `RelayClientCallbacks` (relayClient.ts:193).
Keep it OPTIONAL (consistent with the other optional callbacks; TerminalPanel host path passes none).
(2) At the drop site (relayClient.ts:259, the `case 'resize':`), replace the drop with
`this.callbacks.onResize?.(frame.cols, frame.rows); break`. The frame is already parsed — no new
decode. Remove/replace the stale "informational" comment with plain host-authority intent prose.
(3) Create `frontend/src/lib/terminalScale.ts` exporting pure
`computeGuestScale(containerW, containerH, gridW, gridH): number` — if gridW<=0 || gridH<=0 return 1;
`s = Math.min(containerW/gridW, containerH/gridH)`; return `s > 1 ? 1 : s`.
(4) Write `terminalScale.test.ts` covering the <behavior> cases. Extend `relayClient.test.ts` with a
test that feeds a 5-byte 0x02 frame and asserts `onResize` fires with the decoded cols/rows.
Run prettier/eslint.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- --run relayClient terminalScale</automated>
  </verify>
  <acceptance_criteria>
`computeGuestScale` passes all behavior cases including the 1.0 cap and zero-grid guard. The relayClient
test proves a 0x02 frame invokes `onResize` with correct decoded values. `onResize` is optional and
backward-compatible (existing callers unaffected).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: isGuest-gated honor + scale in TerminalPanel + desktop CSS</name>
  <files>frontend/src/components/TerminalPanel.tsx, frontend/src/style.css, frontend/src/components/__tests__/TerminalPanel.scale.test.tsx</files>
  <read_first>
frontend/src/components/TerminalPanel.tsx:30-53 (fitTerminal — host authority, keep for host),
frontend/src/components/TerminalPanel.tsx:66-70 (remote/wsURL props),
frontend/src/components/TerminalPanel.tsx:295,305-307 (onOpen/term.onResize → sendResize — host only),
frontend/src/components/TerminalPanel.tsx:700 (ResizeObserver → fitTerminal),
frontend/src/lib/terminalScale.ts (computeGuestScale, from Task 1),
frontend/src/style.css:45-57 (.xterm-viewport / fitTerminal padding comment),
RESEARCH Change-Layer 6 steps 2-3 + Pitfalls 5 & 7.
  </read_first>
  <behavior>
    - Guest (remote=true OR wsURL set): a RelayClient onResize(cols,rows) callback calls term.resize(cols,rows) then applies a transform via computeGuestScale; never calls client.sendResize.
    - Guest: container ResizeObserver triggers recomputeScale only (NOT fitTerminal).
    - Host (no remote, no wsURL): fitTerminal + sendResize on open/onResize preserved; no 0x02 honor; no transform applied.
    - Scale applied to term.element.style.transform never exceeds scale(1).
  </behavior>
  <action>
Per RESEARCH Change-Layer 6:
(1) Compute `const isGuest = remote || !!wsURL` in `TerminalPanel`.
(2) Wire a `recomputeScale()` that reads cell metrics from `term._core._renderService.dimensions.css.cell`
(same source as `fitTerminal`), computes `gridW = term.cols*cellW`, `gridH = term.rows*cellH`, reads the
container's clientWidth/clientHeight, calls `computeGuestScale(...)`, and sets
`term.element.style.transform = 'scale(' + s + ')'` (transform-origin from CSS, below). Call
`term.resize` BEFORE `recomputeScale` on a 0x02 (Pitfall 5).
(3) When `isGuest`: pass `onResize: (cols, rows) => { term.resize(cols, rows); recomputeScale() }` into
the RelayClient callbacks; attach the existing container ResizeObserver (TerminalPanel.tsx:700) to call
`recomputeScale()` INSTEAD of fitTerminal; do NOT call `client.sendResize` from onOpen (`:295`) or
`term.onResize` (`:305-307`). Guests never drive resize (D-03 invariant).
(4) When NOT guest (host): leave today's fitTerminal + sendResize + ResizeObserver→fitTerminal behavior
entirely unchanged; do not pass onResize.
(5) CSS: in `frontend/src/style.css`, add a guest scale rule — `.xterm { transform-origin: top left; }`
(scoped so it does not disturb the host; if a guest-only container class is cleaner, add it and apply
on the guest branch). The scale value is applied imperatively from JS; do NOT hardcode a transform.
Write `TerminalPanel.scale.test.tsx` covering the <behavior>: mount as guest (remote/ wsURL) and assert
onResize → term.resize + a capped transform; mount as host and assert sendResize still fires and no
transform/0x02-honor occurs. Reuse existing TerminalPanel test mocks (xterm mock pattern in the sibling
test files). Run prettier/eslint + `tsc`.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- --run TerminalPanel.scale && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
Guest mount honors 0x02 (term.resize) + applies a capped (<=1) transform + never sends resize; host
mount preserves fitTerminal/sendResize and applies no transform. `tsc --noEmit` clean (wails-dev build
parity — vitest alone is not proof the app builds). Tests pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer server → desktop guest viewer | A 0x02 frame from a remote peer / web-share server crosses into the desktop xterm resize path |
| desktop host vs guest mode | The same component is the trusted host for local sessions and a guest for remote/web-share sessions; gating must not leak guest behavior into the host path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-157-03 | Tampering | desktop 0x02 honor (`relayClient.ts`/`TerminalPanel.tsx`) | medium | mitigate | relayClient already guards `data.length < 5` (relayClient.ts:141); term.resize on guest only; computeGuestScale guards zero grid. A crafted zero-dim frame cannot break the renderer. |
| T-157-07 | Elevation (mode leak) | isGuest gate | medium | mitigate | All guest behavior gated on `remote || wsURL`; the host path is never given onResize and never scales — asserted by the host-path branch of TerminalPanel.scale.test.tsx. |
</threat_model>

<verification>
- `cd frontend && pnpm test -- --run relayClient terminalScale TerminalPanel.scale` green.
- `cd frontend && npx tsc --noEmit` clean (build parity gate per project rule: vitest tolerates TS errors `tsc && vite build` rejects).
- Host-path invariance asserted (no transform, sendResize preserved).
</verification>

<success_criteria>
ROADMAP criterion 5 (cross-surface parity, release-blocking) satisfied: desktop guest path matches the
web guest; host path pixel-pristine.
</success_criteria>

<output>
Create `.planning/phases/157-terminal-screen-share-semantics-issue-109/157-04-SUMMARY.md` when done.
List the new test files (terminalScale.test.ts, TerminalPanel.scale.test.tsx) + extended relayClient.test.ts
for the Plan 05 traceability map.
</output>
