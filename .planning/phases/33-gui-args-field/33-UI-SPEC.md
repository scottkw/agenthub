---
phase: 33
slug: gui-args-field
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-26
---

# Phase 33 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — hand-written CSS modules in `frontend/src/style.css` |
| Icon library | none — unicode characters used inline (e.g. `&times;`) |
| Font | "Cascadia Code", "MesloLGS NF", "Fira Code", monospace (body); inherit for form inputs |

Source: `frontend/src/style.css` — established across Phases 1–32.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Tight button padding (e.g. `padding: 2px 6px` close button) |
| sm | 8px | Element gaps, section margin-bottom between label and input (`margin-bottom: 8px`), gap within `.folder-row` / `.args-row` |
| md | 16px | Body padding top, section vertical separation (`margin-bottom: 24px` uses lg), horizontal input padding |
| lg | 24px | Section spacing within modal body (`new-session-modal__section` margin-bottom) |
| xl | 32px | Not applicable for this phase |
| 2xl | 48px | Not applicable for this phase |
| 3xl | 64px | Not applicable for this phase |

Exceptions: Button hit-target padding uses asymmetric values (`4px 8px` for `.browse-btn`, `6px 16px` for footer actions) — these are established modal patterns, not deviations.

Source: `frontend/src/style.css` lines 596–675.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body / input text | 13px | 400 (regular) | 1.5 |
| Section label (uppercase) | 13px | 600 (semibold) | 1.2 |
| Modal heading | 16px | 600 (semibold) | 1.2 |
| Small / meta (button labels, status) | 11px | 400 (regular) | 1.0 |

Notes:
- Section labels use `text-transform: uppercase` and `letter-spacing: 0.08em` — maintain this on the "Extra Arguments" label.
- Input placeholder text is styled at `color: #414868` (muted) — this is a color contract item, not a weight change.
- Only 2 weights in use: 400 (regular) and 600 (semibold). No intermediate weights.

Source: `frontend/src/style.css` lines 529–565.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#1a1b26` | App background, terminal background, table cell separators |
| Secondary (30%) | `#1e2030` | Modal surface, hover state backgrounds, selected agent button |
| Accent (10%) | `#7aa2f7` | Active tab underline, focus border on inputs, selected folder path text, primary CTA button background, active URL links |
| Destructive | `#f7768e` | Close button hover (`×`), error text in settings, tab close hover |

Supporting palette (from existing code — not new additions):
- `#16161e` — input/select background, tab bar background, code block background
- `#292e42` — border color for all inputs, buttons, separators
- `#3b4261` — hover border on cancel button, close button hover background
- `#565f89` — muted text (inactive tabs, section labels, placeholders)
- `#a9b1d6` — secondary text (button labels, body copy)
- `#c0caf5` — primary text (modal headings, active tab, input values)
- `#414868` — placeholder text in inputs
- `#9ece6a` — success/online status
- `#89b4fa` — primary CTA hover state

Accent reserved for: active tab indicator underline, focused input border, selected folder path color, "Create Session" button, active URL text in status bar. NOT for informational labels or inactive form elements.

Source: `frontend/src/style.css` — all hex values confirmed from existing declarations.

---

## Component Inventory

### New: Args Row (within NewSessionModal)

Location: below the "Working Directory" section, above the footer.

The modal's inherited focal hierarchy places the agent selector (top) as the primary decision point and "Create Session" (footer CTA) as the terminal action. The args field is a secondary, optional refinement that sits between these two anchors — it must not compete visually with either.

```
.new-session-modal__section                        (reuse existing class)
  .new-session-modal__section-label                (reuse: "EXTRA ARGUMENTS")
  .new-session-modal__args-row                     (new)
    .new-session-modal__args-input                 (new)
    .new-session-modal__args-clear                 (new, conditional render)
```

#### `.new-session-modal__args-row`
- `display: flex; gap: 8px; align-items: center`
- Mirrors `.new-session-modal__folder-row` pattern exactly

#### `.new-session-modal__args-input`
- `flex: 1`
- `background: #16161e` (input background token)
- `border: 1px solid #292e42` (border token — default)
- `border-radius: 4px`
- `color: #c0caf5` (primary text)
- `font-size: 13px; font-family: inherit`
- `padding: 8px 16px`
- `outline: none`
- `min-height: 32px` (matches folder-display height; `box-sizing: border-box` applies)
- Focus state: `border-color: #7aa2f7` (accent)
- Placeholder: `color: #414868` (muted placeholder)

#### `.new-session-modal__args-clear`
- Conditional render: only visible when `argsText !== ''`
- `padding: 4px 8px`
- `border: 1px solid #292e42`
- `border-radius: 4px`
- `font-size: 11px`
- `background: transparent`
- `color: #a9b1d6`
- `cursor: pointer; white-space: nowrap; font-family: inherit`
- Hover: `background: #1e2030; color: #c0caf5`
- Mirrors `.new-session-modal__browse-btn` pattern exactly

Source: `frontend/src/style.css` lines 596–636 (folder-row and browse-btn established pattern).

---

## Interaction Contract

### Args Field State Machine

```
[empty field, no stored args]
    → user types → [non-empty field] → Clear Args button appears
    → user clears → [empty field] → Clear Args button disappears

[agent selection] → load from localStorage key agenthub:args:{cliName}
    → found: pre-fill argsText with stored value
    → not found: argsText = ''

[form submit]
    → argsText non-empty: localStorage.setItem(key, argsText), pass parsed args[]
    → argsText empty: localStorage.removeItem(key), pass []

[Clear Args button click]
    → setArgsText('')
    → localStorage.removeItem(key)
    → Clear Args button disappears
```

### Focus Behavior
- Args input receives focus via standard tab order (after Browse button, before footer buttons).
- No custom focus management required.
- Enter key in args input does NOT submit the form (input is type="text", not in a `<form>` element — consistent with existing modal pattern).

### Disabled / Loading States
- Args input remains enabled during `creating` state (after Create Session click). Field is informational only after submit.
- No spinner or loading indicator on the args field itself.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Create Session" (existing — no change) |
| Section label | "EXTRA ARGUMENTS" (uppercase per `.new-session-modal__section-label` pattern) |
| Input placeholder | `e.g. --model claude-opus-4-5` |
| Clear button label | "Clear Args" |
| Clear button aria-label | "Clear arguments" |
| Empty state (no agents detected) | Not in scope for this phase — existing modal handles it |
| Error state | Not in scope — args field has no validation error state; ARGS-06 (shell quoting) is deferred |
| Destructive confirmation | No destructive action in this phase. Clear Args button removes localStorage entry but is non-destructive (re-enterable) and does not require confirmation. |

Notes on copy:
- "EXTRA ARGUMENTS" not "ADDITIONAL ARGUMENTS" — "extra" matches the CLI convention (`agenthub new claude -- --flag`) and the codebase's internal naming (`extraArgs`).
- Placeholder shows a real agent flag (`--model claude-opus-4-5`) rather than an abstract `<arg>` — this educates new users on expected format.
- "Clear Args" uses a verb + noun label, adding noun context so sighted users understand what is being cleared. Not "Reset" (which implies to default) or "Delete" (which implies destructive).

Source: REQUIREMENTS.md ARGS-02/04/05; RESEARCH.md architecture pattern section.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| Third-party | none | not applicable |

No new packages or registries are introduced in this phase. All changes are pure React/TypeScript additions to existing files.

---

## Accessibility Contract

- `<input type="text">` with explicit `<label>` linked via `className="new-session-modal__section-label"` (not `htmlFor` — existing modal uses visual label pattern, maintain consistency).
- Clear Args button has `aria-label="Clear arguments"` for screen readers.
- Focus ring: native `outline: none` is used throughout the codebase; focus state is communicated via `border-color: #7aa2f7` (accent). This is the established pattern — maintain it.
- Color contrast: `#c0caf5` text on `#16161e` background = 9.2:1 (exceeds WCAG AA 4.5:1). Placeholder `#414868` on `#16161e` = 2.5:1 (below AA — accepted, matches existing placeholder pattern in settings panel and folder display).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
