---
status: complete
phase: 105-deferred-v3-2-uat-re-run
source: [105-SUMMARY.md, 105-UAT-RUNBOOK.md]
started: 2026-05-17T00:00:00Z
updated: 2026-05-17T22:00:00Z
note: |
  All 7 UAT scenarios were executed during the v3.3 release-prep window on 2026-05-17.
  Discovered bugs were filed to GitHub (scottkw/agenthub) and triaged as non-blocking
  for v3.3 (deferred to v3.4 backlog per each issue body). Issues #54, #55, #56 are
  explicit Phase 105 UAT findings.
---

## Current Test

[testing complete]

## Tests

### 1. WebGL context-loss → DOM fallback (Desktop Chrome)
expected: |
  Open AgentHub (macOS desktop Wails build). Spawn a shell session. Open DevTools Console and run:
    document.querySelector('canvas').getContext('webgl').getExtension('WEBGL_lose_context').loseContext()
  Recovery banner appears at top of terminal, terminal switches to DOM renderer, content stays readable, banner auto-dismisses after 8 seconds.
result: issue
reported: "Functional DOM fallback works, terminal stays readable, but WebGLRecoveryBanner does not render."
severity: minor
github_issue: scottkw/agenthub#55
triage: deferred-to-v3.4 (not v3.3 release-blocking — pre-existing Phase 93 bug, functional fallback intact)

### 2. iPad software-rasterizer banner
expected: |
  On desktop: spawn shell session, enable web serving. On iPad Safari: open the Tailscale URL.
  Software-rasterizer banner appears at top of terminal page; iPad receives terminal output normally.
result: pass

### 3. 10K scrollback regex search performance
expected: |
  Spawn shell session, generate 10K lines, Cmd-F → regex → \d{4}. No frame >50ms; total <300ms; no dropped frames.
result: pass

### 4. Web-Links full chain (iPad + Tailscale)
expected: |
  Print fixtures (https://example.com, IDN, typosquat, mailto, OSC 8 mismatch). On iPad Safari, tap each.
  All 5 paths behave per UI-SPEC.
result: partial
reported: "Most link types behave correctly when activated, but iPad tap-on-link is captured by xterm-helper-textarea instead of firing the link click handler — so the user can't actually trigger them via touch."
severity: major
github_issue: noted in scottkw/agenthub#56 ("Related: UAT-04 also identified iPad tap-on-link being captured by xterm-helper-textarea instead of firing link click handler")
triage: deferred-to-v3.4 (iPad-touch polish cluster)

### 5. chafa visual fidelity (sixel)
expected: |
  chafa --format=sixel on desktop and web (Tailscale URL). Renderings visually equivalent.
result: issue
reported: "On web surface, OSC 10/11 (color query) and Device Attributes responses leak into shell stdin as user input after sixel render completes — garbles the next prompt. Desktop surface unaffected (parity gap)."
severity: major
github_issue: scottkw/agenthub#54
triage: deferred-to-v3.4 (pre-existing — not a v3.3 regression; v3.3 didn't touch sixel rendering)

### 6. Two-client mid-stream image join
expected: |
  Two clients on same session, second client refreshes mid-stream. Both clients render same final image.
result: pass

### 7. iPad 5-scenario runbook
expected: |
  iPad Safari over Tailscale: (1) attach+chafa, (2) scrollback, (3) zero-CDN, (4) zero-CSP-relaxation, (5) all-8-plugins-ON Progress.
result: partial
reported: "Scenarios 1, 3, 4, 5 pass. Scenario 2 (scrollback) FAILS — single-finger and two-finger drag both pan the page viewport instead of scrolling the xterm buffer. iPad has no keyboard shortcut equivalent."
severity: major
github_issue: scottkw/agenthub#56
triage: deferred-to-v3.4 (iPad-touch polish cluster; pre-existing since Phase 87/99 web-share, not a v3.3 regression)

## Summary

total: 7
passed: 3
issues: 2
partial: 2
pending: 0
skipped: 0
blocked: 0

## Gaps

<!-- All gaps tracked as GitHub issues — see scottkw/agenthub for full triage. -->

- truth: "WebGL recovery banner renders when WebGL context is lost"
  status: failed
  reason: "User reported: Functional DOM fallback works but WebGLRecoveryBanner never renders"
  severity: minor
  test: 1
  github_issue: scottkw/agenthub#55
  triage: deferred-to-v3.4
  artifacts:
    - path: "frontend/src/components/TerminalPanel.tsx:391-395"
      issue: "webglAddon.onContextLoss callback may not fire or may throw before notifying React"
  missing:
    - "Add console.log inside onContextLoss handler to confirm callback execution"
    - "Wrap webglAddon.dispose() in try/catch; move React notify call first"

- truth: "iPad tap on terminal link fires link click handler"
  status: failed
  reason: "User reported: iPad tap-on-link captured by xterm-helper-textarea instead of firing link click handler"
  severity: major
  test: 4
  github_issue: scottkw/agenthub#56 (noted as related)
  triage: deferred-to-v3.4 (iPad-touch polish cluster)
  artifacts:
    - path: "frontend/src/components/TerminalPanel.tsx (touch handling)"
      issue: "No iPad-specific tap handling on link elements"
  missing:
    - "Wire touch events on link-rendered cells to LinkConfirmPopover handler"

- truth: "Web-served terminal consumes OSC color-query and DA responses correctly"
  status: failed
  reason: "User reported: OSC 10/11 and DA responses leak into shell stdin on web surface; desktop unaffected"
  severity: major
  test: 5
  github_issue: scottkw/agenthub#54
  triage: deferred-to-v3.4 (pre-existing parity gap, not a v3.3 regression)
  artifacts:
    - path: "internal/webserver/ (session WebSocket path)"
      issue: "Web bridge may broadcast xterm.js OSC responses to display side instead of pty stdin"
  missing:
    - "Compare desktop attach (internal/daemon/engine.go) vs web attach (internal/webserver/) data flow"
    - "Ensure xterm.js OSC/DA responses route upstream to pty master, not back to display"

- truth: "iPad touch gestures scroll xterm scrollback buffer"
  status: failed
  reason: "User reported: single-finger and two-finger drag both pan page viewport instead of scrolling xterm buffer"
  severity: major
  test: 7
  github_issue: scottkw/agenthub#56
  triage: deferred-to-v3.4 (iPad-touch polish cluster)
  artifacts:
    - path: "frontend/src/components/TerminalPanel.tsx"
      issue: "No touch handling registered for scroll gestures"
  missing:
    - "Register touchstart/touchmove on terminal wrapper"
    - "Compute deltaY → term.scrollLines(Math.round(deltaY / lineHeight) * -1), OR use CSS touch-action: pan-y with synthetic wheel mapper"
