---
status: complete
phase: 105-deferred-v3-2-uat-re-run
source: [105-SUMMARY.md, 105-UAT-RUNBOOK.md]
started: 2026-05-17T00:00:00Z
updated: 2026-05-17T00:15:00Z
note: |
  UAT-01 and UAT-02 verified live on 2026-05-17. UAT-03 through UAT-07 confirmed
  by the user as already verified during later phases (107/108 shell UX work
  exercised the same code paths end-to-end).
---

## Current Test

[testing complete]

## Tests

### 1. WebGL context-loss → DOM fallback (Desktop Chrome)
expected: |
  Open AgentHub (macOS desktop Wails build). Spawn a shell session. Open DevTools Console and run:
    document.querySelector('canvas').getContext('webgl').getExtension('WEBGL_lose_context').loseContext()
  Recovery banner appears at top of terminal, terminal switches to DOM renderer, content stays readable, banner auto-dismisses after 8 seconds.
  Note: Production Wails build has DevTools disabled — use `wails dev` or web-share to a regular Chrome to access Console.
result: pass

### 2. iPad software-rasterizer banner
expected: |
  On desktop: spawn shell session, enable web serving (ShellWebShareBanner appears one-time → "Enable web sharing"). Copy Tailscale URL.
  On iPad Safari: open the Tailscale URL.
  Software-rasterizer banner appears at top of terminal page; iPad receives terminal output normally.
result: pass

### 3. 10K scrollback regex search performance
expected: |
  Spawn shell session. Generate 10K lines:
    for i in $(seq 1 10000); do echo "test line $i $RANDOM"; done
  Open DevTools → Performance → Record. Cmd-F to open find bar, enable regex toggle, type \d{4}. Stop recording.
  No frame >50ms during the search; total search completion <300ms; results render without dropped frames.
result: pass

### 4. Web-Links full chain (iPad + Tailscale)
expected: |
  Spawn shell session; enable web serving. Print these fixtures in the shell:
    echo "https://example.com"
    echo "https://пример.рф"
    echo "https://paypa1.com"
    echo "mailto:test@example.com"
    printf '\e]8;;https://evil.com\e\\Click here\e]8;;\e\\\n'
  On iPad: open Tailscale URL, navigate to session. Long-press / Cmd-click each link.
  Expected behaviors:
    - https://example.com → opens in new tab
    - https://пример.рф → LinkConfirmPopover shows Punycode form
    - https://paypa1.com → LinkConfirmPopover shows typosquat warning
    - mailto:test@example.com → mail app opens with composed message
    - OSC 8 mismatch → LinkConfirmPopover shows display-text vs href-mismatch warning
result: pass

### 5. chafa visual fidelity (sixel)
expected: |
  Spawn shell session on desktop. Run:
    chafa --format=sixel <some-image.png>
  (fall back to --format=symbols if sixel unavailable). Take screenshot of desktop rendering.
  Open the same session on web (Tailscale URL); compare renderings side-by-side.
  Desktop and web renderings are visually equivalent (minor anti-alias differences allowed).
  Note: iTerm2 IIP (OSC 1337) is explicitly out-of-scope per Phase 103 POLISH-05 — test sixel only.
result: pass

### 6. Two-client mid-stream image join
expected: |
  Spawn shell session; enable web serving. Open Tailscale URL on TWO browser windows (or one iPad + one desktop).
  In the shell, start a long sixel render or streaming command that emits sixel escapes over several seconds.
  Mid-stream, refresh the second client (or open it fresh).
  Second client renders the same final image as the first — no partial garbage, no missing rows. Both clients show identical output.
result: pass

### 7. iPad 5-scenario runbook
expected: |
  On physical iPad Safari over Tailscale, run all 5 scenarios:
    1. Attach + chafa: open session, run chafa on small image, verify sixel renders.
    2. Scrollback: generate 1000 lines, scroll up via iPad gestures, verify scrollback works.
    3. Zero-CDN: confirm no external CDN requests (DevTools or paired-desktop Network inspector).
    4. Zero-CSP-relaxation: confirm no CSP errors in console.
    5. All-8-plugins-ON Progress: enable every plugin via settings; verify Progress indicator shows correctly during plugin toggles.
  All 5 scenarios pass without regression.
result: pass

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
