---
phase: 169-tailscale-detection-fix
plan: 02
type: execute
wave: 2
depends_on: ["169-01"]
files_modified:
  - frontend/src/components/SettingsTab.tsx
  - frontend/src/App.tsx
  - frontend/src/components/__tests__/SettingsTab.tailscale-status.test.tsx
  - TESTING.md
autonomous: true
requirements: [FIX-05]
must_haves:
  truths:
    - "When TailscaleHealth.permissionLimited is true, the Settings Tailscale status block shows a DISTINCT state (not 'Not Connected' / 'Daemon Stopped') with actionable guidance: grant this account admin, or install the Homebrew tailscale build (D-05, SC3)"
    - "The permission-limited copy never implies Connected — it does not render the word 'Connected' and does not use the 'ok' status dot (verified in a vitest test, and colorblind-safe per project rule: text label, not color alone)"
    - "The SDK-connected path still renders 'Connected' (regression — the new branch is checked first but does not disturb the healthy path)"
    - "TESTING.md reflects the revert + new tests: the stale Phase 169-01 CLI-fallback manifest note and M-45 wording are replaced with honest-detection + macsys (Standalone signed-installer) specificity (REVIEW IN-02)"
  artifacts:
    - "frontend/src/components/SettingsTab.tsx: optional permissionLimited field on the tailscaleHealth prop interface + a distinct status label/class/description branch"
    - "frontend/src/App.tsx: optional permissionLimited on both tailscaleHealth type mirrors (useState shape + tailscale:health EventsOn handler)"
    - "frontend/src/components/__tests__/SettingsTab.tailscale-status.test.tsx (NEW): asserts the permission-limited render + no-false-Connected + connected-regression"
    - "TESTING.md: updated Suite Manifest note + Section 4 traceability rows + rewritten Category W / M-45"
  key_links:
    - "SettingsTab consumes the backend json:\"permissionLimited\" field (169-01) via the tailscaleHealth prop; permissionLimited is checked BEFORE connected/daemonUp in tailscaleStatusText and tailscaleStatusClass"
---

<objective>
Surface the honest permission-limited Tailscale state (produced by 169-01) in the Settings
status block with accurate, actionable guidance, and reconcile the regression suite (TESTING.md)
with the reverted-plus-rewritten backend.

Purpose: SC3 — a non-admin macsys user sees an accurate message ("Tailscale is running but its
status is unreadable on this account") plus a remedy (grant admin, or use the Homebrew build),
never a misleading "Not Connected" and never a false "Connected". Presentation-only; the
permission fact is decided server-side (169-01).

Output: The extended SettingsTab cascade + type mirrors, a dedicated vitest test, and a
TESTING.md that documents the new Go tests and the corrected M-45 manual gate.

Prohibitions:
- MUST NOT render "Connected" or the success ('ok') status dot in the permission-limited state.
- MUST NOT rely on color alone to convey the state (project standing rule: colorblind-safe —
  use the text label; verified at source level, not by eye).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/169-tailscale-detection-fix/169-CONTEXT.md
@.planning/phases/169-tailscale-detection-fix/169-RESEARCH.md
@.planning/phases/169-tailscale-detection-fix/169-01-SUMMARY.md

# UI surface to extend (status cascade)
@frontend/src/components/SettingsTab.tsx
@frontend/src/App.tsx

# An existing SettingsTab vitest for the render harness + wails-mock idiom to copy
@frontend/src/components/__tests__/SettingsTab.notify-permission-hint.test.tsx

# Regression-suite home (standing rule — Section 2 manifest, Section 4 traceability, Section 5 Category W)
@TESTING.md
</context>

<interface_context>
Backend field (from 169-01, already serialized): `TailscaleHealth.PermissionLimited bool` with
json tag `permissionLimited`. The frontend is a pure consumer.

Type mirrors that must gain the optional field (mirror the existing `acceptDns?: boolean`
optional precedent — do NOT make it required, to avoid churning generated wailsjs models):
- `frontend/src/components/SettingsTab.tsx` — the inline `tailscaleHealth` prop interface (~lines 51-60).
- `frontend/src/App.tsx` — the `useState` shape (~lines 161-171) AND the `tailscale:health`
  `EventsOn` handler param type (~lines 626-636).

Status cascade helpers to extend (SettingsTab.tsx ~lines 295-309): `tailscaleStatusClass` and
`tailscaleStatusText` — insert the `permissionLimited` branch AFTER `connected` but BEFORE
`daemonUp`, so the healthy path is unchanged and the new state pre-empts "Not Connected".
Description block at ~lines 735-751 gets a `permissionLimited` branch with the guidance copy.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SettingsTab permission-limited status + guidance, with vitest coverage (D-05, SC3)</name>
  <files>frontend/src/components/SettingsTab.tsx, frontend/src/App.tsx, frontend/src/components/__tests__/SettingsTab.tailscale-status.test.tsx</files>
  <behavior>
    - Rendering SettingsTab with tailscaleHealth.permissionLimited=true (connected=false):
      the status text reads a distinct label (e.g. "Permission Limited"), NOT "Connected" and
      NOT "Not Connected"; the description contains the grant-admin / Homebrew-build guidance;
      the status dot does NOT carry the 'ok' modifier class.
    - Rendering with connected=true (permissionLimited absent/false): the status text still
      reads "Connected" (regression — healthy path intact).
    - The word "Connected" is not present anywhere in the permission-limited render output
      (guards against a false-positive impression — the core SC).
  </behavior>
  <action>
In `frontend/src/App.tsx`: add `permissionLimited?: boolean` to BOTH tailscaleHealth type
mirrors — the `useState<{...}>` shape and the `tailscale:health` `EventsOn` handler param —
placed alongside the existing `acceptDns?: boolean` optional (same optional style, so no
generated-model churn and tsc stays green).

In `frontend/src/components/SettingsTab.tsx`:
- Add `permissionLimited?: boolean` to the inline `tailscaleHealth` prop interface.
- In `tailscaleStatusClass`: after the `connected` check, add `if (h.permissionLimited) return 'warn'`
  (a caution state, never 'ok'), then the existing `daemonUp`/`binaryFound` checks.
- In `tailscaleStatusText`: after the `connected` check, add
  `if (h.permissionLimited) return 'Permission Limited'` (or a clearer short label of your
  choosing that does NOT contain the word "Connected"), then the existing branches.
- In the description `<p>` block, add a branch (checked before the `daemonUp` branch): when
  `permissionLimited`, render accurate guidance, e.g. "Tailscale is running, but AgentHub can't
  read its status on this macOS account. Grant this account admin access, or install the
  Homebrew `tailscale` build (which uses a different socket path)." Keep it factual — do not
  imply Connected.
- (Optional, planner's discretion) In the "Show diagnostics" details, you may add a row noting
  the daemon is running but status is unreadable; not required for SC3.

Create `frontend/src/components/__tests__/SettingsTab.tailscale-status.test.tsx`, copying the
render harness + wails-mock idiom from `SettingsTab.notify-permission-hint.test.tsx` (mock the
same App bindings; `GetTailscaleStatus` unused here — the health object is passed via the
`tailscaleHealth` prop). Assert the three Behavior cases against the rendered DOM text/classes.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run SettingsTab.tailscale-status && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
The new vitest file passes all three cases; `tsc --noEmit` is clean (optional field mirrors keep
the app compiling); the permission-limited render shows the distinct label + guidance and never
the word "Connected" or the 'ok' dot; the connected path still renders "Connected".
  </done>
</task>

<task type="auto">
  <name>Task 2: Reconcile TESTING.md with the revert + new tests (Suite Manifest, traceability, Category W / M-45) — standing rule</name>
  <files>TESTING.md</files>
  <action>
Follow the repo standing rule (CLAUDE.md / TESTING.md Section 6). All test changes this phase:
169-01 removed 3 Go tests and added 2 (same file `internal/webserver/tailscale_test.go` — Go
file count unchanged at 374); this plan added 1 new vitest FILE.

Edits:
- Section 2 Suite Manifest: bump the vitest file count from 141 to 142 and the Total from 526 to
  527 (the new `SettingsTab.tailscale-status.test.tsx`). The Go count stays 374 (tailscale_test.go
  already existed).
- Replace the stale Phase 169-01 manifest Note (the paragraph beginning "Note: Phase 169-01
  (FIX-05, #120, v4.2 — Tailscale CLI status fallback...")" with an accurate note: the CLI
  `status` fallback was INVALIDATED (CR-01) and REVERTED; the honest permission-aware file-probe
  replaced it. Summarize the current tests: `TestCheckHealth_PermissionLimited` (SDK error +
  injected macsys-blocked probe means PermissionLimited=true, Connected stays false) and
  `TestCheckHealth_DaemonDown_NotPermissionLimited` (probe false means today's daemon-down state);
  note the 3 `TestCheckHealth_CLIFallback_*` tests were removed; and the new frontend
  `SettingsTab.tailscale-status.test.tsx` (permission-limited copy + no-false-Connected +
  connected regression).
- Section 4 traceability: update the FIX-05 row for `internal/webserver/tailscale_test.go` to the
  new test names (drop the CLIFallback names); ADD a FIX-05 row for
  `frontend/src/components/__tests__/SettingsTab.tailscale-status.test.tsx`. Path column must be a
  repo-relative file path only (no test names in the path column).
- Section 5 Category W: REWRITE M-45. Remove all "CLI `tailscale status --json` fallback" and
  "reports Connected" framing. New wording: on a non-admin macOS account using the **macsys
  (Standalone signed-installer / direct `tailscale.com` download)** distribution — NOT App Store,
  NOT Homebrew (REVIEW IN-02) — where `sameuserproof` is `0640 root:admin` and the SDK read fails,
  confirm Settings → Tailscale shows the DISTINCT permission-limited state with grant-admin /
  Homebrew-build guidance, and does NOT show "Connected". Keep the admin-account negative control
  (SC2). Keep the "why not automatable" rationale (requires a genuine non-admin macsys account;
  unit tests via injected fakes prove the branch logic but cannot exercise real EACCES routing).

After editing, run `bash tests/check-traceability-paths.sh` (repo standing rule) — it must pass.
  </action>
  <verify>
    <automated>bash tests/check-traceability-paths.sh && grep -q 'SettingsTab.tailscale-status.test.tsx' TESTING.md && test $(grep -c 'CLI .*status.*--json.*fallback' TESTING.md) -eq 0 && grep -q '527' TESTING.md</automated>
  </verify>
  <done>
`bash tests/check-traceability-paths.sh` passes; the Suite Manifest reads 142 vitest / 527 Total;
the stale CLI-fallback manifest note and M-45 wording are gone, replaced with honest-detection +
macsys-Standalone specificity; a traceability row for the new vitest file exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon → frontend (Wails RPC / event) | Serialized `TailscaleHealth.permissionLimited` crosses from the trusted backend to the presentation layer; the frontend only renders it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-169-05 | Spoofing (misleading UI) | SettingsTab status cascade | medium | mitigate | permissionLimited is checked BEFORE connected/daemonUp in status text/class so the state can never render as "Connected"; a vitest asserts the word "Connected" is absent from the permission-limited output and the 'ok' dot is not applied |
| T-169-06 | Information disclosure | guidance copy | low | accept | Copy names only the remedy (grant admin / Homebrew build); it exposes no token, path secret, or node identity beyond what the existing status block already shows |

No package installs in this plan — supply-chain threat is not applicable.
</threat_model>

<verification>
- `cd frontend && pnpm test -- --run SettingsTab.tailscale-status` green.
- `cd frontend && pnpm exec tsc --noEmit` clean (per memory: run tsc, not just vitest).
- `bash tests/check-traceability-paths.sh` passes.
- Phase gate before /gsd-verify-work: full backend suite green (`go test ./... -count=1`),
  `cd frontend && pnpm exec tsc --noEmit && pnpm exec vite build` clean, then M-45 live acceptance
  on a real non-admin macsys account (human_judgment).
</verification>

<success_criteria>
- SC3 satisfied: distinct, accurate, actionable permission-limited state in Settings; never a
  false Connected; colorblind-safe (text label).
- Regression suite reconciled: TESTING.md manifest counts, traceability, and M-45 all reflect the
  reverted-plus-honest implementation; traceability-path check passes.
</success_criteria>

<output>
Create `.planning/phases/169-tailscale-detection-fix/169-02-SUMMARY.md` when done.
Carry M-45 forward as a deferred `human_judgment: true` live-acceptance gate (non-admin macsys).
</output>
