---
phase: 164-web-share-chat-layout-polish-message-header-overflow-fix-res
plan: 02
type: execute
wave: 2
depends_on: ["164-01"]
files_modified:
  - frontend/src/components/Hub/ChatPanel.tsx
  - frontend/src/components/Hub/ChatPanel.test.tsx
  - frontend/src/style.css
  - frontend/src/components/Hub/chatToggleOverlap.test.ts
  - TESTING.md
autonomous: true
requirements: [CHAT-LAYOUT-02]

must_haves:
  truths:
    - "A left-edge drag handle on the chat drawer lets the user resize the drawer width on all three surfaces (GUI session tab, Hub interactive modal, web-share guest) from one shared implementation (CONTEXT Decision 3, Decision 5)."
    - "The chosen width persists to localStorage and is restored on reload (CONTEXT Decision 4)."
    - "Width is clamped both ways to a bounded min/max within ~280–640px — it cannot be dragged narrower than the min or wider than the max, including a tampered localStorage value (CONTEXT Decision 4)."
    - "Resizing the drawer does NOT resize the terminal or trigger a PTY sendResize — overlay mode (D-02, pre-existing) is preserved."
    - "The chat toggle button stays clear of the resized drawer because its offset tracks the live drawer width (CHAT-FIX-01 offset no longer hard-coded to 360px)."
  artifacts:
    - "frontend/src/components/Hub/ChatPanel.tsx — width state, drag handle, localStorage load/save, clamp helper, CSS-custom-property application"
    - "frontend/src/style.css — --chat-panel-width consumed by .chat-panel, .hub-modal__body--interactive .chat-panel, and the .chat-panel--open ~ .hub-modal__chat-toggle offset"
    - "frontend/src/components/Hub/ChatPanel.test.tsx — clamp + persistence + handle coverage"
    - "frontend/src/components/Hub/chatToggleOverlap.test.ts — updated to the new width-tracking offset"
  key_links:
    - "A single --chat-panel-width custom property is the single source of truth consumed by both .chat-panel width rules AND the toggle offset calc."
    - "Width state init = clampChatWidth(localStorage value || default) → applied as --chat-panel-width → toggle offset = calc(var(--chat-panel-width) + 12px)."
    - "Resize updates only the CSS custom property / drawer width; it never calls sendResize and never changes the terminal grid (D-02)."
---

<objective>
Implement CHAT-LAYOUT-02: make the chat drawer resizable width-wise. Add a left-edge drag handle, persist the chosen width to localStorage, clamp it within a bounded min/max (~280–640px), and apply it through a single `--chat-panel-width` CSS custom property so the GUI session tab, Hub interactive modal, AND web-share guest all get the resize affordance from one shared `ChatPanel` + `style.css` change (CONTEXT Decisions 3/4/5).

Purpose: Replace the fixed 360px drawer with a user-adjustable, persisted width on every surface, while preserving D-02 overlay mode (no terminal resize / no PTY sendResize) and keeping the chat toggle clear of the resized drawer.
Output: Updated `ChatPanel.tsx`, `style.css`, updated CSS source-gate test, extended vitest coverage, TESTING.md traceability.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-web-share-chat-layout-polish-message-header-overflow-fix-res/164-CONTEXT.md

# Files this plan modifies (read before editing)
@frontend/src/components/Hub/ChatPanel.tsx
@frontend/src/components/Hub/ChatPanel.test.tsx
@frontend/src/style.css
@frontend/src/components/Hub/chatToggleOverlap.test.ts

# Reference: the three sibling surfaces that render ChatPanel + the toggle (DO NOT modify — confirms the toggle is a sibling of .chat-panel under a shared parent, so a :root custom property reaches both)
@frontend/src/components/Hub/WebShareSessionView.tsx
@frontend/src/components/Hub/TerminalChatHost.tsx
@frontend/src/components/Hub/HubInteractiveModal.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (NEW symbols)

- **Exported pure helper** in `ChatPanel.tsx`: `clampChatWidth(px: number): number` — clamps to the min/max bounds; unit-testable like the existing `getRowStyle` / `validateAlias` helpers. Applied to BOTH the live drag value and the value read from localStorage.
- **Width bound constants** in `ChatPanel.tsx`: `CHAT_WIDTH_MIN = 280`, `CHAT_WIDTH_MAX = 640`, `CHAT_WIDTH_DEFAULT = 360` (exact values at planner discretion within the ~280–640px envelope; both bounds finite).
- **localStorage key**: `agenthub.chatPanelWidth` (stores the integer px width).
- **CSS custom property**: `--chat-panel-width` (default `360px`) — set at runtime by ChatPanel; consumed by `.chat-panel`, `.hub-modal__body--interactive .chat-panel`, and the toggle offset calc.
- **New DOM element + class**: `.chat-panel__resize-handle` (left-edge drag handle inside the drawer).
- No new test files (existing `ChatPanel.test.tsx` and `chatToggleOverlap.test.ts` are extended/updated).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Width state, clamp helper, and localStorage persistence</name>
  <files>frontend/src/components/Hub/ChatPanel.tsx, frontend/src/components/Hub/ChatPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/ChatPanel.tsx lines ~196-249 (existing pure exported helpers validateAlias/getRowStyle — mirror this exported-helper style) and lines ~301-363 (component state + refs region where width state belongs).
    - frontend/src/components/Hub/ChatPanel.test.tsx lines ~62-79 (vi.stubGlobal('fetch') pattern; add a localStorage stub the same way) and the exported-helper import block.
  </read_first>
  <behavior>
    - clampChatWidth(100) returns CHAT_WIDTH_MIN (clamps below-min up to min).
    - clampChatWidth(9999) returns CHAT_WIDTH_MAX (clamps above-max down to max).
    - clampChatWidth(420) returns 420 (in-range passthrough; coerced to integer).
    - clampChatWidth(NaN) returns CHAT_WIDTH_DEFAULT (non-finite input falls back to default, never NaN).
    - On mount with a valid localStorage value (e.g. '500'): the drawer initializes to 500 (restored, D-04).
    - On mount with a tampered/out-of-range localStorage value (e.g. '5000'): the drawer initializes to CHAT_WIDTH_MAX (clamp applied on read — D-04 + threat T-164-11).
    - On mount with no/invalid localStorage value: the drawer initializes to CHAT_WIDTH_DEFAULT.
  </behavior>
  <action>
    Per CONTEXT Decision 4: add exported `clampChatWidth(px: number)` plus `CHAT_WIDTH_MIN`/`CHAT_WIDTH_MAX`/`CHAT_WIDTH_DEFAULT` constants to ChatPanel.tsx (chosen bounds 280/640/360 at your discretion within the ~280–640px envelope; both bounds must be finite). clampChatWidth must coerce to an integer and fall back to the default for non-finite input. Add `width` state initialized from `clampChatWidth(Number(localStorage.getItem('agenthub.chatPanelWidth')))` (wrap localStorage access defensively so a throwing/absent localStorage degrades to the default — never crash the panel). On every committed width change, write the clamped integer back to `localStorage.setItem('agenthub.chatPanelWidth', …)`. Do not yet wire the drag handle or CSS in this task — only the helper, constants, state, and persistence. Extend ChatPanel.test.tsx: stub localStorage (mirror the vi.stubGlobal fetch pattern); add clampChatWidth unit tests for below-min / above-max / in-range / NaN; add a component mount test asserting an out-of-range stored value is clamped on restore.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/Hub/ChatPanel.test.tsx</automated>
  </verify>
  <done>clampChatWidth is exported and clamps both directions + NaN→default; width state restores from localStorage clamped; persistence round-trips; ChatPanel.test.tsx passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Left-edge drag handle + single-source-of-truth CSS custom property</name>
  <files>frontend/src/components/Hub/ChatPanel.tsx, frontend/src/style.css, frontend/src/components/Hub/ChatPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/ChatPanel.tsx lines ~765-772 (the drawer root `<div className="chat-panel…">` where the handle and the inline `--chat-panel-width` style belong) and the existing pointer-capture pattern at handleInjectPointerDown (~665-689) as the analog for pointer-drag handlers.
    - frontend/src/style.css lines ~6007-6061 (.hub-modal__body--interactive .chat-panel width:360px + .chat-panel--open ~ .hub-modal__chat-toggle right:372px) and lines ~6673-6695 (.chat-panel base width:360px).
    - frontend/src/components/Hub/chatToggleOverlap.test.ts (the source-gate that currently asserts `right: 372px` — it WILL break when the offset becomes a calc and must be updated in Task 3).
  </read_first>
  <behavior>
    - A `.chat-panel__resize-handle` element renders at the left edge of the drawer (test: it is present in the rendered DOM).
    - Pointer-drag on the handle updates the drawer width; the live width is applied via the `--chat-panel-width` CSS custom property (test: dragging within bounds updates the applied width; dragging past a bound clamps to that bound via clampChatWidth).
    - Resizing never calls the RelayClient: the mocked sendChat/sendSessionInject (and there is NO sendResize on the chat client) are never invoked by a resize gesture — D-02 overlay is preserved (test: no relay send call fires during a resize drag).
    - The applied width survives as the persisted value (delegates to Task 1 persistence).
  </behavior>
  <action>
    Per CONTEXT Decision 3 + Decision 5: add a left-edge drag handle (`.chat-panel__resize-handle`) inside the `.chat-panel` root. Implement the drag with pointer events (mirror the setPointerCapture pattern already used by handleInjectPointerDown) — at your discretion vs CSS `resize`, but it must produce a clamped width via clampChatWidth and must NOT trigger any layout that resizes the terminal. Apply the live width through a single `--chat-panel-width` CSS custom property: set it at runtime from ChatPanel (e.g. on `document.documentElement` / `:root` so it also reaches the sibling toggle button, which is rendered as a sibling of `.chat-panel` — not a child — in WebShareSessionView/TerminalChatHost/HubInteractiveModal). In style.css, change the two `width: 360px` rules (`.chat-panel` base ~line 6681 and `.hub-modal__body--interactive .chat-panel` ~line 6014) to `width: var(--chat-panel-width, 360px)`, and change the toggle offset `.chat-panel--open ~ .hub-modal__chat-toggle { right: 372px }` (~line 6060) to track the live width, e.g. `right: calc(var(--chat-panel-width, 360px) + 12px)`. Add minimal `.chat-panel__resize-handle` styling (left-edge, full-height, col-resize cursor, above the drawer content). CRITICAL (D-02): the resize path must only change drawer width / the custom property — it must never call `sendResize` and never alter the terminal grid. Extend ChatPanel.test.tsx: assert the resize handle renders; simulate a pointer drag and assert the applied width clamps at both bounds; assert no relay send method is called during a resize gesture.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/Hub/ChatPanel.test.tsx</automated>
  </verify>
  <done>Resize handle renders; pointer drag updates a clamped width applied via --chat-panel-width; the two width CSS rules and the toggle offset all consume --chat-panel-width; no sendResize/relay send fires on resize; ChatPanel.test.tsx passes.</done>
</task>

<task type="auto">
  <name>Task 3: Update the toggle-offset source-gate + register tests in TESTING.md</name>
  <files>frontend/src/components/Hub/chatToggleOverlap.test.ts, TESTING.md</files>
  <read_first>
    - frontend/src/components/Hub/chatToggleOverlap.test.ts (test (b) asserts `right: 372px` inside the relocation block — this MUST be updated to the new width-tracking offset; test (a) selector presence and test (c) base `right:12px` stay valid).
    - TESTING.md Section 2 (Suite Manifest `> Note:` pattern), Section 4 (Traceability rows — CHAT-FIX-01 / CHAT-* row format), Section 6 (Standing Convention).
  </read_first>
  <action>
    (1) Update chatToggleOverlap.test.ts test (b): the relocation rule no longer hard-codes `right: 372px` — it now tracks the live drawer width. Replace the `/right\s*:\s*372px/` assertion with one that matches the new width-tracking offset (assert the rule body references `--chat-panel-width`, e.g. matches `right` set to a `calc(... var(--chat-panel-width ...) ... )`). Keep tests (a) selector-present and (c) base `right:12px` unchanged. (2) Per the standing regression convention, add a Suite Manifest `> Note:` (Section 2) for Phase 164-02 — no new test files; EXTENDED `frontend/src/components/Hub/ChatPanel.test.tsx` (clampChatWidth + width persistence/restore + resize-handle drag + D-02 no-sendResize) and UPDATED `frontend/src/components/Hub/chatToggleOverlap.test.ts` (offset now tracks --chat-panel-width); update the running test counts in the prior note's style. (3) Add Traceability rows (Section 4) for CHAT-LAYOUT-02 mapping `frontend/src/components/Hub/ChatPanel.test.tsx` and `frontend/src/components/Hub/chatToggleOverlap.test.ts` (path column = repo-relative file path only; test names in the Notes column). Run the traceability path check.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/Hub/chatToggleOverlap.test.ts && cd .. && bash tests/check-traceability-paths.sh</automated>
  </verify>
  <done>chatToggleOverlap.test.ts asserts the offset tracks --chat-panel-width (not 372px) and passes; base right:12px gate preserved; TESTING.md has the Phase 164-02 Suite Manifest note and CHAT-LAYOUT-02 traceability rows; `bash tests/check-traceability-paths.sh` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → client state | The persisted drawer width (integer px) is read back from localStorage, which the user can edit. |
| width value → CSS custom property | A numeric width is written into the `--chat-panel-width` CSS custom property applied to the DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164-11 | Tampering | localStorage width value | low | mitigate | clampChatWidth is applied on READ (not only on drag) per CONTEXT Decision 4, so a tampered/out-of-range/non-numeric stored value is clamped into the bounded range (or falls back to default), never breaking layout. |
| T-164-12 | Injection | --chat-panel-width application | low | mitigate | The width is coerced to a finite integer via clampChatWidth before being written to the custom property; only a numeric px value reaches CSS — no string concatenation of untrusted text into style, no dangerouslySetInnerHTML. |
| T-164-13 | Denial of service (layout) | resize drag / overlay | low | mitigate | Min/max clamp bounds the drawer both ways; D-02 overlay (pre-existing) is preserved (no sendResize), so a resize cannot collapse or distort the terminal grid. Existing chat-content sanitization (SEC-02/SEC-03) is unchanged — no new untrusted-input rendering path. |
| T-164-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new dependencies are added in this phase — no package installs, so the supply-chain gate is not applicable. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/Hub/ChatPanel.test.tsx src/components/Hub/chatToggleOverlap.test.ts` passes.
- `cd frontend && npx vitest run src/components/__tests__/style.hub.test.ts` still passes (no regression to chat CSS gates).
- `bash tests/check-traceability-paths.sh` exits 0.
- Source check: the resize path contains no `sendResize` call; both `.chat-panel` width rules and the toggle offset reference `var(--chat-panel-width…)`.
- Frontend build sanity (tsc, per the post-merge gate lesson): `cd frontend && npx tsc --noEmit` is clean.
</verification>

<success_criteria>
- A left-edge drag handle resizes the chat drawer on all three surfaces via the shared component.
- Width persists to localStorage and restores (clamped) on reload; bounded both ways within ~280–640px.
- Resizing never resizes the terminal / triggers sendResize (D-02 preserved).
- The chat toggle offset tracks the live drawer width (no longer hard-coded 372px).
- TESTING.md updated; traceability check green.
</success_criteria>

<output>
Create `.planning/phases/164-web-share-chat-layout-polish-message-header-overflow-fix-res/164-02-SUMMARY.md` when done
</output>
