---
phase: 142-hub-settings-redesign-polish
plan: 04
type: execute
wave: 3
depends_on: ["142-01", "142-03"]
files_modified:
  - frontend/src/components/Hub/SessionCard.tsx
  - frontend/src/components/SettingsTab.tsx
  - frontend/src/components/Hub/HubFilterBar.tsx
  - frontend/src/components/Hub/HubEmptyState.tsx
  - frontend/src/style.css
autonomous: true
requirements: [POL-01, POL-02, POL-03]
must_haves:
  truths:
    - "D-04: Card header reserves a dedicated gutter so the menu/handle icons never overlap content at any width, and the mini-preview is taller (~6 lines / 88px) and legible"
    - "D-06: Light/Dark is a single role=switch toggle whose state is shown by a Sun/Moon icon + text label on the knob (colorblind-safe, not color/position), with uiTheme persistence + [data-ui-theme] wiring untouched"
    - "D-07: Both New-session buttons (filter-bar + empty-state) are restyled to the comp's + New Session affordance with onClick wiring unchanged"
    - "Hub card menu and handle icons never overlap session name/status/preview at any card width"
    - "The in-card mini-preview is taller (~6 lines, 88px) and legible"
    - "Settings Appearance Light/Dark is a single role=switch toggle with a Sun/Moon icon + Light/Dark text on the knob, colorblind-safe, persistence intact"
    - "Both Hub New-session buttons (filter-bar + empty-state) render the comp's minimal text affordance with a PlusIcon"
  artifacts:
    - path: "frontend/src/components/SettingsTab.tsx"
      provides: "Single role=switch Light/Dark toggle with icon+text knob"
      contains: "role=\"switch\""
    - path: "frontend/src/style.css"
      provides: ".hub-card gutter + preview height, theme-toggle, new-session affordance CSS with light overrides + motion contract"
      contains: "settings-panel__theme-toggle"
  key_links:
    - from: "frontend/src/components/SettingsTab.tsx"
      to: "frontend/src/App.tsx uiTheme wiring"
      via: "onUiThemeChange(opposite-of-current); App.tsx ~279-294 untouched"
      pattern: "onUiThemeChange"
    - from: "frontend/src/components/Hub/HubFilterBar.tsx"
      to: "@heroicons/react/24/outline PlusIcon"
      via: "PlusIcon in the new-session button; onClick wiring unchanged"
      pattern: "PlusIcon"
---

<objective>
Resolve the three surgical post-redesign polish items on top of the landed
Direction 01 redesign:
- POL-01: reserve a header gutter on `.hub-card` so the absolute-positioned menu
  and handle icons never overlap card content at any width (D-04), and raise the
  mini-preview to ~6 lines (88px) for legibility.
- POL-02: replace the two-button Light/Dark control with a single `role="switch"`
  toggle whose state is shown by a Sun/Moon icon + "Light"/"Dark" text on the knob
  (D-06, colorblind-safe). Existing `uiTheme` persistence + `[data-ui-theme]`
  wiring in App.tsx stays untouched.
- POL-03: restyle both "New session" buttons (filter-bar top-right + empty-state)
  to the comp's minimal `+ New Session` text affordance with a `PlusIcon` (D-07).
  onClick wiring unchanged.

This plan depends on Plan 03 because both edit `frontend/src/style.css`
(exclusive-ownership serialization). Token discipline is LOCKED: every new
color-bearing CSS rule uses `var(--hub-*)` and gets a `[data-ui-theme="light"]`
counterpart; animations use the motion two-block contract.

Purpose: Bring card layout, the theme control, and the new-session affordance to
the comp's interaction quality.
Output: Card gutter + preview CSS, single toggle control + CSS, restyled buttons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-hub-settings-redesign-polish/142-CONTEXT.md
@.planning/phases/142-hub-settings-redesign-polish/142-RESEARCH.md
@.planning/phases/142-hub-settings-redesign-polish/142-PATTERNS.md
@.planning/phases/141-redesign-implementation/141-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: POL-01 — card header gutter + taller mini-preview (CSS-first)</name>
  <read_first>
    - frontend/src/components/Hub/SessionCard.tsx (drag-handle / menu-btn elements ~338-358, MiniPreview render — confirm class names; TSX is unchanged for POL-01)
    - frontend/src/components/Hub/MiniPreview.tsx (confirm preview height is CSS-driven, no inline height)
    - frontend/src/style.css (.hub-card padding + drag-handle/menu-btn absolute positions ~5018-5089, .hub-card__preview height:56px ~5063, light token block ~4082-4149)
    - .planning/phases/142-hub-settings-redesign-polish/142-PATTERNS.md (style.css POL-01 targets ~546-561)
    - .planning/phases/142-hub-settings-redesign-polish/142-RESEARCH.md (POL-01 gutter math: 36px top, 88px preview ≈6 lines, narrow-width reflow)
    - .planning/phases/141-redesign-implementation/141-UI-SPEC.md (token + motion contracts)
  </read_first>
  <files>frontend/src/style.css, frontend/src/components/Hub/SessionCard.tsx</files>
  <action>
    POL-01 is CSS-first (RESEARCH recommends the padding-top approach to avoid TSX changes). In style.css:
    Change `.hub-card` top padding to reserve the icon row: set top padding to `36px` (8px icon-top + 20px icon-height + 8px gap), keeping the existing horizontal and bottom padding (current is `12px 16px` → becomes `36px 16px 12px`). This places the absolute menu/handle icons (`top: 8px`) in a dedicated gutter above ROW 1 (status + name + CLI badge) so they never overlap content at any width. Leave `.hub-card__drag-handle` (`top:8px;left:8px`) and `.hub-card__menu-btn` (`top:8px;right:8px`) positions unchanged.
    Change `.hub-card__preview` `height: 56px` → `height: 88px` (≈6 lines at 11px/1.3 line-height). Keep its `overflow:hidden`, border, background, and padding.
    These are padding/height (non-color) changes referencing existing `--hub-preview-*` tokens, so no new `[data-ui-theme="light"]` override is required. If you introduce any color-bearing value, add the matching light override.
    SessionCard.tsx: prefer ZERO edits (RESEARCH confirms none needed). Touch it only if the gutter cannot be achieved via `.hub-card` padding — and then layout only, never drag/menu logic.
    Do NOT change MiniPreview.tsx (height is CSS-driven).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit 2>&1 | tail -8 && grep -nE "hub-card__preview|padding-top: 36px|36px 16px" frontend/src/style.css | head</automated>
  </verify>
  <acceptance_criteria>
    - style.css `.hub-card` reserves a 36px top gutter (top padding 36px, e.g. `36px 16px 12px`)
    - style.css `.hub-card__preview` height is `88px` (was 56px)
    - `.hub-card__drag-handle` and `.hub-card__menu-btn` remain `top: 8px` absolute (now within the gutter)
    - No raw hex added to `.hub-card*` rules — `grep -nE "\.hub-card.*#[0-9a-fA-F]{3,6}" frontend/src/style.css` shows no NEW hex from this task
    - MiniPreview.tsx unchanged; SessionCard.tsx unchanged (or layout-only if touched)
    - `cd frontend && pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Card gutter reserves space for menu/handle icons; preview raised to 88px; no logic changes; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: POL-02 — single role=switch Light/Dark toggle with icon+text knob</name>
  <read_first>
    - frontend/src/components/SettingsTab.tsx (current two-button control ~438-461, Appearance section header, existing heroicons usage if any)
    - frontend/src/components/__tests__/SettingsTab.appearance-theme.test.tsx (the RED contract from Plan 01 this task must satisfy)
    - frontend/src/App.tsx (uiTheme state + [data-ui-theme] effect + handleUiThemeChange ~279-294 — MUST stay untouched; confirm the onUiThemeChange prop signature)
    - frontend/src/style.css (existing .settings-panel__btn pattern, --hub-radius-pill / surface / text tokens, motion contract ~328-343, light block ~4082-4149)
    - .planning/phases/142-hub-settings-redesign-polish/142-PATTERNS.md (SettingsTab section ~359-432; style.css POL-02 toggle CSS ~563-615; heroicons + colorblind shared patterns)
    - .planning/phases/142-hub-settings-redesign-polish/142-RESEARCH.md (POL-02 replacement design, D-06 colorblind-safe contract, role=switch ARIA)
    - .planning/phases/141-redesign-implementation/141-UI-SPEC.md (token + motion + colorblind contracts)
  </read_first>
  <files>frontend/src/components/SettingsTab.tsx, frontend/src/style.css</files>
  <action>
    Import `SunIcon, MoonIcon` from `@heroicons/react/24/outline` (already installed; size via CSS class, no Tailwind w-/h-).
    Replace the Appearance two-button `role="group"` block (~438-461) with a single `<button type="button" role="switch">` per PATTERNS.md ~385-411: `aria-checked={uiTheme === 'light'}`, an `aria-label` that names current state + the action ("Light mode — click to switch to dark" / "Dark mode — click to switch to light"), `className` toggling `settings-panel__theme-toggle--light` when light, and `onClick={() => onUiThemeChange(uiTheme === 'light' ? 'dark' : 'light')}`. Inside render a `settings-panel__theme-toggle-track` containing a `settings-panel__theme-toggle-knob` (`aria-hidden="true"`) that shows `<SunIcon/> <span>Light</span>` when light and `<MoonIcon/> <span>Dark</span>` when dark — icon SHAPE + TEXT carry the state (colorblind-safe, D-06), not knob position or color alone. Keep a short description `<p>` below.
    Do NOT modify App.tsx `uiTheme`/`[data-ui-theme]`/`handleUiThemeChange` wiring — only the SettingsTab control changes; persistence and DOM attribute flow already work.
    Add the toggle CSS to style.css per PATTERNS.md ~563-615: `.settings-panel__theme-toggle` (transparent button reset), `.settings-panel__theme-toggle-track` (pill track using `--hub-radius-pill`, `--hub-surface-elevated`, `--hub-border`), `.settings-panel__theme-toggle-knob` (positioned knob using `--hub-surface`, `--hub-text-secondary`, icon+label flex), `.settings-panel__theme-toggle-icon` (size). Knob sits left in dark and right in light: add `[data-ui-theme="light"] .settings-panel__theme-toggle-knob { left:auto; right:4px; }` (or use the `--light` class). Provide a `[data-ui-theme="light"]` counterpart for any color-bearing rule whose value is not already a shared adapting token. Wrap the knob slide transition in `@media (prefers-reduced-motion: no-preference)` with a `reduce` static fallback.
    Use ONLY `var(--hub-*)` tokens (no raw hex).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit 2>&1 | tail -8 && pnpm test run src/components/__tests__/SettingsTab.appearance-theme.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - SettingsTab.tsx Appearance control is a single `[role="switch"]` button with `aria-checked` reflecting `uiTheme==='light'`
    - The knob shows `SunIcon`+"Light" when light and `MoonIcon`+"Dark" when dark (icon shape + text, not color/position alone)
    - onClick calls `onUiThemeChange` with the opposite of the current theme; App.tsx ~279-294 wiring is unchanged (`git diff --stat frontend/src/App.tsx` shows no change from this task)
    - style.css has `.settings-panel__theme-toggle`, `-track`, `-knob` rules using only `var(--hub-*)` tokens with a `[data-ui-theme="light"]` knob-position override
    - The knob transition is wrapped in `prefers-reduced-motion: no-preference` with a `reduce` fallback
    - `grep -nE "settings-panel__theme-toggle.*#[0-9a-fA-F]{3,6}" frontend/src/style.css` is empty (no raw hex)
    - `cd frontend && pnpm exec tsc --noEmit` exits 0
    - SettingsTab.appearance-theme.test.tsx now PASSES (the Plan 01 RED contract is green)
  </acceptance_criteria>
  <done>Single colorblind-safe role=switch toggle replaces the two buttons; persistence untouched; tokenized CSS + motion contract; appearance test green.</done>
</task>

<task type="auto">
  <name>Task 3: POL-03 — restyle both New-session buttons to the comp affordance</name>
  <read_first>
    - frontend/src/components/Hub/HubFilterBar.tsx (current `.hub-filter__new-session` button ~134-141, onNewSession wiring)
    - frontend/src/components/Hub/HubEmptyState.tsx (current `.hub__empty-cta` button ~43, onNewSession wiring)
    - frontend/src/components/__tests__/Sidebar.test.tsx (the POL-03 source-gate from Plan 01: asserts PlusIcon in both files)
    - frontend/src/style.css (current .hub-filter__new-session + .hub__empty-cta styles ~4533-4641, light block, motion contract)
    - .planning/phases/142-hub-settings-redesign-polish/142-PATTERNS.md (HubFilterBar/HubEmptyState sections ~436-485; style.css POL-03 targets ~617-652; heroicons shared pattern)
    - .planning/phases/142-hub-settings-redesign-polish/142-RESEARCH.md (POL-03 comp target = minimal text-link with accent + prefix; PlusIcon-in-TSX recommended over ::before)
    - agenthub-v4.0-redesign/AgentHub UI redesign/c-sessions.png (the comp sidebar "+ New Session" affordance — POL-03 styling target)
  </read_first>
  <files>frontend/src/components/Hub/HubFilterBar.tsx, frontend/src/components/Hub/HubEmptyState.tsx, frontend/src/style.css</files>
  <action>
    In both HubFilterBar.tsx and HubEmptyState.tsx: import `PlusIcon` from `@heroicons/react/24/outline` (installed; size via CSS, no Tailwind w-/h-). Add the icon inside the existing buttons before the "New session" text: HubFilterBar uses `<PlusIcon className="hub-filter__new-session-icon" aria-hidden="true" />`; HubEmptyState uses `<PlusIcon className="hub__empty-cta-icon" aria-hidden="true" />`. Keep the existing button class names and `onClick={onNewSession}` wiring exactly as-is — only the interior glyph and CSS change.
    In style.css: restyle `.hub-filter__new-session` to the comp affordance per PATTERNS.md ~617-652 — `background: transparent`, `border: none`, `color: var(--hub-text-secondary)`, flex with `gap`, height 28px, `margin-left:auto`; `:hover { color: var(--hub-text-primary) }`; `.hub-filter__new-session-icon { width:14px; height:14px; color: var(--hub-accent) }`. Restyle `.hub__empty-cta` the same minimal affordance but centered (height ~32px, no `margin-left:auto`) with `.hub__empty-cta-icon` matching. All colors via `var(--hub-*)` tokens that already adapt across themes — add a `[data-ui-theme="light"]` override only if a computed value differs. Remove the old bordered/filled-surface rules these buttons previously used so the new transparent affordance fully replaces them. Wrap any hover/color transition in the motion two-block contract.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit 2>&1 | tail -8 && pnpm test run src/components/__tests__/Sidebar.test.tsx 2>&1 | grep -iE "PlusIcon|POL-03|pass|fail" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - HubFilterBar.tsx and HubEmptyState.tsx both import and render `PlusIcon` inside their existing New-session buttons; `onClick={onNewSession}` unchanged
    - style.css `.hub-filter__new-session` and `.hub__empty-cta` are transparent/borderless text affordances with an accent-colored `*-icon` using `var(--hub-*)` tokens
    - `grep -nE "hub-filter__new-session.*#[0-9a-fA-F]{3,6}|hub__empty-cta.*#[0-9a-fA-F]{3,6}" frontend/src/style.css` is empty (no raw hex)
    - Any color/hover transition on these rules is wrapped in `prefers-reduced-motion` two-block contract
    - `cd frontend && pnpm exec tsc --noEmit` exits 0
    - The POL-03 source-gate test in Sidebar.test.tsx (PlusIcon in both files) now PASSES
  </acceptance_criteria>
  <done>Both New-session buttons render the comp's minimal + New Session affordance; onClick unchanged; tokenized CSS; POL-03 source gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click → uiTheme change | Theme preference is client-only; persisted to localStorage via existing App.tsx wiring (untouched) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-07 | Tampering | localStorage uiTheme value | accept | Existing App.tsx wiring already reads/writes this; this plan does not change persistence. Worst case is an unexpected theme attribute, no security impact. |
| T-142-08 | Information Disclosure | Mini-preview rendering session output (POL-01 taller height) | accept | Preview already renders the session tail via the existing headless-VT path (Phase 139); raising height shows more of the SAME already-rendered content — no new data surface |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | No package installs — RESEARCH.md confirms zero new packages (SunIcon/MoonIcon/PlusIcon ship in already-installed @heroicons/react) |
</threat_model>

<verification>
- `cd frontend && pnpm exec tsc --noEmit` exits 0.
- `cd frontend && pnpm test run src/components/__tests__/SettingsTab.appearance-theme.test.tsx src/components/__tests__/Sidebar.test.tsx 2>&1 | tail -20` — POL-02 toggle tests + POL-03 source gate green.
- Token discipline: `grep -nE "(settings-panel__theme-toggle|hub-filter__new-session|hub__empty-cta|hub-card).*#[0-9a-fA-F]{3,6}" frontend/src/style.css` shows no NEW raw hex introduced by this plan.
- Manual (per VALIDATION.md): card icons clear of content at narrow widths — resize Hub grid in the running app; confirm menu/handle never overlap name/status/preview.
</verification>

<success_criteria>
- Card header gutter prevents icon overlap at any width; preview raised to 88px.
- Light/Dark is a single colorblind-safe role=switch toggle; persistence intact; appearance test green.
- Both New-session buttons match the comp affordance with PlusIcon; source gate green.
- All new CSS tokenized with light overrides + motion contract; no raw hex.
- tsc clean.
</success_criteria>

<output>
Create `.planning/phases/142-hub-settings-redesign-polish/142-04-SUMMARY.md` when done
</output>
