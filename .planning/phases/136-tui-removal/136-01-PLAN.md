---
phase: 136-tui-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/
  - cmd_tui.go
  - main.go
  - cmd_cli.go
  - internal/daemon/remote_files_parity_test.go
  - internal/daemon/remote_files_write_parity_test.go
  - go.mod
  - go.sum
autonomous: true
requirements: [NAV-01, TEST-06]

must_haves:
  truths:
    - "`agenthub tui` exits non-zero / is not recognized (TUI surface no longer launches)"
    - "The `internal/tui` package directory no longer exists"
    - "No `.go` file in the live tree imports `internal/tui`"
    - "go.mod contains no charm.land bubbletea/bubbles/lipgloss or charmbracelet/glamour deps"
    - "`go build ./...` is green with no dangling import"
  artifacts:
    - path: "main.go"
      provides: "Command dispatch switch with the `case \"tui\":` branch removed"
      contains: "func runCLI"
    - path: "cmd_cli.go"
      provides: "usage() help text with the `tui` subcommand line removed"
      contains: "func usage"
    - path: "go.mod"
      provides: "Require block pruned of all TUI-exclusive charm deps via go mod tidy"
      contains: "module github.com/scottkw/agenthub"
  key_links:
    - from: "main.go runCLI switch"
      to: "default: unknown-command arm"
      via: "removal of case \"tui\" so `agenthub tui` falls through to default"
      pattern: "unknown command"
    - from: "go.mod require block"
      to: "deleted internal/tui package"
      via: "go mod tidy recomputing the dependency closure"
      pattern: "go mod tidy"
---

<objective>
Delete the AgentHub TUI surface: the entire `internal/tui/` package (18 source + 14 test files), its only top-level consumer `cmd_tui.go`, the command dispatch and usage references in `main.go` / `cmd_cli.go`, and the two `internal/daemon` parity test files that import `internal/tui`. Then prune the now-unused charm.land/charmbracelet dependencies via `go mod tidy`.

Purpose: NAV-01 narrows the cross-surface parity contract to GUI/CLI/web by retiring `agenthub tui` entirely. TEST-06 deletes (does not migrate) all TUI tests. This is a pure removal — no behavior is added.

Output: A tree with `internal/tui/` gone, no `internal/tui` import paths, `agenthub tui` falling through to the unknown-command arm, and a tidied go.mod with the four TUI-exclusive charm deps removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-tui-removal/136-RESEARCH.md
@.planning/phases/136-tui-removal/136-VALIDATION.md

<notes>
- RESEARCH.md is the AUTHORITATIVE deletion inventory. Treat its file list and ordering as verified against the live tree.
- ORDERING LANDMINE (RESEARCH section 7): `cmd_tui.go` and the two daemon parity test files import `internal/tui`. They MUST be removed in the same atomic operation as the package directory, or `go build ./...` breaks with "cannot find package internal/tui". Do the deletions together before running build/test.
- `internal/attach/` SURVIVES untouched — it is shared by the CLI attach path and does NOT import `internal/tui`. Do NOT delete or modify it (the line-2 reference in `internal/attach/attach.go` is a comment only — handled in plan 02).
- Do NOT touch `internal/daemon/engine.go` or its tests — `opencode-tui.json` there is OpenCode CLI's config format, NOT AgentHub's TUI.
- `golang.org/x/term` is shared by `cmd_attach.go`, `internal/statusbar/bar.go`, and `internal/attach/attach_unix.go`. Do NOT remove it from go.mod. `go mod tidy` keeps it correctly.
- Use `go mod tidy` for dependency cleanup — never hand-edit go.mod/go.sum.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic deletion of the TUI package and all its import consumers</name>
  <read_first>
    - .planning/phases/136-tui-removal/136-RESEARCH.md (deletion inventory: sections 1, 2, 7)
    - .planning/phases/136-tui-removal/136-VALIDATION.md (proving commands)
    - cmd_tui.go (the top-level consumer importing internal/tui — to be deleted whole)
    - main.go (the runCLI command-dispatch switch — `case "tui":` at ~line 209)
    - cmd_cli.go (the usage() help text — `tui` line at ~line 46)
    - internal/daemon/remote_files_parity_test.go (imports internal/tui — delete whole)
    - internal/daemon/remote_files_write_parity_test.go (imports internal/tui — delete whole)
  </read_first>
  <files>internal/tui/, cmd_tui.go, main.go, cmd_cli.go, internal/daemon/remote_files_parity_test.go, internal/daemon/remote_files_write_parity_test.go</files>
  <action>
    Perform these deletions and edits as one atomic batch so the tree never has a dangling `internal/tui` import:
    1. `git rm -r internal/tui` — delete the entire package directory (all 18 source + 14 test files).
    2. `git rm cmd_tui.go` — delete the whole top-level TUI command file (it imports internal/tui and constructs tui.Run / tui.ListRemoteGroup / tui.RemoteSessionEntry).
    3. `git rm internal/daemon/remote_files_parity_test.go` and `git rm internal/daemon/remote_files_write_parity_test.go` — delete both daemon parity test files outright (they are `package daemon_test`, import internal/tui as a direct-client oracle; daemon proxy correctness is independently covered by remote_files_test.go, relay_remote_files_test.go, remote_caps_test.go which have no TUI import).
    4. Edit `main.go`: remove the two lines `case "tui":` and `err = cmdTUI(client)` from the `switch cmd` in `runCLI()`. Do NOT add a stub — the existing `default:` arm already prints `agenthub: unknown command "tui"` and exits 1.
    5. Edit `cmd_cli.go`: remove the `tui` line from the `usage()` text (the line reading `tui   Launch interactive terminal UI`).
    Do NOT touch internal/attach/, internal/daemon/engine.go, or golang.org/x/term in go.mod.
  </action>
  <verify>
    <automated>go build ./... && ! ls internal/tui 2>/dev/null && ! grep -rn "internal/tui" --include="*.go" . --exclude-dir=.claude | grep -v "^Binary"</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0
    - `internal/tui` directory does not exist: `! ls internal/tui 2>/dev/null`
    - No Go import paths remain: `grep -rn "internal/tui" --include="*.go" . --exclude-dir=.claude` returns nothing
    - `cmd_tui.go` does not exist: `! ls cmd_tui.go 2>/dev/null`
    - Both parity test files gone: `! ls internal/daemon/remote_files_parity_test.go internal/daemon/remote_files_write_parity_test.go 2>/dev/null`
    - `grep -n 'case "tui"' main.go` returns nothing
    - `grep -n 'tui' cmd_cli.go` returns nothing (usage line removed)
    - `internal/attach/` directory still exists and is unchanged: `ls internal/attach/attach.go`
    - `internal/daemon/engine.go` unmodified by this task
  </acceptance_criteria>
  <done>The TUI package, its top-level consumer, the dispatch case, the usage line, and both daemon parity tests are removed; `go build ./...` is green with zero `internal/tui` import paths in the tree.</done>
</task>

<task type="auto">
  <name>Task 2: Prune unused charm dependencies with go mod tidy</name>
  <read_first>
    - .planning/phases/136-tui-removal/136-RESEARCH.md (section 4: go.mod / dependency cleanup; pitfall 4 and pitfall 5)
    - go.mod (current require block — charm.land/* at lines 6-8, charmbracelet/glamour line 11, charmbracelet/x/ansi line 12)
  </read_first>
  <files>go.mod, go.sum</files>
  <action>
    Only after Task 1 has made `go build ./...` green: run `go mod tidy`. This recomputes the dependency closure and drops the four TUI-exclusive direct deps (`charm.land/bubbletea/v2`, `charm.land/bubbles/v2`, `charm.land/lipgloss/v2`, `github.com/charmbracelet/glamour`) plus `github.com/charmbracelet/x/ansi` and any orphaned charm-ecosystem indirect deps. Do NOT hand-edit go.mod or go.sum — `go mod tidy` is the authoritative mechanism. Do NOT remove `golang.org/x/term` (shared by cmd_attach.go, internal/statusbar/bar.go, internal/attach/attach_unix.go — tidy keeps it). Commit the resulting go.mod and go.sum changes.
  </action>
  <verify>
    <automated>go mod tidy && go build ./... && ! grep -E "charm.land/(bubbletea|bubbles|lipgloss)|charmbracelet/glamour|charmbracelet/x/ansi" go.mod && grep -q "golang.org/x/term" go.mod</automated>
  </verify>
  <acceptance_criteria>
    - `go mod tidy` runs clean and `go build ./...` exits 0 afterward
    - `grep -E "charm.land/(bubbletea|bubbles|lipgloss)|charmbracelet/glamour" go.mod` returns nothing
    - `grep "charmbracelet/x/ansi" go.mod` returns nothing
    - `grep "golang.org/x/term" go.mod` still returns a match (shared dep preserved)
    - `git diff go.mod` shows charm.land/* lines removed from the require block
  </acceptance_criteria>
  <done>go.mod and go.sum are tidied; the four TUI-exclusive charm direct deps and `x/ansi` are absent; `golang.org/x/term` is preserved; `go build ./...` remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a pure DELETION phase. No new trust boundary is introduced. The boundaries that change:

| Boundary | Description | Change |
|----------|-------------|--------|
| TUI client → remote peer (direct HTTPS + cap token over Tailscale) | The TUI's `RemoteFilesClient` opened a direct cap-bearing HTTPS path | REMOVED — this attack surface is deleted entirely |
| GUI/web → daemon proxy → remote peer | Daemon proxy enforces the cap-leak invariant independently | UNCHANGED — survives and is still covered by remote_files_test.go / relay_remote_files_test.go / remote_caps_test.go |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-01 | Information Disclosure | `internal/tui/remote_files_client.go` cap-token path | n/a (removed) | The cap-leak surface is DELETED, not mitigated. The surviving daemon proxy enforces the same invariant independently. No new exposure. |
| T-136-SC | Tampering | go.mod / go.sum via `go mod tidy` | accept | No new packages are installed. `go mod tidy` only REMOVES deps. Package Legitimacy Gate N/A (no installs). |

**ASVS:** Not applicable — a pure removal introduces no new authentication, session, crypto, or input-validation paths. Security impact is uniformly positive (one direct-HTTPS cap surface removed; nothing added).
</threat_model>

<verification>
After both tasks:
- `go build ./...` exits 0
- `! ls internal/tui 2>/dev/null` (directory gone)
- `grep -rn "internal/tui" --include="*.go" . --exclude-dir=.claude` returns nothing
- `! grep -E "charm.land/(bubbletea|bubbles|lipgloss)|charmbracelet/glamour|charmbracelet/x/ansi" go.mod`
- `grep "golang.org/x/term" go.mod` still matches
- `internal/attach/` and `internal/daemon/engine.go` untouched
</verification>

<success_criteria>
- `internal/tui/`, `cmd_tui.go`, and both daemon parity test files are deleted
- `main.go` dispatch and `cmd_cli.go` usage no longer reference `tui`
- `go build ./...` is green with zero `internal/tui` import paths
- go.mod is tidied of all four TUI-exclusive charm deps + `x/ansi`; `golang.org/x/term` preserved
</success_criteria>

<output>
Create `.planning/phases/136-tui-removal/136-01-SUMMARY.md` when done
</output>
