---
phase: 125-react-editor-codemirror-6-desktop-web
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/files/write.go
  - internal/files/handler.go
  - internal/files/write_test.go
  - internal/webserver/vendor_drift_test.go
  - cmd/playwright-fixture/main.go
  - frontend/e2e/global-setup.ts
  - frontend/e2e/fixture-env.ts
autonomous: true
requirements: [EDIT-01, EDIT-05, EDIT-08, EDIT-13]
must_haves:
  truths:
    - "PUT /api/files/write with a stale If-Match returns HTTP 412; with a matching validator returns 200; with no header (new file) returns 200"
    - "GET /api/files/read emits an ETag header the client can echo verbatim as If-Match"
    - "The Playwright fixture mints a third cap carrying files.write and emits WRITE_CAP="
    - "vendor_drift_test asserts package.json CodeMirror versions match pnpm-lock.yaml resolved versions"
  artifacts:
    - path: "internal/files/write.go"
      provides: "If-Match precondition check in Handler.Write, 412 on mismatch"
      contains: "StatusPreconditionFailed"
    - path: "internal/files/handler.go"
      provides: "ETag header on Read response"
      contains: "ETag"
    - path: "internal/files/write_test.go"
      provides: "TestWrite_IfMatch* (match/mismatch/new-file)"
      contains: "TestWrite_IfMatch"
    - path: "internal/webserver/vendor_drift_test.go"
      provides: "CodeMirror version-parity gate"
      contains: "codemirror"
    - path: "cmd/playwright-fixture/main.go"
      provides: "files.write cap variant + WRITE_CAP emission"
      contains: "WRITE_CAP"
    - path: "frontend/e2e/global-setup.ts"
      provides: "WRITE_CAP= parse"
      contains: "WRITE_CAP="
  key_links:
    - from: "internal/files/write.go Handler.Write"
      to: "internal/files/sandbox.go Sandbox.Stat"
      via: "validator computed from on-disk FileInfo"
      pattern: "sb\\.Stat"
    - from: "internal/files/handler.go Handler.Read ETag"
      to: "internal/files/write.go If-Match validator"
      via: "identical format \"<UnixNano>-<size>\""
      pattern: "UnixNano"
---

<objective>
Wave 0 scaffolding: the ONE backend change (If-Match/412 + ETag emission), its Go unit tests, the CodeMirror vendor-drift parity gate, and the Playwright write-cap fixture plumbing. This unblocks every downstream React plan (EDIT-05/08 cannot be satisfied without server 412; EDIT-13 web-share scenarios cannot run without a files.write cap fixture).

Purpose: Establish the server-side optimistic-concurrency contract and test fixtures BEFORE any frontend work, so downstream plans build against a real 412 and a real write-enabled cap rather than stubs.
Output: 412-capable write route, ETag-emitting read route, passing Go tests, version-parity gate, WRITE_CAP fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-RESEARCH.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-PATTERNS.md

<interfaces>
<!-- Validator format contract — load-bearing. Client and server MUST agree byte-for-byte. -->
Server-computed validator (write.go) and ETag (handler.go) format:
  quoted string `"<ModTime().UnixNano()>-<Size()>"`  e.g.  "1718384000123456789-2048"

Sandbox.Stat (internal/files/sandbox.go:169) — already shipped, os.Root-confined, path-validated:
  func (s *Sandbox) Stat(relPath string) (os.FileInfo, error)

writeWriteError (write.go:236-249) — already maps fs.ErrExist→409, ErrPathValidation→403, fs.ErrNotExist→404. The 412 is a precondition, NOT a write error — it lives INLINE in Handler.Write, not in writeWriteError.

Handler.Write is the single method mounted on daemon + webserver + remote proxy. The 412 check is inherited by all three surfaces.

Playwright fixture cap mint (cmd/playwright-fixture/main.go:178-202): owner = "read,write,files.read"; viewer = "read". Neither carries files.write.
Env emission block (~line 263-269); global-setup.ts parses VIEWER_CAP= at line 148; fixture-env.ts FixtureEnv interface lines 14-17.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server If-Match/412 precondition + ETag emission</name>
  <read_first>
    internal/files/write.go (Handler.Write, imports, writeWriteError) ; internal/files/handler.go (Handler.Read ServeContent + Last-Modified branch, lines ~291-302) ; internal/files/sandbox.go (Sandbox.Stat, lines 169-180)
  </read_first>
  <behavior>
    - Write with If-Match matching the on-disk validator → file written, 200.
    - Write with If-Match NOT matching the on-disk validator → 412 Precondition Failed, file NOT modified.
    - Write with If-Match "*" or absent → write proceeds (force-overwrite / new-file path).
    - Write with If-Match present but target does not exist → write proceeds (new file).
    - Read of an existing file → response carries ETag header `"<UnixNano>-<size>"`.
  </behavior>
  <action>
    In internal/files/write.go Handler.Write, insert the If-Match precondition check AFTER the `rel == ""` guard and BEFORE `sb.WriteFileAtomic`. Read `r.Header.Get("If-Match")`; when present and not `"*"`, call `sb.Stat(rel)` — if the target exists, compute `cur := fmt.Sprintf("%q", fmt.Sprintf("%d-%d", fi.ModTime().UnixNano(), fi.Size()))` and return `http.Error(w, "file modified by another process", http.StatusPreconditionFailed)` when `ifMatch != cur`. If Stat errors (target missing) the write proceeds as a new file. Add `"fmt"` to the import block (currently absent). Do NOT add this to writeWriteError — it is a precondition, not a write error. Do NOT add any capability logic (loopback-trust per the write.go header comment).
    In internal/files/handler.go Handler.Read, set the same-format ETag header on BOTH the zero-byte branch and immediately before `http.ServeContent`: `w.Header().Set("ETag", fmt.Sprintf("%q", fmt.Sprintf("%d-%d", fi.ModTime().UnixNano(), fi.Size())))`. Add `"fmt"` import if absent. This is the server-emit-ETag resolution of RESEARCH Open Q6 — the client echoes it verbatim, eliminating the RFC3339-vs-UnixNano format-mismatch risk.
  </action>
  <verify>
    <automated>go test ./internal/files/... -run TestWrite_IfMatch -count=1</automated>
  </verify>
  <acceptance_criteria>
    `go test ./internal/files/... -run TestWrite_IfMatch -count=1` exits 0. `grep -n StatusPreconditionFailed internal/files/write.go` returns a match. `grep -c 'ETag' internal/files/handler.go` is >= 1.
  </acceptance_criteria>
  <done>Handler.Write returns 412 on validator mismatch and 200 on match/new-file; Handler.Read emits an ETag header in the documented format.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Go unit tests for If-Match (TestWrite_IfMatch*)</name>
  <read_first>
    internal/files/write_test.go (TestWriteFileAtomic_Overwrite for sandbox setup) ; internal/files/handler_test.go (httptest.NewRequest Handler-level wiring pattern)
  </read_first>
  <behavior>
    - TestWrite_IfMatch_Match: PUT with the correct on-disk validator → 200, content persisted.
    - TestWrite_IfMatch_Mismatch: PUT with a stale validator → 412, original content preserved.
    - TestWrite_IfMatch_NewFile: PUT with no If-Match to a non-existent path → 200, file created.
  </behavior>
  <action>
    Add the three cases to internal/files/write_test.go exercising Handler.Write through `httptest.NewRequest`/`httptest.NewRecorder` (the check lives in the HTTP handler, not the Sandbox primitive — handler_test.go is the wiring analog). Build the expected validator the SAME way Handler.Write does (`"<UnixNano>-<size>"` quoted) by stat-ing the file after the initial write. Assert status codes and that on 412 the on-disk bytes are unchanged.
  </action>
  <verify>
    <automated>go test ./internal/files/... -run TestWrite_IfMatch -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    `go test ./internal/files/... -run TestWrite_IfMatch -count=1 -race` exits 0 with three test cases reporting PASS.
  </acceptance_criteria>
  <done>Three If-Match test cases pass under the race detector.</done>
</task>

<task type="auto">
  <name>Task 3: CodeMirror vendor-drift parity gate + Playwright WRITE_CAP fixture</name>
  <read_first>
    internal/webserver/vendor_drift_test.go (TestXtermVendorVersionsMatchPnpmLock, lock-parse loop lines 27-36, per-package compare 59-73) ; cmd/playwright-fixture/main.go (owner/viewer cap mint lines 178-202, env emission ~263-269) ; frontend/e2e/global-setup.ts (VIEWER_CAP= parse line 148, resolveEnv object) ; frontend/e2e/fixture-env.ts (FixtureEnv interface lines 14-17)
  </read_first>
  <action>
    (a) vendor_drift_test.go: add a NEW test (e.g. TestCodeMirrorVersionsMatchPnpmLock) asserting `frontend/package.json` declared versions == `frontend/pnpm-lock.yaml` resolved versions for every `@codemirror/*` package AND the `codemirror` package. Reuse the existing pnpm-lock parse loop; swap the xterm regex for a `@codemirror/`-and-`codemirror` matcher; read package.json dependencies instead of a VERSION file. Do NOT create a `web/vendor/codemirror/` directory or VERSION manifest — CodeMirror is Vite-bundled, not web-served (RESEARCH Open Q1; the UI-SPEC `web/vendor/codemirror/` wording is superseded). The test should currently pass trivially (zero CodeMirror deps yet) and become load-bearing once Plan 02 installs the packages.
    (b) cmd/playwright-fixture/main.go: mint a third cap `writeClaims` with `Perms: "read,files.read,files.write"`, GrantID `grant-playwright-fixture-write`, sign with the existing fixedTestKey, call `ws.AddGrant`, and emit `fmt.Printf("WRITE_CAP=%s\n", writeToken)` in the env block (mirror viewerClaims exactly). Leave the existing viewer read-only cap intact (it covers the 403-without-cap scenario).
    (c) frontend/e2e/global-setup.ts: parse `WRITE_CAP=` mirroring the `VIEWER_CAP=` line; add `writeCap` to the resolveEnv object. (d) frontend/e2e/fixture-env.ts: add `writeCap: string` to the FixtureEnv interface and a `writeAppUrl(env)` helper alongside `viewerAppUrl`.
  </action>
  <verify>
    <automated>go test ./internal/webserver/... -run CodeMirror -count=1 && cd frontend && pnpm exec tsc --noEmit -p e2e/tsconfig.json 2>/dev/null || cd frontend && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `go test ./internal/webserver/... -run CodeMirror -count=1` exits 0. `grep -c 'WRITE_CAP' cmd/playwright-fixture/main.go` >= 1. `grep -c 'WRITE_CAP=' frontend/e2e/global-setup.ts` >= 1. `grep -c 'writeCap' frontend/e2e/fixture-env.ts` >= 1. Frontend e2e TypeScript compiles without error.
  </acceptance_criteria>
  <done>Version-parity gate exists and passes; fixture mints and emits a files.write cap; global-setup parses WRITE_CAP and fixture-env exposes writeCap + writeAppUrl.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/web-share client → webserver write route | untrusted If-Match header crosses here |
| daemon socket → Handler.Write | loopback-trust (no auth); capability gating is webserver-tier only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01 | Tampering | Handler.Write lost-update (concurrent edit overwrite) | mitigate | If-Match precondition compares on-disk mtime+size validator via os.Root-confined Sandbox.Stat; 412 on mismatch, write rejected |
| T-125-02 | Tampering | If-Match validator forgery to skip the check | accept | "*" / absent intentionally means force-overwrite (a deliberate user choice in the 412 modal, EDIT-08); server is not the authority on which edit "wins", only on detecting divergence |
| T-125-03 | Spoofing | Playwright fixture cap | accept | test-only fixed key in cmd/playwright-fixture; never shipped in release binary |
| T-125-SC | Tampering | npm CodeMirror installs (deferred to Plan 02) | mitigate | vendor_drift_test version-parity gate added here; legitimacy checkpoint in Plan 02 (packages [ASSUMED], org-scoped @codemirror/) |
</threat_model>

<verification>
- `go test ./internal/files/... ./internal/webserver/... -count=1 -race` green.
- Frontend e2e TypeScript compiles.
- No capability logic added to write.go (loopback-trust preserved).
</verification>

<success_criteria>
Server returns 412 on If-Match mismatch and emits ETag on Read; Go tests pass; CodeMirror version-parity gate present; Playwright fixture mints and emits a files.write cap parsed by global-setup. (SC2, SC3, SC5 foundations)
</success_criteria>

<output>
Create `.planning/phases/125-react-editor-codemirror-6-desktop-web/125-01-SUMMARY.md` when done
</output>
