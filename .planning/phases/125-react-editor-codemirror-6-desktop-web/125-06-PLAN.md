---
phase: 125-react-editor-codemirror-6-desktop-web
plan: 06
type: execute
wave: 6
depends_on: ["125-05"]
files_modified:
  - frontend/e2e/files-write.spec.ts
  - frontend/e2e/web-csp.spec.ts
autonomous: false
requirements: [EDIT-13, EDIT-01]
must_haves:
  truths:
    - "Playwright cross-browser (Chromium + Firefox + WebKit) passes all 14 EDIT-13 scenarios"
    - "A web-share write with a files.write cap succeeds; a viewer without the cap gets HTTP 403"
    - "Zero CSP violations are observed in any browser during the editor + write flows"
    - "vendor_drift_test.go passes with CodeMirror packages version-matched"
  artifacts:
    - path: "frontend/e2e/files-write.spec.ts"
      provides: "14-scenario cross-browser write/editor e2e merge gate"
      contains: "files.write"
    - path: "frontend/e2e/web-csp.spec.ts"
      provides: "zero-CSP-violation assertion extended to cover the editor"
      contains: "csp"
  key_links:
    - from: "files-write.spec.ts"
      to: "WRITE_CAP fixture (Plan 01)"
      via: "writeAppUrl(env) for the web-share write + 412 scenarios"
      pattern: "writeAppUrl|WRITE_CAP"
    - from: "files-write.spec.ts 403 scenario"
      to: "viewer read-only cap"
      via: "write attempt → HTTP 403 without files.write"
      pattern: "403"
---

<objective>
Phase merge gate: the Playwright cross-browser e2e suite covering all 14 EDIT-13 scenarios, the zero-CSP-violation assertion, and the vendor-drift gate. This is the SC5 gate before /gsd:verify-work.

Purpose: Prove the full write surface works identically across Chromium/Firefox/WebKit on the web-share surface, with cap enforcement, conflict handling, and no CSP regressions.
Output: files-write.spec.ts (14 scenarios), extended web-csp.spec.ts, green vendor-drift gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-RESEARCH.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-PATTERNS.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-VALIDATION.md

<interfaces>
<!-- Reuse verbatim. -->
files-browser.spec.ts (Phase 120 merge gate) — the analog: imports @playwright/test, loadFixtureEnv, filesApiURL/appUrl/viewerAppUrl from fixture-env (40-50); test.describe.configure({ mode: 'serial' }) (53); cross-browser via playwright.config.ts (Chromium+Firefox+WebKit).
Plan 01 added: WRITE_CAP fixture cap + global-setup parse + fixture-env writeCap + writeAppUrl(env). Use writeAppUrl for write-enabled web-share; the existing viewer read-only cap covers the 403-without-cap scenario.
web-csp.spec.ts — existing CSP console-error assertion; extend to navigate the editor + write flow and assert zero CSP violations.
14 EDIT-13 scenarios (RESEARCH §Validation EDIT-13 row): local write-and-save, web-share write with files.write cap, 403 without cap, create file, mkdir, delete file, delete directory (recursive confirm), rename, cross-directory move, single upload, multi-file upload, 412 conflict flow, binary-file no-edit, large-file guard.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: files-write.spec.ts — 14-scenario cross-browser e2e + CSP extension</name>
  <read_first>
    frontend/e2e/files-browser.spec.ts (imports, serial config, cross-browser structure) ; frontend/e2e/fixture-env.ts (writeAppUrl, writeCap added in Plan 01) ; frontend/e2e/web-csp.spec.ts (CSP console-error assertion) ; .planning/phases/125-react-editor-codemirror-6-desktop-web/125-RESEARCH.md (§Validation EDIT-13 scenario list)
  </read_first>
  <action>
    Create frontend/e2e/files-write.spec.ts mirroring files-browser.spec.ts (imports, `test.describe.configure({ mode: 'serial' })`, cross-browser via playwright.config.ts). Add a `writeAppUrl(env)` usage for the write-enabled web-share surface (WRITE_CAP) and use the existing viewer read-only cap for the 403 scenario. Implement all 14 EDIT-13 scenarios: local write-and-save, web-share write with files.write cap, 403 without cap, create file, mkdir, delete file, delete directory (recursive confirm with file count), rename, cross-directory move, single upload, multi-file upload, 412 conflict flow (stale If-Match → conflict modal → discard/force), binary-file no-edit (Edit affordance absent), large-file guard (>500KB notice). Target the editor via the shared `data-testid="file-browser-preview"` so read/edit modes don't require branching. Extend web-csp.spec.ts to drive the editor + a write op and assert zero CSP violations (console-error scan) — covers EDIT-01's "no new CSP amendments" success criterion. Run `pnpm exec playwright install` first if browsers are absent.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec playwright test files-write.spec.ts web-csp.spec.ts && go test ./internal/webserver/... -run CodeMirror -count=1</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && pnpm exec playwright test files-write.spec.ts web-csp.spec.ts` exits 0 across Chromium, Firefox, and WebKit projects. `go test ./internal/webserver/... -run CodeMirror -count=1` exits 0. The spec file references all 14 scenario names and uses both the WRITE_CAP (write success / 412) and the read-only viewer cap (403). Zero CSP violations asserted. `grep -c "412" frontend/e2e/files-write.spec.ts` >= 1 (conflict scenario present). `grep -c -i "binary" frontend/e2e/files-write.spec.ts` >= 1 (binary-file no-edit scenario present).
  </acceptance_criteria>
  <done>All 14 EDIT-13 scenarios pass cross-browser; zero CSP violations; vendor-drift gate green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Playwright e2e suite passes the web-share surface across three browsers. Two residues are NOT Playwright-drivable on the Wails desktop (RESEARCH/VALIDATION manual-only): the CodeMirror Tab-indent / Cmd-V interaction vs the Phase 49 clipboard handler, and the desktop GUI visual render of the editor + affordances (cross-surface parity is release-blocking).</what-built>
  <how-to-verify>
    1. Build and run the desktop app (`wails dev`, since prod has DevTools disabled).
    2. Open a text file, click Edit, confirm the editor mounts with syntax highlighting; confirm Tab inserts indentation (does not move focus) and Cmd-V does not double-paste inside CM6.
    3. Save with Cmd+S; confirm the dirty bullet clears and "Saved" appears. Exercise create / mkdir / rename / delete / move / upload.
    4. Confirm the desktop UX matches the web-share UX byte-for-byte (cross-surface parity). Verify state indicators and destructive modals carry icon + literal text (you are colorblind — verify the glyph/text, not the color).
    5. Cross-check scottkw/agenthub open issues for anything filed citing "Discovered during Phase 125".
  </how-to-verify>
  <resume-signal>Type "approved" if desktop parity + Tab/Cmd-V hold, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web-share browser → cap-gated webserver write routes | the most-exposed write path under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-16 | Elevation of Privilege | viewer without files.write reaching a write route | mitigate | e2e 403-without-cap scenario asserts requireFilesWrite denies the read-only viewer cap |
| T-125-17 | Tampering | CSP bypass via editor inline styles / injected content | mitigate | web-csp.spec asserts zero CSP violations driving the editor; style-src 'unsafe-inline' already covers CM6 (no worker-src, no new amendment) |
| T-125-18 | Tampering | CodeMirror dependency drift between manifest and lockfile | mitigate | vendor_drift CodeMirror version-parity gate runs in the e2e command |
</threat_model>

<verification>
- `cd frontend && pnpm exec playwright test files-write.spec.ts web-csp.spec.ts` green across all three browsers.
- `go test ./internal/webserver/... -run CodeMirror` green.
- Desktop parity + Tab/Cmd-V human-verified (release-blocking, colorblind-safe at source).
</verification>

<success_criteria>
Playwright cross-browser passes all 14 scenarios with zero CSP violations and a green vendor-drift gate; desktop parity human-verified. (SC5)
</success_criteria>

<output>
Create `.planning/phases/125-react-editor-codemirror-6-desktop-web/125-06-SUMMARY.md` when done
</output>
