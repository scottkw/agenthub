---
phase: 125-react-editor-codemirror-6-desktop-web
plan: 03
type: execute
wave: 3
depends_on: ["125-02"]
files_modified:
  - frontend/src/lib/filesApi.ts
  - frontend/src/lib/useFilesWrite.ts
  - frontend/src/components/Editor.tsx
  - frontend/src/components/FileBrowser/EditorHeader.tsx
  - frontend/src/components/FileBrowser/SaveIndicator.tsx
  - frontend/src/components/FileBrowser/modals/UnsavedChangesModal.tsx
  - frontend/src/components/FileBrowser/modals/ConflictModal.tsx
  - frontend/src/components/FileBrowserTab.tsx
  - frontend/src/components/__tests__/Editor.test.tsx
autonomous: true
requirements: [EDIT-05, EDIT-06, EDIT-07, EDIT-08]
must_haves:
  truths:
    - "Cmd/Ctrl+S calls PUT /api/files/write with an If-Match header (etag echoed from the read ETag); browser default is prevented"
    - "A three-state save indicator shows idle / Saving… / Saved (~1.5s transient), each carrying icon + text"
    - "A dirty bullet ● + aria-label Modified appears when the buffer differs from the saved snapshot, clears on save"
    - "Navigating away (file-switch / navigate-up / tab-close) while dirty opens the Unsaved-changes modal; no beforeunload is used"
    - "A 412 response opens the conflict modal with Force overwrite / Save as new file / Discard my changes; the buffer is never silently discarded"
  artifacts:
    - path: "frontend/src/lib/useFilesWrite.ts"
      provides: "{ write, del, rename, mkdir, upload, isSaving, saveError } — write+save state here"
      contains: "isSaving"
    - path: "frontend/src/components/FileBrowser/SaveIndicator.tsx"
      provides: "role=status aria-live three-state indicator (icon+text)"
      contains: "aria-live"
    - path: "frontend/src/components/FileBrowser/modals/ConflictModal.tsx"
      provides: "412 conflict modal, three branches, buffer preserved"
      contains: "Force overwrite"
    - path: "frontend/src/components/FileBrowser/modals/UnsavedChangesModal.tsx"
      provides: "React-level unsaved guard, Keep editing default focus"
      contains: "Keep editing"
  key_links:
    - from: "Editor.tsx Cmd-S keymap"
      to: "useFilesWrite.write"
      via: "PUT with If-Match echoed from readFileText etag"
      pattern: "If-Match"
    - from: "useFilesWrite save 412"
      to: "ConflictModal"
      via: "isConflict() → open modal, buffer preserved"
      pattern: "isConflict"
---

<objective>
Save flow: Cmd/Ctrl+S → atomic write with If-Match, the three-state save indicator + dirty marker, the React-level unsaved-changes guard, and the 412 conflict modal. Wires Editor.tsx's stubbed save props from Plan 02 to a real implementation.

Purpose: Deliver the locked optimistic-concurrency save contract (EDIT-05/06/07/08) with the colorblind-safe indicators and the never-silently-discard-buffer guarantee.
Output: useFilesWrite hook, EditorHeader, SaveIndicator, UnsavedChangesModal, ConflictModal, save wiring in FileBrowserTab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-RESEARCH.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-PATTERNS.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-UI-SPEC.md

<interfaces>
<!-- Reuse verbatim. -->
QuitConfirmModal.tsx — THE modal pattern: overlay click-cancel + e.stopPropagation (57-64); role="dialog" aria-modal aria-labelledby (59-62); Escape via window keydown (26-32); SAFE-button default focus via ref+useEffect (23,35-37,95-101); acting guard (22,97-114); dotColor values #9ece6a saved / #e0af68 saving / #f7768e error (11-19).
filesApi FilesApiClient.buildQuery (102-108) + fetchOrThrow (110-122) + FilesApiError (57-85, add isConflict()=status===412, isCollision()=status===409). readFileText now returns etag (Plan 02).
Server: PUT /api/files/write returns 412 on If-Match mismatch (Plan 01). Force overwrite → re-PUT with If-Match omitted or "*". Save-as-new → PUT new path {basename}-copy{ext}, no If-Match.
SaveIndicator: role="status" aria-live="polite"; saving=ArrowPathIcon+"Saving…"; saved=CheckCircleIcon+"Saved" ~1.5s; error=ExclamationTriangleIcon. Icon+text carry meaning; color is decoration (colorblind contract). Dirty marker ● aria-label="Modified".
COPY (verbatim, UI-SPEC §Copywriting): Unsaved title "Unsaved changes" / body "You have unsaved changes. Save or discard?" / primary "Save" / secondary "Discard" / default-focus "Keep editing". Conflict heading "This file was modified by another process" / body "Saving now will overwrite the other change. Choose how to continue — your edits are preserved either way." / "Force overwrite" (destructive) / "Save as new file" (primary) / "Discard my changes" (default focus). Save error inline: "Couldn't save the file. Your changes are still here — try again."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useFilesWrite hook + filesApi writeFile + Cmd-S/dirty wiring in Editor</name>
  <read_first>
    frontend/src/lib/filesApi.ts (readFileText etag, buildQuery, fetchOrThrow, FilesApiError) ; frontend/src/lib/useFilesCapability.ts (useCallback/useState/AbortController cleanup shape) ; frontend/src/components/Editor.tsx (Plan 02 stub props for onSave/dirty)
  </read_first>
  <behavior>
    - writeFile(sid, path, body, ifMatch?) PUTs application/octet-stream with the If-Match header when provided; throws FilesApiError(412) on conflict.
    - useFilesWrite exposes { write, del, rename, mkdir, upload, isSaving, saveError } (del/rename/mkdir/upload are stubbed signatures filled in Plan 04/05; write+save fully implemented here).
    - On save 200: snapshot the saved doc, clear dirty, show Saved ~1.5s. On 412: surface conflict, buffer NOT cleared. On other error: set saveError ("Couldn't save the file. Your changes are still here — try again.").
    - Dirty = EditorState.doc.toString() !== savedSnapshot, updated on every transaction.
  </behavior>
  <action>
    Add to filesApi.ts: `isConflict()`/`isCollision()` predicates on FilesApiError, and `writeFile(sid, path, body, ifMatch?)` mirroring readFileText's URL+fetchOrThrow shape with an optional If-Match header (PATTERNS §filesApi). Create frontend/src/lib/useFilesWrite.ts following the useFilesCapability hook structure: implement `write` (sets isSaving, PUT with If-Match echoed from the read etag, handles 200/412/error per behavior) and `isSaving`/`saveError` state; declare del/rename/mkdir/upload as stub members for Plan 04/05 (keep the full return shape so consumers compile). Wire Editor.tsx: the Mod-s keymap (preventDefault, run → onSave) and the updateListener dirty computation against savedSnapshot, calling the props Plan 02 stubbed. Add unit cases to Editor.test.tsx asserting the Mod-s keymap binding and the dirty-vs-snapshot expression.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run Editor</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && pnpm test -- --run Editor` exits 0. `grep -c "isConflict" frontend/src/lib/filesApi.ts` >= 1. `grep -c "If-Match" frontend/src/lib/useFilesWrite.ts frontend/src/lib/filesApi.ts | grep -v ':0'` non-empty. `grep -c "Mod-s" frontend/src/components/Editor.tsx` >= 1.
  </acceptance_criteria>
  <done>writeFile sends If-Match; useFilesWrite drives the three-state save and dirty tracking; Cmd-S is wired in the editor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EditorHeader + SaveIndicator + UnsavedChangesModal + ConflictModal</name>
  <read_first>
    frontend/src/components/QuitConfirmModal.tsx (full pattern) ; frontend/src/components/FileBrowser/PreviewPane.tsx (preview-header 94-117) ; .planning/phases/125-react-editor-codemirror-6-desktop-web/125-UI-SPEC.md (§2,3,4 + Color colorblind table + Copywriting)
  </read_first>
  <behavior>
    - EditorHeader reuses .file-browser__preview-header: filename + path subline + DirtyMarker (● aria-label Modified) + SaveIndicator + Save (--primary) + Cancel (--secondary).
    - SaveIndicator: role=status aria-live=polite; idle shows nothing; saving = spinner icon + "Saving…"; saved = check icon + "Saved" ~1.5s; error path shows ExclamationTriangle. Color is decoration only.
    - UnsavedChangesModal: QuitConfirmModal pattern, default focus on "Keep editing"; buttons Save / Discard / Keep editing.
    - ConflictModal: QuitConfirmModal pattern, ExclamationTriangle + heading; Force overwrite (destructive, NOT default focus) / Save as new file (primary) / Discard my changes (default focus). Save-as-new opens an inline name input prefilled {basename}-copy{ext}. Buffer preserved on every branch.
  </behavior>
  <action>
    Create EditorHeader.tsx + SaveIndicator.tsx reusing the preview-header structure and the dotColor values from QuitConfirmModal (PATTERNS §EditorHeader/SaveIndicator). Create modals/UnsavedChangesModal.tsx and modals/ConflictModal.tsx by copying the QuitConfirmModal overlay/dialog/Escape/safe-default-focus/acting-guard pattern; all copy strings VERBATIM from UI-SPEC §Copywriting. Force overwrite → re-PUT with If-Match "*" (server treats "*" as skip-check, Plan 01); Save as new file → PUT {basename}-copy{ext} with no If-Match; Discard my changes → re-fetch server content + replace buffer + clear dirty. Every state carries icon+text (colorblind contract — verify at source). Wire all four into FileBrowserTab/Editor: Cmd-S 412 → ConflictModal; navigation triggers (file-switch, breadcrumb navigate-up, tab-close) route through one guardThen(action) helper that opens UnsavedChangesModal when dirty (NO beforeunload — EDIT-07). Extend Editor.test.tsx asserting the three conflict branches and the guardThen wiring on all three navigation triggers.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run Editor && cd frontend && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && pnpm test -- --run Editor` exits 0 and `pnpm exec tsc --noEmit` exits 0. `grep -c "aria-live" frontend/src/components/FileBrowser/SaveIndicator.tsx` >= 1. `grep -c "Force overwrite" frontend/src/components/FileBrowser/modals/ConflictModal.tsx` >= 1. `grep -c "Keep editing" frontend/src/components/FileBrowser/modals/UnsavedChangesModal.tsx` >= 1. `grep -c "beforeunload" frontend/src/components/FileBrowserTab.tsx frontend/src/components/Editor.tsx 2>/dev/null | grep -v ':0' | wc -l` == 0.
  </acceptance_criteria>
  <done>Editor header shows dirty + three-state save indicator; unsaved guard and 412 conflict modals work with safe default focus and verbatim copy; buffer never silently discarded; no beforeunload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client save → webserver write route | If-Match header + buffer cross here |
| 412 force-overwrite branch | user-chosen clobber of a divergent file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-07 | Tampering | force-overwrite silently clobbering a concurrent change | mitigate | force-overwrite is reachable ONLY via the explicit 412 ConflictModal user choice; default focus is the safe "Discard my changes"; buffer preserved on every branch |
| T-125-08 | DoS (user data) | buffer loss on 412/error | mitigate | buffer NEVER cleared without explicit user action (locked); save error keeps content visible inline (non-takeover) |
| T-125-09 | Tampering | line-ending corruption on save round-trip | mitigate | send the raw CM6 buffer (no CRLF→LF re-encode); octet-stream body |
</threat_model>

<verification>
- `cd frontend && pnpm test` + `pnpm exec tsc --noEmit` green.
- No `beforeunload` anywhere in the editor/tab path.
- All modal copy strings match UI-SPEC verbatim; destructive buttons never hold default focus (colorblind/safety contract — verify at source).
</verification>

<success_criteria>
Cmd/Ctrl+S saves atomically with If-Match; three-state indicator + dirty marker + React-level unsaved guard work; 412 opens the three-branch conflict modal; buffer never silently discarded. (SC2, SC3)
</success_criteria>

<output>
Create `.planning/phases/125-react-editor-codemirror-6-desktop-web/125-03-SUMMARY.md` when done
</output>
