---
phase: 125-react-editor-codemirror-6-desktop-web
plan: 02
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - frontend/package.json
  - frontend/pnpm-lock.yaml
  - frontend/src/lib/languageFor.ts
  - frontend/src/lib/filesApi.ts
  - frontend/src/lib/useFilesCapability.ts
  - frontend/src/components/Editor.tsx
  - frontend/src/components/FileBrowser/PreviewPane.tsx
  - frontend/src/components/__tests__/Editor.test.tsx
autonomous: false
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-11, EDIT-12]
must_haves:
  truths:
    - "Clicking the pencil Edit button mounts a CodeMirror 6 editor in place of the read-only preview, reusing the already-loaded text (no re-fetch)"
    - "Syntax highlighting matches file extension for Go, TS/TSX, JS/JSX, Python, JSON, YAML, Markdown, Bash/shell, HTML, CSS, Rust, C/C++"
    - "Edit button is absent for binary files and when canWrite is false"
    - "Files > 500 KB show a warn-then-proceed large-file notice; files near 5 MB mount in plain-text mode with a syntax-disabled notice"
    - "Read-only↔editable toggles via Compartment.reconfigure (no remount)"
  artifacts:
    - path: "frontend/src/components/Editor.tsx"
      provides: "CM6 mount, Compartment toggle, large-file/binary guards"
      min_lines: 60
    - path: "frontend/src/lib/languageFor.ts"
      provides: "extension → LanguageSupport (lazy), Bash via legacy-modes"
      contains: "languageFor"
    - path: "frontend/src/lib/useFilesCapability.ts"
      provides: "canWrite resolution (desktop signal / web-share probe)"
      contains: "canWrite"
    - path: "frontend/src/lib/filesApi.ts"
      provides: "readFileText returns etag from ETag header"
      contains: "etag"
  key_links:
    - from: "frontend/src/components/FileBrowser/PreviewPane.tsx"
      to: "Editor.tsx"
      via: "Edit button onEdit, gated canWrite && text/markdown && !isBinary"
      pattern: "canWrite"
    - from: "Editor.tsx"
      to: "languageFor.ts"
      via: "lazy LanguageSupport by filename extension"
      pattern: "languageFor"
user_setup: []
---

<objective>
Editor core: install CodeMirror 6, build the language detector, mount the editor with the Compartment read-only↔editable toggle, wire the pencil Edit button (gated on canWrite + text + !binary), and implement the large-file / binary guards. Extend useFilesCapability with canWrite and readFileText to capture the ETag.

Purpose: Deliver a working, syntax-highlighted, gated editor surface that downstream save/affordance plans build on.
Output: Editor.tsx, languageFor.ts, canWrite signal, ETag-aware read, Edit button in the preview header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-RESEARCH.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-PATTERNS.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-UI-SPEC.md

<interfaces>
<!-- Reuse these existing contracts; do NOT explore the codebase to rediscover them. -->
FileEntry (filesApi.ts:13-22): { name; size:number; mtime:string; mode; isDir; isSymlink; isBinary:boolean }
filesApi readFileText (filesApi.ts:146-157) returns { text; contentType; size } — EXTEND to also return `etag` from res.headers.get('etag').
useFilesCapability (useFilesCapability.ts:31-57): 4-state probe of files.read via listFiles; isMissingFilesReadPerm() maps 403. CapabilityState 4-state at line 17.
PreviewPane DownloadButton slot (PreviewPane.tsx:105-115); `.file-browser__preview-header`, `.file-browser__btn--icon`.
Heroicons: @heroicons/react/24/outline at width=14 height=14 aria-hidden="true". Editor uses PencilSquareIcon.
canWrite source (RESEARCH Open Q5 — RESOLVED): desktop = SessionInfo.FilesWrite (SetSessionFilesWrite binding shipped 124; IssueCapabilitiesResponse.homeDir present); web-share = probe the write route, mapping a files.write-denied 403 (mirror isMissingFilesReadPerm with isMissingFilesWritePerm matching "files.write").
Theme (RESEARCH Open Q4 — RESOLVED): hand-roll a small EditorView.theme({...}) using existing TokyoNight hexes from style.css (cursor #7aa2f7, gutter, selection). Do NOT use @codemirror/theme-one-dark (introduces non-TokyoNight hexes; UI-SPEC mandates zero new hexes). Drop the theme-one-dark import. Syntax token colors may use a dark default.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>About to install 17 [ASSUMED] CodeMirror packages via pnpm (RESEARCH Package Legitimacy Audit — slopcheck unavailable; org-scoped @codemirror/ packages corroborated against npm registry + codemirror.net).</what-built>
  <how-to-verify>
    1. Confirm the package list and pinned versions match RESEARCH §Standard Stack (codemirror@6.0.2, @codemirror/state@6.6.0, /view@6.43.1, /commands@6.10.3, /language@6.12.3, /language-data@6.5.2, /lang-go@6.0.1, /lang-python@6.2.1, /lang-javascript@6.2.5, /lang-json@6.0.2, /lang-yaml@6.1.3, /lang-markdown@6.5.0, /lang-html@6.4.11, /lang-css@6.3.1, /lang-rust@6.0.2, /lang-cpp@6.0.3, /legacy-modes@6.5.3).
    2. Spot-check provenance on npmjs.com/package for codemirror and one @codemirror/lang-* package (publisher = Marijn Haverbeke / codemirror org).
    3. After install, confirm pnpm-lock.yaml resolved versions + integrity hashes recorded.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or list packages to remove.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install CodeMirror 6 + build languageFor + extend readFileText/useFilesCapability</name>
  <read_first>
    frontend/package.json ; frontend/src/lib/filesApi.ts (readFileText 146-157, FilesApiError 57-85) ; frontend/src/lib/useFilesCapability.ts (probe 31-57)
  </read_first>
  <action>
    Run the pnpm add command from RESEARCH §Standard Stack (all 17 packages at the pinned versions, NOT theme-one-dark) from `frontend/`. Create frontend/src/lib/languageFor.ts: an async `languageFor(filename): Promise<Extension|LanguageSupport|[]>` using `@codemirror/language-data` `languages.find(...)` + `desc.load()` for the common languages, and `StreamLanguage.define(shell)` from `@codemirror/legacy-modes/mode/shell` for sh/bash/zsh (RESEARCH Open Q2 — @codemirror/lang-shell does not exist). Unknown extension → return `[]` (plain text). Extend filesApi.ts `readFileText` to also return `etag: res.headers.get('etag') ?? undefined`. Extend useFilesCapability.ts to additionally resolve `canWrite`: on web-share probe the write route mapping a files.write-denied 403 via a new `isMissingFilesWritePerm()`; on desktop derive from the SessionInfo.FilesWrite signal (do NOT use the list-probe for write — the daemon socket is auth-less, RESEARCH Pitfall 2). Keep canRead behaviour unchanged.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run useFilesCapability && go test ./internal/webserver/... -run CodeMirror -count=1</automated>
  </verify>
  <acceptance_criteria>
    Packages resolved in pnpm-lock.yaml. `go test ./internal/webserver/... -run CodeMirror -count=1` exits 0 (version parity now load-bearing). `cd frontend && pnpm test -- --run useFilesCapability` exits 0. `grep -c "files.write" frontend/src/lib/useFilesCapability.ts` >= 1. `grep -c "legacy-modes" frontend/src/lib/languageFor.ts` >= 1.
  </acceptance_criteria>
  <done>17 CodeMirror packages installed and version-parity-gated; languageFor maps extensions (Bash via legacy-modes); readFileText returns etag; useFilesCapability resolves canWrite per surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Editor.tsx CM6 mount + Compartment toggle + large-file/binary guards + PreviewPane Edit button</name>
  <read_first>
    frontend/src/components/FileBrowser/PreviewPane.tsx (DownloadButton slot 105-115, preview-header 94-117, state.kind) ; .planning/phases/125-react-editor-codemirror-6-desktop-web/125-RESEARCH.md (§Code Examples CM6 mount + Compartment) ; .planning/phases/125-react-editor-codemirror-6-desktop-web/125-UI-SPEC.md (§Component & Interaction Contract 1 + 6, Color, Copywriting)
  </read_first>
  <behavior>
    - Editor mounts CM6 imperatively in useEffect with the supplied initialContent (no re-fetch); read-only by default, flips to editable via Compartment.reconfigure on edit-enter.
    - Language compartment is set lazily from languageFor(filename); near 5 MB → no language pack (plain text) + "Syntax highlighting disabled for large files." caption.
    - >500 KB → render LargeFileNotice ("This is a large file ({N} MB). Edits may be slow.") warn-then-proceed (non-blocking).
    - PreviewPane shows the Pencil Edit button only when canWrite && (state.kind==='text'||'markdown') && !isBinary; binary → no button.
  </behavior>
  <action>
    Create frontend/src/components/Editor.tsx mounting CM6 per RESEARCH §Code Examples: persistent EditorView in useEffect (cleanup destroys it), `editable` and `language` Compartments, basicSetup, a hand-rolled TokyoNight `EditorView.theme` (no theme-one-dark), and the updateListener wiring left as a prop hook for Plan 03 (dirty/onSave land next wave — accept callbacks/props now, keep no-op safe defaults). Reuse PreviewPane's `.file-browser__preview` wrapper + `data-testid="file-browser-preview"` so Playwright targets the pane without read/edit branching. Implement the LargeFileNotice banner and the near-5MB plain-text mode + caption (UI-SPEC copy verbatim). In PreviewPane.tsx, add the Pencil Edit button after the Download slot, gated `canWrite && (state.kind==='text'||state.kind==='markdown') && !isBinary`, `aria-label={`Edit ${filename}`}`, calling an `onEdit` prop. Per EDIT-12, all gating reads canWrite (UI authority is UX-only; server enforces). Add a source-inspection unit test frontend/src/components/__tests__/Editor.test.tsx asserting: Compartment toggle path present, large-file thresholds (500KB/5MB) referenced, languageFor invoked, Edit button gating expression present in PreviewPane.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run Editor</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && pnpm test -- --run Editor` exits 0. `grep -c "Compartment" frontend/src/components/Editor.tsx` >= 1. `grep -c "Syntax highlighting disabled for large files" frontend/src/components/Editor.tsx` >= 1. `grep -c "theme-one-dark" frontend/src/components/Editor.tsx` == 0 (filtered: `grep -v '^//' frontend/src/components/Editor.tsx | grep -c theme-one-dark` == 0). PreviewPane Edit button gating contains `canWrite` and `isBinary`.
  </acceptance_criteria>
  <done>Editor mounts CM6 with Compartment toggle, language-by-extension, and large-file/binary guards; PreviewPane exposes the gated Edit button. Save wiring is stubbed for Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file content → CodeMirror rendering | untrusted bytes rendered in the editor |
| client canWrite gate → server | UI gate is advisory; server (requireFilesWrite) is the authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-04 | Tampering (stored XSS) | Editor.tsx rendering file content | mitigate | CM6 renders content as text via its own DOM layer, never innerHTML; no rehype-raw; verify no dangerouslySetInnerHTML in Editor.tsx |
| T-125-05 | Elevation of Privilege | canWrite UI gate bypass | accept | UI gate is UX-only; server requireFilesWrite + HasPerm(files.write) (shipped 124) is the real authority; a bypassed Edit button hits 403 |
| T-125-06 | Information Disclosure | binary file edit corruption | mitigate | isBinary → no Edit affordance anywhere (EDIT-03); binary keeps download-only path |
| T-125-SC | Tampering | 17 CodeMirror npm installs | mitigate | blocking-human legitimacy checkpoint (this plan) + vendor_drift version-parity gate (Plan 01) |
</threat_model>

<verification>
- `cd frontend && pnpm test` green for Editor + useFilesCapability.
- `go test ./internal/webserver/... -run CodeMirror` green (version parity now real).
- No new color hexes introduced; theme uses existing TokyoNight tokens (colorblind contract — verify at source).
- No dangerouslySetInnerHTML in Editor.tsx.
</verification>

<success_criteria>
Editor mounts with extension-based syntax highlighting, Compartment read-only↔editable toggle, large-file and binary guards; Edit button gated on canWrite + text + !binary. (SC1)
</success_criteria>

<output>
Create `.planning/phases/125-react-editor-codemirror-6-desktop-web/125-02-SUMMARY.md` when done
</output>
