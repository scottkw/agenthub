---
phase: 156-install-links-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/WelcomeTab.tsx
  - frontend/src/components/__tests__/WelcomeTab.install.test.tsx
  - TESTING.md
autonomous: true
requirements: [INSTALL-01, INSTALL-02]
must_haves:
  truths:
    - "Welcome screen Linux command reads the raw.githubusercontent.com install.sh URL (not the broken agenthub.dev host)"
    - "Welcome screen Windows command reads `winget install scottkw.agenthub`"
    - "Welcome screen repo link reads `github.com/scottkw/agenthub`"
    - "No placeholder URL or wrong package id remains visible anywhere on the Welcome screen"
  artifacts:
    - frontend/src/components/WelcomeTab.tsx
    - frontend/src/components/__tests__/WelcomeTab.install.test.tsx
    - TESTING.md
  key_links:
    - "WelcomeTab.tsx line 42 URL points at scripts/install.sh on main (the script created in plan 156-02)"
    - "Source-gate vitest test asserts exact host raw.githubusercontent.com and negative-asserts the dead agenthub.dev host"
---

<objective>
Correct the three wrong distribution strings on the Welcome screen and lock them with a source-gate test.

Purpose: The Welcome screen currently shows a dead Linux URL (`agenthub.dev/install.sh`), a wrong winget id (`agenthub`), and a wrong repo org (`agenthub-dev/agenthub`). These are the user-facing strings; fixing them satisfies the visible half of INSTALL-01 (the curl URL string) and all of INSTALL-02.
Output: Corrected WelcomeTab.tsx, a new vitest source-gate, and registered TESTING.md traceability/count updates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-install-links-distribution/156-RESEARCH.md
@docs/install-links-fix.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- `frontend/src/components/WelcomeTab.tsx` (modified — lines 42, 46, 54)
- `frontend/src/components/__tests__/WelcomeTab.install.test.tsx` (new vitest source-gate, 6 assertions)
- `TESTING.md` (modified — vitest suite count 126→127, Total 501→502, one new Section 4 traceability row for INSTALL-02)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WelcomeTab install-string source-gate test (Wave 0, RED)</name>
  <files>frontend/src/components/__tests__/WelcomeTab.install.test.tsx</files>
  <read_first>
    - .planning/phases/156-install-links-distribution/156-RESEARCH.md (section "WelcomeTab.install.test.tsx — Vitest Source Gate" — the exact test body to follow)
    - frontend/src/components/WelcomeTab.tsx (current wrong strings on lines 42/46/54)
    - frontend/src/components/__tests__/ (sibling tests for import/style conventions — this is a string source-gate like style.hub.test.ts, NOT a render test)
  </read_first>
  <behavior>
    - Reads WelcomeTab.tsx as a UTF-8 string via readFileSync (source-gate pattern, no render).
    - INSTALL-01 positive: source contains `https://raw.githubusercontent.com/scottkw/agenthub/main/scripts/install.sh`.
    - INSTALL-01 negative: source does NOT contain the dead host string `agenthub.dev`.
    - INSTALL-02 positive: source contains `winget install scottkw.agenthub`.
    - INSTALL-02 negative: source does NOT match the bare-id winget command (winget install agenthub without the scottkw. prefix).
    - INSTALL-02 positive: source contains `github.com/scottkw/agenthub`.
    - INSTALL-02 negative: source does NOT contain the wrong org `agenthub-dev/agenthub`.
  </behavior>
  <action>
    Create the source-gate test exactly per the RESEARCH "Vitest Source Gate" blueprint: import readFileSync/join and node:path, read `../../components/WelcomeTab.tsx`, and write six it() assertions (three toContain positives, three negative assertions). For the bare-id negative use a regex that asserts the prefixed form is required (negative-lookahead so `scottkw.agenthub` does not trip it). Group under one describe titled for INSTALL-01, INSTALL-02. Do NOT render the component — this is a static string gate consistent with the codebase's existing style.*.test.ts gates.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/components/__tests__/WelcomeTab.install.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists and is picked up by vitest.
    - At this point the run is RED: the three positive assertions fail because WelcomeTab.tsx still holds the old strings. This confirms the gate is wired to the right strings before the fix lands.
  </acceptance_criteria>
  <done>WelcomeTab.install.test.tsx exists, runs under vitest, and fails against the current (unfixed) source — proving it gates the correct strings.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the three Welcome screen strings</name>
  <files>frontend/src/components/WelcomeTab.tsx</files>
  <read_first>
    - frontend/src/components/WelcomeTab.tsx (lines 38–54 — the install grid and links block)
    - .planning/phases/156-install-links-distribution/156-RESEARCH.md (table "WelcomeTab.tsx Exact Strings" — exact before/after values)
    - docs/install-links-fix.md (locked decision: line 54 stays a text-only span, do NOT convert to a link)
  </read_first>
  <action>
    Make three exact edits in WelcomeTab.tsx, touching nothing else (line 38 macOS brew command is correct — leave it):
    1. Line 42 `<code>` body → `curl -fsSL https://raw.githubusercontent.com/scottkw/agenthub/main/scripts/install.sh | sh` (per INSTALL-01).
    2. Line 46 `<code>` body → `winget install scottkw.agenthub` (per INSTALL-02; lowercase id, case-sensitive — exactly `scottkw.agenthub`).
    3. Line 54 `<span className="welcome-tab__link">` body → `github.com/scottkw/agenthub` (per INSTALL-02). Keep it a `<span>` — text only, do not add an href.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/components/__tests__/WelcomeTab.install.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All six assertions in WelcomeTab.install.test.tsx pass (GREEN).
    - WelcomeTab.tsx line 54 is still a `<span>` (no `<a href>` introduced).
    - The macOS brew line is unchanged.
  </acceptance_criteria>
  <done>The Welcome screen source carries the corrected curl URL, `winget install scottkw.agenthub`, and `github.com/scottkw/agenthub`; the source-gate is fully green.</done>
</task>

<task type="auto">
  <name>Task 3: Register the new vitest test in TESTING.md</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md (Section 2 Suite Manifest row "vitest" count and Total row; Section 4 traceability map format; Section 6 standing convention)
    - .planning/phases/156-install-links-distribution/156-RESEARCH.md (section "TESTING.md Updates Required" — exact counts and traceability row text)
    - ./CLAUDE.md (AgentHub standing rule: repo-relative path-only in the traceability path column)
  </read_first>
  <action>
    Update TESTING.md per the standing convention for one new vitest file:
    1. Section 2 Suite Manifest: bump the vitest group count 126 → 127, and bump the Total row 501 → 502. (Plan 156-02 will take it to 503; this plan only owns the vitest delta.)
    2. Section 4 Traceability map: add one row mapping INSTALL-02 to `frontend/src/components/__tests__/WelcomeTab.install.test.tsx`, suite group vitest, note "Source-gate: raw GitHub URL, scottkw.agenthub, github.com/scottkw/agenthub present in WelcomeTab.tsx". Path column must be the repo-relative `.tsx` file path only — no test names.
    Then run the traceability path-check.
  </action>
  <verify>
    <automated>bash tests/check-traceability-paths.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/check-traceability-paths.sh` exits 0 (every mapped path, including the new row, exists on disk).
    - TESTING.md vitest count reads 127 and Total reads 502.
    - The new Section 4 row's path column is a repo-relative `.tsx` path with no test name appended.
  </acceptance_criteria>
  <done>TESTING.md registers WelcomeTab.install.test.tsx with correct counts and a valid traceability row; the path-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Welcome screen → user's shell | The displayed install command is copy-pasted into a terminal; a wrong host string would route the user's `curl \| sh` to an attacker-controlled or dead host |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-156-05 | Spoofing | WelcomeTab.tsx line 42 displayed URL | medium | mitigate | Source-gate test asserts the exact `raw.githubusercontent.com/scottkw/agenthub` host and negative-asserts the dead `agenthub.dev` host, so a regression to an unowned/wrong host fails CI |
| T-156-06 | Tampering | WelcomeTab.tsx line 46 package id | low | mitigate | Source-gate asserts exact `scottkw.agenthub` id and rejects the bare `agenthub` id (a squattable/incorrect catalog id) |
</threat_model>

<verification>
- `cd frontend && pnpm vitest run src/components/__tests__/WelcomeTab.install.test.tsx` — 6/6 green.
- `bash tests/check-traceability-paths.sh` — exits 0.
- Manual source read confirms line 54 remains a `<span>`.
</verification>

<success_criteria>
ROADMAP SC-2 met: the Welcome screen shows `winget install scottkw.agenthub` and links to `github.com/scottkw/agenthub`, the Linux curl URL points at the real raw.githubusercontent.com install.sh path, and no placeholder URL or wrong id remains — all enforced by an automated source-gate.
</success_criteria>

<output>
Create `.planning/phases/156-install-links-distribution/156-01-SUMMARY.md` when done.
</output>
