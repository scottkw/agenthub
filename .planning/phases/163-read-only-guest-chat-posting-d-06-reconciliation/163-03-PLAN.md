---
phase: 163-read-only-guest-chat-posting-d-06-reconciliation
plan: 03
type: execute
wave: 2
depends_on: [163-01, 163-02]
files_modified:
  - .planning/research/PITFALLS.md
  - TESTING.md
autonomous: true
requirements: [ROCHAT-01, ROCHAT-02, SEC-RO-01]
must_haves:
  truths:
    - "PITFALLS.md no longer states RO cap holders cannot post chat; it records the reconciled rule: RO posts chat + presence/typing, but cannot @session-inject or write PTY."
    - "TESTING.md Section 2 Suite Manifest reflects the new/flipped test counts; Section 4 has ROCHAT-01, ROCHAT-02, SEC-RO-01 traceability rows with repo-relative paths only."
    - "tests/check-traceability-paths.sh passes."
  artifacts:
    - .planning/research/PITFALLS.md
    - TESTING.md
  key_links:
    - "Section 4 path column must be a repo-relative .go/.ts/.tsx/.sh file path with no test names — enforced by tests/check-traceability-paths.sh (standing rule, agenthub/CLAUDE.md)."
  prohibitions:
    - "No Section 4 row may put a test function name in the path column."
---

<objective>
Reconcile the D-06 vs SEC-01 documentation conflict and register the test changes in the canonical regression home. This closes the SEC-RO-01 paper trail and satisfies the mandatory TESTING.md standing rule (this phase flipped + added tests).

Purpose: PITFALLS.md Pitfall 1 currently instructs "RO cap holders: read chat, cannot post" — the exact rule this phase reverses. Leaving it stale would re-introduce the gate in a future phase. TESTING.md must track the flipped/added tests so the regression suite stays authoritative.
Output: PITFALLS.md reconciled; TESTING.md Section 2 + Section 4 updated; traceability path check green.

Runs in Wave 2 because it cites the FINAL test names + counts produced by 163-01 (Go) and 163-02 (frontend).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/research/PITFALLS.md
@TESTING.md
@.planning/phases/163-read-only-guest-chat-posting-d-06-reconciliation/163-01-SUMMARY.md
@.planning/phases/163-read-only-guest-chat-posting-d-06-reconciliation/163-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile D-06 vs SEC-01 in PITFALLS.md per SEC-RO-01</name>
  <files>.planning/research/PITFALLS.md</files>
  <read_first>.planning/research/PITFALLS.md — Pitfall 1 "RO Cap Holders Bypassing Read-Only via Chat Input" (~l.11-28, esp. the "How to avoid" bullet ~l.21 "RO cap holders: read chat, cannot post"); Technical Debt Patterns table row "Client-side @session suppression for RO users" (~l.238); Security Mistakes table row "Checking only token validity ... before allowing chat post" (~l.273); "Looks Done But Isn't" RO enforcement item (~l.294); Pitfall-to-Phase Mapping row "RO cap bypass via chat / @session" (~l.324)</read_first>
  <action>
    Update PITFALLS.md so it no longer says RO cannot post chat, recording the reconciled v4.1 rule (Phase 163, user decision 2026-06-27): **RO cap holders ARE full chat participants — they post chat and emit presence/typing — but they CANNOT `@session`-inject or write PTY stdin (MsgInput).**

    Specific edits (keep the file's tone and the genuine inject/PTY warnings intact — only the chat-post stance changes):
    - Pitfall 1 "How to avoid" (~l.20-23): replace the "RO cap holders: read chat, cannot post" bullet with the reconciled rule. Keep and emphasize the `@session` bullet (RO must NOT inject; enforce write-perm before `session.Write()`) and add an explicit cross-reference: as of Phase 163 the chat-post gate was intentionally removed (D-06); the inject/PTY gate is the one that must hold. Add a short note that `HandleChatSend` no longer checks `sub.ReadOnly`, while `HandleInject` (ErrReadOnly) and MsgInput discard still do.
    - Technical Debt Patterns row (~l.238) "Client-side @session suppression for RO users": keep — it is about @session inject (still gated), not chat post. Leave unless it conflates chat-post; if it does, scope it to inject only.
    - Security Mistakes row (~l.273): reword so the risk is specifically RO `@session` inject / PTY write (not chat post). Chat post by RO is now allowed by design.
    - "Looks Done But Isn't" RO item (~l.294): reword to "RO cap holder CAN post chat (D-06) but an RO `@session` inject yields zero PTY writes" — the new verification semantics.
    - Pitfall-to-Phase Mapping row (~l.324): update the verification cell to "RO-capped client CAN post chat; @session inject → no PTY write" and add Phase 163 as the reconciliation point.

    Do not weaken any inject/PTY/sanitizer guidance — those threats are unchanged and release-blocking.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && grep -n "cannot post\|cannot post chat" .planning/research/PITFALLS.md | grep -i "RO\|read-only" ; grep -qi "Phase 163" .planning/research/PITFALLS.md</automated>
  </verify>
  <acceptance_criteria>
    - No remaining sentence asserts RO cap holders cannot post chat.
    - PITFALLS.md records the reconciled rule (RO posts chat; RO cannot @session-inject / write PTY) and cites Phase 163.
    - Inject/PTY/sanitizer guidance is unchanged in substance.
  </acceptance_criteria>
  <done>PITFALLS.md reconciled: D-06 chat-post allowed for RO, inject/PTY still gated, Phase 163 cited.</done>
</task>

<task type="auto">
  <name>Task 2: Register the flipped/added tests in TESTING.md (Section 2 + Section 4) per the standing rule</name>
  <files>TESTING.md</files>
  <read_first>TESTING.md — Section 2 Suite Manifest (~l.22-42, the running "reaching N Go / N vitest / N Playwright / N total" delta-note convention); Section 4 Requirement→Test Traceability Map (~l.94+, esp. existing CHAT-01 rows ~l.186-188 that currently describe "RO subscriber returns ErrChatReadOnly" / "RO client frame silently dropped", and SEC-01 inject rows ~l.183-184); the path-column rule (repo-relative file path only)</read_first>
  <action>
    Using the final test names + counts recorded in 163-01-SUMMARY.md and 163-02-SUMMARY.md:

    Section 2 (Suite Manifest): add a Phase 163 delta note in the running convention. The Go test files are EXTENDED in place (flips + one new function in hub_chatsend_test.go), and frontend specs are EXTENDED in place (vitest ChatPanel tests + Playwright chat-parity). State the net count change (Go function count is roughly flat — flips rename, +1 new regression-guard function; vitest +N from the new RO tests; Playwright flat — SC-3 renamed not added). Report the resulting "N Go / N vitest / N Playwright / N total" using the actual numbers from the summaries.

    Section 4 (Traceability Map): 
    - Update the existing CHAT-01 rows (~l.186-188) whose descriptions claim "RO subscriber returns ErrChatReadOnly" / "RO client frame silently dropped" — these now describe the FLIPPED behavior (RO subscriber persists + broadcasts). Update the description text; keep the repo-relative path column (`internal/relay/hub_chatsend_test.go`, `internal/relay/server_chatsend_test.go`, `internal/webserver/server_chatsend_test.go`).
    - Add new rows:
      - `ROCHAT-01` → `internal/relay/hub_chatsend_test.go` (RO subscriber HandleChatSend persists + broadcasts) and a frontend row `ROCHAT-01` → `frontend/e2e/chat-parity.spec.ts` (RO viewer Send enabled + RO message appears).
      - `ROCHAT-02` → `internal/relay/hub_chatsend_test.go` (TestHandleChatSend_ROCanPostInjectStillGated: RO HandleInject still ErrReadOnly + zero PTY) and reference the standing guards `internal/webserver/inject_test.go`, `internal/relay/server_inject_test.go`, `internal/webserver/capability_test.go`; plus a frontend row `ROCHAT-02` → `frontend/src/components/Hub/ChatPanel.test.tsx` (RO inject gesture does not call sendSessionInject).
      - `SEC-RO-01` → `internal/relay/hub_chatsend_test.go` (dual-invariant regression guard proving only chat-send was loosened).
    - Path column rule: EVERY new/edited path cell must be a single repo-relative `.go`/`.ts`/`.tsx`/`.sh` path with NO test function name in it. Put test names in the description column only.

    After editing, run the path checker.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && bash tests/check-traceability-paths.sh && grep -qE "ROCHAT-01|ROCHAT-02|SEC-RO-01" TESTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/check-traceability-paths.sh` exits 0.
    - TESTING.md Section 4 has ROCHAT-01, ROCHAT-02, and SEC-RO-01 rows; the stale CHAT-01 "ErrChatReadOnly / silently dropped" descriptions are corrected.
    - Section 2 carries a Phase 163 delta note with the actual final counts.
    - No path-column cell contains a test function name.
  </acceptance_criteria>
  <done>TESTING.md Section 2 + Section 4 updated for Phase 163; traceability path check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → future planners | Stale "RO cannot post" guidance could cause a future phase to re-introduce the gate, regressing D-06. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-05 | Repudiation | PITFALLS.md / TESTING.md drift | low | mitigate | Reconcile PITFALLS.md to the D-06 rule and register the flipped tests in TESTING.md so the regression home reflects reality; cite Phase 163. Verify: `bash tests/check-traceability-paths.sh`. |
| T-163-06 | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase — docs + test-registration edits only; no dependency surface introduced. |
</threat_model>

<verification>
- `bash tests/check-traceability-paths.sh` exits 0.
- PITFALLS.md no longer says RO cannot post chat; cites Phase 163.
- TESTING.md has ROCHAT-01/ROCHAT-02/SEC-RO-01 rows + a Section 2 Phase 163 delta note.
</verification>

<success_criteria>
The D-06 vs SEC-01 conflict is reconciled in research docs and the test changes are registered in the canonical regression home, with the path-format check passing.
</success_criteria>

<output>
Create `.planning/phases/163-read-only-guest-chat-posting-d-06-reconciliation/163-03-SUMMARY.md` when done. Record the reconciled PITFALLS.md rule and the final TESTING.md counts.
</output>
