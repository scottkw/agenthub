---
phase: 137-share-modal-cap-model
plan: 03
type: execute
wave: 2
depends_on: ["137-01", "137-02"]
files_modified:
  - frontend/src/components/SessionSharePanel.tsx
  - frontend/src/components/Hub/SessionShareModal.tsx
  - frontend/src/components/Hub/SessionCard.tsx
autonomous: true
requirements: [SHARE-01, SHARE-02, SHARE-04, SHARE-05, SHARE-06]
must_haves:
  truths:
    - "Each local Hub session card shows a Share button that opens the per-session Share modal (SHARE-01)."
    - "The modal's 'Share the session' toggle, when ON, reveals copyable RO + RW links/codes each with a QR code (SHARE-02/04)."
    - "The 'Enable remote file browsing' toggle is disabled when sharing is OFF; toggling it ON calls SetSessionBrowse then re-issues caps so URLs reflect the new perms (SHARE-03 wiring / Pitfall 1)."
    - "In local-network mode the LAN Basic Auth password is shown in the modal (SHARE-04)."
    - "The modal seeds from server truth on open and clears stale URLs on web-server restart with no regression (SHARE-05)."
    - "Remote peer cards show the Share button disabled with a lock icon + tooltip, colorblind-safe (SHARE-06 / D-13)."
    - "SessionSharePanel no longer contains the CAP-05 two-gate write opt-in (D-02/D-11)."
  artifacts:
    - path: "frontend/src/components/Hub/SessionShareModal.tsx"
      provides: "Per-card Share modal: share toggle, browse toggle, LAN password, homeDir warning, reused SessionSharePanel"
      min_lines: 80
    - path: "frontend/src/components/Hub/SessionCard.tsx"
      provides: "Share button (row5) with onShare prop + remote-disabled colorblind-safe state"
      contains: "hub-card__share"
    - path: "frontend/src/components/SessionSharePanel.tsx"
      provides: "Simplified panel (CAP-05 two-gate stripped; write link always shown when sharing ON; browseEnabled scope hint)"
      contains: "browseEnabled"
  key_links:
    - from: "frontend/src/components/Hub/SessionCard.tsx"
      to: "SessionShareModal (via onShare prop)"
      via: "Share button onClick fires onShare(session) with stopPropagation"
      pattern: "onShare"
    - from: "frontend/src/components/Hub/SessionShareModal.tsx"
      to: "wailsjs SetSessionBrowse + IssueCapabilities"
      via: "browse toggle then re-issue caps"
      pattern: "SetSessionBrowse"
    - from: "SessionShareModal"
      to: "SessionSharePanel"
      via: "renders simplified panel with RO/RW URLs/codes + browseEnabled hint"
      pattern: "SessionSharePanel"
---

<objective>
Build the GUI: a dedicated Share button on each Hub card (disabled + colorblind-safe on remote peers) that opens a new per-session Share modal. The modal reuses the simplified SessionSharePanel (CAP-05 two-gate stripped), adds the "Share the session" and "Enable remote file browsing" toggles, migrates the LAN password + reconcile/seeding lifecycle from the soon-removed DaemonManagerPanel, and retains the home-dir warning.

Purpose: Delivers the user-facing SHARE-01/02/04/05/06 surface and turns the Plan 01 frontend tests GREEN. Consumes the SetSessionBrowse binding and SessionInfo.BrowseEnabled from Plan 02.
Output: SessionShareModal.tsx (new), Share button on SessionCard, simplified SessionSharePanel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-share-modal-cap-model/137-CONTEXT.md
@.planning/phases/137-share-modal-cap-model/137-RESEARCH.md
@.planning/phases/137-share-modal-cap-model/137-PATTERNS.md
@.planning/phases/137-share-modal-cap-model/137-VALIDATION.md

<interfaces>
From Plan 02 (now available): Wails binding SetSessionBrowse(sessionID, enabled) -> Promise<void>; SessionInfo carries browseEnabled: boolean (server-truth, seeds the browse toggle on open).
Existing Wails bindings reused: IssueCapabilities(sessionId) -> { readUrl, writeUrl, readCode, writeCode, homeDir }; ToggleWebServing; GetLocalNetworkPassword; GetCapabilityQRCode; runtime ClipboardSetText, BrowserOpenURL.

Simplified SessionSharePanel props (D-11): { sessionId, readURL, writeURL, readCode, writeCode, browseEnabled? } — no ownerWriteEnabled, no allowFileEditing/showWriteConfirm. The write link row is always rendered when writeURL/writeCode are present.

SessionCard: isLocal = !hostname || hostname === '' (already derived ~line 156). Open button pattern at ~388-402 (row5, e.stopPropagation, aria-label). Add onShare?: (session: SessionInfo) => void prop.

Modal pattern: HubModal.tsx overlay + phase machine (entering/open/exiting), prefers-reduced-motion handling, focus return. Use a leaner hub-share-modal inner class (no grow-from-card animation, no terminal inert trap).

SHARE-05 lifecycle to migrate from DaemonManagerPanel.tsx: LAN password fetch (69-77), toggle-then-reissue (90-143), reconcile/server-truth seeding (176-234), restart-clears-cache (171-174).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Simplify SessionSharePanel (strip CAP-05) and add the Share button to SessionCard</name>
  <files>frontend/src/components/SessionSharePanel.tsx, frontend/src/components/Hub/SessionCard.tsx</files>
  <behavior>
    - SessionSharePanel renders the write link row whenever writeURL/writeCode are provided, with no viewer opt-in gate.
    - SessionSharePanel shows scope text "Watch + browse files" when browseEnabled is true and "Watch only — no file access" (or equivalent) when false.
    - SessionCard renders a Share button on local cards; clicking it fires onShare(session) and does not bubble to the card onClick.
    - On remote peer cards (hostname set) the Share button is disabled with a lock icon + tooltip/aria-label "Only the session owner can share" (colorblind-safe, D-13).
  </behavior>
  <read_first>
    - frontend/src/components/SessionSharePanel.tsx (props incl. ownerWriteEnabled ~59-71; allowFileEditing/showWriteConfirm/surfaceWriteLink state ~107-116; handleWriteOptin* handlers ~140-163; scope text ~236-238; the CAP-05 opt-in row + confirm dialog JSX)
    - frontend/src/components/Hub/SessionCard.tsx (lines 24-51 STATUS_CONFIG colorblind pattern; 156-157 isLocal; 388-402 Open button row5 analog)
    - .planning/phases/137-share-modal-cap-model/137-PATTERNS.md (sections SessionSharePanel.tsx strip CAP-05; SessionCard.tsx add Share button — exact JSX + colorblind-safe disabled state)
    - frontend/src/components/__tests__/SessionSharePanel.test.tsx and SessionCard.share.test.tsx (the contract from Plan 01)
  </read_first>
  <action>
    SessionSharePanel.tsx: Remove the ownerWriteEnabled prop, the allowFileEditing + showWriteConfirm state, the surfaceWriteLink derived value, and the handleWriteOptinToggle/Confirm/Cancel handlers. Remove the CAP-05 opt-in row JSX and the write-confirm dialog. Make the write link row always render when writeURL/writeCode are present. Add an optional browseEnabled?: boolean prop used only for the scope text line (Watch + browse files vs Watch only). The panel calls no toggle APIs itself — it only renders URLs/codes/QR/clipboard it receives. Keep the existing QR (GetCapabilityQRCode) and clipboard (ClipboardSetText) behavior intact (SHARE-04). Update the props interface to { sessionId, readURL, writeURL, readCode, writeCode, browseEnabled? }.
    SessionCard.tsx: Add onShare?: (session: SessionInfo) => void to the props. Import LockClosedIcon from @heroicons/react/24/outline. Add a Share button following the Open button pattern (row5): className hub-card__share; onClick={(e) => { e.stopPropagation(); onShare?.(session) }}; disabled={!isLocal}; aria-label and title = isLocal ? `Share ${name}` : 'Only the session owner can share'; when !isLocal render <LockClosedIcon aria-hidden="true" className="hub-card__share-lock" /> before the "Share" label so shape+text+tooltip carry the disabled state (color is reinforcement only — D-13). If the card onClick uses class-based guards, also add hub-card__share to the guarded closest() set (Pitfall 6) in addition to stopPropagation. Add minimal CSS hooks if needed (hub-card__share / hub-card__share-lock) consistent with the existing hub-card__open styling — final visual styling is deferred to Phase 140, so keep it functional and unobtrusive.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- SessionSharePanel SessionCard.share</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test SessionSharePanel and SessionCard.share pass.
    - SessionSharePanel.tsx contains no ownerWriteEnabled / allowFileEditing / showWriteConfirm / surfaceWriteLink symbols; write link renders unconditionally when writeURL/writeCode present.
    - SessionCard renders hub-card__share; on a remote fixture the button has disabled and aria-label/title "Only the session owner can share" plus the lock icon.
    - Clicking Share on a local card calls onShare and not the card onClick (stopPropagation verified by the Plan 01 no-bubble test).
  </acceptance_criteria>
  <done>SessionSharePanel is the simplified post-CAP-05 panel; SessionCard has a colorblind-safe Share button wired to onShare; the related Plan 01 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build SessionShareModal (toggles, LAN password, homeDir warning, server-truth lifecycle)</name>
  <files>frontend/src/components/Hub/SessionShareModal.tsx</files>
  <behavior>
    - Modal renders a "Share the session" toggle; ON calls ToggleWebServing and reveals the SessionSharePanel with RO + RW links/codes (SHARE-02).
    - "Enable remote file browsing" toggle is disabled/no-op while sharing is OFF; when sharing ON, toggling it calls SetSessionBrowse then IssueCapabilities to refresh URLs (SHARE-03 wiring / Pitfall 1), and passes browseEnabled into the panel.
    - In local mode (webServerMode local + running) the LAN Basic Auth password is fetched via GetLocalNetworkPassword and displayed (SHARE-04).
    - When session.homeDir is true, the home-dir warning shows before browse can be enabled (D-09).
    - On open with session.webEnabled true and no cached share, IssueCapabilities is called once (server-truth seeding); when webServerRunning flips false then true, cached URLs are cleared (SHARE-05). Browse toggle initial state seeds from session.browseEnabled (Plan 02 SessionInfo field).
    - Escape and click-outside dismiss the modal; focus returns to the Share button.
  </behavior>
  <read_first>
    - frontend/src/components/Hub/HubModal.tsx (lines 96-121 phase machine + prefers-reduced-motion + focus return; 168-191 overlay/dialog JSX + Escape handler)
    - frontend/src/components/DaemonManagerPanel.tsx (69-77 LAN password fetch; 90-143 toggle-then-reissue; 171-174 restart-clears-cache; 176-234 reconcile/server-truth seeding)
    - frontend/src/components/SessionSharePanel.tsx (the simplified panel from Task 1 — props it now accepts)
    - frontend/src/components/HomeDirWriteWarning.tsx (reused as-is, D-09)
    - .planning/phases/137-share-modal-cap-model/137-PATTERNS.md (section SessionShareModal.tsx NEW — overlay, phase machine, LAN password, toggle-then-reissue, reconcile, restart-clear patterns)
    - frontend/src/components/__tests__/SessionShareModal.test.tsx (the contract from Plan 01)
  </read_first>
  <action>
    Create SessionShareModal.tsx as a leaner overlay than HubModal (no grow-from-card animation, no inert terminal trap). Reuse the hub-modal-overlay phase-machine pattern (entering/open/exiting, prefers-reduced-motion, onAnimationEnd transitions, Escape + click-outside dismiss, focus return to the opener) with a simpler hub-share-modal inner class. Props: { session, webServerMode, webServerRunning, onClose }.
    State and effects (migrate from DaemonManagerPanel, scoped to the single session):
    - shareEnabled seeds from session.webEnabled; browseEnabled seeds from session.browseEnabled (Plan 02 field).
    - LAN password effect: when webServerMode === 'local' and webServerRunning, GetLocalNetworkPassword().then(setLanPassword), else clear (SHARE-04).
    - "Share the session" toggle handler: ToggleWebServing(session.id, next); on ON, IssueCapabilities to populate RO/RW URLs/codes; on OFF, clear cached share.
    - "Enable remote file browsing" toggle handler: disabled when !shareEnabled; on change call SetSessionBrowse(session.id, next), then if shareEnabled call IssueCapabilities again and update URLs (Pitfall 1 stale-URL avoidance); on IssueCapabilities error clear the cached share so a re-render refetches.
    - Server-truth seeding effect on open: if session.webEnabled and no cached share -> IssueCapabilities once. Restart-clear effect: when webServerRunning transitions, clear cached share URLs (SHARE-05).
    - Render HomeDirWriteWarning when session.homeDir is true (D-09), positioned before the browse toggle.
    - Render the simplified SessionSharePanel (Task 1) with the fetched readURL/writeURL/readCode/writeCode and the current browseEnabled hint, only when shareEnabled and caps are present.
    Keep copy/labels functional; final visual polish is deferred to Phase 140. Do not rebuild the share UI from scratch — reuse SessionSharePanel (D-11).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- SessionShareModal</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test SessionShareModal passes (share toggle reveals links; browse toggle disabled when sharing off; browse-on calls SetSessionBrowse then IssueCapabilities; LAN password shown in local mode; homeDir warning shown; server-truth seeding on open; stale URLs cleared on restart).
    - The browse toggle re-issues caps after SetSessionBrowse when sharing is ON (Pitfall 1 — no stale RO URL lacking files.read).
    - The modal seeds browseEnabled from session.browseEnabled and shareEnabled from session.webEnabled.
    - Escape and click-outside dismiss; focus returns to the opener.
  </acceptance_criteria>
  <done>SessionShareModal delivers the two-toggle Share surface with migrated LAN password + reconcile/seeding lifecycle and the retained home-dir warning; its Plan 01 tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire SessionShareModal into the Hub (open from card Share button) and run full suite</name>
  <files>frontend/src/components/Hub/SessionCard.tsx</files>
  <read_first>
    - frontend/src/components/Hub/SessionCard.tsx (the onShare prop added in Task 1; how the Hub container renders SessionCard and manages modal state — find the parent Hub view that owns card-level modals)
    - frontend/src/components/Hub/HubModal.tsx (how the Hub interactive modal is mounted/dismissed by the parent — mirror that ownership for the share modal)
    - .planning/phases/137-share-modal-cap-model/137-VALIDATION.md (full-suite sampling rule)
  </read_first>
  <action>
    Locate the Hub parent component that renders the grid of SessionCards and owns modal state (the same place HubModal is opened from a card). Add share-modal state (the session being shared, or null). Pass onShare={(session) => setShareTarget(session)} to each SessionCard. Render <SessionShareModal session={shareTarget} webServerMode={...} webServerRunning={...} onClose={() => setShareTarget(null)} /> when shareTarget is set, sourcing webServerMode/webServerRunning from the same props/store the (soon-removed) DaemonManagerPanel used. Do not remove DaemonManagerPanel or the Sessions page — that is Phase 138; this phase only ADDS the Share modal path (RESEARCH assumption A3). Ensure the Share button opens the Share modal and not the Hub interactive modal (the Task 1 stopPropagation + guard handles this). Run the full frontend suite and the Go daemon/webserver suites to confirm no cross-surface regression.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test && cd /Users/ken/dev/agenthub && go test ./internal/daemon/... ./internal/webserver/...</automated>
  </verify>
  <acceptance_criteria>
    - The Hub parent passes onShare to SessionCard and mounts SessionShareModal when a card's Share button is clicked.
    - Clicking Share opens the Share modal (not the Hub interactive modal); clicking the card body still opens the interactive modal.
    - DaemonManagerPanel / Sessions page is untouched (removal is Phase 138).
    - Full frontend suite (pnpm test) passes; go test ./internal/daemon/... ./internal/webserver/... passes (full phase gate green).
  </acceptance_criteria>
  <done>The Share button opens the per-card Share modal end-to-end; full frontend + Go daemon/webserver suites are green; the Sessions page remains until Phase 138.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner GUI (modal) -> daemon socket | Share/browse toggles call Wails bindings -> daemon; loopback-trusted |
| card ownership check | isLocal gates the Share affordance; remote peers cannot reach the share path (UI gate reinforcing SHARE-06; the backend never issues caps for sessions the daemon does not own) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-SHARE06 | Spoofing/EoP | remote peer re-share | mitigate | Share button disabled on !isLocal cards; colorblind-safe lock+tooltip (D-13); UI gate over the backend ownership truth |
| T-137-01b | Elevation of Privilege | stale RO URL after browse toggle | mitigate | Browse toggle re-issues caps after SetSessionBrowse when sharing ON (Pitfall 1); backend also clears grants on toggle (Plan 02) |
| T-137-04 | Information Disclosure | CAP-05 two-gate removal in UI | mitigate | Stripping the dead opt-in cannot grant access — backend perms are the authority; panel only renders URLs/codes it receives; documented D-02 reversal |
| T-137-07 | Tampering | CSRF on web write routes | accept (unchanged) | Frontend changes do not touch webserver write-route Origin checks |
| T-137-SC | Tampering | npm installs | accept | No new packages; LockClosedIcon comes from already-present @heroicons/react |
</threat_model>

<verification>
- pnpm test (full frontend suite) passes including the new SessionCard.share, SessionShareModal, and updated SessionSharePanel tests.
- go test ./internal/daemon/... ./internal/webserver/... passes (no cross-surface regression from the frontend wiring).
- Manual UAT (deferred, daemon-backed, per VALIDATION.md Manual-Only): present RO vs RW code in a browser to confirm read-only vs read/write file browse; verify disabled remote Share button by source inspection (colorblind-safe).
</verification>

<success_criteria>
- Local Hub cards have a working Share button; remote cards have a disabled colorblind-safe one (SHARE-01/06).
- The Share modal reveals copyable RO+RW links/codes with QR and (in local mode) the LAN password (SHARE-02/04).
- The browse toggle inherits-from-code wiring re-issues caps (SHARE-03 surface) and the SHARE-05 lifecycle (seeding, restart-clear) carries forward with no regression.
- CAP-05 two-gate stripped (D-02/D-11); home-dir warning retained (D-09).
- Full frontend + Go suites green.
</success_criteria>

<output>
Create .planning/phases/137-share-modal-cap-model/137-03-SUMMARY.md when done.
</output>
