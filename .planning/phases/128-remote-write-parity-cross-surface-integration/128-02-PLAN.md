---
phase: 128-remote-write-parity-cross-surface-integration
plan: 02
type: execute
wave: 2
depends_on: ["128-01"]
files_modified:
  - internal/tui/remote_files_client.go
  - internal/tui/files.go
  - internal/tui/remote_files_client_test.go
  - frontend/src/lib/filesApi.ts
  - frontend/src/lib/useFilesWrite.ts
  - frontend/src/components/FileBrowserTab.tsx
  - frontend/src/lib/useFilesWrite.test.ts
  - frontend/src/lib/filesApi.test.ts
autonomous: true
requirements: [RMW-05]
must_haves:
  truths:
    - "When a remote cap expires mid-edit (HTTP 401), the editor buffer is preserved (never cleared) and a distinct 'access expired' message is shown — not the generic retry copy"
    - "The 401 cap-expiry path is handled in both the Go TUI surface and the TS GUI/web surface"
    - "An in-flight upload that hits a 401 (or is aborted) cleanly removes its upload-queue entry — no stuck progress bar"
    - "Server-side temp-file cleanup on interrupted remote writes is confirmed already correct (no new server code)"
    - "The new 401 message contains no cap token (CAP-LEAK invariant holds)"
  artifacts:
    - path: "frontend/src/lib/useFilesWrite.ts"
      provides: "WriteOutcome 'expired' member + isUnauthorized() catch branch"
      contains: "expired"
    - path: "internal/tui/remote_files_client.go"
      provides: "401 typed sentinel ErrRemoteCapExpired"
      contains: "ErrRemoteCapExpired"
  key_links:
    - from: "frontend/src/lib/useFilesWrite.ts"
      to: "ACCESS_EXPIRED_MESSAGE"
      via: "isUnauthorized() catch branch returns 'expired', buffer preserved"
      pattern: "isUnauthorized"
    - from: "frontend/src/components/FileBrowserTab.tsx"
      to: "upload queue entry removal"
      via: "401/abort during upload removes the queue entry"
      pattern: "abort|onabort"
---

<objective>
Surface a distinct "access expired" message (not the generic save-error copy) when a remote cap expires mid-edit (HTTP 401), while keeping the editor buffer preserved (T-125-08, do NOT regress). Verify the in-flight upload XHR cleanly removes its queue entry on a 401/abort. Confirm the server already cleans orphaned temp files (no new server code).

Purpose: RMW-05. Cross-surface parity — the cap-expiry behavior must be equivalent in GUI/web and TUI.
Output: a `'expired'` WriteOutcome + isUnauthorized() catch branch (TS); a 401 sentinel + TUI surface copy (Go); upload-abort queue-cleanup verification; tests in both languages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-remote-write-parity-cross-surface-integration/128-RESEARCH.md
@.planning/phases/128-remote-write-parity-cross-surface-integration/128-PATTERNS.md
@.planning/phases/128-remote-write-parity-cross-surface-integration/128-01-SUMMARY.md

<interfaces>
<!-- Extracted from current source. 128-01 already added isMethodNotAllowed() + WriteOutcome 'peer-outdated'. -->

frontend/src/lib/filesApi.ts:
  isUnauthorized(): boolean  // line 69 — status === 401 (ALREADY EXISTS — reuse)
  Upload XHR: filesApi.ts line 336 (new XMLHttpRequest); load handler line 346
    (401 rejects as FilesApiError(401,...) via line 350); error handler line 354
    (FilesApiError(0,'network error')); abort handler line 358 (FilesApiError(0,'upload aborted')).

frontend/src/lib/useFilesWrite.ts:
  WriteOutcome (after 128-01): 'saved' | 'conflict' | 'error' | 'peer-outdated'  // extend with 'expired'
  write() catch block: isConflict branch line 120; (128-01 inserted isMethodNotAllowed branch);
    generic branch line 129 preserves buffer (T-125-08).

internal/tui/remote_files_client.go (after 128-01):
  ErrRemotePeerNoWriteSupport + remotePeerOutdatedMessage already added.
  4 write methods with StatusCode checks at lines 232/259/299/327.

internal/files/sandbox.go: WriteFileAtomic calls root.Remove(tmp) on every failure path (server temp cleanup — already correct).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Go RemoteFilesClient 401 sentinel + TUI "access expired" surface copy</name>
  <read_first>
    - internal/tui/remote_files_client.go (the 4 write methods; the 405 sentinel block 128-01 added is the immediate template)
    - internal/tui/files.go (TUI write-error rendering — the same place 128-01 mapped ErrRemotePeerNoWriteSupport)
    - 128-PATTERNS.md "internal/tui/remote_files_client.go (RMW-04 + RMW-05 — Go parity)" (shows the 401 sentinel alongside the 405 one)
  </read_first>
  <behavior>
    - Test: a fixture write method returning HTTP 401 makes each of the 4 write methods return the sentinel ErrRemoteCapExpired (errors.Is matches).
    - Test: the TUI write surface maps ErrRemoteCapExpired to an "access expired" message distinct from the generic error.
    - Test: a non-401/non-405 error (e.g. 500) still returns the generic formatted error (no regression).
    - Test: the 401 error string contains no cap token.
  </behavior>
  <action>
    Add a package-level sentinel `var ErrRemoteCapExpired = errors.New("your access to this remote session has expired")` in remote_files_client.go (copy wording is at Claude's discretion per assumption A3 — only the 405 string is verbatim-locked; keep it cap-free). In EACH of the 4 write methods, insert `if resp.StatusCode == http.StatusUnauthorized` BEFORE the generic `!= http.StatusOK` block (and alongside the 128-01 405 check), returning the zero response + ErrRemoteCapExpired. In internal/tui/files.go, map ErrRemoteCapExpired (errors.Is) to a distinct "access expired" surface message — NOT the generic write-error copy. Scope to write verbs only. Keep CAP-LEAK invariant.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test ./internal/tui/ -run TestRemoteFilesClient -race -count=1</automated>
  </verify>
  <done>All 4 Go write methods return ErrRemoteCapExpired on 401; TUI surface shows a distinct "access expired" message; non-401 errors unchanged; tests green with -race.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TS 401 "access expired" branch + WriteOutcome 'expired' (buffer preserved)</name>
  <read_first>
    - frontend/src/lib/useFilesWrite.ts (write() catch block; the isConflict and 128-01 isMethodNotAllowed branches are the templates; generic branch preserves buffer at line 129)
    - frontend/src/lib/filesApi.ts (isUnauthorized() already exists at line 69 — reuse)
    - frontend/src/components/FileBrowserTab.tsx (outcome dispatch — 128-01 added 'peer-outdated')
    - 128-PATTERNS.md "frontend/src/lib/useFilesWrite.ts" (Pattern 3: 401 branch BEFORE generic; buffer NEVER cleared)
  </read_first>
  <behavior>
    - Test: useFilesWrite.write() catching a FilesApiError(401) returns 'expired', sets saveError to ACCESS_EXPIRED_MESSAGE, sets saveState 'idle', and does NOT clear the edit buffer (T-125-08).
    - Test: the 401 branch fires BEFORE the generic branch (a 401 never produces the generic "Couldn't save" copy).
    - Test: existing 'conflict' (412), 'peer-outdated' (405), and generic 'error' outcomes are unchanged.
  </behavior>
  <action>
    In useFilesWrite.ts, add a module const `ACCESS_EXPIRED_MESSAGE` with discretion-chosen wording such as "Your access to this remote session has expired. Your changes are still here." (cap-free; emphasize the buffer is retained). Extend `WriteOutcome` to include `'expired'`. Insert a branch in the write() catch block — after the isMethodNotAllowed (128-01) branch and BEFORE the generic branch — that on `err instanceof FilesApiError && err.isUnauthorized()` sets saveError to ACCESS_EXPIRED_MESSAGE, sets saveState 'idle', and returns 'expired'. Do NOT clear editContent (T-125-08). In FileBrowserTab.tsx, handle the 'expired' outcome by surfacing ACCESS_EXPIRED_MESSAGE distinctly (mirror the 'conflict'/'peer-outdated' dispatch).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm exec vitest run src/lib/useFilesWrite.test.ts src/lib/filesApi.test.ts</automated>
  </verify>
  <done>WriteOutcome includes 'expired'; write() returns 'expired' on 401 with the buffer preserved and a distinct message; the 401 branch precedes the generic branch; FileBrowserTab surfaces it distinctly; vitest green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Upload-abort-on-401 queue cleanup + server temp-cleanup confirmation</name>
  <read_first>
    - frontend/src/lib/filesApi.ts (the upload XHR block lines 336-363: load handler line 350 rejects FilesApiError(401); error 354; abort 358)
    - frontend/src/components/FileBrowserTab.tsx (the upload-queue UI — where queue entries are added/removed and progress is tracked)
    - internal/files/sandbox.go (WriteFileAtomic root.Remove(tmp) on failure — server temp cleanup, already correct)
    - 128-RESEARCH.md Open Question 1 (the upload-abort verification scope)
  </read_first>
  <behavior>
    - Test: an upload XHR whose load handler sees HTTP 401 rejects with FilesApiError(401), and the caller removes that file's upload-queue entry (no stuck progress bar).
    - Test: an aborted upload (abort handler, FilesApiError(0,'upload aborted')) also removes the queue entry.
    - Test (Go, confirmation only — may already pass): WriteFileAtomic removes the temp file on a write failure path (no orphaned temp).
  </behavior>
  <action>
    Verify the in-flight upload XHR surfaces a mid-upload 401 as an abortable, queue-clearing error (Open Q1). The load handler already rejects FilesApiError(401) at filesApi.ts:350 and the abort handler exists at :358. In FileBrowserTab.tsx, ensure the upload-queue entry for a file is REMOVED (not left at partial progress) when its upload promise rejects with a FilesApiError — covering both the 401 (load branch) and abort cases. If the queue already removes entries on rejection, add the regression test only (no new wiring). Do NOT add a server cleanup goroutine — confirm via test that internal/files/sandbox.go WriteFileAtomic already removes its temp on failure (root.Remove(tmp)); cite the existing test if one covers it, otherwise add a minimal one. No silent fallbacks — let rejections propagate (CLAUDE.md let-it-crash).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm exec vitest run src/lib/filesApi.test.ts src/components/FileBrowserTab.test.tsx 2>/dev/null; cd /Users/ken/dev/agenthub && go test ./internal/files/ -run TestWriteFileAtomic -race -count=1</automated>
  </verify>
  <done>A mid-upload 401 or abort removes the upload-queue entry (verified by test); the server temp-cleanup-on-failure is confirmed by a passing Go test; no stuck progress bar; no new server cleanup code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote peer → client (write/upload) | Cap-expiry (401) crosses here mid-operation |
| in-flight upload XHR → upload queue | Abort/error must clean queue state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-03 | Information Disclosure | new 401 "access expired" message (Go + TS) | mitigate | Fixed cap-free string; no interpolated URL/cap (CAP-LEAK invariant) |
| T-128-04 | Denial of Service (disk) | orphaned temp file after interrupted remote write | accept (already mitigated) | Sandbox.WriteFileAtomic root.Remove(tmp) on every failure path — confirmed by test, no new code |
| T-128-05 | Denial of Service (client) | stuck upload-queue entry on cap-expiry | mitigate | 401/abort rejection removes the queue entry (verified) |
| T-128-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (zero new deps) |
</threat_model>

<verification>
- `go test ./internal/tui/ -run TestRemoteFilesClient -race -count=1` green
- `go test ./internal/files/ -run TestWriteFileAtomic -race -count=1` green
- `pnpm exec vitest run src/lib/useFilesWrite.test.ts src/lib/filesApi.test.ts` green
- Editor buffer preserved on 401 (T-125-08 not regressed)
- 401 message distinct from generic save error in BOTH surfaces
</verification>

<success_criteria>
RMW-05 satisfied: cap-expiry mid-edit preserves the buffer and shows a distinct "access expired" message in GUI/web and TUI; in-flight uploads clean their queue entry on 401/abort; server temp cleanup confirmed already correct.
</success_criteria>

<output>
Create `.planning/phases/128-remote-write-parity-cross-surface-integration/128-02-SUMMARY.md` when done
</output>
