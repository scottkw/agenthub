---
phase: 45-release-please-ci-signing-removal
plan: 02
type: execute
wave: 2
depends_on:
  - 45-01
files_modified: []
autonomous: false
requirements:
  - REL-01
must_haves:
  truths:
    - "RELEASE_PLEASE_TOKEN repo-level secret exists in GitHub"
    - "GitHub Actions can create and approve pull requests for this repo"
    - "Pushing a conventional commit to main triggers release-please and creates a Release PR"
  artifacts: []
  key_links:
    - from: ".github/workflows/release-please.yml"
      to: "RELEASE_PLEASE_TOKEN secret"
      via: "secrets.RELEASE_PLEASE_TOKEN"
      pattern: "token.*RELEASE_PLEASE_TOKEN"
---

<objective>
Verify end-to-end release-please functionality: ensure the PAT secret is configured, GitHub Actions PR creation is enabled, and a test push to main produces a Release PR.

Purpose: The workflow files from Plan 01 are inert without the PAT secret and correct repo settings. This plan gates Phase 45 completion on a real working Release PR.
Output: Confirmed working release-please automation on GitHub
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-release-please-ci-signing-removal/45-RESEARCH.md
@.planning/phases/45-release-please-ci-signing-removal/45-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create RELEASE_PLEASE_TOKEN PAT and configure repo settings</name>
  <what-built>Plan 01 created the release-please.yml workflow, release-please-config.json, and .release-please-manifest.json. The workflow references `secrets.RELEASE_PLEASE_TOKEN` which does not yet exist.</what-built>
  <read_first>
    .github/workflows/release-please.yml
  </read_first>
  <action>
Two manual steps required (cannot be done via CLI):

**Step 1: Create a Classic Personal Access Token**
1. Go to https://github.com/settings/tokens
2. Click "Generate new token" -> "Generate new token (classic)"
3. Note: "release-please token for scottkw/agenthub"
4. Expiration: choose an appropriate duration (90 days or custom)
5. Select scope: `repo` (full control of private repositories)
6. Click "Generate token" and copy the token value

**Step 2: Add token as repo-level secret**
1. Go to https://github.com/scottkw/agenthub/settings/secrets/actions
2. Click "New repository secret"
3. Name: `RELEASE_PLEASE_TOKEN`
4. Value: paste the PAT from Step 1
5. Click "Add secret"

**Step 3: Enable GitHub Actions PR creation**
1. Go to https://github.com/scottkw/agenthub/settings/actions
2. Scroll to "Workflow permissions"
3. Ensure "Allow GitHub Actions to create and approve pull requests" is checked
4. Click "Save" if changed
  </action>
  <verify>
    <automated>gh api repos/scottkw/agenthub/actions/secrets --jq '.secrets[].name' | grep -q RELEASE_PLEASE_TOKEN && echo "Secret exists"</automated>
  </verify>
  <acceptance_criteria>
    - `gh api repos/scottkw/agenthub/actions/secrets --jq '.secrets[].name'` output contains `RELEASE_PLEASE_TOKEN`
    - GitHub repo settings show "Allow GitHub Actions to create and approve pull requests" is enabled
  </acceptance_criteria>
  <resume-signal>Type "done" after completing all 3 steps above</resume-signal>
  <done>RELEASE_PLEASE_TOKEN secret is visible in repo secrets list; GitHub Actions PR creation is enabled</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify release-please creates a Release PR after push to main</name>
  <what-built>All release-please files are committed and the RELEASE_PLEASE_TOKEN secret is configured. After Plan 01 changes are pushed to main, release-please should detect the conventional commits since v1.7 and create a Release PR.</what-built>
  <read_first>
    .github/workflows/release-please.yml
    release-please-config.json
    .release-please-manifest.json
  </read_first>
  <action>
After Plan 01's changes are pushed to main (they contain conventional commits), release-please should automatically run. If no Release PR appears within 2 minutes:

1. Check the Actions tab: https://github.com/scottkw/agenthub/actions/workflows/release-please.yml
2. Look for a workflow run triggered by the push
3. If the run failed, check the error message (common: 403 = PR creation not enabled, token issue)
4. If the run succeeded but no PR: check if bootstrap-sha is correct and there are conventional commits after it

Expected outcome: A Release PR titled something like "chore(main): release 1.8.0" should appear in the Pull Requests tab.
  </action>
  <how-to-verify>
    1. Visit https://github.com/scottkw/agenthub/actions to confirm release-please workflow ran successfully
    2. Visit https://github.com/scottkw/agenthub/pulls to confirm a Release PR exists
    3. The Release PR title should contain "1.8.0"
    4. The Release PR should include a CHANGELOG.md update
    5. Run: `gh pr list --repo scottkw/agenthub --label "autorelease: pending"` to confirm via CLI
  </how-to-verify>
  <verify>
    <automated>gh pr list --repo scottkw/agenthub --label "autorelease: pending" --json title --jq '.[0].title' | grep -q "1.8.0" && echo "Release PR exists with correct version"</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr list --repo scottkw/agenthub --label "autorelease: pending"` returns at least one PR
    - The Release PR title contains `1.8.0`
    - The release-please workflow run at https://github.com/scottkw/agenthub/actions/workflows/release-please.yml shows a successful (green) run
  </acceptance_criteria>
  <resume-signal>Type "approved" if Release PR appeared correctly, or describe any issues</resume-signal>
  <done>A Release PR for v1.8.0 exists on GitHub, created automatically by release-please after pushing conventional commits to main</done>
</task>

</tasks>

<verification>
1. `gh api repos/scottkw/agenthub/actions/secrets --jq '.secrets[].name'` includes RELEASE_PLEASE_TOKEN
2. `gh pr list --repo scottkw/agenthub --label "autorelease: pending"` returns a Release PR
3. Release PR title contains version 1.8.0
</verification>

<success_criteria>
- RELEASE_PLEASE_TOKEN secret exists as a repo-level secret on GitHub
- GitHub Actions PR creation permission is enabled
- A Release PR for v1.8.0 was automatically created by release-please after pushing to main
- The Release PR includes CHANGELOG.md updates derived from conventional commits since v1.7
</success_criteria>

<output>
After completion, create `.planning/phases/45-release-please-ci-signing-removal/45-02-SUMMARY.md`
</output>
