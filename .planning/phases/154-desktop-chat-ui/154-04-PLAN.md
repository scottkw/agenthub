---
phase: 154-desktop-chat-ui
plan: 04
type: execute
wave: 3
depends_on: ["154-03"]
files_modified:
  - frontend/src/components/Hub/ChatBadge.tsx
  - frontend/src/components/Hub/MentionPopover.tsx
  - frontend/src/components/Hub/ChatBadge.test.tsx
  - frontend/src/components/Hub/MentionPopover.test.tsx
  - frontend/src/style.css
autonomous: true
requirements: [NOTIF-01, MENTION-01]
must_haves:
  truths:
    - ChatBadge renders nothing at count 0, the count number for normal unread, and an @ glyph (not the count) when hasMention is true — the glyph carries the mention state independent of color (NOTIF-01, D-10).
    - MentionPopover always shows @session pinned first in its own "Agent" section, above the filtered participant list, and @session is never removed by the filter text (MENTION-01, D-07).
    - ArrowUp/ArrowDown move the active option (wrapping), Enter selects it, Escape closes — keyboard-navigable (MENTION-01).
  artifacts:
    - frontend/src/components/Hub/ChatBadge.tsx (count + hasMention props).
    - frontend/src/components/Hub/MentionPopover.tsx (participants + filter + active index + onSelect/onClose).
    - chat-badge / chat-badge--mention and mention-popover__* BEM classes in style.css.
  key_links:
    - MentionPopover is bottom-anchored to the composer wrapper (no caret tracking) — positioning props supplied by the parent in plan 154-06.
    - ChatBadge is consumed by both the chat toggle (plan 154-05/06) and SessionCard (plan 154-06).
---

<objective>
Build the two interaction/chrome components: `ChatBadge.tsx` (the unread badge with a distinct colorblind-safe @mention state, used on both the chat toggle and the Hub session card) and `MentionPopover.tsx` (the `@` autocomplete with `@session` pinned first, filterable and keyboard-navigable).

Purpose: Both are self-contained and unit-testable in isolation. ChatBadge encodes the NOTIF-01/D-10 badge contract; MentionPopover encodes MENTION-01/D-07. ChatPanel (later) wires them to live state.
Output: ChatBadge.tsx, MentionPopover.tsx, their CSS, and tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-desktop-chat-ui/154-RESEARCH.md
@.planning/phases/154-desktop-chat-ui/154-PATTERNS.md
@.planning/phases/154-desktop-chat-ui/154-UI-SPEC.md
@frontend/src/components/Hub/SessionCard.tsx
@frontend/src/lib/relayClient.ts
</context>

<artifacts>
Symbols this plan produces:
- `ChatBadge` React component (`frontend/src/components/Hub/ChatBadge.tsx`) — props `{ count: number; hasMention: boolean }`.
- `MentionPopover` React component (`frontend/src/components/Hub/MentionPopover.tsx`) — props `{ participants: PresenceEntry[]; filter: string; activeIndex: number; onSelect: (alias) => void; onClose: () => void }`.
- CSS BEM classes: `chat-badge`, `chat-badge--mention`, `mention-popover`, `mention-popover__section-label`, `mention-popover__item`, `mention-popover__item--session`, `mention-popover__item--active`, `mention-popover__alias`, `mention-popover__desc`, `mention-popover__divider`, `mention-popover__session-icon`.
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatBadge.tsx — count + colorblind-safe @mention glyph state</name>
  <files>frontend/src/components/Hub/ChatBadge.tsx, frontend/src/components/Hub/ChatBadge.test.tsx, frontend/src/style.css</files>
  <read_first>
    - .planning/phases/154-desktop-chat-ui/154-PATTERNS.md "ChatBadge.tsx" section (the exact component + the SessionCard ATTN multi-signal precedent at lines 447–450)
    - .planning/phases/154-desktop-chat-ui/154-UI-SPEC.md §10 (badge shape, 18px circle, count vs @ glyph, aria-labels)
  </read_first>
  <behavior>
    - count=0 → renders null (no DOM node).
    - count=3, hasMention=false → renders "3" with aria-label "3 unread messages".
    - count=1, hasMention=false → aria-label "1 unread message" (singular).
    - count=3, hasMention=true → renders the "@" glyph (NOT "3") with aria-label "3 unread messages, including a mention".
  </behavior>
  <action>
    Create `ChatBadge.tsx` exactly per PATTERNS "ChatBadge.tsx" and UI-SPEC §10: an 18px filled circle, `--hub-accent` background, `--hub-bg` foreground, weight 600, 11px. The signal for the mention state is the `@` glyph replacing the count — color is reinforcement only (D-10, standing colorblind rule). Add `chat-badge` and `chat-badge--mention` CSS to style.css. Singular/plural the aria-label correctly. Return null at count 0.

    Create `ChatBadge.test.tsx` covering the four behaviors. TESTING.md registration is consolidated in plan 154-06.
  </action>
  <verify>
    <automated>cd frontend && pnpm test run src/components/Hub/ChatBadge.test.tsx</automated>
  </verify>
  <done>ChatBadge renders count vs @ glyph with correct aria-labels and hides at 0; tests green.</done>
  <acceptance_criteria>
    - `pnpm test run src/components/Hub/ChatBadge.test.tsx` passes all four behaviors.
    - Mention state asserted by glyph text "@" AND aria-label containing "mention" (two non-color channels).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MentionPopover.tsx — @session pinned, filterable, keyboard-navigable</name>
  <files>frontend/src/components/Hub/MentionPopover.tsx, frontend/src/components/Hub/MentionPopover.test.tsx, frontend/src/style.css</files>
  <read_first>
    - .planning/phases/154-desktop-chat-ui/154-PATTERNS.md "MentionPopover.tsx" section (role=listbox/option markup, @session pinned section, keyboard-dismiss pattern from SessionCard lines 267–296)
    - .planning/phases/154-desktop-chat-ui/154-UI-SPEC.md §8 (sections, row heights, keyboard nav contract, focus indicator)
    - frontend/src/lib/relayClient.ts (PresenceEntry interface — personKey, tailnetID, alias)
  </read_first>
  <behavior>
    - With participants [alice, bob] and filter "" the popover renders the "Agent" section with @session as the first option, then a divider, then alice and bob.
    - With filter "al" the participant list shows only alice, but @session is STILL the first pinned option (never filtered away — D-07).
    - activeIndex highlights exactly one option with mention-popover__item--active and aria-selected=true.
    - Pressing Enter (or clicking) the @session row calls onSelect with "@session"; selecting alice calls onSelect with "@alice".
    - Escape calls onClose.
  </behavior>
  <action>
    Create `MentionPopover.tsx` per PATTERNS + UI-SPEC §8. Props `{ participants: PresenceEntry[]; filter: string; activeIndex: number; onSelect: (alias: string) => void; onClose: () => void }`. Render `role="listbox"`; Section 1 is the pinned "Agent" section with the `@session` option (CommandLineIcon in `--hub-accent`, "@session" alias, "Inject into terminal" desc) — ALWAYS first, ALWAYS rendered regardless of filter (D-07). Then a divider, then Section 2 = participants filtered by `filter` (case-insensitive prefix/substring on alias). Index 0 is always @session; participant indices follow. Apply `mention-popover__item--active` + `aria-selected` to the option at `activeIndex`. Wire `onSelect`/`onClose`. Keyboard handling (Arrow/Enter/Escape navigation that mutates activeIndex) is owned by the composer in plan 154-06; this component renders from `activeIndex` and exposes `onSelect`/`onClose` — but it MUST install an Escape global-keydown listener (SessionCard pattern) so Escape closes even when focus is in the textarea. Bottom-anchor positioning (`position:absolute; bottom:100%`) is applied by the parent wrapper — accept a `style`/`className` passthrough. Add `mention-popover__*` CSS to style.css per UI-SPEC §8 (elevated surface, border, shadow matching .settings-search__results, 240px max-height scroll, focus indicator `--hub-sidebar-item-hover-bg`).

    Create `MentionPopover.test.tsx` covering the five behaviors. TESTING.md registration is consolidated in plan 154-06.
  </action>
  <verify>
    <automated>cd frontend && pnpm test run src/components/Hub/MentionPopover.test.tsx && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>MentionPopover pins @session first under all filters, renders the participant list filtered, highlights activeIndex, and fires onSelect/onClose; tests green; tsc clean.</done>
  <acceptance_criteria>
    - `pnpm test run src/components/Hub/MentionPopover.test.tsx` passes including the "filter never removes @session" assertion (D-07).
    - `pnpm exec tsc --noEmit` clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Participant aliases → popover render | Aliases (participant-controlled strings) are rendered as text in the popover and badge aria-labels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-154-07 | Spoofing | @session pinned row | medium | mitigate | @session is rendered in its own dedicated "Agent" section with a CommandLineIcon and fixed copy, structurally separated from participant rows so a participant cannot self-alias as "@session" and be mistaken for the agent target (D-07). The actual inject path is gated server-side (plan 154-01) regardless of UI. |
| T-154-08 | Tampering | alias text in DOM | low | mitigate | Aliases render as React text nodes (auto-escaped), never as HTML; no dangerouslySetInnerHTML. |
</threat_model>

<verification>
- `cd frontend && pnpm test run src/components/Hub/ChatBadge.test.tsx src/components/Hub/MentionPopover.test.tsx` green.
- `cd frontend && pnpm exec tsc --noEmit` clean.
</verification>

<success_criteria>
ChatBadge shows a colorblind-safe @mention state via glyph, MentionPopover keeps @session pinned and filterable+navigable — both proven in isolation, ready for ChatPanel to wire to live state.
</success_criteria>

<output>
Create `.planning/phases/154-desktop-chat-ui/154-04-SUMMARY.md` when done.
</output>
