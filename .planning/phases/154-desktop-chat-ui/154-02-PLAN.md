---
phase: 154-desktop-chat-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/pnpm-lock.yaml
  - frontend/src/lib/relayClient.ts
  - frontend/src/lib/relayClient.test.ts
autonomous: false
requirements: [CHAT-01]
user_setup: []
must_haves:
  truths:
    - relayClient.ts exposes encodeChatSendFrame and encodeSessionInjectFrame producing 1-byte-prefix + JSON-body frames matching the relay wire format.
    - parseServerFrame decodes a MsgChat (0x30) frame into { type:'chat', message } and a MsgInjectError (0x36) frame into { type:'inject_error', reason }.
    - RelayClient invokes onChat/onPresence/onTyping/onInjectError callbacks when the matching frame arrives, and TerminalPanel (which passes only onOutput/onOpen/onClose) still type-checks and runs unchanged.
  artifacts:
    - MSG_CHAT, MSG_CHAT_SEND, MSG_SESSION_INJECT, MSG_INJECT_ERROR constants in relayClient.ts.
    - ChatMessage TypeScript interface in relayClient.ts (alias field, NOT authorAlias).
    - frontend/package.json contains @tanstack/react-virtual ^3.14.3 and react-textarea-autosize ^8.5.9.
  key_links:
    - New RelayClientCallbacks members are all optional (?:) and invoked with optional chaining (?.()) — backward-compatible with TerminalPanel.
    - ws.onmessage dispatches every frame type via callbacks, not just output.
---

<objective>
Establish the frontend wire-protocol foundation every chat component depends on: install the two missing npm packages and extend `frontend/src/lib/relayClient.ts` to complete the MsgChat (0x30) / MsgChatSend (0x31) dispatch stubs, add the MsgSessionInject (0x35) encoder, and surface chat/presence/typing/inject_error to callers — without breaking TerminalPanel.

Purpose: Nothing else in the phase can compile until the ChatMessage type, the frame encoders, and the dispatching `ws.onmessage` exist. The two new packages (`@tanstack/react-virtual`, `react-textarea-autosize`) gate the virtualizer and composer work.
Output: installed packages, extended relayClient.ts, and relayClient.test.ts proving encode/parse/dispatch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-desktop-chat-ui/154-RESEARCH.md
@.planning/phases/154-desktop-chat-ui/154-PATTERNS.md
@frontend/src/lib/relayClient.ts
@internal/relay/protocol.go
</context>

<artifacts>
Symbols this plan produces:
- Constants `MSG_CHAT=0x30`, `MSG_CHAT_SEND=0x31`, `MSG_SESSION_INJECT=0x35`, `MSG_INJECT_ERROR=0x36` (relayClient.ts).
- `ChatMessage` interface (relayClient.ts) — `{ v, id, sessionID, authorID, alias, content, mentions?, sessionInject?, ts }`.
- Encoders `encodeChatSendFrame(content)`, `encodeSessionInjectFrame(text)` (relayClient.ts).
- Extended `ServerFrame` union: `chat` and `inject_error` variants.
- Extended `RelayClientCallbacks`: optional `onChat`, `onPresence`, `onTyping`, `onInjectError`.
- Package deps: `@tanstack/react-virtual@^3.14.3`, `react-textarea-autosize@^8.5.9`.
</artifacts>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify @tanstack/react-virtual [SUS] on npmjs.com before install</name>
  <action>BLOCKING-HUMAN gate (not auto-approvable, workflow.auto_advance is ignored). Do NOT run Task 1 install until the human confirms package legitimacy via the verification steps below.</action>
  <what-built>Nothing yet — this gate precedes the package install. RESEARCH flagged @tanstack/react-virtual as [SUS] (recency artifact: v3.14.4 published the day of research). Per the package-legitimacy gate, a SUS package requires human verification before install. react-textarea-autosize is [OK] (8 yrs, 7.7M/wk) and needs no gate.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@tanstack/react-virtual and confirm: publisher is the TanStack org, repository is github.com/TanStack/virtual, weekly downloads are in the millions, and version 3.14.3 exists.
    2. Confirm this is the canonical TanStack virtualizer architecture-locked in STATE.md (v4.1 architecture notes), not a typosquat.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or describe the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install @tanstack/react-virtual + react-textarea-autosize</name>
  <files>frontend/package.json, frontend/pnpm-lock.yaml</files>
  <read_first>
    - frontend/package.json (confirm react-markdown 10.1.0, remark-gfm ^4.0.1, rehype-sanitize ^6.0.0 already present and rehype-raw ABSENT — must stay absent for SEC-03)
  </read_first>
  <action>
    From the repo root run `pnpm --filter frontend add @tanstack/react-virtual@^3.14.3 react-textarea-autosize@^8.5.9` (pnpm is the project package manager). @tanstack/react-virtual ships its own types; react-textarea-autosize ships types too (no separate @types needed — verify after install that `import TextareaAutosize from 'react-textarea-autosize'` type-resolves; only add `-D @types/react-textarea-autosize` if tsc reports a missing-types error). Do NOT add rehype-raw under any circumstance (SEC-03). Confirm the two new entries land in frontend/package.json with the pinned ^ ranges.
  </action>
  <verify>
    <automated>cd frontend && pnpm ls @tanstack/react-virtual react-textarea-autosize | grep -E '3\.14|8\.5' && grep -c 'rehype-raw' package.json | grep -q '^0$'</automated>
  </verify>
  <done>Both packages present in package.json and node_modules; rehype-raw still absent from package.json.</done>
  <acceptance_criteria>
    - `pnpm ls @tanstack/react-virtual react-textarea-autosize` shows the installed versions.
    - `grep rehype-raw frontend/package.json` returns nothing.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend relayClient.ts — constants, ChatMessage, encoders, dispatch + tests</name>
  <files>frontend/src/lib/relayClient.ts, frontend/src/lib/relayClient.test.ts</files>
  <read_first>
    - frontend/src/lib/relayClient.ts (full file — existing MSG_* constants block lines 1–12, PresenceEntry, ServerFrame union, encodeTypingFrame/encodeAliasSetFrame builders at ~63, parseServerFrame MSG_PRESENCE case at ~104, RelayClientCallbacks interface at ~130, ws.onmessage at ~167)
    - internal/relay/protocol.go lines 247–257 (ChatMessage struct — note AuthorAlias is json:"alias", NOT "authorAlias"; mirror tags exactly)
    - .planning/phases/154-desktop-chat-ui/154-PATTERNS.md "relayClient.ts" section (the exact additions and the four code patterns to copy)
  </read_first>
  <behavior>
    - encodeChatSendFrame("hi") returns a Uint8Array whose byte[0] === 0x31 and whose remaining bytes JSON-decode to { content: "hi" }.
    - encodeSessionInjectFrame("run") returns a Uint8Array whose byte[0] === 0x35 and body decodes to { text: "run" }.
    - parseServerFrame on a 0x30 frame with a JSON ChatMessage body returns { type:'chat', message } with message.alias populated (proves the alias not authorAlias tag).
    - parseServerFrame on a malformed 0x30 body returns { type:'unknown' } (try/catch).
    - parseServerFrame on a 0x36 frame returns { type:'inject_error', reason }.
    - A RelayClient given only { onOutput } still constructs and dispatches output without throwing when a chat frame arrives (optional callback chaining) — backward compat.
  </behavior>
  <action>
    Add the four new constants (MSG_CHAT 0x30, MSG_CHAT_SEND 0x31, MSG_SESSION_INJECT 0x35, MSG_INJECT_ERROR 0x36) after the existing 0x32–0x34 block, following the one-liner hex+arrow-comment style. Add the `ChatMessage` interface mirroring the Go struct json tags exactly — CRITICAL: use `alias: string` not `authorAlias` (RESEARCH Pitfall 4). Add `encodeChatSendFrame(content: string)` and `encodeSessionInjectFrame(text: string)` by copying the `encodeTypingFrame` 1-byte-prefix-plus-JSON pattern exactly (PATTERNS lines 40–49). Extend the `ServerFrame` union with `{ type:'chat'; message: ChatMessage }` and `{ type:'inject_error'; reason: string }`. Add MSG_CHAT and MSG_INJECT_ERROR cases to `parseServerFrame` using the existing decode-slice(1)+JSON.parse+try/catch template (PATTERNS lines 52–64). Extend `RelayClientCallbacks` with OPTIONAL `onPresence?`, `onTyping?`, `onChat?`, `onInjectError?` (RESEARCH Pitfall 2 — all optional for TerminalPanel backward compat). Replace the `ws.onmessage` if-chain with a switch over `frame.type` dispatching each variant via optional chaining (`this.callbacks.onChat?.(frame.message)`, etc.); keep the existing `onOutput` behavior. This wires CHAT-01's client send/receive path.

    Create `frontend/src/lib/relayClient.test.ts` (vitest) covering the six behaviors above. TESTING.md registration is consolidated in plan 154-06.
  </action>
  <verify>
    <automated>cd frontend && pnpm test run src/lib/relayClient.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Encoders, ChatMessage type, parse cases, and dispatching callbacks exist; relayClient.test.ts green; tsc --noEmit clean (proves TerminalPanel still satisfies the callbacks interface).</done>
  <acceptance_criteria>
    - `pnpm test run src/lib/relayClient.test.ts` passes all six behaviors.
    - `pnpm exec tsc --noEmit` reports zero errors (backward compat with TerminalPanel's RelayClient construction).
    - grep confirms `alias` (not `authorAlias`) in the ChatMessage interface.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → frontend dependency tree | Supply-chain trust: new third-party packages enter the build. |
| Server WS frame → parseServerFrame | Untrusted binary frames decoded into typed ServerFrame values on the client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-154-SC | Tampering | npm install (@tanstack/react-virtual [SUS], react-textarea-autosize [OK]) | high | mitigate | Blocking-human checkpoint verifies @tanstack/react-virtual on npmjs.com (publisher, repo, downloads) before install; SUS is a recency artifact of the canonical TanStack package. rehype-raw explicitly NOT installed (SEC-03). |
| T-154-05 | Tampering | parseServerFrame JSON decode | low | mitigate | Every parse case wraps JSON.parse in try/catch returning { type:'unknown' }; malformed frames cannot throw into the dispatch loop. |
</threat_model>

<verification>
- `cd frontend && pnpm test run src/lib/relayClient.test.ts` green.
- `cd frontend && pnpm exec tsc --noEmit` clean.
- `grep rehype-raw frontend/package.json` returns nothing (SEC-03 guard).
</verification>

<success_criteria>
The frontend builds with the two new packages installed, relayClient.ts encodes/decodes chat + inject frames and dispatches all frame types through optional callbacks, TerminalPanel continues to compile unchanged, and rehype-raw remains absent.
</success_criteria>

<output>
Create `.planning/phases/154-desktop-chat-ui/154-02-SUMMARY.md` when done.
</output>
