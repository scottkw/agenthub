---
phase: 154-desktop-chat-ui
plan: 03
type: execute
wave: 2
depends_on: ["154-02"]
files_modified:
  - frontend/src/components/Hub/ChatMessage.tsx
  - frontend/src/components/Hub/ChatDaySeparator.tsx
  - frontend/src/components/Hub/ChatMessage.test.tsx
  - frontend/src/components/Hub/ChatDaySeparator.test.tsx
  - frontend/src/style.css
autonomous: true
requirements: [CHAT-02, NOTIF-02, SEC-03, CHAT-04]
must_haves:
  truths:
    - A first-in-group message renders avatar + alias + tailnet ID + HH:MM timestamp; the timestamp's title attribute is the full ISO-8601 datetime; a consecutive message from the same author collapses the header (CHAT-02, D-04 — flat left-aligned Slack-style avatar rows, not chat bubbles).
    - A message whose mentions include the current user renders THREE independent signals — a left accent bar, a background tint, and an @you chip — none of which is color-only (NOTIF-02, D-05).
    - A SessionInject:true message renders as a system-style row (separator rule + CommandLineIcon + "→ injected into terminal" caption), not a normal bubble (D-06).
    - Pasting <script>alert(1)</script> or <img src=x onerror=alert(1)> renders inert — no <script> element and no onerror attribute in the DOM (SEC-03).
  artifacts:
    - frontend/src/components/Hub/ChatMessage.tsx (default + named export, props for one ChatMessage + grouping flags + current-user detection).
    - frontend/src/components/Hub/ChatDaySeparator.tsx (presentational day-label row).
    - chat-msg__*, chat-msg--mention, chat-day-sep__* BEM classes in style.css.
  key_links:
    - ChatMessage imports the ChatMessage type from ../../lib/relayClient (plan 154-02).
    - Markdown rendered via react-markdown + remark-gfm + rehype-sanitize ONLY — no rehype-raw import anywhere (SEC-03).
    - Sticky CSS for the separator is applied by the PARENT virtualizer wrapper (plan 154-05), not inside ChatDaySeparator.
---

<objective>
Build the two pure render components for the message thread: `ChatMessage.tsx` (Slack-style avatar rows with consecutive collapse, the colorblind-safe @mention-of-me treatment, the @session inject system-row, and safe Markdown bodies) and `ChatDaySeparator.tsx` (the day-label row the virtualizer will stick to the top). This plan owns the SEC-03 safe-Markdown rendering and the two Phase-153 carry-forward visual treatments (D-05 mention row, D-06 inject indicator).

Purpose: These are the leaf components the ChatPanel virtualizer renders. They are self-contained and unit-testable without the WebSocket or virtualizer, so SEC-03 and the colorblind-safe signals get proven in isolation before integration.
Output: ChatMessage.tsx, ChatDaySeparator.tsx, their CSS, and tests including the XSS proofs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-desktop-chat-ui/154-RESEARCH.md
@.planning/phases/154-desktop-chat-ui/154-PATTERNS.md
@.planning/phases/154-desktop-chat-ui/154-UI-SPEC.md
@frontend/src/components/Hub/SessionCard.tsx
@frontend/src/lib/relayClient.ts
</context>

<artifacts>
Symbols this plan produces:
- `ChatMessage` React component (`frontend/src/components/Hub/ChatMessage.tsx`).
- `ChatDaySeparator` React component (`frontend/src/components/Hub/ChatDaySeparator.tsx`).
- Helper `tailnetIdToHue(tailnetID)` and `formatHHMM(ts)` (co-located, exported for tests).
- CSS BEM classes: `chat-msg`, `chat-msg__header`, `chat-msg__avatar`, `chat-msg__alias`, `chat-msg__tailnet-id`, `chat-msg__time`, `chat-msg__body`, `chat-msg__body--consecutive`, `chat-msg--mention`, `chat-msg__you-chip`, `chat-msg__inject-indicator`, `chat-msg__inject-rule`, `chat-msg__inject-caption`, `chat-msg__inject-icon`, `chat-day-sep`, `chat-day-sep__rule`, `chat-day-sep__label`.
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatMessage.tsx — avatar rows, @mention signals, inject indicator, safe Markdown</name>
  <files>frontend/src/components/Hub/ChatMessage.tsx, frontend/src/components/Hub/ChatMessage.test.tsx, frontend/src/style.css</files>
  <read_first>
    - .planning/phases/154-desktop-chat-ui/154-PATTERNS.md "ChatMessage.tsx" section (the row markup, @mention three-signal block, inject indicator block, avatar hue helper, safe-Markdown block — copy these patterns)
    - .planning/phases/154-desktop-chat-ui/154-UI-SPEC.md §3 (row structure), §4 (@mention-of-me), §5 (inject indicator) — authoritative layout/tokens/copy; do not re-derive
    - frontend/src/components/Hub/SessionCard.tsx lines 8–21 (heroicons import pattern: /24/outline) and lines 446–466 (ATTN multi-signal precedent)
    - frontend/src/lib/relayClient.ts (ChatMessage interface from plan 154-02)
  </read_first>
  <behavior>
    - First-in-group render shows avatar (initial from alias), alias text, tailnet ID in parens, and HH:MM time whose title attribute equals new Date(ts).toISOString().
    - isConsecutive=true render omits the header and applies chat-msg__body--consecutive (CHAT-02 collapse).
    - isMentionOfMe=true adds class chat-msg--mention AND renders a chat-msg__you-chip element AND sets a role="listitem" aria-label containing "mentioned you" (three signals: bar via CSS ::before, chip glyph, aria) (NOTIF-02 / D-05).
    - sessionInject=true renders chat-msg__inject-indicator with the CommandLineIcon and the exact caption "→ injected into terminal", and an aria-label "Injected into terminal by {alias}" (D-06).
    - Markdown body: input "<script>alert(1)</script>" produces NO <script> element in the rendered output; input "<img src=x onerror=alert(1)>" produces no onerror attribute (assert via container.querySelector('script') === null and absence of [onerror]) (SEC-03).
  </behavior>
  <action>
    Create `ChatMessage.tsx` rendering one message per UI-SPEC §3/§4/§5 and PATTERNS. Props: the ChatMessage object, `isFirstInGroup: boolean`, `isMentionOfMe: boolean` (computed by the parent from mentions.includes(currentUserTailnetID)). Header (first-in-group only): `chat-msg__avatar` with inline `background: hsl(${tailnetIdToHue(authorID)}, 55%, 45%)` and white uppercase initial; `chat-msg__alias` (weight 600); `chat-msg__tailnet-id` (muted, 11px, in parens); `chat-msg__time` (`<time>` with `title={new Date(ts).toISOString()}` and visible `formatHHMM(ts)`). When isMentionOfMe, render the `chat-msg__you-chip` after the alias and add `chat-msg--mention` to the row. When `sessionInject`, render the `chat-msg__inject-indicator` block (rule + CommandLineIcon + caption) per D-06 instead of a plain trailing line. Body: render `message.content` with `react-markdown` + `remark-gfm` + `rehype-sanitize`. DO NOT import or add rehype-raw (SEC-03); react-markdown v10 disables raw HTML by default and rehype-sanitize is belt-and-suspenders. react-markdown v10 removed the className prop — wrap in `<div className="chat-msg__body">`.

    Add the helpers `tailnetIdToHue` (deterministic hash→hue, PATTERNS) and `formatHHMM` (24h HH:MM). Export them for tests.

    Add all `chat-msg__*` / `chat-msg--mention` CSS to `frontend/src/style.css` using `--hub-*` tokens per UI-SPEC §Color: left accent bar via `.chat-msg--mention::before` (3px, `var(--hub-accent)`, position absolute left 0, full height); background tint `rgba(122, 162, 247, 0.08)`; you-chip per §4; inject indicator background `--hub-surface` with `--hub-border` rule per §5. Use BEM naming; no Tailwind, no CSS modules. Guard any animation behind `prefers-reduced-motion: reduce`.

    Comment-text discipline: the SEC-03 tests negative-grep for the literal script/onerror tokens in the rendered DOM — do NOT place those literal payload strings in any component-file comment or JSDoc (they belong only in the test file's test inputs). Describe them by concept in component comments.

    Create `ChatMessage.test.tsx` (vitest + @testing-library/react) covering the five behaviors. The SEC-03 test must assert `container.querySelector('script')` is null and no element carries an `onerror` attribute. TESTING.md registration is consolidated in plan 154-06.
  </action>
  <verify>
    <automated>cd frontend && pnpm test run src/components/Hub/ChatMessage.test.tsx</automated>
  </verify>
  <done>ChatMessage renders all four states (group head, consecutive, mention, inject) and SEC-03 XSS payloads render inert; ChatMessage.test.tsx green.</done>
  <acceptance_criteria>
    - `pnpm test run src/components/Hub/ChatMessage.test.tsx` passes including the two XSS assertions.
    - `grep -rn "rehype-raw" frontend/src` returns nothing.
    - @mention row exposes 3 channels (bar class, chip element, aria-label) verifiable in the test DOM.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChatDaySeparator.tsx — day-label row + date formatting</name>
  <files>frontend/src/components/Hub/ChatDaySeparator.tsx, frontend/src/components/Hub/ChatDaySeparator.test.tsx, frontend/src/style.css</files>
  <read_first>
    - .planning/phases/154-desktop-chat-ui/154-PATTERNS.md "ChatDaySeparator.tsx" section (component shape + formatDaySeparator helper)
    - .planning/phases/154-desktop-chat-ui/154-UI-SPEC.md §11 (day separator layout, tokens, copy "Today"/"Yesterday", 28px row height)
  </read_first>
  <behavior>
    - ChatDaySeparator with label "Today" renders a centered label with a rule on each side and aria-label "Messages from Today".
    - formatDaySeparator(ts) returns "Today" for a timestamp earlier today, "Yesterday" for yesterday, and a locale-short label ("Mon, Jun 23" style) for older dates.
  </behavior>
  <action>
    Create `ChatDaySeparator.tsx` as a presentational component taking `{ label: string }` and rendering `chat-day-sep` (centered `chat-day-sep__label` between two `chat-day-sep__rule` `<hr>`s) per UI-SPEC §11. Add and export `formatDaySeparator(tsMs: number): string` (PATTERNS helper). CRITICAL: this component does NOT set position:sticky — the sticky/absolute wrapper CSS is applied by the parent virtualizer in plan 154-05 (avoids the transform-vs-sticky conflict, RESEARCH Pitfall 1). Add `chat-day-sep__*` CSS to style.css per UI-SPEC §11 (11px dim label, `--hub-surface` opaque background so it occludes content beneath when stuck, `--hub-border` rules, 28px height).

    Create `ChatDaySeparator.test.tsx` covering the two behaviors (render structure + the three date branches via fake timers or fixed Date). TESTING.md registration is consolidated in plan 154-06.
  </action>
  <verify>
    <automated>cd frontend && pnpm test run src/components/Hub/ChatDaySeparator.test.tsx</automated>
  </verify>
  <done>ChatDaySeparator renders the labeled rule row; formatDaySeparator returns Today/Yesterday/locale-short correctly; tests green.</done>
  <acceptance_criteria>
    - `pnpm test run src/components/Hub/ChatDaySeparator.test.tsx` passes all date branches.
    - Component file contains no `position: sticky` (parent owns stickiness).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored chat content → Markdown render | Message bodies (authored by any participant, possibly hostile) are rendered as Markdown in the desktop owner's WKWebView. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-154-01 | Tampering / Spoofing | ChatMessage Markdown body | high | mitigate | react-markdown v10 (raw HTML disabled) + rehype-sanitize defaultSchema (strips script, blocks all on* handlers and javascript: protocol); rehype-raw NOT imported. SEC-03 test asserts <script> and onerror render inert. |
| T-154-06 | Information Disclosure | avatar hue / identity display | low | accept | Avatar color is supplementary; alias + tailnet ID text are always present, so a hue collision never hides identity (D-04). |
</threat_model>

<verification>
- `cd frontend && pnpm test run src/components/Hub/ChatMessage.test.tsx src/components/Hub/ChatDaySeparator.test.tsx` green.
- `cd frontend && pnpm exec tsc --noEmit` clean.
- `grep -rn "rehype-raw" frontend/src` returns nothing (SEC-03 guard).
</verification>

<success_criteria>
The thread-row components render all message states with colorblind-safe @mention and inject treatments, day separators format dates correctly, and pasted XSS payloads render completely inert — all proven by unit tests without needing the live WebSocket.
</success_criteria>

<output>
Create `.planning/phases/154-desktop-chat-ui/154-03-SUMMARY.md` when done.
</output>
