---
phase: 177-close-gap-fnl-09-wire-funnelwriteactive-through-app-go-lists
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.go
  - frontend/src/wailsjs/wailsjs/go/models.ts
autonomous: true
requirements: [FNL-09]
must_haves:
  truths:
    - "app.go SessionInfo struct carries FunnelWriteActive with json tag funnelWriteActive and NOT omitempty (false serializes)"
    - "app.go ListSessions copies s.FunnelWriteActive into every result element beside FunnelActive"
    - "models.ts SessionInfo declares funnelWriteActive: boolean AND copies this.funnelWriteActive = source[funnelWriteActive] in the constructor"
    - "frontend consumers (badge, tab icon, modal resync) receive a live true/false value on the 3s ListSessions poll"
  artifacts:
    - "app.go SessionInfo.FunnelWriteActive field (json:funnelWriteActive)"
    - "models.ts SessionInfo.funnelWriteActive field + createFrom/constructor copy"
  key_links:
    - "daemon.SessionInfo.FunnelWriteActive -> app.SessionInfo.FunnelWriteActive in the ListSessions conversion loop"
    - "app.go serialized JSON funnelWriteActive -> models.ts SessionInfo constructor deserialization"
---

<objective>
Wire the daemon's `SessionInfo.FunnelWriteActive` boolean across the two seams where it is currently dropped: the app.go Wails bridge (SessionInfo struct + ListSessions conversion) and the frontend runtime binding (`models.ts` field declaration + `createFrom` constructor copy). After this plan, the already-built, already-correct frontend consumers (Hub-card FULL ACCESS badge, session-tab write-exposure icon, share-modal teardown resync) receive a real `true`/`false` value on the existing 3s ListSessions poll and render.

Purpose: Phase 171 shipped the public read-write ("FULL ACCESS") Funnel flow end-to-end, but the persistent colorblind-safe exposure indicator is silently dead in the shipped native GUI because `funnelWriteActive` never crosses daemon -> app.go -> frontend. This is a surgical cross-phase integration gap-closure (FNL-09), NOT a new feature. Mirror the working `funnelActive` (FNL-01) reference in every spot; change nothing else.

Output: The two wiring edits — app.go (D-01, D-02) and models.ts (D-03, D-04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-close-gap-fnl-09-wire-funnelwriteactive-through-app-go-lists/177-CONTEXT.md

# The seam being wired (read these regions before editing)
@app.go
@frontend/src/wailsjs/wailsjs/go/models.ts
@frontend/src/wailsjs/go/main/App.d.ts
@internal/daemon/types.go
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)

New symbols added by this plan:
- `app.SessionInfo.FunnelWriteActive` — new Go struct field, `bool`, json tag `funnelWriteActive`, NOT omitempty (app.go, package main).
- One new copy line in `app.go` `ListSessions()`: `FunnelWriteActive: s.FunnelWriteActive,`.
- `SessionInfo.funnelWriteActive` — new field on the frontend runtime binding class (`frontend/src/wailsjs/wailsjs/go/models.ts`), new to that binding.
- One new constructor copy line in that same class: `this.funnelWriteActive = source["funnelWriteActive"];`.

Not produced here (already correct, do NOT touch): the daemon field (`internal/daemon/types.go:40`, `api.go:733`), the App.d.ts stub (already declares `funnelWriteActive` at line ~27), and all frontend consumers.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add FunnelWriteActive to the app.go Wails bridge (D-01 + D-02)</name>
  <files>app.go</files>
  <read_first>
    - app.go SessionInfo struct (~lines 31-60): note the existing `FunnelActive bool \`json:"funnelActive"\`` at ~line 52 and its Phase 165 / FNL-01 comment explaining why it is NOT omitempty.
    - app.go ListSessions conversion (~lines 495-532): note the `FunnelActive: s.FunnelActive,` copy line at ~line 521 inside the `for i, s := range sessions` loop.
    - internal/daemon/types.go (~line 40): the source-of-truth field `FunnelWriteActive bool \`json:"funnelWriteActive"\`` (do NOT change it — it is already populated at api.go:733).
  </read_first>
  <action>
    Mirror the existing `FunnelActive` handling in exactly two places in app.go, implementing D-01 and D-02.

    D-01 — SessionInfo struct: add a `FunnelWriteActive bool` field with json tag `funnelWriteActive` immediately beside the existing `FunnelActive` field (~line 52). It MUST NOT use omitempty — a `false` value must serialize so the frontend poll can detect a true->false write-share teardown flip, identical to the rule already documented on FunnelActive / BrowseEnabled / HomeDir (a silent false-drop is a UAT-class bug). Carry a short comment citing Phase 171 / FNL-09 and the Phase-171 write-share consent flow, matching the comment style of the neighboring FunnelActive field. Do not use the words v1, simplified, placeholder, or "for now" — this is the full, final wiring.

    D-02 — ListSessions conversion: inside the `for i, s := range sessions` loop, add `FunnelWriteActive: s.FunnelWriteActive,` immediately beside the existing `FunnelActive: s.FunnelActive,` copy (~line 521), so the daemon value is propagated into every result element. A short comment mirroring the neighboring FNL-01 propagation comment is appropriate.

    Run `go fmt` on app.go after editing. Do not touch any other field, the daemon package, or the frontend in this task.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && gofmt -l app.go | (! grep -q app.go) && go build ./... && grep -c 'FunnelWriteActive' app.go | grep -qx 2 && grep -q 'json:"funnelWriteActive"' app.go && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `app.go` contains exactly two `FunnelWriteActive` references: the struct field declaration and the ListSessions copy.
    - The struct tag is `json:"funnelWriteActive"` with NO omitempty.
    - `go build ./...` succeeds; `gofmt -l app.go` reports no formatting diff.
  </acceptance_criteria>
  <done>app.go SessionInfo carries FunnelWriteActive (json funnelWriteActive, no omitempty) and ListSessions copies s.FunnelWriteActive into every element; the repo builds and is gofmt-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire funnelWriteActive into the frontend runtime binding and confirm stub consistency (D-03 + D-04)</name>
  <files>frontend/src/wailsjs/wailsjs/go/models.ts</files>
  <read_first>
    - frontend/src/wailsjs/wailsjs/go/models.ts, `SessionInfo` class (~lines 216-255): the field declaration block lists `funnelActive: boolean;` at ~line 227, and the `constructor(source)` copies `this.funnelActive = source["funnelActive"];` at ~line 249. BOTH sites must gain the write sibling.
    - frontend/src/wailsjs/go/main/App.d.ts (~line 27): the hand-authored stub ALREADY declares `funnelWriteActive: boolean` — confirm it is present and keep it consistent; do not diverge the two files.
  </read_first>
  <action>
    Implement D-03 by hand-editing `frontend/src/wailsjs/wailsjs/go/models.ts` in two places inside the `SessionInfo` class, mirroring `funnelActive` exactly:

    1. Add `funnelWriteActive: boolean;` to the field declaration list, immediately beside `funnelActive: boolean;` (~line 227).
    2. Add `this.funnelWriteActive = source["funnelWriteActive"];` to the constructor, immediately beside `this.funnelActive = source["funnelActive"];` (~line 249). This constructor copy is load-bearing: without it, `createFrom` silently drops the field during deserialization even after app.go serializes it (this is the specific gap the audit flagged that the short fix-list understated).

    D-04 — do NOT run a full `wails build` / binding regeneration to produce this change; hand-edit the file. This matches the established project pattern (binding regeneration is not part of the plan/execute flow; the 170-03 deviation hand-authored the same stubs).

    Then verify (read-only) that `frontend/src/wailsjs/go/main/App.d.ts` already declares `funnelWriteActive: boolean` at ~line 27 and matches. If — and only if — it is missing, add it to keep the two consistent; per the read above it is already present, so expect NO change to App.d.ts.

    Do not touch any frontend consumer (App.tsx, SessionCard.tsx, TabBar.tsx, SessionShareModal.tsx) — they already read `session.funnelWriteActive` correctly.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && grep -c 'funnelWriteActive' src/wailsjs/wailsjs/go/models.ts | grep -qx 2 && grep -q 'this.funnelWriteActive = source\["funnelWriteActive"\]' src/wailsjs/wailsjs/go/models.ts && grep -q 'funnelWriteActive: boolean' src/wailsjs/go/main/App.d.ts && pnpm exec tsc --noEmit && pnpm exec vite build && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `models.ts` SessionInfo contains BOTH `funnelWriteActive: boolean;` (declaration) AND `this.funnelWriteActive = source["funnelWriteActive"];` (constructor) — exactly two references.
    - `App.d.ts` still declares `funnelWriteActive: boolean` (unchanged / consistent).
    - `tsc --noEmit` reports no type errors AND `vite build` completes clean (vitest alone is insufficient proof per the standing post-merge gate rule).
  </acceptance_criteria>
  <done>models.ts SessionInfo declares funnelWriteActive and copies it in the constructor; the App.d.ts stub is consistent; the frontend typechecks and builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon -> app.go RPC | Session state (including public-write exposure) crosses from the daemon into the Wails bridge; a dropped field here is a silent state-loss, not an injection vector. |
| app.go -> frontend (Wails deserialization) | Serialized JSON crosses into the frontend runtime binding; `createFrom` dropping a field silently corrupts the deserialized session state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-177-01 | Information Disclosure | app.go SessionInfo / ListSessions + models.ts createFrom | high | mitigate | The pre-fix silent false-drop means a user is NOT shown the persistent FULL ACCESS indicator while their session is publicly writable — an exposure-awareness failure. Wiring the field end-to-end (D-01..D-04, NOT omitempty so teardown flips propagate) restores the indicator; the Plan 02 Go regression guard (D-05) prevents the seam from silently regressing. |
| T-177-02 | Tampering | Wails frontend binding files | low | accept | Binding files are hand-edited (D-04) rather than regenerated; risk of hand-edit drift is accepted and mitigated by the App.d.ts consistency check plus the durable Go-side parity guard in Plan 02. No package installs in this phase (no supply-chain surface). |
</threat_model>

<verification>
- `go build ./...` succeeds.
- `app.go` has FunnelWriteActive in both the struct (json:"funnelWriteActive", no omitempty) and the ListSessions copy.
- `models.ts` SessionInfo has funnelWriteActive in both the declaration and the constructor.
- `frontend`: `pnpm exec tsc --noEmit` and `pnpm exec vite build` both succeed.
- No frontend consumer files and no daemon files were modified.
</verification>

<success_criteria>
The daemon's `FunnelWriteActive` value flows unbroken: daemon SessionInfo -> app.go SessionInfo (struct + ListSessions) -> serialized JSON -> models.ts SessionInfo (declaration + constructor). The Go project builds; the frontend typechecks and builds. The existing FULL ACCESS badge / tab icon / modal-resync consumers now receive a live boolean.
</success_criteria>

<output>
Create `.planning/phases/177-close-gap-fnl-09-wire-funnelwriteactive-through-app-go-lists/177-01-SUMMARY.md` when done.
</output>
