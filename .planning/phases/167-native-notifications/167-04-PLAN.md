---
phase: 167-native-notifications
plan: 04
type: execute
wave: 3
depends_on: ["167-03"]
files_modified:
  - frontend/src/components/SettingsTab.tsx
  - frontend/src/components/SettingsSearch.tsx
  - frontend/src/components/__tests__/SettingsTab.notify-toggle.test.tsx
  - TESTING.md
autonomous: true
requirements: [NTF-04]
must_haves:
  truths:
    - "A notify-on-waiting toggle renders in the Settings Behavior section, defaults off, and persists via SetNotifyOnWaiting"
    - "The toggle sits under #settings-behavior (NOT #settings-session-behavior) per the user correction"
    - "Settings search surfaces the notify toggle and jumps to settings-behavior"
    - "M-41 manual item + Category U + traceability rows are registered and check-traceability-paths.sh exits 0"
  artifacts:
    - "frontend/src/components/SettingsTab.tsx — notify toggle in Behavior section"
    - "frontend/src/components/SettingsSearch.tsx — search index entry"
    - "frontend/src/components/__tests__/SettingsTab.notify-toggle.test.tsx"
    - "TESTING.md — Category U + M-41 + Suite Manifest note + Traceability rows"
  key_links:
    - "toggle onChange → SetNotifyOnWaiting(next); initial state ← GetNotifyOnWaiting()"
    - "toggle rendered under <h3 id=\"settings-behavior\">, default OFF"
    - "traceability rows added for engine_notify_test.go, api_notify_test.go, app_test.go, SettingsTab.notify-toggle.test.tsx"
---

<objective>
Add the user-facing Settings toggle (NTF-04) in the **Behavior** section — per the LOCKED user
correction that overrides NTF-04's "Session Behavior" wording (decision #7) — defaulting OFF, and
register the phase's tests + the M-41 manual notification-delivery item in TESTING.md per the
project's Standing Convention.

Purpose: This closes the user-visible half of NTF-04 and satisfies the repo-root CLAUDE.md rule
that TESTING.md is the canonical regression-suite home (new test files → Suite Manifest +
Traceability; non-automatable OS delivery → a new M-NN manual item).
Output: The toggle, its test, and a complete TESTING.md registration that passes the path checker.

## Artifacts this plan produces
- `SettingsTab.tsx` — notify-on-waiting toggle (Behavior section), state hooks mirroring startMinimized
- `SettingsSearch.tsx` — one SEARCH_INDEX entry targeting `settings-behavior`
- `SettingsTab.notify-toggle.test.tsx` — render/default/load/save coverage (mirrors SettingsTab.shell-warn-toggle.test.tsx)
- `TESTING.md` — new `### Category U — Native Notifications (NTF)` + **M-41**, a §2 Suite Manifest note, and §4 traceability rows for all Phase-167 test files
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-native-notifications/167-CONTEXT.md
@.planning/phases/167-native-notifications/167-VALIDATION.md
@.planning/phases/167-native-notifications/167-03-SUMMARY.md
@frontend/src/components/SettingsTab.tsx
@TESTING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Notify-on-waiting toggle in the Behavior section + search entry</name>
  <read_first>
    - frontend/src/components/SettingsTab.tsx:15-16 (wailsjs imports), 105-109 (startMinimized state hooks), 185-189 (load useEffect), 332-344 (save handler), 443-468 (Behavior section + startMinimized toggle JSX)
    - frontend/src/components/SettingsSearch.tsx:24-40 (SEARCH_INDEX; the "Start minimized" entry targeting settings-behavior)
    - frontend/src/components/__tests__/SettingsTab.shell-warn-toggle.test.tsx (test shape to mirror)
  </read_first>
  <files>frontend/src/components/SettingsTab.tsx, frontend/src/components/SettingsSearch.tsx, frontend/src/components/__tests__/SettingsTab.notify-toggle.test.tsx</files>
  <behavior>
    - Renders a labelled toggle inside the Behavior section (under #settings-behavior), unchecked by default
    - On mount, GetNotifyOnWaiting() resolves and sets the checked state
    - Flipping the toggle calls SetNotifyOnWaiting(next) with the new boolean and reflects it in the UI
    - A SetNotifyOnWaiting rejection surfaces an inline error and does not flip the displayed state
    - Search for the toggle label returns an entry whose target is settings-behavior
  </behavior>
  <action>
    Import `GetNotifyOnWaiting`, `SetNotifyOnWaiting` from the wailsjs App bindings (added in Plan 03).
    Mirror the `startMinimized` toggle end-to-end within the **Behavior** section (`<h3 id="settings-behavior">`,
    per LOCKED decision #7 — NOT the Session Behavior section): add a `notifyOnWaiting` state plus its
    load `useEffect` (GetNotifyOnWaiting → setNotifyOnWaiting) and a save handler that calls
    SetNotifyOnWaiting(next), updates state on success, and sets an inline error on failure. Render the
    toggle row with an accessible label describing the awaiting-input notification and default-off
    semantics. Add one `SettingsSearch.tsx` SEARCH_INDEX entry for the toggle label with
    `target: 'settings-behavior'`. Write `SettingsTab.notify-toggle.test.tsx` mirroring
    `SettingsTab.shell-warn-toggle.test.tsx` to cover render/default-off/load/save/save-error.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- SettingsTab.notify-toggle && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- SettingsTab.notify-toggle` passes and `npx tsc --noEmit` is clean
    - The toggle JSX is inside the `settings-behavior` section (not `settings-session-behavior`)
    - `SettingsSearch.tsx` has an entry with `target: 'settings-behavior'` for the notify label
  </acceptance_criteria>
  <done>Default-off notify toggle renders in the Behavior section and persists via SetNotifyOnWaiting; notify-toggle test green.</done>
</task>

<task type="auto">
  <name>Task 2: Register Phase-167 tests + M-41 in TESTING.md (Standing Convention)</name>
  <read_first>
    - TESTING.md §2 Suite Manifest (line ~22-58, phase-note format), §4 Traceability Map (line ~118-135, row format), §5 Manual Checklist (Category T / M-37..M-40 precedent at ~583-613), §6 Standing Convention
    - .planning/phases/167-native-notifications/167-VALIDATION.md (the M-41 text to add verbatim)
    - tests/check-traceability-paths.sh (the gate that must exit 0)
  </read_first>
  <files>TESTING.md</files>
  <action>
    Add a §2 Suite Manifest note summarizing Phase 167's test additions (new Go files
    `internal/daemon/engine_notify_test.go`, `internal/daemon/api_notify_test.go`, root
    `app_test.go` additions; new vitest file
    `frontend/src/components/__tests__/SettingsTab.notify-toggle.test.tsx`) and update the running
    counts consistent with the existing note style. Add §4 Traceability rows mapping NTF-04 →
    `internal/daemon/engine_notify_test.go` and `internal/daemon/api_notify_test.go` and
    `frontend/src/components/__tests__/SettingsTab.notify-toggle.test.tsx`; NTF-02/NTF-03 →
    `app_test.go`; with the exact test names in the Notes column and repo-relative paths only in the
    path column. Add a new `### Category U — Native Notifications (NTF)` under §5 containing **M-41**
    exactly as specified in 167-VALIDATION.md (per-platform macOS/Windows/Linux enable→hide-to-tray→
    drive-to-waiting→confirm exactly one attributed notification; toggle-OFF confirms none; note it is
    non-automatable because CI runners have no live desktop session). Then run
    `bash tests/check-traceability-paths.sh` and ensure it exits 0.
  </action>
  <verify>
    <automated>grep -q 'Category U' TESTING.md && grep -q 'M-41' TESTING.md && bash tests/check-traceability-paths.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/check-traceability-paths.sh` exits 0
    - `### Category U — Native Notifications (NTF)` and **M-41** exist in §5
    - §4 has traceability rows for `engine_notify_test.go`, `api_notify_test.go`, `app_test.go`, and `SettingsTab.notify-toggle.test.tsx` (path column = repo-relative file paths only)
  </acceptance_criteria>
  <done>Category U + M-41 + Suite Manifest note + traceability rows added; check-traceability-paths.sh exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webview toggle → App.SetNotifyOnWaiting | Local Wails RPC flips a boolean preference; no new remote surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-08 | Tampering | SettingsTab toggle → SetNotifyOnWaiting | low | accept | Local-machine-only Wails binding; failure surfaces an inline error and leaves state unchanged (mirrors startMinimized). No remote-accessible surface added (RESEARCH Security Domain: V2-V4/V6 not applicable). |
</threat_model>

<verification>
- `pnpm test -- SettingsTab.notify-toggle` + full `pnpm test` green; `npx tsc --noEmit` clean
- `bash tests/check-traceability-paths.sh` exits 0
- Toggle confirmed in the Behavior section (default OFF)
- Full Go suite green: `go test -race -short ./...`
</verification>

<success_criteria>
Users can opt into awaiting-input notifications via a default-off toggle in Settings → Behavior;
all Phase-167 tests and the M-41 manual delivery item are registered per the Standing Convention
and the traceability path checker passes.
</success_criteria>

<output>
Create `.planning/phases/167-native-notifications/167-04-SUMMARY.md` when done
</output>
