---
status: complete
phase: 167-native-notifications
source: [167-VERIFICATION.md]
started: 2026-07-01T00:00:00.000Z
updated: 2026-07-01T13:15:00.000Z
---

## Current Test

[testing complete]

## Tests

### 1. M-41 — Cross-platform on-screen notification delivery (macOS/Windows/Linux) — RE-TEST on signed build after 167-05 hardening
expected: Exactly ONE native OS notification appears, identifying the session name and agent type, while the app window is hidden in the tray. On macOS the notification must show real "AgentHub" attribution (native UNUserNotificationCenter path, not a generic label). Repeating with the toggle OFF must produce no notification. Verify on all three target OSes (macOS, Windows, Linux). The four session-lifecycle signals (toast, tab dot, Hub-card glow, tab auto-close-on-exit) must all work and no longer die together.
result: issue
reported: "On a signed production build (macOS): NotifyOnWaiting toggle ON, drove a Claude Code session non-waiting→waiting with the GUI minimized/another app frontmost — NO native toast appeared. Confirmed across multiple configurations: (a) app focused, (b) GUI minimized, (c) macOS notification permission for AgentHub was 'Off' initially with no prompt ever shown, (d) permission manually set to Allow + app restarted + backgrounded. The dot on the tab, the glowing border on the Hub card, and the 'Needs Input' chip DID fire correctly. Only the toast is missing. The 167-05 crash is fixed (no more simultaneous signal death); this is the separate live-delivery failure."
severity: blocker
note: "The crash-regression signals (tab dot, Hub glow, Needs Input chip, tab auto-close) are all healthy now — 167-05 hardening confirmed. The remaining failure is that the native macOS notification never displays."

## Summary

total: 1
passed: 0
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "When a session is awaiting input the native OS toast fires (toggle ON, window hidden), the attention dot appears on the session tab, the Hub card shows its glowing 'waiting' border, and the tab auto-closes when the session process exits."
  status: resolved
  resolved_by: "167-05 (commits f5caa4b4, cebf30eb) — hardened the darwin native notification path (bundle-id guard + @try/@catch, log-and-swallow when unbundled) so the always-on tray poller can no longer crash the GUI process. Live-delivery re-test on a signed build tracked as pending Test 1 above."
  original_status_was: failed
  reason: "User reported all four session-lifecycle signals broke together (toast, tab dot, Hub card glow, tab auto-close-on-exit). Testing under `wails dev`."
  severity: major
  test: 1
  root_cause: "Phase 167 wired an unguarded native macOS UNUserNotificationCenter call (tray_objc_darwin.m) into the always-on 5s tray poller via App.maybeNotifyWaiting, gated by the NotifyOnWaiting toggle. Under `wails dev` the process is unbundled (no bundle id), so the first `waiting` transition with the toggle ON raises an uncaught NSInternalInconsistencyException ('bundleProxyForCurrentProcess is nil') that crashes the entire GUI process. All four signals live in that one process (WebView + per-session pollSessionStatus goroutines + 3s ListSessions React poll + tray poller), so they die simultaneously. Latent until M-41 flipped the toggle ON (default OFF). Genuine code regression (unguarded crash-prone native call in an always-on poller) but with an environment-sensitive trigger: fires under unbundled `wails dev`, likely latent on a signed production .app with a valid bundle id + notification entitlement."
  artifacts:
    - path: "tray_objc_darwin.m"
      issue: "Lines ~160-181: unguarded [UNUserNotificationCenter currentNotificationCenter] call — no bundle-id guard, no @try/@catch. The actual fault site."
    - path: "app.go"
      issue: "maybeNotifyWaiting + its call at refreshTrayState (~line 1456): new always-on trigger that reaches the fault site when notifyOnWaiting is ON."
    - path: "notification_darwin.go"
      issue: "cgo Go wrapper for the native call."
  missing:
    - "Guard the native path on [[NSBundle mainBundle] bundleIdentifier] != nil and wrap the UNUserNotificationCenter block in @try/@catch, making sendNotification a log-and-swallow no-op when there is no valid bundle (mirror the beeep Win/Linux log-and-swallow contract)."
    - "Re-run M-41 on a signed production build (not `wails dev`) after hardening."
  debug_session: ".planning/debug/waiting-signals-regression.md"

- truth: "On a signed production macOS build with the NotifyOnWaiting toggle ON and AgentHub backgrounded, a session transitioning to `waiting` fires exactly one native OS notification (NTF-01..04 headline deliverable)."
  status: failed
  reason: "User reported: no native toast ever appears on a signed build, even with macOS notification permission granted, the app restarted, and the window backgrounded. The tab dot / Hub-card glow / 'Needs Input' chip all fire correctly, so the daemon status-detection and the whole waiting-detection UI path work — only the native notification is missing."
  severity: blocker
  test: 1
  root_cause: "UNKNOWN — needs runtime instrumentation. The Go-side trigger path was verified CORRECT by code reading during UAT and is NOT the fault: engine.ListSessions() (internal/daemon/engine.go:521) populates SessionInfo.Status from the live heuristic detector (e.sessionStatuses) — the SAME 'waiting' value the frontend chip uses; GET /sessions → DaemonClient.ListSessions() → App.ListSessions() (app.go:439) preserves Status; the 5s tray poller App.refreshTrayState() (app.go:1443, background-independent) calls maybeNotifyWaiting (app.go:163) which correctly edge-detects non-waiting→'waiting' (gated only by notifyOnWaiting.Load()) and calls sendNotification. Status-string constants match end-to-end (status.StatusWaiting == 'waiting'). So the failure is downstream of the trigger, in the native delivery/authorization layer, OR in unobservable runtime state (is notifyOnWaiting actually true in-process? is sendNotification actually being invoked? does requestAuthorization return granted?). The native path has NO observability on its failure paths, which is itself a defect and blocks black-box diagnosis."
  artifacts:
    - path: "tray_objc_darwin.m"
      issue: "sendNotification (lines 177-210): calls requestAuthorizationWithOptions on EVERY send and silently returns at `if (!granted) return;` (line 190) with NO NSLog — a denied/undecided permission kills the toast invisibly. No UNUserNotificationCenterDelegate.willPresentNotification is registered, so macOS suppresses the banner entirely while AgentHub is the frontmost app. No success/attempt logging anywhere on the happy path."
    - path: "app.go"
      issue: "domReady init (~line 230-233) loads notifyOnWaiting ONCE via GetNotifyOnWaiting() and only stores on err==nil — if the daemon is not ready at that instant the cache stays false silently and is never refreshed for the process lifetime. maybeNotifyWaiting/sendNotification emit no log on the fire path, so there is no way to confirm from outside whether the trigger reached the native call."
    - path: "SettingsTab.tsx / SetNotifyOnWaiting"
      issue: "Enabling the in-app toggle does NOT proactively request macOS notification authorization — authorization is only lazily requested on the next waiting-transition, so the permission prompt is easy to miss and, once macOS records a denial, the feature is silently dead with no user-facing remediation hint."
  missing:
    - "Add logging on ALL native-notification paths (attempt, not-granted, authorization error, success) so delivery is diagnosable — the #1 blocker to root-causing this."
    - "Request UNUserNotificationCenter authorization PROACTIVELY when the user enables the NotifyOnWaiting toggle (SetNotifyOnWaiting), surfacing the prompt then; if authorization is denied, show an in-app hint to enable it in System Settings → Notifications."
    - "Register a UNUserNotificationCenterDelegate implementing willPresentNotification returning .banner/.list/.sound so notifications also display while AgentHub is the frontmost app."
    - "Verify at runtime (with the new logging) that notifyOnWaiting is true in-process and that maybeNotifyWaiting actually invokes sendNotification on the non-waiting→waiting edge on a signed build; consider refreshing the notifyOnWaiting cache on SetNotifyOnWaiting AND re-loading if the initial GetNotifyOnWaiting errored."
    - "Re-run M-41 on the signed build after instrumentation confirms where delivery breaks."
