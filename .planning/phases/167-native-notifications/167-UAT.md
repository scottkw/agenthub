---
status: diagnosed
phase: 167-native-notifications
source: [167-VERIFICATION.md]
started: 2026-07-01T00:00:00.000Z
updated: 2026-07-01T18:30:00.000Z
---

## Current Test

[testing complete]

## Tests

### 1. M-41 — Cross-platform on-screen notification delivery (macOS/Windows/Linux)
expected: Exactly ONE native OS notification appears, identifying the session name and agent type, while the app window is hidden in the tray. On macOS the notification must show real "AgentHub" attribution (native UNUserNotificationCenter path, not a generic label). Repeating with the toggle OFF must produce no notification. Verify on all three target OSes (macOS, Windows, Linux).
result: issue
reported: "Well, we have had a regression. Not only did the toast alert not appear, but the dot on the tab and the glowing border on the Hub card are no longer working when a session is awaiting input. [Follow-up] Exiting a shell session and a Claude Code session also no longer closes the tab. Testing was done with `wails dev`, not a build."
severity: major

## Summary

total: 1
passed: 0
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "When a session is awaiting input the native OS toast fires (toggle ON, window hidden), the attention dot appears on the session tab, the Hub card shows its glowing 'waiting' border, and the tab auto-closes when the session process exits."
  status: failed
  reason: "User reported all four session-lifecycle signals broke together (toast, tab dot, Hub card glow, tab auto-close-on-exit). Testing under `wails dev`."
  severity: major
  test: 1
  root_cause: "Phase 167 wired an unguarded native macOS UNUserNotificationCenter call (tray_objc_darwin.m) into the always-on 5s tray poller via App.maybeNotifyWaiting, gated by the NotifyOnWaiting toggle. Under `wails dev` the process is unbundled (no bundle id), so the first `waiting` transition with the toggle ON raises an uncaught NSInternalInconsistencyException ('bundleProxyForCurrentProcess is nil') that crashes the entire GUI process. All four signals live in that one process (WebView + per-session pollSessionStatus goroutines + 3s ListSessions React poll + tray poller), so they die simultaneously. Latent until M-41 flipped the toggle ON (default OFF). Genuine code regression (unguarded crash-prone native call in an always-on poller) but with an environment-sensitive trigger: fires under unbundled `wails dev`, likely latent on a signed production .app with a valid bundle id + notification entitlement."
  artifacts:
    - path: "tray_objc_darwin.m"
      issue: "Lines ~160-181: unguarded [UNUserNotificationCenter currentNotificationCenter] call — no bundle-id guard, no @try/@catch. The actual fault site."
    - path: "app.go"
      issue: "maybeNotifyWaiting + its call at refreshTrayState (~line 1456): new always-on trigger that reaches the fault site when notifyOnWaiting is ON."
    - path: "notification_darwin.go"
      issue: "cgo Go wrapper for the native call."
  missing:
    - "Guard the native path on [[NSBundle mainBundle] bundleIdentifier] != nil and wrap the UNUserNotificationCenter block in @try/@catch, making sendNotification a log-and-swallow no-op when there is no valid bundle (mirror the beeep Win/Linux log-and-swallow contract)."
    - "Re-run M-41 on a signed production build (not `wails dev`) after hardening."
  debug_session: ".planning/debug/waiting-signals-regression.md"
