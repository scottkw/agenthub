---
phase: 68
slug: agent-tailscale-discovery-install-instructions
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-11
---

# Phase 68 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — hand-rolled BEM CSS in `frontend/src/style.css` |
| Preset | not applicable |
| Component library | none (custom React components) |
| Icon library | @heroicons/react v2.2.0 |
| Font | Cascadia Code / Fira Code / monospace (existing body font) |

**Source:** `frontend/package.json`, `frontend/src/style.css` lines 53–54 (verified in codebase).

No `components.json` found. Project uses hand-authored CSS with a TokyoNight-inspired dark color palette. shadcn gate: not applicable — this is a Wails desktop app (not Next.js/Vite SPA standalone). No initialization needed.

---

## Scope of UI Work in This Phase

Phase 68 has exactly one frontend change:

**INST-01**: Replace the macOS install `<code>` block in `WelcomeTab.tsx` line 94 from:
```
brew install agenthub
```
to:
```
brew tap scottkw/agenthub && brew install --cask agenthub
```

All other changes in this phase (DISC-01, DISC-02, DISC-03) are purely Go backend — no new UI components, no new CSS classes, no new interactions.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Install grid row gap (`.welcome-tab__install-grid` gap: 8px) |
| md | 16px | Logo bottom margin |
| lg | 24px | Daemon panel padding |
| xl | 32px | Welcome content horizontal padding |
| 2xl | 48px | Welcome content top/bottom padding |
| 3xl | 64px | Major page-level spacing |

No spacing changes in this phase — INST-01 is a string replacement only.

**Source:** `style.css` lines 1013–1094 (verified in codebase).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 13px | 400 (regular) | 1.6 |
| Label | 12px | 600 (semibold) | 1.0 |
| Heading | 11px | 600 (semibold) | 1.0 (uppercase + letter-spacing 0.1em) |
| Code | 12px | 400 (regular) | 1.0 |

**Source:** `style.css` lines 1047–1094 (verified). The `welcome-tab__code` element uses 12px at weight 400 in monospace font. The `welcome-tab__heading` uses 11px at weight 600 with `text-transform: uppercase`. No typography changes are needed for INST-01 — the updated command string renders in the existing `.welcome-tab__code` style.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#1a1b26` | App background, welcome tab background |
| Secondary (30%) | `#16161e` | Tab bar, code block backgrounds, sidebar |
| Accent (10%) | `#7aa2f7` | Platform labels (macOS/Linux/Windows), links, update banner border |
| Destructive | `#f7768e` | Error states only (existing; not used in this phase) |

Accent reserved for: platform label text (`.welcome-tab__install-label`), hyperlinks, update banner left border, download button background. No new accent use introduced in this phase.

**Source:** `style.css` lines 1077–1094 and 50–54 (verified in codebase). The macOS/Linux/Windows install label color `#7aa2f7` is unchanged by INST-01 — only the code string inside `.welcome-tab__code` changes.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | No interactive CTA in INST-01. The `<code>` element is selectable text, not a button. |
| macOS install command | `brew tap scottkw/agenthub && brew install --cask agenthub` |
| Empty state heading | Not applicable — WelcomeTab always renders content |
| Empty state body | Not applicable |
| Error state | Not applicable — no error states introduced in this phase |
| Destructive confirmation | None — no destructive actions in this phase |

**Interaction contract for INST-01:**
- The `<code>` block is plain selectable text — no copy button, no click handler.
- GitHub issue #14 explicitly stated: "just && them together." No additional UI affordance needed.
- Element: `<code className="welcome-tab__code">brew tap scottkw/agenthub && brew install --cask agenthub</code>`

**Source:** RESEARCH.md Pattern 2 (verified), GitHub issue #14 intent confirmed in research.

---

## Component Inventory

| Component | File | Change |
|-----------|------|--------|
| WelcomeTab | `frontend/src/components/WelcomeTab.tsx` line 94 | String replacement only |
| WelcomeTab test | `frontend/src/components/__tests__/WelcomeTab.test.tsx` line 33 | Update assertion to include `brew tap scottkw/agenthub` |

No new components. No new CSS classes. No new style rules.

**Existing class to use:** `.welcome-tab__code` — defined in `style.css` lines 1085–1094. Applies monospace font, 12px, `#a9b1d6` text color, `#16161e` background, 1px `#292e42` border, 4px border-radius.

---

## Test Assertion Update (INST-01)

The existing test at line 33 of `WelcomeTab.test.tsx`:
```ts
expect(raw).toContain('brew install agenthub')
```

This test still passes after the change (substring match). However, a new assertion is required to lock in the tap command:
```ts
expect(raw).toContain('brew tap scottkw/agenthub && brew install --cask agenthub')
```

The old assertion may optionally be replaced or kept — keeping it is safe since the new string is a superset.

**Source:** RESEARCH.md Pitfall 5 (verified).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn |
| third-party | none | not applicable |

No third-party registries. No vetting required.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
