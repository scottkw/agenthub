---
phase: 68-agent-tailscale-discovery-install-instructions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/WelcomeTab.tsx
  - frontend/src/components/__tests__/WelcomeTab.test.tsx
autonomous: true
requirements:
  - INST-01

must_haves:
  truths:
    - "Welcome screen macOS install command shows brew tap scottkw/agenthub && brew install --cask agenthub"
    - "WelcomeTab test asserts the full brew tap + install --cask command"
  artifacts:
    - path: "frontend/src/components/WelcomeTab.tsx"
      provides: "Updated macOS install command"
      contains: "brew tap scottkw/agenthub && brew install --cask agenthub"
    - path: "frontend/src/components/__tests__/WelcomeTab.test.tsx"
      provides: "Test asserting brew tap command"
      contains: "brew tap scottkw/agenthub"
  key_links:
    - from: "frontend/src/components/__tests__/WelcomeTab.test.tsx"
      to: "frontend/src/components/WelcomeTab.tsx"
      via: "raw source import for assertion"
      pattern: "WelcomeTab\\.tsx\\?raw"
---

<objective>
Update the WelcomeTab macOS install command to show the correct single-line Homebrew tap + cask install command, and update the test to assert the full command.

Purpose: Users can copy a single command that both adds the tap and installs the cask, instead of the current incorrect `brew install agenthub` (which would fail without the tap).

Output: Updated `WelcomeTab.tsx` with corrected install command, updated test asserting the brew tap portion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-agent-tailscale-discovery-install-instructions/68-RESEARCH.md
@.planning/phases/68-agent-tailscale-discovery-install-instructions/68-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/components/WelcomeTab.tsx line 92-95 (current):
```tsx
<div className="welcome-tab__install-item">
  <span className="welcome-tab__install-label">macOS</span>
  <code className="welcome-tab__code">brew install agenthub</code>
</div>
```

From frontend/src/components/__tests__/WelcomeTab.test.tsx line 32-33 (current):
```ts
it('includes installation instructions for macOS', () => {
  expect(raw).toContain('brew install agenthub')
})
```

CSS class `.welcome-tab__code` is defined in style.css lines 1085-1094:
monospace, 12px, #a9b1d6 text, #16161e background, 1px #292e42 border, 4px radius.
No CSS changes needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update macOS install command and test assertion</name>
  <files>frontend/src/components/WelcomeTab.tsx, frontend/src/components/__tests__/WelcomeTab.test.tsx</files>
  <read_first>
    - frontend/src/components/WelcomeTab.tsx (must see line 94 to confirm exact current string before replacing)
    - frontend/src/components/__tests__/WelcomeTab.test.tsx (must see line 32-33 to confirm current assertion before updating)
  </read_first>
  <action>
**WelcomeTab.tsx line 94** — Replace the macOS install `<code>` content:

Before:
```tsx
<code className="welcome-tab__code">brew install agenthub</code>
```

After:
```tsx
<code className="welcome-tab__code">brew tap scottkw/agenthub && brew install --cask agenthub</code>
```

This is a pure string replacement. No structural change, no new elements, no CSS changes. The `.welcome-tab__code` class handles all styling.

**WelcomeTab.test.tsx lines 32-33** — Update the macOS install test to assert the full tap + cask command:

Before:
```ts
it('includes installation instructions for macOS', () => {
  expect(raw).toContain('brew install agenthub')
})
```

After:
```ts
it('includes installation instructions for macOS', () => {
  expect(raw).toContain('brew tap scottkw/agenthub && brew install --cask agenthub')
})
```

The old assertion `toContain('brew install agenthub')` would still pass (substring), but updating it to the full string prevents false positives if someone later changes the command back to just `brew install`. The new assertion locks the exact command format.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- --reporter=verbose 2>&1 | head -80</automated>
  </verify>
  <acceptance_criteria>
    - `WelcomeTab.tsx` contains `brew tap scottkw/agenthub && brew install --cask agenthub`
    - `WelcomeTab.tsx` does NOT contain a standalone `brew install agenthub` without `tap` prefix on the same line
    - `WelcomeTab.test.tsx` contains `brew tap scottkw/agenthub && brew install --cask agenthub`
    - `pnpm test` in frontend directory exits 0
  </acceptance_criteria>
  <done>macOS install command updated to single copyable brew tap + install --cask command. Test locks the full command string. INST-01 is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Display-only | The code element is read-only display text — no execution, no clipboard API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-04 | Information Disclosure | Homebrew tap name in UI | accept | The tap name `scottkw/agenthub` is public (GitHub repo scottkw/homebrew-agenthub). Displaying it in install instructions is the intended purpose. |
</threat_model>

<verification>
- `cd frontend && pnpm test` — all tests pass
- `grep "brew tap scottkw/agenthub" frontend/src/components/WelcomeTab.tsx` returns 1 match
- `grep "brew tap scottkw/agenthub" frontend/src/components/__tests__/WelcomeTab.test.tsx` returns 1 match
</verification>

<success_criteria>
- WelcomeTab macOS install section displays `brew tap scottkw/agenthub && brew install --cask agenthub`
- Test asserts the full brew tap + cask command
- All frontend tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/68-agent-tailscale-discovery-install-instructions/68-02-SUMMARY.md`
</output>
