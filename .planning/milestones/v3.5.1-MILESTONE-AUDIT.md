---
milestone: v3.5.1
audited: 2026-06-16
status: passed
scores:
  requirements: 11/11
  phases: 2/2
  integration: 11/11
  flows: 2/2
gaps: {}
tech_debt:
  - phase: tooling
    items:
      - "wails CLI v2.10.2 is older than the repo-pinned wails v2.12.0 — every `wails dev`/`wails build` rewrites go.mod down to v2.10.2 (reverted each time during this milestone). Reconcile by upgrading the CLI (`go install github.com/wailsapp/wails/v2/cmd/wails@v2.12.0`) or aligning the pin."
      - "Production `wails build` binding regeneration fails on this CLI (`xattr: No such file: .../wailsbindings`); build requires `-skipbindings` since wailsjs bindings are hand-maintained."
      - "Plain `wails build` emits a malformed ad-hoc signature (Identifier=a.out, resources not sealed) → 'damaged' on other Macs; must re-sign with the Developer ID Application cert before distribution."
  - phase: "130 (Phase 122 origin)"
    items:
      - "Orphaned Go RPC: `GetRemoteSessions` (app.go) retained + still bound but no longer the primary frontend path (GetRemoteSessionsWithMeta replaced it). Tested by TestNilClientGetRemoteSessions; not dead, but a candidate for removal."
  - phase: "129 (bookkeeping)"
    items:
      - "129-03 SUMMARY frontmatter does not cite the literal DNS-01/DNS-02/DNS-03 IDs (requirements verified via VERIFICATION 6/6 + traceability; cross-check tooling under-counts)."
  - phase: env
    items:
      - "Pre-existing: `internal/release` TestSER03_NoAutoSavePatterns fails locally on a stray gitignored `cmd/playwright-fixture/dist/assets/*.js` build artifact. Not a milestone defect; absent in CI (the SER-03 walk skips top-level dist but not nested cmd/**/dist)."
manual_uat_status:
  two_machine_e2e: "PASS 2026-06-16 (discover→list→pick→join→browse on live two-machine tailnet; #24 on-ramp proven)"
  pending_onscreen: "RB-03 non-tailnet trust boundary, prefers-reduced-motion spinner, colorblind state legibility — source-verified, on-screen confirm optional (non-blocking)"
nyquist:
  compliant_phases: [129, 130]
  partial_phases: []
  missing_phases: []
  overall: compliant
---

# Milestone v3.5.1 — Remote Browse Completion + Release-Gate Fix — Audit

**Audited:** 2026-06-16
**Status:** ✅ PASSED
**Definition of done:** Close the desktop-GUI remote-browse on-ramp (discover→list→pick a tailnet peer's sessions) and clear the release-gate blocker, retiring umbrella epic #24. Closes Issues #86, #83, #87.

## Requirements Coverage — 11/11 satisfied (3-source cross-reference)

| REQ | Phase | VERIFICATION | SUMMARY cites | Traceability | Final |
|-----|-------|--------------|---------------|--------------|-------|
| RACE-01 | 129 | passed (6/6) | 129-02 | Complete | **satisfied** |
| RACE-02 | 129 | passed | 129-02 | Complete | **satisfied** |
| RACE-03 | 129 | passed | 129-02 | Complete | **satisfied** |
| DNS-01 | 129 | passed | (frontmatter omits ID) | Complete | **satisfied** (VERIFICATION + traceability authoritative) |
| DNS-02 | 129 | passed | (frontmatter omits ID) | Complete | **satisfied** |
| DNS-03 | 129 | passed | (frontmatter omits ID) | Complete | **satisfied** |
| RB-01 | 130 | passed (5/5) | 130-01/03/04 | Complete | **satisfied** |
| RB-02 | 130 | passed | 130-04 | Complete | **satisfied** |
| RB-03 | 130 | passed | 130-01/03 | Complete | **satisfied** |
| RB-04 | 130 | passed | 130-02/04 | Complete | **satisfied** |
| RB-05 | 130 | passed | 130-01/03 | Complete | **satisfied** |

No unsatisfied requirements. No orphaned requirements.

## Phases — 2/2 passed

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 129 Write Concurrency Fix + DNS Error UX | passed | 6/6 | #87 per-path single-winner lock; #83 accept-dns actionable error + proactive banner (DNS-03 banner-gating fix found+fixed during UAT) |
| 130 Remote Browse GUI On-Ramp | passed | 5/5 | #86 tailnet-trusted metadata-only discovery; honest panel states; relay-surface RB-05 test; live two-machine UAT PASS |

## Cross-Phase Integration — PASS (11/11 WIRED)

Integration checker verdict: **PASS, no blockers.** All 11 requirements have verified end-to-end wiring. Key cross-phase links confirmed:
- Phase 129 DNS-03 `RemoteBrowseDNSWarning` banner coexists with the Phase 130 Remote Sessions panel (banner-stack gating includes the `acceptDns` trigger; no duplication).
- Phase 130 remote write reuses the Phase 129 single-winner `WriteFileAtomic` per-path lock on the upstream; proxy comments agree (RACE-02).
- `/api/sessions/meta` → `FetchAllPeerSessionsMeta` → `GetRemoteSessionsWithMeta` RPC → `RemoteSessionsPanel`, with the runtime `App.js` binding gap (found during UAT) fixed in commit 32fe10c.
- RB-05 relay-surface test exercises discover→pick→browse through `api.RelayHandler()`.

## E2E Flows — 2/2 complete

1. **Remote browse on-ramp** (discover→list→pick→join→browse): PASS — proven live on a two-machine tailnet 2026-06-16. Closes umbrella #24.
2. **Concurrent write single-winner + DNS error UX**: PASS — release-gate race test deterministic; accept-dns actionable error + proactive banner verified live (banner render confirmed on-screen).

## UAT-Surfaced Bugs — all fixed in-milestone (per user decision)

Three pre-Phase-130 bugs surfaced during the two-machine UAT and were fixed (folded into v3.5.1):
1. Write-toggle re-hydration from server-authoritative `filesWrite` (Phase 124 parity) — `667807b`.
2. Join code shown as readable/copyable text (Phase 122 UX) — `9fb33b0`.
3. Join-code copy corrected "5-character" → 8-char `XXXX-XXXX` on GUI + TUI (Phase 122, cross-surface) — `7b85c16`.

Plus the DNS-03 banner-gating fix (`App.tsx` banner-stack mount condition) and the `App.js` runtime-binding fix — both found by UAT, both fixed.

## Nyquist Compliance — COMPLIANT (2/2)

Both phases have VALIDATION.md with Wave-0 tests created RED-first and turned green (incl. the release-blocking RB-05 relay-surface test). `nyquist_compliant: true`, `wave_0_complete: true`.

## Verdict

All 11 requirements satisfied, integration fully wired, both E2E flows complete, live two-machine UAT passed, umbrella #24 closed. Tech debt is minor and non-blocking (chiefly the wails-CLI/go.mod version mismatch and signing — operational, pre-release release-engineering items). **Milestone v3.5.1 is ready to complete.**
