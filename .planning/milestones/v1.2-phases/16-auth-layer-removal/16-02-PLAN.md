---
phase: 16-auth-layer-removal
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/SettingsPanel.tsx
  - frontend/src/components/StatusBar.tsx
  - frontend/src/components/__tests__/SettingsPanel.test.tsx
  - frontend/src/components/__tests__/StatusBar.test.tsx
  - frontend/src/wailsjs/go/main/App.d.ts
  - frontend/src/wailsjs/go/main/App.js
  - web/dashboard.html
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "SettingsPanel has exactly 2 tabs: CLI Paths and Web Server (no Security tab)"
    - "Start Web Server button disabled condition is only ctDisclosed-based, not password-based"
    - "StatusBar has no Copy Link button and no onCopyTokenLink prop"
    - "Wails bindings do not export SetWebPassword, IsWebPasswordSet, or GenerateSessionToken"
    - "Dashboard HTML loads directly into session list without login form"
    - "Dashboard HTML has no CA certificate section"
    - "Frontend tests all pass"
  artifacts:
    - path: "frontend/src/components/SettingsPanel.tsx"
      provides: "Settings panel with 2 tabs, no Security tab"
    - path: "frontend/src/components/StatusBar.tsx"
      provides: "Status bar without Copy Link button"
    - path: "frontend/src/App.tsx"
      provides: "App without handleCopyTokenLink or GenerateSessionToken"
    - path: "web/dashboard.html"
      provides: "Dashboard without login section, CA section, or token link buttons"
    - path: "frontend/src/wailsjs/go/main/App.d.ts"
      provides: "Wails bindings without auth method exports"
  key_links:
    - from: "frontend/src/components/SettingsPanel.tsx"
      to: "wailsjs/go/main/App"
      via: "Import statement must NOT import SetWebPassword or IsWebPasswordSet"
      pattern: "import.*from.*wailsjs/go/main/App"
    - from: "frontend/src/App.tsx"
      to: "wailsjs/go/main/App"
      via: "Import statement must NOT import GenerateSessionToken"
      pattern: "import.*from.*wailsjs/go/main/App"
---

<objective>
Remove all frontend auth UI: delete Security tab from SettingsPanel, remove Copy Link button from StatusBar, remove token link handler from App.tsx, update Wails binding stubs, strip login/CA/token sections from dashboard.html, update all frontend tests.

Purpose: Frontend must match the backend auth removal from Plan 01. Dead UI elements referencing removed Go methods would cause runtime errors.
Output: Clean frontend with no auth UI; all frontend tests pass.
</objective>

<execution_context>
@/Users/ken/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ken/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-auth-layer-removal/16-RESEARCH.md
@.planning/phases/16-auth-layer-removal/16-UI-SPEC.md
@.planning/phases/16-auth-layer-removal/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Wails bindings, App.tsx, SettingsPanel.tsx, StatusBar.tsx</name>
  <files>
    frontend/src/wailsjs/go/main/App.d.ts,
    frontend/src/wailsjs/go/main/App.js,
    frontend/src/App.tsx,
    frontend/src/components/SettingsPanel.tsx,
    frontend/src/components/StatusBar.tsx
  </files>
  <read_first>
    frontend/src/wailsjs/go/main/App.d.ts,
    frontend/src/wailsjs/go/main/App.js,
    frontend/src/App.tsx,
    frontend/src/components/SettingsPanel.tsx,
    frontend/src/components/StatusBar.tsx
  </read_first>
  <action>
1. Edit `frontend/src/wailsjs/go/main/App.d.ts`:
   - Delete the line: `export function SetWebPassword(arg1:string):Promise<void>;`
   - Delete the line: `export function IsWebPasswordSet():Promise<boolean>;`
   - Delete the line: `export function GenerateSessionToken(arg1:string):Promise<string>;`
   - Keep all other exports unchanged.

2. Edit `frontend/src/wailsjs/go/main/App.js`:
   - Delete the function: `export function SetWebPassword(arg1) { return window['go']['main']['App']['SetWebPassword'](arg1); }`
   - Delete the function: `export function IsWebPasswordSet() { return window['go']['main']['App']['IsWebPasswordSet'](); }`
   - Delete the function: `export function GenerateSessionToken(arg1) { return window['go']['main']['App']['GenerateSessionToken'](arg1); }`

3. Edit `frontend/src/components/SettingsPanel.tsx`:
   - Remove `SetWebPassword` and `IsWebPasswordSet` from the import statement from `../wailsjs/go/main/App`
   - Remove state variables: `webPassword`, `isPasswordSet`, `passwordSaving`, `passwordError` (all useState hooks)
   - Remove the `handleSetPassword` async function
   - Remove the `IsWebPasswordSet()` call in the `loadWebState` effect (or wherever the initial state is loaded)
   - Narrow the `activeTab` type/state from `'cli-paths' | 'web-server' | 'security'` to `'cli-paths' | 'web-server'`
   - Delete the Security tab button from the tab list: the `<button>` with text "Security" (or similar role="tab")
   - Delete the entire Security tab content panel (the `{activeTab === 'security' && (...)}` JSX block)
   - Update the Start Web Server button's disabled condition: change from `(!isPasswordSet || !ctDisclosed)` (or similar) to `(!ctDisclosed)`. The check should only be: disabled when CT not disclosed AND server not already running.
   - Remove the tooltip text "Set a password in the Security tab first" — replace disabled tooltip with "Acknowledge the Certificate Transparency disclosure first" (per UI-SPEC)

4. Edit `frontend/src/components/StatusBar.tsx`:
   - Remove the `onCopyTokenLink` prop from the `StatusBarProps` interface
   - Remove `onCopyTokenLink` from the destructured props
   - Delete the "Copy Link" `<button>` element from the WEB ON branch (the `webServerRunning && webEnabled` state)
   - Remaining WEB ON elements: state badge, session URL link, Disable Web button, QR button

5. Edit `frontend/src/App.tsx`:
   - Remove import of `GenerateSessionToken` from `../wailsjs/go/main/App`
   - Delete the `handleCopyTokenLink` function (or equivalent that calls `GenerateSessionToken`)
   - Remove the `onCopyTokenLink={handleCopyTokenLink}` prop from the `<StatusBar>` JSX
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `App.d.ts` does NOT contain `SetWebPassword`
    - `App.d.ts` does NOT contain `IsWebPasswordSet`
    - `App.d.ts` does NOT contain `GenerateSessionToken`
    - `App.js` does NOT contain `SetWebPassword`
    - `App.js` does NOT contain `IsWebPasswordSet`
    - `App.js` does NOT contain `GenerateSessionToken`
    - `SettingsPanel.tsx` does NOT contain `SetWebPassword`
    - `SettingsPanel.tsx` does NOT contain `IsWebPasswordSet`
    - `SettingsPanel.tsx` does NOT contain `security` (as tab identifier)
    - `SettingsPanel.tsx` does NOT contain `handleSetPassword`
    - `SettingsPanel.tsx` does NOT contain `isPasswordSet`
    - `SettingsPanel.tsx` contains disabled condition referencing only `ctDisclosed` (not password)
    - `StatusBar.tsx` does NOT contain `onCopyTokenLink`
    - `StatusBar.tsx` does NOT contain `Copy Link`
    - `App.tsx` does NOT contain `GenerateSessionToken`
    - `App.tsx` does NOT contain `handleCopyTokenLink`
    - `App.tsx` does NOT contain `onCopyTokenLink`
    - TypeScript compilation (`npx tsc --noEmit`) exits 0
  </acceptance_criteria>
  <done>All frontend React/TS auth references removed; TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Strip dashboard.html and update frontend tests</name>
  <files>
    web/dashboard.html,
    frontend/src/components/__tests__/SettingsPanel.test.tsx,
    frontend/src/components/__tests__/StatusBar.test.tsx
  </files>
  <read_first>
    web/dashboard.html,
    frontend/src/components/__tests__/SettingsPanel.test.tsx,
    frontend/src/components/__tests__/StatusBar.test.tsx
  </read_first>
  <action>
1. Edit `web/dashboard.html`:

   a. Delete the entire `<section id="login-section">` block and all child elements (password input, login button, error div).

   b. Delete the `doLogin()` JavaScript function.

   c. Delete the `#login-error` element reference.

   d. Make `#dashboard-section` visible by default — remove the `display:none` inline style or CSS rule. The dashboard should render directly on page load.

   e. In `refreshSessions()`: remove the 401-handling branch that redirects to the login section. If the response is not ok, just log or ignore — there is no login to redirect to.

   f. Remove the auto-check login on page load — replace with direct `refreshSessions()` call in `DOMContentLoaded` or at the end of the script. Delete `checkLogin()` or equivalent function if it exists.

   g. Delete the `copyTokenLink()` JavaScript function.

   h. In `buildSessionCard()` (or equivalent function that renders session cards): delete the "Copy Token Link" button element.

   i. Delete the `#copy-token-result` element if it exists.

   j. Delete the CA certificate installation `<section>` (the one with `.ca-section` class or `id="ca-section"`).

   k. Delete dead CSS rules:
      - `.ca-section`, `.platform-tabs`, `.platform-tab`, `.platform-content` rules
      - `input[type="password"]` rule
      - `#login-error`, `#login-section`, `#dashboard-section` rules that are now dead
      - `#copy-token-result` rule if it exists

2. Edit `frontend/src/components/__tests__/SettingsPanel.test.tsx`:

   a. Delete the test case: `'Security tab shows CA certificate path'` (already failing — pre-existing).

   b. Delete any other test cases that reference the Security tab (e.g., "Security tab" in test name, clicking security tab button, asserting security tab content).

   c. Update the Wails mock: remove `SetWebPassword` and `IsWebPasswordSet` from the mock object if they are mocked via `vi.mock`.

   d. If there are tests for "Start Web Server button disabled when password not set", delete or rewrite them. The button should now only be disabled when CT is not disclosed.

   e. Keep all CLI Paths and Web Server tab tests that don't reference auth.

3. Edit `frontend/src/components/__tests__/StatusBar.test.tsx`:

   a. Remove `onCopyTokenLink` from any test prop objects (e.g., `onCopyTokenLink: vi.fn()` or similar).

   b. Delete test cases that assert "Copy Link" button behavior.

   c. Keep all other StatusBar tests unchanged.

4. Run frontend tests: `cd frontend && pnpm test`
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard.html` does NOT contain `login-section`
    - `dashboard.html` does NOT contain `doLogin`
    - `dashboard.html` does NOT contain `copyTokenLink`
    - `dashboard.html` does NOT contain `ca-section`
    - `dashboard.html` does NOT contain `input[type="password"]`
    - `dashboard.html` does NOT contain `Copy Token Link`
    - `dashboard.html` contains `refreshSessions()` called directly (not gated by login check)
    - `dashboard.html` `#dashboard-section` is NOT `display:none`
    - `SettingsPanel.test.tsx` does NOT contain `Security tab shows CA certificate path`
    - `SettingsPanel.test.tsx` does NOT contain `SetWebPassword`
    - `SettingsPanel.test.tsx` does NOT contain `IsWebPasswordSet`
    - `StatusBar.test.tsx` does NOT contain `onCopyTokenLink`
    - `pnpm test` exits 0 with 0 failures
  </acceptance_criteria>
  <done>Dashboard HTML cleaned of all auth/token/CA UI; frontend tests updated and all passing</done>
</task>

</tasks>

<verification>
```bash
# Full test suite (Go + Frontend)
cd /Users/ken/dev/agenthub && go test ./... -race && cd frontend && pnpm test

# Verify no auth references in frontend
! grep -rq 'SetWebPassword\|IsWebPasswordSet\|GenerateSessionToken' frontend/src/ && echo "frontend clean"

# Verify dashboard.html is clean
! grep -q 'login-section\|doLogin\|copyTokenLink\|ca-section' web/dashboard.html && echo "dashboard clean"
```
</verification>

<success_criteria>
- SettingsPanel has 2 tabs (CLI Paths, Web Server) — Security tab gone
- StatusBar has no Copy Link button
- Dashboard HTML renders session list without login gate
- Wails bindings have no auth method exports
- All frontend tests pass (0 failures)
- Full suite (Go + Frontend) is green
</success_criteria>

<output>
After completion, create `.planning/phases/16-auth-layer-removal/16-02-SUMMARY.md`
</output>
