---
phase: 99-settings-ui-polish-migration-final-csp-audit-release-gate
plan: 05
type: execute
wave: 4
depends_on: [99-01, 99-02, 99-03, 99-04]
files_modified:
  - .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "99-iPad-UAT.md exists with 5 numbered UAT scenarios cloning the 93-iPad-UAT.md shape verbatim — header, prereqs, numbered steps, verbatim-copy quote-blocks, sub-checks, PASS criteria, sign-off checklist."
    - "UAT-1 covers all-plugins-enabled flow (default state: 7 ON / Progress OFF) — attach → type → emit OSC 9;4 progress → emit sixel image (chafa --format=iterm2 chart.png)."
    - "UAT-2 covers scrollback-through-history → detach → re-attach → confirm scrollback intact (multi-client byte-fidelity)."
    - "UAT-3 covers zero-CDN audit via Safari Web Inspector remote debugging from dev Mac (Network tab filter for cdn.jsdelivr.net | unpkg.com | esm.sh — must be empty)."
    - "UAT-4 covers CSP zero-violation audit via Safari Web Inspector Console tab — zero `csp` / `content security policy` errors during full session."
    - "UAT-5 covers second-pass with ALL-8-plugins-ON (toggle Progress ON in Settings before the session)."
    - "Sign-off block at the bottom references 99-VALIDATION.md and `/gsd-verify-work 99` as the final command."
    - "Real iPad device required — iOS Simulator is NOT a substitute (per ROADMAP SC-4 verbatim)."
  artifacts:
    - path: ".planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md"
      provides: "5-scenario manual UAT runbook for iPad Safari Tailscale verification"
      min_lines: 80
  key_links:
    - from: ".planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md"
      to: ".planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-VALIDATION.md"
      via: "Sign-off block references VALIDATION.md for final aggregation"
      pattern: "99-VALIDATION.md"
    - from: ".planning/phases/93-vendoring-discipline-web-parity-for-already-shipping-addons/93-iPad-UAT.md"
      to: ".planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md"
      via: "Phase 99 clones the Phase 93 shape (header, scenarios, sign-off)"
      pattern: "iPad Safari"
---

<objective>
Implement SC-4 (iPad Safari Tailscale UAT portion): author the manual UAT runbook `99-iPad-UAT.md` covering 5 scenarios on a real iPad over Tailscale, then HUMAN executes the runbook on a real device and signs off.

Purpose: ROADMAP SC-4 mandates "iPad Safari Tailscale UAT (real device, not emulator) reports zero CSP violations and zero CDN requests during a full attach/render/scrollback/detach session with all v3.2 plugins enabled." Headless Playwright cannot reproduce iPad-Safari-over-Tailscale specifics (network stack, capability-token cookie behavior, software-rasterized WebGL, viewport quirks). The runbook is the load-bearing artifact; the human checkpoint is its execution.

Output:
1. New `.planning/phases/99-.../99-iPad-UAT.md` — manual runbook (autonomous markdown authoring).
2. Human executes the runbook on a real iPad over Tailscale and signs off.

This plan is `autonomous: false` because the runbook EXECUTION requires real hardware and real human eyes. Authoring the runbook is autonomous; running it is not.

Depends on Plan 99-04 because the cross-browser config in playwright.config.ts must be green (proves WebKit-engine CSP enforcement works headlessly) before iPad Safari (a real-device WebKit) is meaningful to test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-RESEARCH.md
@.planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-PATTERNS.md
@.planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-VALIDATION.md
@.planning/phases/93-vendoring-discipline-web-parity-for-already-shipping-addons/93-iPad-UAT.md

<interfaces>
<!-- Existing analog -->

From .planning/phases/93-.../93-iPad-UAT.md (template — clone shape verbatim):
- Header: `# Phase XX — iPad Safari Manual UAT` + 1-line description + `## Prerequisites` block.
- 5 numbered UAT sections: `## UAT-N: <title>` followed by numbered steps + `>` blockquoted verbatim copy + bulleted sub-checks + `**PASS criteria:** ...`.
- `## Sign-Off` block at the bottom: 5 checkboxes (`- [ ] UAT-N PASS`) + final aggregation reference to `XX-VALIDATION.md` and `/gsd-verify-work XX`.

From 99-RESEARCH.md "iPad UAT runbook scope":
- Clone Phase 93 shape verbatim, but extend with: zero-CDN audit + CSP zero-violation audit + all-plugins-enabled flow.
- Two passes: default state (7 ON / Progress OFF) and all-8-ON.

From 99-VALIDATION.md (lines 70-74 — manual-only verifications):
- "iPad Safari Tailscale full session flow" — SC-4 — must produce screenshots of Web Inspector console (zero CSP) and Network tab (zero CDN), signed off in 99-VERIFICATION.md.

Tailscale URL shape (from 93-iPad-UAT.md prereqs):
- `https://<tailnet-fqdn>.ts.net/sessions/<id>?cap=<token>` — capability-bearing share link.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 99-iPad-UAT.md runbook (5 scenarios, sign-off block)</name>
  <read_first>
    - .planning/phases/93-vendoring-discipline-web-parity-for-already-shipping-addons/93-iPad-UAT.md (entire file — clone the shape)
    - .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-RESEARCH.md (Claude's Discretion: "iPad UAT runbook scope" subsection)
    - .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-VALIDATION.md (manual-only verifications table)
  </read_first>
  <files>.planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md</files>
  <behavior>
    - Test 1: File exists with at least 5 `## UAT-N` sections.
    - Test 2: Each UAT section has numbered steps, verbatim quoted copy in `>` blockquotes (where applicable), sub-checks, and a PASS criteria line.
    - Test 3: Sign-Off block at the bottom has 5 checkboxes (one per UAT), plus a final reference to `99-VALIDATION.md` and `/gsd-verify-work 99`.
    - Test 4: Prereqs section names: real iPad on Tailnet, AgentHub running with web-server enabled + at least one shared session, Tailscale URL shape, dev Mac with Safari (for Web Inspector remote debugging).
  </behavior>
  <action>
    Create `.planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md` cloning the shape of `93-iPad-UAT.md`. Use this exact structure:

    ```markdown
    # Phase 99 — iPad Safari Manual UAT (v3.2 Release Gate)

    > Manual UAT script for the runtime behaviors that headless Playwright cannot reproduce on iPad Safari over Tailscale. Run during `/gsd-verify-work 99` before v3.2 release sign-off.

    ## Prerequisites

    - Real iPad with Safari, joined to the same Tailnet as the dev Mac running AgentHub. **iOS Simulator is NOT a substitute** (per ROADMAP SC-4 — Safari WebKit on iOS has subtle CSP / WebAssembly / network differences from desktop WebKit that the simulator hides).
    - Dev Mac with Safari ≥ 16 and "Develop" menu enabled (Safari → Settings → Advanced → Show Develop menu in menu bar).
    - **iPad must be USB-connected to the dev Mac** for Web Inspector remote debugging (Settings → Safari → Advanced → Web Inspector ON on iPad).
    - AgentHub built with `wails build -tags wailsassets` and running. Web server enabled. At least one terminal session created and capability-shared via the share panel.
    - The Tailscale URL of the form `https://<tailnet-fqdn>.ts.net/sessions/<id>?cap=<token>`.
    - **Default plugin state for UAT-1 through UAT-4:** 7 ON / Progress OFF (the v3.2 default).
    - **For UAT-5:** all 8 plugins ON (toggle Progress ON in Settings → Plugins → Save Plugins → reload web session URL).

    ---

    ## UAT-1: All-Plugins-Enabled Attach → Render Flow (SC-4)

    1. On iPad Safari, open the Tailscale URL.
    2. Within 5 seconds the terminal should attach and render its scrollback (capability cookie set, WebSocket connected, xterm DOM mounted).
    3. Type `echo hello` and press Enter — observe the echo appears in the terminal.
    4. From the dev Mac, in the running session, run:
       ```
       chafa --format=iterm2 -s 40x20 https://upload.wikimedia.org/wikipedia/commons/thumb/4/47/PNG_transparency_demonstration_1.png/280px-PNG_transparency_demonstration_1.png
       ```
       (or any local PNG via `chafa --format=iterm2 chart.png`).
    5. Observe the inline image renders in the iPad terminal pane (sixel/IIP via @xterm/addon-image).
    6. From the dev Mac, run a CLI emitting OSC 9;4 progress, e.g.:
       ```
       printf '\033]9;4;1;25\007'   # OSC 9;4 state=1 value=25
       sleep 1
       printf '\033]9;4;1;75\007'   # state=1 value=75
       sleep 1
       printf '\033]9;4;0;0\007'    # state=0 (clear)
       ```
       Confirm: NO progress underline appears (Progress plugin is OFF in UAT-1's default state).

    **PASS criteria:**
    - Terminal attached within 5 seconds. ✅
    - Echo command displays correctly. ✅
    - Inline image renders inside the iPad terminal pane. ✅
    - No progress underline (Progress OFF). ✅

    ---

    ## UAT-2: Scrollback → Detach → Re-attach → Persistence (IMG-04, multi-client byte-fidelity)

    1. From UAT-1's session, type `seq 1 200` and press Enter — produces 200 lines of scrollback.
    2. Scroll up in the iPad terminal — confirm lines 1-200 are visible in scrollback.
    3. Tap the browser back button (or close the Safari tab) to detach.
    4. Re-open the Tailscale URL.
    5. The session re-attaches; scrollback should be fully populated (lines 1-200 + the chafa image from UAT-1 still rendered as inline image during replay).

    **PASS criteria:**
    - Scrollback intact across detach/re-attach. ✅
    - The inline sixel image rendered correctly during scrollback replay (multi-client byte-fidelity preserved). ✅

    ---

    ## UAT-3: Zero-CDN Audit (Phase 89 SEC-08 / Phase 99 release gate)

    1. On the dev Mac with iPad USB-connected: Safari → Develop menu → "<iPad name>" → select the open Tailscale URL tab. Web Inspector opens.
    2. Switch to the **Network** tab in Web Inspector.
    3. Apply a domain filter: type `cdn.jsdelivr.net OR unpkg.com OR esm.sh OR fonts.googleapis.com` in the filter box.
    4. From the dev Mac, drive a full session: type commands, scroll, render an image (re-run UAT-1 chafa).
    5. Observe the Network tab — the filter should match **zero requests**.
    6. Take a screenshot of the Network tab showing zero matches and save to `screenshots/99-iPad-UAT-3-zero-cdn.png`.

    **PASS criteria:**
    - Zero requests to any CDN domain during full session. ✅
    - Screenshot captured. ✅

    ---

    ## UAT-4: CSP Zero-Violation Audit (SC-4 — load-bearing for v3.2 release)

    1. Continue from UAT-3 with Web Inspector still open.
    2. Switch to the **Console** tab.
    3. Clear the console.
    4. From the dev Mac, drive a full session: attach, type, scroll, render image, detach, re-attach.
    5. Observe the console — there must be **zero messages** matching the regex `/csp|content security policy|refused to (execute|load|connect)/i`.
    6. Take a screenshot of the empty console + the timestamp range covering the full UAT-4 session, save to `screenshots/99-iPad-UAT-4-zero-csp.png`.

    **PASS criteria:**
    - Zero CSP violation messages in iPad Safari console during full session. ✅
    - Screenshot captured. ✅

    ---

    ## UAT-5: Second Pass — All 8 Plugins ON (Progress flipped ON)

    1. On the dev Mac, open AgentHub Settings → Plugins.
    2. Toggle **Progress (OSC 9;4)** ON. Click **Save Plugins**.
    3. (Plan 99-01 PUI-02 will fire BannerStack toasts for unicode11/image if those were toggled too — for this UAT, only Progress is changed, so no banner is expected.)
    4. On the iPad, reload the Tailscale URL (the SSE plugin-config stream should propagate the change automatically, but a hard reload is the most reliable way to confirm).
    5. From the dev Mac, run the OSC 9;4 sequence from UAT-1 step 6.
    6. Observe in the iPad terminal: the tab strip (or progress underline area) should now show a progress underline at the value reported by OSC 9;4 (25%, then 75%, then cleared).
    7. While progress is non-zero, the tray icon on the dev Mac should show an aggregate quartile glyph (verify on dev Mac, not iPad).
    8. Re-run UAT-3 and UAT-4 with all-8-ON state — confirm zero CDN requests and zero CSP violations still hold.

    **PASS criteria:**
    - Progress underline visible on the tab during OSC 9;4 emission. ✅
    - Tray icon on dev Mac reflects aggregate quartile. ✅
    - Re-run UAT-3 and UAT-4 still pass with all-8-ON. ✅

    ---

    ## Sign-Off

    - [ ] UAT-1 PASS
    - [ ] UAT-2 PASS
    - [ ] UAT-3 PASS (screenshot captured at `screenshots/99-iPad-UAT-3-zero-cdn.png`)
    - [ ] UAT-4 PASS (screenshot captured at `screenshots/99-iPad-UAT-4-zero-csp.png`)
    - [ ] UAT-5 PASS

    Once all 5 UATs PASS:
    1. Mark `99-VALIDATION.md` § Validation Sign-Off all checkboxes complete.
    2. Update `99-VERIFICATION.md` (created by `/gsd-verify-work 99`) with the iPad UAT screenshots and tester sign-off line.
    3. Run `/gsd-verify-work 99` to finalize phase verification.
    4. v3.2 release gate clears once `/gsd-verify-work 99` reports green.

    ---

    *Tester:* _______________________  *Device:* iPad ___________ (model + iOS version)  *Date:* __________
    ```

    The above is the verbatim file contents. Do NOT compress or truncate. Do NOT introduce extra UAT scenarios beyond these 5 (per RESEARCH "iPad UAT runbook scope: 5 scenarios total").
  </action>
  <verify>
    <automated>test -f .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md &amp;&amp; grep -c "^## UAT-" .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md` exists.
    - `grep -c "^## UAT-" .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md` returns 5.
    - `grep -c "^## Sign-Off" .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md` returns 1.
    - `grep -c "iOS Simulator is NOT a substitute" .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md` returns 1.
    - `grep -c "99-VALIDATION.md" .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md` returns >= 1.
    - `grep -c "/gsd-verify-work 99" .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md` returns >= 1.
    - `grep -c "cdn.jsdelivr.net" .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md` returns >= 1.
    - `grep -c "Web Inspector" .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md` returns >= 1.
    - `grep -c "OSC 9;4" .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md` returns >= 1.
    - File line count >= 80 (substantive, not a stub): `wc -l .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md` >= 80.
  </acceptance_criteria>
  <done>99-iPad-UAT.md authored with 5 numbered UAT scenarios + prereqs + sign-off; clones 93-iPad-UAT.md shape; ready for human execution.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human executes 99-iPad-UAT.md on real iPad + signs off</name>
  <what-built>
    Task 1 produced `.planning/phases/99-.../99-iPad-UAT.md` — a 5-scenario manual UAT runbook for iPad Safari over Tailscale. Task 1 did NOT execute it (real-hardware UAT cannot be automated).
  </what-built>
  <how-to-verify>
    1. Read the runbook end-to-end: `cat .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md`.
    2. Verify all prereqs are in place:
       - Real iPad joined to the Tailnet.
       - AgentHub built with `wails build -tags wailsassets` (production build — required because dev mode bypasses some CSP enforcement; per project memory `project_wails_build_requires_tags`).
       - At least one shared terminal session with a capability-bearing URL.
       - iPad USB-connected to dev Mac.
       - Safari "Develop" menu enabled on dev Mac.
       - Web Inspector enabled on iPad.
    3. Execute UAT-1 through UAT-5 in order, marking each `- [ ] UAT-N PASS` checkbox in the file as you complete it.
    4. Capture screenshots:
       - `screenshots/99-iPad-UAT-3-zero-cdn.png` — Web Inspector Network tab with CDN filter showing zero matches.
       - `screenshots/99-iPad-UAT-4-zero-csp.png` — Web Inspector Console tab showing zero CSP violation messages over the full session.
    5. Fill in the Tester / Device / Date line at the bottom of the runbook.
    6. Commit the marked-up runbook + screenshots to git.
    7. Update `.planning/phases/99-.../99-VALIDATION.md` § Validation Sign-Off — mark all checkboxes.
    8. The orchestrator's `/gsd-verify-work 99` will read the marked-up file + screenshots as evidence.

    **If any UAT fails:** do NOT mark its checkbox. Document the failure in a new gap-closure plan via `/gsd-plan-phase 99 --gaps` and re-run UAT for the affected scenario.
  </how-to-verify>
  <action>Pause for human execution. Resume only after the runbook is fully signed off and screenshots are captured + committed.</action>
  <verify>
    <automated>test -f .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md &amp;&amp; grep -c "\\[x\\] UAT-" .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - All 5 sign-off checkboxes in `99-iPad-UAT.md` show `[x]` not `[ ]`. Verify: `grep -c "\\[x\\] UAT-" .planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-iPad-UAT.md` returns 5.
    - Screenshots exist at `screenshots/99-iPad-UAT-3-zero-cdn.png` and `screenshots/99-iPad-UAT-4-zero-csp.png`.
    - Tester / Device / Date line at the bottom of the file is filled in (no placeholder underscores remain — verify with `grep -c "Tester: _______" .planning/phases/99-.../99-iPad-UAT.md` returns 0).
    - User types "approved" or describes failures.
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 5 UAT checkboxes are marked, screenshots are committed, and the runbook bottom is filled in. Or describe specific UAT failures — orchestrator will route to gap-closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iPad Safari → Tailscale-served terminal | The same boundary as Phase 87/88/89 — capability-token gates session access; CSP gates script execution; vendoring gate prevents CDN exfiltration. UAT verifies all three hold on iPad-Safari-WebKit specifically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-15 | Tampering | iPad WebKit CSP enforcement | mitigate | UAT-4 directly verifies WebKit's CSP enforcement on real iPad hardware (not simulator). Zero violations during full attach/scroll/render/detach session = release gate green. |
| T-99-16 | Information Disclosure | CDN data exfiltration | mitigate | UAT-3 verifies zero CDN requests over the wire from real iPad. Vendoring discipline (Phases 89, 93, 96) extends to iPad-Safari-WebKit. |
| T-99-17 | Spoofing | Capability-bearing share URL | accept | The capability-token URL is the shared-link primitive from Phase 87 SEC-01..SEC-05. UAT inherits the existing security model; no new spoofing surface. |
</threat_model>

<verification>
- 99-iPad-UAT.md authored with 5 UAT sections + sign-off block (verified via grep + line count).
- Human runs the runbook on real iPad over Tailscale and marks all 5 checkboxes [x].
- Two screenshots committed under `screenshots/`.
- 99-VALIDATION.md § Validation Sign-Off marked complete.
- `/gsd-verify-work 99` next to verify v3.2 release-gate phase.
</verification>

<success_criteria>
SC-4 (iPad Safari portion) satisfied: real-device manual UAT executed; zero CSP violations + zero CDN requests proven via Web Inspector remote debugging from dev Mac; all-plugins-enabled flow + ALL-8-ON flow both pass.

Combined with Plan 99-04 (cross-browser headless CSP), Phase 99 SC-4 is fully satisfied.

v3.2 release-gate phase complete; ready for `/gsd-verify-work 99` and v3.2 final release tag.
</success_criteria>

<output>
After completion, create `.planning/phases/99-settings-ui-polish-migration-final-csp-audit-release-gate/99-05-SUMMARY.md` documenting:
- Path to runbook and number of UAT scenarios.
- Tester name + iPad model + iOS version + date.
- Paths to the two screenshots committed.
- Any UAT failures + gap-closure plan IDs (if applicable).
- Confirmation that 99-VALIDATION.md sign-off section is marked complete.
- Recommendation: proceed to `/gsd-verify-work 99` to close the phase.
</output>
