---
phase: 97
plan: 06
type: execute
wave: 3
depends_on: [97-01, 97-02, 97-03, 97-04, 97-05]
files_modified:
  - web/embed.go
  - web/terminal.html
  - web/assets/terminal.js
  - .planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md
autonomous: false
requirements: [SER-01, SER-02, SER-03]
tags: [phase-97, serialize, web-parity, uat, wave-3]
must_haves:
  truths:
    - "web/embed.go //go:embed directive includes vendor/xterm/addons/addon-serialize.js (the file already exists from Plan 97-01; this plan REGISTERS it with the embed.FS)"
    - "web/terminal.html has a <script src=\"/assets/xterm/addons/addon-serialize.js\"></script> tag inserted AFTER addon-image and BEFORE terminal.js (UMD load order: addon's global must be defined before terminal.js consumes it)"
    - "web/assets/terminal.js initTerminal() constructs SerializeAddon via UMD global alongside other addons, gated by pluginConfig.serialize"
    - "Web-side construction is VENDORING-DISCIPLINE PARITY ONLY — the addon is loaded but no UI invokes it in v3.2 (per 97-RESEARCH §Web Parity Scope: native Save dialog is desktop-only; <a download> blob URL approach for web is deferred to a future SER-FUT-WEB phase)"
    - "97-HUMAN-UAT.md exists with 4 manual test scenarios per VALIDATION.md: SER-OFF (toggle OFF banner appears), SER-ON (native dialog opens, file is plain text), CANCEL (no file written), SER-02 verbatim caption visible in Settings"
    - "After this plan + the human-checkpoint sign-off, ALL Phase 97 SER-01..SER-03 success criteria are demonstrably satisfied"
  artifacts:
    - path: "web/embed.go"
      provides: "//go:embed directive extended to include vendor/xterm/addons/addon-serialize.js"
      contains: "addon-serialize.js"
    - path: "web/terminal.html"
      provides: "<script> tag for addon-serialize.js inserted after addon-image and before terminal.js"
      contains: "addon-serialize.js"
    - path: "web/assets/terminal.js"
      provides: "initTerminal() constructs SerializeAddon via UMD global with pluginConfig.serialize gate (vendoring-discipline parity; no UI consumer in v3.2)"
      contains: "SerializeAddon"
    - path: ".planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md"
      provides: "4 manual UAT scenarios: SER-OFF banner, SER-ON dialog + plain-text file, CANCEL silence, SER-02 caption visibility"
      min_lines: 80
  key_links:
    - from: "web/vendor/xterm/addons/addon-serialize.js (vendored in Plan 97-01)"
      to: "/assets/xterm/addons/addon-serialize.js (HTTP)"
      via: "web/embed.go //go:embed + FileServerFS route"
      pattern: "addon-serialize.js"
    - from: "web/terminal.html <script> tag"
      to: "global SerializeAddon UMD symbol"
      via: "browser <script> load + UMD factory assignment"
      pattern: "addon-serialize.js"
    - from: "web/assets/terminal.js initTerminal()"
      to: "term.loadAddon(new SerializeAddon.SerializeAddon())"
      via: "UMD global construction (parity only — no UI consumer)"
      pattern: "SerializeAddon"
---

<objective>
Land the WEB-01 vendoring-discipline parity gate: (1) extend `web/embed.go`'s `//go:embed` directive to include `vendor/xterm/addons/addon-serialize.js` (the file already exists from Plan 97-01; this plan REGISTERS it with the embed.FS so the static file server can deliver it); (2) add the `<script src="/assets/xterm/addons/addon-serialize.js"></script>` tag in `web/terminal.html` AFTER addon-image and BEFORE terminal.js (UMD load order is load-bearing: terminal.js's `initTerminal()` consumes the global `SerializeAddon` object that the addon's IIFE preamble defines, so the script must run first); (3) extend `web/assets/terminal.js` `initTerminal()` to construct `SerializeAddon` via the UMD global alongside other addons, gated by `pluginConfig.serialize`; (4) author `97-HUMAN-UAT.md` with the 4 manual test scenarios from `97-VALIDATION.md` "Manual-Only Verifications"; (5) provide a human-checkpoint task at end where the user runs the UAT runbook and signs off on the visual/native-dialog behavior that automated tests cannot fully cover.

**Web parity scope discipline (load-bearing per 97-RESEARCH §"Web Parity Scope"):** Phase 97 ships desktop-only "Save Terminal As…" — the right-click context menu lives in the GUI shell; web-served pages have a different keyboard/menu vocabulary and the natural web equivalent (a `<a download>` blob URL with the addon's `serialize()` output) is parallel work deferred to a future `SER-FUT-WEB` phase. The web-side construction in THIS plan is vendoring-discipline parity ONLY — the addon is loaded into `term.loadAddon()` so the `vendor_drift_test.go` parity check stays GREEN and the v3.2 vendoring story is whole, but no Save UI exists on the web side. This must be documented in the SUMMARY and in `97-HUMAN-UAT.md`.

This is the autonomous=false gate plan — it includes a `checkpoint:human-verify` task at the end where the human runs the UAT runbook and signs off on the visual/native-dialog behavior. After this plan, Phase 97 SER-01..SER-03 are demonstrably satisfied and `/gsd-verify-work 97` can flip the success criteria to GREEN.

Output:
- `web/embed.go` //go:embed directive extended (one-line append or new continuation line)
- `web/terminal.html` `<script>` tag added after addon-image and before terminal.js
- `web/assets/terminal.js` `initTerminal()` SerializeAddon construction block (gated by `pluginConfig.serialize`; try/catch wrapped per existing addon pattern)
- `.planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md` with 4 manual test scenarios + sign-off checklist
- Human checkpoint signed off
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/97-serialize-addon-save-session-ux/97-RESEARCH.md
@.planning/phases/97-serialize-addon-save-session-ux/97-PATTERNS.md
@.planning/phases/97-serialize-addon-save-session-ux/97-VALIDATION.md
@.planning/phases/97-serialize-addon-save-session-ux/97-01-SUMMARY.md
@.planning/phases/97-serialize-addon-save-session-ux/97-02-SUMMARY.md
@.planning/phases/97-serialize-addon-save-session-ux/97-03-SUMMARY.md
@.planning/phases/97-serialize-addon-save-session-ux/97-04-SUMMARY.md
@.planning/phases/97-serialize-addon-save-session-ux/97-05-SUMMARY.md
@.planning/phases/96-image-addon-csp-audit/96-06-PLAN.md
@.planning/phases/96-image-addon-csp-audit/96-HUMAN-UAT.md
@web/embed.go
@web/terminal.html
@web/assets/terminal.js
@web/vendor/xterm/VERSION

<interfaces>
<!-- Web parity files to extend. -->

From web/embed.go (current — Phase 96 shape, lines 5-11):
```go
//go:embed dashboard.html terminal.html join.html
//go:embed assets/terminal.js assets/terminal.css
//go:embed assets/dashboard.js assets/dashboard.css
//go:embed assets/join.js assets/join.css
//go:embed vendor/xterm/xterm.js vendor/xterm/xterm.css vendor/xterm/addon-fit.js vendor/xterm/VERSION
//go:embed vendor/xterm/addons/addon-webgl.js vendor/xterm/addons/addon-unicode11.js vendor/xterm/addons/addon-clipboard.js vendor/xterm/addons/addon-search.js vendor/xterm/addons/addon-web-links.js
//go:embed vendor/xterm/addons/addon-image.js
var WebFS embed.FS
```

After this plan: append `vendor/xterm/addons/addon-serialize.js` to the existing line containing `addon-image.js` OR create a new continuation line if line-length exceeds 120 cols. Both forms accepted by Go's `//go:embed` directive parser.

From web/terminal.html (current — Phase 96 shape, lines 43-50):
```html
<script src="/assets/xterm/xterm.js"></script>
<script src="/assets/xterm/addon-fit.js"></script>
<script src="/assets/xterm/addons/addon-webgl.js"></script>
<script src="/assets/xterm/addons/addon-unicode11.js"></script>
<script src="/assets/xterm/addons/addon-clipboard.js"></script>
<script src="/assets/xterm/addons/addon-search.js"></script>
<script src="/assets/xterm/addons/addon-web-links.js"></script>
<script src="/assets/xterm/addons/addon-image.js"></script>
```

After this plan: insert `<script src="/assets/xterm/addons/addon-serialize.js"></script>` AFTER the addon-image line and BEFORE `<script src="/assets/terminal.js"></script>` (the latter is at line ~64 per Phase 96).

From web/assets/terminal.js (current — Phase 96 ImageAddon construction at lines 248-257):
```javascript
if (pluginConfig.image) {
  try {
    var storageLimit = (pluginConfig.imageConfig && pluginConfig.imageConfig.storageLimit) || 16;
    var imageAddon = new ImageAddon.ImageAddon({
      storageLimit: storageLimit,
      enableSizeReports: false
    });
    term.loadAddon(imageAddon);
  } catch (e) { /* addon UMD may not be present, or WASM bootstrap failed — silent */ }
}
```

After this plan: add a structurally-parallel construction block IMMEDIATELY AFTER the ImageAddon block:
```javascript
// Phase 97 SER-01: SerializeAddon construction — hot-swap-friendly on
// desktop (TerminalPanel.tsx) but the web client has no save UI in v3.2,
// so we just register the addon for parity. Future SER-FUT-WEB work may
// add a web-side save button consuming this addon's serialize() method.
if (pluginConfig.serialize) {
  try {
    var serializeAddon = new SerializeAddon.SerializeAddon();
    term.loadAddon(serializeAddon);
  } catch (e) { /* addon UMD may not be present — silent */ }
}
```

UMD-global pitfall: `new SerializeAddon.SerializeAddon()` — the UMD module assigns a NAMESPACE object to `window.SerializeAddon`, which has a `.SerializeAddon` class property (Phase 96 verified this for ImageAddon: `new ImageAddon.ImageAddon(...)` — same shape). VERIFY by inspecting the vendored UMD file's IIFE preamble during implementation; if the global is the class directly, drop the namespace prefix. Mirror Phase 96's verification approach.

From `pluginConfig` defaults block in web/assets/terminal.js (around lines 118-130 per 97-PATTERNS.md):
- Verify `serialize: true` is in the default; if missing, add it (alphabetical position: progress < search < serialize < unicode11 < webgl in the existing ordering, OR matching the actual ordering in the file).
- The defaults block is what the web client uses when no `/api/plugin-config` response has been received yet.
</interfaces>

<closely-related>
- 97-PATTERNS.md §`web/embed.go` (lines 711-731), §`web/terminal.html` (lines 754-771), §`web/assets/terminal.js` (lines 774-810) — verbatim mechanical recipes.
- 97-RESEARCH.md §"Web Parity Scope" — locked decision: ship vendored file + script tag + initTerminal construction; defer web Save UI to SER-FUT-WEB.
- 97-RESEARCH.md §"Architectural Responsibility Map" row "Vendored addon serving" — "no runtime web consumer in v3.2".
- Phase 96 Plan 06 (`.planning/phases/96-image-addon-csp-audit/96-06-PLAN.md`) — the verbatim structural mirror: vendor + embed.go + terminal.html + initTerminal + UAT runbook + human checkpoint. Phase 96 also did the drift-test min-count bump in this plan; Phase 97 already did that bump in Plan 97-01 (Wave 0 unblock), so this plan does NOT touch vendor_drift_test.go.
- Phase 96 HUMAN-UAT.md is the verbatim shape mirror for the Phase 97 UAT runbook (4 scenarios + sign-off checklist).
- 97-VALIDATION.md "Manual-Only Verifications" table — the 4 scenarios this UAT runbook formalizes.
</closely-related>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Web parity — extend embed.go + terminal.html + initTerminal SerializeAddon construction</name>
  <files>web/embed.go, web/terminal.html, web/assets/terminal.js</files>
  <read_first>
    - web/embed.go (full — confirm current Phase 96 shape: line 11 has `//go:embed vendor/xterm/addons/addon-image.js`; identify whether to append on the same line or add a new continuation line based on current line length)
    - web/terminal.html (full — confirm Phase 96 shape: lines 43-50 list 8 vendored addon `<script>` tags ending with addon-image.js; line ~64 is `<script src="/assets/terminal.js"></script>`; identify exact insertion site for addon-serialize.js)
    - web/assets/terminal.js (full — locate `initTerminal()` function; identify the ImageAddon construction block at lines 248-257; identify the `pluginConfig` defaults block at lines 118-130; verify the UMD-global shape by spot-checking how ImageAddon is constructed: `new ImageAddon.ImageAddon(...)`)
    - web/vendor/xterm/addons/addon-serialize.js (Plan 97-01 vendored — spot-check the UMD IIFE preamble: `head -3` should show `(function (global, factory) {...})` and the global assignment line should reveal whether the global is `SerializeAddon` namespace object with a `.SerializeAddon` class, OR `SerializeAddon` directly as the class)
    - .planning/phases/97-serialize-addon-save-session-ux/97-PATTERNS.md §`web/embed.go` (lines 711-731), §`web/terminal.html` (lines 754-771), §`web/assets/terminal.js` (lines 774-810)
    - .planning/phases/96-image-addon-csp-audit/96-06-PLAN.md Task 1 (the structural mirror — copy the embed.go/terminal.html/terminal.js extension steps verbatim and adapt names from `addon-image` to `addon-serialize`)
  </read_first>
  <behavior>
    - `web/embed.go` includes `vendor/xterm/addons/addon-serialize.js` in a `//go:embed` directive. `go build ./web/...` exits 0. A request to `/assets/xterm/addons/addon-serialize.js` returns 200 + `application/javascript` (verified during human checkpoint via curl/devtools).
    - `web/terminal.html` has the `<script src="/assets/xterm/addons/addon-serialize.js"></script>` tag inserted directly after the addon-image line and before terminal.js. The HTML is well-formed (no missing close tags).
    - `web/assets/terminal.js` `initTerminal()` constructs `SerializeAddon` (gated by `pluginConfig.serialize`) using the verified UMD-global shape (likely `new SerializeAddon.SerializeAddon()`). Construction is wrapped in try/catch with silent error swallowing per existing addon pattern.
    - `pluginConfig` defaults in terminal.js include `serialize: true` (matching the daemon default — Phase 96 set it).
    - The web side has NO save-button UI — addon is loaded for vendoring-discipline parity only, per RESEARCH §"Web Parity Scope" locked decision. Comment in terminal.js cites this scope.
    - `go build ./...` exits 0; `go test ./internal/webserver/... -count=1` exits 0 (vendor_drift_test.go from Plan 97-01 + cross-checks).
  </behavior>
  <action>
    Step A — modify `web/embed.go`. Use the Edit tool to extend the `//go:embed` directive to include `addon-serialize.js`. Two equivalent forms:

    **Form 1 (preferred — append to existing line if under ~120 cols):** Locate the line:
    ```
    //go:embed vendor/xterm/addons/addon-image.js
    ```
    Change to:
    ```
    //go:embed vendor/xterm/addons/addon-image.js vendor/xterm/addons/addon-serialize.js
    ```

    **Form 2 (if Form 1 exceeds line-length):** Add a new continuation line directly after the addon-image directive:
    ```
    //go:embed vendor/xterm/addons/addon-image.js
    //go:embed vendor/xterm/addons/addon-serialize.js
    ```

    Use Form 1 unless adding the path makes the line > 120 chars. Verify with `go build ./web/...` after editing.

    Step B — modify `web/terminal.html`. Use the Edit tool to insert the `<script>` tag for addon-serialize.js. Locate the line:
    ```html
    <script src="/assets/xterm/addons/addon-image.js"></script>
    ```

    Insert directly AFTER it (and BEFORE `<script src="/assets/terminal.js"></script>`):
    ```html
    <script src="/assets/xterm/addons/addon-serialize.js"></script>
    ```

    Maintain the indentation of surrounding `<script>` tags.

    Step C — modify `web/assets/terminal.js`. Two changes:

    **C1 — pluginConfig defaults.** Locate the `pluginConfig` defaults block (around lines 118-130). Verify the keys for serialize:
    - If `serialize: true` is already present in the defaults block, do nothing.
    - If absent, ADD `serialize: true` in alphabetical position (or matching the existing ordering — match what's there).

    **C2 — SerializeAddon construction.** Locate the ImageAddon construction block (around lines 248-257). Insert a structurally-parallel block IMMEDIATELY AFTER the ImageAddon block:
    ```javascript
    // Phase 97 SER-01: SerializeAddon construction — hot-swap-friendly on
    // desktop (TerminalPanel.tsx). Web client has NO save UI in v3.2 per
    // 97-RESEARCH §"Web Parity Scope" locked decision; we register the
    // addon for vendoring-discipline parity only so vendor_drift_test.go
    // stays green and a future SER-FUT-WEB plan can wire a web-side save
    // button without re-vendoring. The addon is loaded but inert.
    if (pluginConfig.serialize) {
      try {
        var serializeAddon = new SerializeAddon.SerializeAddon();
        term.loadAddon(serializeAddon);
      } catch (e) { /* addon UMD may not be present — silent */ }
    }
    ```

    UMD-global verification: BEFORE finalizing the construction line, verify the actual UMD shape of the vendored file. Run:
    ```bash
    head -10 web/vendor/xterm/addons/addon-serialize.js
    ```

    Look for the IIFE preamble. If it reads roughly `(function (global, factory) { ... })((typeof self !== "undefined" ? self : this), function (exports) { ... return Object.defineProperty(exports, "SerializeAddon", { ... }) })`, the global is a NAMESPACE OBJECT with a `.SerializeAddon` class — use `new SerializeAddon.SerializeAddon()` (matching Phase 96's `new ImageAddon.ImageAddon()`).

    If the IIFE assigns the class DIRECTLY to the global (rare for `@xterm/*` family), use `new SerializeAddon()` instead. Phase 96 confirmed `ImageAddon` is the namespace form, so SerializeAddon almost certainly follows the same shape — but verify before committing.

    Document the resolved shape in a code comment for future maintenance.
  </action>
  <verify>
    <automated>
      grep -q "addon-serialize.js" web/embed.go \
        &amp;&amp; grep -q "addon-serialize.js" web/terminal.html \
        &amp;&amp; grep -q "SerializeAddon" web/assets/terminal.js \
        &amp;&amp; grep -q "pluginConfig.serialize" web/assets/terminal.js \
        &amp;&amp; go build ./web/... \
        &amp;&amp; go vet ./web/... \
        &amp;&amp; gofmt -l web/embed.go | (! grep .) \
        &amp;&amp; go test ./internal/webserver/... -count=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "addon-serialize.js" web/embed.go` >= 1 (//go:embed includes the file)
    - `grep -c "addon-serialize.js" web/terminal.html` == 1 (script tag added)
    - The `<script>` tag for addon-serialize is positioned AFTER addon-image and BEFORE terminal.js: `awk '/addon-image.js/,/terminal.js/' web/terminal.html | grep -q "addon-serialize"` exits 0
    - `grep -c "SerializeAddon" web/assets/terminal.js` >= 1
    - `grep -q "pluginConfig.serialize" web/assets/terminal.js` exits 0 (toggle gate present)
    - `grep -q "vendoring-discipline parity" web/assets/terminal.js` exits 0 (scope-discipline comment present)
    - `go build ./web/...` exits 0 (//go:embed compiles)
    - `go vet ./web/...` exits 0
    - `gofmt -l web/embed.go` produces NO output
    - `go test ./internal/webserver/... -count=1` exits 0 (vendor_drift_test still green)
    - `go test ./... -count=1` exits 0 (no regression)
  </acceptance_criteria>
  <done>
    Web parity vendoring-discipline complete: embed.go + terminal.html + initTerminal SerializeAddon construction in place; addon loaded but inert on web side per locked scope decision.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 97-HUMAN-UAT.md with 4 manual test scenarios</name>
  <files>.planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/96-image-addon-csp-audit/96-HUMAN-UAT.md (full — verbatim structural mirror; copy frontmatter shape, preamble, per-scenario layout: Why manual / Setup / Verify / Sign-off line)
    - .planning/phases/97-serialize-addon-save-session-ux/97-VALIDATION.md "Manual-Only Verifications" table — the 4 scenarios this runbook formalizes
    - .planning/phases/97-serialize-addon-save-session-ux/97-RESEARCH.md §"User Constraints / Locked Decisions" — informs the verbatim secrets-warning text the user should see
  </read_first>
  <behavior>
    - `.planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md` exists with valid frontmatter (phase: 97; type: human-uat; created date; requirements list [SER-01, SER-02, SER-03]; plans list [97-01..97-06]).
    - The file documents 4 manual test scenarios per VALIDATION.md "Manual-Only Verifications":
      - **SER-OFF — toggle Serialize OFF, right-click tab → Save…, verify banner appears (no native dialog).**
      - **SER-ON — toggle Serialize ON, right-click tab → Save…, native dialog appears, save to ~/Desktop/test.txt, verify file is plain text (no `\x1b[`) and contains expected scrollback.**
      - **CANCEL — toggle ON, right-click → Save…, click Cancel, verify NO file written and NO error toast.**
      - **SER-02 CAPTION — open Settings → Plugins → Serialize row, verify italic caption reads verbatim "Saved files include any secrets, tokens, or sensitive data printed in the session."**
    - Each scenario has 4 sections: **Why manual** (what automation cannot cover), **Setup** (how to prepare the environment), **Verify** (numbered steps + expected outcome), **Sign-off line** (checkbox + signature).
    - Build instructions reference `wails build -tags wailsassets` per project memory.
    - The runbook is consumable by a human running through it sequentially in ~15 minutes.
  </behavior>
  <action>
    Author `.planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md` using this exact content:

    ```markdown
    ---
    phase: 97
    type: human-uat
    created: 2026-05-07
    requirements: [SER-01, SER-02, SER-03]
    plans: [97-01, 97-02, 97-03, 97-04, 97-05, 97-06]
    ---

    # Phase 97 Human UAT — Serialize Addon + Save-Session UX

    > Run these AFTER Plans 97-01 through 97-05 are complete and the standard
    > test gates are green (`go test ./... -count=1`, `cd frontend && pnpm test --run`,
    > `cd frontend && pnpm tsc --noEmit`).

    ## Setup

    ```bash
    # Build the desktop app with vendored web assets.
    wails build -tags wailsassets

    # Open the built app.
    open build/bin/AgentHub.app   # macOS
    # or run the binary directly on Linux/Windows
    ```

    Confirm the app launches without errors and a default session is available.

    ---

    ## Scenario 1 — SER-OFF: Save menu item shows banner when Serialize is disabled

    **Why manual:** Live affordance behavior across Settings toggle + right-click context menu + banner stack rendering — automated tests cover the source-scan invariants but cannot exercise the cross-component event chain at runtime.

    **Setup:** Open AgentHub. Open or create a terminal session. Open Settings → Plugins. Toggle "Save terminal as text" (Serialize) **OFF**. Click Save in Settings. Wait for "Saved" indicator.

    **Verify:**
    1. Right-click the terminal tab.
    2. Confirm the "Save Terminal As…" menu item is **visible** (NOT hidden — discoverability per RESEARCH locked decision).
    3. Click "Save Terminal As…".
    4. **Expected:** A banner/toast appears with text similar to "Enable the Serialize plugin in Settings to save sessions." NO native Save dialog opens.
    5. Dismiss the banner. Confirm no file was written to disk.

    **Sign-off:** [ ] Verified by ____________________ on ________

    ---

    ## Scenario 2 — SER-ON: Native Save dialog opens, file is plain text, content matches scrollback

    **Why manual:** Native OS Save dialog cannot be exercised in headless CI; visual confirmation of dialog UX, file write integrity, and ANSI-strip correctness require human inspection.

    **Setup:** In Settings → Plugins, toggle Serialize **ON** (this is the default; if it was on before Scenario 1, simply re-enable). Click Save. Open or create a terminal session and run a command that produces colored output (e.g. `ls --color=always` or `echo -e "\033[1;31mError\033[0m: test"`).

    **Verify:**
    1. Right-click the tab.
    2. Click "Save Terminal As…".
    3. **Expected:** A native Save dialog opens. The dialog title contains "(file will include any printed secrets)" or similar warning text.
    4. The default filename is the tab name + timestamp + `.txt` extension (e.g. `claude-code-2026-05-07-143022.txt`).
    5. Save to `~/Desktop/agenthub-uat-test.txt`. Confirm the file appears on the Desktop.
    6. Open the file in TextEdit (macOS) / Notepad / `cat`:
       - **Expected:** The file is plain UTF-8 text. NO `\x1b[` escape sequences anywhere. The colored "Error: test" output appears as plain "Error: test" without color codes.
       - **Expected:** The file contains the visible scrollback content from the session.

    **Sign-off:** [ ] Verified by ____________________ on ________

    ---

    ## Scenario 3 — CANCEL: User-cancellation writes no file and shows no error

    **Why manual:** Real native dialog cancellation behavior (Esc key, Cancel button) is OS-specific and cannot be exercised in headless CI.

    **Setup:** Continue from Scenario 2's setup (Serialize ON, session with content).

    **Verify:**
    1. Right-click the tab → "Save Terminal As…".
    2. When the native Save dialog opens, press **Esc** (or click Cancel).
    3. **Expected:** Dialog closes. NO error toast appears. NO file is written at the default path.
    4. Repeat with the Cancel button: right-click → Save → click Cancel button. Same expected outcome.

    **Sign-off:** [ ] Verified by ____________________ on ________

    ---

    ## Scenario 4 — SER-02 CAPTION: Verbatim secrets warning visible in Settings

    **Why manual:** Visual / text-rendered verification of warning copy — automated source-scan asserts the literal string is in the source, but only a human eye confirms the caption renders as italic text directly under the toggle (not buried elsewhere).

    **Setup:** Open Settings → Plugins.

    **Verify:**
    1. Locate the row labeled "Save terminal as text" with the Serialize toggle.
    2. **Expected:** Directly under the toggle, in italic styling, the caption reads VERBATIM:
       > Saved files include any secrets, tokens, or sensitive data printed in the session.
    3. Confirm:
       - The text is italic (matches the unicode11 / image "Applies to new sessions you create" caption styling).
       - The text is positioned as a description directly under the toggle, NOT as a tooltip or hover-only.
       - The text matches the verbatim string above (no paraphrasing, no added/removed words).

    **Sign-off:** [ ] Verified by ____________________ on ________

    ---

    ## Final Sign-Off

    - [ ] All 4 scenarios pass on macOS
    - [ ] (Optional) Verified on Linux: ___________
    - [ ] (Optional) Verified on Windows: ___________
    - [ ] No CSP violations observed in browser devtools (web parity check — open the web-served terminal page in Chrome, inspect Console for `Refused to load` or CSP errors; expect ZERO since addon-serialize is pure JS with no WebAssembly/Worker/blob constructs per RESEARCH §"Mandatory Pre-Phase CSP Audit")
    - [ ] No regression on Phase 96 image scenarios (sanity check)

    **Tester:** ____________________
    **Date:** ____________________
    **Build:** AgentHub `___________` (paste version from About dialog or build output)

    **Notes / issues observed:**

    ```
    (free-form notes)
    ```
    ```

    Save the file to `.planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md`.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md \
        &amp;&amp; grep -q "type: human-uat" .planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md \
        &amp;&amp; grep -q "requirements: \[SER-01, SER-02, SER-03\]" .planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md \
        &amp;&amp; grep -q "Scenario 1" .planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md \
        &amp;&amp; grep -q "Scenario 2" .planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md \
        &amp;&amp; grep -q "Scenario 3" .planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md \
        &amp;&amp; grep -q "Scenario 4" .planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md \
        &amp;&amp; grep -q "Saved files include any secrets, tokens, or sensitive data printed in the session." .planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md \
        &amp;&amp; grep -q "wails build -tags wailsassets" .planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md \
        &amp;&amp; [ "$(wc -l &lt; .planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md)" -ge 80 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md` exits 0
    - File has valid YAML frontmatter with `phase: 97`, `type: human-uat`, `requirements: [SER-01, SER-02, SER-03]`, `plans: [97-01, 97-02, 97-03, 97-04, 97-05, 97-06]`
    - All 4 scenarios documented (SER-OFF, SER-ON, CANCEL, SER-02 CAPTION) with Setup/Verify/Sign-off sections each
    - Verbatim SER-02 secrets-warning string appears in the document
    - `wails build -tags wailsassets` instruction present (per project memory)
    - File is at least 80 lines (sanity bound for 4 scenarios + frontmatter + final sign-off)
    - Final sign-off section includes platform checkboxes, tester name, date, build version, notes
  </acceptance_criteria>
  <done>
    97-HUMAN-UAT.md authored with 4 scenarios + final sign-off; document ready for the next checkpoint task.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human runs UAT runbook and signs off</name>
  <files>(no files modified — verification only; 97-HUMAN-UAT.md is updated by the tester with sign-off marks)</files>
  <action>Build the desktop app via `wails build -tags wailsassets` and run the 4 manual UAT scenarios documented in `.planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md`. Sign off each scenario by ticking the checkbox and adding tester name + date in the runbook. Commit the signed-off 97-HUMAN-UAT.md. If any scenario fails, do NOT mark approved — describe the failure and the likely Plan 97-XX owner so a gap-closure plan can be authored via `/gsd-plan-phase 97 --gaps`.</action>
  <verify>
    <automated>echo "human-verify checkpoint — automation completed by Tasks 1-2 (web parity + UAT runbook); this checkpoint is for human UAT sign-off"</automated>
  </verify>
  <done>All 4 scenarios in 97-HUMAN-UAT.md signed off (checkboxes ticked, tester name + date filled in, build version recorded); 97-HUMAN-UAT.md committed to git; tester returns "approved" via resume-signal.</done>
  <what-built>
    Phase 97 Plans 01-06 are complete:
    - SerializeAddon vendored + drift-test gate (Plan 97-01)
    - Pure helpers stripAnsi + sanitizeFilename (Plan 97-02)
    - App.tsx saver registry pipeline (Plan 97-03)
    - TerminalPanel hot-swap arm + TabBar Save Terminal As… menu item (Plan 97-04)
    - (*App).SaveTerminalSession Wails RPC + bindings + SER-02 caption (Plan 97-05)
    - Web parity: vendored, embedded, script-tagged, addon-loaded (this plan, Task 1)
    - 97-HUMAN-UAT.md runbook (this plan, Task 2)
  </what-built>
  <how-to-verify>
    Run the manual UAT scenarios in `.planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md`:

    1. Build the desktop app:
       ```bash
       wails build -tags wailsassets
       ```

    2. Open the built app: `open build/bin/AgentHub.app` (macOS).

    3. Walk through all 4 scenarios in 97-HUMAN-UAT.md, signing off each one:
       - **Scenario 1 (SER-OFF):** Toggle Serialize OFF in Settings. Right-click tab → Save Terminal As… → confirm banner appears (no dialog). PASS / FAIL?
       - **Scenario 2 (SER-ON):** Toggle Serialize ON. Generate colored output. Right-click → Save → save to ~/Desktop. Confirm file is plain text (no \x1b sequences) and content matches scrollback. PASS / FAIL?
       - **Scenario 3 (CANCEL):** Right-click → Save → Esc / Cancel. Confirm no file written, no error toast. PASS / FAIL?
       - **Scenario 4 (SER-02 CAPTION):** Settings → Plugins → Serialize row. Confirm italic caption reads verbatim "Saved files include any secrets, tokens, or sensitive data printed in the session." PASS / FAIL?

    4. (Optional) Open the web-served terminal in Chrome with devtools open. Confirm zero CSP violations and zero requests to external CDNs in the Network tab.

    5. Fill in tester name, date, build version, and any free-form notes in 97-HUMAN-UAT.md.

    6. Commit the signed-off 97-HUMAN-UAT.md to git.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 4 scenarios pass and 97-HUMAN-UAT.md is signed off.

    Type a description of what failed if any scenario does not pass — and which Plan (97-01..97-06) is the likely owner of the regression. Phase will then need a gap-closure plan via `/gsd-plan-phase 97 --gaps`.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendored UMD asset → web/embed.FS | Same-origin static asset; CSP `script-src 'self'` enforcement (Phase 89) covers; addon-serialize has zero CSP-relevant constructs (RESEARCH §"Mandatory Pre-Phase CSP Audit"). |
| Web client → SerializeAddon construction | The addon is loaded but no UI invokes it on web in v3.2; no exposed RCE / save / write surface from the web side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-06-01 | Tampering | Future drift between vendored addon-serialize.js and pnpm-lock version | mitigate | Plan 97-01 already bumped vendor_drift_test.go min-count gate to 9; this plan does not regress that. The drift test parses both pnpm-lock and VERSION and asserts equality — adding a future addon-serialize@0.15.0 bump in package.json would break the drift test until VERSION is updated and the file is re-vendored. |
| T-97-06-02 | Information Disclosure | Web user sees "Save Terminal As…" affordance but cannot use it (since no UI is wired) | accept | RESEARCH §"Web Parity Scope" locked decision — desktop-only. Web client has neither a context menu Save item nor a keyboard shortcut. The addon load is invisible to the user. SER-FUT-WEB tracks future web parity. |
| T-97-06-03 | Information Disclosure | Manual UAT runbook leaks app file paths or session content | accept | Sample paths (~/Desktop/agenthub-uat-test.txt) are user-side test fixtures; no production data leaks. UAT scenarios use synthetic content (`echo -e "Error: test"`). |
| T-97-06-04 | Denial of Service | Vendored addon UMD load failure crashes terminal page | mitigate | terminal.js wraps construction in try/catch with silent failure (mirror of existing addon-image / addon-clipboard pattern). The terminal page works without the addon — only the (unused-in-v3.2) save-button hypothetical UI would not function. |
</threat_model>

<verification>
After Tasks 1 and 2 complete (and the human checkpoint passes):

```bash
# Web parity gates
go build ./web/...
go test ./internal/webserver/... -count=1
grep "addon-serialize" web/embed.go web/terminal.html web/assets/terminal.js | wc -l  # ≥ 3

# Full Phase 97 test suite green
go test ./... -count=1
cd frontend && pnpm test --run && pnpm tsc --noEmit

# Build and run for manual UAT
wails build -tags wailsassets

# UAT runbook ready
test -f .planning/phases/97-serialize-addon-save-session-ux/97-HUMAN-UAT.md
```

## Truths Mapped to Tests

| Truth | Verified By |
|-------|-------------|
| addon-serialize embedded in web/embed.FS | `go build ./web/...` succeeds; integration via curl `/assets/xterm/addons/addon-serialize.js` returns 200 (manual during UAT) |
| terminal.html script tag in correct position | grep + position check in acceptance criteria |
| terminal.js initTerminal constructs SerializeAddon | grep + manual UAT browser console (no "SerializeAddon undefined" errors) |
| Web has NO save UI in v3.2 (scope discipline) | absence of any `onclick="save"` or save-button selector in web/assets/*.css/.html (manual review during UAT) |
| Manual UAT runbook documents 4 scenarios | grep "Scenario 1/2/3/4" |
| Verbatim SER-02 caption text appears in runbook | grep |
| Final human sign-off | `<resume-signal>` task returns "approved" |
</verification>

<success_criteria>
- All Task 1 acceptance criteria green (web parity vendoring-discipline complete)
- All Task 2 acceptance criteria green (97-HUMAN-UAT.md authored with 4 scenarios)
- Task 3 human checkpoint signed off ("approved" or equivalent)
- `go test ./... -count=1` exits 0; `cd frontend && pnpm test --run && pnpm tsc --noEmit` exits 0
- `wails build -tags wailsassets` succeeds
- After this plan + sign-off: Phase 97 SER-01..SER-03 are demonstrably satisfied; `/gsd-verify-work 97` can flip the success criteria to GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/97-serialize-addon-save-session-ux/97-06-SUMMARY.md` covering:
- Web parity vendoring-discipline complete: //go:embed registered, terminal.html script tag added, initTerminal SerializeAddon construction (vendoring parity only — no UI consumer in v3.2 per locked scope decision)
- 97-HUMAN-UAT.md authored with 4 scenarios (SER-OFF, SER-ON, CANCEL, SER-02 CAPTION) + final sign-off section
- Human-checkpoint signed off — Phase 97 SER-01..SER-03 demonstrably satisfied
- Web Save UI (a `<a download>` blob URL approach) deferred to SER-FUT-WEB; document rationale in the summary
- Phase 97 ships text-only saves; HTML output deferred to SER-FUT-01; auto-save explicitly out of scope per SER-03 (locked by Plan 97-01's GREEN-now no_autosave_test.go regression test)
</output>
