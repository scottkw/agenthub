---
phase: 94
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/pnpm-lock.yaml
  - web/vendor/xterm/VERSION
  - web/vendor/xterm/addons/addon-search.js
  - web/embed.go
  - internal/webserver/vendor_drift_test.go
  - frontend/src/components/FindBar/__tests__/FindBar.focus.test.tsx
  - frontend/src/components/FindBar/__tests__/FindBar.dismiss.test.tsx
  - frontend/src/components/FindBar/__tests__/FindBar.matchCount.test.tsx
  - frontend/src/components/FindBar/__tests__/FindBar.persistence.test.tsx
  - frontend/src/components/FindBar/__tests__/FindBar.cancel.test.tsx
  - frontend/src/components/FindBar/__tests__/FindBar.visual.test.tsx
  - frontend/src/components/FindBar/__tests__/FindBar.themeMatrix.test.tsx
  - frontend/src/lib/__tests__/isXtermFocused.test.ts
  - internal/daemon/search_config_test.go
  - internal/webserver/plugin_settings_search_sse_test.go
  - internal/webserver/findbar_perf_e2e_test.go
  - internal/webserver/findbar_web_e2e_test.go
  - internal/webserver/find_bar_test.go
autonomous: true
requirements: [SRC-01, SRC-02, SRC-03, SRC-04, SRC-05]
tags: [phase-94, search, vendor, scaffolding, wave-0]
must_haves:
  truths:
    - "@xterm/addon-search@0.16.0 is listed in frontend/package.json + frontend/pnpm-lock.yaml"
    - "web/vendor/xterm/VERSION lists @xterm/addon-search@0.16.0"
    - "web/vendor/xterm/addons/addon-search.js exists and is byte-equal to frontend/node_modules/@xterm/addon-search/lib/addon-search.js"
    - "web/embed.go //go:embed directive includes vendor/xterm/addons/addon-search.js"
    - "internal/webserver/vendor_drift_test.go min-count guard requires >= 6 @xterm/* packages"
    - "All RED test scaffolds listed in 94-VALIDATION.md Wave 0 Requirements exist as files"
    - "vendor_drift_test.go passes (lockfile + VERSION agree on addon-search)"
  artifacts:
    - path: web/vendor/xterm/addons/addon-search.js
      provides: vendored addon-search UMD bundle for web parity (mitigation T-94-01)
    - path: internal/webserver/vendor_drift_test.go
      provides: CI gate enforcing addon-search version parity
      contains: "if len(pnpmVersions) < 6"
    - path: frontend/src/components/FindBar/__tests__/FindBar.focus.test.tsx
      provides: RED scaffold for SRC-01 focus-conditioning
    - path: internal/daemon/search_config_test.go
      provides: RED scaffold for SRC-02 persistence
    - path: internal/webserver/findbar_perf_e2e_test.go
      provides: RED scaffold for SRC-03 perf budget (build-tagged e2e)
    - path: internal/webserver/findbar_web_e2e_test.go
      provides: RED scaffold for SRC-05 web parity (build-tagged e2e)
  key_links:
    - from: frontend/package.json
      to: web/vendor/xterm/VERSION
      via: vendor_drift_test.go regex
      pattern: "@xterm/addon-search@0.16.0"
    - from: web/embed.go
      to: web/vendor/xterm/addons/addon-search.js
      via: //go:embed directive
      pattern: "vendor/xterm/addons/addon-search.js"
---

<objective>
Wave 0 scaffolding for Phase 94. Install + vendor `@xterm/addon-search@0.16.0`, extend the vendor-drift CI gate to enforce parity (T-94-01 mitigation), and lay down every RED test scaffold listed in `94-VALIDATION.md ## Wave 0 Requirements`. After this plan, every later plan can import `@xterm/addon-search`, vendor checks are green, and every requirement (SRC-01..SRC-05) has at least one test file that compiles RED waiting to be turned GREEN.

Purpose: Honor Nyquist gate (every later task has an `<automated>` to run) AND the locked T-94-01 mitigation ("extend vendor_drift_test.go to count addon-search").
Output: 1 new vendored asset, 1 modified embed.go, 1 modified vendor_drift_test, 11 RED test scaffolds, 1 lib helper test.

Note on 17 files_modified: this plan intentionally lays down a uniform Wave 0 scaffold sweep (vendor + embed + 11 RED test scaffolds + 1 helper test + 1 vendor-drift bump) in a single batch. The high file count is justified by the Nyquist Wave 0 requirement that every downstream task has an `<automated>` verify command — which means every test file must exist (compiling RED) before Wave 1 starts. Splitting the sweep across multiple plans would not reduce per-task context cost (each scaffold is ~5 lines) and would only delay the green-light for parallel Wave 1+2+3 work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-search-addon-find-bar-desktop-web/94-UI-SPEC.md
@.planning/phases/94-search-addon-find-bar-desktop-web/94-RESEARCH.md
@.planning/phases/94-search-addon-find-bar-desktop-web/94-PATTERNS.md
@.planning/phases/94-search-addon-find-bar-desktop-web/94-VALIDATION.md
@./CLAUDE.md
@web/embed.go
@web/vendor/xterm/VERSION
@internal/webserver/vendor_drift_test.go

<interfaces>
<!-- Existing pnpm-lock.yaml @xterm/* lockfile keys (Phase 93 generalization). -->

From `internal/webserver/vendor_drift_test.go` line 18:
```go
var pnpmXtermKeyRe = regexp.MustCompile(`^  '(@xterm/(?:xterm|addon-[\w-]+))@([0-9.]+)':`)
```
Regex automatically matches `addon-search`. Only the min-count guard at line 34 (`if len(pnpmVersions) < 5`) needs a bump to 6.

From `web/embed.go`:
```go
//go:embed vendor/xterm/addons/addon-webgl.js vendor/xterm/addons/addon-unicode11.js vendor/xterm/addons/addon-clipboard.js
```
Append `vendor/xterm/addons/addon-search.js` to the same line OR add a fresh `//go:embed` line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install + vendor @xterm/addon-search@0.16.0; bump vendor-drift min-count from 5 to 6; extend embed.go</name>
  <files>frontend/package.json, frontend/pnpm-lock.yaml, web/vendor/xterm/VERSION, web/vendor/xterm/addons/addon-search.js, web/embed.go, internal/webserver/vendor_drift_test.go</files>
  <read_first>
    - 94-RESEARCH.md ## Standard Stack (verifies version 0.16.0, main: lib/addon-search.js, no peer deps declared)
    - 94-RESEARCH.md ## Code Examples Example 5 (vendor_drift_test.go zero-effort coverage — only min-count bumps from 5 to 6)
    - 94-PATTERNS.md `web/embed.go` and `internal/webserver/vendor_drift_test.go` analog sections
    - web/embed.go (current //go:embed directives — Phase 93 added 3 addons under vendor/xterm/addons/)
    - web/vendor/xterm/VERSION (current 5-line manifest: xterm + addon-fit + addon-webgl + addon-unicode11 + addon-clipboard)
    - internal/webserver/vendor_drift_test.go (line 18 regex; line 33-35 min-count guard message includes addon list)
  </read_first>
  <action>
Implements requirements: SRC-01..SRC-05 (foundation — every requirement transitively depends on the vendored asset). Mitigates T-94-01 (HIGH) — vendor drift between web bundle and frontend/node_modules copy.

Step 1 — Install in frontend (pnpm preferred per CLAUDE.md):
```
cd frontend && pnpm add @xterm/addon-search@0.16.0
```
Verify the resulting `frontend/package.json` `dependencies` block contains `"@xterm/addon-search": "0.16.0"` (exact pin, NOT `^0.16.0` — match Phase 93's exact-pin convention for `@xterm/*` deps; if existing entries use `^`, match the existing style — check before deciding).

Step 2 — Vendor the UMD bundle:
```
cp frontend/node_modules/@xterm/addon-search/lib/addon-search.js web/vendor/xterm/addons/addon-search.js
```
Verify the file is non-empty and is the CJS UMD form (`(function (root, factory) {` near the top).

Step 3 — Append to `web/vendor/xterm/VERSION` (after the existing 5 lines, no blank-line separator since the file currently is one entry per line with no trailing blank):
```
@xterm/addon-search@0.16.0
```
Final manifest must list exactly 6 packages.

Step 4 — Extend `web/embed.go`. Current line 9 reads:
```go
//go:embed vendor/xterm/addons/addon-webgl.js vendor/xterm/addons/addon-unicode11.js vendor/xterm/addons/addon-clipboard.js
```
Append `vendor/xterm/addons/addon-search.js` to the same directive (matches Phase 93 grouping convention; keep line under ~200 chars):
```go
//go:embed vendor/xterm/addons/addon-webgl.js vendor/xterm/addons/addon-unicode11.js vendor/xterm/addons/addon-clipboard.js vendor/xterm/addons/addon-search.js
```

Step 5 — Bump min-count guard in `internal/webserver/vendor_drift_test.go`. Current line 33-34:
```go
if len(pnpmVersions) < 5 {
    t.Fatalf("failed to parse at least 5 @xterm/* packages (xterm, addon-fit, addon-webgl, addon-unicode11, addon-clipboard) from pnpm-lock.yaml: found %v (Phase 93 WEB-02 — the lockfile format may have changed; see 89-RESEARCH.md Q3)", pnpmVersions)
}
```
Replace with (per T-94-01 mitigation locked in security_threat_model_block):
```go
if len(pnpmVersions) < 6 {
    t.Fatalf("failed to parse at least 6 @xterm/* packages (xterm, addon-fit, addon-webgl, addon-unicode11, addon-clipboard, addon-search) from pnpm-lock.yaml: found %v (Phase 94 SRC-01..05 — addon-search joined the manifest; T-94-01 mitigation)", pnpmVersions)
}
```
Do NOT touch the regex at line 18 — it auto-matches addon-search per RESEARCH §"Code Examples" Example 5.

Step 6 — Verify: `cd /Users/ken/dev/agenthub && go test ./internal/webserver/ -run TestXtermVendorVersionsMatchPnpmLock -count=1` MUST pass (lockfile and VERSION now both list 6 packages with matching versions).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test ./internal/webserver/ -run TestXtermVendorVersionsMatchPnpmLock -count=1 && grep -c "@xterm/addon-search@0.16.0" web/vendor/xterm/VERSION | grep -q "^1$" && grep -c "addon-search.js" web/embed.go | grep -qE "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` includes `@xterm/addon-search` at version `0.16.0` (grep: `grep -c '"@xterm/addon-search"' frontend/package.json` returns 1)
    - `web/vendor/xterm/addons/addon-search.js` exists and is non-empty (`test -s web/vendor/xterm/addons/addon-search.js`)
    - `web/vendor/xterm/VERSION` contains exactly one line matching `@xterm/addon-search@0.16.0` (`grep -c '^@xterm/addon-search@0\.16\.0$' web/vendor/xterm/VERSION` returns 1)
    - `web/embed.go` //go:embed directive includes `vendor/xterm/addons/addon-search.js` (`grep -c 'vendor/xterm/addons/addon-search.js' web/embed.go` >= 1)
    - `internal/webserver/vendor_drift_test.go` min-count guard reads `< 6` (`grep -c 'len(pnpmVersions) < 6' internal/webserver/vendor_drift_test.go` returns 1)
    - `go test ./internal/webserver/ -run TestXtermVendorVersionsMatchPnpmLock -count=1` exits 0
  </acceptance_criteria>
  <done>
    addon-search vendored, version manifest aligned, vendor-drift gate covers it, embed.go serves the file. T-94-01 (HIGH) mitigated.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create RED test scaffolds for all 11 Wave 0 entries (Go + TS) and the isXtermFocused helper test</name>
  <files>frontend/src/components/FindBar/__tests__/FindBar.focus.test.tsx, frontend/src/components/FindBar/__tests__/FindBar.dismiss.test.tsx, frontend/src/components/FindBar/__tests__/FindBar.matchCount.test.tsx, frontend/src/components/FindBar/__tests__/FindBar.persistence.test.tsx, frontend/src/components/FindBar/__tests__/FindBar.cancel.test.tsx, frontend/src/components/FindBar/__tests__/FindBar.visual.test.tsx, frontend/src/components/FindBar/__tests__/FindBar.themeMatrix.test.tsx, frontend/src/lib/__tests__/isXtermFocused.test.ts, internal/daemon/search_config_test.go, internal/webserver/plugin_settings_search_sse_test.go, internal/webserver/findbar_perf_e2e_test.go, internal/webserver/findbar_web_e2e_test.go, internal/webserver/find_bar_test.go</files>
  <read_first>
    - 94-VALIDATION.md ## Wave 0 Requirements (lists every scaffold needed) and ## Per-Task Verification Map (lists exact test names)
    - 94-RESEARCH.md ## Validation Architecture > Phase Requirements → Test Map
    - 94-PATTERNS.md FindBar.test.tsx and TerminalPanel.test.tsx (modify) sections (analog patterns: createRoot + flushSync + ?raw imports + verbatim copy assertions)
    - frontend/src/components/__tests__/WebGLRecoveryBanner.test.tsx (analog harness: createRoot/flushSync + vi.useFakeTimers + verbatim ARIA assertions)
    - frontend/src/components/__tests__/TerminalPanel.hot-swap.test.tsx (analog ?raw source-inspection style)
    - frontend/src/lib/__tests__/webglProbe.test.ts (analog pure-helper test pattern)
    - internal/daemon/plugin_settings_test.go (analog Go defaults assertion pattern)
    - internal/webserver/assets_test.go (analog TestAssets_VendoredAddons pattern)
  </read_first>
  <action>
Implements requirements: SRC-01..SRC-05 (Wave 0 RED scaffolds — Nyquist gate for every later task). Each scaffold MUST be wired so the test runner finds it AND fails RED (compiles or skips with a clear "Wave 0 — Plan 94-XX must implement" message). NEVER use `it.skip` to mask incomplete work — use `expect(false).toBe(true)` with a `Pending until Plan 94-XX implements ...` message OR (preferred for Go) `t.Skip("Pending until Plan 94-XX implements ...")` so they show as SKIP in CI rather than RED. Read 94-VALIDATION.md final paragraph: "All scaffolds RED on Wave 0; turn GREEN as later waves implement." So we use the explicit-fail pattern for TS (RED), and t.Skip for Go (because Go test framework treats compile failure as RED itself; skips with reason are visible).

Use a uniform header at the top of each scaffold:
```ts
// Phase 94 Wave 0 RED scaffold — Plan 94-XX (or NN) will implement.
// See: .planning/phases/94-search-addon-find-bar-desktop-web/94-VALIDATION.md
//      ## Per-Task Verification Map (row YY)
```

For TS scaffolds (Vitest), each test reads (verbatim shape):
```ts
import { describe, it, expect } from 'vitest'

describe('FindBar — focus conditioning (SRC-01) — Plan 94-03 implementation', () => {
  it('Cmd-F preventDefault only when xtermRef.contains(document.activeElement)', () => {
    expect.fail('RED scaffold — Plan 94-03 implements FindBar.tsx + TerminalPanel keydown handler (UI-SPEC §"Opening the Find Bar"; RESEARCH §"Pattern 2"). See 94-VALIDATION.md row 03-findbar wave 2.')
  })
})
```

For Go scaffolds:
```go
package webserver

import "testing"

// Phase 94 Wave 0 RED scaffold — Plan 94-04 implements (SRC-03 perf budget).
// See: .planning/phases/94-search-addon-find-bar-desktop-web/94-VALIDATION.md row 04-perf wave 2.
func TestFindBar_10kPerf(t *testing.T) {
    t.Skip("RED scaffold — Plan 94-04 implements 10k-line scrollback perf harness via chromedp. RESEARCH §\"Performance Envelope\".")
}
```

Tests to scaffold (one file each; one or more `it`/`Test` per file as listed below — match VALIDATION.md row test-name fragments):

1. `frontend/src/components/FindBar/__tests__/FindBar.focus.test.tsx`
   - Tests: `Cmd-F preventDefault only when xtermRef.contains(document.activeElement)` (SRC-01) — Plan 94-03
2. `frontend/src/components/FindBar/__tests__/FindBar.dismiss.test.tsx`
   - Tests: `Esc dismisses + restores xterm focus` (SRC-01) — Plan 94-03
3. `frontend/src/components/FindBar/__tests__/FindBar.matchCount.test.tsx`
   - Tests: `onDidChangeResults updates "{i} of {n}" label` (SRC-02) — Plan 94-03
   - Tests: `0 of 0 + --no-results modifier when query non-empty AND count zero` (SRC-02) — Plan 94-03
4. `frontend/src/components/FindBar/__tests__/FindBar.persistence.test.tsx`
   - Tests: `regex/case/wholeWord toggles round-trip to daemon SearchConfig via SetPluginSettings` (SRC-02) — Plan 94-03
5. `frontend/src/components/FindBar/__tests__/FindBar.cancel.test.tsx`
   - Tests: `Closing find bar clears decorations + cancels pending debounce` (SRC-03) — Plan 94-04
6. `frontend/src/components/FindBar/__tests__/FindBar.visual.test.tsx`
   - Tests: `Slide-in animation 200ms; selectionBackground used (no decorations option passed)` (SRC-04) — Plan 94-03
7. `frontend/src/components/FindBar/__tests__/FindBar.themeMatrix.test.tsx`
   - Tests: `Highlight contrast valid on TokyoNight + light theme snapshot` (SRC-04) — Plan 94-05
8. `frontend/src/lib/__tests__/isXtermFocused.test.ts`
   - Tests: `returns false when termContainer is null` AND `returns false when document.activeElement is null` AND `returns true when activeElement is descendant of termContainer` AND `returns false when activeElement is sibling of termContainer (modal scenario — Pitfall 1)` — Plan 94-03 implements `frontend/src/lib/isXtermFocused.ts`. Use real DOM (jsdom) with `document.body.appendChild` setup/teardown; do not stub `document.activeElement` directly (jsdom respects `el.focus()`).
9. `internal/daemon/search_config_test.go`
   - `TestSearchConfig_DefaultsZero` (SRC-02) — Plan 94-02
   - `TestSearchConfig_RoundTripJSON` — Plan 94-02
   - `TestPluginSettings_DefaultsMerge_SearchConfig` (Phase 93 fixture has no searchConfig → defaults populate) — Plan 94-02
10. `internal/webserver/plugin_settings_search_sse_test.go`
    - `TestPluginSettingsSSE_Search` (SSE broadcast emits settings:plugins with nested SearchConfig) — Plan 94-02
11. `internal/webserver/findbar_perf_e2e_test.go` — MUST start with `//go:build e2e` build tag (matches Phase 89 chromedp test pattern).
    - `TestFindBar_10kPerf` (SRC-03) — Plan 94-04
12. `internal/webserver/findbar_web_e2e_test.go` — MUST start with `//go:build e2e`.
    - `TestFindBar_Web` (SRC-05 web parity) — Plan 94-05
13. `internal/webserver/find_bar_test.go` (no build tag)
    - `TestAssets_AddonSearch` (WEB-01: GET `/assets/xterm/addons/addon-search.js` returns 200 + javascript content-type) — Plan 94-05 implements the actual route exercise (asset is already served from this plan; RED here because route registration confirmation deferred to web parity wave)
    - `TestTerminalHTML_FindBar` (web HTML contains `<div id="find-bar"` and addon-search script tag) — Plan 94-05
    - `TestTerminalJS_SearchAddon` (terminal.js contains `SearchAddon.SearchAddon` constructor expression) — Plan 94-05

For TS files: ensure each lives under a `__tests__` directory that vitest discovers (frontend/vitest.config.ts already includes both `src/__tests__/` and `src/components/**/__tests__/`). Verify by running `pnpm --filter ./frontend exec vitest --run --reporter=verbose --testNamePattern "RED scaffold"` — the new tests should appear and FAIL RED (not be skipped silently).

For the existing `find_bar_test.go` `TestAssets_AddonSearch`: we CAN run this one GREEN even at Wave 0 since the asset is already served (Task 1 added it to embed.go + the existing FileServerFS routes serve `vendor/xterm/addons/*` per Phase 93). Implement that ONE test fully (not RED skip). Pattern verbatim from `internal/webserver/assets_test.go::TestAssets_VendoredAddons` adapted for the single new path. The other two tests in that file (TestTerminalHTML_FindBar, TestTerminalJS_SearchAddon) stay as `t.Skip` RED scaffolds per Plan 94-05.

After creating all scaffolds, verify scaffolds compile and run:
```
cd /Users/ken/dev/agenthub
pnpm --filter ./frontend exec vitest --run src/components/FindBar src/lib/__tests__/isXtermFocused.test.ts 2>&1 | tail -40
go build ./... 2>&1 | head
go test ./internal/daemon/ -run TestSearchConfig -count=1 2>&1 | head
go test ./internal/webserver/ -run "TestPluginSettingsSSE_Search|TestAssets_AddonSearch|TestTerminalHTML_FindBar|TestTerminalJS_SearchAddon" -count=1 2>&1 | head
go test -tags e2e ./internal/webserver/ -run "TestFindBar_10kPerf|TestFindBar_Web" -count=1 2>&1 | head
```
TS scaffolds RED-fail (count of failures matches scaffold count). Go scaffolds report SKIP for the Plan-94-04/94-05 deferred tests AND PASS for `TestAssets_AddonSearch`. NOTHING should fail to compile.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./... && go test ./internal/webserver/ -run TestAssets_AddonSearch -count=1 && grep -lc "RED scaffold" frontend/src/components/FindBar/__tests__/*.test.tsx frontend/src/lib/__tests__/isXtermFocused.test.ts internal/daemon/search_config_test.go internal/webserver/plugin_settings_search_sse_test.go internal/webserver/findbar_perf_e2e_test.go internal/webserver/findbar_web_e2e_test.go internal/webserver/find_bar_test.go | wc -l | grep -qE "^[[:space:]]*(13|14)$"</automated>
  </verify>
  <acceptance_criteria>
    - All 13 scaffold files exist (`ls -1 frontend/src/components/FindBar/__tests__/*.test.tsx | wc -l` returns 7 AND `ls -1 internal/daemon/search_config_test.go internal/webserver/{plugin_settings_search_sse,findbar_perf_e2e,findbar_web_e2e,find_bar}_test.go frontend/src/lib/__tests__/isXtermFocused.test.ts | wc -l` returns 6)
    - Every scaffold contains the literal phrase `RED scaffold` (`grep -l "RED scaffold" frontend/src/components/FindBar/__tests__/*.test.tsx | wc -l` returns 7)
    - Every Go e2e scaffold has the `//go:build e2e` tag (`head -1 internal/webserver/findbar_perf_e2e_test.go | grep -c '//go:build e2e'` returns 1; same for findbar_web_e2e_test.go)
    - `go build ./...` exits 0 (all Go scaffolds compile)
    - `TestAssets_AddonSearch` PASSES (already-implemented test confirming addon-search.js is served at /assets/xterm/addons/addon-search.js)
    - Each scaffold file references `Plan 94-NN` for the implementing plan (`grep -c 'Plan 94-' <file>` >= 1 for every scaffold)
    - Each scaffold cites `94-VALIDATION.md` row OR `RESEARCH §...` (grep returns >= 1 per file)
  </acceptance_criteria>
  <done>
    All 11 Wave 0 scaffolds exist as RED tests + 2 web-asset scaffolds. `TestAssets_AddonSearch` is GREEN (asset already served). Every later plan has a target test to turn GREEN; Nyquist gate honored.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pnpm registry → frontend/node_modules | Supply-chain — addon-search@0.16.0 fetched from registry (Phase 90 SLSA L2 attestations cover release artifacts; lockfile pin in pnpm-lock.yaml is the per-PR gate) |
| frontend/node_modules → web/vendor | Local copy — vendor_drift_test.go enforces version parity at CI (this plan's load-bearing change) |
| web/vendor → embed.FS → browser | Server boundary — bundled into Go binary at build, served same-origin under existing v3.1 CSP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Tampering | web bundle vs frontend/node_modules copy of @xterm/addon-search | mitigate | Bump `internal/webserver/vendor_drift_test.go` min-count guard from 5 to 6 (Task 1, Step 5). Regex auto-detects new addon; min-count is the load-bearing assertion. |
| T-94-05 | Information Disclosure | web-mode origin/CSP | accept | Vendored under `web/vendor/`, served same-origin under existing v3.1 `script-src 'self'` CSP. No CSP amendment needed. Phase 89 pattern. |
| T-94-06 (Plan 94-01 only) | Tampering | RED scaffolds masking real failures | mitigate | All scaffolds use explicit `expect.fail(...)` (TS) or `t.Skip(...)` (Go) with a clear `Plan 94-NN must implement` message — never silent skip. Acceptance criteria grep verifies the marker phrase. |
</threat_model>

<verification>
- vendor_drift_test.go passes with 6 packages parsed from lockfile + VERSION
- TestAssets_AddonSearch passes (route serves addon-search.js)
- All RED scaffold files exist and contain Plan-94-NN references
- `go build ./...` exits 0
- frontend/package.json + pnpm-lock.yaml + web/vendor/xterm/VERSION all agree on `@xterm/addon-search@0.16.0`
</verification>

<success_criteria>
- `go test ./internal/webserver/ -run "TestXtermVendorVersionsMatchPnpmLock|TestAssets_AddonSearch" -count=1` exits 0
- `pnpm --filter ./frontend exec vitest --run src/components/FindBar src/lib/__tests__/isXtermFocused.test.ts 2>&1 | grep -c "RED scaffold"` reports >= 7 RED tests as expected
- 6 @xterm packages now governed by vendor-drift CI gate
- Wave 0 scaffolds in place for SRC-01, SRC-02, SRC-03, SRC-04, SRC-05
</success_criteria>

<output>
After completion, create `.planning/phases/94-search-addon-find-bar-desktop-web/94-01-SUMMARY.md`
</output>
