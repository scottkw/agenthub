---
phase: 149-google-antigravity-agent
plan: 03
type: execute
wave: 2
depends_on: ["149-01", "149-02"]
files_modified:
  - TESTING.md
  - README.md
autonomous: true
requirements: [AGENT-01]

must_haves:
  truths:
    - "TESTING.md registers the new/extended agy test files in the Suite Manifest and Traceability map (AGENT-01 rows)"
    - "TESTING.md Section 5 has an M-15 live-launch manual item for the waitlist-gated agy REPL"
    - "README documents Google Antigravity as a supported agent with the waitlist note"
    - "The full project test suite + type-check + build is green before phase verification"
  artifacts:
    - path: "TESTING.md"
      provides: "Suite Manifest update + AGENT-01 traceability rows + M-15 manual item"
      contains: "M-15"
    - path: "README.md"
      provides: "Google Antigravity in the supported-agents list + waitlist note"
      contains: "Antigravity"
  key_links:
    - from: "TESTING.md Traceability §4 AGENT-01 rows"
      to: "internal/pty/detect_test.go / frontend/src/lib/agentBadge.test.ts / etc."
      via: "repo-relative path column validated by check-traceability-paths.sh"
      pattern: "AGENT-01"
---

<objective>
Satisfy the mandatory AgentHub regression-test convention for Phase 149 and document the new agent: register the agy test files in TESTING.md (Suite Manifest + Traceability), add the M-15 live-launch manual checklist item (D-03 waitlist fallback), add the README waitlist note, and gate on the full green suite.

Purpose: The repo CLAUDE.md makes TESTING.md updates a hard rule for any phase that adds/renames tests (Sections 2/4/5/6). D-03 requires the live REPL launch to become a manual M-15 item since `agy` is waitlist-gated and cannot be installed for live UAT this phase. README must carry the waitlist note (CONTEXT D-03, §specifics).
Output: Updated TESTING.md, updated README.md, full-suite verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-google-antigravity-agent/149-CONTEXT.md
@.planning/phases/149-google-antigravity-agent/149-RESEARCH.md
@.planning/phases/149-google-antigravity-agent/149-VALIDATION.md
@.planning/phases/149-google-antigravity-agent/149-01-SUMMARY.md
@.planning/phases/149-google-antigravity-agent/149-02-SUMMARY.md

<interfaces>
<!-- TESTING.md structure (read 2026-06-23) the executor must follow -->

Section 2 Suite Manifest: a table (Group | Count | Location | Run Command | Guards) with a
  "Total" row, followed by a "> Note: Counts updated Phase NNN..." running changelog line.
  Phase 149 adds: 0 NEW Go test files (detect_test/path_windows_test/detector_test are EXTENDED,
  not new — extended files do not change the file count), 0 NEW vitest files (agentBadge.test.ts +
  style.hub.test.ts EXTENDED). So counts are UNCHANGED; append a Phase 149 note documenting the
  extensions (no count delta).

Section 4 Traceability Map: table (Requirement | Test File | Suite Group | Notes). Path column =
  repo-relative path ending .go/.ts/.tsx/.sh ONLY (no test names — those go in Notes).
  Validated by `bash tests/check-traceability-paths.sh` (must exit 0).

Section 5 Manual Checklist: categories A..H; last item is M-14. Phase 149 adds a new
  "Category I — Live Agent Launch (AGENT-01)" with item M-15. Format per existing items:
  bold M-NN, behavior description, "_Why not automatable:_", "_Source:_".

VALIDATION §Per-Task Verification Map → these AGENT-01 test files exist after Plans 01/02:
  internal/pty/detect_test.go (TestKnownCLIs, TestDetectCLIs_FindsAgy, TestDetectCLI_AgyNotFound)
  internal/daemon/path_windows_test.go (TestPlatformExtraBins_WindowsIncludesAgyBin)
  internal/status/detector_test.go (TestDetector_AgyIdle, TestDetector_AgyWaiting)
  frontend/src/lib/agentBadge.test.ts (agy modifier)
  frontend/src/components/__tests__/style.hub.test.ts (agy three-site source gates + WCAG comment)

README supported-agents text (~line 7 and ~line 76): "Claude Code, Codex, Gemini CLI, OpenCode"
  appears in the intro paragraph and the "CLI auto-detection" feature bullet — both list the 4 agents.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register agy tests in TESTING.md (Suite Manifest §2 + Traceability §4) and run the path-check</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md Section 2 (Suite Manifest table + the running "> Note: Counts updated Phase NNN" line — append a Phase 149 note)
    - TESTING.md Section 4 (Traceability Map — see the HELP-01 and FIX-02 rows for the exact path-column-only format; one row per test file)
    - TESTING.md Section 6 (Standing Convention — the rule being satisfied)
    - tests/check-traceability-paths.sh (the validator; path column must be a real repo-relative file ending in .go/.ts/.tsx/.sh)
  </read_first>
  <action>
    Section 2: append a Phase 149 sentence to the running "> Note:" line documenting that AGENT-01 EXTENDED existing files (internal/pty/detect_test.go, internal/daemon/path_windows_test.go, internal/status/detector_test.go, frontend/src/lib/agentBadge.test.ts, frontend/src/components/__tests__/style.hub.test.ts) with NO file-count delta (extensions, not new files). Do not change the Total counts.
    Section 4: add AGENT-01 rows (one per test FILE, path column only, test names in Notes):
      - `internal/pty/detect_test.go` | Go | knownCLIs has agy/"Google Antigravity"; DetectCLIs finds agy stub; DetectCLI("agy") ErrCLINotFound when absent
      - `internal/daemon/path_windows_test.go` | Go | platformExtraBins includes %LOCALAPPDATA%\agy\bin (Windows matrix)
      - `internal/status/detector_test.go` | Go | DefaultAgyPatterns idle (`> `) + waiting (`[y/n]`); PatternsForCLI("agy") not Fallback
      - `frontend/src/lib/agentBadge.test.ts` | vitest | agentBadgeModifier('agy') === 'agy'
      - `frontend/src/components/__tests__/style.hub.test.ts` | vitest | agy three color sites (#ff9e64) + honest WCAG comment (dark 8.72:1 / light 2.03:1)
    Then run the path-check script and confirm it exits 0.
  </action>
  <verify>
    <automated>bash tests/check-traceability-paths.sh</automated>
  </verify>
  <acceptance_criteria>
    - TESTING.md Section 4 contains 5 AGENT-01 rows, each path column a real repo-relative file ending in .go or .ts
    - TESTING.md Section 2 "> Note:" line mentions Phase 149 / AGENT-01 extensions
    - `bash tests/check-traceability-paths.sh` exits 0
    - `grep -c 'AGENT-01' TESTING.md` is >= 5
  </acceptance_criteria>
  <done>AGENT-01 test files registered in Suite Manifest + Traceability; path-check green.</done>
</task>

<task type="auto">
  <name>Task 2: Add M-15 live-launch manual item (§5) and README waitlist note</name>
  <files>TESTING.md, README.md</files>
  <read_first>
    - TESTING.md Section 5 (Categories A..H; last item M-14 under "Category H — In-App Help Page" — follow that item's exact format: bold M-NN, behavior, "_Why not automatable:_", "_Source:_")
    - README.md intro paragraph (~line 7: "Claude Code, Codex, Gemini CLI, OpenCode") and the "CLI auto-detection" feature bullet (~line 76, same 4-agent list) — both must add Google Antigravity
    - CONTEXT D-03 (the exact M-NN text mandate: "Live Antigravity launch — verify when waitlist access is granted")
    - MEMORY.md reference_live_uat_daemon_gotchas + reference_browser_uat_help_harness (why live agent launch cannot be automated this phase)
  </read_first>
  <action>
    TESTING.md Section 5: add a new "### Category I — Live Agent Launch (AGENT-01)" with item M-15. Behavior: when `agy` waitlist access is granted, run `agenthub new agy <dir>` (and verify the GUI/web picker shows "Google Antigravity"), confirm agy launches an interactive PTY REPL, auth completes via browser-loopback OAuth or the documented SSH/OTP degradation path, the status badge renders #ff9e64, and the card spine/chip + tab dot all show the agy color in lockstep. "_Why not automatable:_" closed-beta/waitlist binary (D-03) — agy cannot be installed for live UAT this phase; live PTY REPL + browser OAuth require a real binary. "_Source:_" CONTEXT D-03; 149-VALIDATION.md Manual-Only Verifications; AGENT-01 (#65).
    README.md: in BOTH the intro paragraph and the CLI auto-detection feature bullet, add "Google Antigravity" to the agent list. Add a concise waitlist note (e.g., a parenthetical or sentence): Google Antigravity (`agy`) is integrated as a selectable agent; it is currently in limited/waitlist rollout, so live launch is verified manually once access is granted (per the TESTING.md M-15 item). Keep the binary name `agy` and the display name "Google Antigravity" consistent with detection (D-09).
  </action>
  <verify>
    <automated>grep -q 'M-15' TESTING.md && grep -qi 'antigravity' README.md && grep -qi 'waitlist' README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - TESTING.md contains a bolded `**M-15**` item under a "Live Agent Launch" category referencing live agy REPL launch + waitlist gating
    - TESTING.md M-15 item includes "_Why not automatable:_" and "_Source:_" lines (format parity with M-14)
    - README.md mentions "Google Antigravity" in both the intro and the CLI auto-detection bullet
    - README.md contains a waitlist note for agy live launch
    - `grep -c 'antigravity' README.md` (case-insensitive) is >= 1
  </acceptance_criteria>
  <done>M-15 live-launch item present (D-03); README documents the agent + waitlist note.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite phase gate</name>
  <files>(verification only — no source edits)</files>
  <read_first>
    - .planning/phases/149-google-antigravity-agent/149-VALIDATION.md §Full suite command + Sampling Rate (phase gate = full suite green before /gsd:verify-work)
    - MEMORY.md feedback_post_merge_gate_run_tsc (tsc --noEmit + build are mandatory; vitest alone is insufficient)
  </read_first>
  <action>
    Run the full phase gate exactly as VALIDATION specifies: Go suite, frontend vitest, type-check, and frontend build. Command sequence: `go test ./... && cd frontend && pnpm test --run && pnpm exec tsc --noEmit && pnpm exec vite build`. This is verification-only (no source edits). If anything fails, the failure belongs to Plans 01/02's code changes — fix at the source and re-run; do not paper over. This task proves AGENT-01 is delivered at the source/unit level (D-03 acceptance) with the whole project green.
  </action>
  <verify>
    <automated>go test ./... && cd frontend && pnpm test --run && pnpm exec tsc --noEmit && pnpm exec vite build</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./...` exits 0
    - `cd frontend && pnpm test --run` exits 0
    - `cd frontend && pnpm exec tsc --noEmit` exits 0
    - `cd frontend && pnpm exec vite build` exits 0
  </acceptance_criteria>
  <done>Full project suite + type-check + build all green; AGENT-01 source-level acceptance met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auth-guidance documentation (if any) | README/TESTING text only — must never instruct credential entry into AgentHub |

## STRIDE Threat Register

RESEARCH §Security Domain: no new auth/network/input surfaces this phase. The only security-adjacent note: any auth guidance (D-13, deferred to post-M-15 per RESEARCH Open Question 3) must NOT capture or store Google credentials — it may only direct the user to run `agy auth login` externally. This plan adds documentation only.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-03 | Information Disclosure | Auth-guidance text capturing Google credentials | mitigate | README/TESTING text directs the user to run `agy auth login` externally; NO credential-input field or instruction to paste secrets into AgentHub. Trivially satisfied — this plan writes prose only. |
| T-149-SC | Tampering | npm/pip/cargo installs | n/a | No external packages added this phase (RESEARCH §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `bash tests/check-traceability-paths.sh` exits 0 (mandatory repo convention)
- `grep 'M-15' TESTING.md` present; `grep -i 'antigravity\|waitlist' README.md` present
- Full suite: `go test ./...` + `pnpm test --run` + `pnpm exec tsc --noEmit` + `pnpm exec vite build` all green
</verification>

<success_criteria>
- AGENT-01 test files registered in TESTING.md Suite Manifest §2 + Traceability §4 (convention satisfied)
- M-15 live-launch manual item present (D-03 waitlist fallback)
- README documents Google Antigravity + waitlist note (D-03, §specifics)
- Full project suite + tsc + build green (D-03 source-level acceptance)
</success_criteria>

<output>
Create `.planning/phases/149-google-antigravity-agent/149-03-SUMMARY.md` when done
</output>
