---
phase: 149-google-antigravity-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/pty/detect.go
  - internal/pty/detect_test.go
  - internal/daemon/path_windows.go
  - internal/daemon/path_windows_test.go
  - internal/status/detector.go
  - internal/status/detector_test.go
autonomous: true
requirements: [AGENT-01]

must_haves:
  truths:
    - "DetectCLIs() returns an 'agy' entry named \"Google Antigravity\" when an agy binary is on PATH"
    - "DetectCLI(\"agy\") resolves to the binary path, and ErrCLINotFound when absent"
    - "On Windows, AugmentServicePath includes %LOCALAPPDATA%\\agy\\bin so a service-mode daemon finds agy"
    - "An agy session classifies as idle at its '> ' prompt rather than perma-running"
  artifacts:
    - path: "internal/pty/detect.go"
      provides: "knownCLIs entry {Name: agy, DisplayName: Google Antigravity}"
      contains: "agy"
    - path: "internal/daemon/path_windows.go"
      provides: "Windows PATH entry %LOCALAPPDATA%\\agy\\bin"
      contains: "\"agy\", \"bin\""
    - path: "internal/status/detector.go"
      provides: "DefaultAgyPatterns + agy case in PatternsForCLI"
      contains: "DefaultAgyPatterns"
  key_links:
    - from: "internal/status/detector.go PatternsForCLI"
      to: "DefaultAgyPatterns"
      via: "switch/if on cliName == agy"
      pattern: "agy"
    - from: "internal/daemon/path.go AugmentServicePath"
      to: "platformExtraBins"
      via: "Windows build-tagged extra bins"
      pattern: "platformExtraBins"
---

<objective>
Add the Google Antigravity CLI (`agy`) to the Go backend so it is detected on PATH across all surfaces, discoverable when the daemon runs as a Windows service, and classified correctly by the status heuristic.

Purpose: `knownCLIs` is the single source of truth that flows to GUI/CLI/web; this plan wires detection + PATH + status so AGENT-01 launches uniformly through the existing engine with zero engine changes (CONTEXT D-13: clean PTY case requires no shim).
Output: One CLISpec row, one Windows PATH entry, a minimal `DefaultAgyPatterns`, and extended Go unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-google-antigravity-agent/149-CONTEXT.md
@.planning/phases/149-google-antigravity-agent/149-RESEARCH.md
@.planning/phases/149-google-antigravity-agent/149-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase 2026-06-23. Use directly — no exploration needed. -->

From internal/pty/detect.go:
  type CLISpec struct { Name string; DisplayName string }
  var knownCLIs = []CLISpec{
    {Name: "claude", DisplayName: "Claude Code"},
    {Name: "codex", DisplayName: "OpenAI Codex"},
    {Name: "gemini", DisplayName: "Gemini CLI"},
    {Name: "opencode", DisplayName: "OpenCode"},
  }
  func DetectCLIs() []DetectedCLI            // exec.LookPath per spec; flows to GUI/CLI/web
  func DetectCLI(name string) (*DetectedCLI, error) // ErrCLINotFound when absent/unknown

From internal/daemon/path_windows.go (//go:build windows):
  func platformExtraBins() []string
    // existing LOCALAPPDATA entries: filepath.Join(local, "pnpm"),
    //   filepath.Join(local, "Programs", "nodejs"),
    //   filepath.Join(local, "Microsoft", "WindowsApps")

From internal/status/detector.go:
  type PatternSet struct { Working, Idle, Waiting []*regexp.Regexp }
  func DefaultClaudePatterns() PatternSet
  func FallbackPatterns() PatternSet  // empty → conservative "running" default
  func PatternsForCLI(cliName string) PatternSet // currently: if "claude" → Default; else Fallback
  // classify priority: Waiting(suffix) > Idle(suffix) > Working(full) > default Running

Engine spawn (no change needed): SessionEngine.ResolveCLI(name) returns cliPaths[name]
override if set, else the bare name; engine spawns via PTY using PATH. agy flows uniformly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add agy to knownCLIs + extend detection unit tests</name>
  <files>internal/pty/detect.go, internal/pty/detect_test.go</files>
  <read_first>
    - internal/pty/detect.go (knownCLIs slice ~line 25 — the single insertion point; see how opencode/gemini rows are shaped)
    - internal/pty/detect_test.go (TestKnownCLIs_HasExpectedEntries asserts the exact expected name list + len; TestDetectCLIs_FindsInstalledCLIs writes a shell-script stub on PATH and asserts found — the analog for an agy found/not-found test)
  </read_first>
  <behavior>
    - TestKnownCLIs_HasExpectedEntries: expected list updated to {claude, codex, gemini, opencode, agy}; len assertion passes at 5
    - New test: DetectCLIs finds an `agy` stub written to a temp PATH dir (DisplayName == "Google Antigravity", non-empty Path); skip on Windows like the existing stub test
    - New test: DetectCLI("agy") returns ErrCLINotFound when no agy on PATH (stored-path override is engine-level, not detect-level — assert detect's not-found path here)
  </behavior>
  <action>
    Append exactly one row to knownCLIs: Name "agy", DisplayName "Google Antigravity" (per D-04 separate top-level entry; per D-09 the key MUST be the real binary name `agy`, NOT `antigravity` — using `antigravity` breaks exec.LookPath and every downstream surface). Add a trailing comment noting Phase 149. Then update TestKnownCLIs_HasExpectedEntries: add "agy" to the expected slice (now 5 entries). Add TestDetectCLIs_FindsAgy mirroring TestDetectCLIs_FindsInstalledCLIs (write a `#!/bin/sh` stub named `agy` in t.TempDir(), t.Setenv PATH, assert found with DisplayName "Google Antigravity"; runtime.GOOS == "windows" → t.Skip). Add TestDetectCLI_AgyNotFound asserting DetectCLI("agy") returns ErrCLINotFound against an empty temp PATH.
  </action>
  <verify>
    <automated>go test ./internal/pty/ -run 'TestKnownCLIs|TestDetectCLI' -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/pty/detect.go contains a CLISpec literal with Name "agy" and DisplayName "Google Antigravity"
    - `grep -c 'antigravity' internal/pty/detect.go` is 0 (key is `agy`, not `antigravity`; comment may say "Antigravity" but not the lowercase key) — verify the key token: `grep 'Name: *"agy"' internal/pty/detect.go` matches
    - `go test ./internal/pty/ -run TestKnownCLIs` exits 0
    - `go test ./internal/pty/ -run TestDetectCLIs_FindsAgy` exits 0
    - `go test ./internal/pty/ -run TestDetectCLI_AgyNotFound` exits 0
  </acceptance_criteria>
  <done>knownCLIs has 5 entries including agy/"Google Antigravity"; all pty detection tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add %LOCALAPPDATA%\agy\bin to Windows PATH augmentation + unit test</name>
  <files>internal/daemon/path_windows.go, internal/daemon/path_windows_test.go</files>
  <read_first>
    - internal/daemon/path_windows.go (platformExtraBins — the LOCALAPPDATA block appends pnpm/Programs/nodejs/WindowsApps; add the agy entry here)
    - internal/daemon/path_windows_test.go (TestPlatformExtraBins_PreservesExistingEntries lists the existing wants; TestPlatformExtraBins_LocalAppDataEmpty asserts env-dependent entries skip when LOCALAPPDATA empty — the analog shape for the agy assertions)
  </read_first>
  <behavior>
    - platformExtraBins() includes `C:\Users\test\AppData\Local\agy\bin` when LOCALAPPDATA is set (RESEARCH Fact 1: canonical install path is %LOCALAPPDATA%\agy\bin\agy.exe)
    - When LOCALAPPDATA is empty, no entry ending in `agy\bin` is returned (graceful degradation, mirroring the WindowsApps skip)
    - Existing entries (pnpm, Programs\nodejs, npm, Tailscale, PowerShell 7) are preserved
  </behavior>
  <action>
    In platformExtraBins(), inside the existing `if local := os.Getenv("LOCALAPPDATA"); local != ""` block, append filepath.Join(local, "agy", "bin") with a comment: `// agy CLI installer — %LOCALAPPDATA%\agy\bin\agy.exe (Phase 149)`. The parent dir is `agy`, NOT `Antigravity` (RESEARCH Pitfall: the Windows installer uses %LOCALAPPDATA%\agy\bin, not %LOCALAPPDATA%\Antigravity). Then add TestPlatformExtraBins_WindowsIncludesAgyBin (t.Setenv LOCALAPPDATA to `C:\Users\test\AppData\Local`, assert containsString(got, `C:\Users\test\AppData\Local\agy\bin`)) and extend TestPlatformExtraBins_LocalAppDataEmpty to also assert no element HasSuffix `agy\bin` when LOCALAPPDATA is empty. These tests are build-tagged `//go:build windows` and run on the Windows CI matrix.
  </action>
  <verify>
    <automated>GOOS=windows go vet ./internal/daemon/ && GOOS=windows go build ./internal/daemon/</automated>
  </verify>
  <acceptance_criteria>
    - internal/daemon/path_windows.go contains `filepath.Join(local, "agy", "bin")`
    - `grep -c 'Antigravity' internal/daemon/path_windows.go` is 0 (parent dir is `agy`)
    - internal/daemon/path_windows_test.go contains TestPlatformExtraBins_WindowsIncludesAgyBin asserting the `agy\bin` path
    - `GOOS=windows go build ./internal/daemon/` exits 0 (cross-compile sanity; the Windows-tagged test runs on CI Windows matrix per VALIDATION Wave 0)
    - `GOOS=windows go vet ./internal/daemon/` exits 0
  </acceptance_criteria>
  <done>platformExtraBins returns %LOCALAPPDATA%\agy\bin; Windows-tagged test added; cross-compile clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register minimal agy status patterns (idle + waiting)</name>
  <files>internal/status/detector.go, internal/status/detector_test.go</files>
  <read_first>
    - internal/status/detector.go (DefaultClaudePatterns is the analog: Idle uses `❯\s*$`, Waiting uses `\[y/n\]` variants; PatternsForCLI dispatches by cliName; FallbackPatterns returns empty → perma-running)
    - internal/status/detector_test.go (newClaudeDetector helper + TestDetector_ClaudeIdle feeds "...\n❯ " asserting StatusIdle; TestDetector_Waiting feeds "[y/n]" asserting StatusWaiting — mirror these for agy)
  </read_first>
  <behavior>
    - DefaultAgyPatterns(): Idle matches the agy `> ` prompt at end of a line (`>\s*$`); Waiting matches `[y/n]` confirmation variants (same as claude). Working left empty (no reliable indicator yet — FallbackPatterns default running applies during work; full tuning deferred to post-M-15 live sampling per CONTEXT D-13 + RESEARCH Open Question 1)
    - PatternsForCLI("agy") returns DefaultAgyPatterns (not FallbackPatterns), so an idle agy session classifies idle rather than perma-running (RESEARCH Pitfall 5)
    - PatternsForCLI for any other unknown name still returns FallbackPatterns (no regression)
  </behavior>
  <action>
    Add func DefaultAgyPatterns() PatternSet returning Idle: [`>\s*$`] and Waiting: [`\[y/n\]|\[Y/n\]|\[y/N\]`], Working empty. Add a comment marking the patterns [ASSUMED] from TUI screenshots (RESEARCH), to be verified/tuned after M-15 live access. Convert PatternsForCLI from the `if cliName == "claude"` form to a switch (case "claude" → DefaultClaudePatterns; case "agy" → DefaultAgyPatterns; default → FallbackPatterns). Add TestDetector_AgyIdle (feed "Output done\n> " → StatusIdle) and TestDetector_AgyWaiting (feed "Apply changes? [y/n]" → StatusWaiting) using a local newAgyDetector helper that constructs NewDetector with DefaultAgyPatterns. Add TestPatternsForCLI_AgyNotFallback asserting PatternsForCLI("agy").Idle is non-empty.
  </action>
  <verify>
    <automated>go test ./internal/status/ -run 'Agy|PatternsForCLI' -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/status/detector.go contains func DefaultAgyPatterns
    - PatternsForCLI contains `case "agy"` returning DefaultAgyPatterns
    - `go test ./internal/status/ -run TestDetector_AgyIdle` exits 0
    - `go test ./internal/status/ -run TestDetector_AgyWaiting` exits 0
    - `go test ./internal/status/` (full package) exits 0 — no regression to claude/fallback tests
  </acceptance_criteria>
  <done>agy idle/waiting classify correctly; full status package green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agy binary launch (engine → PTY) | Same boundary as all existing agents; agy is spawned via the identical cliPath+args+WorkDir relay path |

## STRIDE Threat Register

RESEARCH §Security Domain: this phase introduces NO new authentication flows, network endpoints, or input-validation surfaces. `agy` reuses the existing capability-based session-auth / PTY-relay / CORS model identically to claude/codex/gemini/opencode. No new ASVS L1 categories apply.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-01 | Spoofing | exec.LookPath("agy") resolving a hijacked binary | accept | Identical exposure to all 4 existing agents; PATH augmentation only adds well-known per-platform install dirs (no new privileged lookup). No agy-specific change to trust posture. |
| T-149-02 | Tampering | Stored CLI-path override pointing agy at an arbitrary binary | accept | Reuses the existing engine cliPaths override + stale-path filter (engine.go:182) shared by every agent; no new code path introduced here. |
| T-149-SC | Tampering | npm/pip/cargo installs | n/a | No external packages added this phase (RESEARCH §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `go test ./internal/pty/... ./internal/status/...` green (quick run per VALIDATION Sampling Rate)
- `GOOS=windows go build ./internal/daemon/` clean (Windows PATH entry compiles)
- `grep -rc 'antigravity' internal/pty/detect.go internal/daemon/path_windows.go` is 0 for the lowercase key token (D-09)
- `gofmt -l internal/pty/ internal/daemon/ internal/status/` prints nothing (all modified Go files formatted; CLAUDE.md `go fmt` convention)
- `golangci-lint run ./internal/pty/... ./internal/daemon/... ./internal/status/...` exits 0 (CLAUDE.md lint gate; if golangci-lint is unavailable in the environment, fall back to `go vet ./internal/pty/... ./internal/daemon/... ./internal/status/...`)
</verification>

<success_criteria>
- knownCLIs contains {Name: "agy", DisplayName: "Google Antigravity"} (D-04, D-09)
- %LOCALAPPDATA%\agy\bin in platformExtraBins with a Windows-matrix test (RESEARCH Fact 1 + Pitfall 2)
- PatternsForCLI("agy") returns minimal idle/waiting patterns, not Fallback (D-13, RESEARCH Pitfall 5)
- All extended Go unit tests green; no regression in existing pty/daemon/status suites
</success_criteria>

<output>
Create `.planning/phases/149-google-antigravity-agent/149-01-SUMMARY.md` when done
</output>
