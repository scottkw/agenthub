---
phase: 149-google-antigravity-agent
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/agentBadge.ts
  - frontend/src/lib/agentBadge.test.ts
  - frontend/src/style.css
  - frontend/src/components/__tests__/style.hub.test.ts
autonomous: true
requirements: [AGENT-01]

must_haves:
  truths:
    - "agentBadgeModifier('agy') returns 'agy' so the tab dot + card spine + chip resolve the agy color"
    - "All three per-agent color sites (tab dot, card spine, card chip) carry the agy color #ff9e64 in lockstep"
    - "The agy badge contrast numbers are documented honestly at source (dark 8.72:1 PASS, light 2.03:1 FAIL)"
  artifacts:
    - path: "frontend/src/lib/agentBadge.ts"
      provides: "agy case in the switch (single source of truth)"
      contains: "case 'agy'"
    - path: "frontend/src/style.css"
      provides: "three agy badge CSS sites at #ff9e64"
      contains: "data-agent=\"agy\""
  key_links:
    - from: "frontend/src/lib/agentBadge.ts"
      to: "style.css .tab__agent-badge--agy / .hub-card[data-agent=agy]"
      via: "BEM modifier key === data-agent key === knownCLIs key"
      pattern: "agy"
---

<objective>
Give the Google Antigravity agent (`agy`) a distinct status-badge color identity across every per-agent color site in the frontend, with honest source-level WCAG documentation.

Purpose: D-06 locks the badge color at #ff9e64 (TokyoNight orange, the only unused main accent). D-08 requires all three color sites + the agentBadge.ts switch updated in lockstep so the tab dot and card spine/chip can never drift. D-07 requires hex-level contrast verification documented honestly (this is a colorblind project).
Output: One switch case, three CSS rules, extended vitest source-gate tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-google-antigravity-agent/149-CONTEXT.md
@.planning/phases/149-google-antigravity-agent/149-RESEARCH.md
@.planning/phases/149-google-antigravity-agent/149-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase 2026-06-23. -->

From frontend/src/lib/agentBadge.ts:
  export function agentBadgeModifier(cli: string): string | null
  // existing switch: cases 'claude'|'opencode'|'codex'|'gemini'|'cursor'|'aider' return cli;
  //   shell variants → 'shell'; default → null

style.css — THREE color sites, each grouped by agent (add agy to all three):
  Site 1 (tab dot, ~line 1717, after .tab__agent-badge--shell):
    .tab__agent-badge--claude { background: #7aa2f7; } ... --aider { background: #f7768e; }
  Site 2 (card spine, ~line 4809, after .hub-card[data-agent="shell"]):
    .hub-card[data-agent="claude"] { border-left: 3px solid #7aa2f7; } /* claude — periwinkle */ ...
  Site 3 (card chip, ~line 5026, after .hub-card[data-agent="shell"] .hub-card__badge):
    .hub-card[data-agent="claude"] .hub-card__badge { color: #7aa2f7; border-color: rgba(122,162,247,0.45); } ...

agy color (D-06, LOCKED): #ff9e64  → rgba border: rgba(255, 158, 100, 0.45)

WCAG (RESEARCH §WCAG-AA Contrast Analysis, D-07): #ff9e64 dark hub-surface #16181f = 8.72:1 PASS;
light hub-surface #ffffff = 2.03:1 FAIL. Same gap as ALL 7 existing badges (none have light overrides).
Do NOT claim AA pass in light; document the real numbers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add agy case to agentBadge.ts switch + test</name>
  <files>frontend/src/lib/agentBadge.ts, frontend/src/lib/agentBadge.test.ts</files>
  <read_first>
    - frontend/src/lib/agentBadge.ts (the switch — agy joins the group of cases that return cli verbatim, alongside claude/opencode/codex/gemini/cursor/aider; per D-09 the modifier MUST be 'agy', the real binary key, NOT 'antigravity')
    - frontend/src/lib/agentBadge.test.ts (each existing case has a `returns 'X' for cli='X'` test — mirror for agy; note the null-fallback tests at the end must still pass)
  </read_first>
  <behavior>
    - agentBadgeModifier('agy') === 'agy'
    - agentBadgeModifier of unknown/empty still returns null (no regression)
  </behavior>
  <action>
    Add `case 'agy':` to the group of fall-through cases that `return cli` (immediately alongside the existing aider/cursor cases). The returned modifier key MUST be `agy` (D-09: equals the knownCLIs key equals the real binary name; using `antigravity` would never match the CSS data-agent="agy" or the Go detection). Add a test `returns 'agy' for cli='agy'` mirroring the existing per-agent tests.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run agentBadge</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/lib/agentBadge.ts switch contains `case 'agy'`
    - `grep -c "case 'antigravity'" frontend/src/lib/agentBadge.ts` is 0
    - frontend/src/lib/agentBadge.test.ts contains a test asserting agentBadgeModifier('agy') toBe('agy')
    - `cd frontend && pnpm test --run agentBadge` exits 0 (all cases + null-fallbacks green)
  </acceptance_criteria>
  <done>agentBadgeModifier('agy') === 'agy'; agentBadge test suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add agy color to all three CSS sites in lockstep + WCAG comment + source-gate tests</name>
  <files>frontend/src/style.css, frontend/src/components/__tests__/style.hub.test.ts</files>
  <read_first>
    - frontend/src/style.css site 1 ~line 1711-1717 (.tab__agent-badge--* block; agy goes after --shell)
    - frontend/src/style.css site 2 ~line 4802-4810 (.hub-card[data-agent="*"] spine block with COLORBLIND-SAFE header comment; agy goes in this group)
    - frontend/src/style.css site 3 ~line 5017-5026 (.hub-card[data-agent="*"] .hub-card__badge chip block; agy goes after shell)
    - frontend/src/components/__tests__/style.hub.test.ts (the GAP-04 block lists all 8 spine rules in the "all agent spine rules are present" test, and asserts the claude chip/spine exact source strings — add agy to the presence assertion and add agy-specific assertions mirroring claude)
  </read_first>
  <behavior>
    - .tab__agent-badge--agy { background: #ff9e64; } present
    - .hub-card[data-agent="agy"] spine rule with border-left: 3px solid #ff9e64 present
    - .hub-card[data-agent="agy"] .hub-card__badge chip rule with color: #ff9e64 and border-color rgba(255, 158, 100, 0.45) present
    - The tab-dot rule carries a source comment documenting both contrast numbers (dark 8.72:1 PASS; light 2.03:1 FAIL — same gap as all existing agents, text chip carries identity)
  </behavior>
  <action>
    Add the agy color to ALL THREE sites in one pass (D-08 lockstep — missing any one causes drift between tab dot and card spine/chip):
    Site 1: `.tab__agent-badge--agy { background: #ff9e64; }` with the WCAG-honest comment: `/* agy — orange; dark: 8.72:1 AA PASS; light: 2.03:1 FAIL (same gap as all existing agents — text chip carries identity) */` (D-07: document the actual numbers, do NOT claim a blanket AA pass; user is colorblind — verified at hex source, not by eye).
    Site 2: `.hub-card[data-agent="agy"] { border-left: 3px solid #ff9e64; } /* agy — orange */` placed in the spine group (alongside the unknown fallback row).
    Site 3: `.hub-card[data-agent="agy"] .hub-card__badge { color: #ff9e64; border-color: rgba(255, 158, 100, 0.45); }` placed in the chip group.
    The data-agent key MUST be `agy` (D-09 — matches agentBadge.ts modifier and knownCLIs key; `antigravity` would never match).
    Then extend style.hub.test.ts: add "agy" to the "all agent spine rules are present" presence test; add a test asserting the exact tab-dot rule `.tab__agent-badge--agy` with #ff9e64; add a test asserting the spine rule `.hub-card[data-agent="agy"]` with `border-left: 3px solid #ff9e64`; add a test asserting the chip rule contains `color: #ff9e64`; add a source-assertion test that the WCAG comment documents both `8.72:1` and `2.03:1` (honest documentation gate per D-07).
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run style.hub</automated>
  </verify>
  <acceptance_criteria>
    - style.css contains `.tab__agent-badge--agy` with `#ff9e64`
    - style.css contains `.hub-card[data-agent="agy"]` spine rule with `border-left: 3px solid #ff9e64`
    - style.css contains `.hub-card[data-agent="agy"]` chip rule with `color: #ff9e64` and `rgba(255, 158, 100, 0.45)`
    - style.css agy comment contains both `8.72:1` and `2.03:1` (honest WCAG numbers, not a blanket "AA pass")
    - `grep -c 'data-agent="antigravity"' frontend/src/style.css` is 0
    - `cd frontend && pnpm test --run style.hub` exits 0
  </acceptance_criteria>
  <done>All three agy color sites present at #ff9e64 with honest WCAG comment; style.hub source gates green.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-merge frontend gate — tsc + full vitest</name>
  <files>frontend/ (verification only — no source edits)</files>
  <read_first>
    - frontend/vitest.config.ts (test config; full run is `pnpm test --run`)
    - MEMORY.md feedback_post_merge_gate_run_tsc — vitest tolerates TS errors that `tsc && vite build` (wails dev) rejects; a green vitest run is NOT proof the app builds, so tsc --noEmit is mandatory for any TS-touching plan
  </read_first>
  <action>
    Run the frontend wave-merge gate: `cd frontend && pnpm test --run && pnpm exec tsc --noEmit`. This is a verification-only task (no source edits) ensuring the agentBadge + style.css changes do not break the type-check or any other vitest suite. If tsc reports errors introduced by this plan's edits, fix them; the edits here are additive (one switch case, three CSS rules, test additions) so no type errors are expected.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm test --run` exits 0 (full vitest suite, no regression)
    - `cd frontend && pnpm exec tsc --noEmit` exits 0 (type-check clean — the gate vitest alone does not provide)
  </acceptance_criteria>
  <done>Full frontend vitest suite green AND tsc --noEmit clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is presentation-only CSS/TS; no data crosses a trust boundary. |

## STRIDE Threat Register

RESEARCH §Security Domain: no new auth/network/input surfaces. Badge color identity is pure frontend presentation.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-02F | Information Disclosure | Badge color as sole identity cue | accept | Colorblind-safe by design: agent is also shown as the `{cli}` text chip + display name in the picker; color is reinforcement only (documented in the existing COLORBLIND-SAFE source comments, extended for agy). |
| T-149-SC | Tampering | npm installs | n/a | No new npm packages added this phase. |
</threat_model>

<verification>
- `cd frontend && pnpm test --run agentBadge && pnpm test --run style.hub` green
- `cd frontend && pnpm exec tsc --noEmit` clean (mandatory — vitest does not catch what wails dev rejects)
- grep confirms `agy` (not `antigravity`) is the key in both agentBadge.ts and style.css
</verification>

<success_criteria>
- agentBadgeModifier('agy') === 'agy' (D-09)
- Three CSS sites carry #ff9e64 in lockstep (D-06, D-08)
- WCAG numbers documented honestly at source: dark 8.72:1 PASS, light 2.03:1 FAIL (D-07)
- Full vitest suite + tsc --noEmit both green
</success_criteria>

<output>
Create `.planning/phases/149-google-antigravity-agent/149-02-SUMMARY.md` when done
</output>
