---
phase: 139-card-rendering-tab-strip
plan: 02
type: execute
wave: 2
depends_on: ["139-01"]
files_modified:
  - frontend/src/components/TabBar.tsx
  - frontend/src/style.css
autonomous: true
requirements: [TAB-01, TAB-02, TAB-03]
must_haves:
  truths:
    - "D-05: Tabs shrink below the old 80px floor when many are open and collapse to an icon-only floor (status dot + close ×) where the name disappears; only then does the strip scroll"
    - "D-09: Scroll-position-aware chevrons (‹/›) appear only when the tab list overflows and reach every tab; no native scrollbar is exposed"
    - "D-06 / D-07 / D-08: Close ×, the Phase 98 progress underline (tab__progress), rename (via the right-click tab__context-menu), and a full-name hover title/tooltip all remain functional at the icon-only floor"
  artifacts:
    - path: "frontend/src/components/TabBar.tsx"
      provides: "Chevron state + ResizeObserver + outer-div title"
      contains: "ResizeObserver"
    - path: "frontend/src/style.css"
      provides: ".tab flex-shrink:1 + min-width floor + @container hide + chevron rules"
      contains: "container-type: inline-size"
  key_links:
    - from: "frontend/src/components/TabBar.tsx"
      to: ".tab-list scroll container"
      via: "listRef.current.scrollBy on chevron click"
      pattern: "scrollBy"
    - from: "frontend/src/style.css @container rule"
      to: ".tab__name / .tab__rename-input"
      via: "display:none at the icon-only floor"
      pattern: "@container"
---

<objective>
Implement the browser-style (Chrome-like) tab strip: tabs flex-shrink from 180px max past the current 80px min down to an icon-only floor (status dot + close ×), then the strip scrolls via scroll-position-aware ‹ / › chevrons. All Phase 98 / rename / close affordances survive at the floor. This is the TAB-01..03 cluster — fully independent of the CARD-05 VT work (no file overlap).

Purpose: Make every tab reachable regardless of count, matching Chrome favicon-only behavior (CONTEXT D-05/D-09).
Output: GREEN on the TabBar tab-strip tests authored in Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-card-rendering-tab-strip/139-RESEARCH.md
@.planning/phases/139-card-rendering-tab-strip/139-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tab flex-shrink + icon-only floor (CSS) — TAB-01</name>
  <files>frontend/src/style.css, frontend/src/components/TabBar.tsx</files>
  <read_first>
    - frontend/src/style.css (lines 82–135 — .tab-bar / .tab-list / .tab rules; lines 102–106 scrollbar-width:none and ::-webkit-scrollbar hide MUST be preserved)
    - frontend/src/components/TabBar.tsx (lines 192–251 — .tab structure: tab__status, tab__name, tab__rename-input, tab__close, tab__progress are all children/siblings of .tab)
    - frontend/src/components/__tests__/TabBar.test.tsx (the floor/title cases authored in Plan 01)
    - .planning/phases/139-card-rendering-tab-strip/139-PATTERNS.md (section "frontend/src/style.css" — exact before/after for .tab; Pitfall 4 — do NOT hide tab__status/tab__close/tab__progress)
  </read_first>
  <behavior>
    - Test (Plan 01 title case): the OUTER .tab div carries title === full tab name (so name is discoverable when tab__name is hidden)
    - Visual floor: at narrow width the .tab__name and .tab__rename-input are display:none; tab__status, tab__close, tab__progress remain visible
  </behavior>
  <action>
    In frontend/src/style.css change the .tab rule: flex-shrink: 0 → flex-shrink: 1; min-width: 80px → min-width: 32px; add container-type: inline-size. Add a new @container (max-width: 59px) block that sets .tab__name { display: none } and .tab__rename-input { display: none } — scope ONLY those two selectors (Pitfall 4: never hide .tab__status, .tab__close, or .tab__progress). Pick the floor px (32 content box ≈ 52 total) so the status dot + close × stay legible; if 32 clips the close ×, raise it and record the chosen value in the SUMMARY. Preserve unchanged: scrollbar-width:none (line ~102), .tab-list::-webkit-scrollbar { display:none } (lines ~104–106), overflow-x:auto on .tab-list, and max-width:180px. In frontend/src/components/TabBar.tsx move/duplicate the title={titleText-or-tab.name} attribute onto the OUTER <div className="tab ..."> (currently title is only on the inner tab__name span at ~line 222) so D-07 holds at the floor — use the full tab name (tab.name) for the outer title so the hover tooltip is the name itself, not the rename hint.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && grep -q "flex-shrink: 1" src/style.css && grep -q "container-type: inline-size" src/style.css && grep -q "@container" src/style.css && pnpm test -- --run src/components/__tests__/TabBar.test.tsx 2>&1 | grep -qi "title"</automated>
  </verify>
  <acceptance_criteria>
    - style.css .tab block contains `flex-shrink: 1`, `min-width: 32px` (or the documented higher floor), and `container-type: inline-size`
    - style.css contains `@container (max-width: 59px)` (or the documented threshold) that hides `.tab__name` and `.tab__rename-input` ONLY
    - style.css still contains `scrollbar-width: none` and `::-webkit-scrollbar` display:none (preserved)
    - TabBar.tsx outer `.tab` div has a `title` attribute bound to the full tab name; the Plan 01 title test passes
  </acceptance_criteria>
  <done>Tabs shrink to an icon-only floor; name + inline-rename hide at the floor while status/close/progress survive; full-name tooltip on the outer tab; scrollbar hiding preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Overflow chevrons + scroll-position awareness (TAB-02) and floor-affordance wiring (TAB-03)</name>
  <files>frontend/src/components/TabBar.tsx, frontend/src/style.css</files>
  <read_first>
    - frontend/src/components/TabBar.tsx (lines 110–138 existing state/useEffects; lines 173–307 the return/JSX incl. contextMenu block and tab__close/tab__progress)
    - frontend/src/components/TerminalPanel.tsx (lines 678–687 — the ResizeObserver setup + disconnect cleanup idiom to copy)
    - frontend/src/components/__tests__/TabBar.test.tsx (Plan 01 chevron + position-aware + context-menu-rename cases)
    - frontend/src/style.css (the .tab-bar block ~lines 82–92 for chevron background/border color tokens #16161e / #292e42 / #9aa5ce / #c0caf5)
    - .planning/phases/139-card-rendering-tab-strip/139-PATTERNS.md (sections "frontend/src/components/TabBar.tsx" and "frontend/src/style.css" chevron rules; Shared Patterns → ResizeObserver cleanup)
  </read_first>
  <behavior>
    - Test (Plan 01 chevron): when scrollWidth > clientWidth a button[aria-label="Scroll tabs right"] renders; when not overflowing, neither chevron renders
    - Test (Plan 01 position-aware): at scrollLeft===0 the left chevron (aria-label="Scroll tabs left") is hidden; at end the right chevron is hidden
    - Test (Plan 01 rename): right-click context menu Rename fires onRename without the tab__name double-click target (works at the icon-only floor)
  </behavior>
  <action>
    In frontend/src/components/TabBar.tsx add state: listRef = useRef<HTMLDivElement>(null), canScrollLeft / canScrollRight = useState(false). Add a checkScroll() that sets canScrollLeft = el.scrollLeft > 0 and canScrollRight = el.scrollLeft + el.clientWidth < el.scrollWidth - 1. Add a useEffect that attaches a passive 'scroll' listener and a ResizeObserver(checkScroll) on listRef.current, calls checkScroll() once initially, and on cleanup removes the listener and calls ro.disconnect() (copy the TerminalPanel.tsx cleanup shape). Attach ref={listRef} to the existing <div className="tab-list">. Render a left <button className="tab-bar__chevron tab-bar__chevron--left" aria-label="Scroll tabs left" tabIndex={0}> only when canScrollLeft, and a right counterpart (aria-label="Scroll tabs right") only when canScrollRight; onClick calls listRef.current?.scrollBy({ left: ∓160, behavior: 'smooth' }). Chevron glyphs ‹ / ›. Because they are native <button>s they answer Enter/Space — no extra keydown handler needed (D-09 keyboard accessibility). Confirm the existing tab__context-menu rename path (D-06) and the tab__progress underline (D-08) are untouched so they remain functional at the floor. In frontend/src/style.css add .tab-bar__chevron rules (flex-shrink:0; width:24px; height:100%; background #16161e; border:none; color #9aa5ce; font-size:16px; cursor:pointer; centered flex; border-right 1px solid #292e42) and .tab-bar__chevron--right (border-right:none; border-left:1px solid #292e42) and a :hover color #c0caf5. Do NOT expose any native scrollbar (D-09).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && grep -q "ResizeObserver" src/components/TabBar.tsx && grep -q "Scroll tabs right" src/components/TabBar.tsx && grep -q "tab-bar__chevron" src/style.css && pnpm test -- --run src/components/__tests__/TabBar.test.tsx 2>&1 | grep -qiE "pass|✓"</automated>
  </verify>
  <acceptance_criteria>
    - TabBar.tsx contains a ResizeObserver on listRef with a disconnect() cleanup and a 'scroll' listener removed in cleanup
    - TabBar.tsx renders chevron buttons gated by canScrollLeft / canScrollRight with aria-labels "Scroll tabs left" / "Scroll tabs right" and scrollBy({behavior:'smooth'}) handlers
    - style.css contains `.tab-bar__chevron` and `.tab-bar__chevron--right` rules
    - `pnpm test -- --run src/components/__tests__/TabBar.test.tsx` is GREEN (chevron, position-aware, rename-at-floor cases)
  </acceptance_criteria>
  <done>Position-aware chevrons appear only on overflow and scroll the existing overflow-x container; context-menu rename and progress underline remain functional at the floor; no native scrollbar exposed; TabBar tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → tab strip DOM | Local UI interaction only; tab names originate from session metadata the user created |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-02 | Information disclosure | title tooltip exposing full tab name | accept | Tab names are user-authored session labels shown only to the local user; no new disclosure |
| T-139-03 | Denial | ResizeObserver / scroll listener leak | mitigate | useEffect cleanup removes the scroll listener and calls ro.disconnect() (TerminalPanel cleanup idiom) — no leaked observers across tab changes |
| T-139-SC | Tampering | npm installs | accept | No new packages — pure CSS + React using existing APIs (ResizeObserver, scrollBy) |
</threat_model>

<verification>
- `pnpm test -- --run src/components/__tests__/TabBar.test.tsx` GREEN.
- `grep` gates confirm the CSS floor + chevron rules and the ResizeObserver wiring landed.
- Manual/visual (live app) check of the icon-only floor + progress underline is recorded as a human-check in the SUMMARY (the user is colorblind — verify the status-dot legibility note, not color meaning).
</verification>

<success_criteria>
- Tabs flex-shrink to an icon-only floor; name/inline-rename hidden at the floor; status/close/progress preserved.
- Overflow chevrons appear only on overflow, are position-aware, keyboard-accessible, and reach every tab.
- Rename (context menu) and the progress underline remain functional at the floor.
- All Plan 01 TabBar tests pass; native scrollbar stays hidden.
</success_criteria>

<output>
Create `.planning/phases/139-card-rendering-tab-strip/139-02-SUMMARY.md` when done. Record the chosen floor px and threshold.
</output>
