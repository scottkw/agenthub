---
phase: 141-redesign-implementation
plan: 02
type: execute
wave: 2
depends_on: ["141-01"]
files_modified:
  - frontend/src/style.css
autonomous: true
requirements: [RDS-02, RDS-04]
must_haves:
  truths:
    - "Sidebar, Welcome tab, tab bar, and status-bar chrome consume --hub-* tokens (no hardcoded chrome hex) so the light/dark toggle repaints them uniformly"
    - "D-03-fenced selectors (.tab__agent-badge--*, .tab-status-bar__state--*) keep their hardcoded hex"
    - "Hover transitions on migrated selectors live inside prefers-reduced-motion: no-preference guards with reduce fallbacks"
  artifacts:
    - path: "frontend/src/style.css"
      provides: "Tokenized sidebar (218–293), Welcome (1302–1404), tab bar + status bar + terminal container (82–368) rules"
      contains: "var(--hub-surface)"
  key_links:
    - from: "migrated chrome selectors"
      to: "--hub-* token declarations"
      via: "var() reference resolved in both theme blocks"
      pattern: "var\\(--hub-"
---

<objective>
Migrate the first batch of non-Hub surface CSS in `style.css` — the app sidebar,
the Welcome tab (S-01), and the terminal tab bar / tmux-style status bar / terminal
container (S-03) — from hardcoded TokyoNight hex to the `--hub-*` token system, so
the `[data-ui-theme="light"]` toggle works uniformly across these surfaces. Wrap
hover transitions in motion guards.

Purpose: RDS-02 (redesign applied) and RDS-04 (colorblind-safe both themes,
reduced-motion honored) for these three surfaces. Recolor-only (D-13) — no layout,
spacing, or structural changes.

Output: Tokenized sidebar/Welcome/tab-bar/status-bar rules with D-03 fences intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-redesign-implementation/141-UI-SPEC.md
@.planning/phases/141-redesign-implementation/141-RESEARCH.md
@.planning/phases/141-redesign-implementation/141-PATTERNS.md
@.planning/phases/141-redesign-implementation/141-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate sidebar (218–293) and Welcome tab (1302–1404) to tokens</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css lines 218–293 (sidebar) and ~4537 (`.sidebar__item--active`), and lines 1302–1404 (Welcome tab)
    - 141-RESEARCH.md §"Sidebar" hex table (lines 408–433) and §"S-01: Welcome Tab" hex table (lines 437–457)
    - 141-PATTERNS.md §"Sidebar block migration" (lines 115–197) and §"S-01 Welcome Tab block migration" (lines 201–214)
  </read_first>
  <action>
    Selector-by-selector (NOT global find/replace — Pitfall 4), replace each hex in the
    sidebar and Welcome rules with the token from the RESEARCH hex tables: `#16161e`→
    `var(--hub-surface)`, `#292e42`→`var(--hub-border)`, `#9aa5ce`→`var(--hub-text-muted)`,
    `#1e2030`→`var(--hub-surface-elevated)`, `#c0caf5`→`var(--hub-text-primary)`,
    `#a9b1d6`→`var(--hub-text-secondary)`, `#1a1b26`→`var(--hub-bg)`, `#7aa2f7`→
    `var(--hub-accent)`. For `.sidebar__item--active` at ~4537 replace the raw
    `rgba(122,162,247,0.12)` with `var(--hub-sidebar-item-active-bg)` and drop the fallback
    hex from `var(--hub-accent, #7aa2f7)` so it reads `var(--hub-accent)`. Wrap the
    `.sidebar`/`.sidebar__toggle`/`.sidebar__item` hover/width transitions per the motion
    guard in 141-PATTERNS.md lines 179–197 (no-preference guard + reduce fallback). No new
    `[data-ui-theme="light"]` per-selector overrides are needed — all tokens used here are
    already declared in both blocks.
  </action>
  <verify>
    <!-- Negated colorblind hex gate (user is colorblind — this gate is CRITICAL).
         Each grep exits non-zero when forbidden chrome hex remains; the leading `!`
         flips that so the command exits 0 ONLY when NO chrome hex is present.
         `rgba(0,` (D-03-fenced shadow opacity) is excluded — it is not theme color. -->
    <automated>! sed -n '218,293p' frontend/src/style.css | grep -E '#[0-9a-fA-F]{3,6}' | grep -v 'rgba(0,' | grep -q . && ! sed -n '1302,1404p' frontend/src/style.css | grep -E '#[0-9a-fA-F]{3,6}' | grep -v 'rgba(0,' | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `! sed -n '218,293p' frontend/src/style.css | grep -E '#[0-9a-fA-F]{3,6}' | grep -v 'rgba(0,' | grep -q .` exits 0 (no chrome hex in the sidebar range; only `rgba(0,...)` shadow opacity, if any, is permitted).
    - `! sed -n '1302,1404p' frontend/src/style.css | grep -E '#[0-9a-fA-F]{3,6}' | grep -v 'rgba(0,' | grep -q .` exits 0 (no hex in the Welcome range).
    - `.sidebar__item--active` uses `var(--hub-sidebar-item-active-bg)` and `var(--hub-accent)` (no fallback hex): `grep -n 'var(--hub-accent, #7aa2f7)' frontend/src/style.css` returns 0.
    - Sidebar hover/width transitions are inside a `@media (prefers-reduced-motion: no-preference)` block with a matching `reduce` fallback.
  </acceptance_criteria>
  <done>Sidebar + Welcome chrome fully tokenized; motion-guarded; no chrome hex remains in those ranges.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate tab bar + status bar + terminal container (82–368); preserve D-03 fences</name>
  <files>frontend/src/style.css</files>
  <read_first>
    - frontend/src/style.css lines 82–368 (tab bar, `.tab*`, `.tab-bar__chevron`, `.tab-status-bar*`, `.terminal-container`) and agent-badge rules ~1097–1103
    - 141-RESEARCH.md §"S-03: Terminal / Tab Bar / Status Bar" hex tables (lines 461–528) — including the explicit "Do NOT migrate" lists at lines 490–499 and 516–521
    - 141-PATTERNS.md §"S-03 Tab Bar + Status Bar block migration" (lines 218–261), especially the D-03 fence block (lines 244–259)
  </read_first>
  <action>
    Migrate every selector in the S-03 hex tables (RESEARCH lines 467–528) to its `--hub-*`
    token, selector-by-selector. Specifically: tab-bar/tab/chevron chrome →
    `var(--hub-surface)`/`var(--hub-border)`/`var(--hub-text-muted)`/`var(--hub-surface-elevated)`/
    `var(--hub-text-secondary)`/`var(--hub-bg)`/`var(--hub-text-primary)`; active tab
    border-bottom and rename-input border `#7aa2f7`→`var(--hub-accent)`; `.tab__close:hover`
    `#f7768e`→`var(--hub-destructive)` and `#3b4261`→`var(--hub-border-hover)`. Status bar:
    `.tab-status-bar` chrome → surface/border/muted tokens; `.tab-status-bar__hint` `#545c7e`→
    `var(--hub-text-dim)` (token added in plan 01); `.tab-status-bar__btn*` → border/secondary/
    elevated/primary tokens; `--active` `#7aa2f7`→`var(--hub-accent)`. `.terminal-container`
    `#1a1b26`→`var(--hub-bg)`. DO NOT migrate the D-03 fence selectors: leave
    `.tab__agent-badge--*` (all 7 per-agent hex) and `.tab-status-bar__state--on/off/inactive`
    (`#9ece6a`/`#9aa5ce`/`#414868`) exactly as hardcoded hex — these are semantic
    per-agent / colorblind-safe-via-text signals. Wrap migrated hover transitions in the
    motion guard pattern (141-PATTERNS.md lines 402–426).
  </action>
  <verify>
    <!-- Negated colorblind hex gate (user is colorblind — CRITICAL). The grep allow-list
         excludes the D-03-fenced status-state hex (#9ece6a / #9aa5ce / #414868) and
         `rgba(0,` shadow opacity; any OTHER hex in 82–368 fails the gate (exits non-zero,
         the leading `!` flips to non-zero → fail). The second positive grep separately
         CONFIRMS the agent-badge D-03 fence is still present (claude badge keeps #7aa2f7). -->
    <automated>! sed -n '82,368p' frontend/src/style.css | grep -E '#[0-9a-fA-F]{3,6}' | grep -viE '#9ece6a|#9aa5ce|#414868|rgba\(0,' | grep -q . && grep -A1 'tab__agent-badge--claude' frontend/src/style.css | grep -q '#7aa2f7'</automated>
  </verify>
  <acceptance_criteria>
    - The negated gate exits 0: the only hex surviving in 82–368 are the D-03-fenced `.tab-status-bar__state--*` values (`#9ece6a`, `#9aa5ce`, `#414868`) and any `rgba(0,...)` shadow opacity — no other hex.
    - The 7 `.tab__agent-badge--*` rules still hold their original per-agent hex: `grep -A1 'tab__agent-badge--claude' frontend/src/style.css | grep -q '#7aa2f7'` exits 0; none was rewritten to `var(--hub-accent)`.
    - `.tab-status-bar__hint` uses `var(--hub-text-dim)`.
    - `grep -c 'var(--hub-text-dim)' frontend/src/style.css` >= 1 (token consumed).
    - Migrated tab/status-bar hover transitions sit inside `@media (prefers-reduced-motion: no-preference)` with `reduce` fallbacks.
  </acceptance_criteria>
  <done>Tab bar, status bar, terminal container tokenized; D-03 fences intact; motion-guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS-only recolor of existing chrome surfaces; no input, auth, network, or data-flow change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-02 | Tampering | style.css chrome selectors | accept | Token substitution changes only color resolution; no secrets/links newly exposed; status/agent semantics preserved via D-03 fence. No new threat. |

No new threats introduced — recolor/token migration only; no change to data handling, auth, or network surfaces. No package installs.
</threat_model>

<verification>
- Colorblind grep gate (VALIDATION.md), negated form — each exits 0 only when no forbidden hex remains:
  - `! sed -n '218,293p' frontend/src/style.css | grep -E '#[0-9a-fA-F]{3,6}' | grep -v 'rgba(0,' | grep -q .`
  - `! sed -n '1302,1404p' frontend/src/style.css | grep -E '#[0-9a-fA-F]{3,6}' | grep -v 'rgba(0,' | grep -q .`
  - `! sed -n '82,368p' frontend/src/style.css | grep -E '#[0-9a-fA-F]{3,6}' | grep -viE '#9ece6a|#9aa5ce|#414868|rgba\(0,' | grep -q .`
  - D-03 fence still present: `grep -A1 'tab__agent-badge--claude' frontend/src/style.css | grep -q '#7aa2f7'`.
- `cd frontend && pnpm exec tsc --noEmit` passes. (This plan is CSS-only and does not touch the GroupSidebar/StatusBar tests; the full `pnpm test` gate is enforced at Plans 03/04, where Plan 05's RED→GREEN test updates are guaranteed complete. Running the full suite here would false-positive on Plan 05's still-RED suites.)
</verification>

<success_criteria>
- Sidebar, Welcome, tab bar, status bar, terminal container consume `--hub-*` tokens.
- D-03 fenced agent-badge and status-state hex untouched.
- Migrated transitions motion-guarded; `tsc --noEmit` green.
</success_criteria>

<output>
Create `.planning/phases/141-redesign-implementation/141-02-SUMMARY.md` when done.
</output>
