---
phase: 142-hub-settings-redesign-polish
plan: 03
type: execute
wave: 2
depends_on: ["142-01"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/Sidebar.tsx
  - frontend/src/components/Hub/HubPanel.tsx
  - frontend/src/lib/hubGroupCounts.ts
  - frontend/src/components/Hub/GroupSidebar.tsx
  - frontend/src/components/Hub/GroupSidebar.test.tsx
  - frontend/src/style.css
autonomous: true
requirements: [POL-05]
must_haves:
  truths:
    - "D-01: Group navigation moves into the main left sidebar as an expandable sub-list nested under the Hub item; selecting a group filters the Hub and the grid spans full width with the GroupSidebar side-panel removed"
    - "D-02: Applies Direction 01 visual language to the nested-sidebar structure (the comp depicts no Groups concept); drag-to-assign onto a sidebar group item is preserved with the per-card Move-to-group menu as fallback, and the hubGroups data model + live counts survive the move"
    - "Hub group navigation appears as an expandable sub-list under the Hub item in the main sidebar"
    - "Selecting a group in the sidebar opens the Hub filtered to that group; the session grid spans full width"
    - "No two collapsible side-by-side panels remain — the GroupSidebar side panel is deleted"
    - "Drag-to-assign still works: dropping a session card onto a sidebar group item assigns it; the per-card Move-to-group menu remains as keyboard fallback"
    - "Live running/total/attention/waiting counts per group (and global) survive the move, flowing up from HubPanel via callback"
  artifacts:
    - path: "frontend/src/components/Sidebar.tsx"
      provides: "Nested group sub-list under Hub (render, select, inline create, drag-drop targets)"
      contains: "sidebar__group-list"
    - path: "frontend/src/lib/hubGroupCounts.ts"
      provides: "computeCounts/computeGlobalCounts extracted from GroupSidebar"
      exports: ["computeCounts", "computeGlobalCounts"]
    - path: "frontend/src/components/Hub/HubPanel.tsx"
      provides: "GroupSidebar removed; group state received as props; counts emitted via onGroupCountsChange"
      contains: "onGroupCountsChange"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/Sidebar.tsx"
      via: "groupDefs/activeGroupId/onGroupSelect/onDropOnGroup/groupCounts props"
      pattern: "groupDefs"
    - from: "frontend/src/components/Hub/HubPanel.tsx"
      to: "frontend/src/App.tsx"
      via: "onGroupCountsChange callback (counts flow up; allSessions NOT lifted)"
      pattern: "onGroupCountsChange"
    - from: "frontend/src/components/Hub/SessionCard.tsx"
      to: "frontend/src/components/Sidebar.tsx"
      via: "HTML5 dataTransfer text/plain memberKey drop"
      pattern: "getData\\('text/plain'\\)"
---

<objective>
Resolve POL-05: move Hub group navigation out of the secondary side-by-side
`GroupSidebar` panel into an expandable sub-list nested under the "Hub" item in
the main left sidebar (D-01). The session grid then spans full width with no
second collapsible panel beside it.

Per D-01/D-02/D-03: lift `groupDefs` + `activeGroupId` from HubPanel.tsx to
App.tsx; thread them to Sidebar.tsx (render + select + create + drag-drop) and
back to HubPanel.tsx (filtering unchanged). Counts flow UP from HubPanel via an
`onGroupCountsChange` callback (do NOT lift `allSessions`). Preserve drag-to-assign
(drop card onto sidebar group item) with the per-card "Move to group…" menu kept
as keyboard fallback. Delete `GroupSidebar.tsx` + its test; migrate its drag/count
tests (already done in Plan 01's Sidebar test). Apply Direction 01 visual language
to the new sub-list (D-02 — the comp has no Groups concept, so this is the agreed
structure styled with the locked tokens).

Purpose: Eliminate the two-panel layout; consolidate navigation to a single
sidebar surface per the Hub-first IA.
Output: Sidebar group sub-list, lifted App state, simplified HubPanel, extracted
count helpers, deleted GroupSidebar, new CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-hub-settings-redesign-polish/142-CONTEXT.md
@.planning/phases/142-hub-settings-redesign-polish/142-RESEARCH.md
@.planning/phases/142-hub-settings-redesign-polish/142-PATTERNS.md
@.planning/phases/141-redesign-implementation/141-UI-SPEC.md

<interfaces>
<!-- Contracts the executor must implement against. Extracted from codebase + PATTERNS.md. -->

From frontend/src/lib/hubGroups.ts:
  export interface HubGroupDef { id: string; name: string; memberKeys: string[] }
  export function loadGroups(): HubGroupDef[]
  export function createGroup(defs: HubGroupDef[], name: string): HubGroupDef[]
  export function assignToGroup(defs: HubGroupDef[], groupId: string, key: string): HubGroupDef[]
  export function removeFromGroup(defs: HubGroupDef[], key: string): HubGroupDef[]
  export function memberKey(name: string, workDir?: string): string

Group counts shape (from GroupSidebar.tsx computeCounts/computeGlobalCounts):
  type GroupCounts = { running: number; total: number; waiting: number; attention: number }

Pseudo-group ids (from HubPanel.tsx filtering ~361-371):
  null        → "All" (no filter)
  "__other__" → sessions not in any named group (drop here = removeFromGroup)
  "<id>"      → sessions whose memberKey ∈ group.memberKeys

New SidebarProps additions (PATTERNS.md ~66-81):
  groupDefs: HubGroupDef[]
  activeGroupId: string | null
  onGroupSelect: (id: string | null) => void
  onCreateGroup: (name: string) => void
  onDropOnGroup: (groupId: string, mKey: string) => void
  groupCounts: Record<string, GroupCounts>
  globalGroupCounts: GroupCounts

New HubPanel prop additions (PATTERNS.md ~262-272):
  activeGroupId: string | null
  groupDefs: HubGroupDef[]
  onDropOnGroup: (groupId: string, mKey: string) => void
  onGroupCountsChange: (counts: Record<string, GroupCounts>, global: GroupCounts) => void
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract count helpers + lift group state to App; simplify HubPanel (remove GroupSidebar)</name>
  <read_first>
    - frontend/src/components/Hub/HubPanel.tsx (group state ~259-286, filtering ~361-391 incl. __other__, GroupSidebar render ~508-519, handleCreateGroup/handleDropOnGroup/handleAssignGroup, SessionCard onAssignGroup wiring ~468-471 — full file)
    - frontend/src/components/Hub/GroupSidebar.tsx (computeCounts/computeGlobalCounts ~25-53 — the functions to extract)
    - frontend/src/App.tsx (existing uiTheme state lift ~279-294 as the idiom; existing Sidebar render ~1460; existing HubPanel render; localStorage guard idiom)
    - frontend/src/lib/hubGroups.ts (CRUD signatures — full file)
    - .planning/phases/142-hub-settings-redesign-polish/142-PATTERNS.md (App.tsx section ~184-252, HubPanel section ~256-283)
    - .planning/phases/142-hub-settings-redesign-polish/142-RESEARCH.md (POL-05 state-lift design, count callback recommendation, pitfall 4 — keep groupDefs+onDropOnGroup as HubPanel props for the per-card menu)
  </read_first>
  <files>frontend/src/lib/hubGroupCounts.ts, frontend/src/App.tsx, frontend/src/components/Hub/HubPanel.tsx</files>
  <action>
    Create `frontend/src/lib/hubGroupCounts.ts` exporting `computeCounts(sessions, memberKeys: Set&lt;string&gt;): GroupCounts` and `computeGlobalCounts(sessions): GroupCounts` and the `GroupCounts` type, copied verbatim from GroupSidebar.tsx ~25-53 (running/idle/waiting count rules, `isAttentionStatus` import preserved). This is the shared count source so both the removed GroupSidebar logic and the new Sidebar render agree.
    In App.tsx, add lifted state with the localStorage try/catch guard idiom: `const [groupDefs, setGroupDefs] = useState&lt;HubGroupDef[]&gt;(() => loadGroups())`, `const [activeGroupId, setActiveGroupId] = useState&lt;string | null&gt;(null)`, and `groupCounts`/`globalGroupCounts` state initialized to `{}` / `{running:0,total:0,attention:0,waiting:0}`. Add useCallback handlers: `handleGroupSelect(id)`, `handleCreateGroup(name)` → `setGroupDefs(prev => createGroup(prev, name))`, `handleDropOnGroup(groupId, key)` → for `'__other__'` call `removeFromGroup(prev, key)` else `assignToGroup(prev, groupId, key)` (mirror HubPanel ~383-391), and `handleGroupCountsChange(counts, global)` → set both count states. Import `loadGroups, createGroup, assignToGroup, removeFromGroup, type HubGroupDef` from lib/hubGroups.
    Pass the new props to the existing `<Sidebar>` render (groupDefs, activeGroupId, onGroupSelect, onCreateGroup, onDropOnGroup, groupCounts, globalGroupCounts) and to the existing `<HubPanel>` render (activeGroupId, groupDefs, onDropOnGroup, onGroupCountsChange).
    In HubPanel.tsx: remove the internal `groupDefs`/`activeGroupId`/`sidebarCollapsed` state and `handleSidebarToggle` (~259-286), remove the `import { GroupSidebar }` (~11), and remove the GroupSidebar render block (~508-519). Add the new props `activeGroupId`, `groupDefs`, `onDropOnGroup`, `onGroupCountsChange`. KEEP `groupDefs` + `onDropOnGroup` flowing to SessionCard's per-card "Move to group" menu (handleAssignGroup) — pitfall 4: do not remove the per-card path. Replace the `hub__body` flex row + GroupSidebar with the full-width grid: `&lt;div className="hub__grid-scroll"&gt;{body}&lt;/div&gt;` (drop the `hub__body` wrapper or keep it as a non-flex single child — choose the minimal diff that removes the side-by-side layout). Add a `useEffect` that computes per-group + global counts from `allSessions`/`groupDefs` using hubGroupCounts helpers and calls `onGroupCountsChange(counts, global)` whenever `allSessions` or `groupDefs` changes (counts keyed by group id, with the global object passed separately).
    Keep `'__other__'` filtering behavior in HubPanel intact (driven by the prop `activeGroupId`). Remove the now-obsolete `hub-group-sidebar-collapsed` localStorage usage.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit 2>&1 | tail -15 && pnpm test run src/components/Hub/HubPanel.test.tsx 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/lib/hubGroupCounts.ts exists and exports `computeCounts`, `computeGlobalCounts`, and `GroupCounts`
    - App.tsx owns `groupDefs`, `activeGroupId`, `groupCounts`, `globalGroupCounts` state and passes them to both Sidebar and HubPanel
    - HubPanel.tsx no longer imports or renders `GroupSidebar`; `git grep -n GroupSidebar frontend/src/components/Hub/HubPanel.tsx` returns nothing
    - HubPanel.tsx calls `onGroupCountsChange` from a useEffect keyed on allSessions/groupDefs
    - HubPanel.tsx still passes `groupDefs` + an assign/drop path to SessionCard (per-card Move-to-group menu intact)
    - HubPanel.tsx group filtering (including `__other__`) is driven by the `activeGroupId` prop
    - The `hub-group-sidebar-collapsed` localStorage key is no longer referenced in HubPanel.tsx
    - `cd frontend && pnpm exec tsc --noEmit` exits 0
    - HubPanel.test.tsx `.hub__group-sidebar`-absent and onGroupCountsChange tests now PASS
  </acceptance_criteria>
  <done>Count helpers extracted; group state lifted to App; HubPanel side-panel removed and counts emitted upward; tsc clean; HubPanel tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Sidebar nested group sub-list + CSS; delete GroupSidebar</name>
  <read_first>
    - frontend/src/components/Sidebar.tsx (current SidebarProps, render, collapse/localStorage pattern — full file)
    - frontend/src/components/Hub/GroupSidebar.tsx (GroupSidebarItem ARIA + drag handlers ~77-128, inline create input ~131-167, "All"/"__other__" items ~284-308, CARRY-01 li-owns-class/button-owns-aria structure — full file, then DELETE it)
    - frontend/src/components/Hub/GroupSidebar.test.tsx (the test being deleted)
    - frontend/src/components/__tests__/Sidebar.test.tsx (the RED tests from Plan 01 this task must satisfy)
    - frontend/src/style.css (token block: dark ~4014-4080, light ~4082-4149; sidebar rules incl. .sidebar__item--active ~4692; motion contract ~328-343; existing .hub__group-sidebar-item rules ~4697+ as the visual analog)
    - .planning/phases/142-hub-settings-redesign-polish/142-PATTERNS.md (Sidebar.tsx section ~29-181; style.css POL-05 section ~654-695; shared token/motion/colorblind/heroicons patterns)
    - .planning/phases/141-redesign-implementation/141-UI-SPEC.md (token system, colorblind + motion contracts, accent locks)
    - .planning/phases/142-hub-settings-redesign-polish/142-RESEARCH.md (POL-05 ARIA contract, collapsed-sidebar handling Q2 recommendation, drag auto-expand recommendation)
  </read_first>
  <files>frontend/src/components/Sidebar.tsx, frontend/src/style.css, frontend/src/components/Hub/GroupSidebar.tsx, frontend/src/components/Hub/GroupSidebar.test.tsx</files>
  <action>
    Extend SidebarProps with the seven POL-05 props from the &lt;interfaces&gt; block. Import `type HubGroupDef` from `../lib/hubGroups` and `ChevronDownIcon`/`ChevronRightIcon` from `@heroicons/react/24/outline` (no Tailwind w-/h- classes — size via CSS, per the heroicons shared pattern).
    Render a nested `&lt;ul className="sidebar__group-list" aria-label="Hub groups"&gt;` directly under the Hub `&lt;li&gt;`/button, shown only when `!collapsed && groupDefs.length > 0` (Q2: hide group sub-list when sidebar collapsed). First item is "All" (`activeGroupId === null`); then one item per `groupDefs`; include an "__other__" item labeled "Other" (parity with GroupSidebar) when there are named groups. Each item follows the CARRY-01 structure EXACTLY: the `&lt;li&gt;` owns visual classes (`sidebar__group-item`, `--active`, `--drag-over`) + drag handlers; the inner `&lt;button&gt;` owns interactive ARIA (`type="button"`, `aria-pressed={isActive}`, `aria-label` containing `"${name} group, ${counts.running}/${counts.total} sessions"`, onClick → `onGroupSelect(id)` then `onOpenHub()`, onKeyDown Space handling). Render the running/total count from `groupCounts[id]` (and `globalGroupCounts` for "All").
    Drag-drop: on the `&lt;li&gt;` implement onDragOver (preventDefault, set drag-over), onDragLeave (clear), onDrop (preventDefault, `key = e.dataTransfer.getData('text/plain')`, `if (key) onDropOnGroup(id, key)`); "All" (id===null) must NOT call onDropOnGroup (guard). Implement drag auto-expand: when a drag enters the sidebar while collapsed, temporarily set collapsed=false for the drag duration, restoring on drag-leave/drop (per RESEARCH recommendation) — keep this minimal and do not persist the change to the `sidebar-collapsed` localStorage key.
    Inline group creation: a "New group" affordance that reveals a text input (autoFocus); Enter with trimmed value calls `onCreateGroup` and resets; Escape/blur dismisses; empty Enter is a no-op (copy GroupSidebar ~131-167). Retain `inputValue.trim()` (V5 input guard).
    Add the `sidebar__group-*` CSS to style.css per PATTERNS.md ~654-695: `.sidebar__group-list`, `.sidebar__group-item`, `.sidebar__group-item__btn` (incl. `:hover`), `.sidebar__group-item--active .sidebar__group-item__btn`, `.sidebar__group-item--drag-over .sidebar__group-item__btn`, `.sidebar__group-item__name`, `.sidebar__group-item__count`, `.sidebar__group-new` + `.sidebar__group-new-input`. Use ONLY `var(--hub-*)` tokens (no raw hex). Tokens referenced (`--hub-text-muted`, `--hub-text-primary`, `--hub-accent`, `--hub-sidebar-item-hover-bg`, `--hub-sidebar-item-active-bg`, `--hub-drag-over-border`, `--hub-drag-over-bg`) already resolve in both dark `:root` and `[data-ui-theme="light"]` blocks — confirm each exists in the light block and add a `[data-ui-theme="light"]` override only for any new color-bearing rule whose value is NOT already a shared token. Wrap any transition (group-item hover, expand chevron) in the `@media (prefers-reduced-motion: no-preference)` / `reduce` two-block contract.
    Remove obsolete `.hub__group-sidebar*` CSS rules that styled the deleted side panel (those no longer referenced after GroupSidebar deletion); leave any rule still referenced elsewhere.
    DELETE `frontend/src/components/Hub/GroupSidebar.tsx` and `frontend/src/components/Hub/GroupSidebar.test.tsx` (its drag/count tests were migrated to Sidebar.test.tsx in Plan 01).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec tsc --noEmit 2>&1 | tail -15 && pnpm test run src/components/__tests__/Sidebar.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx renders `ul.sidebar__group-list` with an "All" item + one item per group + an "Other" item, only when `!collapsed && groupDefs.length > 0`
    - Each group item: `&lt;li&gt;` owns class + drag handlers; inner `&lt;button&gt;` owns `aria-pressed` + `aria-label` with `running/total sessions` (CARRY-01 structure)
    - Clicking a group item calls `onGroupSelect(id)` then `onOpenHub()`; clicking "All" calls `onGroupSelect(null)`
    - Drop on a group `&lt;li&gt;` reads `dataTransfer.getData('text/plain')` and calls `onDropOnGroup(id, key)`; drop on "All" does not
    - Inline "New group" input: trimmed Enter calls `onCreateGroup`; Escape/blur dismisses; empty Enter is a no-op
    - style.css contains `.sidebar__group-list` and `.sidebar__group-item*` rules using only `var(--hub-*)` tokens — `grep -nE "sidebar__group-(list|item)" frontend/src/style.css` is non-empty and `grep -nE "sidebar__group.*#[0-9a-fA-F]{3,6}" frontend/src/style.css` is empty (no raw hex)
    - Any new animated `.sidebar__group*` rule is wrapped in both `prefers-reduced-motion: no-preference` and `reduce` blocks
    - `frontend/src/components/Hub/GroupSidebar.tsx` and `GroupSidebar.test.tsx` no longer exist
    - `git grep -n "GroupSidebar" frontend/src` returns nothing
    - `cd frontend && pnpm exec tsc --noEmit` exits 0
    - All POL-05 tests in Sidebar.test.tsx (render/select/All/drop/drop-on-All-guard/create + CSS source gates) now PASS
  </acceptance_criteria>
  <done>Sidebar nested group sub-list implemented with CARRY-01 ARIA + drag-drop + inline create; tokenized CSS with motion contract; GroupSidebar deleted; tsc clean; Sidebar POL-05 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keyboard → group-name input | Client-only group name; persisted to localStorage `agenthub:hubGroups:v1`; never sent to a server |
| HTML5 drag dataTransfer → drop handler | `text/plain` memberKey string read from a same-origin drag within the Wails webview |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-04 | Tampering | Group-name input (Sidebar inline create) | mitigate | Retain existing `inputValue.trim()` guard; group name is client-only localStorage data, no server round-trip, no HTML interpolation (rendered as text content, not innerHTML) |
| T-142-05 | Tampering | localStorage `agenthub:hubGroups:v1` | accept | Local single-user app; localStorage is already the group store; no integrity boundary added by this move. loadGroups() already tolerates malformed/blocked storage (try/catch) |
| T-142-06 | Injection (XSS) | Group name rendered in sidebar | mitigate | Name rendered via React text children (auto-escaped), never `dangerouslySetInnerHTML`; aria-label built via template string is a DOM attribute, not markup |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | No package installs — RESEARCH.md confirms zero new packages |
</threat_model>

<verification>
- `cd frontend && pnpm exec tsc --noEmit` exits 0.
- `cd frontend && pnpm test run src/components/__tests__/Sidebar.test.tsx src/components/Hub/HubPanel.test.tsx 2>&1 | tail -20` — all POL-05 tests green.
- `git grep -n "GroupSidebar" frontend/src` returns nothing (component + test deleted, no dangling refs).
- `grep -nE "sidebar__group.*#[0-9a-fA-F]{3,6}" frontend/src/style.css` empty (token discipline).
</verification>

<success_criteria>
- Group navigation lives as a nested sub-list under Hub in the main sidebar; grid spans full width; GroupSidebar deleted.
- Drag-to-assign onto sidebar group items works; per-card Move-to-group menu retained.
- Live counts flow up via onGroupCountsChange; allSessions not lifted.
- All new CSS tokenized with light overrides + motion contract; no raw hex.
- tsc clean; POL-05 Sidebar + HubPanel tests green.
</success_criteria>

<output>
Create `.planning/phases/142-hub-settings-redesign-polish/142-03-SUMMARY.md` when done
</output>
