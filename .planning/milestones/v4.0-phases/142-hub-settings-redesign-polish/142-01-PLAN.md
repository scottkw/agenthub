---
phase: 142-hub-settings-redesign-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/__tests__/SettingsTab.appearance-theme.test.tsx
  - frontend/src/components/__tests__/Sidebar.test.tsx
  - frontend/src/components/Hub/HubPanel.test.tsx
autonomous: true
requirements: [POL-02, POL-03, POL-04, POL-05]
must_haves:
  truths:
    - "The Settings appearance test asserts a single role=switch toggle, not two aria-pressed buttons"
    - "The Sidebar test covers a nested group sub-list (render, select, drag-drop) and fails until POL-05 lands"
    - "A source-gate test asserts pendingThemeRef exists in TerminalPanel.tsx and fails until POL-04 lands"
    - "A source-gate test asserts PlusIcon is used in both New-session buttons and fails until POL-03 lands"
    - "A HubPanel test asserts the GroupSidebar side-panel is absent and fails until POL-05 lands"
  artifacts:
    - path: "frontend/src/components/__tests__/Sidebar.test.tsx"
      provides: "RED tests for POL-05 group sub-list (render/select/drag) + POL-04/POL-03 source gates"
      contains: "role=\"switch\""
    - path: "frontend/src/components/__tests__/SettingsTab.appearance-theme.test.tsx"
      provides: "Updated POL-02 toggle assertions"
      contains: "aria-checked"
  key_links:
    - from: "Sidebar.test.tsx"
      to: "frontend/src/style.css"
      via: "readFileSync source-gate inspection"
      pattern: "pendingThemeRef|PlusIcon"
---

<objective>
Stand up Wave 0 test scaffolding for Phase 142. All POL items either modify
existing tested behavior (POL-02 settings toggle, POL-05 group nav) or have no
test yet (POL-03 button glyph, POL-04 repaint guard). This plan writes/updates
the RED tests and source-gate assertions so every later task has an `<automated>`
verify to satisfy the Nyquist contract.

This plan does NOT touch any production source. It only edits/creates test files.
The RED tests are EXPECTED to fail until their implementing plan lands — that is
the point of Wave 0.

Purpose: Give Plans 02/03/04 a failing test to make green; prevent silent
behavior drift in POL-02 and POL-05 which change already-tested DOM.
Output: Updated SettingsTab appearance test, expanded Sidebar test, updated
HubPanel test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-hub-settings-redesign-polish/142-CONTEXT.md
@.planning/phases/142-hub-settings-redesign-polish/142-RESEARCH.md
@.planning/phases/142-hub-settings-redesign-polish/142-PATTERNS.md
@.planning/phases/142-hub-settings-redesign-polish/142-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SettingsTab appearance test for the single role=switch toggle (POL-02 RED)</name>
  <read_first>
    - frontend/src/components/__tests__/SettingsTab.appearance-theme.test.tsx (current two-button assertions — full file)
    - frontend/src/components/SettingsTab.tsx (current control ~438-461, so test mocks/imports stay valid)
    - .planning/phases/142-hub-settings-redesign-polish/142-PATTERNS.md (the SettingsTab test section ~699-755 — exact old→new query patterns)
    - .planning/phases/142-hub-settings-redesign-polish/142-RESEARCH.md (POL-02 section, D-06 colorblind-safe contract)
  </read_first>
  <files>frontend/src/components/__tests__/SettingsTab.appearance-theme.test.tsx</files>
  <action>
    Replace the two-button DOM assertions with single-toggle assertions per PATTERNS.md. Concretely:
    Replace the `button[aria-pressed]` queries (finding "Light"/"Dark" buttons) with a single `[role="switch"]` query. Assert `aria-checked === "true"` when `uiTheme==='light'` and `"false"` when `uiTheme==='dark'`. Assert that clicking the switch calls `onUiThemeChange` with the OPPOSITE of the current theme (initial dark → click → `onUiThemeChange('light')`; initial light → click → `onUiThemeChange('dark')`).
    Update the source-inspection (`raw`/`readFileSync`) assertions: remove the `expect(raw).toContain('aria-pressed')` and `role="group"` checks; add `expect(raw).toContain('role="switch"')` and `expect(raw).toContain('aria-checked')`. Add a colorblind-safe source assertion: `expect(raw).toMatch(/SunIcon|MoonIcon/)` AND `expect(raw).toMatch(/Light|Dark/)` proving icon+text knob (D-06). Keep the `onUiThemeChange(uiTheme === 'light' ? 'dark' : 'light')` toggle-call source assertion.
    Remove the `role="group"` aria-label="Interface theme" test; replace with a `[role="switch"]` presence test.
    Do NOT change the vitest mock setup block (all mocked Wails bindings still required). Do NOT touch SettingsTab.tsx.
    These assertions WILL FAIL now (control is still two buttons) — that is correct for Wave 0.
  </action>
  <verify>
    <automated>cd frontend && pnpm test run src/components/__tests__/SettingsTab.appearance-theme.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - SettingsTab.appearance-theme.test.tsx contains `[role="switch"]` query and no longer contains `button[aria-pressed]` for the theme control
    - SettingsTab.appearance-theme.test.tsx asserts `aria-checked` for both light and dark states
    - SettingsTab.appearance-theme.test.tsx asserts `onUiThemeChange` is called with the opposite theme on click
    - SettingsTab.appearance-theme.test.tsx source-gate asserts presence of `role="switch"`, `aria-checked`, and a Sun/Moon icon + Light/Dark text
    - The test file currently FAILS (control is still two buttons) — confirmed by a red run
  </acceptance_criteria>
  <done>Test rewritten to the single-toggle contract; runs red against current source; no production source touched.</done>
</task>

<task type="auto">
  <name>Task 2: Expand Sidebar test for nested group sub-list + POL-03/POL-04 source gates (RED)</name>
  <read_first>
    - frontend/src/components/__tests__/Sidebar.test.tsx (current 3-item nav tests + cssRaw source-gate pattern — full file)
    - frontend/src/components/Sidebar.tsx (current SidebarProps + render — full file)
    - frontend/src/components/Hub/GroupSidebar.tsx (group sub-list ARIA, GroupSidebarItem drag handlers, computeCounts ~25-53, "All" item, "__other__" handling — for the contract being migrated)
    - frontend/src/components/Hub/GroupSidebar.test.tsx (drag-drop + counts tests being migrated)
    - .planning/phases/142-hub-settings-redesign-polish/142-PATTERNS.md (Sidebar.tsx section ~29-181 — SidebarProps additions, group-item ARIA, drag handlers)
    - .planning/phases/142-hub-settings-redesign-polish/142-RESEARCH.md (POL-05 feasibility, group count callback design, ARIA contract ~308-321)
  </read_first>
  <files>frontend/src/components/__tests__/Sidebar.test.tsx</files>
  <action>
    Add a new describe block "Sidebar group sub-list (POL-05)" rendering Sidebar with the new props from PATTERNS.md SidebarProps: `groupDefs`, `activeGroupId`, `onGroupSelect`, `onCreateGroup`, `onDropOnGroup`, `groupCounts`, `globalGroupCounts`. Use the existing `renderSidebar` helper; extend its defaultProps with these (groupDefs as a 1-2 entry `HubGroupDef[]` from lib/hubGroups types, empty counts objects).
    Migrate from GroupSidebar.test.tsx these behaviors as RED tests against Sidebar:
    (a) renders a group sub-list (`ul.sidebar__group-list`) with one `[role="..."]`/button per group plus an "All" item when `groupDefs.length > 0`;
    (b) each group item button has `aria-pressed` reflecting `activeGroupId === g.id`, and an `aria-label` containing `running/total sessions`;
    (c) clicking a group item calls `onGroupSelect(g.id)` then `onOpenHub`;
    (d) clicking "All" calls `onGroupSelect(null)`;
    (e) a drop event on a group `<li>` reads `dataTransfer.getData('text/plain')` and calls `onDropOnGroup(g.id, key)` — simulate via dispatching a `drop` DragEvent with a stubbed `dataTransfer`;
    (f) dropping on "All" does NOT call `onDropOnGroup` (id===null guard, per GroupSidebar line 114);
    (g) inline "New group" creation: clicking the new-group affordance reveals an input; Enter with trimmed text calls `onCreateGroup`.
    Add cssRaw source-gate tests (reuse the file's `readFileSync` of style.css): assert `.sidebar__group-item` and `.sidebar__group-list` rules exist (POL-05 CSS).
    Add two cross-file source-gate tests reading the relevant source files via readFileSync:
    - POL-04: read TerminalPanel.tsx, assert it contains `pendingThemeRef` AND `fitTerminal` appears after `clearTextureAtlas` (assert `raw.includes('pendingThemeRef')`).
    - POL-03: read HubFilterBar.tsx and HubEmptyState.tsx, assert each contains `PlusIcon`.
    All new tests EXPECTED to fail now. Do NOT modify production source.
  </action>
  <verify>
    <automated>cd frontend && pnpm test run src/components/__tests__/Sidebar.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.test.tsx has a "group sub-list (POL-05)" describe block exercising render, aria-pressed, group-select+onOpenHub, All-select, drop→onDropOnGroup, drop-on-All-guard, and inline create
    - Sidebar.test.tsx source-gate asserts `pendingThemeRef` in TerminalPanel.tsx (POL-04)
    - Sidebar.test.tsx source-gate asserts `PlusIcon` in both HubFilterBar.tsx and HubEmptyState.tsx (POL-03)
    - Sidebar.test.tsx source-gate asserts `.sidebar__group-item` and `.sidebar__group-list` exist in style.css
    - Existing 3-item nav tests still pass; the new POL-05/03/04 tests FAIL (confirmed by red run)
  </acceptance_criteria>
  <done>Sidebar test expanded with POL-05 behavior tests + POL-03/POL-04 source gates; new tests red, old tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Update HubPanel test to assert GroupSidebar side-panel is absent (POL-05 RED)</name>
  <read_first>
    - frontend/src/components/Hub/HubPanel.test.tsx (current tests — full file)
    - frontend/src/components/Hub/HubPanel.tsx (current hub__body flex row + GroupSidebar render ~508-519, group state ~259-286)
    - .planning/phases/142-hub-settings-redesign-polish/142-PATTERNS.md (HubPanel.tsx section ~256-283 — props additions + hub__body simplification + onGroupCountsChange)
    - .planning/phases/142-hub-settings-redesign-polish/142-RESEARCH.md (POL-05 state-lift design, count callback)
  </read_first>
  <files>frontend/src/components/Hub/HubPanel.test.tsx</files>
  <action>
    Add/adjust tests so HubPanel no longer renders the side-by-side GroupSidebar:
    (a) assert the rendered HubPanel contains NO `.hub__group-sidebar` element (the deleted side panel) — `expect(container.querySelector('.hub__group-sidebar')).toBeNull()`;
    (b) assert the grid area `.hub__grid-scroll` still renders (full-width grid survives);
    (c) if HubPanel is rendered with the new `activeGroupId` prop set to a group id, assert visibleSessions filtering still applies (the existing filtering test, retargeted to the prop instead of internal state — keep behavior identical);
    (d) add a test that HubPanel calls `onGroupCountsChange` (a new mock prop) at least once after mount with an object containing per-group and global counts shape `{running,total,attention,waiting}`.
    Update the HubPanel render-helper to supply the new required props (`activeGroupId`, `groupDefs`, `onDropOnGroup`, `onGroupCountsChange`) per PATTERNS.md. Any test that previously drove the internal `activeGroupId` state via the GroupSidebar must be retargeted to pass `activeGroupId` as a prop.
    Tests (a)/(d) EXPECTED to fail now (GroupSidebar still rendered; onGroupCountsChange not yet called). Do NOT modify HubPanel.tsx.
  </action>
  <verify>
    <automated>cd frontend && pnpm test run src/components/Hub/HubPanel.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - HubPanel.test.tsx asserts `.hub__group-sidebar` is absent from the rendered DOM
    - HubPanel.test.tsx asserts `.hub__grid-scroll` still renders
    - HubPanel.test.tsx drives group filtering via the `activeGroupId` prop (not internal state)
    - HubPanel.test.tsx asserts `onGroupCountsChange` is invoked with `{running,total,attention,waiting}`-shaped data
    - The GroupSidebar-absent and onGroupCountsChange tests FAIL against current source (confirmed by red run)
  </acceptance_criteria>
  <done>HubPanel test updated to the post-POL-05 contract; structural-absence + counts-callback tests red; filtering test green via prop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan; no runtime trust boundary is created or crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Tampering | Test files (no production effect) | accept | Tests run in CI sandbox; no runtime surface, no data, no network |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH.md confirms zero new packages; nothing to verify |
</threat_model>

<verification>
- `cd frontend && pnpm test run src/components/__tests__/SettingsTab.appearance-theme.test.tsx src/components/__tests__/Sidebar.test.tsx src/components/Hub/HubPanel.test.tsx 2>&1 | tail -20` — new RED tests fail, pre-existing tests still pass.
- No production source file changed: `git status --porcelain frontend/src | grep -v '.test.' ` returns empty.
</verification>

<success_criteria>
- POL-02 settings test rewritten to single role=switch toggle (red against current source).
- POL-05 Sidebar group sub-list tests added (red).
- POL-03/POL-04 source-gate tests added (red).
- HubPanel GroupSidebar-absent + counts-callback tests added (red).
- All pre-existing passing tests in these files remain green.
</success_criteria>

<output>
Create `.planning/phases/142-hub-settings-redesign-polish/142-01-SUMMARY.md` when done
</output>
