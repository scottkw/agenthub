---
phase: 146-open-session-capability-bug
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/webserver/sessions_meta_embed_test.go
  - internal/webserver/sessions_meta_test.go
  - internal/daemon/mint_join_codes_test.go
  - frontend/src/components/__tests__/App.open-remote.test.tsx
  - frontend/src/lib/__tests__/remoteAdapter.test.ts
autonomous: true
requirements: [FIX-03]

must_haves:
  truths:
    - "The 3 new test files exist and define the contract (ro_join_code/rw_join_code shape, exchange-then-open flow, RO-vs-RW selection)"
    - "TestSessionsMeta_NoCapInResponse allows ro_join_code/rw_join_code while still rejecting cap/token/grant (RB-03 preserved)"
    - "All new Go tests fail (red) because production code does not yet exist; existing tests still pass"
  artifacts:
    - path: "internal/webserver/sessions_meta_embed_test.go"
      provides: "TestSessionsMeta_EmbedJoinCodes + TestSessionsMeta_NilIssuer (red)"
      contains: "TestSessionsMeta_EmbedJoinCodes"
    - path: "internal/daemon/mint_join_codes_test.go"
      provides: "TestMintSessionJoinCodes (red)"
      contains: "TestMintSessionJoinCodes"
    - path: "frontend/src/components/__tests__/App.open-remote.test.tsx"
      provides: "handleOpenRemoteSession source-inspection contract (red)"
      contains: "handleOpenRemoteSession"
  key_links:
    - from: "internal/webserver/sessions_meta_test.go"
      to: "ro_join_code/rw_join_code allowed-keys"
      via: "TestSessionsMeta_NoCapInResponse allowed map"
      pattern: "ro_join_code"
---

<objective>
Wave 0: Write the failing/scaffold tests that define the FIX-03 contract BEFORE any production code exists. This locks the field shapes (`ro_join_code`/`rw_join_code`), the owner-side mint behavior, the viewer-side exchange-then-open flow, and the RB-03 access-control guard. Subsequent waves make these tests pass.

Purpose: Nyquist validation is enabled (VALIDATION.md). Every downstream task has an `<automated>` verify command pointing at a test created here. Writing tests first guarantees the contract is fixed before implementation diverges.
Output: 3 new test files + 2 existing-test updates, all red (new tests fail; existing tests still green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-open-session-capability-bug/146-RESEARCH.md
@.planning/phases/146-open-session-capability-bug/146-PATTERNS.md
@.planning/phases/146-open-session-capability-bug/146-VALIDATION.md

<interfaces>
<!-- Contract that these tests assert against. Production code in Waves 1+2 must satisfy this. -->

Go owner-side (Wave 1 will add):
- sessionMetaItem gains: ROJoinCode string `json:"ro_join_code,omitempty"`, RWJoinCode string `json:"rw_join_code,omitempty"`
- WebServer gains: SetJoinCodeIssuer(fn func(string) (roCode, rwCode string, err error))
- API gains: mintSessionJoinCodes(sessionID string) (roCode, rwCode string, err error)  // mints RO+RW tokens, calls ws.AddGrant for both, issues join codes

Frontend viewer-side (Wave 2 will add):
- RemoteSession (App.d.ts + remoteSession.ts): roJoinCode?: string; rwJoinCode?: string
- AdaptedRemoteSessionInfo: SessionInfo & { url: string; roJoinCode?: string; rwJoinCode?: string }
- handleOpenRemoteSession(session: AdaptedRemoteSessionInfo): exchanges code via ExchangeJoinCodeAtURL, opens baseURL + "/sessions/" + id + "?cap=" + token via BrowserOpenURL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Go owner-side scaffold tests (webserver + daemon)</name>
  <files>internal/webserver/sessions_meta_embed_test.go, internal/webserver/sessions_meta_test.go, internal/daemon/mint_join_codes_test.go</files>
  <read_first>
    - internal/webserver/sessions_meta_test.go (analog: TestSessionsMeta_NoCapInResponse allowed-keys map ~L188-216, testServer + ssExtTestKey helpers from server_test.go same package)
    - internal/webserver/server.go (sessionMetaItem struct L42-54, handleSessionsMeta L831-859, SetSessionResolver / SetJoinCodes setter shapes)
    - internal/daemon/api_test.go (analog: issueCapsTestSetup + TestIssueCapabilities_BrowseOff_NoFilesPerms ~L1999-2057; configureCapabilityStateForTest ~L833-847)
    - internal/daemon/api.go (issueCapabilitiesForSession L1083-1175 — the function mintSessionJoinCodes will be extracted from)
  </read_first>
  <action>
    Create internal/webserver/sessions_meta_embed_test.go in package webserver_test. Add TestSessionsMeta_EmbedJoinCodes: build a server via testServer(t), SetSigningKey(ssExtTestKey), SetSessionResolver, EnableSession("sess-a"), wire a fake issuer via the (not-yet-existing) ws.SetJoinCodeIssuer returning deterministic codes ("ro-code-"+id, "rw-code-"+id, nil); GET /api/sessions/meta and assert the JSON item contains ro_join_code and rw_join_code with the expected values. Add TestSessionsMeta_NilIssuer: same setup WITHOUT calling SetJoinCodeIssuer; assert items decode with empty/absent ro_join_code/rw_join_code (handler must not panic, degraded mode).
    Then update internal/webserver/sessions_meta_test.go: in TestSessionsMeta_NoCapInResponse add "ro_join_code": true and "rw_join_code": true to the allowed map; keep the sensitiveKeys blacklist (cap, token, grant, grants, content, key, signing_key, hmac) unchanged so RB-03 still rejects raw cap fields.
    Create internal/daemon/mint_join_codes_test.go in package daemon. Add a mintCodesTestSetup(t) helper mirroring issueCapsTestSetup (testDaemon + NewWebServer + SetWebServerForTest + configureCapabilityStateForTest). Add TestMintSessionJoinCodes: create + EnableSession a session, call api.mintSessionJoinCodes(sid), assert both codes non-empty and distinct, and assert each code exchanges to a token whose grant is active (probe via the same exchange path used in TestCapabilityIssueAndExchange / probeGrant pattern in api_test.go). These reference symbols (ws.SetJoinCodeIssuer, api.mintSessionJoinCodes) that do not exist yet — the package will not compile, which is the intended RED state for Wave 0.
    Do NOT implement any production symbol in this task.
  </action>
  <verify>
    <automated>go vet ./internal/webserver/... ./internal/daemon/... 2>&1 | grep -qiE "SetJoinCodeIssuer|mintSessionJoinCodes" && echo "RED-as-expected (symbols undefined)" || (go test -run 'TestSessionsMeta_EmbedJoinCodes|TestMintSessionJoinCodes' ./internal/webserver/... ./internal/daemon/... 2>&1; echo "tests present")</automated>
  </verify>
  <acceptance_criteria>
    - internal/webserver/sessions_meta_embed_test.go exists and contains `func TestSessionsMeta_EmbedJoinCodes(` and `func TestSessionsMeta_NilIssuer(`
    - internal/webserver/sessions_meta_test.go allowed map now contains `"ro_join_code"` and `"rw_join_code"`; sensitiveKeys list unchanged
    - internal/daemon/mint_join_codes_test.go exists and contains `func TestMintSessionJoinCodes(`
    - The new tests reference `SetJoinCodeIssuer` and `mintSessionJoinCodes` (RED: compile/run fails because production symbols are absent — this is correct for Wave 0)
  </acceptance_criteria>
  <done>3 Go test changes committed; the new symbols are referenced; existing webserver/daemon tests untouched except the allowed-keys additions.</done>
</task>

<task type="auto">
  <name>Task 2: Write frontend viewer-side scaffold tests (open-remote + adapter)</name>
  <files>frontend/src/components/__tests__/App.open-remote.test.tsx, frontend/src/lib/__tests__/remoteAdapter.test.ts</files>
  <read_first>
    - frontend/src/components/__tests__/App.fileBrowserMode.test.tsx (analog: source-inspection via `App.tsx?raw` — the established pattern for App.tsx behavior assertions)
    - frontend/src/components/__tests__/SessionCard.share.test.tsx (analog: mock setup + render + prop assertion; D-08 onOpenSession-vs-onOpenInBrowser assertion target)
    - frontend/src/lib/__tests__/remoteAdapter.test.ts (existing file to extend; adaptRemoteSession test shape, makeSession/makePeer factories)
    - frontend/src/lib/remoteAdapter.ts (adaptRemoteSession L10-29; AdaptedRemoteSessionInfo type)
    - frontend/src/App.tsx (handleOpenRemoteSession L1062-1064 — the target of the source-inspection assertions)
  </read_first>
  <action>
    Create frontend/src/components/__tests__/App.open-remote.test.tsx using the `import raw from '../../App.tsx?raw'` source-inspection pattern. Add assertions that will be RED until Wave 2 rewrites the handler: (a) the handleOpenRemoteSession body contains "ExchangeJoinCodeAtURL" (not a bare BrowserOpenURL(session.url)); (b) it references "rwJoinCode" and "isPeerSelf" for D-06 selection; (c) the not-shared path pairs "roJoinCode" with "setSaveBanner" (D-03, no raw 401 dead-end); (d) the handler references "expired" or "session-gone" and calls setSaveBanner on exchange failure (Pitfall 4); (e) it constructs a cap-bearing URL matching /\/sessions\/.*\?cap=/.
    Extend frontend/src/lib/__tests__/remoteAdapter.test.ts: add a test that adaptRemoteSession passes through roJoinCode/rwJoinCode when present on the input session, and a test that roJoinCode is undefined when the input session has no join code (D-03 not-shared input shape). Use the existing makeSession/makePeer factories; extend them to accept the new optional fields.
    These tests assert against source/behavior that does not exist yet (RED). Do NOT modify App.tsx or remoteAdapter.ts in this task.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- App.open-remote remoteAdapter --run 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/__tests__/App.open-remote.test.tsx exists and contains assertions for `ExchangeJoinCodeAtURL`, `isPeerSelf`, `rwJoinCode`, and a `/sessions/.*\?cap=` URL shape
    - frontend/src/lib/__tests__/remoteAdapter.test.ts contains a pass-through test referencing `roJoinCode` and `rwJoinCode`
    - `pnpm test -- App.open-remote --run` reports failing assertions (RED) — the App.tsx source does not yet contain ExchangeJoinCodeAtURL in the open handler
  </acceptance_criteria>
  <done>Both frontend test changes committed; App.open-remote tests are RED; remoteAdapter pass-through tests are RED; no production source modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer peer → owner `/api/sessions/meta` | tailnet network-layer-trusted discovery response; must not leak raw cap tokens (RB-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-01 | Information disclosure | `sessionMetaItem` JSON keys | mitigate | TestSessionsMeta_NoCapInResponse keeps cap/token/grant/grants/content/key/signing_key/hmac on the sensitiveKeys blacklist; only ro_join_code/rw_join_code added to allowed map (RB-03 preserved) — this Wave-0 test locks the guard |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: zero new dependencies); no slopcheck checkpoint required |
</threat_model>

<verification>
- Go: new test files exist and reference the undefined production symbols (RED expected pre-Wave-1).
- Go: `go test ./internal/webserver/... -run TestSessionsMeta_NoCapInResponse` still passes after the allowed-keys edit (the edit only widens the allow-list; the blacklist is unchanged).
- Frontend: `cd frontend && pnpm test -- App.open-remote remoteAdapter --run` reports RED assertions for the open-remote contract.
</verification>

<success_criteria>
- All 3 new test files created; 2 existing tests updated per VALIDATION.md Wave 0 Requirements.
- New tests are RED (reference not-yet-implemented symbols/behavior); the RB-03 existing test remains GREEN.
- Field-shape contract (ro_join_code/rw_join_code, roJoinCode/rwJoinCode) is fixed in tests before any implementation.
</success_criteria>

<output>
Create `.planning/phases/146-open-session-capability-bug/146-00-SUMMARY.md` when done.
</output>
