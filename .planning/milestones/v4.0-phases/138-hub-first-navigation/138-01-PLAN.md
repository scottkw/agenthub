---
phase: 138-hub-first-navigation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/components/__tests__/Sidebar.test.tsx
  - frontend/src/components/__tests__/App.nav.test.tsx
  - frontend/src/components/__tests__/App.hub.test.tsx
  - frontend/src/components/__tests__/style.hub.test.ts
  - frontend/src/components/__tests__/SessionCard.share.test.tsx
autonomous: true
requirements: [NAV-02, NAV-03, NAV-04, NAV-05, CARD-01, CARD-02, CARD-03, CARD-04]
must_haves:
  truths:
    - "Sidebar.test.tsx asserts exactly 3 items (Home/Hub/Settings) and that Sessions/Remote/New Session buttons are absent"
    - "App.nav.test.tsx no longer asserts onOpenRemoteSessions/onOpenDaemonManager/onAdd wiring; asserts their absence instead"
    - "App.hub.test.tsx asserts DAEMON_MANAGER_TAB/REMOTE_SESSIONS_TAB/DaemonManagerPanel/RemoteSessionsPanel are absent and new HubPanel props are wired"
    - "style.hub.test.ts asserts new CARD-03/CARD-04 CSS classes and preserves CARD-04 anti-regression classes; drops .hub__header assertions"
    - "SessionCard.share.test.tsx has CARD-02/CARD-03/CARD-04 test cases that fail RED against current SessionCard"
  artifacts:
    - path: "frontend/src/components/__tests__/Sidebar.test.tsx"
      provides: "3-item sidebar assertions (NAV-02..05)"
      contains: "toBe(3)"
    - path: "frontend/src/components/__tests__/SessionCard.share.test.tsx"
      provides: "CARD-02/03/04 card indicator + affordance assertions"
      contains: "hub-card__conn"
  key_links:
    - from: "Sidebar.test.tsx"
      to: "Sidebar.tsx"
      via: "querySelectorAll('button.sidebar__item')"
      pattern: "sidebar__item"
---

<objective>
Wave 0 test scaffolding. Update every stale structural test to encode the post-138 surface
(3-item sidebar, header-less Hub, no Sessions/Remote panels) and add new RED-failing test
cases for the card indicators and affordances (CARD-02/03/04). These tests MUST fail against
the current code — they define the contract that Plans 02-04 make green.

Purpose: Establish the Nyquist feedback floor before any implementation. Per RESEARCH.md
Pitfall 3 and VALIDATION.md, these tests currently assert the OPPOSITE of the target state
and would otherwise block the phase with false reds.
Output: 5 updated/extended test files that compile and run (RED where implementation is pending).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-hub-first-navigation/138-CONTEXT.md
@.planning/phases/138-hub-first-navigation/138-RESEARCH.md
@.planning/phases/138-hub-first-navigation/138-PATTERNS.md
@.planning/phases/138-hub-first-navigation/138-UI-SPEC.md
@.planning/phases/138-hub-first-navigation/138-VALIDATION.md

<interfaces>
Source-inspection test pattern (App.nav.test.tsx, App.hub.test.tsx):
  import raw from '../../App.tsx?raw'   // assert with raw.toContain / raw.not.toContain
CSS source test pattern (style.hub.test.ts):
  const cssRaw = readFileSync(resolve(__dirname, '../../style.css'), 'utf-8')
SessionCard render test pattern (SessionCard.share.test.tsx): existing renderCard() + flushSync +
  localSession / remoteSession fixtures; props are passed via renderCard({ ... }).
SessionCardProps (target) will gain: isRemote?, isConnected?, onKill?, onOpenInBrowser?, onBrowseFiles?
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite Sidebar + App nav source-inspection tests for the 3-item sidebar</name>
  <files>frontend/src/components/__tests__/Sidebar.test.tsx, frontend/src/components/__tests__/App.nav.test.tsx</files>
  <read_first>
    - frontend/src/components/__tests__/Sidebar.test.tsx (current assertions: items.length===6, Sessions/New Session buttons)
    - frontend/src/components/__tests__/App.nav.test.tsx (NAV-02/03/04 describe blocks asserting old wiring + TAB-01 block)
    - frontend/src/components/Sidebar.tsx (current props + button structure — the target)
    - .planning/phases/138-hub-first-navigation/138-PATTERNS.md (Sidebar.test.tsx + App.hub.test.tsx sections — exact assertions to add/remove)
  </read_first>
  <action>
    In Sidebar.test.tsx: remove `onOpenDaemonManager`, `onOpenRemoteSessions`, `onAdd` from the
    `renderSidebar` defaultProps (keep `onSettings`, `onHome`, `onOpenHub`). Delete the tests
    "renders a Sessions sidebar__item button", "renders New Session label and aria-label for the add
    button", and "Sessions button (aria-label=Sessions) contains an SVG element". Change
    `expect(items.length).toBe(6)` to `toBe(3)`. Change the expanded-icons assertion from
    `toBeGreaterThanOrEqual(7)` to `toBeGreaterThanOrEqual(4)` (1 toggle + 3 nav items). Add four
    new tests: items.length === 3; querySelector `button[aria-label="Sessions"]` is null; same for
    `button[aria-label="Remote"]`; same for `button[aria-label="New Session"]`.
    In App.nav.test.tsx: replace the NAV-02 and NAV-03 describe blocks with assertions that
    `raw` does NOT contain `onOpenRemoteSessions=`, `onOpenDaemonManager=`, `t.type === 'remote-sessions'`,
    or `t.type === 'daemon-manager'`. Replace the NAV-04 block: assert `raw` does NOT contain
    `onAdd={handleAddTab}` and does NOT contain `const handleAddTab`. Keep the NAV-01/NAV-05/TAB-01
    blocks (Home/Settings/TabBar) unchanged — they still hold post-138.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/__tests__/Sidebar.test.tsx src/components/__tests__/App.nav.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.test.tsx contains `expect(items.length).toBe(3)` and contains `button[aria-label="Remote"]` null-check, `button[aria-label="Sessions"]` null-check, `button[aria-label="New Session"]` null-check
    - Sidebar.test.tsx renderSidebar defaultProps does NOT contain `onOpenDaemonManager`, `onOpenRemoteSessions`, or `onAdd`
    - App.nav.test.tsx contains `not.toContain('onOpenRemoteSessions')` and `not.toContain('onOpenDaemonManager')` and `not.toContain('onAdd={handleAddTab}')`
    - Both test files compile and execute under vitest (no parse/import errors); tests targeting deleted-state assertions are RED against current code (expected — implementation pending in later plans)
  </acceptance_criteria>
  <done>Sidebar.test.tsx and App.nav.test.tsx encode the 3-item sidebar; they run (RED where code not yet changed) with no syntax/import errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update App.hub + style.hub tests for header removal, panel deletion, and CARD-03/04 CSS</name>
  <files>frontend/src/components/__tests__/App.hub.test.tsx, frontend/src/components/__tests__/style.hub.test.ts</files>
  <read_first>
    - frontend/src/components/__tests__/App.hub.test.tsx (current routing/header expectations)
    - frontend/src/components/__tests__/style.hub.test.ts (current .hub__header / token assertions)
    - .planning/phases/138-hub-first-navigation/138-PATTERNS.md (App.hub.test.tsx + style.hub.test.ts sections — exact blocks to add/remove)
    - .planning/phases/138-hub-first-navigation/138-UI-SPEC.md (CARD-03 CSS contract; CARD-04 preservation list)
  </read_first>
  <action>
    In App.hub.test.tsx (uses `import raw from '../../App.tsx?raw'`): remove any test asserting
    presence of `DAEMON_MANAGER_TAB`, `REMOTE_SESSIONS_TAB`, `DaemonManagerPanel`, or
    `RemoteSessionsPanel`. Add a describe "NAV-03/04: panels removed" with four `raw.not.toContain`
    assertions for those four identifiers. Add a describe "CARD-01: header-less Hub wiring" asserting
    `raw` contains `onKill=`, `onOpenInBrowser=`, and `onBrowseFiles=` (HubPanel wiring) and that
    HubPanel.tsx?raw (add `import hubRaw from '../Hub/HubPanel.tsx?raw'`) does NOT contain
    `hub__header`.
    In style.hub.test.ts: remove assertions for `.hub__header`, `.hub__title`, `.hub__new-session-btn`.
    Add describe "CARD-03 connection CSS" asserting cssRaw contains `.hub-card__conn`,
    `.hub-card__conn--connected`, `.hub-card__conn-icon`. Add describe "CARD-04 anti-regression"
    asserting cssRaw still contains `.hub__card-row`, `.hub-card--attention`, and `240px`. Add
    describe "CARD-04 destructive menu CSS" asserting cssRaw contains
    `.hub-card__menu-item--destructive` and `--hub-destructive`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/__tests__/App.hub.test.tsx src/components/__tests__/style.hub.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - App.hub.test.tsx contains `not.toContain('DAEMON_MANAGER_TAB')`, `not.toContain('REMOTE_SESSIONS_TAB')`, `not.toContain('DaemonManagerPanel')`, `not.toContain('RemoteSessionsPanel')`
    - App.hub.test.tsx asserts HubPanel source does NOT contain `hub__header` and App source contains `onKill=`, `onOpenInBrowser=`, `onBrowseFiles=`
    - style.hub.test.ts contains assertions for `.hub-card__conn`, `.hub-card__conn--connected`, `.hub-card__conn-icon`, `.hub-card__menu-item--destructive`, `--hub-destructive`
    - style.hub.test.ts retains assertions for `.hub__card-row`, `.hub-card--attention`, `240px` and contains NO assertion for `.hub__header`
    - Both files compile and run under vitest with no import/parse errors
  </acceptance_criteria>
  <done>App.hub.test.tsx and style.hub.test.ts encode the post-138 routing/CSS contract and run (RED where code not yet changed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend SessionCard.share.test.tsx with CARD-02/03/04 RED tests</name>
  <files>frontend/src/components/__tests__/SessionCard.share.test.tsx</files>
  <read_first>
    - frontend/src/components/__tests__/SessionCard.share.test.tsx (existing renderCard, RenderOpts, localSession/remoteSession fixtures)
    - frontend/src/components/Hub/SessionCard.tsx (current origin row at ~357-369, cardAriaLabel at ~180, overflow menu at ~291-320, Share disabled at ~422)
    - .planning/phases/138-hub-first-navigation/138-PATTERNS.md (SessionCard.share.test.tsx section — exact tests to add)
    - .planning/phases/138-hub-first-navigation/138-UI-SPEC.md (Accessibility matrix; copywriting: "Connected"/"Available"/"Kill session")
  </read_first>
  <action>
    Extend the RenderOpts interface and renderCard helper to accept `isRemote`, `isConnected`,
    `onKill`, `onOpenInBrowser`, `onBrowseFiles` and pass them through to `<SessionCard>`. Add
    describe "CARD-02: origin indicator (provenance)": local card (isRemote:false) `.hub-card__origin`
    textContent contains "Local" and has an svg; remote card (isRemote:true) `.hub-card__origin`
    contains the hostname and has an svg. Add describe "CARD-03: connection indicator
    (colorblind-safe)": connected remote (isRemote:true, isConnected:true) renders
    `.hub-card__conn--connected`, textContent contains "Connected", contains an svg; available remote
    (isConnected:false) renders `.hub-card__conn` without `--connected`, textContent "Available";
    local card has NO `.hub-card__conn`; connected card `article` aria-label contains ", connected".
    Add describe "CARD-04: Kill + remote affordances": opening the overflow menu (click
    `.hub-card__menu-btn`) on a live local card shows "Kill session" text; clicking the Kill item
    once then asserting onCardClick was NOT called (stopPropagation guard); remote card overflow menu
    contains "Open in browser" and "Browse files"; local card menu contains neither.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/__tests__/SessionCard.share.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - SessionCard.share.test.tsx RenderOpts includes `isRemote`, `isConnected`, `onKill`, `onOpenInBrowser`, `onBrowseFiles` and renderCard forwards them to SessionCard
    - File contains test assertions for `.hub-card__conn--connected`, text `Connected`, text `Available`, text `Kill session`, text `Open in browser`, text `Browse files`, and aria-label `, connected`
    - File contains a test asserting `onCardClick` is NOT called after clicking the Kill menu item
    - File compiles and runs under vitest; CARD-02/03/04 assertions are RED against current SessionCard (expected — implementation in Plans 02/03)
  </acceptance_criteria>
  <done>SessionCard.share.test.tsx defines the full CARD-02/03/04 card contract and runs (RED), with no import/parse errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → source files | Tests only read source/CSS as strings or mount components in JSDOM; no network, no daemon, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-01 | Information Disclosure | CARD-03 connection test fixtures | mitigate | Tests assert only the boolean `isConnected` rendering; no cap token appears in any fixture or assertion (token lives in daemon RemoteCapStore, never React — T-122-03-01) |
| T-138-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — test edits only against existing vitest + @heroicons; no registry interaction |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/__tests__/Sidebar.test.tsx src/components/__tests__/App.nav.test.tsx src/components/__tests__/App.hub.test.tsx src/components/__tests__/style.hub.test.ts src/components/__tests__/SessionCard.share.test.tsx` — all five files execute with no syntax/import errors. RED failures on not-yet-implemented behavior are expected and acceptable for Wave 0.
- `cd frontend && npx tsc --noEmit` — type-checks (test prop additions reference props that Plans 02/03 will add; if tsc fails only on the new optional props being absent from SessionCardProps, that is the expected RED — document it in the SUMMARY).
</verification>

<success_criteria>
All five test files encode the post-138 surface and the CARD-02/03/04 contract, compile, and run under
vitest. Stale assertions for the old sidebar/header/panels are removed; new assertions are present and
RED where their implementation is pending in later plans.
</success_criteria>

<output>
Create `.planning/phases/138-hub-first-navigation/138-01-SUMMARY.md` when done. Record which assertions
are intentionally RED (pending Plans 02-04) so executors of later plans know their green target.
</output>
