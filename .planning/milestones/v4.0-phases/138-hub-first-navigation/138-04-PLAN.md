---
phase: 138-hub-first-navigation
plan: 04
type: execute
wave: 3
depends_on: ["138-01", "138-02", "138-03"]
files_modified:
  - frontend/src/components/Sidebar.tsx
  - frontend/src/App.tsx
  - frontend/src/components/TabBar.tsx
  - frontend/src/components/DaemonManagerPanel.tsx
  - frontend/src/components/RemoteSessionsPanel.tsx
  - frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx
autonomous: true
requirements: [NAV-02, NAV-03, NAV-04, NAV-05]
must_haves:
  truths:
    - "The sidebar renders exactly Home, Hub, Settings — no Sessions, Remote, or New Session items"
    - "DaemonManagerPanel.tsx and RemoteSessionsPanel.tsx are deleted and not imported anywhere"
    - "App.tsx has no DAEMON_MANAGER_TAB/REMOTE_SESSIONS_TAB consts, handlers, polls, or render branches"
    - "The remote-sessions poll still runs for the Hub (only the REMOTE_SESSIONS_TAB half of the guard is removed)"
    - "App wires onKill/onOpenInBrowser/onBrowseFiles/remotePeers into HubPanel"
    - "Tab.type union no longer contains 'daemon-manager' or 'remote-sessions'"
  artifacts:
    - path: "frontend/src/components/Sidebar.tsx"
      provides: "3-item sidebar"
      contains: "Squares2X2Icon"
    - path: "frontend/src/App.tsx"
      provides: "Cleaned routing + HubPanel wiring"
      contains: "onKill={"
  key_links:
    - from: "App.tsx remote poll"
      to: "Hub remote cards"
      via: "activeId === HUB_TAB.id guard only"
      pattern: "HUB_TAB.id"
    - from: "App.tsx"
      to: "HubPanel parity handlers"
      via: "onKill/onOpenInBrowser/onBrowseFiles/remotePeers"
      pattern: "onBrowseFiles="
---

<objective>
The deletion + routing-cleanup wave. With every parity affordance already live on the Hub (Plan 03) and
the remote types relocated (Plan 02), it is now safe to collapse the sidebar to Home/Hub/Settings, wire
the Hub parity handlers from App, delete the two panels and their dead routing/polls/Tab-type members,
and remove the now-orphaned panel test. This wave makes NAV-02..05 true.

Purpose: Completes the Hub-first navigation goal — Sessions and Remote pages removed, sidebar collapsed,
all session creation routed to the HubFilterBar button — without starving the Hub's remote data poll
(RESEARCH Pitfall 4).
Output: 3-item sidebar, cleaned App.tsx/TabBar, deleted panels + orphan test, full suite green, tsc clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-hub-first-navigation/138-CONTEXT.md
@.planning/phases/138-hub-first-navigation/138-RESEARCH.md
@.planning/phases/138-hub-first-navigation/138-PATTERNS.md
@.planning/phases/138-hub-first-navigation/138-03-SUMMARY.md

<interfaces>
Sidebar.tsx current: imports Bars3Icon, ServerStackIcon(REMOVE), HomeIcon, GlobeAltIcon(REMOVE),
  PlusIcon(REMOVE), Cog6ToothIcon, Squares2X2Icon(KEEP). Props onHome, onOpenRemoteSessions(REMOVE),
  onOpenDaemonManager(REMOVE), onAdd(REMOVE), onSettings, onOpenHub, activePanel. Buttons to remove:
  aria-label="Remote", aria-label="Sessions", aria-label="New Session".
App.tsx routing cleanup (RESEARCH §Routing inventory) — line numbers approximate, confirm by grep:
  - import RemoteSessionsPanel (line 48) + import DaemonManagerPanel (line 47) -> REMOVE
  - DAEMON_MANAGER_TAB const (~88) + REMOTE_SESSIONS_TAB const (~89) -> REMOVE
  - handleOpenDaemonManager (~992) + handleOpenRemoteSessions (~1141) + handleAddTab (~737) -> REMOVE
    (handleAddTab: only caller is sidebar onAdd; confirm via grep before removing)
  - DaemonManager poll effect (~891-911) -> REMOVE entirely
  - Remote poll guard (~946): change `if (activeId !== REMOTE_SESSIONS_TAB.id && activeId !== HUB_TAB.id) return`
    -> `if (activeId !== HUB_TAB.id) return`  (KEEP the effect — Hub needs remote data)
  - handleOpenDaemonManager body sets DAEMON_MANAGER_TAB (~1000-1001) and handleOpenRemoteSessions sets
    REMOTE_SESSIONS_TAB (~1147-1148) -> removed with the handlers
  - DaemonManagerPanel render branch (~1357-1369) + RemoteSessionsPanel render branch (~1478-1486) -> REMOVE
  - Tab-type filter strings 'daemon-manager'/'remote-sessions' (~1517, ~1556) -> REMOVE from both lists
  - <Sidebar> props onOpenRemoteSessions/onOpenDaemonManager/onAdd (~1326-1328) -> REMOVE
  - <HubPanel> (~1379+): ADD onKill={(id) => void handleCloseTab(id)}, onOpenInBrowser={handleOpenRemoteSession},
    onBrowseFiles={handleBrowseFilesRemote}, remotePeers={remotePeers}
KEEP these App handlers (now consumed by HubPanel): handleCloseTab (~749), handleOpenRemoteSession (~1062),
  handleBrowseFilesRemote (~1069). KEEP remoteCapsCached, remotePeers state and the remote poll.
TabBar.tsx Tab.type union (~line 8): remove 'daemon-manager' and 'remote-sessions'.
RemoteSessionsPanel.test.tsx imports RemoteSessionsPanel + RemoteSessionsPanelProps from the panel ->
  DELETE this test file (it tests the deleted component; lib type needs were already repointed in Plan 02).
Wave 0 tests (Plan 01) already assert this end state — they go GREEN here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse the sidebar to Home / Hub / Settings (NAV-02..05)</name>
  <files>frontend/src/components/Sidebar.tsx</files>
  <read_first>
    - frontend/src/components/Sidebar.tsx (imports ~1-10; SidebarProps ~14-22; Remote/Sessions/New Session buttons ~67-101; Hub active-state pattern ~86-92)
    - .planning/phases/138-hub-first-navigation/138-PATTERNS.md (Sidebar.tsx section — exact imports/props/buttons to trim)
    - frontend/src/components/__tests__/Sidebar.test.tsx (Plan 01 assertions: 3 items, no Sessions/Remote/New Session — the green target)
  </read_first>
  <behavior>
    - Sidebar renders exactly 3 `button.sidebar__item` elements: Home, Hub, Settings (plus the collapse toggle)
    - No button with aria-label "Sessions", "Remote", or "New Session" exists
    - Hub/Home/Settings items, the collapse toggle, and width variables are visually unchanged
  </behavior>
  <action>
    Remove `ServerStackIcon`, `GlobeAltIcon`, and `PlusIcon` from the heroicons import (keep Bars3Icon,
    HomeIcon, Cog6ToothIcon, Squares2X2Icon). Remove `onOpenRemoteSessions`, `onOpenDaemonManager`, and
    `onAdd` from the SidebarProps interface. Delete the three button blocks: aria-label="Remote"
    (GlobeAltIcon), aria-label="Sessions" (ServerStackIcon), and aria-label="New Session" (PlusIcon).
    Leave Home, Hub (Squares2X2Icon active-state pattern), Settings, and the collapse toggle untouched.
    The resulting nav has exactly 3 `.sidebar__item` buttons.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/__tests__/Sidebar.test.tsx 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx imports do NOT contain `ServerStackIcon`, `GlobeAltIcon`, or `PlusIcon`
    - SidebarProps does NOT contain `onOpenRemoteSessions`, `onOpenDaemonManager`, or `onAdd`
    - Sidebar.tsx contains no `aria-label="Remote"`, `aria-label="Sessions"`, or `aria-label="New Session"`
    - Sidebar.test.tsx passes GREEN (items.length === 3; null-checks for removed buttons); `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The sidebar is exactly Home/Hub/Settings; removed props and icon imports are gone; Sidebar.test.tsx green; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clean App.tsx routing + wire Hub parity handlers; trim TabBar Tab.type union</name>
  <files>frontend/src/App.tsx, frontend/src/components/TabBar.tsx</files>
  <read_first>
    - frontend/src/App.tsx (all sites enumerated in the <interfaces> block — confirm each by grep before editing)
    - frontend/src/components/TabBar.tsx (Tab.type union ~line 8)
    - .planning/phases/138-hub-first-navigation/138-RESEARCH.md (Routing / Dead-Link Cleanup Inventory — authoritative site list)
    - .planning/phases/138-hub-first-navigation/138-PATTERNS.md (App.tsx + TabBar sections)
    - frontend/src/components/__tests__/App.nav.test.tsx + App.hub.test.tsx (Plan 01 assertions — the green target)
  </read_first>
  <behavior>
    - App.tsx defines no DAEMON_MANAGER_TAB/REMOTE_SESSIONS_TAB and imports neither panel
    - The remote-sessions poll still fires when the Hub tab is active (only the REMOTE_SESSIONS_TAB half of the guard removed)
    - HubPanel receives onKill, onOpenInBrowser, onBrowseFiles, remotePeers
    - Tab.type union omits 'daemon-manager' and 'remote-sessions'
  </behavior>
  <action>
    Remove the `import { DaemonManagerPanel }` and `import { RemoteSessionsPanel }` lines. Remove the
    DAEMON_MANAGER_TAB and REMOTE_SESSIONS_TAB consts. Remove handleOpenDaemonManager,
    handleOpenRemoteSessions, and handleAddTab (grep-confirm handleAddTab has no caller besides the
    deleted sidebar onAdd). Remove the DaemonManager poll effect entirely. In the remote poll, change the
    guard to `if (activeId !== HUB_TAB.id) return` (drop the REMOTE_SESSIONS_TAB clause; KEEP the effect).
    Remove the `{activeId === DAEMON_MANAGER_TAB.id && (...)}` and `{activeId === REMOTE_SESSIONS_TAB.id && (...)}`
    render branches. Remove `'daemon-manager'` and `'remote-sessions'` from both Tab-type filter lists.
    In the `<Sidebar>` JSX, remove the onOpenRemoteSessions, onOpenDaemonManager, and onAdd props. In the
    `<HubPanel>` JSX, add onKill={(id) => void handleCloseTab(id)},
    onOpenInBrowser={handleOpenRemoteSession}, onBrowseFiles={handleBrowseFilesRemote}, and
    remotePeers={remotePeers}. KEEP handleCloseTab, handleOpenRemoteSession, handleBrowseFilesRemote,
    remoteCapsCached, remotePeers, and the remote poll. In TabBar.tsx, remove 'daemon-manager' and
    'remote-sessions' from the Tab.type union.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -20 && npx vitest run src/components/__tests__/App.nav.test.tsx src/components/__tests__/App.hub.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DAEMON_MANAGER_TAB\|REMOTE_SESSIONS_TAB" frontend/src/App.tsx` returns 0
    - `grep -c "DaemonManagerPanel\|RemoteSessionsPanel" frontend/src/App.tsx` returns 0
    - App.tsx contains `if (activeId !== HUB_TAB.id) return` and the remote poll effect is NOT deleted (remotePeers still populated)
    - App.tsx `<HubPanel>` JSX contains `onKill=`, `onOpenInBrowser=`, `onBrowseFiles=`, and `remotePeers=`
    - `grep -c "'daemon-manager'\|'remote-sessions'" frontend/src/App.tsx` returns 0; TabBar.tsx Tab.type union contains neither string
    - `npx tsc --noEmit` exits 0; App.nav.test.tsx and App.hub.test.tsx pass GREEN
  </acceptance_criteria>
  <done>App.tsx routing is cleaned with no dead consts/handlers/branches; the Hub poll survives; HubPanel is wired with parity handlers; TabBar union trimmed; tsc clean; nav tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Delete DaemonManagerPanel + RemoteSessionsPanel + orphan panel test; final suite gate</name>
  <files>frontend/src/components/DaemonManagerPanel.tsx, frontend/src/components/RemoteSessionsPanel.tsx, frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/RemoteSessionsPanel.tsx (confirm its types are already relocated by Plan 02; nothing outside the deleted set should still import from it)
    - frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx (imports RemoteSessionsPanel + RemoteSessionsPanelProps from the panel — deleted with it)
    - .planning/phases/138-hub-first-navigation/138-RESEARCH.md (deletion hazard + Pitfall 1)
    - .planning/phases/138-hub-first-navigation/138-PATTERNS.md (execution order steps 7-10)
  </read_first>
  <behavior>
    - Both panel component files are deleted from the repo
    - No remaining source or test file imports from '../components/RemoteSessionsPanel' or '../components/DaemonManagerPanel'
    - The full vitest suite and tsc both pass
  </behavior>
  <action>
    Before deleting, run `grep -rn "DaemonManagerPanel\|RemoteSessionsPanel" frontend/src/` and confirm
    the ONLY remaining references are the three files being deleted (the two panels + RemoteSessionsPanel.test.tsx).
    If any other importer remains (e.g. a missed lib import), STOP and repoint that import at
    lib/remoteSession.ts first (it should already be repointed by Plan 02). Then delete
    DaemonManagerPanel.tsx, RemoteSessionsPanel.tsx, and RemoteSessionsPanel.test.tsx (use `git rm`). Run
    the full type-check and full vitest suite as the phase gate.
  </action>
  <verify>
    <automated>cd frontend && REFS=$(grep -rln "components/RemoteSessionsPanel\|components/DaemonManagerPanel" src/ | grep -v '^$'); echo "remaining-refs:[$REFS]"; test -z "$REFS"; npx tsc --noEmit 2>&1 | tail -10</automated>
    <automated>cd frontend && npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/DaemonManagerPanel.tsx`, `frontend/src/components/RemoteSessionsPanel.tsx`, and `frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx` no longer exist
    - `grep -rl "components/RemoteSessionsPanel\|components/DaemonManagerPanel" frontend/src/` returns no files
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npx vitest run` — full suite passes (0 failures)
  </acceptance_criteria>
  <done>Both panels and the orphan panel test are deleted; no dangling imports; tsc clean; the full vitest suite is green — Phase 138 navigation goal complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidebar/router state -> rendered surface | Removing routes narrows the reachable UI surface; no new boundary introduced |
| remote poll -> Hub remote cards | Hub still depends on the (retained) remote-sessions poll for its data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-08 | Denial of Service (self-inflicted) | remote poll removal | mitigate | Only the REMOTE_SESSIONS_TAB half of the poll guard is removed; the HUB_TAB guard is retained so the Hub's remote cards keep refreshing (RESEARCH Pitfall 4). Verified by remotePeers remaining populated and the Hub remote-card tests staying green. |
| T-138-09 | Tampering (dead-code rot) | orphaned handlers/consts | mitigate | tsc --noEmit + full vitest gate ensure no dangling import or dead Tab-type reference survives deletion. |
| T-138-SC | Tampering | npm/pip/cargo installs | accept | No package installs — deletions and edits against existing source only; no registry interaction. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0 (no dangling imports to deleted panels).
- `cd frontend && npx vitest run` — full suite passes with 0 failures (Wave 0 structural tests from Plan 01 now all green).
- `grep -rl "components/RemoteSessionsPanel\|components/DaemonManagerPanel" frontend/src/` returns nothing.
- Sidebar renders exactly 3 items; remote poll retained (Hub remote cards still populate).
- Phase gate after this plan: live dev-browser UAT for the 3-item sidebar, sole New-Session entry, and colorblind-safe card indicators (verify icon/text/hex at source, not by eye — MEMORY.md user_colorblind).
</verification>

<success_criteria>
NAV-02..05 are true: the sidebar is Home/Hub/Settings, the Sessions and Remote pages are deleted with no
dead routing/polls/Tab-type members, the Hub's remote data poll survives, App wires the parity handlers
into HubPanel, the orphan panel test is removed, and tsc + the full vitest suite are green.
</success_criteria>

<output>
Create `.planning/phases/138-hub-first-navigation/138-04-SUMMARY.md` when done. Record the full-suite pass
count and confirm the phase is ready for `/gsd:verify-work` (including the live colorblind-safe UAT).
</output>
