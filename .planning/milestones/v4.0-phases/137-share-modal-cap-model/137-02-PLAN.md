---
phase: 137-share-modal-cap-model
plan: 02
type: execute
wave: 1
depends_on: ["137-01"]
files_modified:
  - internal/daemon/engine.go
  - internal/daemon/api.go
  - internal/daemon/types.go
  - internal/daemon/client.go
  - app.go
autonomous: true
requirements: [SHARE-03, SHARE-05]
must_haves:
  truths:
    - "Browse OFF: the RO token carries Perms 'read' and the RW token carries 'read,write' — no filesystem perms (D-03)."
    - "Browse ON: the RO token carries 'read,files.read' (never files.write) and the RW token carries 'read,write,files.read,files.write' (D-04)."
    - "The per-session browse toggle is the sole driver of file-perm injection; the global filesRead setting and the per-session files.write two-gate no longer exist (D-02/D-07)."
    - "Toggling browse off invalidates outstanding caps (ClearGrants), matching the web-serve toggle-off lifecycle (SHARE-05)."
    - "SessionInfo.BrowseEnabled surfaces server-truth browse state so the modal can seed on open (SHARE-05)."
    - "When browse is ON, file permission inherits fully from the presented share code — the per-session browse toggle is the only owner-side gate (no separate write opt-in, no viewer confirm) (D-01)."
    - "RO-code holders gaining read-only filesystem access when browse is ON is intended and accepted (D-05)."
    - "The browse toggle is per-session and defaults OFF — browseEnabledFor returns false for an unknown session id (D-06)."
    - "Browse state is ephemeral in-memory (sessionBrowse map), reset on daemon restart, mirroring the sessionWrites/web-serve lifecycle (D-08)."
  artifacts:
    - path: "internal/daemon/engine.go"
      provides: "sessionBrowse map + browseEnabledFor/SetSessionBrowse; filesRead/filesWriteDefault/sessionWrites removed"
      contains: "sessionBrowse"
    - path: "internal/daemon/api.go"
      provides: "Perm injection matrix (D-03/D-04) + POST /sessions/{id}/browse handler with ClearGrants"
      contains: "browseEnabledFor"
    - path: "internal/daemon/types.go"
      provides: "SessionInfo.BrowseEnabled + SessionBrowseRequest"
      contains: "BrowseEnabled"
    - path: "internal/daemon/client.go"
      provides: "SetSessionBrowse client method (POST /sessions/{id}/browse)"
      contains: "SetSessionBrowse"
    - path: "app.go"
      provides: "SetSessionBrowse Wails binding; SetSessionFilesWrite binding retired"
      contains: "func (a *App) SetSessionBrowse"
  key_links:
    - from: "internal/daemon/api.go issueCapabilitiesForSession"
      to: "internal/daemon/engine.go browseEnabledFor"
      via: "perm injection branch reads browse flag"
      pattern: "browseEnabledFor"
    - from: "app.go SetSessionBrowse"
      to: "internal/daemon/client.go SetSessionBrowse"
      via: "Wails binding delegates to daemon client then POST /sessions/{id}/browse"
      pattern: "SetSessionBrowse"
---

<objective>
Implement the security-core cap model: collapse the global filesRead setting and the per-session files.write two-gate into a single per-session browse toggle that drives the D-03/D-04 perm-injection matrix. Add the daemon endpoint, client method, and Wails binding for the toggle, surface browse state in SessionInfo for server-truth seeding, and wire toggle-off to ClearGrants.

Purpose: This is the isolated, well-commented security delta the later /gsd:secure-phase audits. It turns the RED Go tests from Plan 01 GREEN.
Output: A clean, commented perm-injection delta; new browse endpoint/binding; retired global + write-gate code paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-share-modal-cap-model/137-CONTEXT.md
@.planning/phases/137-share-modal-cap-model/137-RESEARCH.md
@.planning/phases/137-share-modal-cap-model/137-PATTERNS.md
@.planning/phases/137-share-modal-cap-model/137-VALIDATION.md

<interfaces>
Constants (internal/capability/capability.go:30,37): PermFilesRead == "files.read", PermFilesWrite == "files.write". Use capability.HasPerm for any perm checks; never strings.Contains.

D-03/D-04 target perm strings (must match Plan 01 tests exactly):
- Browse OFF: rPerms = "read"; wPerms = "read,write"
- Browse ON:  rPerms = "read,"+capability.PermFilesRead; wPerms = "read,write,"+capability.PermFilesRead+","+capability.PermFilesWrite

Analogs to copy:
- engine.go:602-633 filesWriteEnabledFor / filesWriteEnabledForUnlocked / SetSessionFilesWrite (map+mutex pattern)
- api.go:1273-1285 handleSetSessionFilesWrite route handler
- api.go:139 route registration POST /sessions/{id}/files-write
- api.go:1033-1058 handleWebServe toggle-off ClearGrants pattern
- types.go:130-134 SessionFilesWriteRequest; types.go:20-35 SessionInfo
- client.go:323-328 SetSessionFilesWrite client method
- app.go:827-835 SetSessionFilesWrite Wails binding
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse engine fields into sessionBrowse; rewrite api.go perm injection (D-02/D-03/D-04/D-07)</name>
  <files>internal/daemon/engine.go, internal/daemon/api.go</files>
  <behavior>
    - Browse OFF (default, no map entry): issueCapabilitiesForSession yields RO Perms "read", RW Perms "read,write".
    - Browse ON (SetSessionBrowse true): RO Perms "read,files.read" (no files.write); RW Perms "read,write,files.read,files.write".
    - browseEnabledFor returns false for an unknown session id (default OFF, D-06).
    - filesReadEnabled / filesWriteEnabledFor / SetSessionFilesWrite / filesRead / filesWriteDefault / sessionWrites no longer exist anywhere in the package.
  </behavior>
  <read_first>
    - internal/daemon/engine.go (lines 44-47 fields; 85-110 daemonSettings struct + comments; 145-205 loadSettingsFromDisk incl. FilesRead defaults-merge literal ~167-170 + assignments 192-193; 214-224 saveSettingsToDisk snapshot; 255-258 NewSessionEngine map init; 460-475 ListSessions HomeDir/FilesWrite population; 588-633 filesReadEnabled/filesWriteEnabledFor/SetSessionFilesWrite/sessionCwdIsHome)
    - internal/daemon/api.go (lines 1096-1146 issueCapabilitiesForSession perm block + sign/grant/URL/joincode tail)
    - .planning/phases/137-share-modal-cap-model/137-PATTERNS.md (sections internal/daemon/engine.go and internal/daemon/api.go — exact new code shape, comments, the D-07 audit comment)
    - internal/capability/capability.go (lines 15-61 Claims, PermFilesRead/Write, HasPerm)
  </read_first>
  <action>
    engine.go: Remove fields filesRead *bool, filesWriteDefault bool, sessionWrites map[string]bool (44-46); add sessionBrowse map[string]bool (ephemeral per-session browse state, default OFF per D-06/D-08). Remove FilesRead and FilesWrite from daemonSettings (108-109) and rewrite the comment block (85-100) to record the Phase 137 / D-07 removal of the global filesRead kill-switch. In loadSettingsFromDisk drop the FilesRead defaults-merge literal (the s := daemonSettings{FilesRead: &tr, ...} pre-populate ~167-170, and the now-unused tr var) and the e.filesRead = s.FilesRead / e.filesWriteDefault = s.FilesWrite assignments (~192-193). In saveSettingsToDisk remove the FilesRead/FilesWrite snapshot lines (~220-221). In NewSessionEngine replace the sessionWrites map init with sessionBrowse: make(map[string]bool) (~257). In ListSessions replace FilesWrite: e.filesWriteEnabledForUnlocked(s.ID) with BrowseEnabled: e.browseEnabledForUnlocked(s.ID) (~474). Delete filesReadEnabled (596-599), filesWriteEnabledFor + filesWriteEnabledForUnlocked (602-620), SetSessionFilesWrite (623+). Keep sessionCwdIsHome / sessionCwdIsHomeUnlocked unchanged (D-09). Add browseEnabledFor(sessionID string) bool (RLock; e.sessionBrowse[sessionID], false if absent), browseEnabledForUnlocked(sessionID string) bool (no lock, for ListSessions which already holds the lock; mirror filesWriteEnabledForUnlocked), and SetSessionBrowse(sessionID string, enabled bool) (Lock; lazily init map; set entry). Comment browseEnabledFor referencing D-07 (deliberate removal of the global filesReadEnabled kill-switch) for the secure-phase audit.
    api.go: Replace the perm block at 1096-1116. Build rPerms := "read" and wPerms := "read,write"; if a.engine.browseEnabledFor(sessionID) then rPerms = "read,"+capability.PermFilesRead and wPerms = "read,write,"+capability.PermFilesRead+","+capability.PermFilesWrite. Construct rClaims with Perms: rPerms and wClaims with Perms: wPerms (keep existing SID/IAT/GrantID/V fields). Add the well-commented security header per PATTERNS.md noting D-03/D-04, "files.write NEVER appears in rPerms" (Pitfall 2), and the deliberate reversals (T-124-07 write no longer separately gated; CAP-08/global removed) with an explicit "Audit: secure-phase reviews Reversal 1 and Reversal 3 in 137-RESEARCH.md". Leave the sign/AddGrant/URL/joinCodes.Issue tail (1118-1146) unchanged. No strings.Contains anywhere.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./... && go test ./internal/daemon/... -run TestIssueCapabilities -v</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... succeeds (no dangling references to filesRead/filesWriteEnabledFor/SetSessionFilesWrite).
    - go test ./internal/daemon/... -run TestIssueCapabilities passes: BrowseOff yields exactly read / read,write; BrowseOn RO is exactly read,files.read; BrowseOn RW is exactly read,write,files.read,files.write.
    - grep confirms zero occurrences of filesReadEnabled, filesWriteEnabledFor, SetSessionFilesWrite, filesRead, filesWriteDefault, sessionWrites in internal/daemon/engine.go.
    - api.go perm block reads from browseEnabledFor and contains the secure-phase audit comment referencing Reversal 1 and Reversal 3.
  </acceptance_criteria>
  <done>engine.go collapses to a single sessionBrowse map; api.go injects perms from the browse flag per the D-03/D-04 matrix with an audit-ready comment; the Plan 01 browse-matrix tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add browse endpoint + client method + Wails binding + SessionInfo field; retire files-write surface</name>
  <files>internal/daemon/types.go, internal/daemon/api.go, internal/daemon/client.go, app.go</files>
  <read_first>
    - internal/daemon/types.go (lines 20-35 SessionInfo; 130-134 SessionFilesWriteRequest)
    - internal/daemon/api.go (line 139 route registration; lines 1033-1058 handleWebServe ClearGrants pattern; lines 1273-1285 handleSetSessionFilesWrite)
    - internal/daemon/client.go (lines 323-328 SetSessionFilesWrite)
    - app.go (lines 827-835 SetSessionFilesWrite binding)
    - .planning/phases/137-share-modal-cap-model/137-PATTERNS.md (sections types.go, client.go, app.go — exact structural copies)
  </read_first>
  <action>
    types.go: Add BrowseEnabled bool with tag json:"browseEnabled" to SessionInfo immediately after FilesWrite — plain bool, NOT omitempty (false must serialize so the modal can seed, per RESEARCH open question 2). Remove the FilesWrite field from SessionInfo (it is replaced by BrowseEnabled; nothing reads FilesWrite after the DaemonManagerPanel removal — confirm via grep before removing, and if any non-Plan-03 consumer remains, leave FilesWrite and only ADD BrowseEnabled, noting the decision in the SUMMARY). Add SessionBrowseRequest{ Enabled bool json:"enabled" } mirroring SessionFilesWriteRequest; remove SessionFilesWriteRequest.
    api.go: Register POST /sessions/{id}/browse mapped to a new handleSetSessionBrowse (copy handleSetSessionFilesWrite structure: decode SessionBrowseRequest, call a.engine.SetSessionBrowse(id, req.Enabled)). After setting browse, follow the SHARE-05 / Pitfall 1 + Pitfall (stale caps) rule: if the toggle changes file-perm exposure, outstanding caps for that session must be invalidated so the next IssueCapabilities re-mints with correct perms — call the same ClearGrants path handleWebServe uses for the session (mirror api.go:1033-1058; clear grants for this session id) so a browse toggle-off cannot leave a stale files.read cap live (threat: stale cap after toggle). Comment this handler noting browse toggle-off clears grants (parity with handleWebServe). Remove the POST /sessions/{id}/files-write route registration (139) and delete handleSetSessionFilesWrite (1273-1285).
    client.go: Add SetSessionBrowse(sessionID string, enabled bool) error doing c.doJSON(http.MethodPost, "/sessions/"+sessionID+"/browse", SessionBrowseRequest{Enabled: enabled}, nil). Remove SetSessionFilesWrite.
    app.go: Add func (a *App) SetSessionBrowse(sessionID string, enabled bool) error with the nil-client guard, delegating to a.client.SetSessionBrowse. Remove the SetSessionFilesWrite binding.
    After edits, regenerate/refresh the Wails bindings if the build requires it (the frontend wailsjs bindings for SetSessionBrowse are consumed in Plan 03 — note in SUMMARY whether wails generate is needed or the binding is hand-mirrored).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./... && grep -q "POST /sessions/{id}/browse" internal/daemon/api.go && grep -q "func (a \*App) SetSessionBrowse" app.go && grep -q "BrowseEnabled bool" internal/daemon/types.go && ! grep -rq "SetSessionFilesWrite" internal/daemon/ app.go</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... succeeds.
    - POST /sessions/{id}/browse is registered and handleSetSessionBrowse decodes SessionBrowseRequest and calls engine.SetSessionBrowse.
    - handleSetSessionBrowse clears grants for the session (parity with handleWebServe) so a toggle-off invalidates outstanding caps.
    - SessionInfo carries BrowseEnabled bool with json:"browseEnabled" (no omitempty).
    - grep finds zero SetSessionFilesWrite references under internal/daemon/ and in app.go (route, handler, client method, binding all retired).
  </acceptance_criteria>
  <done>The browse toggle has a full daemon-socket path (endpoint to client to Wails binding), SessionInfo surfaces browse state for seeding, toggle-off clears grants, and the retired files-write surface is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner GUI -> daemon socket | Browse toggle + IssueCapabilities cross here; loopback-trusted, no auth gate (daemon binds 127.0.0.1) |
| web visitor -> webserver routes | Untrusted; presents a cap token; perms enforced by requireFilesRead/requireFilesWrite (unchanged this phase) |
| issuance time vs enforcement time | Perms frozen into the token at issuance (api.go); enforcement reads token content only — an old RO token issued pre-browse carries only "read" and correctly 403s on file routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-01 | Elevation of Privilege | issueCapabilitiesForSession (file browse without consent) | mitigate | Browse toggle default OFF (D-06); browseEnabledFor returns false for absent map entry; no global kill-switch needed |
| T-137-02 | Elevation of Privilege | RO token perm injection | mitigate | rPerms ON = "read,files.read" only; files.write NEVER written to rPerms (Pitfall 2); pinned by TestIssueCapabilities_BrowseOn_ROPermsExact |
| T-137-03 | Elevation of Privilege | RO holder writing files | mitigate | RW-only files.write injection; webserver requireFilesWrite (unchanged) 403s RO; pinned by Plan 01 TestFilesRoutes_RO_BrowseOn_WriteRoute403 |
| T-137-05 | Elevation of Privilege | global filesRead removal (D-07) | mitigate | Per-session default OFF replaces the global kill-switch; documented reversal 3 in api.go comment for secure-phase |
| T-137-06 | Tampering | new perm-injection code | mitigate | Whole-token capability.HasPerm only; no strings.Contains; pinned by Plan 01 TestHasPerm_NoStringsContains_Browse |
| T-137-07 | Tampering | CSRF on web write routes | accept (unchanged) | originAllowedForWrite in requireFilesWrite untouched; browse endpoint is a daemon-socket call, not a webserver route, so no CSRF applies to it |
| stale-cap | Repudiation | browse toggle-off | mitigate | handleSetSessionBrowse clears grants on toggle (parity with handleWebServe) so a toggle-off invalidates outstanding files.read caps |
| T-137-SC | Tampering | go module installs | accept | No new packages (RESEARCH Package Legitimacy Audit: none); no install tasks |
</threat_model>

<verification>
- go build ./... is green (no dangling filesRead/files-write references).
- go test ./internal/daemon/... -run TestIssueCapabilities passes the four D-03/D-04 matrix cells.
- go test ./internal/webserver/... -run TestFilesRoutes_R and -run TestRequireFiles pass (enforcement + RO-no-write invariants).
- grep gates confirm the retired surface (SetSessionFilesWrite, filesReadEnabled, filesRead, sessionWrites) is gone.
- The api.go perm block carries the secure-phase audit comment (Reversal 1, Reversal 3) so the deltas are isolated and reviewable.
</verification>

<success_criteria>
- Single per-session sessionBrowse map is the sole driver of file-perm injection (D-02/D-07).
- D-03/D-04 perm matrix produces exact strings; RO token never carries files.write.
- Browse endpoint + client method + Wails binding exist; SessionInfo.BrowseEnabled seeds the modal.
- Browse toggle-off clears grants (no stale caps).
- Retired files-write/global-filesRead surface fully removed; full Go suite green.
</success_criteria>

<output>
Create .planning/phases/137-share-modal-cap-model/137-02-SUMMARY.md when done.
</output>
