---
phase: 137-share-modal-cap-model
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/daemon/api_test.go
  - internal/daemon/engine_migration_test.go
  - internal/webserver/files_routes_test.go
  - internal/webserver/capability_test.go
  - frontend/src/components/__tests__/SessionCard.share.test.tsx
  - frontend/src/components/__tests__/SessionShareModal.test.tsx
  - frontend/src/components/__tests__/SessionSharePanel.test.tsx
autonomous: true
requirements: [SHARE-01, SHARE-02, SHARE-03, SHARE-04, SHARE-05, SHARE-06]
must_haves:
  truths:
    - "Browse-matrix Go tests exist and pin the exact D-03/D-04 perm strings (RED until Plan 02 lands)."
    - "The four old global-flag issueCapabilities tests and the two FilesRead settings-migration tests are retired."
    - "New frontend test files for the SessionCard Share button and SessionShareModal exist and define SHARE-01/02/04/05/06 + D-09/D-13."
    - "SessionSharePanel.test.tsx no longer asserts CAP-05 two-gate behavior; the write link is asserted always-shown when sharing is ON."
  artifacts:
    - path: "internal/daemon/api_test.go"
      provides: "Browse-matrix issueCapabilities tests (exact D-03/D-04 perm strings)"
      contains: "TestIssueCapabilities_BrowseOn_ROPermsExact"
    - path: "internal/webserver/files_routes_test.go"
      provides: "Browse-aware route tests (RO 200 read, RO 403 write, RW 200 write)"
      contains: "TestFilesRoutes_RO_BrowseOn"
    - path: "frontend/src/components/__tests__/SessionShareModal.test.tsx"
      provides: "Modal contract: share toggle, browse toggle, LAN password, homeDir warning, server-truth seeding, stale-URL clear"
    - path: "frontend/src/components/__tests__/SessionCard.share.test.tsx"
      provides: "Share button render + onShare fire + no-bubble + remote-disabled contract"
  key_links:
    - from: "internal/daemon/api_test.go"
      to: "api.engine.SetSessionBrowse / browseEnabledFor"
      via: "test calls SetSessionBrowse before issueCapabilitiesForSession"
      pattern: "SetSessionBrowse"
    - from: "frontend test mocks"
      to: "wailsjs/go/main/App"
      via: "vi.mock injects SetSessionBrowse + IssueCapabilities + GetLocalNetworkPassword"
      pattern: "SetSessionBrowse"
---

<objective>
Establish the Nyquist test contract for Phase 137 BEFORE any implementation. Retire the tests pinned to the old global filesRead model and the CAP-05 two-gate, and create new RED tests pinning the D-03/D-04 perms matrix, browse-aware webserver enforcement, and the new Share modal/card frontend behavior.

Purpose: These tests are the executable specification. Plan 02 (backend) and Plan 03 (frontend) turn them GREEN. The security-delta Go tests (exact perm strings, RO-no-write 403, whole-token grep) are the primary audit target for the later /gsd:secure-phase pass.
Output: Retired obsolete tests; new RED Go + frontend test files defining the contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-share-modal-cap-model/137-CONTEXT.md
@.planning/phases/137-share-modal-cap-model/137-RESEARCH.md
@.planning/phases/137-share-modal-cap-model/137-PATTERNS.md
@.planning/phases/137-share-modal-cap-model/137-VALIDATION.md

<interfaces>
New engine methods Plan 02 will add (tests are written RED against these):
- browseEnabledFor(sessionID string) bool
- SetSessionBrowse(sessionID string, enabled bool)

Perms matrix (D-03/D-04) pinned exactly:
- Browse OFF: RO Perms == "read"; RW Perms == "read,write"
- Browse ON:  RO Perms == "read,files.read"; RW Perms == "read,write,files.read,files.write"

Existing test helpers to reuse:
- api_test.go: extractClaimsFromURL(t, urlStr, key) returns capability.Claims; issueCapsTestSetup (to be rewritten to drop the filesRead param).
- files_routes_test.go: newFilesTestServer(t) returns (ws, client, sid, tmp).
- capability_test.go: issueCapFor(t, ws, sid, perms string) string mints a real HMAC token with the literal perm string; doRequest, fileURL helpers.
- Constants: capability.PermFilesRead == "files.read", capability.PermFilesWrite == "files.write".

Frontend Wails bindings the modal/card call (mock these): IssueCapabilities, ToggleWebServing, SetSessionBrowse, GetLocalNetworkPassword, GetCapabilityQRCode; runtime ClipboardSetText, BrowserOpenURL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire obsolete Go tests; add D-03/D-04 browse-matrix tests (RED)</name>
  <files>internal/daemon/api_test.go, internal/daemon/engine_migration_test.go</files>
  <read_first>
    - internal/daemon/api_test.go (lines 1982-2115: the four TestIssueCapabilities_* tests + issueCapsTestSetup/extractClaimsFromURL helpers)
    - internal/daemon/engine_migration_test.go (lines 200-260: TestSettingsMigration_FilesReadDefaultsTrue, TestSettingsMigration_FilesReadExplicitFalse; lines 133-172: TestSettingsMigrationIdempotent survivor)
    - .planning/phases/137-share-modal-cap-model/137-PATTERNS.md (section internal/daemon/api_test.go — helper + assertion patterns and new test names)
    - internal/daemon/engine.go (lines 596-633: filesReadEnabled/filesWriteEnabledFor/SetSessionFilesWrite being retired)
  </read_first>
  <action>
    In api_test.go delete the four global-flag tests TestIssueCapabilities_OwnerHasFilesRead_WhenSettingNil, TestIssueCapabilities_ViewerNoFilesRead, TestIssueCapabilities_OwnerNoFilesReadWhenDisabled, TestIssueCapabilities_OwnerHasFilesReadWhenExplicitTrue. Rewrite the issueCapsTestSetup helper to drop the filesRead param (it currently does api.engine.filesRead = filesRead, a field Plan 02 removes); keep its webserver wiring + key return. Add three RED tests calling api.engine.SetSessionBrowse(sid, ...) before api.issueCapabilitiesForSession(sid), asserting EXACT perm strings via extractClaimsFromURL:
    TestIssueCapabilities_BrowseOff_NoFilesPerms — no SetSessionBrowse (default OFF): RO claims.Perms == "read", RW claims.Perms == "read,write".
    TestIssueCapabilities_BrowseOn_ROPermsExact — SetSessionBrowse(sid,true): RO claims.Perms == "read,"+capability.PermFilesRead exactly, AND assert !capability.HasPerm(roClaims.Perms, capability.PermFilesWrite).
    TestIssueCapabilities_BrowseOn_RWPermsExact — SetSessionBrowse(sid,true): RW claims.Perms == "read,write,"+capability.PermFilesRead+","+capability.PermFilesWrite exactly.
    Use exact string equality with t.Errorf (not HasPerm) for the matrix assertions so a stray extra perm fails.
    In engine_migration_test.go delete TestSettingsMigration_FilesReadDefaultsTrue and TestSettingsMigration_FilesReadExplicitFalse (they read e.filesRead, removed in Plan 02 per D-07). Do NOT touch TestSettingsMigrationIdempotent or TestSettingsMigrationV3_1ToV3_2 — they assert schemaVersion/mtime, not filesRead. Add a top-of-file comment noting the two FilesRead tests were retired for Phase 137 / D-07.
    These tests are EXPECTED to fail to compile/run until Plan 02 adds SetSessionBrowse and removes filesRead — that is the intended RED state.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && grep -q TestIssueCapabilities_BrowseOn_ROPermsExact internal/daemon/api_test.go && grep -q TestIssueCapabilities_BrowseOn_RWPermsExact internal/daemon/api_test.go && grep -q TestIssueCapabilities_BrowseOff_NoFilesPerms internal/daemon/api_test.go && ! grep -q TestIssueCapabilities_OwnerHasFilesRead_WhenSettingNil internal/daemon/api_test.go && ! grep -q TestSettingsMigration_FilesReadDefaultsTrue internal/daemon/engine_migration_test.go && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms the three new browse-matrix test names exist in api_test.go.
    - grep confirms all four old global-flag test names are gone from api_test.go.
    - grep confirms TestSettingsMigration_FilesReadDefaultsTrue and TestSettingsMigration_FilesReadExplicitFalse are gone from engine_migration_test.go.
    - grep -c TestSettingsMigrationIdempotent on engine_migration_test.go returns 1 (survivor untouched).
    - The RW test asserts the exact string read,write,files.read,files.write; the RO test asserts exactly read,files.read and asserts files.write absent via capability.HasPerm.
  </acceptance_criteria>
  <done>Obsolete global-flag and FilesRead-migration tests deleted; three RED browse-matrix tests pinning exact D-03/D-04 perm strings exist; survivor migration tests untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add browse-aware webserver route tests + whole-token grep gate</name>
  <files>internal/webserver/files_routes_test.go, internal/webserver/capability_test.go</files>
  <read_first>
    - internal/webserver/files_routes_test.go (lines 37-58: newFilesTestServer helper; lines 93-300: existing owner/viewer route tests — copy request/assert style + HTTP method used for the write-route probe)
    - internal/webserver/capability_test.go (line ~38: issueCapFor signature; lines 774-796: TestHasPerm_NoStringsContains_Write static-grep gate)
    - internal/webserver/capability_mw.go (lines 102-170: requireFilesRead / requireFilesWrite — UNCHANGED this phase)
    - .planning/phases/137-share-modal-cap-model/137-PATTERNS.md (section internal/webserver/files_routes_test.go — exact new test bodies + issueCapFor usage)
  </read_first>
  <action>
    In files_routes_test.go add three tests using issueCapFor(t, ws, sid, perms) with exact browse-ON perm strings, then doRequest against fileURL, mirroring the existing route-test style (same HTTP method/helper the existing write-route tests use):
    TestFilesRoutes_RO_BrowseOn_FilesReadRoute200 — perms "read,files.read"; GET /api/files/list expects 200.
    TestFilesRoutes_RO_BrowseOn_WriteRoute403 — perms "read,files.read" (no files.write); write-route probe expects 403 (RO code never writes even with browse ON — T-137-02/03 security delta).
    TestFilesRoutes_RW_BrowseOn_WriteRoute200 — perms "read,write,files.read,files.write"; write-route probe expects 200.
    In capability_test.go add TestHasPerm_NoStringsContains_Browse: read internal/daemon/api.go and internal/daemon/engine.go, strip comment lines (filter lines whose trimmed prefix is //) before scanning, and assert zero occurrences of strings.Contains applied to a perm literal (files.read/files.write). Pins the whole-token-only invariant (T-137-06). Do NOT modify the existing TestRequireFilesWrite / TestRequireFilesRead / TestHasPerm_NoStringsContains_Write tests — they assert unchanged CSRF + enforcement invariants (T-137-07) and must keep passing.
    The three files_routes tests use literal perms via issueCapFor so they pass as soon as the routes exist (they already do); their value is pinning the RO-no-write invariant against future regressions.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && grep -q TestFilesRoutes_RO_BrowseOn_WriteRoute403 internal/webserver/files_routes_test.go && grep -q TestFilesRoutes_RW_BrowseOn_WriteRoute200 internal/webserver/files_routes_test.go && grep -q NoStringsContains_Browse internal/webserver/capability_test.go && go test ./internal/webserver/... -run TestFilesRoutes_R</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms TestFilesRoutes_RO_BrowseOn_WriteRoute403 and TestFilesRoutes_RW_BrowseOn_WriteRoute200 exist.
    - grep confirms TestHasPerm_NoStringsContains_Browse exists in capability_test.go.
    - go test ./internal/webserver/... -run TestFilesRoutes_R passes (route tests use literal perms and the routes already exist).
    - The existing TestRequireFilesWrite / TestRequireFilesRead tests are unmodified (CSRF + enforcement invariants preserved).
  </acceptance_criteria>
  <done>Three browse-aware route tests + the whole-token grep gate over api.go/engine.go exist and pass; existing CSRF/enforcement tests untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Create frontend test files (Share button + modal) and update SessionSharePanel tests</name>
  <files>frontend/src/components/__tests__/SessionCard.share.test.tsx, frontend/src/components/__tests__/SessionShareModal.test.tsx, frontend/src/components/__tests__/SessionSharePanel.test.tsx</files>
  <read_first>
    - frontend/src/components/__tests__/SessionSharePanel.test.tsx (lines 1-100: vi.mock setup, renderPanel helper, afterEach cleanup; the CAP-05 describe block to retire)
    - frontend/src/components/SessionSharePanel.tsx (current props incl. ownerWriteEnabled + allowFileEditing/showWriteConfirm state being stripped in Plan 03)
    - frontend/src/components/Hub/SessionCard.tsx (lines 156-157 isLocal; lines 388-402 Open button row5 analog)
    - frontend/src/components/DaemonManagerPanel.tsx (lines 69-77 LAN password fetch; 90-143 toggle-then-reissue; 171-234 reconcile + restart-clear — the SHARE-05 behaviors)
    - .planning/phases/137-share-modal-cap-model/137-PATTERNS.md (sections: Frontend test files NEW; SessionSharePanel.test.tsx update — exact mock + render patterns)
  </read_first>
  <action>
    Create SessionCard.share.test.tsx using the vi.mock + createRoot/flushSync + afterEach cleanup pattern from SessionSharePanel.test.tsx. Tests (RED until Plan 03 adds the Share button + onShare prop):
    - renders a Share button on a local card (hostname: "") with accessible label Share <name>.
    - clicking Share fires onShare and does NOT fire onCardClick (assert stopPropagation: render with both onShare and onClick/onCardClick mocks; click Share; onShare called once, card click handler not called).
    - on a remote card (hostname: "remote.host") the Share button has disabled attribute and aria-label/title "Only the session owner can share" and renders a non-color affordance (lock icon present — query by the lock svg / class hub-card__share-lock). This pins D-13 colorblind-safe.
    Create SessionShareModal.test.tsx mocking IssueCapabilities, ToggleWebServing, SetSessionBrowse, GetLocalNetworkPassword, GetCapabilityQRCode. Tests (RED until Plan 03 builds the modal):
    - "Share the session" toggle present; toggling ON reveals RO + RW link rows (calls ToggleWebServing then renders SessionSharePanel content).
    - "Enable remote file browsing" toggle present and disabled/no-op when sharing is OFF; when sharing ON, toggling it calls SetSessionBrowse then IssueCapabilities (server-truth re-issue, SHARE-05 / Pitfall 1).
    - local-mode fixture (webServerMode local + running) surfaces the LAN Basic Auth password via GetLocalNetworkPassword (SHARE-04).
    - homeDir fixture (session.homeDir true) shows the home-dir warning before browse is enabled (D-09).
    - server-truth seeding: opening with session.webEnabled true and no cached share calls IssueCapabilities once (SHARE-05).
    - stale-URL clear: when webServerRunning flips false then true, cached share URLs are cleared (SHARE-05 restart behavior).
    Update SessionSharePanel.test.tsx: delete the CAP-05 describe block (tests for ownerWriteEnabled prop, allowFileEditing state, showWriteConfirm dialog, surfaceWriteLink gate). Add tests asserting the simplified panel always renders the write link row when writeURL/writeCode are provided (no gate), and renders scope text reflecting the new browseEnabled prop (Watch only vs Watch + browse files). Keep the existing QR/clipboard tests passing (SHARE-04).
    Keep frontend mocks aligned with the Wails bindings Plan 02/03 will expose; these tests are RED now and GREEN after Plan 03.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && test -f frontend/src/components/__tests__/SessionCard.share.test.tsx && test -f frontend/src/components/__tests__/SessionShareModal.test.tsx && grep -q "Only the session owner can share" frontend/src/components/__tests__/SessionCard.share.test.tsx && grep -q SetSessionBrowse frontend/src/components/__tests__/SessionShareModal.test.tsx && ! grep -q allowFileEditing frontend/src/components/__tests__/SessionSharePanel.test.tsx && echo FRONTEND_SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - SessionCard.share.test.tsx and SessionShareModal.test.tsx exist.
    - SessionCard.share.test.tsx asserts the remote-disabled state via the lock affordance + "Only the session owner can share" (not color) and asserts onShare does not bubble to the card click handler.
    - SessionShareModal.test.tsx references SetSessionBrowse, GetLocalNetworkPassword, and IssueCapabilities (browse toggle re-issue + LAN password + server-truth seeding).
    - SessionSharePanel.test.tsx no longer contains allowFileEditing / ownerWriteEnabled / showWriteConfirm assertions and asserts the write link is always shown when sharing ON.
  </acceptance_criteria>
  <done>Two new frontend test files define the SHARE-01/02/04/05/06 + D-09/D-13 contract (RED); SessionSharePanel.test.tsx is updated to the simplified post-CAP-05 contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner GUI → daemon socket | Browse toggle + IssueCapabilities cross here; loopback-trusted, no auth gate |
| web visitor → webserver routes | Untrusted; presents a cap token; perms enforced by requireFilesRead/requireFilesWrite |
| issuance time vs enforcement time | Perms are frozen into the token at issuance (api.go); enforcement reads token content only |

## STRIDE Threat Register

This plan creates the TEST contract that pins the security deltas implemented in Plan 02. The threats below are the audit targets; this plan mitigates them by making each one a failing-then-passing test.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-02 | Elevation of Privilege | issueCapabilitiesForSession (RO token) | mitigate | TestIssueCapabilities_BrowseOn_ROPermsExact asserts RO == "read,files.read" and files.write absent |
| T-137-03 | Elevation of Privilege | webserver requireFilesWrite | mitigate | TestFilesRoutes_RO_BrowseOn_WriteRoute403 pins RO cap cannot reach write route |
| T-137-06 | Tampering | new perm-injection code | mitigate | TestHasPerm_NoStringsContains_Browse greps api.go/engine.go for substring perm matching (comment-filtered) |
| T-137-07 | Tampering | requireFilesWrite CSRF | accept (unchanged) | Existing TestRequireFilesWrite kept passing; this phase does not touch originAllowedForWrite |
| T-137-SC | Tampering | npm/go installs | accept | No new packages added (RESEARCH Package Legitimacy Audit: none) — no install tasks in this plan |
</threat_model>

<verification>
- go test ./internal/webserver/... -run TestFilesRoutes_R passes (literal-perm route tests).
- Go scaffold greps in Tasks 1-2 confirm retired tests gone and new tests present.
- Frontend scaffold greps in Task 3 confirm the two new files + simplified panel test.
- The new browse-matrix Go tests and frontend modal/card tests are RED (compile/run fail) until Plans 02/03 land — this is the expected Nyquist RED baseline.
</verification>

<success_criteria>
- Old global-flag issueCapabilities tests + two FilesRead migration tests retired.
- D-03/D-04 browse-matrix Go tests pin exact perm strings.
- Browse-aware webserver route tests + whole-token grep gate exist and pass.
- New frontend Share button + Share modal test files exist; SessionSharePanel tests updated to the post-CAP-05 contract.
- No new external packages introduced.
</success_criteria>

<output>
Create .planning/phases/137-share-modal-cap-model/137-01-SUMMARY.md when done.
</output>
