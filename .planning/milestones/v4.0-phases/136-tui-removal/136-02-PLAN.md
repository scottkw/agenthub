---
phase: 136-tui-removal
plan: 02
type: execute
wave: 2
depends_on: [136-01]
files_modified:
  - README.md
  - internal/daemon/client.go
  - internal/attach/attach.go
  - frontend/src/lib/filesApi.ts
autonomous: true
requirements: [NAV-01, TEST-06]

must_haves:
  truths:
    - "User-facing docs (README) no longer present `agenthub tui` as a usable surface"
    - "No source comment references the deleted `internal/tui` package as if it still exists"
    - "The surviving Go test suite passes under -race"
    - "The frontend test suite passes (no regression)"
    - "`agenthub tui` exits with a non-zero code at runtime"
  artifacts:
    - path: "README.md"
      provides: "Docs describing GUI/CLI/web surfaces only — no TUI Mode section, no `agenthub tui` usage, no Bubble Tea in the architecture diagram/package table"
      contains: "AgentHub"
    - path: "internal/daemon/client.go"
      provides: "Comment at the former line 516 no longer referencing internal/tui/files_client.go as a live path"
      contains: "package daemon"
  key_links:
    - from: "README architecture/surface descriptions"
      to: "the three surviving surfaces (GUI/CLI/web)"
      via: "removal of TUI Mode section + Bubble Tea references"
      pattern: "GUI"
    - from: "phase gate"
      to: "green Go + frontend suites and non-zero `agenthub tui`"
      via: "go test -race -short ./... && pnpm test && ./agenthub tui exit code"
      pattern: "go test"
---

<objective>
Clean up all user-facing and in-source references to the removed TUI surface (README, three comment-only references), then run the full phase-gate verification: the surviving Go test suite under -race, the frontend test suite, and a runtime smoke that `agenthub tui` exits non-zero.

Purpose: Closes the TEST-06 "surviving suites pass" gate and the NAV-01 runtime/docs gates. After plan 01 removed the code, this plan removes the documentation residue and proves the whole matrix is green with zero TUI residue.

Output: Docs and comments that describe only GUI/CLI/web, and a recorded green run of `go test -race -short ./...`, `pnpm test`, and the `agenthub tui` non-zero exit smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-tui-removal/136-RESEARCH.md
@.planning/phases/136-tui-removal/136-VALIDATION.md
@.planning/phases/136-tui-removal/136-01-SUMMARY.md

<notes>
- This plan runs AFTER plan 01 (the package and consumers are already deleted; go.mod is tidied). It touches only docs/comments + runs the gate.
- RESEARCH section 1 enumerates the comment-only references. RESEARCH section 6 + VALIDATION.md give the exact proving commands.
- README has many TUI references: the intro paragraph (line ~7), a dedicated "TUI Mode" section (line ~110), the architecture ASCII diagram (lines ~218-219), the package table (line ~259), the "Shell" surface lists (lines ~164-168), and historical changelog entries. Remove TUI as a CURRENT surface (intro, TUI Mode section, architecture diagram, package table, current surface lists). HISTORICAL changelog entries describing past releases may stay factually accurate but must not present `agenthub tui` as a currently available command — when in doubt, leave dated release-history bullets but drop "TUI Mode"/current-surface presentation.
- `frontend/src/lib/filesApi.ts` line ~115 is a comment referencing the deleted file — update it (no behavior change). The frontend test suite must still pass.
- `DaemonManagerPanel.tsx` is deferred to Phase 138 (NAV-03) — do NOT modify it here.
- Do NOT modify any `.go` logic in this plan beyond comment text.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove TUI references from README and source comments</name>
  <read_first>
    - .planning/phases/136-tui-removal/136-RESEARCH.md (section 1 "comment-only TUI references" + section 7 step 5)
    - README.md (intro line ~7, "TUI Mode" section ~110, architecture diagram ~218-219, package table ~258-259, surface lists ~164-168)
    - internal/daemon/client.go (line ~516 comment referencing internal/tui/files_client.go)
    - internal/attach/attach.go (line ~2 package comment mentioning "TUI (internal/tui/attach.go)")
    - frontend/src/lib/filesApi.ts (line ~115 comment referencing internal/tui/remote_files_client.go)
  </read_first>
  <files>README.md, internal/daemon/client.go, internal/attach/attach.go, frontend/src/lib/filesApi.ts</files>
  <action>
    Update docs and comments to remove the deleted TUI surface:
    1. README.md: remove `agenthub tui` and Bubble Tea TUI as a CURRENT surface — edit the intro paragraph (drop "and a full-screen Bubble Tea TUI (`agenthub tui`)" so only GUI/CLI/web remain), delete the "### TUI Mode" section and its bullets, remove the `TUI (agenthub tui)` box from the architecture ASCII diagram, remove the `internal/tui` row from the package/architecture table, and drop TUI from current cross-surface lists (e.g. the Shell-surface enumeration). Leave dated release-history bullets factually intact but do not present `agenthub tui` as a currently available command.
    2. internal/daemon/client.go: update or delete the comment at the former line 516 so it no longer references `internal/tui/files_client.go` as a live path. Comment text only — no code change.
    3. internal/attach/attach.go: update the package comment (line ~2) to drop the "and TUI (internal/tui/attach.go)" clause; the CLI attach reference stays. Comment text only.
    4. frontend/src/lib/filesApi.ts: update the comment at line ~115 to remove the `internal/tui/remote_files_client.go` reference (point at the daemon proxy parity contract instead, or drop the stale path). Comment text only.
    Do NOT modify DaemonManagerPanel.tsx (deferred to Phase 138). Do NOT change any Go/TS logic.
  </action>
  <verify>
    <automated>! grep -rn "internal/tui" README.md internal/daemon/client.go internal/attach/attach.go frontend/src/lib/filesApi.ts && ! grep -niE "agenthub tui|TUI Mode" README.md && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "internal/tui" README.md internal/daemon/client.go internal/attach/attach.go frontend/src/lib/filesApi.ts` returns nothing
    - `grep -niE "agenthub tui" README.md` returns nothing
    - `grep -ni "TUI Mode" README.md` returns nothing
    - `go build ./...` exits 0 (comment-only Go edits did not break compilation)
    - `frontend/src/components/DaemonManagerPanel.tsx` is unmodified by this task
  </acceptance_criteria>
  <done>README presents only GUI/CLI/web as current surfaces; the three comment-only references to `internal/tui` are removed; `go build ./...` stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — Go race suite, frontend suite, and agenthub tui non-zero exit</name>
  <read_first>
    - .planning/phases/136-tui-removal/136-RESEARCH.md (section 6 verification approach)
    - .planning/phases/136-tui-removal/136-VALIDATION.md (per-task verification map + sampling rate)
  </read_first>
  <files>agenthub (built binary; verification only — no source files modified)</files>
  <action>
    Run the full phase verification matrix and record the results in the SUMMARY:
    1. Build the binary and run the TUI smoke: `go build -o agenthub . && ./agenthub tui; echo $?` — confirm a non-zero exit code and that stderr reads `agenthub: unknown command "tui"`.
    2. Run the surviving Go suite under race: `go test -race -short ./...` — must pass with no "tui" build-failed packages.
    3. Mirror the CI Windows path: `go test -race -short ./internal/...` — must pass.
    4. Run the frontend suite: `cd frontend && pnpm test` — must pass (no TUI Go dependency; proves no regression).
    5. Confirm `./agenthub --help` (or bare `./agenthub`) usage text lists no `tui` subcommand.
    Do not write or migrate any test files — TEST-06 is deletion, not addition. If any suite is red, STOP and report rather than papering over it.
  </action>
  <verify>
    <automated>go build -o agenthub . && { ./agenthub tui; [ $? -ne 0 ]; } && go test -race -short ./... && go test -race -short ./internal/... && ( cd frontend && pnpm test ) && ! ./agenthub --help 2>&1 | grep -qw tui</automated>
  </verify>
  <acceptance_criteria>
    - `./agenthub tui; echo $?` prints a non-zero exit code
    - `./agenthub tui` writes `agenthub: unknown command "tui"` to stderr
    - `go test -race -short ./...` exits 0 with no `[build failed]` packages
    - `go test -race -short ./internal/...` exits 0 (Windows CI parity path)
    - `cd frontend && pnpm test` exits 0
    - `./agenthub --help` output contains no `tui` subcommand line
  </acceptance_criteria>
  <done>The Go race suite, the Go internal suite, and the frontend suite are all green; `agenthub tui` exits non-zero with the unknown-command message; help text lists no TUI subcommand. Results recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Pure documentation/comment cleanup plus a read-only verification run. No trust boundary is introduced, modified, or crossed. The TUI cap-leak surface was already removed in plan 01.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-02 | Information Disclosure | README / source comments | n/a | Editing prose to remove references to a deleted surface introduces no runtime path. No data crosses any boundary. |

**ASVS:** Not applicable. No new authentication, session, crypto, or input-validation paths. No packages installed (no Package Legitimacy Gate). Security impact is neutral-to-positive (docs no longer advertise a removed surface).
</threat_model>

<verification>
- `grep -rn "internal/tui" README.md internal/daemon/client.go internal/attach/attach.go frontend/src/lib/filesApi.ts` returns nothing
- `grep -niE "agenthub tui|TUI Mode" README.md` returns nothing
- `./agenthub tui` exits non-zero with `agenthub: unknown command "tui"`
- `go test -race -short ./...` and `go test -race -short ./internal/...` green
- `cd frontend && pnpm test` green
- `./agenthub --help` lists no `tui` subcommand
</verification>

<success_criteria>
- README and the three comment-only references describe only GUI/CLI/web
- The full Go race suite, the internal suite, and the frontend suite pass
- `agenthub tui` exits non-zero with the unknown-command message
</success_criteria>

<output>
Create `.planning/phases/136-tui-removal/136-02-SUMMARY.md` when done
</output>
