---
phase: 159-web-share-chat-parity-route-shared-session-links-to-the-chat
plan: 04
type: execute
wave: 4
gap_closure: true
depends_on: ["159-02"]
files_modified:
  - frontend/src/components/TabBar.tsx
  - frontend/src/App.tsx
  - frontend/e2e/web-share-scope.spec.ts
  - TESTING.md
autonomous: true
requirements: [WEBCHAT-05]
tags: [frontend, web-share, scope, tabbar, react]

must_haves:
  truths:
    - "In web mode the TabBar shows no session-menu chevron ('Session menu' button absent) and no right-click/double-click rename"
    - "A web-share guest cannot open the rename input on the session tab"
    - "The × close button still works in web mode; desktop mode TabBar is unchanged"
  artifacts:
    - "frontend/src/components/TabBar.tsx — new webMode prop gates double-click rename, onContextMenu, and the chevron button"
    - "frontend/src/App.tsx — passes webMode={mode === 'web'} to TabBar"
    - "frontend/e2e/web-share-scope.spec.ts — +1 test (no chevron/menu, no rename-on-double-click)"
    - "TESTING.md — WEBCHAT-05 traceability row + note"
  key_links:
    - "RenameSession / SaveTerminalSession are Wails RPCs (Call via runtime bridge); window.go/window.runtime are undefined in a browser, so they fail silently and only relabel the local tab — verified live"
---

<objective>
Close the third scope gap from Phase 159 live UAT: the tab dropdown menu let a web-share
guest rename the session tab (and offered Save Terminal As). Both are Wails RPCs with no
bridge in a browser — verified live that `window.go`/`window.runtime` are undefined and no
HTTP rename path exists, so rename never reached the host (it failed silently and relabeled
only the local browser tab). Remove the affordance for clarity: suppress the tab
session-menu and double-click/right-click rename in web mode; keep the × close button.
</objective>

<tasks>

## Task 1 — TDD: web guest cannot rename the tab
RED: extend `web-share-scope.spec.ts` — in web mode assert no `[data-testid="tab-chevron"]`
/ "Session menu" button, and double-clicking the tab name does not open `.tab__rename-input`.
Fails on current code (chevron + rename present).
GREEN: add a `webMode` prop to `TabBar`; gate the double-click handler, the `onContextMenu`
handler, and the chevron button on `!webMode`. Pass `webMode={mode === 'web'}` from App.
Verify: new test passes chromium/firefox/webkit; tsc clean; chat-parity green.

## Task 2 — Register WEBCHAT-05 in TESTING.md (traceability + note).

</tasks>
