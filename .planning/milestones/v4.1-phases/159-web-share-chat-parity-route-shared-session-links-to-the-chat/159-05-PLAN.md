---
phase: 159-web-share-chat-parity-route-shared-session-links-to-the-chat
plan: 05
type: execute
wave: 5
gap_closure: true
depends_on: ["159-02"]
files_modified:
  - frontend/src/style.css
  - frontend/src/components/__tests__/style.hub.test.ts
  - TESTING.md
autonomous: true
requirements: [WEBCHAT-06]
tags: [frontend, chat, css, layout]

must_haves:
  truths:
    - "A long chat author name truncates with an ellipsis on one line instead of wrapping char-by-char"
    - "The long raw nodekey gives up space first (truncates) so the alias stays readable"
    - "Desktop and web chat both benefit (CSS is shared)"
  artifacts:
    - "frontend/src/style.css — .chat-msg__header min-width:0; .chat-msg__alias + .chat-msg__tailnet-id nowrap/overflow/ellipsis; tailnet-id flex-shrink:1000"
    - "frontend/src/components/__tests__/style.hub.test.ts — +3 source-gate tests"
    - "TESTING.md — WEBCHAT-06 traceability row + note"
---

<objective>
Cosmetic chat-rendering gap from 159 live UAT: in the narrow chat panel a long author
name (tailnet hostname fallback before an alias is set) wrapped char-by-char into a tall
stack and the raw nodekey overflowed. Truncate both with an ellipsis on one line (CSS-only).
</objective>

<tasks>
## Task 1 — CSS truncation + source-gate test
Add nowrap/overflow/ellipsis + min-width:0 to .chat-msg__alias and .chat-msg__tailnet-id
(tailnet-id flex-shrink:1000 so the nodekey truncates first); min-width:0 on .chat-msg__header.
Extend style.hub.test.ts with source-gate assertions. Verify vitest + visual (long name → one line).

## Task 2 — Register WEBCHAT-06 in TESTING.md.
</tasks>
