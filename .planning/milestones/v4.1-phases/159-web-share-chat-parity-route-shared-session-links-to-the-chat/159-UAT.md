---
status: passed
phase: 159-web-share-chat-parity-route-shared-session-links-to-the-chat
source: [159-VERIFICATION.md]
started: 2026-06-27T00:00:00Z
updated: 2026-06-29T00:57:36Z
---

> **Reconciliation (2026-06-29):** Tests 1, 2, 4 passed LIVE. Test 3's gap (RO guest
> cannot post chat) was carved out into **Phase 163** ("Read-Only Guest Chat Posting —
> D-06 reconciliation") and shipped+verified there: `hub.go:652` removed the
> `ErrChatReadOnly` gate (RO can now post), `@session` inject + PTY stay RO-gated
> (`hub.go:585`), 163 VERIFICATION `passed` (10/10), 163 SECURITY `threats_open: 0`
> (SEC-RO-01), ROCHAT-01/02 live e2e chromium-green on `/app/` — the exact surface the
> 159 redirect lands on. Test 3 is therefore **resolved-by-163**, not an open 159 gap.
> No fix plans generated. Verified by user decision (verify-work 159, 2026-06-29).

## Live-Test Results (2026-06-27, automated via playwright-fixture + dev-browser)

Driven against a **fresh-built** server (`go build -tags=playwrightfixture,wailsassets
./cmd/playwright-fixture`) that includes the Phase 159 redirect, with a real
headless Chromium navigating the **actually-shared** `/sessions/{id}?cap=` link.

| Test | Result | Evidence |
|------|--------|----------|
| 1 — Redirect to chat-capable SPA | ✅ PASS | curl + browser: `/sessions/{id}?cap=` → `302` → lands on `/app/?session=&cap=`; SPA boots; ChatPanel + chat toggle + composer + seeded history + `@local` mention + `→ injected into terminal` indicator all render (3 screenshots). RO cap redirects identically; **no-cap → 401, no Location** (redirect never precedes cap validation). CSP + `Cache-Control: no-store` preserved on the 302. |
| 2 — Chat round-trip | ✅ PASS (LIVE) | Bidirectional over the real relay/Tailscale TLS: desktop "local"→web (rendered in dev-browser guest) AND web (distinct nodekey identity)→desktop, both persisted to chats/<id>.jsonl. Two distinct author IDs, no merge. |
| 3 — RO guest chat participation | ❌ ISSUE (gap) | RO redirects ✅, reads thread ✅, PTY input rejected ✅ — but RO CANNOT post chat (server `HandleChatSend`→`ErrChatReadOnly` SEC-01 + frontend suppression). User decision 2026-06-27: RO SHOULD post chat (only `@session` inject stays gated). See Gaps + Test 3. Reverses SEC-01 → fix + security re-review. |
| 4 — Scale parity on host resize | ✅ PASS (LIVE) | Host window enlarged → host grid grew (guest rows 44→59, host authority); guest `.xterm` CSS-downscaled to 0.696 (<1.0, downscale-only) through the redirect path. VIEW-04/05 preserved. |

**Bottom line:** The phase goal — a remote guest opening the *actually-shared* link
reaches a working chat surface — is **confirmed end-to-end in a real browser**
(redirect + SPA + ChatPanel + guest send). The residual items (true bidirectional
round-trip across two real peers, live RO chat participation, resize scale parity)
are inherently un-automatable here and remain for a two-machine live check.

## Current Test

number: 1
name: Redirect to chat-capable SPA on the actually-shared link
status: passed (automated browser, 2026-06-27)
expected: |
  Open the RW share URL `/sessions/{id}?cap=TOKEN` (a real live-daemon share
  link, NOT an `/app/` URL) in a browser. The browser is redirected to
  `/app/?session=...&cap=...`; WebShareSessionView renders (NOT the vanilla-JS
  terminal.html viewer); ChatPanel, the chat toggle (`hub-modal__chat-toggle`),
  the unread/mention badge, and the presence roster are present in the DOM.
awaiting: residual two-peer live check (Tests 2-full, 3-full, 4)

## Tests

### 1. Redirect to chat-capable SPA (M-31 Steps 1-2)
result: pass
status: passed (LIVE daemon 2026-06-27, fresh 20:29 build over real Tailscale TLS) — RW share link 302→/app/?session=&cap= (id+cap preserved), Cache-Control:no-store + CSP preserved, no-cap→401 no Location; dev-browser guest followed redirect, SPA rendered ChatPanel+toggle+composer+presence(2 peers)+terminal. Supersedes the earlier playwright-fixture pass.
expected: |
  Start a local daemon session; enable web-share; copy the RW share URL of the
  form `/sessions/{id}?cap=TOKEN` (NOT an `/app/` URL). Open it in a browser.
  Browser is redirected to `/app/?session=...&cap=...`; WebShareSessionView
  renders (not terminal.html); ChatPanel, chat toggle, unread/mention badge,
  and presence roster are present.
why_human: |
  web-share WebSocket blocks automated input (project memory); requires a live
  daemon + real browser; cross-surface parity is release-blocking.

### 2. Chat round-trip (M-31 Steps 3-4)
result: pass
status: passed (LIVE bidirectional, 2026-06-27, fresh 20:29 daemon over Tailscale TLS). desktop→web: owner "local"/Mac.attlocal.net sent "desktop->web hello 159" → persisted to chats/<id>.jsonl AND rendered in real dev-browser guest (author+timestamp+day-separator). web→desktop: real-browser guest (distinct identity nodekey:456d9361…/kens-personal-macbook-air) sent "web->desktop reply 159" → persisted server-side. Two distinct author IDs, no identity merge (re-confirms Phase 152 identity/presence on the shared surface). Note: dev-browser synthetic keystrokes land in the PTY not the chat composer, so the web→desktop send used a real browser.
expected: |
  With the redirected SPA guest open, send a chat message from the desktop/Hub
  side and confirm it arrives in the browser; send a reply from the browser back
  to the desktop. Messages flow bidirectionally (desktop→web and web→desktop).
why_human: |
  Requires a live relay session with two active WebSocket peers; cannot be
  exercised without a real PTY and real peer.

### 3. RO guest chat participation (M-31 Step 6, D-06)
result: pass
resolved_by: Phase 163 (Read-Only Guest Chat Posting — D-06 reconciliation)
status: resolved-by-163 — at 159 UAT time RO COULD NOT post chat (relay/hub.go HandleChatSend returned ErrChatReadOnly when sub.ReadOnly, SEC-01/T-154-03; frontend ChatPanel.tsx suppressed Send). This gap was carved out into Phase 163 per the user's D-06 decision (2026-06-27) and SHIPPED+VERIFIED there: hub.go:652 removed the ErrChatReadOnly gate so RO guests can now post chat, while @session inject (hub.go:585 ErrReadOnly) + PTY input discard (relay/server.go, webserver/server.go) stay RO-gated. 163 VERIFICATION status=passed (10/10 must-haves); 163 SECURITY status=verified, threats_open=0 (SEC-RO-01); ROCHAT-01/02 live e2e chromium-green on /app/ — the surface the 159 redirect (Test 1) lands on. The redirect + RO-posting therefore compose end-to-end. severity: major (historical; resolved)
expected: |
  Repeat the round-trip with a RO share URL (`/sessions/{id}?cap=RO_TOKEN`). The
  RO guest receives and participates in chat (D-06 — RO guests remain full chat
  participants); PTY input from the RO guest is gated but chat is not.
why_human: |
  Live daemon required; RO cap behavior requires end-to-end relay verification
  through the redirect path.

### 4. Phase 157-04 scale parity through the redirect (M-31 Step 5)
result: pass
status: passed (LIVE 2026-06-27, fresh 20:29 daemon over Tailscale TLS). Host (desktop) window enlarged → host grid grew (guest rendered rows 44→59, host authority). Guest .xterm CSS-downscaled to matrix(0.696,…) (<1.0, downscale-only, never upscaled past 1.0) to fit its viewport — VIEW-04/05 preserved through the /sessions→/app redirect path. Confirmed via dev-browser measurement + screenshot.
expected: |
  With the redirected SPA guest open, resize the host PTY (e.g. resize the
  desktop session pane). The browser terminal re-scales to honor the new
  host-authority grid — downscales to fit, never upscales past 1.0, stays
  readable and not clipped (Phase 157 VIEW-01/05 preserved through the redirect).
why_human: |
  Requires a real rendered xterm with live host-authority resize frames;
  structural presence of Phase 157 code cannot prove resize frames propagate
  through the redirect-to-SPA path.

## Gaps

- truth: "RO web guests are full chat participants — they can post chat messages; only PTY/@session inject is gated (D-06, user-reaffirmed 2026-06-27)."
  status: resolved   # resolved-by-Phase-163 (2026-06-28); hub.go:652 ErrChatReadOnly gate removed; 163 VERIFICATION passed, SECURITY threats_open:0 (SEC-RO-01), ROCHAT-01/02 e2e green
  reason: "RO chat posting is gated server-side AND in the UI. Daemon: relay/hub.go HandleChatSend returns ErrChatReadOnly when sub.ReadOnly (SEC-01/T-154-03); the webserver read-pump path routes RO MsgChatSend to the same gate. Frontend: ChatPanel.tsx isReadOnly disables Send, short-circuits send() (l.556-560) and the inject gesture (l.571,582), renders the chat-composer__readonly-label (l.905-910). Live-verified: RO cap reaches /app/, reads thread, PTY input rejected, but cannot post chat."
  severity: major
  test: 3
  security_review_required: true   # reverses SEC-01; PITFALLS.md flags RO-posting-chat as a risk — must re-audit
  scope_note: "Spans Phase 154 (server chat gate, both relay + webserver read-pumps) and Phase 155 (frontend SC-3 suppression + its tests). MUST keep PTY input + @session inject RO-gated; only loosen MsgChatSend. Reconcile D-06 vs SEC-01 in the decision docs."
  artifacts:
    - internal/relay/hub.go (HandleChatSend ErrChatReadOnly gate ~l.648)
    - internal/relay/server.go (read-pump MsgChatSend case ~l.374)
    - internal/webserver/server.go (web read-pump MsgChatSend path)
    - frontend/src/components/Hub/ChatPanel.tsx (isReadOnly send/inject/Send-button/label, l.300-361,556-582,893-910)
    - .planning/phases/155*/* (SC-3 tests asserting RO-cannot-post)
    - .planning/research/PITFALLS.md (l.21 — RO read chat / cannot post: revisit)
  missing: []
  confirmed_boundary: "User 2026-06-27: 'RO users can participate in chat but cannot @ the session to inject a prompt.' → loosen MsgChatSend RO gate ONLY; HandleInject (ErrReadOnly) + MsgInput PTY gate stay. Frontend: enable chat composer Send for RO, KEEP @session inject gesture gated for RO, remove/reword the 'Read only' chat-composer label."
