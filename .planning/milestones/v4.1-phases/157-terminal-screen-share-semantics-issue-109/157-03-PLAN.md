---
phase: 157-terminal-screen-share-semantics-issue-109
plan: 03
type: execute
wave: 2
depends_on: ["157-01"]
files_modified:
  - web/assets/terminal.js
  - web/assets/terminal.css
autonomous: true
requirements: [VIEW-04, VIEW-05]
must_haves:
  truths:
    - "The web guest viewer honors a server-pushed MsgResize (0x02) by calling term.resize(cols,rows) and stops self-sizing (VIEW-04, success criterion 4)."
    - "The web guest CSS-downscales the host grid to fit: s = min(containerW/gridW, containerH/gridH), capped at s <= 1.0 (downscale-only), recomputed on window resize AND on every 0x02 (VIEW-05, success criterion 4)."
    - "The host view is never disturbed (web is always a guest; the host is desktop/CLI and never scales)."
  artifacts:
    - "web/assets/terminal.js: 0x02 dispatch (term.resize + recomputeScale), MsgResize const, recomputeScale() using cell-metric math, FitAddon no longer auto-fits, no client-driven 0x11 send"
    - "web/assets/terminal.css: #terminal overflow:hidden + .xterm transform-origin:top left for the scale transform"
  key_links:
    - "recomputeScale measures gridW = term.cols * cellW (layout size from term._core._renderService.dimensions.css.cell), NOT getBoundingClientRect (which would oscillate post-transform)."
    - "On a 0x02 frame, term.resize is called BEFORE recomputeScale (scale depends on the new term.cols/rows)."
  prohibitions:
    - "The web guest MUST NOT call fitAddon.fit() to re-derive grid size from pixels (that defeats host-authority)."
    - "The web guest MUST NOT send a client-driven 0x11 resize on open or on term.onResize (it never drives the PTY)."
    - "The scale factor MUST NOT exceed 1.0 (no upscaling)."
---

<objective>
Make the web terminal viewer (`web/assets/terminal.js` + `terminal.css`) a conforming guest: honor
the server-pushed 0x02 resize frame (`term.resize`), stop self-sizing via FitAddon, stop sending
client-driven 0x11 resize, and CSS-downscale the host grid to fit the viewport (capped at s <= 1.0,
recomputed on window resize and every 0x02). Web is ALWAYS a guest (the web-share surface never hosts
a PTY), so this behavior is unconditional here.

Purpose: VIEW-04/05 on the web surface — the guest renders the host's exact grid (byte-for-byte
identical wrap points → no garble) and reconciles its viewport by scaling pixels, not re-gridding.
Output: Modified `terminal.js` (dispatch + recomputeScale + disabled self-fit) and `terminal.css`
(transform container rules).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/157-terminal-screen-share-semantics-issue-109/157-RESEARCH.md
@web/assets/terminal.js
@web/assets/terminal.css
</context>

<artifacts_this_phase_produces>
- `web/assets/terminal.js`: new `MsgResize = 0x02` const; 0x02 branch in the `ws.onmessage` dispatch; new `recomputeScale()` function; `window.addEventListener('resize', recomputeScale)`.
- `web/assets/terminal.css`: `#terminal { overflow: hidden }` + `#terminal .xterm { transform-origin: top left }`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Honor 0x02 resize + recomputeScale in terminal.js; disable self-sizing</name>
  <files>web/assets/terminal.js</files>
  <read_first>
web/assets/terminal.js:4 (MsgResize2 const — add MsgResize 0x02 beside it),
web/assets/terminal.js:168-171 (FitAddon init + fit()),
web/assets/terminal.js:182-187 (cell-metric read pattern via term._core._renderService.dimensions.css.cell),
web/assets/terminal.js:990-1007 (ws.onopen initial 0x11 send + ws.onmessage dispatch),
web/assets/terminal.js:1032-1042 (term.onResize → 0x11 send + window resize → fitAddon.fit()),
RESEARCH Change-Layers 4 + 5 (exact dispatch + recomputeScale snippets).
  </read_first>
  <action>
Per RESEARCH Change-Layers 4 + 5:
(1) Add a `MsgResize = 0x02` const beside `MsgResize2 = 0x11` (terminal.js:4).
(2) In the `ws.onmessage` dispatch (terminal.js:995-1007), add a branch: when `msgType === MsgResize`
and `payload.length >= 4`, decode big-endian `cols=(payload[0]<<8)|payload[1]`,
`rows=(payload[2]<<8)|payload[3]`, clamp each to >= 1 (xterm rejects resize(0,0)), call
`term.resize(cols, rows)` THEN `recomputeScale()` (order matters — scale reads the new term.cols/rows).
(3) Add `recomputeScale()`: read `cellW/cellH` from `term._core._renderService.dimensions.css.cell`
(same private API already used at terminal.js:182-187); guard missing dims/zero cells; compute
`gridW = term.cols*cellW`, `gridH = term.rows*cellH`, container from
`document.getElementById('terminal').clientWidth/clientHeight`; `s = Math.min(cw/gridW, ch/gridH)`;
if `s > 1` set `s = 1` (VIEW-05 cap); apply `term.element.style.transform = 'scale(' + s + ')'`.
(4) Stop self-sizing: remove the `ws.onopen` initial 0x11 send (terminal.js:990-992) and the
`term.onResize → ws.send(0x11)` (terminal.js:1032-1037); replace the
`window.addEventListener('resize', () => fitAddon.fit())` (terminal.js:1039-1042) with
`window.addEventListener('resize', recomputeScale)`. Keep the FitAddon LOADED (it may be referenced
for cell metrics) but stop calling its fit() at terminal.js:168-171 (initial self-fit) — the grid is
now set only by 0x02. Use `transform-origin: top left` (set in CSS, Task 2).
Confirm via grep that no other `fitAddon.fit()` call remains beyond the two known sites.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && node --check web/assets/terminal.js && grep -q 'function recomputeScale' web/assets/terminal.js && grep -q 'term.resize' web/assets/terminal.js && ! grep -qE 'fitAddon\.fit\(\)' web/assets/terminal.js</automated>
  </verify>
  <acceptance_criteria>
`node --check` passes. `recomputeScale` exists and is registered on window resize. The 0x02 dispatch
calls `term.resize` then `recomputeScale`. The scale is capped at 1. No `fitAddon.fit()` call remains
(grep count 0). No client-driven 0x11 send remains on open or term.onResize.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: terminal.css transform container rules</name>
  <files>web/assets/terminal.css</files>
  <read_first>
web/assets/terminal.css:54 (#terminal container rules),
RESEARCH Change-Layer 5 (container model).
  </read_first>
  <action>
Per RESEARCH Change-Layer 5: add `overflow: hidden;` to the `#terminal` rule (terminal.css:54) so
sub-pixel overhang from the scaled grid is clipped. Add a `#terminal .xterm { transform-origin: top
left; }` rule so the grid pins to the top-left corner and scales toward bottom-right (no negative
offset / clipping). The scale value itself is applied imperatively from JS (Task 1); do NOT hardcode
a transform in CSS. Keep all existing `#terminal` flex/min-height/position rules intact.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && grep -c 'transform-origin' web/assets/terminal.css && grep -c 'overflow: hidden' web/assets/terminal.css</automated>
  </verify>
  <acceptance_criteria>
`#terminal` clips overflow and `.xterm` has `transform-origin: top left`. No hardcoded scale transform
in CSS. Existing container layout preserved.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → web guest viewer | A 0x02 frame (cols/rows) crosses into client code that resizes the local xterm |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-157-03 | Tampering | web viewer 0x02 dispatch (`terminal.js`) | medium | mitigate | Guard `payload.length >= 4` and clamp cols/rows to >= 1 before `term.resize` (resize(0,0) is invalid in xterm); prevents a crafted/zero-dim frame from breaking the renderer. |
| T-157-06 | (usability, not security) | pointer hit-test under transform | low | accept | xterm mouse-selection may be offset on a scaled guest (documented limitation A3); not a VIEW-01..05 blocker — note for UAT. |
</threat_model>

<verification>
- `node --check web/assets/terminal.js` passes (no syntax regression in the vendored web asset).
- Structural grep gates (recomputeScale present, term.resize on 0x02, scale capped, no fitAddon.fit, transform-origin in CSS) pass.
- Behavioral proof (host + smaller guest, no garble) is the manual Category P UAT item added in Plan 05.
</verification>

<success_criteria>
ROADMAP criterion 4 (web half): web guest honors 0x02 + CSS-downscales capped at 1.0; host never disturbed.
</success_criteria>

<output>
Create `.planning/phases/157-terminal-screen-share-semantics-issue-109/157-03-SUMMARY.md` when done.
Note that web viewer behavior is structurally gated + manual-UAT verified (terminal.js is the vendored
web asset, outside vitest scope) so Plan 05 records the Category P manual item.
</output>
