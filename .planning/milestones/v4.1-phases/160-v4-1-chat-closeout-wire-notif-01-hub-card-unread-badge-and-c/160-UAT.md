---
status: complete
phase: 160-v4-1-chat-closeout-wire-notif-01-hub-card-unread-badge-and-c
source: [160-01-SUMMARY.md, 160-02-SUMMARY.md, 160-03-SUMMARY.md, 160-04-SUMMARY.md, 160-05-SUMMARY.md]
started: 2026-06-28T14:23:21Z
updated: 2026-06-28T14:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Hub card unread badge on backgrounded session
expected: With session A's modal closed, an incoming chat message makes session A's Hub card show a ChatBadge with count > 0, incrementing per message.
result: pass
source: automated
verified_by: |
  Agent-verified via the prop-chain test suite (209/209 pass):
  - useChatUnreadListeners.test.tsx: "opens one RelayClient per non-modal session",
    "excludes openModalSessionId from the subscription set",
    "calls onUnreadChange with correct (sessionId, count, hasMention) when onChat fires"
  - HubPanel.test.tsx: "NOTIF-01a: badge appears on card when unread is reported for a backgrounded session"
  - SessionCardGrid.test.tsx: "NOTIF-01: session present in unreadBySessionId map shows ChatBadge with count > 0"
  - ChatBadge.tsx source: count>0 renders the count number.
  Residual (human-only): true live daemon→relay-WS→Wails-app render with a real second peer.

### 2. Badge resets when opening the session
expected: Clicking session A's card (opening its chat modal) resets that card's badge to 0 immediately — no lingering or flickering count once the modal is open.
result: pass
source: automated
verified_by: |
  HubPanel.test.tsx: "NOTIF-01b: opening a session modal resets that session badge to 0".
  HubPanel.handleCardClick deletes the session's unreadMap entry (new Map(prev) + m.delete)
  before setModalState, batched in one React 18 commit (no flicker). ChatBadge renders null at count===0.

### 3. @mention indicator on a backgrounded session
expected: While session A's modal is closed, a chat message containing an @mention sets the mention state on session A's badge (the badge renders its @-mention styling, not just a plain count).
result: pass
source: automated
verified_by: |
  - ChatBadge.test.tsx: "renders @ glyph (NOT the count) when hasMention=true — glyph is the non-color signal (D-10)"
  - SessionCardGrid.test.tsx: "NOTIF-01: hasMention=true renders mention-style badge with @ glyph"
  - ChatBadge.tsx source: hasMention renders the "@" glyph (shape-based, colorblind-safe per D-10);
    aria-label appends "including a mention". Color is reinforcement only.
  Residual (human-only): live @mention frame over a real relay WS to the running app.

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — all NOTIF-01 user-observable behaviors verified via automated prop-chain + source inspection]

## Notes

Phase 160 is a v4.1 chat-closeout phase. The only user-facing feature is NOTIF-01
(the Hub session-card unread badge), covered by the 3 tests above. The remaining
phase deliverables are internal/non-observable tech-debt and were already proven in
160-VERIFICATION.md (15/15) by automated tests, re-confirmed here:
- IN-02 (control-only inject → zero PTY writes): go TestInject_ControlOnlyInput PASS
- IN-04 (SanitizeChatContent doc comment correction): comment-only, no behavior change
- WR-01/WR-03 (install.sh grep -F + root mkdir -p): tests/install-sh.test.sh 13/13 PASS
- WR-02 (TESTING.md build-script Run Command runs both scripts): doc change
These are not human UAT checkpoints (no observable runtime behavior for a manual tester).
