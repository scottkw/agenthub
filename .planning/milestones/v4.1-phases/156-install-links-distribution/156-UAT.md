---
status: passed
phase: 156-install-links-distribution
source: [156-VERIFICATION.md]
started: 2026-06-27T12:02:20Z
updated: 2026-06-28T23:30:00Z
---

## Current Test

[testing complete — 2/2 pass]

## Tests

### 1. M-25 — Linux install on a clean amd64 box (SC-1 / INSTALL-01)
expected: |
  Run `curl -fsSL https://raw.githubusercontent.com/scottkw/agenthub/main/scripts/install.sh | sh`
  on a real Linux box or an `ubuntu:22.04` Docker container.
  - Output contains "SHA256 verified."
  - Binary installed to a standard path (`/usr/local/bin` for root, `~/.local/bin` otherwise)
  - `agenthub --help` exits 0
  - Idempotent re-run succeeds
  - PATH warning appears for non-root install
  Why human: install.sh passes shellcheck, `sh -n`, and all static-pattern gates — but
  these prove code structure, not that the live SHA256 download → compare → install
  state transition actually succeeds against a real published release.
result: pass
source: user-decision
note: "Marked pass by user decision (2026-06-28) on the strength of the static gates — install.sh passes shellcheck, `sh -n`, and all static-pattern gates, and the SHA256 verify→install logic was unit/structure-verified. CAVEAT (recorded honestly): the LIVE download path was NOT exercised at marking time — `origin/main` is still 371 commits behind local HEAD, so `https://raw.githubusercontent.com/scottkw/agenthub/main/scripts/install.sh` still returned HTTP 404. The end-to-end `curl|sh` against a published release will only succeed once `main` is pushed to origin; re-run a live install then to confirm against the real GitHub-served script."

### 2. M-26 Step 3 — WINGET_TOKEN provisioned (SC-3 gate / INSTALL-03)
expected: |
  Run `gh secret list | grep WINGET_TOKEN` in the `scottkw/agenthub` repo context.
  - A row is returned for `WINGET_TOKEN` (classic PAT, `public_repo` scope only)
  Note: `gh secret list` returned empty during automated verification — could not confirm
  presence or absence. M-26 steps 4–7 (trigger live submission, shepherd the catalog PR,
  post-acceptance reset) are explicitly NON-blockers; only token provisioning is the gate item.
result: pass
source: automated
note: "Confirmed via `gh secret list --repo scottkw/agenthub` (authed as scottkw): WINGET_TOKEN present, created 2026-04-05T21:57:28Z. Earlier empty result was due to missing --repo/auth context, not an absent secret."

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
