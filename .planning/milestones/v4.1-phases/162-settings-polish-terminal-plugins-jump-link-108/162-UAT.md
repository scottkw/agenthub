---
status: complete
phase: 162-settings-polish-terminal-plugins-jump-link-108
source: [162-VERIFICATION.md]
started: 2026-06-28T18:10:00Z
updated: 2026-06-28T18:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Jump link click-to-scroll (GUI + web-share surface)
expected: |
  Sticky jump bar shows "Terminal Plugins" as the last (7th) link. Clicking it scrolls the
  viewport to the "Terminal Plugins" section header; the header is fully visible below the
  sticky jump bar (not hidden behind it); the section renders last in the Settings body.
result: pass
source: automated
evidence: |
  Driven via dev-browser (headless Chromium) against an isolated vite harness mounting the
  real SettingsJumpBar + SettingsSearch components with the real src/style.css. Jump bar
  rendered exactly 7 links in order: Behavior, Session Behavior, Appearance, Web Server,
  Security, Paths, Terminal Plugins (last, target=settings-plugins). Clicking "Terminal
  Plugins" scrolled the body from scrollTop 0 → 1682 (== maxScroll). The h3 reads "Terminal
  Plugins", is the last <h3> in the body (isLastSection=true), is in-viewport, and sits BELOW
  the sticky bar (h3 top 234 ≥ bar bottom 128; bar position=sticky; scroll-margin-top=80px) —
  not hidden behind it.

### 2. Settings search for "terminal plugins"
expected: |
  Typing "terminal" (or "terminal plugins") in the Settings search box surfaces a result
  labeled "Terminal Plugins" pointing to the settings-plugins anchor; clicking it scrolls
  to the section correctly.
result: pass
source: automated
evidence: |
  Typing "terminal" surfaced ["Terminal Plugins"→settings-plugins, "Terminal Theme"→
  settings-appearance]. Typing the full phrase "terminal plugins" surfaced exactly one
  result: "Terminal Plugins". Clicking the "Terminal Plugins" result scrolled the body to
  scrollTop 1682 with the section in-viewport and location.hash = "#settings-plugins".

### 3. Cross-surface parity on web-share
expected: |
  Open Settings in the web-share view; the jump bar shows the same 7 links in the same
  order with "Terminal Plugins" last. Clicking it scrolls to the section on the web surface.
result: pass
source: automated
evidence: |
  Parity is code-level guaranteed AND runtime-confirmed. SettingsJumpBar and SettingsSearch
  have a single consumer (SettingsTab.tsx), rendered by the single App.tsx SPA — there is no
  web-share-specific settings/jump-bar duplicate (grep confirmed SETTINGS_JUMP_LINKS is
  referenced only by SettingsJumpBar + SettingsSearch). The web-share surface serves the same
  React bundle to a plain browser; the dev-browser run above executed in real headless
  Chromium (the web-share rendering engine, not the Wails webview), confirming the 7-link
  order with "Terminal Plugins" last and correct click-to-scroll on the browser surface.

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — all tests passed]
