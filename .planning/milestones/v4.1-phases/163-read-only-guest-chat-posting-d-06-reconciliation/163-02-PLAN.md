---
phase: 163-read-only-guest-chat-posting-d-06-reconciliation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Hub/ChatPanel.tsx
  - frontend/src/components/Hub/ChatPanel.test.tsx
  - frontend/e2e/chat-parity.spec.ts
autonomous: true
requirements: [ROCHAT-01, ROCHAT-02]
must_haves:
  truths:
    - "When isReadOnly is true, the chat composer Send button is enabled (not disabled, no aria-disabled, no dimmed/not-allowed styling) and pressing Enter or clicking Send calls sendChat."
    - "When isReadOnly is true, the @session press-and-hold inject gesture still short-circuits — sendSessionInject is never called from an RO client."
    - "No blanket 'Read only' composer label is shown; any retained RO affordance copy must not say chat is read-only."
  artifacts:
    - frontend/src/components/Hub/ChatPanel.tsx
    - frontend/src/components/Hub/ChatPanel.test.tsx
    - frontend/e2e/chat-parity.spec.ts
  key_links:
    - "ChatPanel is the single shared component — the same edit lands chat-send for the GUI tab, the Hub interactive modal, and the web-share guest (via the Phase 159 redirect)."
    - "isReadOnly state is retained because the inject gesture (handleInjectPointerDown) still depends on it; only the chat-send/label uses are removed."
  prohibitions:
    - "RO @session inject performs zero PTY writes (handleInjectPointerDown returns early when isReadOnly; sendSessionInject not called)."
---

<objective>
Enable the shared ChatPanel composer Send for read-only guests and remove the blanket "Read only" label, while keeping the `@session` press-and-hold inject gesture RO-gated. This is the frontend half of the D-06 reconciliation; because ChatPanel is un-forked, the change applies across the GUI tab, Hub modal, and web-share guest simultaneously.

Purpose: a remote RO web guest who reaches chat via the Phase 159 redirect can now type and post messages; the inject (PTY-writing) affordance stays blocked.
Output: composer enabled for RO; inject gesture still gated; vitest + Playwright RO assertions flipped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/components/Hub/ChatPanel.tsx
@frontend/src/components/Hub/ChatPanel.test.tsx
@frontend/e2e/chat-parity.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enable RO chat-send + remove the Read-only label in shared ChatPanel per ROCHAT-01</name>
  <files>frontend/src/components/Hub/ChatPanel.tsx</files>
  <read_first>frontend/src/components/Hub/ChatPanel.tsx — Send button JSX (~l.1028-1044), readonly-label JSX (~l.1046-1054), handleSend (~l.614-622), Enter handler in handleTextareaKeyDown (~l.598-612), handleInjectPointerDown (~l.665-680, KEEP gated), isReadOnly state (~l.330), handleAliasCommit (~l.654-663, already the D-06 exception — do not change)</read_first>
  <behavior>
    - Test (RO send enabled): with isReadOnly=true and a non-empty draft, the [data-chat-send] button has no `disabled` and no `aria-disabled=true`; clicking it calls sendChat with the draft.
    - Test (RO Enter sends): with isReadOnly=true and a non-empty draft, pressing Enter (no Shift) calls sendChat and clears the textarea.
    - Test (RO inject still gated): with isReadOnly=true and an `@session …` draft, completing the press-and-hold does NOT call sendSessionInject.
    - Test (no Read-only label): with isReadOnly=true, the composer renders no element with text "Read only".
  </behavior>
  <action>
    In `frontend/src/components/Hub/ChatPanel.tsx`:

    1. Send button (~l.1028-1044): remove `isReadOnly` from the `disabled` expression so it reads `disabled={!draft.trim()}`. Remove the `aria-disabled={isReadOnly}` attribute. Remove the `style={isReadOnly ? { opacity: 0.4, cursor: 'not-allowed' } : undefined}` line. Change `aria-label` from the conditional to the plain string for sending a message (no "read-only access" variant). Keep the `data-chat-send` attribute (Playwright queries it).

    2. `handleSend` (~l.614-615): remove the `if (isReadOnly) return` defense-in-depth early-return so RO clicks post.

    3. Enter handler (~l.601-610): remove the `&& !isReadOnly` condition on the send guard so RO Enter routes to sendChat. Keep the strict separation (Enter ALWAYS routes to sendChat, never inject — Pitfall 7 / D-08).

    4. Remove the entire `chat-composer__readonly-label` block (~l.1046-1054) — the blanket "Read only" copy is now false. Do not replace it with any text that says chat is read-only. (No CSS file change: the label used inline styles only.)

    KEEP UNCHANGED (RO inject still gated — the regression guard):
    - `handleInjectPointerDown` (~l.665-667): the `if (isReadOnly) return` early-return stays.
    - The inject button branch (~l.1012-1027) and its press-and-hold wiring stay.
    - `isReadOnly` state + its /info perms resolution (~l.330, ~l.387-397) stay (the inject gate needs it).
    - `handleAliasCommit` (~l.654-663) stays (already the D-06 alias exception).

    Run the frontend gate: `pnpm --dir frontend exec tsc --noEmit && pnpm --dir frontend test -- --run ChatPanel`.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && pnpm --dir frontend exec tsc --noEmit && grep -c "Read only" frontend/src/components/Hub/ChatPanel.tsx | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` passes.
    - ChatPanel.tsx Send button has no `isReadOnly` in `disabled`, no `aria-disabled`, no opacity/cursor RO style.
    - No "Read only" text remains in ChatPanel.tsx.
    - `handleInjectPointerDown` still returns early when isReadOnly; `data-chat-send` attribute still present.
  </acceptance_criteria>
  <done>RO composer Send enabled across all surfaces via shared ChatPanel; blanket Read-only label removed; inject gesture still RO-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the RO-cannot-send vitest + Playwright assertions; add RO-can-send / inject-gated coverage per ROCHAT-02</name>
  <files>frontend/src/components/Hub/ChatPanel.test.tsx, frontend/e2e/chat-parity.spec.ts</files>
  <read_first>frontend/src/components/Hub/ChatPanel.test.tsx (mock relay client exposes mockSendChat ~l.55 + mockSendSessionInject ~l.56; composer Enter test ~l.631; alias D-06 tests ~l.770-786), frontend/e2e/chat-parity.spec.ts (PARITY-01 SC-3 test ~l.289-331; header legend ~l.15)</read_first>
  <action>
    Vitest — `frontend/src/components/Hub/ChatPanel.test.tsx`:
    - Grep this file for any assertion that the Send button is disabled / aria-disabled when read-only, or that the "Read only" label renders. If present, flip/remove them (the alias D-06 tests at ~l.770-786 are about the alias control, not Send — leave those).
    - Add a positive test: mount with a read-only resolution (capToken with empty /info perms → isReadOnly=true, the existing fail-safe path), type a non-empty draft, click [data-chat-send] (or press Enter), and assert mockSendChat was called with the draft. Assert no element with text "Read only" is present.
    - Add an inject-gate test: with isReadOnly=true and an `@session …` draft, drive the press-and-hold to completion and assert mockSendSessionInject was NOT called (RO inject stays gated).

    Playwright — `frontend/e2e/chat-parity.spec.ts`:
    - Rewrite the `PARITY-01 SC-3` test (~l.289-331) to the new ROCHAT semantics. Rename it to reflect ROCHAT-01/02 (e.g. "ROCHAT-01 — RO viewer CAN post chat; ROCHAT-02 — @session inject stays gated"). Assertions: the `[data-chat-send]` button is NOT disabled (`await expect(sendBtn).not.toBeDisabled()`); fill a unique message and press Enter; assert the RO client's own message DOES appear (`.chat-msg` filtered by the unique text has count >= 1 within a generous timeout). Keep the WS warm-up wait (`.chat-msg` first visible) before asserting.
    - Update the file/header legend comment (~l.6, ~l.15) that says the send button is "disabled on RO" to reflect RO can post.
    - Do NOT add a browser-driven PTY-write assertion here (PTY proof lives in the Go regression guard from 163-01); a Playwright-level inject check is optional and must not flake — prefer leaving inject-gate proof to the Go suite and the vitest gesture test.

    Run the frontend gate for the affected specs.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && pnpm --dir frontend test -- --run ChatPanel</automated>
  </verify>
  <acceptance_criteria>
    - ChatPanel vitest suite passes, including the new RO-can-send and RO-inject-gated tests.
    - No vitest assertion remains that the Send button is disabled when read-only.
    - chat-parity.spec.ts SC-3 test asserts RO Send is enabled and the RO message appears; legend comment updated.
  </acceptance_criteria>
  <done>Frontend RO-cannot-post assertions flipped to RO-can-post; inject-gesture-gated coverage added; specs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RO guest browser → ChatPanel composer | Client-side affordance; the authoritative gate is server-side (163-01). UI must not re-introduce a PTY-writing path for RO. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-03 | Elevation of Privilege | ChatPanel handleInjectPointerDown | high | mitigate | Removing the Send-button RO gate must NOT remove the inject-gesture RO gate. `handleInjectPointerDown`'s `if (isReadOnly) return` is retained and proven by the new vitest gesture test (mockSendSessionInject not called) — defense-in-depth atop the server gate (163-01 HandleInject ErrReadOnly). |
| T-163-04 | Spoofing | chat author identity | low | accept | Unchanged from Phase 152/154 — author identity is server-stamped (TailnetID/personKey); alias is display-only. This plan does not touch identity. |
</threat_model>

<verification>
- `pnpm --dir frontend exec tsc --noEmit` passes.
- `pnpm --dir frontend test -- --run ChatPanel` is green.
- No "Read only" text in ChatPanel.tsx; Send button enabled for RO; inject gesture still gated.
</verification>

<success_criteria>
RO guests can compose and post chat from the shared ChatPanel on every surface; the `@session` inject gesture stays RO-blocked client-side; tests reflect the new behavior.
</success_criteria>

<output>
Create `.planning/phases/163-read-only-guest-chat-posting-d-06-reconciliation/163-02-SUMMARY.md` when done. In "Artifacts this phase produces" record: removed `chat-composer__readonly-label` JSX block; ChatPanel Send-button RO gate removed; flipped/renamed Playwright `PARITY-01 SC-3` test; new vitest RO-can-send + RO-inject-gated tests (note the vitest test-count delta for 163-03 TESTING.md).
</output>
