---
phase: 155-web-share-chat-ui-cross-surface-parity-gate
plan: 04
type: execute
wave: 3
depends_on: ["155-01", "155-03"]
files_modified:
  - cmd/playwright-fixture/main.go
  - frontend/e2e/chat-parity.spec.ts
  - TESTING.md
autonomous: true
requirements: [PARITY-01, EXPORT-01]
must_haves:
  truths:
    - "The Playwright fixture serves chat history and export for the test session (SetChatHistoryProvider + SetChatExportProvider wired to a seeded ChatStore)."
    - "Two web-share browser contexts exchange a chat message: a message sent by client A appears in client B's thread (PARITY-01 SC-1)."
    - "A RO-cap (viewer) web-share client shows a disabled Send button and cannot post — the server rejects regardless of client behavior (PARITY-01 SC-3)."
    - "Clicking Export downloads a `.md` file whose content contains the YAML frontmatter (session, exported_at) (EXPORT-01 SC-2)."
    - "The @session inject indicator (.chat-msg--inject) renders in the thread after a RW inject (PARITY-01 SC-4)."
  artifacts:
    - "cmd/playwright-fixture/main.go (ChatStore + providers + seed + RO cap)"
    - "frontend/e2e/chat-parity.spec.ts (the parity gate)"
    - "TESTING.md (Section 2 Playwright + Section 4 PARITY-01/EXPORT-01/NOTIF-02 + Section 5 UAT)"
  key_links:
    - "Without SetChatHistoryProvider/SetChatExportProvider the e2e shows an empty thread / 404 export (Pitfall 7)."
    - "Selectors are FROZEN by UI-SPEC §5 — use them verbatim; renaming any class breaks the gate."
    - "Use unique Date.now()-suffixed messages and .filter({ hasText }) — the fixture ChatStore persists across tests in a file (Pitfall 5)."
---

<objective>
Wire the Playwright fixture to serve chat (history + export from a seeded ChatStore) and add `frontend/e2e/chat-parity.spec.ts` — the release-blocking cross-surface parity gate (PARITY-01) plus the export-download assertion (EXPORT-01). Finalize TESTING.md (Playwright registration, PARITY-01/EXPORT-01/NOTIF-02 traceability, Phase 155 manual UAT).

Purpose: This is the phase gate. PARITY-01 is release-blocking per the standing cross-surface rule; the e2e is the automated proof that the web-share surface matches the desktop chat experience and that the RO server gate holds.

Output: Fixture chat wiring, the parity spec, finalized TESTING.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-RESEARCH.md
@.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-PATTERNS.md
@.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-UI-SPEC.md
@.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-VALIDATION.md
</context>

<artifacts>
- `cmd/playwright-fixture/main.go` — constructs `daemon.NewChatStore(tmpDir, sessionID)`, seeds a few `relay.ChatMessage` (incl. one `SessionInject: true`), wires `ws.SetChatHistoryProvider` (returns `json.Marshal(store.Messages())`) + `ws.SetChatExportProvider` (returns `store.Export()`), and ensures a RO cap (viewer) exists for the SC-3 test.
- `frontend/e2e/chat-parity.spec.ts` — Playwright tests: two-client broadcast + presence + typing + unread + @mention parity, RO-viewer Send-disabled + server-gate, export download (YAML frontmatter), @session inject indicator.
- `TESTING.md` — Section 2 Playwright entry; Section 4 PARITY-01 + EXPORT-01 e2e rows + NOTIF-02 row; Section 5 Phase 155 manual UAT items.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Wire ChatStore + providers + RO cap into the Playwright fixture</name>
  <files>cmd/playwright-fixture/main.go</files>
  <read_first>
    - cmd/playwright-fixture/main.go:35-45 (imports), :99 (manager.Create), :103-122 (cfg + NewWebServer), :135-167 (SetPluginSettingsProvider / SetFilesHandler provider-wiring style), :178-202 (owner + viewer cap minting: capability.Claims / capability.Sign / AddGrant)
    - internal/daemon/chat.go:99 (NewChatStore(baseDir, sessionID)), :244 (AppendMessage), :207 (Messages() returns []relay.ChatMessage), :321 (Export())
    - internal/webserver/chat.go:35,45 (SetChatHistoryProvider / SetChatExportProvider provider contracts)
    - .planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-PATTERNS.md — cmd/playwright-fixture/main.go section (confirmed APIs)
  </read_first>
  <action>
    Add an `internal/daemon` import (fixture is `package main` — no import cycle, per RESEARCH). After `manager.Create(...)` (main.go:99) and after `ws` is constructed (main.go:110), construct a ChatStore: `store, err := daemon.NewChatStore(<tmpDir>, sessionID)` using a tempdir as baseDir (e.g. `os.MkdirTemp` or `filepath.Join(os.TempDir(), ...)`), fail-fast on error like the existing fixture setup. Seed 2–3 messages via `store.AppendMessage(relay.ChatMessage{...})` — include distinct aliases/authorIDs and one with `SessionInject: true` so the export + inject-indicator assertions have data. Then wire the providers in the SetFilesHandler/SetPluginSettingsProvider style:
    `ws.SetChatHistoryProvider(func(sid string) ([]byte, bool, error) { b, _ := json.Marshal(store.Messages()); return b, true, nil })` and `ws.SetChatExportProvider(func(sid string) (string, bool, error) { md, err := store.Export(); return md, true, err })`.

    Confirm a RO cap exists for the SC-3 test: the fixture already mints VIEWER with `Perms: "read"` (main.go:191-202) and grants it. The viewer cap grants chat-history GET (it has `read`) but NOT `write` — that is the RO chat cap. If the viewer cap is not already exported into `fixture-env.json` (viewerCap), ensure it is, following the existing owner/viewer export block so `viewerAppUrl(env)` resolves.
  </action>
  <verify>
    <automated>go build ./cmd/playwright-fixture/ && go vet ./cmd/playwright-fixture/</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./cmd/playwright-fixture/` succeeds.
    - The fixture wires BOTH SetChatHistoryProvider and SetChatExportProvider to a seeded ChatStore (Pitfall 7).
    - A RO (viewer, `read`-only) cap is available to the e2e via fixture-env (viewerCap / viewerAppUrl).
  </acceptance_criteria>
  <done>The fixture serves seeded chat history + export and exposes both an RW (owner) and RO (viewer) cap.</done>
</task>

<task type="auto">
  <name>Task 2: chat-parity.spec.ts — the cross-surface parity gate</name>
  <files>frontend/e2e/chat-parity.spec.ts</files>
  <read_first>
    - frontend/e2e/web-csp.spec.ts:19-20 (test.describe + page-event structure)
    - frontend/e2e/fixture-env.ts:88-95 (appUrl / viewerAppUrl / loadFixtureEnv helpers — use these, do not hand-build URLs)
    - .planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-UI-SPEC.md — "5. Playwright CSS Selector Contract" (FROZEN selectors)
    - .planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-RESEARCH.md:415-534 (full test bodies + assertion→criterion map)
  </read_first>
  <action>
    Create `frontend/e2e/chat-parity.spec.ts` importing `{ test, expect }` from `@playwright/test` and `{ appUrl, viewerAppUrl, loadFixtureEnv }` from `./fixture-env`. Use the FROZEN selectors from UI-SPEC §5 verbatim: `.hub-modal__chat-toggle`, `.chat-panel__composer textarea`, `.chat-msg`, `.chat-msg--mention`, `[data-chat-send]`, `[data-chat-export]`, `.chat-presence`, `.chat-msg--inject`, `.chat-badge`, `.chat-typing`.

    Tests to implement (group under a `test.describe('Phase 155 — chat parity gate')`):
    1. PARITY-01 broadcast: open two `browser.newContext({ ignoreHTTPSErrors: true })` pages at `appUrl(env)` (RW). Open chat on both via `.hub-modal__chat-toggle`. Page1 fills `.chat-panel__composer textarea` with a unique `parity-${Date.now()}` message + presses Enter. Assert Page2 shows `.chat-msg` filtered by that text within 5s. Use `.filter({ hasText })`, never bare `.count()` (Pitfall 5).
    2. PARITY-01 presence/typing/unread/@mention: assert `.chat-presence` shows both clients; Page1 typing produces `.chat-typing` on Page2; with chat closed on Page2, a Page1 send raises `.chat-badge` on Page2; an `@alias` message highlights `.chat-msg--mention` on the mentioned page. (Keep timing-sensitive typing assertions lenient; precise debounce timing stays manual per RESEARCH.)
    3. PARITY-01 RO gate ("RO viewer …"): open `viewerAppUrl(env)` (RO). Assert `[data-chat-send]` `toBeDisabled()`. Then attempt a direct send (fill + Enter, or a `page.evaluate` raw WS frame mirroring the Phase 153 adversarial Go test) and assert no new `.chat-msg` appears — the server gate holds regardless of client behavior.
    4. EXPORT-01 download: at `appUrl(env)`, open chat, `page.waitForEvent('download')` around clicking `[data-chat-export]`; assert `download.suggestedFilename()` matches `/^chat-.*\.md$/` and the downloaded file content contains `---`, `session:`, and `exported_at:`.
    5. PARITY-01 SC-4 inject indicator: from the RW page, perform the @session inject gesture and assert `.chat-msg--inject` (the "→ injected into terminal" indicator) renders in the thread (no real PTY needed — the indicator is a chat broadcast, RESEARCH Open Q3).
  </action>
  <verify>
    <automated>pnpm -C frontend exec playwright test chat-parity</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -C frontend exec playwright test chat-parity` passes all tests.
    - The RO test asserts both `[data-chat-send]` disabled AND no message posted from the RO client (server gate).
    - The export test asserts the downloaded `.md` contains YAML frontmatter (`session:` + `exported_at:`).
    - The spec uses only the UI-SPEC §5 frozen selectors.
  </acceptance_criteria>
  <done>The Playwright parity gate proves cross-surface chat parity, the RO server gate, export download, and the inject indicator.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize TESTING.md — Playwright registration, traceability, manual UAT</name>
  <files>TESTING.md</files>
  <read_first>
    - TESTING.md:22-42 (Section 2 Suite Manifest + CI Workflow Mapping — Playwright group)
    - TESTING.md:94-201 (Section 4 traceability — path column is repo-relative `.ts`/`.tsx`/`.go` only)
    - TESTING.md:202-338 (Section 5 manual checklist categories — add a Phase 155 category)
    - .planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-VALIDATION.md ("TESTING.md gaps to resolve in this phase")
  </read_first>
  <action>
    Section 2: add `frontend/e2e/chat-parity.spec.ts` to the Playwright suite group (no build tags, no file moves).

    Section 4: add traceability rows mapping PARITY-01 → `frontend/e2e/chat-parity.spec.ts` and EXPORT-01 → `frontend/e2e/chat-parity.spec.ts` (the e2e download assertion). Also add the dedicated NOTIF-02 traceability row flagged as a minor gap carried from Phase 154 VERIFICATION (map it to the existing Phase 154 test file that exercises unread/badge behavior — locate it; do not invent a path). Path column must be repo-relative file paths only.

    Section 5: add a "Phase 155 — Web-Share Chat" manual UAT category for the items that cannot be automated (per RESEARCH "What Must Remain Manual UAT"): @session inject visual indicator in the native Wails WebView; typing-indicator 500ms/5s timing; overlay no-resize on a live PTY. Reference existing M-NN numbering convention; append new M-NN items.

    Run `bash tests/check-traceability-paths.sh` and ensure exit 0.
  </action>
  <verify>
    <automated>bash tests/check-traceability-paths.sh</automated>
  </verify>
  <acceptance_criteria>
    - Section 2 lists `frontend/e2e/chat-parity.spec.ts` in the Playwright group.
    - Section 4 has PARITY-01, EXPORT-01, and NOTIF-02 rows with repo-relative paths only.
    - Section 5 has Phase 155 manual UAT items for the un-automatable behaviors.
    - `bash tests/check-traceability-paths.sh` exits 0.
  </acceptance_criteria>
  <done>TESTING.md registers the parity spec, traceability for PARITY-01/EXPORT-01/NOTIF-02, and Phase 155 manual UAT; traceability-path check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RO-cap browser client → webserver chat send / @session inject | The adversarial e2e crosses this boundary deliberately to prove the server rejects RO writes |
| two independent browser contexts → shared webserver hub | Messages broadcast across contexts; the gate proves no cross-client leakage of write privilege |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-01 | Elevation of Privilege | RO-cap client posting chat / triggering @session inject | high | mitigate | This plan provides the AUTHORITATIVE adversarial proof: the RO e2e attempts a send (and/or a raw WS frame) and asserts no message is posted — the server gate (HandleChatSend/HandleInject bound to the signed JWT claim) holds regardless of client behavior. Release-blocking; the spec must fail loudly if a RO client can post. |
| T-155-04 | Spoofing | webserver WS upgrade with the test caps | high | mitigate | The fixture mints HMAC-signed caps via `capability.Sign`; the e2e connects with valid caps only. An invalid cap path is already covered by Phase 87/88 + the Plan 155-01 export-route test (missing cap rejected). |
| T-155-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new packages; Playwright + fixture deps already present (RESEARCH Environment Availability: none missing). |
</threat_model>

<verification>
- `go build ./cmd/playwright-fixture/` green.
- `pnpm -C frontend exec playwright test chat-parity` green (PARITY-01 + EXPORT-01 + SC-3 RO gate + SC-4 inject indicator).
- Full phase gate (VALIDATION): `go test ./... && pnpm -C frontend test run && pnpm -C frontend exec playwright test` all green before `/gsd-verify-work`.
- `bash tests/check-traceability-paths.sh` exits 0.
</verification>

<success_criteria>
- The cross-surface parity gate passes: two web-share clients exchange messages with matching presence/typing/unread/@mention (PARITY-01 SC-1).
- A RO-cap web-share viewer cannot post or inject; the server rejects regardless of client (PARITY-01 SC-3).
- Export downloads a `.md` with YAML frontmatter from the web-share surface (EXPORT-01 SC-2).
- The @session inject indicator renders identically on the web-share surface (PARITY-01 SC-4).
- TESTING.md is fully updated and the traceability-path check passes.
</success_criteria>

<output>
Create `.planning/phases/155-web-share-chat-ui-cross-surface-parity-gate/155-04-SUMMARY.md` when done.
</output>
