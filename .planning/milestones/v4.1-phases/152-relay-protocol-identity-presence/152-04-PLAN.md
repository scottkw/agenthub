---
phase: 152-relay-protocol-identity-presence
plan: 04
type: execute
wave: 2
depends_on: ["152-01"]
files_modified:
  - frontend/src/lib/relayClient.ts
  - frontend/src/lib/relayClient.test.ts
autonomous: true
requirements: [IDENT-01, PRESENCE-01, PRESENCE-02]
must_haves:
  truths:
    - "parseServerFrame decodes a MsgPresence (0x32) frame into a typed {type:'presence', participants:[...]} variant and a MsgTyping (0x33) frame into {type:'typing', personKey, alias, typing} — matching the Go PresencePayload/TypingPayload JSON shapes"
    - "An alias-set frame can be encoded client→server via encodeAliasSetFrame with leading byte 0x34, and a typing frame via encodeTypingFrame with leading byte 0x33"
    - "Unknown/future frame types (including 0x30/0x31) still return {type:'unknown'} so old behavior is preserved (backward compatibility)"
  artifacts:
    - frontend/src/lib/relayClient.ts
    - frontend/src/lib/relayClient.test.ts
  key_links:
    - "MSG_PRESENCE/MSG_TYPING/MSG_ALIAS_SET constant values mirror internal/relay/protocol.go (Plan 01) byte-for-byte — the wire contract"
    - "The JSON field names (personKey, tailnetID, origin, alias, connCount, participants, typing) match the Go json tags exactly so payloads parse"
---

<objective>
Extend frontend/src/lib/relayClient.ts with the TypeScript half of the Phase 152 wire
protocol: the MSG_PRESENCE/MSG_TYPING/MSG_ALIAS_SET constants, ServerFrame union variants
for presence and typing, parseServerFrame branches that decode the JSON bodies, and the
client→server encoders for typing-start/stop and set-alias. This is the protocol/parsing
layer only — the chat UI panels that consume these frames are Phases 154/155.

This depends on Plan 01 only for the canonical constant values (0x32/0x33/0x34) and the
JSON payload field names; it shares no files with any Go plan and runs in parallel with
the Hub work (Plan 03).

Purpose: give later UI phases a typed, tested parse/encode surface for presence and typing.
Output: extended relayClient.ts + relayClient.test.ts cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-relay-protocol-identity-presence/152-PATTERNS.md
@frontend/src/lib/relayClient.ts
@frontend/src/lib/relayClient.test.ts
</context>

<artifacts_this_phase_produces>
This plan creates (TypeScript wire layer):
- Constants: MSG_PRESENCE = 0x32, MSG_TYPING = 0x33, MSG_ALIAS_SET = 0x34
- ServerFrame variants: {type:'presence', participants} and {type:'typing', personKey, alias, typing}
- Functions: encodeTypingFrame(typing), encodeAliasSetFrame(alias); parseServerFrame branches for 0x32/0x33
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add presence/typing/alias constants, types, parse + encode</name>
  <files>frontend/src/lib/relayClient.ts, frontend/src/lib/relayClient.test.ts</files>
  <read_first>
    - frontend/src/lib/relayClient.ts (constants lines 2-6; ServerFrame union lines 9-12; encodeInputFrame lines 18-24; parseServerFrame lines 43-64 — the default-unknown backward-compat branch)
    - frontend/src/lib/relayClient.test.ts (existing describe/it structure; how Array.from(frame) and parseServerFrame are asserted)
    - .planning/phases/152-relay-protocol-identity-presence/152-PATTERNS.md (section "TypeScript parseServerFrame Extension" lines 546-559)
    - .planning/phases/152-relay-protocol-identity-presence/152-RESEARCH.md (Pattern 1 struct field json tags lines 200-232 — must match TS field names)
  </read_first>
  <behavior>
    - parseServerFrame of [0x32, ...utf8 of {"participants":[{"personKey":"local:local","tailnetID":"local","origin":"local","alias":"ken","connCount":1}]}] returns {type:'presence', participants:[{personKey:'local:local',tailnetID:'local',origin:'local',alias:'ken',connCount:1}]}
    - parseServerFrame of [0x33, ...utf8 of {"personKey":"k:web","alias":"sam","typing":true}] returns {type:'typing', personKey:'k:web', alias:'sam', typing:true}
    - parseServerFrame of [0x30, ...] and [0x31, ...] returns {type:'unknown'} (Phase 154 stubs not yet handled)
    - encodeTypingFrame(true)[0] === 0x33 and its JSON body parses to {typing:true}
    - encodeAliasSetFrame('ken')[0] === 0x34 and its JSON body parses to {alias:'ken'}
    - A malformed presence body (invalid JSON after 0x32) returns {type:'unknown'} rather than throwing
  </behavior>
  <action>
    Add exported consts MSG_PRESENCE = 0x32, MSG_TYPING = 0x33, MSG_ALIAS_SET = 0x34 to the constant block, with comments noting server→client / bidirectional / client→server direction. Extend the ServerFrame union with two variants: { type: 'presence'; participants: PresenceEntry[] } and { type: 'typing'; personKey: string; alias: string; typing: boolean }, and declare a PresenceEntry interface { personKey: string; tailnetID: string; origin: string; alias: string; connCount: number } whose field names match the Go json tags exactly. In parseServerFrame add case MSG_PRESENCE: decode data.slice(1) via new TextDecoder().decode(...) + JSON.parse inside a try/catch; on parse error return {type:'unknown'}; otherwise return {type:'presence', participants: parsed.participants ?? []}. Add case MSG_TYPING: same try/catch, return {type:'typing', personKey, alias, typing} from the parsed body. Leave the default:return {type:'unknown'} branch intact so 0x30/0x31 and any future bytes stay backward-compatible. Add encodeTypingFrame(typing: boolean): Uint8Array — JSON.stringify({typing}) → UTF-8 bytes prefixed with MSG_TYPING; and encodeAliasSetFrame(alias: string): Uint8Array — JSON.stringify({alias}) → UTF-8 bytes prefixed with MSG_ALIAS_SET (mirror encodeInputFrame's prefix-then-set shape). Add vitest cases to relayClient.test.ts for every behavior bullet, including the malformed-body and unknown-stub cases.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm test -- run src/lib/relayClient.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - relayClient.ts exports MSG_PRESENCE=0x32, MSG_TYPING=0x33, MSG_ALIAS_SET=0x34 (grep -nE 'MSG_PRESENCE *= *0x32' frontend/src/lib/relayClient.ts exits 0)
    - parseServerFrame returns 'presence' and 'typing' variants for 0x32/0x33 and 'unknown' for 0x30/0x31/malformed
    - encodeTypingFrame and encodeAliasSetFrame produce frames with leading bytes 0x33 and 0x34
    - pnpm test on relayClient.test.ts exits 0
  </acceptance_criteria>
  <done>The TypeScript relay client parses presence/typing frames into typed variants and encodes typing/alias-set frames, with backward-compatible handling of reserved/unknown types; all cases are vitest-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client (WS frame) | Untrusted JSON bodies in 0x32/0x33 frames are parsed in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-09 | Tampering | parseServerFrame JSON.parse | medium | mitigate | All JSON.parse of frame bodies is wrapped in try/catch returning {type:'unknown'}, so a malformed or hostile body cannot throw and break the message loop. Field access uses nullish fallbacks (participants ?? []). |
| T-152-10 | Tampering | alias rendering (deferred) | high | transfer | This plan only parses alias text into a typed value; it does NOT render it. Escaping/sanitization at render time is owned by the chat UI phases (154/155). Ingress validation already happened server-side (T-152-01, Plan 01). |
| T-152-SC | Tampering | npm/pnpm installs | low | accept | No packages added — uses existing TextEncoder/TextDecoder/JSON and the project's vitest. |
</threat_model>

<verification>
- pnpm test on relayClient.test.ts green
- Constants match the Go protocol.go byte values exactly (cross-checked against Plan 01)
- default:unknown branch preserved (0x30/0x31 and future bytes remain non-breaking)
</verification>

<success_criteria>
- Presence and typing frames decode into typed variants with Go-matching field names
- Typing/alias-set encoders produce correctly-prefixed frames
- Malformed/unknown frames never throw (backward compatible)
</success_criteria>

<output>
Create `.planning/phases/152-relay-protocol-identity-presence/152-04-SUMMARY.md` when done
</output>
