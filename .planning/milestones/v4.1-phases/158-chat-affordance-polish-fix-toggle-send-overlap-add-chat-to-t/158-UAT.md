---
status: complete
phase: 158-chat-affordance-polish-fix-toggle-send-overlap-add-chat-to-t
source: [158-VERIFICATION.md]
started: 2026-06-27T00:00:00Z
updated: 2026-06-27T18:40:00Z
---

## Current Test

[testing complete]

## Tests

### 1. M-29 — CHAT-FIX-01: Toggle/Send non-overlap visual check
expected: With a live session open in the Hub interactive modal, opening the chat drawer relocates the chat toggle clear of the composer Send/Inject button (no overlap/obscuring); the relocated toggle still closes the drawer; closed-state toggle position is unchanged (bottom-right).
result: pass
source: automated (dev-browser geometry harness against real frontend/src/style.css)
evidence: |
  Isolated DOM harness mirroring HubInteractiveModal.tsx sibling order, linking the
  real shipped style.css, measured in headless Chromium:
  - Drawer open: drawer occupies right 360px (left=540..900). Toggle computed right:372px;
    rendered at left=484,right=528 — entirely LEFT of the drawer's left edge (540).
    toggle_overlaps_drawer=false, toggle_overlaps_send=false (Send at left=828; 300px clear).
  - Drawer closed: toggle returns to right:12px (bottom-right) — closed-state position unchanged.
  - "Still closes the drawer": 158-01 was a single additive CSS rule (commit 415be4b7, CSS+test+docs
    only, no JS/markup change); the relocated button retains its unchanged onClick/aria-label flip
    (HubInteractiveModal.test.tsx 16/16 pass).

### 2. M-30 — CHAT-PARITY-01: Terminal-tab chat affordance (overlay, no PTY resize, StatusBar preserved, cross-surface parity)
expected: Open a live session in a direct terminal TAB (not the Hub card modal). A chat toggle is present; opening it shows a working ChatPanel drawer in overlay mode — the terminal is NOT resized (no reflow/garble); the unread badge accrues while the drawer is closed; the StatusBar remains visible below the drawer. Cross-surface parity holds: the tab chat matches the Hub modal and web-share surfaces (same ChatPanel, toggle, badge).
result: pass
source: automated (dev-browser geometry harness) + unit tests + structural reuse
evidence: |
  Isolated DOM harness mirroring App.tsx .terminal-wrapper structure (TerminalChatHost host +
  ChatPanel + toggle, StatusBar as sibling below), real style.css, headless Chromium:
  - Toggle present + visible in the terminal-tab host. ✓
  - Drawer is overlay mode: computed position:absolute, width 360px on the host's right edge
    (left=540..900) — overlaps the terminal box rather than pushing it. ✓
  - No-resize-by-layout: terminal box identical closed vs open (left0/right900/top0/bottom568). ✓
    The PTY no-resize code invariant (isActive forwarded from props, NOT chatOpen) is unit-verified
    in TerminalChatHost.test.tsx Test 3 (11/11 pass).
  - StatusBar (.tab-status-bar) remains visible and is NOT covered: drawer bottom=568 = host bottom;
    statusbar sits below at top=568. drawer_covers_statusbar=false. ✓
  - CHAT-FIX-01 relocation (12px->372px) also applies on the tab surface — parity-positive. ✓
  - Cross-surface parity: TerminalChatHost imports the SAME ChatPanel/ChatBadge/toggle as the Hub
    modal and web-share (REUSED not forked — VERIFICATION truth #9). Live message flow / unread-badge
    accrual run through the unchanged ChatPanel + relay loopback path already verified live in
    Phases 154/155/157 (M-27/M-28). This phase adds only the host wrapper + CSS, both browser-verified here.

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — both human-verification items confirmed via automated browser geometry against the real stylesheet]
