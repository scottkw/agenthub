---
phase: 9
slug: settings-modal-overhaul
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-19
---

# Phase 9 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — hand-rolled BEM CSS |
| Icon library | none — Unicode glyphs (× for close) |
| Font | "Cascadia Code", "MesloLGS NF", "Fira Code", monospace (inherited from body) |

Source: RESEARCH.md Standard Stack + direct read of `frontend/src/style.css` and `frontend/package.json`.

No `components.json` found. This is a Wails desktop app using plain React + flat BEM CSS. shadcn is not applicable and no packages are added this phase.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding (e.g. path input padding: 4px 8px) |
| sm | 8px | Compact element spacing (e.g. footer gap, table cell padding) |
| md | 16px | Default element spacing (e.g. header padding horizontal, button padding horizontal) |
| lg | 20px | Section padding (body padding, header padding vertical uses 16px — see exception) |
| xl | 24px | Reserved — not used in settings panel |
| 2xl | 32px | Reserved — not used in settings panel |
| 3xl | 64px | Reserved — not used in settings panel |

Exceptions:
- Header padding: `16px 20px` — vertical 16px, horizontal 20px (existing value, keep as-is)
- Body padding: `20px` all sides (existing value, keep as-is)
- Footer padding: `12px 20px` — vertical 12px, horizontal 20px (existing value, keep as-is; 12px is a legacy value on an existing container, not a new spacing decision)
- Tab bar row: `padding: 0 20px` — aligns with header and body horizontal rhythm
- Tab button padding: `8px 16px` — vertical 8px, horizontal 16px (fixed from 10px to conform to 4-point scale)
- Field group vertical gap: `16px` between field groups within a tab panel (fixed from 12px to conform to 4-point scale)
- Table cell padding: `8px 8px` body cells, `4px 8px` header cells (existing values, keep as-is)

Source: Direct read of `frontend/src/style.css` `.settings-panel__*` rules.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Modal heading (h2 "Settings") | 16px | 600 (semibold) | 1.2 |
| Section heading (h3 uppercase labels) | 13px | 400 (regular) | 1.2 |
| Body / form labels / table cells | 13px | 400 (regular) | 1.5 |
| Small / status / error / input text | 12px | 400 (regular) | 1.5 |

Notes:
- Section headings (h3) are `text-transform: uppercase` with `letter-spacing: 0.08em` — existing style, preserve.
- Tab button labels use 13px regular (matching body), not uppercase — standard tab affordance.
- "Save Paths" inline button label: 13px regular (matches `settings-panel__btn`).
- Font family: inherited from `body` — `"Cascadia Code", "MesloLGS NF", "Fira Code", monospace`.
- 13px (body/labels) vs 12px (small/error) are 1px apart. Color is the primary differentiator: 13px body uses `--text-hi` (#c0caf5) or `--text-mid` (#a9b1d6); 12px small uses `--text-dim` (#565f89) or `--red` (#f7768e). Size alone does not distinguish these roles — color and context do.

Source: Direct read of `frontend/src/style.css` `.settings-panel__header h2`, `.settings-panel__body h3`, `.settings-panel__btn`, `.settings-panel__path-input`.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#1e2030` | Settings panel background (`.settings-panel`), field input backgrounds use `#16161e` |
| Secondary (30%) | `#16161e` | Tab bar row background, path inputs (`settings-panel__path-input`), status bar background |
| Accent (10%) | `#7aa2f7` | Active tab underline, focused input border, CLI name column text, accent link URLs |
| Destructive | `#f7768e` | Close button (×) hover color, error message text |
| Surface overlay | `rgba(0,0,0,0.6)` | Modal backdrop (`.settings-overlay`) — existing value |

Accent reserved for:
1. Active tab underline (`border-bottom-color: #7aa2f7` on `.settings-panel__tab-btn--active`)
2. Path input focus border (`:focus { border-color: #7aa2f7 }`)
3. CLI name column text color in the paths table
4. Hover accent: `#89b4fa` on "Set Password" and "Start Web Server" primary action buttons

Color tokens extracted from existing `style.css` (used as literal hex values, not CSS custom properties):
- `--bg-deep: #1a1b26` — body/app background (not used in settings panel surface)
- `--bg-mid: #1e2030` — panel background
- `--bg-bar: #16161e` — tab bar, inputs
- `--border: #292e42` — all borders
- `--text-dim: #565f89` — muted labels, inactive tab text, h3 section headings
- `--text-mid: #a9b1d6` — secondary text, tab hover text
- `--text-hi: #c0caf5` — primary text, active tab text, modal heading
- `--accent: #7aa2f7` — active elements (see reserved list above)
- `--accent-hi: #89b4fa` — hover on primary action buttons
- `--select-bg: #3b4261` — pressed/selected background (e.g. close button hover background)
- `--green: #9ece6a` — server running status (status bar, not in settings modal body)
- `--red: #f7768e` — destructive hover, error text

Source: RESEARCH.md "Existing Color Tokens" section + direct read of `frontend/src/style.css`.

---

## Component Inventory

Components and CSS classes added or modified this phase:

### New CSS classes (add to `frontend/src/style.css`)

| Class | Description |
|-------|-------------|
| `.settings-panel__tabs` | Tab bar row below header: `display: flex; flex-direction: row; border-bottom: 1px solid #292e42; padding: 0 20px; flex-shrink: 0;` |
| `.settings-panel__tab-btn` | Individual tab button: `padding: 8px 16px; background: transparent; border: none; border-bottom: 2px solid transparent; color: #565f89; font-size: 13px; cursor: pointer; margin-bottom: -1px;` |
| `.settings-panel__tab-btn:hover` | Hover: `color: #a9b1d6;` |
| `.settings-panel__tab-btn--active` | Active state: `color: #c0caf5; border-bottom-color: #7aa2f7;` |
| `.settings-panel__tab-panel` | Content wrapper for each tab section: `display: contents;` (or just remove wrapper — sections render directly in body) |
| `.settings-panel__save-paths-row` | Row containing the inline "Save Paths" button at bottom of CLI Paths tab: `display: flex; justify-content: flex-end; margin-top: 16px;` |

### Modified CSS classes (`frontend/src/style.css`)

| Class | Change |
|-------|--------|
| `.settings-panel__footer` | Remove `gap: 8px` (single button needs no gap). Keep `padding: 12px 20px` and `justify-content: flex-end`. |
| `.settings-panel__btn--save` (footer) | Not used in footer after this phase — footer uses only `--cancel` style for Close button |

### Modified React component (`frontend/src/components/SettingsPanel.tsx`)

| Change | Detail |
|--------|--------|
| Add `activeTab` state | `const [activeTab, setActiveTab] = useState<'cli-paths' \| 'web-serving'>('cli-paths')` |
| Add tab bar row | New `<div className="settings-panel__tabs">` row between header and body |
| Conditional body rendering | `{activeTab === 'cli-paths' && ...}` / `{activeTab === 'web-serving' && ...}` via JSX conditionals, not CSS display toggle |
| Move "Save Paths" button | From footer into CLI Paths tab panel below the table as `settings-panel__btn--save` inline button |
| Remove `onClose()` from `handleSaveCLIPaths` | After inline save, stay on tab — show success state or clear silently |
| Footer becomes single "Close" button | Styled as `settings-panel__btn--cancel` (ghost/border) — no primary action in footer |
| Close button (×) aria-label | Header × button must have `aria-label="Close settings"` — it is icon-only (Unicode glyph, no visible text label) |

Source: RESEARCH.md Architecture Patterns (Pattern 1, 2, 3) + Anti-Patterns section.

---

## Interaction Contract

### Primary Visual Anchor

The primary visual anchor is the **tab bar navigation row** (`.settings-panel__tabs`). It is the first interactive element below the header and establishes which content pane is active. The active tab underline (`#7aa2f7`) is the only accent-color element in the modal chrome, drawing the eye to current context before body content.

### Tab Switching

- Default active tab on open: **"CLI Paths"**
- Tab switch: instant (no animation, no transition) — consistent with existing `0.1s` transitions only on hover states
- State preservation: all web serving state (`isServerRunning`, `serverLoading`, `serverURL`, etc.) stays in parent `SettingsPanel` component — survives tab switch
- Only one tab's content visible at a time; use JSX conditionals (`&&`), not `display: none`
- ARIA: tab bar has `role="tablist"`, each button has `role="tab"` and `aria-selected={activeTab === 'cli-paths'}`

### Save Paths (CLI Paths tab)

- "Save Paths" button is inside the CLI Paths tab panel, below the table
- Styled as primary (`settings-panel__btn--save`) — it is the one primary action in that tab
- Disabled while `saving === true`; shows "Saving…" label during save
- On success: does NOT call `onClose()` — stays on tab; dirty state cleared silently
- On error: shows `settings-panel__error` paragraph below the button

### Close (footer)

- Single "Close" button, styled as `settings-panel__btn--cancel` (ghost with border)
- Calls `onClose()` immediately — no confirmation dialog (no destructive action in footer)
- Label: "Close" — intentional single-word label; verb-noun convention is not applicable here because there is no object being acted upon (the action is modal dismissal, not a data operation). This matches standard modal close affordance.

### Web Serving tab (unchanged interactions)

- "Set Password" button: inline, primary style, disabled when input empty or `passwordSaving`
- "Start Web Server" / "Stop Web Server" toggle button: primary when starting, cancel-style when stopping
- Disabled when server is loading or password not yet set (start only)
- Network interface selector and port input: disabled while server is running

### Overlay click to close

- Clicking `.settings-overlay` (backdrop) calls `onClose()` — existing behavior, keep unchanged

Source: RESEARCH.md Pitfall 2 (Save Paths), Pitfall 4 (Close button style), Open Questions 1 and 2, Anti-Patterns section.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (CLI Paths tab) | "Save Paths" |
| Primary CTA (Web Serving tab — set password) | "Set Password" |
| Primary CTA (Web Serving tab — server toggle start) | "Start Web Server" |
| Primary CTA (Web Serving tab — server toggle stop) | "Stop Web Server" |
| Footer close button | "Close" (single-word; intentional — dismissal action, no data object) |
| Tab label 1 | "CLI Paths" |
| Tab label 2 | "Web Serving" |
| Loading: saving paths | "Saving…" |
| Loading: setting password | "Saving…" |
| Loading: starting server | "Starting…" |
| Loading: stopping server | "Stopping…" |
| Empty state (CLI Paths — no CLIs detected) | "No CLIs detected. Install an AI coding CLI and restart the app." |
| Empty state (Network Interfaces — none found) | "No non-loopback network interfaces found. Check that your network adapter is enabled, then reopen Settings." |
| Error state (CLI path save fail) | "{error message from backend}" — displayed below "Save Paths" button as `.settings-panel__error` |
| Error state (password save fail) | "{error message from backend}" — displayed below "Set Password" button |
| Error state (server toggle fail) | "{error message from backend}" — displayed below server toggle button |
| Password input placeholder (not set) | "Set a password to enable web serving" |
| Password input placeholder (already set) | "Change password…" |
| Destructive actions | None this phase — "Stop Web Server" is not destructive (no confirmation needed) |

Source: Direct read of `frontend/src/components/SettingsPanel.tsx` existing copy strings. Inline copy strings kept as-is where they already exist; only footer is changed from "Cancel" / "Save" to "Close".

---

## Layout Structure

```
┌─────────────────────────────────┐
│  Settings                    ×  │  ← .settings-panel__header (16px 20px padding); × has aria-label="Close settings"
├─────────────────────────────────│
│  [CLI Paths]  [Web Serving]     │  ← .settings-panel__tabs (0 20px padding) — PRIMARY VISUAL ANCHOR
├─────────────────────────────────│
│  <scrollable body>              │  ← .settings-panel__body (20px padding, flex: 1)
│    h3 section heading           │
│    table / field groups         │
│    [Save Paths] ← inline btn    │  (CLI Paths tab only, right-aligned, margin-top: 16px)
├─────────────────────────────────│
│                       [Close]   │  ← .settings-panel__footer (12px 20px padding)
└─────────────────────────────────┘
```

Modal dimensions (unchanged):
- Width: 520px, max-width: 95vw
- Max-height: 80vh
- Border-radius: 8px
- Box-shadow: `0 8px 32px rgba(0,0,0,0.5)`

Source: RESEARCH.md Pattern 2 layout diagram + direct read of `frontend/src/style.css` `.settings-panel`.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No new npm packages are installed this phase. All components are hand-rolled in existing `SettingsPanel.tsx` and `style.css`.

Source: RESEARCH.md "Installation: No new packages required."

---

## Pre-Population Audit

| Source | Decisions Used |
|--------|---------------|
| RESEARCH.md | 12 (stack, architecture patterns, CSS examples, color tokens, pitfalls, open questions) |
| REQUIREMENTS.md | 2 (SETT-01, SETT-02) |
| frontend/src/style.css | 8 (all existing color values, spacing values, font sizes, component structure) |
| frontend/src/components/SettingsPanel.tsx | 6 (state variables, handler names, existing copy strings, footer structure) |
| STATE.md decisions | 1 (JSX conditionals, not CSS display toggling — Phase 8 decision) |
| User input this session | 0 — all answered by upstream artifacts |
| Checker revision (2026-03-19) | 6 (tab padding 10px→8px, field gap 12px→16px, Close label note, network empty state solution path, focal point declaration, close button aria-label) |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
