---
phase: 29-build-system-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/build-script.test.sh
  - .github/workflows/build.yml
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03

must_haves:
  truths:
    - "go test -race ./... passes locally with all 194 tests green"
    - "tests/build-script.test.sh passes locally (35/35) without hardcoded path"
    - "CI workflow runs race-enabled tests on all 4 platform legs"
    - "CI workflow runs build-script tests on the ubuntu-latest leg"
  artifacts:
    - path: "tests/build-script.test.sh"
      provides: "Build script behavioral tests with portable path resolution"
      contains: "SCRIPT_DIR="
    - path: ".github/workflows/build.yml"
      provides: "CI workflow with race detector and build-script test step"
      contains: "go test -race"
  key_links:
    - from: ".github/workflows/build.yml"
      to: "tests/build-script.test.sh"
      via: "bash step on Linux runner"
      pattern: "bash tests/build-script.test.sh"
    - from: ".github/workflows/build.yml"
      to: "go test -race"
      via: "test step on all platforms"
      pattern: "go test -race ./\\.\\.\\."
---

<objective>
Fix the build-script test path for CI portability and harden the GitHub Actions workflow to run race-enabled Go tests and build-script verification.

Purpose: Closes BUILD-01 (verify build.sh produces unified binary), BUILD-02 (CI builds and tests unified binary), and BUILD-03 (all tests pass with -race flag) by making the existing test infrastructure CI-ready and adding race detection.

Output: Updated tests/build-script.test.sh (portable path), updated .github/workflows/build.yml (race flag + build-script step)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-build-system-verification/29-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix build-script test path and verify locally</name>
  <files>tests/build-script.test.sh</files>
  <read_first>
    - tests/build-script.test.sh (current hardcoded path on line 10)
    - build.sh (the file being tested — confirm it exists at project root)
  </read_first>
  <action>
In tests/build-script.test.sh, replace the hardcoded absolute path on line 10:

Current (line 10):
```
BUILD_SH="/Users/ken/dev/agenthub/build.sh"
```

Replace with portable resolution using BASH_SOURCE:
```
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BUILD_SH="$SCRIPT_DIR/../build.sh"
```

This changes line 10 from a single line to two lines. The rest of the file is unchanged.

Also update the comment on line 6 from:
```
# Run: bash /Users/ken/dev/agenthub/tests/build-script.test.sh
```
to:
```
# Run: bash tests/build-script.test.sh    (from project root)
```

After editing, run the test suite locally to confirm all 35 tests still pass:
```
cd /Users/ken/dev/agenthub && bash tests/build-script.test.sh
```

Then run the full Go test suite with race detector:
```
cd /Users/ken/dev/agenthub && go test -race -count=1 ./...
```
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && bash tests/build-script.test.sh && go test -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - tests/build-script.test.sh line 10 contains `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`
    - tests/build-script.test.sh contains `BUILD_SH="$SCRIPT_DIR/../build.sh"`
    - tests/build-script.test.sh does NOT contain `/Users/ken/dev/agenthub`
    - `bash tests/build-script.test.sh` exits 0 with "35 passed"
    - `go test -race -count=1 ./...` exits 0 (all packages ok)
  </acceptance_criteria>
  <done>build-script.test.sh uses portable path resolution, passes 35/35 locally, and go test -race passes all tests</done>
</task>

<task type="auto">
  <name>Task 2: Add race flag and build-script step to CI workflow</name>
  <files>.github/workflows/build.yml</files>
  <read_first>
    - .github/workflows/build.yml (current CI workflow — understand step order and matrix structure)
    - tests/build-script.test.sh (confirm the script path for the new CI step)
  </read_first>
  <action>
In .github/workflows/build.yml, make two changes:

**Change 1: Add -race to the Go test step (line 59-60)**

Current:
```yaml
      - name: Run Go tests (all platforms)
        run: go test ./...
```

Replace with:
```yaml
      - name: Run Go tests (all platforms, race detector)
        run: go test -race ./...
```

**Change 2: Add build-script test step after the Go test step (insert after line 60, before the "Build with Wails" step)**

Insert this new step:
```yaml
      - name: Run build script tests
        if: runner.os == 'Linux' && matrix.build.os == 'ubuntu-latest'
        run: bash tests/build-script.test.sh
```

The final step order should be:
1. Checkout
2. Install Linux dependencies (if Linux)
3. Setup Go (actions/setup-go@v5)
4. Run Go tests (all platforms, race detector) -- MODIFIED
5. Run build script tests (Linux ubuntu-latest only) -- NEW
6. Build with Wails
7. ... (rest unchanged)

Do NOT change anything else in the workflow file. Do not modify the matrix, the Go version source, the wails build flags, or any of the signing/upload steps.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && grep -n "go test -race" .github/workflows/build.yml && grep -n "build-script.test.sh" .github/workflows/build.yml && grep -c "go test \./\.\.\." .github/workflows/build.yml | grep -q "^0$" && echo "VERIFIED: no old go test ./... remaining"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/build.yml contains `run: go test -race ./...`
    - .github/workflows/build.yml does NOT contain `run: go test ./...` (without -race)
    - .github/workflows/build.yml contains `run: bash tests/build-script.test.sh`
    - .github/workflows/build.yml contains `if: runner.os == 'Linux' && matrix.build.os == 'ubuntu-latest'` on the build-script step
    - The "Run build script tests" step appears AFTER "Run Go tests" and BEFORE "Build with Wails"
    - Step name is exactly `Run Go tests (all platforms, race detector)`
  </acceptance_criteria>
  <done>CI workflow runs go test with -race on all 4 platforms and runs build-script.test.sh on ubuntu-latest</done>
</task>

</tasks>

<verification>
1. Local: `bash tests/build-script.test.sh` passes 35/35
2. Local: `go test -race -count=1 ./...` passes all tests (194 expected)
3. CI file: `grep -c "go test -race" .github/workflows/build.yml` returns 1
4. CI file: `grep -c "build-script.test.sh" .github/workflows/build.yml` returns 1
5. No hardcoded paths: `grep -r "/Users/ken" tests/` returns nothing
</verification>

<success_criteria>
- tests/build-script.test.sh is portable (no hardcoded absolute paths) and passes 35/35 locally
- .github/workflows/build.yml runs `go test -race ./...` on all matrix legs
- .github/workflows/build.yml runs `bash tests/build-script.test.sh` on ubuntu-latest leg
- All 194 Go tests pass locally with -race flag
- BUILD-01 verified by build-script tests running in CI
- BUILD-02 satisfied by updated CI workflow
- BUILD-03 satisfied by -race flag on go test in CI
</success_criteria>

<output>
After completion, create `.planning/phases/29-build-system-verification/29-01-SUMMARY.md`
</output>
