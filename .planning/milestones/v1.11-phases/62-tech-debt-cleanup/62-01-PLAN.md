---
phase: 62-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/SettingsPanel.tsx
  - frontend/src/components/HealthModal.tsx
  - frontend/src/components/__tests__/HealthModal.test.tsx
  - frontend/src/components/__tests__/App.test.tsx
  - frontend/src/components/__tests__/App.nav.test.tsx
  - frontend/src/style.css
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "SettingsPanel.tsx no longer exists in the codebase"
    - "HealthModal.tsx and its test no longer exist in the codebase"
    - "All vitest tests pass with zero failures"
    - "App.nav.test.tsx NAV-04 describe label says 'New Session' not 'New Tab'"
    - "NET-01 through NET-05 are marked [x] in REQUIREMENTS.md"
  artifacts:
    - path: "frontend/src/components/__tests__/App.test.tsx"
      provides: "Passing source-inspection tests for App.tsx"
    - path: "frontend/src/components/__tests__/App.nav.test.tsx"
      provides: "Passing navigation wiring tests for App.tsx"
  key_links:
    - from: "frontend/src/components/__tests__/App.test.tsx"
      to: "frontend/src/App.tsx"
      via: "raw source import"
      pattern: "import raw from '../../App.tsx\\?raw'"
    - from: "frontend/src/components/__tests__/App.nav.test.tsx"
      to: "frontend/src/App.tsx"
      via: "raw source import"
      pattern: "import raw from '../../App.tsx\\?raw'"
---

<objective>
Delete dead components (SettingsPanel.tsx, HealthModal.tsx) left orphaned by the quick task 260409-vop rewrite, remove their associated tests and CSS, fix all stale test assertions in App.test.tsx and App.nav.test.tsx to match the current App.tsx source, and verify REQUIREMENTS.md checkboxes are up to date.

Purpose: Close tech debt from quick-260409-vop, Phase 57, Phase 58, and Phase 60 rewrites that left dead code and stale tests in the codebase.
Output: Clean codebase with zero test failures, no dead components, accurate requirement tracking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead components and remove orphaned CSS</name>
  <files>
    frontend/src/components/SettingsPanel.tsx
    frontend/src/components/HealthModal.tsx
    frontend/src/components/__tests__/HealthModal.test.tsx
    frontend/src/style.css
  </files>
  <read_first>
    frontend/src/style.css
    frontend/src/components/SettingsPanel.tsx
    frontend/src/components/HealthModal.tsx
    frontend/src/components/__tests__/HealthModal.test.tsx
  </read_first>
  <action>
1. Delete `frontend/src/components/SettingsPanel.tsx` — orphaned component superseded by SettingsTab.tsx in Phase 58. No imports reference it anywhere in the codebase.

2. Delete `frontend/src/components/HealthModal.tsx` — dead component removed from App.tsx by quick task 260409-vop. No imports reference it outside its own test file.

3. Delete `frontend/src/components/__tests__/HealthModal.test.tsx` — tests for the deleted HealthModal component.

4. Edit `frontend/src/style.css` to remove two dead CSS blocks:
   a. Remove the `/* -- Settings panel overlay */` block containing `.settings-overlay { ... }` (approximately lines 304-313). This was the modal overlay for SettingsPanel which is now deleted. Keep all `.settings-panel` rules (they are used by SettingsTab.tsx).
   b. Remove the entire `/* -- Health Modal */` section AND the `/* -- HealthModal Enhancements (Phase 54) */` section. This spans from the comment `/* -- Health Modal ...*/` through `.health-modal__step-number { ... }` (approximately lines 998-1199). The next section after this block starts with `/* -- Tailscale Status Indicator */` — do NOT remove that or anything after it.

Use `rm` for file deletions. Use file editing for style.css changes.
  </action>
  <verify>
    <automated>test ! -f frontend/src/components/SettingsPanel.tsx && test ! -f frontend/src/components/HealthModal.tsx && test ! -f frontend/src/components/__tests__/HealthModal.test.tsx && ! grep -q 'health-modal' frontend/src/style.css && ! grep -q 'settings-overlay' frontend/src/style.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/SettingsPanel.tsx does not exist
    - frontend/src/components/HealthModal.tsx does not exist
    - frontend/src/components/__tests__/HealthModal.test.tsx does not exist
    - frontend/src/style.css contains NO lines matching `health-modal`
    - frontend/src/style.css contains NO lines matching `settings-overlay`
    - frontend/src/style.css still contains `.ts-status {` (Tailscale Status section preserved)
    - frontend/src/style.css still contains `.settings-panel {` (SettingsTab CSS preserved)
  </acceptance_criteria>
  <done>Three dead files deleted, two orphaned CSS blocks removed from style.css, all remaining CSS intact</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale test assertions in App.test.tsx and App.nav.test.tsx</name>
  <files>
    frontend/src/components/__tests__/App.test.tsx
    frontend/src/components/__tests__/App.nav.test.tsx
  </files>
  <read_first>
    frontend/src/App.tsx
    frontend/src/components/__tests__/App.test.tsx
    frontend/src/components/__tests__/App.nav.test.tsx
  </read_first>
  <action>
**Fix App.test.tsx** — 8 stale tests in the `Tailscale health integration (HEALTH-04)` describe block:

The quick task 260409-vop removed HealthModal from App.tsx entirely (replaced by LocalNetworkBanner). App.tsx no longer imports `Environment`, `HealthModal`, or uses `platform`/`env.platform`. The following tests now fail because they assert content that no longer exists in App.tsx:

1. `imports Environment from wailsjs runtime` — DELETE this test. App.tsx no longer imports Environment.
2. `imports HealthModal component` — DELETE this test. App.tsx no longer imports HealthModal.
3. `calls Environment() in Promise.all init` — DELETE this test. App.tsx no longer calls Environment().
4. `renders HealthModal in JSX` — DELETE this test. App.tsx no longer renders HealthModal.
5. `passes health prop to HealthModal` — DELETE this test. App.tsx no longer has HealthModal.
6. `passes platform prop to HealthModal` — DELETE this test. App.tsx no longer has HealthModal.
7. `passes onCheckAgain prop to HealthModal` — DELETE this test. App.tsx no longer has HealthModal.
8. `stores platform from Environment().platform` — DELETE this test. App.tsx no longer stores platform.

After deletion, the `Tailscale health integration (HEALTH-04)` describe block should retain these passing tests:
- `imports GetTailscaleStatus from wailsjs bindings`
- `calls GetTailscaleStatus in Promise.all init`
- `subscribes to tailscale:health event`
- `cleans up health event listener`
- `passes tailscaleHealth prop to SettingsTab`

Additionally, ADD a replacement test to verify the LocalNetworkBanner integration that replaced HealthModal:
```typescript
it('imports LocalNetworkBanner component', () => {
  expect(raw).toContain("import { LocalNetworkBanner } from './components/LocalNetworkBanner'")
})

it('renders LocalNetworkBanner in JSX', () => {
  expect(raw).toContain('<LocalNetworkBanner')
})
```

Rename the describe block from `Tailscale health integration (HEALTH-04)` to `Tailscale health and local network integration` since HealthModal is gone and LocalNetworkBanner is the replacement.

**Fix App.nav.test.tsx** — 3 stale tests in NAV-05 + 1 stale describe label in NAV-04:

NAV-04 describe label fix:
- Change `describe('NAV-04: New Tab sidebar button opens new-session modal', () => {` to `describe('NAV-04: New Session sidebar button opens new-session modal', () => {` (line 46).

NAV-05 — Settings is now a tab (not a modal). App.tsx uses `handleOpenSettings` which calls find-or-create pattern with `t.type === 'settings'` and `SETTINGS_TAB`, then renders `<SettingsTab`. It does NOT use `setShowSettings`, `<SettingsPanel`, `isOpen={showSettings}`, or `onClose={handleSettingsClose}`.

Replace the 3 failing NAV-05 tests:

Delete these:
```typescript
it('onSettings triggers showSettings state', () => {
  expect(raw).toContain('setShowSettings(true)')
})

it('renders SettingsPanel component', () => {
  expect(raw).toContain('<SettingsPanel')
})

it('SettingsPanel receives isOpen and onClose props', () => {
  expect(raw).toContain('isOpen={showSettings}')
  expect(raw).toContain('onClose={handleSettingsClose}')
})
```

Replace with:
```typescript
it('find-or-create pattern checks t.type === settings', () => {
  expect(raw).toContain("t.type === 'settings'")
})

it('uses SETTINGS_TAB constant', () => {
  expect(raw).toContain('SETTINGS_TAB')
})

it('renders SettingsTab component', () => {
  expect(raw).toContain('<SettingsTab')
})
```

After all changes, run `cd frontend && npx vitest run` and confirm 0 failures.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run 2>&1 | grep -E "^.* Tests " | grep -q "0 failed" || cd frontend && npx vitest run 2>&1 | grep "Tests" | grep -v "failed" | grep -q "passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` in frontend/ reports 0 failed tests
    - App.test.tsx does NOT contain the string `HealthModal`
    - App.test.tsx does NOT contain the string `Environment`
    - App.test.tsx does NOT contain the string `env.platform`
    - App.test.tsx contains `import { LocalNetworkBanner }`
    - App.test.tsx contains `<LocalNetworkBanner`
    - App.test.tsx describe block reads `Tailscale health and local network integration`
    - App.nav.test.tsx does NOT contain the string `SettingsPanel`
    - App.nav.test.tsx does NOT contain `setShowSettings`
    - App.nav.test.tsx does NOT contain `isOpen={showSettings}`
    - App.nav.test.tsx contains `t.type === 'settings'`
    - App.nav.test.tsx contains `SETTINGS_TAB`
    - App.nav.test.tsx contains `<SettingsTab`
    - App.nav.test.tsx NAV-04 describe reads `New Session sidebar button` (not `New Tab`)
    - NET-01 through NET-05 in .planning/REQUIREMENTS.md are `[x]` (already checked, no change needed)
  </acceptance_criteria>
  <done>All 11 previously-failing tests replaced with correct assertions, all vitest tests pass, NAV-04 describe label updated to "New Session"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — this phase is internal dead code cleanup and test maintenance only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | N/A | N/A | accept | No new trust boundaries, no user input, no runtime changes — pure dead code deletion and test fixes |
</threat_model>

<verification>
1. `test ! -f frontend/src/components/SettingsPanel.tsx` — file deleted
2. `test ! -f frontend/src/components/HealthModal.tsx` — file deleted
3. `test ! -f frontend/src/components/__tests__/HealthModal.test.tsx` — file deleted
4. `cd frontend && npx vitest run` — 0 test failures
5. `grep -c 'health-modal' frontend/src/style.css` — returns 0
6. `grep -c 'settings-overlay' frontend/src/style.css` — returns 0
7. `grep '\[x\].*NET-0[1-5]' .planning/REQUIREMENTS.md | wc -l` — returns 5
</verification>

<success_criteria>
- Zero dead component files remain (SettingsPanel.tsx, HealthModal.tsx, HealthModal.test.tsx all deleted)
- Zero orphaned CSS blocks (health-modal and settings-overlay removed from style.css)
- All vitest tests pass (0 failures, previously 11 failures)
- App.nav.test.tsx NAV-04 describe reads "New Session" not "New Tab"
- NET-01 through NET-05 confirmed as [x] in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/62-tech-debt-cleanup/62-01-SUMMARY.md`
</output>
