---
milestone: v3.4
audited: 2026-05-21
status: tech_debt
scores:
  requirements: 48/48
  phases: 5/5
  integration: 7/7
  flows: 3/3
gaps: []
tech_debt:
  - id: TD-1
    label: "Phase 120 — Wails desktop click-path UAT deferred to manual milestone audit"
    owner: user
    evidence: ".planning/phases/120-filebrowsertab-tsx-desktop-web/120-VERIFICATION.md (re_verification.human_items_still_deferred[0])"
  - id: TD-2
    label: "Phase 121 — Visual TokyoNight render + lipgloss border perceptual check deferred (user is colorblind, requires sighted spot-check)"
    owner: user
    evidence: ".planning/phases/121-tui-files-view/121-VERIFICATION.md (human_verification items 1-4)"
  - id: TD-3
    label: "Phase 122 — 22-step two-machine tailnet UAT deferred for manual operator run"
    owner: user
    evidence: ".planning/phases/122-remote-session-file-browse-wiring/122-VERIFICATION.md (human_verification items 1-4)"
  - id: TD-4
    label: "Phase 120 WARNINGS WR-01..WR-05 open (user-triage; not goal-blocking)"
    owner: user
    evidence: ".planning/phases/120-filebrowsertab-tsx-desktop-web/120-REVIEW.md + 120-VERIFICATION.md footer"
  - id: TD-5
    label: "Phase 122 deferred follow-on: remove client_remote_files.go::ExchangeJoinCodeAtURL JSON-vs-303 mismatch shim"
    owner: user
    evidence: ".planning/phases/122-remote-session-file-browse-wiring/122-VERIFICATION.md Deferred Items table row 2"
human_uat_pending:
  - "Phase 120 — Wails desktop click-path UAT (open Sessions panel → right-click → Open file browser → verify tab + breadcrumb + file list + previews)"
  - "Phase 121 — TUI visual + remote-session perceptual UAT (TokyoNight palette, lipgloss border, nav smoothness, filter feel, over-cap render at typical widths)"
  - "Phase 122 — Two-machine tailnet 22-step UAT (Machine A web-share + Machine B GUI + Machine B TUI; cross-surface parity check; failure-mode takeover)"
---

# Milestone v3.4 Audit — File Browser (Read-Only) + TUI Parity

**Milestone:** v3.4 (Phases 118-122)
**Audited:** 2026-05-21
**Status:** `tech_debt` (code-level passed; 3 user-acknowledged manual-UAT deferrals)
**Closes:** GitHub Issues #62 + v3.4 slice of #64

---

## 1. Executive Summary

All five v3.4 phases (118-122) merged with **all 48 requirements satisfied at the automated/source-verification layer**. Every cross-phase wire is verified end-to-end against the codebase. The three "human_needed" verification statuses (Phases 120, 121, 122) are explicit user-acknowledged deferrals of manual UATs to the milestone audit phase per AUTO mode — they are NOT code gaps and are classified as `tech_debt` rather than `unsatisfied`. The cross-surface parity rule (project memory: "GUI/TUI/CLI must stay in sync") is honoured: REMOTE-01..05 deliver desktop + TUI parity with Phase 120-06's web-share path, with byte-identical Go cross-surface parity tests + Playwright cross-browser observers providing the strongest possible automated evidence pending the deferred two-machine UAT.

**Final milestone status:** `tech_debt` — release-eligible after the three manual UATs are executed and recorded; no code-level blockers.

---

## 2. Phase-Level Roll-Up

| Phase | Plans | VERIFICATION status | Score | Requirements covered | Final classification |
|-------|-------|---------------------|-------|----------------------|----------------------|
| 118 — FS sandbox core + daemon routes | 5/5 | passed | 5/5 must-haves | 14/14 (FS-01..14) | **satisfied** |
| 119 — Webserver routes + capability plumbing | 2/2 | passed | 5/5 must-haves | 5/5 (WEB-01..05) | **satisfied** |
| 120 — FileBrowserTab.tsx (desktop + web) | 6/6 | passed (re-verified after Plan 06) | 5/5 must-haves | 14/14 (UI-01..14) | **satisfied** (with deferred manual UAT — tech_debt TD-1) |
| 121 — TUI Files view | 3/3 | human_needed | 5/5 must-haves | 10/10 (TUI-01..10; TUI-08 remote moved to Phase 122) | **satisfied** (visual + remote sign-off deferred — tech_debt TD-2) |
| 122 — Remote-session file browse wiring | 6/5 | human_needed | 5/5 requirements at code/test layer | 5/5 (REMOTE-01..05) | **satisfied** (22-step two-machine UAT deferred — tech_debt TD-3) |

**Phases:** 5/5 — all five phase artifacts (PLAN, SUMMARY, VERIFICATION) present and consistent.

---

## 3. Cross-Phase Integration Audit

### 3.1 Wiring Map (verified end-to-end against source)

| # | Integration check | Source-of-truth evidence | Status |
|---|-------------------|-------------------------|--------|
| 1 | Phase 118 daemon mux registers `/api/files/list`, `/stat`, `/read` + `HEAD /read` | `internal/daemon/api.go:144-147` — four `a.mux.HandleFunc("GET /api/files/{list,stat,read}", a.filesHandler.{List,Stat,Read})` + `HEAD /read` registrations; `a.filesHandler = files.NewHandler(...)` at line 81 closing over `engine.GetSessionWorkDir`+`files.NewSandbox` | **WIRED** |
| 2 | Phase 119 webserver mounts SAME handler under `requireFilesRead` | `internal/webserver/server.go:502-505` — same four mux routes wrapped with `ws.requireFilesRead(filesDispatch(...))`; `SetFilesHandler` at line 161 stores handler; `internal/daemon/api.go:420, 910` — `ws.SetFilesHandler(a.filesHandler)` called BEFORE `ws.Start()` at both `AutoStartWebServer` and `handleWebServerStart` sites; shared `a.filesHandler` pointer | **WIRED** (handler is the same `*files.Handler` instance from Phase 118; no duplication) |
| 3 | Phase 120 FileBrowserTab consumes `/api/files/*` via `FilesApiClient` | `frontend/src/components/FileBrowserTab.tsx:23` imports `FilesApiClient, FilesApiError, FileEntry` from `../lib/filesApi`; `.tsx:132` constructs `new FilesApiClient({ baseURL, capToken, pathPrefix })`; `frontend/src/lib/filesApi.ts:124,146,161,174` build URLs as `${baseURL}${pathPrefix}/list?...` etc. with default `pathPrefix='/api/files'` | **WIRED** |
| 4 | Phase 121 TUI consumes `/api/files/*` via `DaemonClient` (Unix socket) for local sessions | `internal/tui/files_cmds.go:28,86,94,115,137` — `tea.Cmd` factories accept a `FilesClient` interface; `*daemon.DaemonClient` satisfies it via duck-typing (and explicit compile-time guard in `files_client.go`); `internal/daemon/client.go` `ListFiles/StatFile/ReadFile/HeadFile` dial the daemon Unix-socket `/api/files/*` endpoints | **WIRED** |
| 5 | Phase 122 daemon proxy `/api/files/remote/{sid}/*` delegates to Phase 118 handler upstream | `internal/daemon/api.go:155-158` registers four remote-proxy routes; `internal/daemon/remote_files.go:101-130` (`handleRemoteFiles{List,Stat,Read}` → `proxyRemoteFiles`) reads cap+baseURL from `RemoteCapStore`, strips caller-supplied `?cap=` (CapInjectionPrevented), forwards to upstream's `/api/files/{op}` (the very Phase 118 routes) over Tailscale HTTPS; `internal/tui/remote_files_client.go` provides a parallel TUI-direct path that hits the SAME upstream Phase 118 routes | **WIRED** (both surfaces — daemon-proxy for desktop GUI + direct HTTPS for TUI — terminate at the Phase 118 handler on the remote peer) |
| 6 | Capability flow: cap with `files.read` → 200; cap without → 403 body `"files.read"` | Phase 118 `internal/capability/capability.go:30,48-53` — `PermFilesRead = "files.read"`, `HasPerm` whole-token comma-split; Phase 118 `internal/webserver/capability_mw.go:102-119` — `requireFilesRead` wrapper returns `"files.read capability required"` body on miss. Phase 119 tests `TestFilesRoutes_OwnerCapReturns200_*` (4 PASS) + `TestFilesRoutes_ViewerCapReturns403_*` (4 PASS, body asserted via `strings.Contains(body, "files.read")`). Phase 118 daemon-socket surface is auth-less by design (WEB-01); Phase 119 webserver surface is cap-gated (WEB-02..03); Phase 120 web-share path verified end-to-end DOM-level by Playwright scenario 14 (PermissionDeniedTakeover); Phase 121 TUI hits the daemon socket (no auth); Phase 122 TUI remote path verified via TUI `applyRemote401IfNeeded` + cross-surface parity test `TestRemoteFiles_CrossSurface_401Propagation`. | **WIRED** across all surfaces |
| 7 | E2E: web-share viewer browses + desktop GUI browses (local + remote) + TUI browses (local + remote) | Playwright `frontend/e2e/files-browser.spec.ts` — 15 scenarios × 3 browsers = 45 cells (scenarios 1-12 API; 13 owner web-share DOM; 14 viewer web-share DOM 403; 16+17 remote-peer upstream contract); Go cross-surface parity `internal/daemon/remote_files_parity_test.go` — daemon-proxy + tui.RemoteFilesClient byte-identical; Phase 121 `TestFiles_Integration_LocalSessionEndToEnd` for TUI local; Phase 120 desktop click-path deferred to manual UAT (TD-1) | **WIRED** (3 surfaces × {local, remote} = 6 paths; 5/6 covered by automation, 1 covered by manual UAT deferral) |

**Cross-phase wiring score:** 7/7 integration checks WIRED. Zero orphaned exports. Zero missing connections.

### 3.2 Export → Consumer Graph

```
Phase 118 (internal/files, internal/capability, internal/daemon)
  PROVIDES:
    - files.Sandbox + NewSandbox (os.OpenRoot TOCTOU-safe)
    - files.Handler + NewHandler + List/Stat/Read (HTTP handler layer)
    - files.FileEntry / FileListResponse (wire types)
    - capability.PermFilesRead + HasPerm
    - webserver.requireFilesRead middleware
    - daemon.API.filesHandler + four daemon-mux routes
    - daemon.DaemonClient.{ListFiles, StatFile, ReadFile, HeadFile}
    - SessionEngine.GetSessionWorkDir + sessionWorkDirs map
  CONSUMED BY:
    - Phase 119: requireFilesRead, files.Handler, a.filesHandler — VERIFIED (server.go:161,502-505; api.go:420,910)
    - Phase 120: files.FileEntry shape mirrored in frontend FilesApi (filesApi.ts) — VERIFIED
    - Phase 121: daemon.DaemonClient satisfies FilesClient — VERIFIED (files_client.go:21-28)
    - Phase 122: daemon proxy fetches `/api/files/{op}` upstream — VERIFIED (remote_files.go proxyRemoteFiles)
    - Phase 122: tui.RemoteFilesClient mirrors DaemonClient method signatures — VERIFIED

Phase 119 (internal/webserver)
  PROVIDES:
    - WebServer.SetFilesHandler(*files.Handler) setter
    - Four /api/files/* routes mounted under requireFilesRead
    - SetStaticAppFS (added Plan 120-04 for /app/ web-share)
  CONSUMED BY:
    - Phase 120: web-share React shell fetches /api/files/* under /app/ — VERIFIED (App.tsx:1190; Playwright scenarios 13-14)
    - Phase 122: remote desktop GUI hits these routes on the REMOTE peer over Tailscale HTTPS — VERIFIED

Phase 120 (frontend FileBrowserTab + lib/filesApi + lib/webMode)
  PROVIDES:
    - FilesApiClient (typed /api/files/* client)
    - FileBrowserTab orchestrator + 12 leaf components
    - useFilesCapability hook (4-state machine)
    - PermissionDeniedTakeover (verbatim "files.read permission required")
    - webMode.detectMode/readWebModeParams (URL-param-driven web mount)
    - data-testid taxonomy (file-browser-tab, file-browser-permission-denied, etc.)
  CONSUMED BY:
    - Phase 122: same FileBrowserTab reused with pathPrefix='/api/files/remote/{sid}' + EnableWebSharingTakeover — VERIFIED (App.tsx:1262, 1271)

Phase 121 (internal/tui)
  PROVIDES:
    - tabFiles tab + filesModel + handleFilesKey
    - FilesClient interface (loadDirCmd/readFileCmd/headFileCmd factories)
    - tea.Cmd discipline (TestFiles_NoSyncFSCalls static-grep gate)
    - help.go tabFiles keybindings group
  CONSUMED BY:
    - Phase 122: same handleFilesKey + Update pipeline for remote; RemoteFilesClient implements FilesClient interface — VERIFIED

Phase 122 (cross-surface remote)
  PROVIDES:
    - daemon.RemoteCapStore + 5 routes (POST /api/remote-files/caps + 4 proxy)
    - tui.RemoteFilesClient (HTTPS+cap; TLS 1.2+ pinned; cap redacted from errors)
    - tui.joinCodePromptModel (Bubble Tea modal)
    - frontend RemoteJoinCodeModal + EnableWebSharingTakeover
    - frontend lib/remoteSession (findRemoteSession + remoteBaseURLFor)
    - Wails App.ExchangeJoinCodeAtURL + App.RegisterRemoteCap bindings
  CONSUMED BY:
    - End-user surfaces (no further integration; this is the leaf phase)
```

### 3.3 Auth Protection Surface Review

| Surface | Routes | Auth model | Verified |
|---------|--------|-----------|----------|
| Daemon Unix socket (`/api/files/*`) | list/stat/read + HEAD | None (in-process trusted local IPC; WEB-01 explicit decision) | YES — auth-less by design; `TestAPI_Files*` (7 PASS) |
| Webserver Tailscale HTTPS (`/api/files/*`) | list/stat/read + HEAD | `requireFilesRead` middleware (cap-token with `files.read` perm) | YES — 17 `TestFilesRoutes_*` PASS; viewer cap → 403 + body "files.read"; missing cap → 401; non-GET/HEAD → 405; no CSP regression |
| Daemon proxy (`/api/files/remote/{sid}/*`) | list/stat/read + HEAD | Local socket auth-less + RemoteCapStore lookup (sid → {baseURL, cap}); caller-`?cap=` stripped | YES — 13 proxy tests + cross-surface parity test |
| Web-share static (`/app/...`) | React shell only | Mode-detected from `/app/?session=...&cap=...` URL params; ALL /api/files/* calls go through requireFilesRead | YES — Playwright scenarios 13+14 DOM-level |

**Unprotected sensitive surfaces:** None found. Daemon socket is intentionally auth-less per WEB-01 (local trust boundary); all network-facing surfaces are cap-gated.

---

## 4. E2E Flow Verification

### Flow 1: Web-share viewer browses files

```
1. Web-share viewer hits https://<host>/app/?session=<sid>&cap=<vcap>     [Phase 119 + 120-06]
2. App.tsx detectMode() → 'web'; fbBaseURL=window.location.origin; fbCapToken=webParams.capToken  [120-06]
3. FileBrowserTab mounts; FilesApiClient.listFiles → GET /api/files/list?session=<sid>&path=&cap=<vcap>  [120]
4. Webserver requireFilesRead checks Perms — if no files.read → 403 body "files.read capability required"  [118-03 + 119]
5. PermissionDeniedTakeover renders verbatim copy "files.read permission required"  [120-04]
   OR (with files.read) — entries[] render; click .md → MarkdownPreview (react-markdown + remark-gfm)  [120-04]
```
**Status:** **WIRED + AUTOMATED** (Playwright scenarios 13+14 × 3 browsers = 6 cells)

### Flow 2: Desktop GUI browses local session

```
1. User right-clicks session → "Open file browser" (TabBar/DaemonManagerPanel)  [120]
2. App.tsx detectMode() → 'desktop'; fbBaseURL='http://127.0.0.1:<relayPort>'; fbCapToken=undefined  [120-06]
3. FilesApiClient hits relay → relay mounts /api/files/* (Phase 120 CR-01 fix) → files.Handler  [120-04 + 118]
4. Entries render; preview pane shows text/markdown/image  [120-04]
```
**Status:** **WIRED + AUTOMATED for the relay-mount + handler path** (unit test `relay/server_files_test.go` PASS); **MANUAL UAT DEFERRED for click-path** (TD-1)

### Flow 3: Desktop GUI browses remote tailnet session

```
1. User clicks "Browse files" on remote session in RemoteSessionsPanel  [122-03]
2. App.tsx findRemoteSession → triggers RemoteJoinCodeModal  [122-03]
3. User pastes join code → App.ExchangeJoinCodeAtURL(baseURL, code) → 303 + cap  [122-03 + 122-01]
4. App.RegisterRemoteCap(sid, baseURL, cap) deposits to daemon's RemoteCapStore  [122-01]
5. FileBrowserTab opens with pathPrefix='/api/files/remote/<sid>'; baseURL=local daemon  [122-03 App.tsx:1271]
6. FilesApiClient hits local daemon /api/files/remote/<sid>/list → proxyRemoteFiles forwards
   to remote peer's /api/files/list (Phase 118 handler on remote machine) over Tailscale HTTPS  [122-01 remote_files.go]
7. Remote 200 → daemon 200 → React renders entries; 401 → EnableWebSharingTakeover  [122-03 + 120-04]
```
**Status:** **WIRED + AUTOMATED at network layer** (cross-surface parity tests + Playwright scenarios 16+17); **MANUAL TWO-MACHINE UAT DEFERRED** (TD-3)

### Flow 4: TUI browses local session

```
1. User presses 'f' on local session in Sessions list  [121-01 update.go:474]
2. handleContentKey:FilesOpen → builds filesModel with *daemon.DaemonClient → dispatches loadDirCmd  [121-01]
3. loadDirCmd → DaemonClient.ListFiles → daemon Unix-socket /api/files/list → files.Handler  [121 + 118]
4. filesListMsg → applyFilesListMsg → renderFilesTab (lipgloss two-pane, TokyoNight palette)  [121-02]
5. Enter on .md → headFileCmd → readFileCmd → glamour.Render  [121-02]
```
**Status:** **WIRED + AUTOMATED** (`TestFiles_Integration_LocalSessionEndToEnd` PASS); **MANUAL VISUAL/PERCEPTUAL UAT DEFERRED** (TD-2)

### Flow 5: TUI browses remote tailnet session

```
1. User presses 'f' on remote session  [122-04 update.go:474, 513]
2. FilesOpen branches: cap cached → fast path with NewRemoteFilesClient; cap absent → joinCodePromptModel  [122-04]
3. User types join code → submit → 303 → cap → cached in remoteCapStore + RemoteFilesClient constructed  [122-04]
4. Same handleFilesKey + applyFilesListMsg pipeline as local — RemoteFilesClient implements FilesClient interface  [122-02/122-04]
5. RemoteFilesClient.ListFiles → direct HTTPS to remote peer /api/files/list with cap header → Phase 118 handler  [122-04 remote_files_client.go]
6. 401 → applyRemote401IfNeeded → cap forgotten + verbatim status copy  [122-04]
```
**Status:** **WIRED + AUTOMATED at unit + cross-surface parity layers**; **MANUAL TWO-MACHINE UAT DEFERRED** (TD-3)

**Flow score:** 3/3 critical flows fully wired (web-share, desktop local, TUI local), with desktop-remote + TUI-remote wired and parity-verified at network layer but deferred for two-machine UAT. **Five surfaces × {local, remote} − web-only-applies-once = 5 distinct E2E flows, all wired.**

---

## 5. Three-Source Requirements Coverage Cross-Reference

For each of the 48 requirements, three sources were cross-checked:
1. **VERIFICATION.md status** — phase-level verifier disposition
2. **SUMMARY frontmatter `requirements:`/tags listing** — implementation evidence
3. **REQUIREMENTS.md checkbox + traceability table** — milestone-level claim

| Requirement | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|-------------|--------------|---------------------|------------------|-------|
| FS-01 | passed (118) | listed (118-01) | [x] Complete | **satisfied** |
| FS-02 | passed (118) | listed (118-04) | [x] Complete | **satisfied** |
| FS-03 | passed (118) | listed (118-02, 118-05) | [x] Complete | **satisfied** |
| FS-04 | passed (118) | listed (118-02, 118-05) | [x] Complete | **satisfied** |
| FS-05 | passed (118) | listed (118-02, 118-05) | [x] Complete | **satisfied** |
| FS-06 | passed (118) | listed (118-02, 118-05) | [x] Complete | **satisfied** |
| FS-07 | passed (118) | listed (118-02) | [x] Complete | **satisfied** |
| FS-08 | passed (118) | listed (118-01) | [x] Complete | **satisfied** |
| FS-09 | passed (118) | listed (118-01) | [x] Complete | **satisfied** |
| FS-10 | passed (118) | listed (118-03) | [x] Complete | **satisfied** |
| FS-11 | passed (118) | listed (118-03) | [x] Complete | **satisfied** |
| FS-12 | passed (118) | listed (118-05) | [x] Complete | **satisfied** |
| FS-13 | passed (118) | listed (118-03) | [x] Complete | **satisfied** |
| FS-14 | passed (118) | listed (118-04) | [x] Complete | **satisfied** |
| WEB-01 | passed (119) | listed (119-01) | [x] Complete | **satisfied** |
| WEB-02 | passed (119) | listed (119-01) | [x] Complete | **satisfied** |
| WEB-03 | passed (119) | listed (119-01) | [x] Complete | **satisfied** |
| WEB-04 | passed (119) | listed (119-01) | [x] Complete | **satisfied** |
| WEB-05 | passed (119) | listed (119-02) | [x] Complete | **satisfied** |
| UI-01 | passed (120 re-verified) | listed (120-04) | [x] Complete | **satisfied** |
| UI-02 | passed (120) | listed (120-03/04) | [x] Complete | **satisfied** |
| UI-03 | passed (120) | listed (120-03) | [x] Complete | **satisfied** |
| UI-04 | passed (120) | listed (120-03) | [x] Complete | **satisfied** |
| UI-05 | passed (120) | listed (120-03) | [x] Complete | **satisfied** |
| UI-06 | passed (120) | listed (120-04) | [x] Complete | **satisfied** |
| UI-07 | passed (120) | listed (120-04) | [x] Complete | **satisfied** |
| UI-08 | passed (120) | listed (120-04) | [x] Complete | **satisfied** |
| UI-09 | passed (120) | listed (120-04) | [x] Complete | **satisfied** |
| UI-10 | passed (120) | listed (120-04) | [x] Complete | **satisfied** |
| UI-11 | passed (120 re-verified after Plan 06) | listed (120-04, 120-06) | [x] Complete | **satisfied** (upgraded from PARTIAL via Plan 06) |
| UI-12 | passed (120) | listed (120-03/04) | [x] Complete | **satisfied** |
| UI-13 | passed (120) | listed (120-04) | [x] Complete | **satisfied** |
| UI-14 | passed (120) | listed (120-05, 120-06) | [x] Complete | **satisfied** (45 cells across chromium/firefox/webkit) |
| TUI-01 | passed at code (121) | listed (121-01, 121-02) | [x] Complete | **satisfied** |
| TUI-02 | passed at code (121) | listed (121-01) | [x] Complete | **satisfied** |
| TUI-03 | passed at code (121) | listed (121-01, 121-02) | [x] Complete | **satisfied** |
| TUI-04 | passed at code (121) | listed (121-02) | [x] Complete | **satisfied** |
| TUI-05 | passed at code (121) | listed (121-02) | [x] Complete | **satisfied** |
| TUI-06 | passed at code (121) | listed (121-01, 121-02) | [x] Complete | **satisfied** |
| TUI-07 | passed at code (121) | listed (121-01, 121-03) | [x] Complete | **satisfied** |
| TUI-08 | Phase 121 marked PARTIAL local-only with toast; Phase 122 supersedes with REMOTE-03/04 | listed (121-02 for local; 122-04 for remote) | [x] Complete | **satisfied** (TUI-08 local-half satisfied by Phase 121; remote-half delivered by Phase 122 REMOTE-03/04) |
| TUI-09 | passed at code (121) | listed (121-02) | [x] Complete | **satisfied** |
| TUI-10 | passed at code (121) | listed (121-01, 121-03) | [x] Complete | **satisfied** |
| REMOTE-01 | passed at code (122) | listed (122-01, 122-03) | [x] Complete | **satisfied** (manual two-machine UAT deferred — TD-3) |
| REMOTE-02 | passed at code (122) | listed (122-03) | [x] Complete | **satisfied** (manual UAT deferred — TD-3) |
| REMOTE-03 | passed at code (122) | listed (122-04) | [x] Complete | **satisfied** (manual UAT deferred — TD-3) |
| REMOTE-04 | passed at code (122) | listed (122-04) | [x] Complete | **satisfied** (manual UAT deferred — TD-3) |
| REMOTE-05 | passed at code (122) | listed (122-05) | [x] Complete | **satisfied** (cross-surface byte-equivalence proven by 3 independent observers; manual UAT deferred — TD-3) |

**Requirements score:** 48/48 satisfied. Zero unsatisfied requirements. Zero requirements with checkbox/SUMMARY/REQUIREMENTS drift.

---

## 6. Requirements Integration Map

| Requirement | Integration path | Status | Issue |
|-------------|------------------|--------|-------|
| FS-01..FS-09, FS-14 | Self-contained in Phase 118 internal/files + internal/daemon; consumed by Phases 119/120/121/122 | WIRED | — |
| FS-10..FS-13 | capability primitives → webserver requireFilesRead (Phase 118) → mounted in Phase 119; consumed by Phase 120 viewer-403 + Phase 122 401-propagation | WIRED | — |
| WEB-01 | daemon-socket `/api/files/*` (Phase 118) → TUI DaemonClient (Phase 121) + daemon proxy (Phase 122) | WIRED | — |
| WEB-02 | Phase 118 files.Handler → Phase 119 SetFilesHandler at both AutoStartWebServer + handleWebServerStart → server.go:502-505 routes | WIRED | — |
| WEB-03 | Phase 118 requireFilesRead → mounted Phase 119 → exercised end-to-end Phase 120 Playwright scenario 14 | WIRED | — |
| WEB-04 | Phase 119 webserver mux runs on every tailnet peer → reached by Phase 122 daemon proxy AND tui.RemoteFilesClient on the REMOTE machine | WIRED | — |
| WEB-05 | Phase 119 CSP defense-in-depth (NoCSPHeader + NoHTMLContentType) + Phase 120 cross-browser Playwright zero-violations | WIRED | — |
| UI-01..UI-13 | Phase 120 self-contained FileBrowserTab + leaf components | WIRED | — |
| UI-14 | Phase 120 Playwright fixture → consumes Phase 119 mounted routes via fixture's webserver | WIRED | — |
| UI-11 (cross-cutting) | Phase 120 fetch() pattern + Phase 120-06 web mode + Phase 122 desktop GUI remote tab gate | WIRED | — |
| TUI-01..TUI-07, TUI-09, TUI-10 | Phase 121 self-contained tui sub-model + tea.Cmd discipline; consumes Phase 118 DaemonClient | WIRED | — |
| TUI-08 (local half) | Phase 121 DaemonClient → daemon Unix-socket → Phase 118 files.Handler | WIRED | — |
| TUI-08 (remote half) | Phase 122 RemoteFilesClient → Tailscale HTTPS → remote peer's Phase 119 cap-gated routes → remote Phase 118 files.Handler; previous Phase 121 toast removed | WIRED | — (remote half explicitly delivered via REMOTE-03/04) |
| REMOTE-01 | Phase 122-03 RemoteJoinCodeModal + App.tsx tab gate + Phase 122-01 RemoteCapStore + Phase 122-01 daemon proxy → Phase 118 handler upstream | WIRED | — |
| REMOTE-02 | Phase 122-03 EnableWebSharingTakeover (401 → verbatim copy) + Phase 122-01 401 propagation through proxy | WIRED | — |
| REMOTE-03 | Phase 122-04 joinCodePromptModel + RemoteFilesClient + FilesOpen remote branch; v3.4 toast removed from update.go (grep = 0) | WIRED | — |
| REMOTE-04 | Phase 122-02/04 FilesClient interface — same handleFilesKey + Update pipeline drives both clients; integration tests cover local; cross-surface parity covers remote | WIRED | — |
| REMOTE-05 | 3-observer cross-surface parity: daemon proxy (Go) + tui.RemoteFilesClient (Go) + Playwright HTTPS (browser) all observe byte-identical responses against shared fixture | WIRED | — |

**Requirements with no cross-phase wiring (self-contained within a single phase):**
FS-01, FS-02, FS-07, FS-08, FS-09, FS-14 (Phase 118 internals); UI-02, UI-03, UI-04, UI-05, UI-12, UI-13 (Phase 120 UI internals); TUI-01, TUI-02, TUI-05, TUI-09, TUI-10 (Phase 121 TUI internals). These are intentionally self-contained per their requirement text and are NOT missing connections — they are leaf nodes in the architecture graph.

---

## 7. Anti-Pattern + Code-Smell Audit

Source-grep across all v3.4 modified files for `TODO|FIXME|XXX|HACK|TBD|PLACEHOLDER|STUB`:

| Phase | Files searched | Hits | Disposition |
|-------|----------------|------|-------------|
| 118 | internal/files/*, internal/capability/capability.go, internal/webserver/capability_mw.go, internal/daemon/engine.go, plugin_settings.go, api.go, client.go | 0 | Clean |
| 119 | internal/webserver/server.go, capability_mw.go, files_routes_test.go | 0 | Clean |
| 120 | frontend/src/components/FileBrowserTab.tsx + FileBrowser/* + lib/filesApi.ts + lib/webMode.ts + App.tsx | 0 in Plan 06 modified files | Clean (WR-01..WR-05 are review WARNINGS, not source debt markers — see TD-4) |
| 121 | internal/tui/files.go, files_cmds.go, help.go | 0 | Clean |
| 122 | internal/daemon/remote_caps.go, remote_files.go; internal/tui/remote_files_client.go, joincode_prompt.go; frontend/src/components/RemoteJoinCodeModal.tsx, EnableWebSharingTakeover.tsx, lib/remoteSession.ts | 0 | Clean |

**Static-grep gates:**
- Phase 118: `TestHasPerm_NoStringsContains` source-inspects to forbid `strings.Contains` in `HasPerm` — passes
- Phase 118: `TestRequireCapability_UnchangedByPhase118` source-inspects to assert separation — passes
- Phase 119: `requireCapability` body grep for `files.read` returns 0 — passes (invariant held)
- Phase 121: `TestFiles_NoSyncFSCalls` grep against `os.{ReadDir,Open,OpenFile,Stat}` in files.go+files_cmds.go returns 0 — passes
- Phase 122: 15 source-grep gates (TLS 1.2+ pin, cap redaction count, route registration counts, descope-marker removal, v3.4 toast removal, etc.) all pass

---

## 8. Tech Debt Summary

| ID | Item | Owner | Action |
|----|------|-------|--------|
| TD-1 | Phase 120 — Wails desktop click-path UAT | user | Run manual UAT; on PASS, no further action. On FAIL, file issue against scottkw/agenthub. |
| TD-2 | Phase 121 — Visual TokyoNight + lipgloss + remote-session perceptual UAT (5 items) | user | Operator (sighted helper) executes the 5-item visual UAT block from 121-VERIFICATION.md `human_verification:` |
| TD-3 | Phase 122 — 22-step two-machine tailnet UAT | user | Operator executes UAT per 122-VERIFICATION.md (Machine A web-share + Machine B GUI + Machine B TUI + cross-surface parity + failure-mode) |
| TD-4 | Phase 120 — WARNINGS WR-01..WR-05 triage (`/app/` dir listings, cache-control, joinPath sanitization, mtime fallback, comment clarity) | user | Triage: file v3.5 follow-on issues OR schedule a fix-now plan. Severity = WARNING; none goal-blocking. |
| TD-5 | Phase 122 — Remove `ExchangeJoinCodeAtURL` JSON-vs-303 mismatch shim | user | File follow-on issue for v3.5 (out of Plan 05 scope; was forward-compat shim added in Plan 03 before Plan 01-recovery landed) |

---

## 9. Final Scoring

- **Requirements:** 48/48 satisfied
- **Phases:** 5/5 complete
- **Cross-phase integration:** 7/7 checks WIRED
- **E2E flows:** 3/3 fully automated + 2 wired-and-parity-verified-at-network-layer pending manual two-machine UAT (TD-3)
- **Code-level blockers:** 0
- **Deferred manual UATs:** 3 (TD-1, TD-2, TD-3) — all user-acknowledged per AUTO mode
- **Open warnings (advisory, non-blocking):** TD-4 (5 frontend warnings), TD-5 (1 cleanup shim)

---

## AUDIT COMPLETE

**Rationale:** Every code-level requirement in v3.4 (Phases 118-122) is satisfied with traceable evidence across VERIFICATION.md status, SUMMARY frontmatter, and REQUIREMENTS.md traceability — no drift. Every cross-phase wire is verified by direct source-of-truth inspection (daemon api.go, webserver server.go, FileBrowserTab.tsx, files_cmds.go/files_client.go, remote_files.go). All five E2E flows are wired; three are fully automated (web-share viewer, TUI local end-to-end, daemon proxy cross-surface parity); the remaining two (desktop click-path, two-machine tailnet) are deferred by explicit user choice per AUTO mode to the manual UAT block of the milestone audit, classified as `tech_debt` rather than `unsatisfied`. The cross-surface parity rule from project memory is honoured: GUI, TUI, and web-share surfaces share a single canonical `/api/files/*` contract proven byte-identical by three independent network-stack observers. Status: **`tech_debt`** — milestone is release-eligible after the three deferred UATs (TD-1, TD-2, TD-3) are executed by the operator.
