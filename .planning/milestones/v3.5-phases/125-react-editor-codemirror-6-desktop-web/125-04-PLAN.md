---
phase: 125-react-editor-codemirror-6-desktop-web
plan: 04
type: execute
wave: 4
depends_on: ["125-03"]
files_modified:
  - frontend/src/lib/filesApi.ts
  - frontend/src/lib/useFilesWrite.ts
  - frontend/src/components/FileBrowser/FileRow.tsx
  - frontend/src/components/FileBrowser/InlineNameInput.tsx
  - frontend/src/components/FileBrowser/BreadcrumbBar.tsx
  - frontend/src/components/FileBrowser/modals/DeleteConfirmModal.tsx
  - frontend/src/components/FileBrowser/modals/CollisionConfirmModal.tsx
  - frontend/src/components/FileBrowser/modals/MoveToPickerModal.tsx
  - frontend/src/components/FileBrowserTab.tsx
  - frontend/src/components/FileBrowser/__tests__/FileRowActions.test.tsx
autonomous: true
requirements: [EDIT-09, EDIT-12]
must_haves:
  truths:
    - "A create-file and a new-folder inline name input commit on Enter / cancel on Escape and call mkdir / write"
    - "Rename (F2 or row action) edits in place; a 409 collision opens the replace modal with Cancel as default focus"
    - "Delete on a file confirms via modal; delete on a directory states the recursive file count N before confirming"
    - "Cross-directory move via the Move to… picker calls rename with both paths; 409 → replace modal"
    - "Every write affordance (create / mkdir / rename / move / delete) is rendered only when canWrite is true"
  artifacts:
    - path: "frontend/src/components/FileBrowser/modals/DeleteConfirmModal.tsx"
      provides: "file + recursive-dir delete confirm; count in dir variant"
      contains: "files inside"
    - path: "frontend/src/components/FileBrowser/modals/CollisionConfirmModal.tsx"
      provides: "409 replace modal, Cancel default focus"
      contains: "already exists"
    - path: "frontend/src/components/FileBrowser/modals/MoveToPickerModal.tsx"
      provides: "directory-tree move picker"
      contains: "Move here"
    - path: "frontend/src/components/FileBrowser/InlineNameInput.tsx"
      provides: "inline create/mkdir/rename input row"
      contains: "InlineNameInput"
  key_links:
    - from: "FileRowActions / BreadcrumbBar toolbar"
      to: "useFilesWrite del/rename/mkdir"
      via: "gated on canWrite"
      pattern: "canWrite"
    - from: "Move to… picker"
      to: "useFilesWrite.rename"
      via: "rename(oldRel, newRel) cross-dir move"
      pattern: "rename"
---

<objective>
Directory write affordances: create file, mkdir, delete (file + recursive directory with file count), rename (F2/inline), and cross-directory move via the Move to… picker. All gated on canWrite. 409 collisions surface the replace modal with Cancel default focus.

Purpose: Deliver EDIT-09 — the full non-upload write-affordance suite — reusing the QuitConfirmModal pattern and the inline-input UX from the UI-SPEC.
Output: del/rename/mkdir in useFilesWrite + filesApi, FileRowActions, InlineNameInput, toolbar buttons, three modals, FileBrowserTab wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-RESEARCH.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-PATTERNS.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-UI-SPEC.md

<interfaces>
<!-- Reuse verbatim. -->
filesApi: add del/rename/mkdir mirroring buildQuery + fetchOrThrow (PATTERNS §filesApi). rename POSTs JSON {oldRel, newRel} (matches server renameRequest, write.go:171-174); rename is move-capable (both paths sandbox-validated, server Rename) — cross-dir move is a rename, NOT copy+delete. Server returns 409 on fs.ErrExist (write.go:244-245) → FilesApiError.isCollision().
FileRow.tsx: iconFor() + .file-browser__row-icon (30-39,110-119); 14px heroicons. Add FileRowActions cluster revealed on :hover/:focus-within, gated canWrite (&& !isDir && !isBinary for Edit). Pass new props OPTIONAL so existing callers/tests don't break.
BreadcrumbBar.tsx: .file-browser__breadcrumb-actions toolbar (122-142) — mount NewFile (DocumentPlusIcon) / NewFolder (FolderPlusIcon) before the existing refresh icon, canWrite only.
Recursive-dir count (RESEARCH Open Q3 — RESOLVED): count via a client-side listFiles walk before opening DeleteConfirmModal (no server change).
COPY (verbatim, UI-SPEC §Copywriting): New file / New folder / placeholders "Filename…" / "Folder name…" / "Move to…". Delete file title "Delete file?" body `Delete "{name}"? This cannot be undone.` Delete dir title "Delete folder?" body `Delete "{name}" and all {N} files inside? This cannot be undone.` Delete primary "Delete" / cancel default-focus "Cancel". Collision title "File already exists" body `A file named "{name}" already exists. Replace it?` primary "Replace" / cancel DEFAULT-focus "Cancel". Move title `Move "{name}" to…` / primary "Move here" / "Cancel". Generic failure "Couldn't complete that. Try again."
QuitConfirmModal.tsx — modal pattern (overlay/Escape/safe-default-focus/acting guard). Destructive button (#f7768e) NEVER default-focused.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: filesApi del/rename/mkdir + useFilesWrite + InlineNameInput + FileRowActions + toolbar</name>
  <read_first>
    frontend/src/lib/filesApi.ts (buildQuery, fetchOrThrow, isCollision) ; frontend/src/lib/useFilesWrite.ts (Plan 03 stubs for del/rename/mkdir) ; frontend/src/components/FileBrowser/FileRow.tsx (iconFor, row-icon) ; frontend/src/components/FileBrowser/BreadcrumbBar.tsx (breadcrumb-actions toolbar 122-142)
  </read_first>
  <behavior>
    - del(sid, path) DELETEs; rename(sid, oldRel, newRel) POSTs JSON; mkdir(sid, path) POSTs — all throw FilesApiError(409) on collision.
    - InlineNameInput commits on Enter, cancels on Escape; create-file → write empty file, new-folder → mkdir.
    - FileRowActions (Edit/Rename/Move/Delete icon buttons) appear on row hover/focus only when canWrite; Edit additionally requires !isDir && !isBinary.
    - BreadcrumbBar toolbar shows New file / New folder buttons only when canWrite.
  </behavior>
  <action>
    Add del/rename/mkdir to filesApi.ts (buildQuery + fetchOrThrow; rename POSTs `{oldRel, newRel}` JSON per server renameRequest). Fill the Plan 03 stub members of useFilesWrite with real implementations. Create InlineNameInput.tsx (Enter commits / Escape cancels, placeholders verbatim). Add a FileRowActions cluster to FileRow.tsx revealed on :hover/:focus-within (CSS on .file-browser__list-row), gated canWrite — new props OPTIONAL. Add New file / New folder icon buttons to the BreadcrumbBar toolbar (canWrite only, before refresh). Add a source unit test FileRowActions.test.tsx asserting the canWrite gating and the Edit-requires-!isBinary expression.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run FileRowActions && cd frontend && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && pnpm test -- --run FileRowActions` exits 0 and `pnpm exec tsc --noEmit` exits 0. `grep -c "canWrite" frontend/src/components/FileBrowser/FileRow.tsx` >= 1. `grep -c "oldRel" frontend/src/lib/filesApi.ts` >= 1. `grep -c "Filename…" frontend/src/components/FileBrowser/InlineNameInput.tsx` >= 1.
  </acceptance_criteria>
  <done>del/rename/mkdir API methods exist; inline create/mkdir/rename input works; row actions + toolbar buttons gated on canWrite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete / Collision / MoveTo modals + recursive count + FileBrowserTab wiring</name>
  <read_first>
    frontend/src/components/QuitConfirmModal.tsx (full pattern) ; frontend/src/components/FileBrowserTab.tsx (modal root, listFiles for the count walk) ; .planning/phases/125-react-editor-codemirror-6-desktop-web/125-UI-SPEC.md (§5 + Copywriting + Color colorblind table)
  </read_first>
  <behavior>
    - DeleteConfirmModal: file variant uses the file copy; directory variant states the recursive {N} file count (counted via a client listFiles walk before opening). Destructive Delete button; Cancel default focus.
    - CollisionConfirmModal (409 on rename/move): "File already exists" + `A file named "{name}" already exists. Replace it?`; Replace destructive; Cancel DEFAULT focus.
    - MoveToPickerModal: new-session-modal-style dialog with a sandbox-rooted dirs-only tree; "Move here" primary disabled until a target is chosen; calls rename(oldRel, newRel). 409 → CollisionConfirmModal.
    - All three reachable from FileRowActions; F2 triggers rename on the selected row.
  </behavior>
  <action>
    Create modals/DeleteConfirmModal.tsx (file + recursive-dir variants; the dir body interpolates the {N} count computed by a client-side listFiles walk in FileBrowserTab before opening), modals/CollisionConfirmModal.tsx (Cancel default focus, Replace destructive), and modals/MoveToPickerModal.tsx (directory tree, Move here primary) — all copying the QuitConfirmModal overlay/Escape/safe-default-focus/acting-guard pattern with copy strings VERBATIM from UI-SPEC. Wire all three plus the F2 rename trigger into FileBrowserTab.tsx. On any 409 from rename/move → open CollisionConfirmModal (Replace re-issues the op with overwrite semantics). Destructive buttons (#f7768e) never default-focused (colorblind/safety contract — verify at source). Extend FileRowActions.test.tsx (or add a modals test) asserting Cancel default focus on the collision modal and the {N}-count interpolation in the dir-delete body.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run FileRowActions && cd frontend && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && pnpm test -- --run FileRowActions` exits 0 and `pnpm exec tsc --noEmit` exits 0. `grep -c "files inside" frontend/src/components/FileBrowser/modals/DeleteConfirmModal.tsx` >= 1. `grep -c "already exists" frontend/src/components/FileBrowser/modals/CollisionConfirmModal.tsx` >= 1. `grep -c "Move here" frontend/src/components/FileBrowser/modals/MoveToPickerModal.tsx` >= 1.
  </acceptance_criteria>
  <done>Delete (file + recursive-dir with count), collision-replace, and cross-dir move modals work with safe default focus and verbatim copy; wired into the tab with F2 rename.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client rename/move/delete → write routes | untrusted source AND destination paths cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-10 | Tampering | rename/move destination-path traversal | mitigate | server Rename validates BOTH paths via validateAndClean (shipped 123, FSW-02); client cannot bypass — UI gate is advisory |
| T-125-11 | Tampering | recursive delete escaping the sandbox | mitigate | server Delete recursive walk stays within os.Root (shipped 123, FSW-04); client count-walk is read-only listFiles |
| T-125-12 | Tampering (data loss) | accidental destructive delete/replace | mitigate | every destructive op behind a modal with the SAFE choice default-focused; recursive-dir delete states the file count before confirming |
</threat_model>

<verification>
- `cd frontend && pnpm test` + `pnpm exec tsc --noEmit` green.
- Destructive buttons never hold default focus; all copy verbatim (colorblind/safety contract — verify at source).
- Cross-dir move uses rename, not copy+delete.
</verification>

<success_criteria>
Create file, mkdir, delete (file + recursive-dir with count), rename, and cross-dir move all work, gated on canWrite, with 409 → Cancel-default replace modal. (SC4)
</success_criteria>

<output>
Create `.planning/phases/125-react-editor-codemirror-6-desktop-web/125-04-SUMMARY.md` when done
</output>
