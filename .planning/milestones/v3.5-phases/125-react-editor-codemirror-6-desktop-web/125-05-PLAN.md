---
phase: 125-react-editor-codemirror-6-desktop-web
plan: 05
type: execute
wave: 5
depends_on: ["125-04"]
files_modified:
  - frontend/src/lib/filesApi.ts
  - frontend/src/lib/useFilesWrite.ts
  - frontend/src/components/FileBrowser/UploadQueuePanel.tsx
  - frontend/src/components/FileBrowser/UploadDropOverlay.tsx
  - frontend/src/components/FileBrowser/BreadcrumbBar.tsx
  - frontend/src/components/FileBrowserTab.tsx
  - frontend/src/components/FileBrowser/__tests__/Upload.test.tsx
autonomous: true
requirements: [EDIT-10, EDIT-12]
must_haves:
  truths:
    - "Single and multiple files upload via a hidden <input type=file multiple> and via drag-and-drop onto the directory listing"
    - "An upload queue shows per-file determinate N% progress, with done / failed states carrying icon + text"
    - "Each upload uses XMLHttpRequest (not fetch) for upload.onprogress events"
    - "An existing-name collision (409) shows a per-row Replace? prompt without blocking other files; over-cap (>50 MiB) skips that file with a clear message"
    - "Upload affordances are rendered only when canWrite is true"
  artifacts:
    - path: "frontend/src/components/FileBrowser/UploadQueuePanel.tsx"
      provides: "per-file progress queue (N%, done, failed)"
      contains: "%"
    - path: "frontend/src/components/FileBrowser/UploadDropOverlay.tsx"
      provides: "drag-and-drop target overlay"
      contains: "Drop files to upload here"
  key_links:
    - from: "useFilesWrite.upload"
      to: "XMLHttpRequest.upload.onprogress"
      via: "per-file N% events"
      pattern: "onprogress"
    - from: "UploadDropOverlay drop / Upload button"
      to: "useFilesWrite.upload"
      via: "FormData with one file part + dir field, gated canWrite"
      pattern: "FormData"
---

<objective>
Upload: single + multi-file via a hidden input and drag-and-drop into the listing, with a per-file progress queue (XHR, not fetch), 409 per-row replace prompts, and the over-cap skip message. Gated on canWrite.

Purpose: Deliver EDIT-10 — the last write affordance — completing the full write surface before the e2e gate.
Output: upload in useFilesWrite (XHR), filesApi upload helper, UploadQueuePanel, UploadDropOverlay, Upload toolbar button, FileBrowserTab wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-RESEARCH.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-PATTERNS.md
@.planning/phases/125-react-editor-codemirror-6-desktop-web/125-UI-SPEC.md

<interfaces>
<!-- Reuse verbatim. -->
Upload is the EXCEPTION to fetchOrThrow — fetch has no upload-progress event (RESEARCH Pitfall 6). Use XMLHttpRequest with xhr.upload.onprogress for per-file N%. The cap token rides as a query param via buildQuery (already includes cap when present, filesApi.ts:106).
Server Handler.Upload (write.go:105-131) takes ONE multipart `file` part + a `dir` field; 50 MiB MaxBytesReader before ParseMultipartForm (shipped 123, FSW-05/12). Multi-file = N XHRs into the queue (one file part each). 409 on existing name (fs.ErrExist).
BreadcrumbBar toolbar: add Upload (ArrowUpTrayIcon) icon button → triggers a hidden <input type="file" multiple>, canWrite only.
COPY (verbatim, UI-SPEC §Copywriting): Upload button "Upload". Queue title `Uploading {N} files` (or `Uploading {name}` single). Per-file done "Done" / failed "Failed — try again". Over-cap `"{name}" is too large (max 50 MB) and was skipped.` Drop overlay `Drop files to upload here`. Per-file 409 → inline Replace? within the queue row (does not block other files).
Colorblind contract: in-progress=N% text; done=CheckCircleIcon+"Done"; failed=ExclamationTriangleIcon+"Failed". Color reinforcement only. role=status aria-live for progress announcements.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: XHR upload in useFilesWrite + filesApi upload helper</name>
  <read_first>
    frontend/src/lib/filesApi.ts (buildQuery 102-108, cap query param 106) ; frontend/src/lib/useFilesWrite.ts (Plan 03 upload stub) ; .planning/phases/125-react-editor-codemirror-6-desktop-web/125-RESEARCH.md (Pitfall 6, Pattern 6)
  </read_first>
  <behavior>
    - upload(sid, dir, file, onProgress) posts a FormData with one `file` part + `dir` field via XMLHttpRequest; xhr.upload.onprogress reports N% to onProgress.
    - Resolves on 2xx; rejects with a 409-distinguishable error on collision and an over-cap-distinguishable error on 413/MaxBytes rejection.
    - Cap token threads via buildQuery as a query param.
  </behavior>
  <action>
    Add an `uploadFile(sid, dir, file, onProgress)` helper to filesApi.ts using XMLHttpRequest (NOT fetchOrThrow): build the URL with buildQuery (cap query param), set up xhr.upload.onprogress, post FormData with one `file` part + a `dir` field (matching server Handler.Upload). Map xhr.status 409 → collision and 413 → over-cap distinguishable rejections. Replace the Plan 03 upload stub in useFilesWrite with a queue-driving implementation that runs N uploads (one per file) and exposes per-file progress/status. Add a source unit test (Upload.test.tsx) asserting XHR usage (not fetch) and the FormData `file`+`dir` shape.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run Upload</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && pnpm test -- --run Upload` exits 0. `grep -c "XMLHttpRequest" frontend/src/lib/filesApi.ts` >= 1. `grep -c "onprogress" frontend/src/lib/filesApi.ts` >= 1. `grep -c "FormData" frontend/src/lib/filesApi.ts` >= 1.
  </acceptance_criteria>
  <done>upload uses XHR with per-file progress; collision and over-cap are distinguishable; cap token threads correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UploadQueuePanel + UploadDropOverlay + Upload toolbar button + tab wiring</name>
  <read_first>
    frontend/src/components/FileBrowser/BreadcrumbBar.tsx (toolbar) ; frontend/src/components/FileBrowserTab.tsx (list container, modal root) ; .planning/phases/125-react-editor-codemirror-6-desktop-web/125-UI-SPEC.md (§5 Upload + Copywriting + Color)
  </read_first>
  <behavior>
    - Upload toolbar button (canWrite only) triggers a hidden <input type=file multiple>; selecting files enqueues them.
    - Drag-over the list shows UploadDropOverlay ("Drop files to upload here"); drop enqueues the files.
    - UploadQueuePanel shows one row per file with a determinate N% bar + status glyph; done="Done", failed="Failed — try again". Over-cap files skip with the over-cap copy; per-file 409 shows an inline Replace? prompt that does not block other rows.
    - role=status aria-live announces progress.
  </behavior>
  <action>
    Create UploadDropOverlay.tsx (dashed #7aa2f7 border + ArrowUpTrayIcon, prompt verbatim) over the list container, and UploadQueuePanel.tsx (new-session-modal chrome, one row per file: N% bar + glyph + status text, per-file Replace? inline on 409, over-cap skip message). Add the Upload icon button + hidden multiple file input to BreadcrumbBar (canWrite only). Wire drag/drop handlers and the queue into FileBrowserTab.tsx, calling useFilesWrite.upload per file. Every status carries icon+text (colorblind contract — verify at source). On a per-file 409, show the inline Replace? prompt; Replace re-uploads with overwrite. Extend Upload.test.tsx asserting the drop-overlay prompt string, the N% progress rendering, and the done/failed icon+text states.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run Upload && cd frontend && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && pnpm test -- --run Upload` exits 0 and `pnpm exec tsc --noEmit` exits 0. `grep -c "Drop files to upload here" frontend/src/components/FileBrowser/UploadDropOverlay.tsx` >= 1. `grep -c "Failed — try again" frontend/src/components/FileBrowser/UploadQueuePanel.tsx` >= 1. `grep -c "is too large (max 50 MB)" frontend/src/components/FileBrowser/UploadQueuePanel.tsx` >= 1.
  </acceptance_criteria>
  <done>Single + multi upload via input and drag-drop with a per-file progress queue, 409 per-row replace, and over-cap skip; gated on canWrite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client upload (multipart + drag-drop) → upload route | untrusted filename + bytes cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-13 | Tampering | upload filename injection (../ in FileHeader.Filename) | mitigate | server sanitizes via filepath.Base + validateAndClean (shipped 123, FSW-05); client cannot bypass |
| T-125-14 | DoS | over-cap upload (>50 MiB) | mitigate | server MaxBytesReader before ParseMultipartForm (shipped 123, FSW-12); client surfaces the over-cap skip message, no truncated file |
| T-125-15 | Tampering | drag-drop directory / zip-slip surface | accept | browser multi-file drag loses directory structure; server takes one file part per request (no zip extraction); directory-zip upload is explicitly out of scope (REQUIREMENTS Out of Scope) |
</threat_model>

<verification>
- `cd frontend && pnpm test` + `pnpm exec tsc --noEmit` green.
- Upload uses XHR (not fetch); over-cap and 409 surface their verbatim messages.
- Upload affordances gated on canWrite (colorblind contract — status icon+text at source).
</verification>

<success_criteria>
Single + multi-file upload via input and drag-drop with per-file progress, 409 replace, and over-cap skip, all gated on canWrite. (SC4)
</success_criteria>

<output>
Create `.planning/phases/125-react-editor-codemirror-6-desktop-web/125-05-SUMMARY.md` when done
</output>
