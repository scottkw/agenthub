---
phase: 127-web-share-write-security-hardening
plan: 02
type: execute
wave: 2
depends_on: ["127-01"]
files_modified:
  - internal/files/sandbox_test.go
autonomous: true
requirements: [SEC-01, SEC-03, SEC-06]
must_haves:
  truths:
    - "WriteFileAtomic through a symlink escaping the sandbox root returns a non-nil error and creates nothing outside root"
    - "Rename whose destination resolves through an escaping symlink returns a non-nil error"
    - "Mkdir through an escaping symlink returns a non-nil error"
    - "A positive control proves the outside target is genuinely writable, so the negative assertion cannot pass via ENOENT masking"
    - "FuzzSandboxWrite (with the Plan 01 seed top-ups) reports zero crashes as the merge gate"
    - "SEC-03 upload coverage holds: existing TestUpload_* (multipart ../ filename sanitized via filepath.Base) and TestMaxUploadBytes_Is50MiB (over-cap rejected by MaxBytesReader before ParseMultipartForm, not truncated) stay green"
  artifacts:
    - path: "internal/files/sandbox_test.go"
      provides: "TestSandbox_WritePathSymlinkEscapeBlocked"
      contains: "TestSandbox_WritePathSymlinkEscapeBlocked"
  key_links:
    - from: "TestSandbox_WritePathSymlinkEscapeBlocked"
      to: "os.OpenRoot terminal boundary in WriteFileAtomic/Rename/Mkdir"
      via: "write through escape/ symlink → error, nothing outside root"
      pattern: "WriteFileAtomic\\(\"escape"
---

<objective>
Add the missing write-path symlink-escape test (SEC-01), confirm the finalized FuzzSandboxWrite corpus stays green (SEC-06), and confirm the already-built upload-abuse coverage holds (SEC-03 — multipart ../ filename sanitization + over-cap rejection are ALREADY-COVERED by existing tests; this phase finalizes the multipart-filename fuzz seed in Plan 01 and confirms the existing suite green). The existing TestSandbox_SymlinkEscapeBlocked (sandbox_test.go:217) covers only the READ path (sb.Open). No test asserts that WriteFileAtomic/Rename/Mkdir through an escaping symlink fails — SC1 requires it explicitly.

Purpose: Make the os.OpenRoot TOCTOU write-path boundary explicit. The protection already holds (every write method does os.OpenRoot(s.rootPath) then native root.OpenFile/root.Rename/root.Mkdir, which reject escaping symlinks atomically) — this test makes SC1 verifiable; it does NOT add protection or change production code.

Output: TestSandbox_WritePathSymlinkEscapeBlocked; confirmed fuzz merge gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-web-share-write-security-hardening/127-RESEARCH.md
@.planning/phases/127-web-share-write-security-hardening/127-PATTERNS.md

<interfaces>
<!-- Read-path analog to mirror: TestSandbox_SymlinkEscapeBlocked, sandbox_test.go:217-248 -->
Load-bearing elements: `if runtime.GOOS == "windows" { t.Skip(...) }`; `sb, root := newTestSandbox(t)` (helper line 26 — returns sandbox + its root path, seeds `a.txt`); `outside := t.TempDir()`; write `outside/secret`; POSITIVE CONTROL os.ReadFile(secretPath) == "leaked"; `os.Symlink(outside, filepath.Join(root, "escape"))` with `t.Skipf` on unsupported.

Sandbox write methods (do NOT modify — assert against them): `WriteFileAtomic(relPath string, content []byte, expectedValidator ...string) error` (sandbox.go:214); `Rename(oldRel, newRel string) error` (sandbox.go:320); `Mkdir(relPath string) error` (sandbox.go:347). All return non-nil error on escape.

newTestSandbox seeds `a.txt` inside root, usable as the Rename source.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TestSandbox_WritePathSymlinkEscapeBlocked</name>
  <read_first>
    - internal/files/sandbox_test.go:217-248 (TestSandbox_SymlinkEscapeBlocked — the read-path analog to mirror)
    - internal/files/sandbox_test.go:26 (newTestSandbox helper — returns sandbox + root, seeds a.txt)
    - 127-PATTERNS.md "TestSandbox_WritePathSymlinkEscapeBlocked" section (lines 137-171) — mirror read→write, add the outside-not-created assertions
  </read_first>
  <action>
    Add `TestSandbox_WritePathSymlinkEscapeBlocked` to sandbox_test.go, mirroring TestSandbox_SymlinkEscapeBlocked. Skip on Windows (symlink creation needs admin). Set up: `sb, root := newTestSandbox(t)`; `outside := t.TempDir()`; write a sentinel file `outside/secret` with content "leaked"; POSITIVE CONTROL — os.ReadFile(secretPath) and assert content == "leaked" so the negative assertions cannot pass merely because the target is missing; create `os.Symlink(outside, filepath.Join(root, "escape"))` with t.Skipf on unsupported.

    Through the `escape` symlink, assert each write operation returns a non-nil error: `sb.WriteFileAtomic("escape/pwned", []byte("x"))`, `sb.Rename("a.txt", "escape/pwned")`, `sb.Mkdir("escape/sub")`. After each, stat the corresponding outside path (`filepath.Join(outside, "pwned")`, `filepath.Join(outside, "sub")`) and assert it was NOT created (os.Stat returns an error / os.IsNotExist). Also re-read `outside/secret` and assert it still equals "leaked" (unmodified). Do NOT modify any production code — the os.Root boundary already enforces this.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test ./internal/files/ -run 'TestSandbox_WritePathSymlinkEscapeBlocked' -count=1 -v</automated>
  </verify>
  <done>Test passes on host (macOS): all three write ops through the escape symlink error out, nothing is created outside root, the sentinel is unmodified, and the positive control confirms the outside path is genuinely reachable.</done>
  <acceptance_criteria>
    - `go test ./internal/files/ -run TestSandbox_WritePathSymlinkEscapeBlocked -count=1` exits 0
    - `grep -c "TestSandbox_WritePathSymlinkEscapeBlocked" internal/files/sandbox_test.go` == 1
    - Test asserts non-nil error for WriteFileAtomic, Rename, and Mkdir through escape/
    - Test includes a positive-control read of outside/secret (guards against ENOENT masking)
    - No production source file (sandbox.go, write.go) modified by this plan
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Confirm FuzzSandboxWrite merge gate + SEC-03 upload-abuse coverage</name>
  <read_first>
    - internal/files/sandbox_test.go:367-441 (FuzzSandboxWrite — seeds finalized in Plan 01; this task only runs the gate)
    - internal/files/write_test.go (TestUpload_* upload-filename sanitization + TestMaxUploadBytes_Is50MiB over-cap — SEC-03 ALREADY-COVERED tests to confirm green)
    - 127-RESEARCH.md "Sampling Rate" (lines 196-199) + SEC-03 row (lines 42, 190) — SEC-03 is already-covered; confirm, do not re-implement
  </read_first>
  <action>
    Run the full internal/files test suite with -race, then the FuzzSandboxWrite merge gate at 60s. This is a confirmation task — no code changes unless the fuzzer surfaces a crash (in which case capture the failing input under testdata/fuzz and STOP per RULE 0, reporting the crashing seed rather than silently patching). The corpus was finalized in Plan 01 (case-variation + multipart seeds); this proves the gate holds after the symlink test lands.

    SEC-03 confirmation (already-covered — do NOT re-implement): run TestUpload_* and TestMaxUploadBytes_Is50MiB and confirm green. These assert multipart FileHeader.Filename ../ injection is sanitized via filepath.Base + name guard (write.go:158-168) and that an over-cap (>50 MiB) upload is rejected by http.MaxBytesReader BEFORE ParseMultipartForm (write.go:131-143) with a clear error, not a truncated file. If either is red, STOP per RULE 0 — a regression in already-shipped coverage is a finding, not a fix-in-place.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -race ./internal/files/... -count=1 && go test -fuzz=FuzzSandboxWrite -fuzztime=60s ./internal/files/</automated>
  </verify>
  <done>Full internal/files suite green under -race; FuzzSandboxWrite 60s reports zero crashes.</done>
  <acceptance_criteria>
    - `go test -race ./internal/files/... -count=1` exits 0
    - `go test -fuzz=FuzzSandboxWrite -fuzztime=60s ./internal/files/` reports 0 crashes (exits 0)
    - `go test ./internal/files/ -run 'TestUpload|TestMaxUploadBytes' -count=1` exits 0 (SEC-03 upload sanitization + over-cap coverage green)
    - No new crash corpus files under internal/files/testdata/fuzz (a new file means a real crash → RULE 0)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandbox root → outside filesystem | A symlink inside the root pointing outside is the classic write-TOCTOU escape; os.OpenRoot is the terminal boundary that rejects it atomically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-05 | Tampering/Elevation | WriteFileAtomic/Rename/Mkdir through escaping symlink | mitigate (verify) | os.OpenRoot + native root.* reject escaping symlinks atomically; TestSandbox_WritePathSymlinkEscapeBlocked makes the write-path guarantee explicit |
| T-127-06 | Tampering | fuzz-discoverable write-path traversal/denylist bypass | mitigate | FuzzSandboxWrite 60s merge gate with finalized corpus (rename-dest traversal, denylist-bypass, case-variation, multipart-filename seeds) → 0 crashes |
| T-127-12 | Tampering/DoS | upload abuse — multipart ../ filename injection + over-cap (>50 MiB) | mitigate (verify) | filepath.Base + name guard sanitizes FileHeader.Filename (write.go:158); MaxBytesReader before ParseMultipartForm rejects over-cap (write.go:131); no archive extraction = no zip-slip surface; confirmed via TestUpload_* + TestMaxUploadBytes_Is50MiB |
</threat_model>

<verification>
- `go test -race ./internal/files/... -count=1` green
- `go test -fuzz=FuzzSandboxWrite -fuzztime=60s ./internal/files/` zero crashes
- No production code modified
</verification>

<success_criteria>
SEC-01 (write-path symlink escape), SEC-03 (upload-abuse coverage confirmed), and SEC-06 (fuzz merge gate) closed: the three write methods provably reject escaping symlinks with nothing created outside root, upload-filename sanitization + over-cap rejection stay green, and the finalized fuzz corpus reports zero crashes.
</success_criteria>

<output>
Create `.planning/phases/127-web-share-write-security-hardening/127-02-SUMMARY.md` when done
</output>
