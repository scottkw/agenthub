---
phase: 127-web-share-write-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/files/sandbox.go
  - internal/files/write_test.go
  - internal/files/sandbox_test.go
autonomous: true
requirements: [SEC-02, SEC-06]
must_haves:
  truths:
    - "Writing/renaming/deleting the daemon's own config file inside a home-rooted sandbox returns ErrProtectedSystemFile on the host OS (macOS ~/Library/Application Support/agenthub, Linux ~/.config/agenthub, Windows AppData/Roaming/agenthub)"
    - "Case-varied denylist targets (.BASHRC, .Bashrc, .SSH/authorized_keys) return ErrProtectedSystemFile across all four write methods"
    - "A file literally named .bashrc inside a NON-home-rooted sandbox stays writable (no over-match)"
    - "FuzzSandboxWrite reports zero crashes with case-variation and multipart-filename seeds added"
  artifacts:
    - path: "internal/files/sandbox.go"
      provides: "denylistCheck with case-folded comparison + os.UserConfigDir-derived daemon config dir prefix"
      contains: "denylistCheck"
    - path: "internal/files/write_test.go"
      provides: "TestDenylist_DaemonConfigDir + case-variation denylist coverage"
      contains: "TestDenylist_DaemonConfigDir"
    - path: "internal/files/sandbox_test.go"
      provides: "FuzzSandboxWrite seed top-ups"
      contains: "FuzzSandboxWrite"
  key_links:
    - from: "internal/files/sandbox.go denylistCheck"
      to: "os.UserConfigDir"
      via: "local two-line derivation (no import of internal/daemon)"
      pattern: "os\\.UserConfigDir"
    - from: "WriteFileAtomic/Rename/Mkdir/Delete"
      to: "denylistCheck"
      via: "single chokepoint — one fix protects all four"
      pattern: "denylistCheck"
---

<objective>
Close the two genuine net-new production-code gaps in the write denylist (SEC-02), both isolated to `denylistCheck` in `internal/files/sandbox.go:96-148`, and finalize the FuzzSandboxWrite corpus (SEC-06) against the new case-variation vector.

Purpose: The denylist is the single chokepoint all four write methods funnel through (WriteFileAtomic:219, Rename src+dest, Mkdir, MkdirAll, Delete). On macOS the daemon's real config dir is `~/Library/Application Support/agenthub/` (os.UserConfigDir returns `~/Library/Application Support`, verified on host) — the current hardcoded `.config/agenthub/` never matches it, so a home-rooted session can overwrite the daemon's settings.json. The base-name comparison is also case-sensitive, so `.BASHRC` bypasses on case-insensitive macOS/Windows volumes.

Output: hardened `denylistCheck`, new + extended denylist tests, finalized fuzz corpus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-web-share-write-security-hardening/127-RESEARCH.md
@.planning/phases/127-web-share-write-security-hardening/127-PATTERNS.md

<interfaces>
<!-- denylistCheck current structure, sandbox.go:96-148 (extend in place, do NOT rewrite the EvalSymlinks CR-02 block). -->
The function builds `abs := filepath.Join(s.rootPath, cleaned)`, resolves `home` via os.UserHomeDir + EvalSymlinks, canonicalizes the existing parent (CR-02 block lines 114-122), computes `rel := filepath.Rel(home, canonAbs)`, returns nil if rel escapes `..`. Then:
- base-name exact switch: `.bashrc`, `.zshrc`, `.profile`, `.bash_profile`, `.zprofile`, `.zshenv`, `.bash_login` → ErrProtectedSystemFile
- dir-prefix loop over `[]string{".ssh/", ".claude/", ".config/agenthub/"}`: match if `relSlash == strings.TrimSuffix(dir,"/")` or `strings.HasPrefix(relSlash, dir)` → ErrProtectedSystemFile

daemonConfigDir derivation (engine.go:62, replicate locally — do NOT import internal/daemon):
`base, err := os.UserConfigDir(); if err != nil { base = os.TempDir() }; dir := filepath.Join(base, "agenthub")`

go.mod:140 has `golang.org/x/text v0.37.0` (indirect) — NFC via golang.org/x/text/unicode/norm is in-tree and available. Decision below is ASCII case-fold only (NFC documented as LOW residual in Plan 03 SECURITY artifact).

Test helpers (write_test.go): `setHomeEnv(t, dir)` (line 467) sets HOME + USERPROFILE; `isProtected(err)` (line 605) checks for "protected system file"; `denyTargets` table-test iterates WriteFileAtomic/Mkdir/Delete/Rename-into (lines 504-575). NON-home negative control: TestDenylist_NonHomeRootedUnaffected (line 581).

FuzzSandboxWrite write-specific seeds end at sandbox_test.go:440 (`f.Add("..%2f..%2f.bashrc")`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden denylistCheck — case-fold + os.UserConfigDir daemon config dir</name>
  <read_first>
    - internal/files/sandbox.go:96-148 (denylistCheck — the function being modified; preserve the CR-02 EvalSymlinks block at 114-122)
    - 127-PATTERNS.md "GAP 1 fix" + "GAP 2 fix" sections (lines 61-97) — the exact derivation and case-fold recipe
    - internal/daemon/engine.go:62-70 (daemonConfigDir — replicate the two-line os.UserConfigDir derivation locally; do NOT import internal/daemon)
  </read_first>
  <behavior>
    - `.BASHRC`, `.Bashrc`, `.BasHrc` (any case) under a home-rooted sandbox → ErrProtectedSystemFile
    - `.SSH/authorized_keys`, `.Claude/CLAUDE.md` (case-varied dir prefix) → ErrProtectedSystemFile
    - daemon config file under os.UserConfigDir()/agenthub/ (host-OS-correct path) when that dir is under $HOME → ErrProtectedSystemFile
    - a file named `.bashrc` in a sandbox rooted OUTSIDE $HOME → nil (no over-match — negative control must still pass)
  </behavior>
  <action>
    Modify ONLY `denylistCheck`. Do NOT touch WriteFileAtomic, Rename, Mkdir, MkdirAll, Delete, validateRelativePath, or any write.go handler (per SEC-02 scope — these are correct and tested; all four write methods already call denylistCheck, so one fix protects all).

    GAP 2 (case-fold): apply `strings.ToLower(base)` before the base-name switch (the protected names are all ASCII, so ToLower is safe and sufficient for the realistic macOS/Windows case vector). Apply `strings.ToLower(relSlash)` before the dir-prefix loop; the prefix slice entries are already lowercase. Do NOT add golang.org/x/text NFC normalization — ASCII case-fold closes the realistic vector; NFC is documented as LOW residual risk in the SECURITY artifact (Plan 03).

    GAP 1 (daemon config dir): replicate the engine.go:62 derivation locally — `cfgBase, err := os.UserConfigDir(); if err == nil { ... }` then `cfgDir := filepath.Join(cfgBase, "agenthub")`. Compute its $HOME-relative form with `filepath.Rel(home, cfgDir)` (use the same already-EvalSymlinks-resolved `home`), convert via `filepath.ToSlash`, and add that lowercased prefix (with trailing "/") to the dir-prefix set so it is checked alongside `.ssh/`, `.claude/`, `.config/agenthub/`. Keep the literal `.config/agenthub/` entry as cross-platform belt-and-suspenders. Skip adding the derived prefix only if filepath.Rel errors or the result escapes `..` (config dir not under $HOME — denylist does not apply). Do NOT import internal/daemon (internal/files must stay free of that dependency).

    Run gofmt; ensure golangci-lint clean.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && gofmt -l internal/files/sandbox.go | grep -q . && exit 1; go build ./internal/files/ && go vet ./internal/files/</automated>
  </verify>
  <done>denylistCheck compiles, gofmt-clean, vets clean; case-fold applied to both base switch and dir-prefix loop; os.UserConfigDir-derived agenthub prefix added; CR-02 EvalSymlinks block untouched; no internal/daemon import.</done>
  <acceptance_criteria>
    - `go build ./internal/files/` exits 0
    - `gofmt -l internal/files/sandbox.go` prints nothing
    - `grep -c "os.UserConfigDir" internal/files/sandbox.go` ≥ 1
    - `grep -c "internal/daemon" internal/files/sandbox.go` == 0 (no daemon import)
    - WriteFileAtomic/Rename/Mkdir/Delete bodies unchanged (diff touches only denylistCheck)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Denylist tests — daemon config dir + case variation; finalize fuzz corpus</name>
  <read_first>
    - internal/files/write_test.go:464-607 (setHomeEnv, TestDenylist_HomeRooted denyTargets table, isProtected, TestDenylist_NonHomeRootedUnaffected negative control)
    - internal/files/sandbox_test.go:367-441 (FuzzSandboxWrite seed list — append after line 440)
    - 127-PATTERNS.md write_test.go + FuzzSandboxWrite sections (lines 101-189) — guard platform path with runtime.GOOS, never hardcode ~/Library/Application Support
  </read_first>
  <action>
    In write_test.go, add `TestDenylist_DaemonConfigDir`: mirror the TestDenylist_HomeRooted setup (fakeHome := t.TempDir(); setHomeEnv(t, fakeHome)). Because setHomeEnv only sets HOME/USERPROFILE, also set the matching platform config-dir env so os.UserConfigDir() lands under the fake HOME — on Linux `t.Setenv("XDG_CONFIG_HOME", filepath.Join(fakeHome, ".config"))`, on macOS/Windows derive the expected relative path from os.UserConfigDir() and skip with t.Skip if the result is not under fakeHome (PITFALLS Pitfall 2: never hardcode the macOS path). Seed `<configdir>/agenthub/settings.json` and its parent dir, root the sandbox at fakeHome, and assert isProtected(err) for WriteFileAtomic/Rename-into/Delete of `agenthub/settings.json` expressed relative to the sandbox root.

    Add case-variation coverage: add a `caseDenyTargets` slice (`.BASHRC`, `.Bashrc`, `.SSH/authorized_keys`, `.Claude/CLAUDE.md`) and assert isProtected for WriteFileAtomic on each (seed prerequisite dirs/files as needed). The existing TestDenylist_NonHomeRootedUnaffected must remain unmodified and still pass.

    In sandbox_test.go, append fuzz seeds after line 440: `f.Add("../../.BASHRC")`, `f.Add("../../.Bashrc")`, `f.Add(".SSH/authorized_keys")`, `f.Add("../../../etc/passwd")` (multipart-filename-style injection). Do not change the fuzz body or merge gate.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test ./internal/files/ -run 'TestDenylist' -count=1 && go test -fuzz=FuzzSandboxWrite -fuzztime=60s ./internal/files/</automated>
  </verify>
  <done>TestDenylist_DaemonConfigDir + case-variation assertions pass on host OS; TestDenylist_NonHomeRootedUnaffected still green; FuzzSandboxWrite 60s reports zero crashes with new seeds.</done>
  <acceptance_criteria>
    - `go test ./internal/files/ -run TestDenylist -count=1` exits 0
    - `go test -fuzz=FuzzSandboxWrite -fuzztime=60s ./internal/files/` reports 0 crashes (exits 0)
    - `grep -c "TestDenylist_DaemonConfigDir" internal/files/write_test.go` == 1
    - `grep -c "\.BASHRC" internal/files/sandbox_test.go` ≥ 1 (fuzz seed present)
    - daemon-config-dir test guards platform path with runtime.GOOS / os.UserConfigDir derivation (no hardcoded "Library/Application Support" literal in the assertion)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| home-rooted session → host filesystem | A session whose sandbox root is at or under $HOME can target dotfiles/config that grant persistence or privilege; denylist is the method-level guard |
| case-insensitive volume → exact-match denylist | macOS/Windows FS treats `.BASHRC` == `.bashrc` at the inode level but a case-sensitive lexical denylist does not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Elevation | denylistCheck — macOS daemon config dir hole | mitigate | Add os.UserConfigDir()-derived `agenthub/` prefix to the protected dir-prefix set; covers macOS Library/Application Support, Linux .config, Windows AppData/Roaming |
| T-127-02 | Elevation | denylistCheck — case-variation bypass | mitigate | strings.ToLower on base name + relSlash before comparison; closes `.BASHRC`/`.Bashrc` on case-insensitive volumes |
| T-127-03 | Tampering | denylist over-match outside $HOME | accept | filepath.Rel `..` guard preserved; TestDenylist_NonHomeRootedUnaffected asserts non-home `.bashrc` stays writable |
| T-127-04 | Tampering | NFC/NFD Unicode denylist residual | accept | All protected names are ASCII; decomposition bypass requires a non-ASCII protected entry which none contain — documented LOW residual in 127-SECURITY.md |
</threat_model>

<verification>
- `go test ./internal/files/ -count=1` green
- `go test -fuzz=FuzzSandboxWrite -fuzztime=60s ./internal/files/` zero crashes
- Diff confirms only denylistCheck changed in sandbox.go (no write-method-body edits)
</verification>

<success_criteria>
SEC-02 (denylist case/config-dir) and SEC-06 (fuzz corpus finalized) closed: daemon config dir + case-varied targets return ErrProtectedSystemFile on the host OS, non-home sandbox unaffected, fuzz corpus green with case-variation + multipart seeds.
</success_criteria>

<output>
Create `.planning/phases/127-web-share-write-security-hardening/127-01-SUMMARY.md` when done
</output>
