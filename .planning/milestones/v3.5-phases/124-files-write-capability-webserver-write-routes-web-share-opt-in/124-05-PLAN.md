---
phase: 124-files-write-capability-webserver-write-routes-web-share-opt-in
plan: 05
type: execute
wave: 2
depends_on: ["124-02"]
files_modified:
  - internal/tui/files.go
  - internal/tui/files_test.go
autonomous: true
requirements: [CAP-06]

must_haves:
  truths:
    - "The TUI Files view shows a colorblind-safe home-dir write warning line (⚠ + literal 'Warning:') when files.write is active for a $HOME-cwd session"
    - "The warning line uses the verbatim 124-UI-SPEC TUI copy and the StatusWaiting amber token (color as reinforcement, glyph+text as primary signal)"
    - "The warning shows under the same HomeDir && FilesWrite condition the GUI banner uses (cross-surface parity)"
  artifacts:
    - path: "internal/tui/files.go"
      provides: "home-dir write warning line in renderFilesTab"
      contains: "Warning: cwd is $HOME"
    - path: "internal/tui/files_test.go"
      provides: "source/render assertion for the verbatim warning + glyph"
      contains: "Warning: cwd is $HOME"
  key_links:
    - from: "internal/tui/files.go renderFilesTab"
      to: "SessionInfo.HomeDir && SessionInfo.FilesWrite"
      via: "lookup files-view session by m.files.sessionID, show warning when both true"
      pattern: "HomeDir"
---

<objective>
Add the colorblind-safe home-directory write warning line to the TUI Files view, shown under the same condition as the GUI banner (`SessionInfo.HomeDir && SessionInfo.FilesWrite`). Cross-surface parity with the GUI banner is release-blocking.

Purpose: SC#4 TUI half / CAP-06 parity. The GUI banner (plan 124-04) and this TUI line both read the single server-computed signal added to `SessionInfo` in plan 124-02 — so the warning shows on both surfaces for the identical session/cwd condition.
Output: a warning line above `renderFilesStatusLine` in `renderFilesTab`, verbatim copy, StatusWaiting amber, plus a render/source test asserting the glyph + text token.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-files-write-capability-webserver-write-routes-web-share-opt-in/124-UI-SPEC.md
@.planning/phases/124-files-write-capability-webserver-write-routes-web-share-opt-in/124-PATTERNS.md

<interfaces>
<!-- Verified from live source. -->

internal/tui/files.go:
- `func (m Model) renderFilesTab(cw, ch int) string` (line 331) — assembles body + `status := m.renderFilesStatusLine(cw)` (line 358), returns `lipgloss.JoinVertical(lipgloss.Left, body, status)` (line 359). Insert the warning line between body and status.
- `func (m Model) renderFilesStatusLine(w int) string` (line 283) — lipgloss style + `truncate(...)` width-clamp pattern to mirror.
- `m.files.sessionID` (filesModel field, line 40) — the session the Files view is scoped to.
- `truncate(text, width)` helper used at lines 194, 288.

internal/tui/model.go:
- `m.sessions []daemon.SessionInfo` (line 118) — iterate to find the session whose ID == m.files.sessionID.

internal/daemon/types.go (after plan 124-02):
- `SessionInfo` carries `HomeDir bool json:"homeDir"` and `FilesWrite bool json:"filesWrite"` (added in plan 124-02 ListSessions population). The TUI reads these directly — no new daemon call.

internal/tui/styles.go:
- `m.styles.StatusWaiting` (line 62, amber `#8c6c3e`/`#e0af68`) — the warning foreground token. Color is REINFORCEMENT only.
- `m.styles.WebOn`/`WebOff` (lines 64/65) — for the optional `[writes ON]`/`[writes OFF]` parity nicety (Surface 5).

124-UI-SPEC.md § "TUI Surface 4" — VERBATIM single-line copy:
`⚠ Warning: cwd is $HOME — writes can affect dotfiles, SSH keys, and shell config. Protected files are blocked.`
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a TUI render change consuming a server-computed signal; no new trust boundary or input parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-17 | Information Disclosure (UX safety) | colorblind user cannot perceive the TUI warning | mitigate | The `⚠` glyph + literal "Warning:" text carry the signal; StatusWaiting amber is reinforcement only; NO bold-alone reliance. Verified at source level (grep glyph + text token). |
| T-124-18 | Repudiation (parity gap) | GUI shows banner but TUI does not (release-blocking) | mitigate | Both surfaces gate on the SAME server signal `SessionInfo.HomeDir && SessionInfo.FilesWrite`; the test asserts the TUI line renders when both are true. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Write Wave-0 failing TUI warning render test</name>
  <files>internal/tui/files_test.go</files>
  <read_first>
    - internal/tui/files_test.go (modify) — existing renderFilesTab / renderFilesStatusLine tests for harness shape
    - internal/tui/files.go — renderFilesTab line 331
    - 124-UI-SPEC.md § "TUI Surface 4" (verbatim copy + colorblind rule)
  </read_first>
  <behavior>
    - When the files-view session has HomeDir=true and FilesWrite=true, renderFilesTab output contains the verbatim "⚠ Warning: cwd is $HOME — writes can affect dotfiles, SSH keys, and shell config. Protected files are blocked."
    - When HomeDir=false OR FilesWrite=false, the warning line is absent.
    - The rendered warning contains both the `⚠` glyph and the literal "Warning:" token (colorblind-safe).
  </behavior>
  <action>Add a test to internal/tui/files_test.go that builds a Model with `m.files.sessionID` set and `m.sessions` containing a SessionInfo with that ID, then renders renderFilesTab. Sub-cases: (HomeDir=true, FilesWrite=true) → output contains the verbatim warning string AND the `⚠` glyph AND "Warning:"; (HomeDir=true, FilesWrite=false) → no warning; (HomeDir=false, FilesWrite=true) → no warning. This MUST fail now (the warning line does not exist) — run to confirm RED.</action>
  <verify>
    <automated>cd internal/tui && go test -run 'HomeDirWarning|FilesWarning|renderFilesTab' -count=1 . 2>&1 | grep -qE 'FAIL|undefined|build failed|Warning' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts the verbatim warning string and the `⚠` glyph.
    - Test covers the absent-warning negative cases.
    - Test is RED.
  </acceptance_criteria>
  <done>TUI warning render test exists, asserts verbatim copy + glyph + negative cases, and is RED.</done>
</task>

<task type="auto">
  <name>Task 1: Render home-dir write warning line in renderFilesTab</name>
  <files>internal/tui/files.go</files>
  <read_first>
    - internal/tui/files.go (modify) — renderFilesTab line 331 (status assembly 358-359), renderFilesStatusLine line 283 (lipgloss+truncate pattern)
    - internal/tui/model.go — m.sessions line 118
    - internal/tui/styles.go — StatusWaiting line 62
    - PATTERNS.md § "internal/tui/files.go (CAP-06 home-dir warning line — TUI parity, RELEASE-BLOCKING)"
  </read_first>
  <action>In renderFilesTab, before building the final JoinVertical, look up the SessionInfo in m.sessions whose ID equals m.files.sessionID. When that session's `HomeDir && FilesWrite` are both true, render a warning line with `lipgloss.NewStyle().Foreground(m.styles.StatusWaiting).Width(cw).Render(truncate(warningText, cw))` where warningText is the VERBATIM 124-UI-SPEC TUI copy: `⚠ Warning: cwd is $HOME — writes can affect dotfiles, SSH keys, and shell config. Protected files are blocked.` Insert this line into the JoinVertical between `body` and `status` (so it sits directly above the status line, with the file view it concerns). The `⚠` glyph + literal "Warning:" carry the signal — do NOT rely on amber alone, do NOT use bold-alone. When the condition is false, render nothing extra (status line unchanged). Optionally (Surface 5 parity nicety) append a `[writes ON]`/`[writes OFF]` text token to any per-session indicator using WebOn/WebOff — never a bare colored dot.</action>
  <verify>
    <automated>cd internal/tui && go test -run 'HomeDirWarning|FilesWarning|renderFilesTab' -count=1 . 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'Warning: cwd is $HOME' internal/tui/files.go` matches the verbatim string.
    - `grep -F '⚠' internal/tui/files.go` matches (glyph present in source).
    - The warning is gated on `HomeDir && FilesWrite`; absent when either is false.
    - `go test ./internal/tui/ -count=1` green.
  </acceptance_criteria>
  <done>TUI Files view shows the verbatim colorblind-safe warning when HomeDir && FilesWrite; tests green; parity condition identical to the GUI banner.</done>
</task>

</tasks>

<verification>
- `go test -race ./internal/tui/ -count=1` green.
- Source colorblind check: `grep -F '⚠' internal/tui/files.go` AND `grep -F 'Warning:' internal/tui/files.go` both match.
- `go vet ./internal/tui/` clean; `gofmt -l` no diffs on touched files.
</verification>

<success_criteria>
- SC#4 (TUI half / CAP-06 parity): home-dir write warning visible in the TUI for a $HOME-cwd session with files.write active, under the identical server signal the GUI banner uses; glyph + text token carry the signal (colorblind-safe).
</success_criteria>

<output>
Create `.planning/phases/124-files-write-capability-webserver-write-routes-web-share-opt-in/124-05-SUMMARY.md` when done
</output>
