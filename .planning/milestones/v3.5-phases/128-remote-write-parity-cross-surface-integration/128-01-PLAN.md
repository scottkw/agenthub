---
phase: 128-remote-write-parity-cross-surface-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tui/remote_files_client.go
  - internal/tui/files.go
  - internal/tui/remote_files_client_test.go
  - frontend/src/lib/filesApi.ts
  - frontend/src/lib/useFilesWrite.ts
  - frontend/src/components/FileBrowserTab.tsx
  - frontend/src/lib/filesApi.test.ts
  - frontend/src/lib/useFilesWrite.test.ts
autonomous: true
requirements: [RMW-04]
must_haves:
  truths:
    - "A write against a v3.4 remote peer (upstream HTTP 405) surfaces the verbatim message 'The remote session is running an older version of AgentHub that does not support file writes.' in BOTH the desktop GUI/web surface and the TUI"
    - "The 405 friendly message is byte-identical across Go and TS (single grep-gated const per language)"
    - "The 405 mapping is scoped to write verbs only (write/upload/delete/rename/mkdir); read/list 405s remain generic"
    - "The new 405 error message contains no cap token (CAP-LEAK invariant holds)"
  artifacts:
    - path: "frontend/src/lib/filesApi.ts"
      provides: "isMethodNotAllowed() predicate + REMOTE_PEER_OUTDATED_MESSAGE const"
      contains: "isMethodNotAllowed"
    - path: "internal/tui/remote_files_client.go"
      provides: "405 typed sentinel ErrRemotePeerNoWriteSupport + remotePeerOutdatedMessage const"
      contains: "ErrRemotePeerNoWriteSupport"
  key_links:
    - from: "frontend/src/lib/useFilesWrite.ts"
      to: "REMOTE_PEER_OUTDATED_MESSAGE"
      via: "isMethodNotAllowed() catch branch returns 'peer-outdated'"
      pattern: "isMethodNotAllowed"
    - from: "internal/tui/files.go"
      to: "ErrRemotePeerNoWriteSupport"
      via: "TUI write surface maps sentinel to verbatim copy"
      pattern: "ErrRemotePeerNoWriteSupport"
---

<objective>
Map an upstream HTTP 405 (a v3.4 remote peer that has no write routes) to the verbatim version-gate message on every write surface — Go TUI and the TS GUI/web surface — so a remote write against an older AgentHub peer shows a friendly "older version" message instead of an opaque 405 or a generic network error.

Purpose: RMW-04. Cross-surface parity is release-blocking (CLAUDE.md/MEMORY) — the message must be byte-identical in Go and TS.
Output: `isMethodNotAllowed()` predicate + verbatim const in TS; typed 405 sentinel + verbatim const in Go; new catch/dispatch branches surfacing the message; unit tests in both languages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-remote-write-parity-cross-surface-integration/128-RESEARCH.md
@.planning/phases/128-remote-write-parity-cross-surface-integration/128-PATTERNS.md

<interfaces>
<!-- Extracted from current source — executor uses these directly, no exploration needed. -->

frontend/src/lib/filesApi.ts (FilesApiError predicate block, lines 57-94):
  isUnauthorized(): boolean  // line 69 — status === 401
  isOverCap(): boolean       // line 82 — status === 413
  isConflict(): boolean      // line 87 — status === 412
  isCollision(): boolean     // line 92 — status === 409
  // NO isMethodNotAllowed yet — add it here mirroring these.

frontend/src/lib/useFilesWrite.ts:
  export type WriteOutcome = 'saved' | 'conflict' | 'error'   // line 35
  write(): catch block at line 117; isConflict() branch returns 'conflict' at line 120-125;
  generic branch at line 129-131 sets "Couldn't save the file. Your changes are still here — try again." (buffer NOT cleared — T-125-08 locked).

internal/tui/remote_files_client.go (4 write methods, each with a StatusCode != http.StatusOK block):
  WriteFile  func sig at line 221; status-check at line 232
  DeleteFile func sig at line 249; status-check at line 259
  RenameFile func sig at line 283; status-check at line 299
  MkdirFile  func sig at line 317; status-check at line 327
  TLS pinned at line 58 (MinVersion VersionTLS12) — DO NOT touch.
  Error strings interpolate ONLY (status, body) — never URL/cap (CAP-LEAK invariant).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Go RemoteFilesClient 405 sentinel + verbatim const + TUI surface copy</name>
  <read_first>
    - internal/tui/remote_files_client.go (the 4 write methods + their StatusCode != http.StatusOK blocks at lines 232/259/299/327)
    - 128-PATTERNS.md section "internal/tui/remote_files_client.go (RMW-04 + RMW-05 — Go parity)" (the sibling status-branch template)
    - internal/tui/files.go (the TUI write surface that consumes RemoteFilesClient errors — find where write errors render to the user)
  </read_first>
  <behavior>
    - Test: a fixture write method returning HTTP 405 makes WriteFile/DeleteFile/RenameFile/MkdirFile each return the sentinel ErrRemotePeerNoWriteSupport (errors.Is matches).
    - Test: the sentinel's message is exactly "The remote session is running an older version of AgentHub that does not support file writes." (verbatim SC3 string).
    - Test: a non-405 error (e.g. 500) still returns the existing generic "remote files write: 500 ..." formatted error (no regression).
    - Test: the 405 error string contains no cap token (assertNoCapInError discipline).
  </behavior>
  <action>
    Add a package-level verbatim const `remotePeerOutdatedMessage` set to the exact SC3 string "The remote session is running an older version of AgentHub that does not support file writes." and a sentinel `var ErrRemotePeerNoWriteSupport = errors.New(remotePeerOutdatedMessage)` in remote_files_client.go. In EACH of the 4 write methods (WriteFile, DeleteFile, RenameFile, MkdirFile), insert a check `if resp.StatusCode == http.StatusMethodNotAllowed` BEFORE the existing generic `!= http.StatusOK` block, returning the zero response value + ErrRemotePeerNoWriteSupport. Scope to write verbs ONLY — do NOT touch the read methods (ListFiles/StatFile/ReadFile/HeadFile) per Open Q2 (a v3.4 peer supports read). In internal/tui/files.go, where remote write errors render, map ErrRemotePeerNoWriteSupport (errors.Is) to display remotePeerOutdatedMessage verbatim rather than the raw error string. Add the `errors` import if absent. Keep the CAP-LEAK invariant: the message is a fixed string, no interpolated URL/cap.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test ./internal/tui/ -run TestRemoteFilesClient -race -count=1 && grep -c 'The remote session is running an older version of AgentHub that does not support file writes.' internal/tui/remote_files_client.go | grep -q '^1$'</automated>
  </verify>
  <done>All 4 Go write methods return ErrRemotePeerNoWriteSupport on upstream 405; the verbatim const appears exactly once in remote_files_client.go; TUI surface displays the verbatim copy; read methods unchanged; tests green with -race.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TS isMethodNotAllowed() + verbatim const + useFilesWrite/FileBrowserTab dispatch</name>
  <read_first>
    - frontend/src/lib/filesApi.ts (the FilesApiError predicate block lines 57-94 — isConflict/isCollision/isOverCap shape)
    - frontend/src/lib/useFilesWrite.ts (the write() catch block lines 117-131 — isConflict branch is the template; generic branch preserves the buffer)
    - frontend/src/components/FileBrowserTab.tsx (existing conflict/save-error dispatch)
    - 128-PATTERNS.md sections "frontend/src/lib/filesApi.ts" and "frontend/src/lib/useFilesWrite.ts"
  </read_first>
  <behavior>
    - Test: new FilesApiError(405, '').isMethodNotAllowed() === true; isMethodNotAllowed() is false for 404/412/409.
    - Test: useFilesWrite.write() catching a FilesApiError(405) returns 'peer-outdated', sets saveError to REMOTE_PEER_OUTDATED_MESSAGE, sets saveState 'idle', and does NOT clear the edit buffer (T-125-08).
    - Test: the existing 'conflict' (412) and generic 'error' outcomes are unchanged (no regression).
  </behavior>
  <action>
    In filesApi.ts, add `export const REMOTE_PEER_OUTDATED_MESSAGE = 'The remote session is running an older version of AgentHub that does not support file writes.'` (byte-identical to the Go const) and an `isMethodNotAllowed(): boolean { return this.status === 405 }` predicate alongside the existing is* predicates, with a JSDoc `/** 405 → remote peer has no write routes (old AgentHub version). RMW-04. */`. In useFilesWrite.ts, extend `WriteOutcome` to `'saved' | 'conflict' | 'error' | 'peer-outdated'` and insert a branch in the write() catch block — BEFORE the generic branch and after the isConflict() branch — that on `err instanceof FilesApiError && err.isMethodNotAllowed()` sets saveError to REMOTE_PEER_OUTDATED_MESSAGE, sets saveState 'idle', and returns 'peer-outdated'. Do NOT clear editContent (T-125-08 locked). In FileBrowserTab.tsx, handle the 'peer-outdated' outcome from write() by surfacing the distinct REMOTE_PEER_OUTDATED_MESSAGE (mirroring the existing 'conflict' dispatch), NOT the generic retry copy. Scope to write verbs only — read 405s remain generic.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm exec vitest run src/lib/filesApi.test.ts src/lib/useFilesWrite.test.ts && grep -c 'The remote session is running an older version of AgentHub that does not support file writes.' src/lib/filesApi.ts | grep -q '^1$'</automated>
  </verify>
  <done>isMethodNotAllowed() predicate exists; REMOTE_PEER_OUTDATED_MESSAGE const appears exactly once in filesApi.ts and is byte-identical to the Go const; WriteOutcome includes 'peer-outdated'; write() returns 'peer-outdated' on 405 without clearing the buffer; FileBrowserTab surfaces the distinct message; vitest green.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-surface verbatim-parity grep gate</name>
  <read_first>
    - 128-PATTERNS.md "Shared Patterns" → "Cross-surface verbatim message (single const per language, grep-gated)"
    - Phase 124-05 precedent: how homeDirWriteWarning was made grep-verifiable (referenced in RESEARCH Pitfall 4)
  </read_first>
  <action>
    Prove the verbatim SC3 string is byte-identical and present in exactly one Go const and one TS const. Run the dual-language grep across the repo (excluding test/planning files): the string "The remote session is running an older version of AgentHub that does not support file writes." must appear in internal/tui/remote_files_client.go (Go) AND frontend/src/lib/filesApi.ts (TS). If it appears in only one language, parity fails. No new code unless the gate exposes drift — in which case fix the drifting const to match SC3 exactly.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && GO=$(grep -rc 'The remote session is running an older version of AgentHub that does not support file writes.' internal/tui/remote_files_client.go) && TS=$(grep -rc 'The remote session is running an older version of AgentHub that does not support file writes.' frontend/src/lib/filesApi.ts) && [ "$GO" -ge 1 ] && [ "$TS" -ge 1 ]</automated>
  </verify>
  <done>The verbatim SC3 string is present in both the Go and TS source consts; cross-surface parity confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote tailnet peer → daemon proxy / TUI client | Untrusted HTTP status + body crosses here over HTTPS |
| client error message → user surface | Error text rendered to the operator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | Information Disclosure | new 405 message in RemoteFilesClient + FilesApiError | mitigate | Message is a fixed string with no interpolated URL/cap (CAP-LEAK invariant); verified by grep that the const has no `%s`/template for the cap |
| T-128-02 | Spoofing | upstream 405 vs proxy's own 405 | mitigate | Map only the UPSTREAM-propagated 405 in the consumer client (Pitfall 3); proxy stays a verbatim byte-pipe — no mapping at proxy |
| T-128-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (zero new deps per RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `go test ./internal/tui/ -run TestRemoteFilesClient -race -count=1` green
- `pnpm exec vitest run src/lib/filesApi.test.ts src/lib/useFilesWrite.test.ts` green
- Verbatim SC3 const present in BOTH Go and TS source
- Read methods (List/Stat/Read/Head) unchanged — 405 mapping scoped to write verbs only
</verification>

<success_criteria>
RMW-04 satisfied: an upstream 405 surfaces the verbatim "older version" message identically in the Go TUI and the TS GUI/web surface; the message is cap-free; read 405s remain generic.
</success_criteria>

<output>
Create `.planning/phases/128-remote-write-parity-cross-surface-integration/128-01-SUMMARY.md` when done
</output>
