---
phase: 128-remote-write-parity-cross-surface-integration
plan: 04
type: execute
wave: 4
depends_on: ["128-01", "128-02", "128-03"]
files_modified:
  - .planning/phases/128-remote-write-parity-cross-surface-integration/128-VERIFICATION.md
autonomous: true
requirements: [RMW-06]
must_haves:
  truths:
    - "The Phase 122 remote-read test suite passes with zero regressions (List/Stat/Read/CrossSurface_Parity)"
    - "A two-machine tailnet write UAT checklist is committed as 128-VERIFICATION.md (Machine A web-share + Machine B GUI + Machine B TUI; cross-surface write parity + cap-expiry failure mode)"
    - "The UAT checklist is OPERATOR-DEFERRED — this plan authors the checklist, it does NOT run the two-machine UAT"
    - "128-VERIFICATION.md notes that umbrella Issue #24 closes when the UAT is executed successfully, and instructs scanning scottkw/agenthub open issues before recording a UAT pass"
  artifacts:
    - path: ".planning/phases/128-remote-write-parity-cross-surface-integration/128-VERIFICATION.md"
      provides: "RMW-01..06 traceability + automated results + verbatim two-machine UAT checklist"
      contains: "Issue #24"
  key_links:
    - from: "128-VERIFICATION.md"
      to: "122-VERIFICATION.md precedent"
      via: "mirror structure (traceability table + automated results + operator UAT checklist)"
      pattern: "RMW-0"
---

<objective>
Confirm the Phase 122 remote-read test suite has zero regressions, and author the committed two-machine tailnet write UAT checklist (mirroring the 122-VERIFICATION.md precedent). The two-machine UAT execution is operator-deferred (requires a second tailnet machine — STATE.md TD-3 still pending); the deliverable is the COMMITTED CHECKLIST, not its execution.

Purpose: RMW-06 + the v3.5 milestone close-out. 128-VERIFICATION.md is the artifact that closes umbrella Issue #24 when the operator executes the UAT.
Output: 128-VERIFICATION.md with RMW-01..06 traceability, automated results, and the verbatim operator UAT checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-remote-write-parity-cross-surface-integration/128-RESEARCH.md
@.planning/phases/128-remote-write-parity-cross-surface-integration/128-PATTERNS.md
@.planning/milestones/v3.4-phases/122-remote-session-file-browse-wiring/122-VERIFICATION.md
@.planning/phases/128-remote-write-parity-cross-surface-integration/128-01-SUMMARY.md
@.planning/phases/128-remote-write-parity-cross-surface-integration/128-02-SUMMARY.md
@.planning/phases/128-remote-write-parity-cross-surface-integration/128-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm zero regression on the Phase 122 remote-read suite</name>
  <read_first>
    - internal/daemon/remote_files_parity_test.go (the 122 read tests: List/Stat/Read/CrossSurface_Parity)
    - internal/daemon/remote_files_test.go (122 read round-trip regression tests)
    - 128-RESEARCH.md "Phase Requirements → Test Map" (RMW-06 regression command)
  </read_first>
  <action>
    Run the full Phase 122 remote-read regression suite and confirm zero failures. The mappings added in 128-01/02 (405/401 on write verbs only) must NOT have changed any read behavior. Capture the pass/fail result for the 128-VERIFICATION.md automated-results section. No code changes expected — this is a guard. If any read test regresses, STOP and report (do not paper over).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test ./internal/daemon/ -run 'TestRemoteFiles_(List|Stat|Read|CrossSurface_Parity)' -race -count=1</automated>
  </verify>
  <done>All Phase 122 remote-read tests pass with -race; result captured for the verification doc; no read behavior changed by the 405/401 write-verb mappings.</done>
</task>

<task type="auto">
  <name>Task 2: Author committed 128-VERIFICATION.md (traceability + automated results + operator UAT checklist)</name>
  <read_first>
    - .planning/milestones/v3.4-phases/122-remote-session-file-browse-wiring/122-VERIFICATION.md (the exact structure to mirror: traceability table, automated results, operator UAT checklist)
    - 128-PATTERNS.md "128-VERIFICATION.md (NEW DOC — mirror 122 precedent)"
    - 128-RESEARCH.md "Common Pitfalls" Pitfall 5 (write the checklist, do NOT run it — operator-deferred)
    - .planning/STATE.md (TD-3 two-machine UAT pending; Issue #24 umbrella)
  </read_first>
  <action>
    Create 128-VERIFICATION.md mirroring the 122-VERIFICATION.md structure. Include: (1) an RMW-01..RMW-06 traceability table mapping each requirement to its proving test/artifact and pass status (cite the 128-01/02/03 tests + this plan's Task 1 regression result); (2) an automated-results section summarizing the 3-observer write-parity proof, the 405 version-gate, the cap-expiry "access expired" behavior, and the 122 zero-regression confirmation; (3) a verbatim TWO-MACHINE TAILNET WRITE UAT CHECKLIST — Machine A running web-share, Machine B running the GUI, Machine B running the TUI — covering cross-surface write parity (edit/save, upload [GUI/web only], delete, rename, cross-dir move, mkdir) AND the cap-expiry failure mode (cap expires mid-edit → buffer preserved + "access expired") AND the v3.4-peer 405 version-gate message. Mark the checklist OPERATOR-DEFERRED (requires a second tailnet machine per STATE.md TD-3). State explicitly that umbrella Issue #24 closes when this UAT is executed successfully, and instruct the operator to scan scottkw/agenthub open issues before recording a UAT pass (MEMORY: cross-check GitHub issues during UAT). Do NOT author a task or step that RUNS the two-machine UAT.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && test -f .planning/phases/128-remote-write-parity-cross-surface-integration/128-VERIFICATION.md && grep -q 'Issue #24' .planning/phases/128-remote-write-parity-cross-surface-integration/128-VERIFICATION.md && grep -qi 'machine a' .planning/phases/128-remote-write-parity-cross-surface-integration/128-VERIFICATION.md && grep -q 'RMW-06' .planning/phases/128-remote-write-parity-cross-surface-integration/128-VERIFICATION.md</automated>
  </verify>
  <done>128-VERIFICATION.md exists with the RMW-01..06 traceability table, automated-results summary, and the verbatim operator-deferred two-machine UAT checklist; it references Issue #24 closure and the GitHub-issue scan; no step runs the UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented UAT procedure → operator execution | The checklist defines what the operator must verify across two machines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-09 | Repudiation | unverified release (UAT skipped) | mitigate | Committed checklist + explicit Issue #24-closes-on-execution gate; operator scans open issues before recording a pass |
| T-128-10 | Tampering | a 405/401 mapping silently regressing read behavior | mitigate | Task 1 runs the full 122 read suite with -race as a regression guard before sign-off |
| T-128-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (doc + regression run only) |
</threat_model>

<verification>
- `go test ./internal/daemon/ -run 'TestRemoteFiles_(List|Stat|Read|CrossSurface_Parity)' -race -count=1` green (zero regression)
- 128-VERIFICATION.md committed with traceability + automated results + operator-deferred two-machine UAT checklist
- Checklist references Issue #24 closure and the open-issue scan; does NOT run the UAT
</verification>

<success_criteria>
RMW-06 satisfied: Phase 122 remote-read suite confirmed zero-regression; the committed two-machine tailnet write UAT checklist (128-VERIFICATION.md) is ready for operator execution to close umbrella Issue #24.
</success_criteria>

<output>
Create `.planning/phases/128-remote-write-parity-cross-surface-integration/128-04-SUMMARY.md` when done
</output>
