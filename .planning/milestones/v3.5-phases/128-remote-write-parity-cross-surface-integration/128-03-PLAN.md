---
phase: 128-remote-write-parity-cross-surface-integration
plan: 03
type: execute
wave: 3
depends_on: ["128-01", "128-02"]
files_modified:
  - cmd/playwright-fixture/main.go
  - internal/daemon/remote_files_write_parity_test.go
  - frontend/e2e/files-browser.spec.ts
autonomous: true
requirements: [RMW-01, RMW-02, RMW-03]
must_haves:
  truths:
    - "Remote write parity is proven by 3 independent observers — daemon-proxy Go, tui.RemoteFilesClient Go, and Playwright HTTPS browser — all byte-equivalent for a write-then-read round trip against the SAME persisted fixture peer"
    - "The fixture peer GENUINELY persists writes (backed by a real files.Sandbox / t.TempDir()), so write-then-read round-trips actual bytes — not canned 'hello world'"
    - "A write by Observer A (proxy) is read-back-identical by Observer B (direct RemoteFilesClient) against the same fixture sandbox (proves both hit one persisted state)"
    - "The GUI's 6 remote write ops route through the daemon proxy over the persisting fixture (RMW-02 e2e write observer)"
    - "Every direct-client error path in the parity test passes assertNoCapInError (CAP-LEAK invariant)"
  artifacts:
    - path: "internal/daemon/remote_files_write_parity_test.go"
      provides: "package daemon_test write-then-read 3-observer parity test"
      contains: "package daemon_test"
    - path: "cmd/playwright-fixture/main.go"
      provides: "startRemotePeerFixture extended to persist writes via real sandbox"
      contains: "WriteFileAtomic"
  key_links:
    - from: "internal/daemon/remote_files_write_parity_test.go"
      to: "tui.NewRemoteFilesClientForTest + daemon.API.Handler"
      via: "reuse the 4 exported *ForTest helpers (no import cycle)"
      pattern: "NewRemoteFilesClientForTest"
    - from: "frontend/e2e/files-browser.spec.ts"
      to: "fixture peer /api/files/write then /api/files/read"
      via: "Playwright APIRequestContext HTTPS write-then-read (Observer C)"
      pattern: "remoteFilesWriteURL|APIRequestContext"
---

<objective>
Prove remote write parity with the same 3-observer bar as the Phase 122 read-parity proof: daemon-proxy Go + tui.RemoteFilesClient Go + Playwright HTTPS browser, all byte-equivalent for a write-then-read round trip against ONE persisting fixture peer. Extend the fixture to genuinely persist writes (Pitfall 2) so the byte-equivalence assertion is not vacuous.

Purpose: RMW-01 (3-observer proof), RMW-02 (GUI 6 ops via proxy e2e write observer), RMW-03 (TUI write via RemoteFilesClient over HTTPS — already unit-tested in 126-01; this proves it in the parity harness).
Output: an extended persisting fixture peer; a new package daemon_test write-parity test; a new Playwright HTTPS write-then-read scenario.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-remote-write-parity-cross-surface-integration/128-RESEARCH.md
@.planning/phases/128-remote-write-parity-cross-surface-integration/128-PATTERNS.md
@internal/daemon/remote_files_parity_test.go

<interfaces>
<!-- The 122 read-parity harness is the exact template. Reuse these verified helpers. -->

internal/daemon/remote_files_parity_test.go (package daemon_test, 455 LOC):
  package daemon_test                                   // line 36 — MANDATORY (avoids tui→daemon cycle, Pitfall 1)
  newFixtureRemotePeer(t)                               // line 88 → httptest.NewTLSServer at line 133 — EXTEND to persist
  newDaemonAPIWithUpstreamCert(t, upstream)             // line 139 → engine.ConfigDirForTest(t.TempDir()) :142,
                                                        //   api.SetRemoteFilesClientForTest(upstream.Client()) :144
  daemon.API.Handler()                                  // wrapped by httptest.NewServer at line 156
  tui.NewRemoteFilesClientForTest(url, cap, client)     // line 182 — direct Observer B
  assertNoCapInError(t, err)                            // lines 443-446 — call on EVERY direct-client error
  TestRemoteFiles_CrossSurface_Parity                   // line 151 — read-parity template; mirror for write-then-read

cmd/playwright-fixture/main.go:
  startRemotePeerFixture                                // lines 407-496 — read-only canned mux today
  guard closure                                         // line 449 — checks ?cap=fixtureRemoteCap (401) + ?session= (404)
  canned read handlers return "hello world"             // line 486 — REPLACE write path with real persistence
  WRITE_CAP emitted to stdout                            // line 282 (global-setup.ts:153 parses it)

frontend/e2e/files-browser.spec.ts:
  scenario 16 (APIRequestContext)                       // line 430 — the contract-not-DOM template
  scenario 17                                            // line 501
frontend/e2e/files-write.spec.ts:
  WRITE_CAP URL builders                                 // lines 37, 86
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend startRemotePeerFixture to persist writes via a real sandbox</name>
  <read_first>
    - cmd/playwright-fixture/main.go (startRemotePeerFixture lines 407-496; the guard closure line 449; canned read handlers; WRITE_CAP at line 282)
    - internal/files/handler.go and internal/files/sandbox.go (the real files.Handler + Sandbox.WriteFileAtomic — mount the real handler so writes persist genuinely)
    - 128-PATTERNS.md "cmd/playwright-fixture/main.go — extend startRemotePeerFixture to persist writes" (Pitfall 2: must genuinely persist, NOT canned bytes)
  </read_first>
  <action>
    Extend startRemotePeerFixture so its write verbs persist into a real files.Sandbox rooted at a temp directory (or mount the real files.Handler over that sandbox), reusing the existing guard closure for ?cap/?session checks. Add {write, upload, delete, rename, mkdir} routes mirroring the real webserver handler set, so a PUT /api/files/write?path=x.txt followed by GET /api/files/read?path=x.txt round-trips the ACTUAL written bytes (NOT the canned "hello world"). Keep the existing read handlers working for backward-compat with scenarios 16/17. The byte-shape must stay consistent with what the Go parity test expects. Do NOT spawn a second webserver process — keep it in-process per the 122 precedent.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./cmd/playwright-fixture/ && grep -q 'WriteFileAtomic\|files.NewHandler\|files.Handler' cmd/playwright-fixture/main.go</automated>
  </verify>
  <done>The fixture peer persists writes through a real sandbox; write-then-read returns the actual written bytes; read handlers still work; fixture compiles and emits WRITE_CAP.</done>
</task>

<task type="auto">
  <name>Task 2: New package daemon_test write-then-read 3-observer parity test</name>
  <read_first>
    - internal/daemon/remote_files_parity_test.go (the full read-parity harness — TestRemoteFiles_CrossSurface_Parity line 151; the 4 *ForTest helpers; assertNoCapInError lines 443-446)
    - internal/daemon/remote_files_test.go (TestRemoteFilesWrite_ForwardsBody / CallerCapStripped — the write proxy already works)
    - 128-PATTERNS.md "internal/daemon/remote_files_write_parity_test.go (NEW — mirror 122 read harness)" (the write-then-read assertion shape)
  </read_first>
  <behavior>
    - Test: Observer A (daemon proxy) PUT /api/files/remote/{sid}/write?path=x.txt body="content-A", then proxy GET .../read?path=x.txt → read-back == "content-A".
    - Test: Observer B (direct tui.RemoteFilesClient) WriteFile(ctx, sid, "y.txt", "content-B"), then ReadFile → == "content-B".
    - Test: cross-observer — a write by A is read-back-identical by B against the SAME fixture sandbox (proves one shared persisted state).
    - Test: assertNoCapInError(t, err) on every direct-client error path.
  </behavior>
  <action>
    Create internal/daemon/remote_files_write_parity_test.go in `package daemon_test` (MANDATORY — avoids the tui→daemon import cycle, Pitfall 1). Reuse the existing exported helpers: newFixtureRemotePeer(t) (now persisting from Task 1), newDaemonAPIWithUpstreamCert(t, upstream), daemon.API.Handler() wrapped by httptest.NewServer, tui.NewRemoteFilesClientForTest(url, cap, client), assertNoCapInError. Mirror TestRemoteFiles_CrossSurface_Parity but as write-then-read: write via the proxy and via the direct client, read back through each, assert byte-equivalence against the canonical written bytes AND that a cross-observer write/read against the same fixture sandbox matches. Call assertNoCapInError on every error path. Run with -race.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test ./internal/daemon/ -run TestRemoteFilesWrite_CrossSurface -race -count=1 && grep -q '^package daemon_test' internal/daemon/remote_files_write_parity_test.go</automated>
  </verify>
  <done>The write-parity test passes with -race; Observer A and Observer B are byte-equivalent for write-then-read against the same persisted fixture; CAP-LEAK invariant asserted on every error path.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright HTTPS write-then-read scenario (Observer C)</name>
  <read_first>
    - frontend/e2e/files-browser.spec.ts (scenarios 16/17 lines 430/501 — the APIRequestContext contract-not-DOM template)
    - frontend/e2e/files-write.spec.ts (WRITE_CAP URL builders lines 37/86)
    - frontend/e2e/global-setup.ts (line 153 — WRITE_CAP parsed from fixture stdout)
    - 128-PATTERNS.md "frontend/e2e/files-browser.spec.ts (NEW scenario 18 — Observer C ...)" (Anti-Pattern: no DOM modal flow — test the upstream contract)
  </read_first>
  <action>
    Add a new Playwright scenario in files-browser.spec.ts that acts as the HTTPS browser observer: using playwrightRequest.newContext({ ignoreHTTPSErrors: true }) and the WRITE_CAP, PUT to the fixture peer's /api/files/write (path e.g. 'x.txt', body 'content-C'), expect HTTP 200, then GET /api/files/read for the same path and assert the response text equals 'content-C'. Use the WRITE_CAP URL builders from files-write.spec.ts. Do NOT drive the DOM modal — the fixture serves /app/ web mode where Wails RPCs don't exist; test the UPSTREAM CONTRACT via APIRequestContext (mirror scenario 16). The byte-shape must agree with the Go parity test's canonical expectations.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm exec playwright test files-browser --project=chromium -g "write" 2>&1 | tail -20</automated>
  </verify>
  <done>The Playwright HTTPS write-then-read scenario passes on chromium (Observer C); read-back bytes equal the written bytes; no DOM modal flow used.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| observer (proxy / TUI client / browser) → fixture peer | HTTPS + cap; the parity surface under test |
| daemon proxy → upstream peer | proxy strips caller-supplied ?cap and force-sets the session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-06 | Elevation of Privilege | caller smuggling a different ?cap through the proxy | accept (already mitigated) | Proxy strips caller cap + force-sets session (TestRemoteFilesWrite_CallerCapStripped); parity test reuses this verified path |
| T-128-07 | Tampering | MITM downgrade on the HTTPS hop | accept (already mitigated) | TLS 1.2+ pinned (MinVersion); InsecureSkipVerify intentional for self-signed fixture cert (//nolint:gosec) — unchanged |
| T-128-08 | Information Disclosure | cap token in a parity-test error string | mitigate | assertNoCapInError on every direct-client error path |
| T-128-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (Playwright + Go stdlib already present, RESEARCH audit) |
</threat_model>

<verification>
- `go test ./internal/daemon/ -run TestRemoteFilesWrite_CrossSurface -race -count=1` green
- `go build ./cmd/playwright-fixture/` succeeds
- Playwright write-then-read scenario green on chromium
- Fixture peer genuinely persists (read-back != canned bytes — Pitfall 2 avoided)
</verification>

<success_criteria>
RMW-01/02/03 satisfied: 3 independent observers (proxy Go, RemoteFilesClient Go, Playwright HTTPS) are byte-equivalent for write-then-read against one persisting fixture peer; the GUI's proxy path and the TUI's direct path are both proven; CAP-LEAK invariant holds.
</success_criteria>

<output>
Create `.planning/phases/128-remote-write-parity-cross-surface-integration/128-03-SUMMARY.md` when done
</output>
