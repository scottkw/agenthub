---
phase: 130-remote-browse-gui-on-ramp
plan: 04
type: execute
wave: 1
depends_on: ["130-02"]
files_modified:
  - frontend/src/components/RemoteSessionsPanel.tsx
  - frontend/src/style.css
  - frontend/src/App.tsx
  - frontend/src/wailsjs/go/main/App.d.ts
autonomous: true
requirements: [RB-01, RB-02, RB-04]
must_haves:
  truths:
    - "RemoteSessionsPanel renders honest per-peer states: session rows when reachable+populated, 'No shareable sessions' when reachable+empty, 'Unreachable' badge when unreachable — never 'No remote peers found' while peers are probed"
    - "App.tsx calls GetRemoteSessionsWithMeta (not GetRemoteSessions) so reachable-but-empty peers are no longer dropped"
    - "Clicking 'Browse Files' on a session row invokes the existing handleBrowseFilesRemote → join-code cap → FileBrowserTab pick flow"
  artifacts:
    - path: "frontend/src/components/RemoteSessionsPanel.tsx"
      provides: "per-peer honest-state rendering + 'Shows shareable sessions' copy"
      contains: "reachable"
    - path: "frontend/src/style.css"
      provides: "remote-panel__peer-unreachable + remote-panel__peer-empty-sessions BEM classes"
      contains: "remote-panel__peer-unreachable"
    - path: "frontend/src/App.tsx"
      provides: "GetRemoteSessionsWithMeta call site"
      contains: "GetRemoteSessionsWithMeta"
    - path: "frontend/src/wailsjs/go/main/App.d.ts"
      provides: "GetRemoteSessionsWithMeta binding declaration"
      contains: "GetRemoteSessionsWithMeta"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "GetRemoteSessionsWithMeta"
      via: "replaces GetRemoteSessions call at the peers-poll site"
      pattern: "GetRemoteSessionsWithMeta\\(\\)"
    - from: "RemoteSessionsPanel onBrowseFiles"
      to: "App.tsx handleBrowseFilesRemote"
      via: "existing onBrowseFiles prop wiring (App.tsx:1319)"
      pattern: "onBrowseFiles"
---

<objective>
Implement the frontend on-ramp: extend RemoteSessionsPanel to render honest per-peer states (reachable+populated, reachable+empty, unreachable) per the approved UI-SPEC, add the supporting BEM CSS, rewire App.tsx to call the new GetRemoteSessionsWithMeta RPC, and update the hand-maintained Wails binding declaration. The pick flow (Browse Files → join-code → FileBrowserTab) reuses the existing Phase 122 path — no new cap flow.

Purpose: Complete RB-01 (reachable peers with shareable sessions are surfaced), RB-04 (honest, colorblind-safe per-peer states — text-first, color only as reinforcement), and RB-02 (pick → open File Browser). This turns the plan-02 vitest suite green.

Output: Four frontend files modified. After this plan the RemoteSessionsPanel suite is green and the discover→list→pick flow is wired end-to-end in the GUI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-remote-browse-gui-on-ramp/130-UI-SPEC.md
@.planning/phases/130-remote-browse-gui-on-ramp/130-RESEARCH.md
@.planning/phases/130-remote-browse-gui-on-ramp/130-02-SUMMARY.md

<interfaces>
<!-- Existing wiring. Use directly — no exploration beyond read_first. -->

frontend/src/App.tsx:
  line 26  — import { GetRemoteSessions } from wailsjs   → change to GetRemoteSessionsWithMeta
  line 895 — const peers = await GetRemoteSessions()      → GetRemoteSessionsWithMeta()
  line 984 — handleBrowseFilesRemote (existing pick → join-code cap → FileBrowserTab flow; Phase 122)
  line 1319 — <RemoteSessionsPanel onBrowseFiles={handleBrowseFilesRemote} ... />  (existing prop wiring)

frontend/src/wailsjs/go/main/App.d.ts:
  line 112 — export function GetRemoteSessions(): Promise<RemotePeerSessions[]>  (hand-maintained; add the new one)

frontend/src/components/RemoteSessionsPanel.tsx:
  current render maps peers→sessions uniformly; status dot has title={s.status} (preserve);
  action buttons have aria-labels (preserve). Add reachable branching per UI-SPEC Pattern 4.

RemotePeerSessions (frontend type, this plan extends): { hostname; reachable: boolean; sessions: RemoteSession[] }

UI-SPEC contracts to honor (130-UI-SPEC.md):
  - Copy: "Shows shareable sessions"; "No shareable sessions" + "This peer has no sessions with web-sharing enabled."; "Unreachable".
  - New CSS classes: .remote-panel__peer-unreachable, .remote-panel__peer-empty-sessions(-title/-body) — exact rules in UI-SPEC §New CSS Classes Required (reuse existing hex tokens only).
  - Colorblind mandate: text label is the primary signal; color is reinforcement only.
  - Accessibility: preserve title={status}, aria-labels; role="status" on loading region; prefers-reduced-motion spinner fallback if missing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render honest per-peer states + copy + CSS (RB-04, RB-01)</name>
  <files>frontend/src/components/RemoteSessionsPanel.tsx, frontend/src/style.css</files>
  <read_first>
    - frontend/src/components/RemoteSessionsPanel.tsx (full — current peer/session render, status-dot title, action-button aria-labels, the meta line "Shows web-enabled sessions only", the empty-panel "No remote peers found")
    - frontend/src/components/__tests__/RemoteSessionsPanel.test.tsx (the plan-02 RED tests this task must satisfy — the exact asserted strings)
    - frontend/src/style.css (existing .remote-panel__* rules ~lines 1515-1693; existing badge padding 2px 8px convention ~lines 1449-1460, 1638-1640)
    - .planning/phases/130-remote-browse-gui-on-ramp/130-UI-SPEC.md (Pattern/State Inventory, Copywriting Contract, New CSS Classes Required, Accessibility Contract, Motion Contract)
    - .planning/phases/130-remote-browse-gui-on-ramp/130-RESEARCH.md (Pattern 4 render branching)
  </read_first>
  <behavior>
    - Extend the frontend RemotePeerSessions type with `reachable: boolean`.
    - Per peer, branch: reachable && sessions.length>0 → meta line "Shows shareable sessions" + existing session rows (unchanged markup, preserve status dot title + action aria-labels); reachable && sessions.length===0 → "Shows shareable sessions" meta + a .remote-panel__peer-empty-sessions block with title "No shareable sessions" and body "This peer has no sessions with web-sharing enabled."; !reachable → .remote-panel__peer-unreachable text badge "Unreachable".
    - "No remote peers found" empty state renders ONLY when peers.length===0 (never when ≥1 peer is probed).
    - Color is reinforcement only; the word "Unreachable" / "No shareable sessions" carries the state.
  </behavior>
  <action>Edit frontend/src/components/RemoteSessionsPanel.tsx: add `reachable: boolean` to the RemotePeerSessions interface; replace the per-peer render with the three-way branch from UI-SPEC Pattern 4 (unreachable badge / empty-sessions block / populated session list). Change the meta-line literal "Shows web-enabled sessions only" → "Shows shareable sessions". Preserve status-dot title={s.status}, action-button aria-labels, and the focus/keyboard behavior. Ensure the loading region has role="status" + aria-label="Loading remote peers" (add if missing) and the empty-panel "No remote peers found" branch is gated on peers.length===0. In frontend/src/style.css add the .remote-panel__peer-unreachable, .remote-panel__peer-empty-sessions, .remote-panel__peer-empty-sessions-title, .remote-panel__peer-empty-sessions-body rules EXACTLY as specified in UI-SPEC §New CSS Classes Required (reuse existing hex tokens #f7768e/#1e2030/#9aa5ce/#c0caf5 — introduce zero new hex values; padding 2px 8px for the badge per the established convention). Add a prefers-reduced-motion fallback for the spinner if not already present (mirror file-browser__spinner). camelCase props / PascalCase component per CLAUDE.md. Do NOT touch App.tsx in this task.</action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm test -- RemoteSessionsPanel.test 2>&1 | grep -Eq "pass|✓|[0-9]+ passed" &amp;&amp; ! (pnpm test -- RemoteSessionsPanel.test 2>&1 | grep -Eq "[1-9][0-9]* failed")</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm test -- RemoteSessionsPanel.test` is GREEN (all plan-02 RB-04 tests pass).
    - `grep -c '#[0-9a-fA-F]\{6\}' frontend/src/style.css` shows no hex value outside the existing TokyoNight token set in the new rules (the four new classes use only #f7768e/#1e2030/#9aa5ce/#c0caf5).
    - `grep -c 'Shows web-enabled sessions only' frontend/src/components/RemoteSessionsPanel.tsx` returns 0; "Shows shareable sessions" present.
    - status-dot title and action-button aria-labels are preserved (still present in the file).
    - The "No remote peers found" branch is conditioned on peers.length===0.
  </acceptance_criteria>
  <done>Panel renders honest, colorblind-safe per-peer states with the UI-SPEC copy and CSS; plan-02 suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire GetRemoteSessionsWithMeta RPC + binding; preserve pick flow (RB-01, RB-02)</name>
  <files>frontend/src/App.tsx, frontend/src/wailsjs/go/main/App.d.ts</files>
  <read_first>
    - frontend/src/App.tsx (import line 26, peers-poll call line 895, handleBrowseFilesRemote line 984, RemoteSessionsPanel usage line 1319)
    - frontend/src/wailsjs/go/main/App.d.ts (line 112 GetRemoteSessions declaration + the RemotePeerSessions model import)
    - frontend/src/components/__tests__/App.remoteFileBrowser.test.tsx (existing RB-02 pick-flow test — confirm it still passes / extend if the mock references the RPC name)
    - frontend/src/lib/remoteSession.ts (findRemoteSession — confirm reachable is additive, no change needed)
    - .planning/phases/130-remote-browse-gui-on-ramp/130-RESEARCH.md (Pitfall 4 — update App.tsx AND App.d.ts; Open Question 1/2)
  </read_first>
  <behavior>
    - App.tsx imports and calls GetRemoteSessionsWithMeta() instead of GetRemoteSessions() at the peers-poll site; peers now include reachable-but-empty and unreachable entries (the RB-01 silent-drop is gone end-to-end).
    - App.d.ts declares GetRemoteSessionsWithMeta(): Promise<RemotePeerSessions[]> (hand-maintained binding).
    - The pick flow is unchanged: onBrowseFiles={handleBrowseFilesRemote} still triggers the Phase 122 join-code cap → FileBrowserTab path. RB-02 test (App.remoteFileBrowser) stays green.
  </behavior>
  <action>Edit frontend/src/App.tsx: change the import at line 26 from GetRemoteSessions to GetRemoteSessionsWithMeta, and the call at line 895 from `await GetRemoteSessions()` to `await GetRemoteSessionsWithMeta()`. Leave handleBrowseFilesRemote and the onBrowseFiles wiring (line 1319) untouched — the existing Phase 122 join-code cap flow and FileBrowserTab handoff are reused per the locked decision (no new cap flow). Edit frontend/src/wailsjs/go/main/App.d.ts: add `export function GetRemoteSessionsWithMeta(): Promise<RemotePeerSessions[]>` alongside the existing line-112 declaration (the binding is hand-maintained per RESEARCH A1). If App.remoteFileBrowser.test.tsx mocks GetRemoteSessions by name, extend the mock to also provide GetRemoteSessionsWithMeta so the RB-02 pick-flow test exercises the new RPC. No change to remoteSession.ts (reachable is additive). camelCase per CLAUDE.md. Run the full frontend suite to confirm no regressions.</action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm test 2>&1 | tail -20 | grep -Eq "passed" &amp;&amp; ! (pnpm test 2>&1 | grep -Eq "[1-9][0-9]* failed")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'GetRemoteSessionsWithMeta' frontend/src/App.tsx` ≥ 2 (import + call); `grep -c 'GetRemoteSessions(' frontend/src/App.tsx` shows the old bare call is gone (only the *WithMeta variant remains at the poll site).
    - `grep -c 'GetRemoteSessionsWithMeta' frontend/src/wailsjs/go/main/App.d.ts` ≥ 1.
    - `cd frontend && pnpm test` is fully GREEN (RemoteSessionsPanel + App.remoteFileBrowser + all 87 files), no failures.
    - handleBrowseFilesRemote and onBrowseFiles wiring are unchanged (pick flow preserved — RB-02).
  </acceptance_criteria>
  <done>App.tsx calls the new RPC; binding declared; pick flow preserved; full frontend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wails RPC → React render | Panel renders only peer/session metadata returned by GetRemoteSessionsWithMeta (no caps/content — enforced backend-side, plan 03 RB-03). |
| user pick → join-code cap flow | Opening a session still requires the intended cap via the existing RemoteJoinCodeModal (Phase 122); discovery metadata alone grants no access (RB-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-09 | Elevation of Privilege | pick flow bypassing cap acquisition | mitigate | onBrowseFiles reuses the existing join-code cap path; metadata discovery does not deposit a cap. Opening still requires the intended grant (verified: handleBrowseFilesRemote unchanged). |
| T-130-10 | Information Disclosure (UX) | misleading panel state | mitigate | Honest per-peer states (plan-02 tests, satisfied here): a probed peer is never shown as "No remote peers found"; unreachable/empty are text-labeled. |
| T-130-SC | Tampering | npm installs | accept | No new packages; uses existing React/vitest. |
</threat_model>

<verification>
- `cd frontend && pnpm test -- RemoteSessionsPanel.test` → GREEN (RB-04)
- `cd frontend && pnpm test` → all 87 files green, no failures (RB-01 wiring + RB-02 pick flow preserved)
- `grep -c 'Shows web-enabled sessions only' frontend/src/components/RemoteSessionsPanel.tsx` → 0
- `grep -c 'GetRemoteSessionsWithMeta' frontend/src/wailsjs/go/main/App.d.ts` → ≥1
</verification>

<success_criteria>
- Panel renders all three per-peer states, colorblind-safe (text-first), with UI-SPEC copy and CSS (zero new hex values).
- App.tsx uses GetRemoteSessionsWithMeta; binding declared; pick flow reuses Phase 122 join-code cap path (RB-02).
- Full frontend suite green; no regressions.
- Old "Shows web-enabled sessions only" literal removed from source.
</success_criteria>

<output>
Create `.planning/phases/130-remote-browse-gui-on-ramp/130-04-SUMMARY.md` when done
</output>
