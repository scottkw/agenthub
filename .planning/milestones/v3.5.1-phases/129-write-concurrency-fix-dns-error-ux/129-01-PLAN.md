---
phase: 129-write-concurrency-fix-dns-error-ux
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/daemon/remote_files_test.go
  - internal/webserver/tailscale_test.go
  - internal/daemon/relay_remote_files_test.go
  - internal/daemon/remote_files_parity_test.go
autonomous: true
requirements: [RACE-01, DNS-01, DNS-02, DNS-03]
must_haves:
  truths:
    - "The three Wave 0 test functions exist and compile"
    - "TestProxyRemoteFiles_AcceptDNSMessage fails (RED) because the DNS-specific message does not yet exist"
    - "TestCheckHealth_AcceptDNS fails (RED) because TailscaleHealth has no AcceptDNS field"
    - "TestRemoteFiles_TwoWriterRace_RelaySurface fails (RED) because the upstream allows both writers to win"
    - "The pre-existing TestWrite_TwoWritersIfMatchRace fails (RED) confirming the documented baseline"
  artifacts:
    - path: "internal/daemon/remote_files_test.go"
      provides: "TestProxyRemoteFiles_AcceptDNSMessage (DNS-01/DNS-02 coverage)"
      contains: "func TestProxyRemoteFiles_AcceptDNSMessage"
    - path: "internal/webserver/tailscale_test.go"
      provides: "TestCheckHealth_AcceptDNS (DNS-03 coverage)"
      contains: "func TestCheckHealth_AcceptDNS"
    - path: "internal/daemon/relay_remote_files_test.go"
      provides: "TestRemoteFiles_TwoWriterRace_RelaySurface (RACE-01 relay-surface coverage)"
      contains: "func TestRemoteFiles_TwoWriterRace_RelaySurface"
  key_links:
    - from: "internal/daemon/relay_remote_files_test.go"
      to: "files.Sandbox.WriteFileAtomic via a sandbox-backed upstream fixture"
      via: "two concurrent PUT requests through api.RelayHandler()"
      pattern: "WriteFileAtomic"
---

<objective>
Create the three missing Wave 0 tests that pin down Phase 129's behavior BEFORE any production code changes, and confirm the pre-existing race test's documented RED baseline.

This wave is test-gap-first: every test written here MUST fail (RED) for the right reason. Waves 1 and 2 turn them green. No production code is modified in this plan.

Purpose: Nyquist validation requires the DNS and relay-surface behaviors to have automated coverage before implementation. The v3.5 audit was blind to a 4-layer breakage precisely because only the webserver/fixture surface was tested — the relay-surface two-writer race test closes that blind spot.

Output: Three new test functions (two new, one in an existing relay test file) plus a sandbox-backed upstream write fixture, all compiling and failing for documented reasons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-write-concurrency-fix-dns-error-ux/129-RESEARCH.md
@.planning/phases/129-write-concurrency-fix-dns-error-ux/129-VALIDATION.md

<interfaces>
<!-- Extracted from codebase. Executor uses these directly — no exploration needed. -->

Existing relay test harness (internal/daemon/relay_remote_files_test.go + remote_files_parity_test.go):
- `func newFixtureRemotePeer(t *testing.T) *httptest.Server` — TLS server mimicking remote peer's /api/files endpoints; cap-guarded with const `fixtureCap = "FIXTURE_CAP"`. Currently `PUT /api/files/write` just returns `{"ok":true}` (no real write).
- `func newDaemonAPIWithUpstreamCert(t *testing.T, upstream *httptest.Server) *daemon.API` — daemon API wired to trust the upstream's self-signed cert.
- `func depositCapOnSocket(t *testing.T, socketSrv *httptest.Server, sessionID, baseURL, cap string)` — deposits a cap via the socket surface.
- Pattern: `socketSrv := httptest.NewServer(api.Handler())` (deposit cap) + `relaySrv := httptest.NewServer(api.RelayHandler())` (file ops). Relay URL shape: `relaySrv.URL + "/api/files/remote/<sid>/write?path=<p>"`. Write PUT sends `If-Match` header.

Existing health check (internal/webserver/tailscale.go):
- `type TailscaleHealth struct { Installed, Connected, HasCerts bool; IP, Domain string; BinaryFound, DaemonUp bool; PlatformHint string }` — NO AcceptDNS field yet.
- `func checkHealth(ctx context.Context, fn statusFunc, customPath string) TailscaleHealth` — injectable `statusFunc = func(ctx) (*ipnstate.Status, error)`. Connected when `status.BackendState == "Running"`.
- The accept-dns probe will need a NEW injectable prefs func; this test must drive that. Inspect the existing `internal/webserver/tailscale_test.go` for the fake `statusFunc` construction idiom and mirror it.

Existing files.Sandbox (internal/files/sandbox.go):
- `func NewSandbox(workDir string) (*Sandbox, error)`
- `func (s *Sandbox) WriteFileAtomic(relPath string, content []byte, expectedValidator ...string) error` — returns `ErrPreconditionFailed` on validator mismatch.
- Validator format (mtime-UnixNano + size, quoted): `fmt.Sprintf("%q", fmt.Sprintf("%d-%d", fi.ModTime().UnixNano(), fi.Size()))`.

Existing proxy (internal/daemon/remote_files.go):
- `func (a *API) proxyRemoteFiles(w http.ResponseWriter, r *http.Request, op string)` — on `client.Do` error currently returns `502 "remote unreachable: ..."` (generic catch-all). No DNS-specific branch yet.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write TestCheckHealth_AcceptDNS (DNS-03 RED)</name>
  <files>internal/webserver/tailscale_test.go</files>
  <read_first>
    - internal/webserver/tailscale.go (TailscaleHealth struct + checkHealth signature + statusFunc idiom)
    - internal/webserver/tailscale_test.go (existing fake statusFunc construction; how checkHealth is invoked in tests)
  </read_first>
  <action>
    Add `func TestCheckHealth_AcceptDNS(t *testing.T)` to internal/webserver/tailscale_test.go. The test must assert that when the node is Connected and the Tailscale prefs report accept-dns disabled (CorpDNS=false), the resulting TailscaleHealth has `AcceptDNS == false`, and when prefs report CorpDNS=true, `AcceptDNS == true`. Because checkHealth does not yet accept a prefs probe, the test will FAIL TO COMPILE — that is the intended RED for DNS-03. Plan 03 introduces the injectable prefs func and the AcceptDNS field; write this test against the SHAPE Plan 03 will deliver: an injectable prefs function (mirror the existing `statusFunc` injection idiom) returning a value with a `CorpDNS bool`-equivalent and an error. Use table cases: (connected + CorpDNS=false → AcceptDNS=false), (connected + CorpDNS=true → AcceptDNS=true), (prefs probe returns error → AcceptDNS=false, no panic). Reference DNS-03. Mirror the exact fake-injection style already used for statusFunc so Plan 03 only has to add the parameter. Do NOT modify tailscale.go in this task — the compile failure is the RED signal.
  </action>
  <verify>
    <automated>go vet ./internal/webserver/ 2>&1 | grep -q "AcceptDNS\|checkHealth" && echo "RED-as-expected: compile gap present" || (go test ./internal/webserver/ -run TestCheckHealth_AcceptDNS 2>&1 | grep -q FAIL && echo "RED")</automated>
  </verify>
  <acceptance_criteria>
    - behavior: `TestCheckHealth_AcceptDNS` exists in internal/webserver/tailscale_test.go.
    - behavior: The package fails to compile OR the test FAILS, because AcceptDNS / the prefs probe do not exist yet (documented RED for DNS-03).
    - source: The test uses table cases covering CorpDNS=false→false, CorpDNS=true→true, and prefs-error→false (no panic).
    - source: The test references DNS-03 in a comment.
  </acceptance_criteria>
  <done>TestCheckHealth_AcceptDNS is present and RED (compile gap or test FAIL) for the documented DNS-03 reason; tailscale.go untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Write TestProxyRemoteFiles_AcceptDNSMessage (DNS-01/DNS-02 RED)</name>
  <files>internal/daemon/remote_files_test.go</files>
  <read_first>
    - internal/daemon/remote_files.go (proxyRemoteFiles error path on client.Do failure ~line 255-258; redactCapTokenFromError usage)
    - internal/daemon/remote_files_parity_test.go (newFixtureRemotePeer, newDaemonAPIWithUpstreamCert, fixtureCap, depositCapOnSocket patterns)
    - internal/daemon/remote_files_test.go (existing proxy test idioms — engine.configDir, t.TempDir usage)
  </read_first>
  <action>
    Add `func TestProxyRemoteFiles_AcceptDNSMessage(t *testing.T)` to internal/daemon/remote_files_test.go. Drive `proxyRemoteFiles` through the daemon surface with a cap deposited against a baseURL whose host is a MagicDNS name (e.g. `https://peer.example-tail.ts.net`) that does not resolve, forcing `client.Do` to return a `*net.DNSError`. Assert: (1) DNS-01 — the 502 response body contains the actionable string "Enable Tailscale DNS (accept-dns) to browse remote sessions"; (2) DNS-02 discrimination — a second sub-case where the upstream baseURL is a reachable non-MagicDNS host that returns a connection-refused / TLS error must NOT contain the actionable DNS string (it gets the generic "remote unreachable" body). To force a deterministic DNSError, point the cap's baseURL at a `.ts.net` hostname guaranteed not to resolve; if the test environment's resolver returns NXDOMAIN reliably this yields `*net.DNSError`. For the non-DNS sub-case, deposit a cap pointing at `https://127.0.0.1:1` (connection refused, not a DNSError). Both sub-cases hit the SAME handler. The test FAILS now because proxyRemoteFiles emits only the generic message for all client.Do errors. Reference DNS-01 and DNS-02. Do NOT modify remote_files.go.
  </action>
  <verify>
    <automated>go test ./internal/daemon/ -run TestProxyRemoteFiles_AcceptDNSMessage 2>&1 | grep -q FAIL && echo "RED" || (echo "NOT-RED — investigate" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - behavior: `TestProxyRemoteFiles_AcceptDNSMessage` exists and FAILS (RED) because the DNS-specific 502 message is not yet emitted.
    - source: The MagicDNS sub-case asserts the body contains "Enable Tailscale DNS (accept-dns) to browse remote sessions" (DNS-01).
    - source: The non-MagicDNS / connection-refused sub-case asserts the body does NOT contain that string (DNS-02 discrimination — not a blanket catch-all).
    - source: The cap-token literal (fixtureCap) does not appear anywhere in the asserted response bodies (preserves existing cap-redaction guarantee).
  </acceptance_criteria>
  <done>TestProxyRemoteFiles_AcceptDNSMessage is RED for the documented DNS-01/DNS-02 reason; both MagicDNS and non-MagicDNS sub-cases present; remote_files.go untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Write TestRemoteFiles_TwoWriterRace_RelaySurface + sandbox-backed upstream (RACE-01 relay RED) and confirm pre-existing race RED baseline</name>
  <files>internal/daemon/relay_remote_files_test.go, internal/daemon/remote_files_parity_test.go</files>
  <read_first>
    - internal/daemon/relay_remote_files_test.go (TestRemoteFiles_MountedOnRelay — relaySrv/socketSrv/depositCapOnSocket pattern; relay write URL shape; If-Match header forwarding)
    - internal/daemon/remote_files_parity_test.go (newFixtureRemotePeer — the upstream PUT /api/files/write handler that currently returns {"ok":true} with no real write)
    - internal/files/sandbox.go (NewSandbox, WriteFileAtomic, validator format, ErrPreconditionFailed)
    - internal/files/write_test.go lines 1140-1235 (TestWrite_TwoWritersIfMatchRace — the single-winner invariant assertions to mirror)
  </read_first>
  <action>
    Two parts.

    Part A — sandbox-backed upstream write handler: The existing `newFixtureRemotePeer` PUT handler discards the body and returns {"ok":true}, so it cannot demonstrate single-winner. Add a NEW helper `newSandboxBackedRemotePeer(t *testing.T) (*httptest.Server, string)` in internal/daemon/relay_remote_files_test.go (or remote_files_parity_test.go) that backs `PUT /api/files/write` with a real `files.Sandbox` rooted at a `t.TempDir()`, seeded with a known file. The handler must read the If-Match header, translate it into the `expectedValidator` argument, call `sb.WriteFileAtomic(path, body, validator)`, and map `files.ErrPreconditionFailed` → HTTP 412, success → 200. Return the server and the temp root path so the test can read final file content. Keep cap-guarding via `fixtureCap`.

    Part B — relay-surface race test: Add `func TestRemoteFiles_TwoWriterRace_RelaySurface(t *testing.T)`. Wire the sandbox-backed upstream through `newDaemonAPIWithUpstreamCert`, deposit the cap on the socket surface, then fire TWO concurrent PUT requests through `api.RelayHandler()` to `/api/files/remote/<sid>/write?path=<seeded>` — both carrying the SAME stale If-Match validator captured before either write. Assert single-winner: exactly one relay response is 200 and exactly one is 412 (or the proxy's mapped conflict status); the final on-disk file in the temp root is all-A or all-B (never interleaved); no `.agenthub-tmp-*` sibling remains. This test FAILS now because the upstream sandbox has the TOCTOU race (both writers win → 2x 200, possibly interleaved). Reference RACE-01.

    Part C — confirm the pre-existing baseline: run TestWrite_TwoWritersIfMatchRace and confirm it currently fails (nilCount=2). Record the observed counts in the SUMMARY. Do NOT modify sandbox.go or write_test.go.
  </action>
  <verify>
    <automated>go test ./internal/daemon/ -run TestRemoteFiles_TwoWriterRace_RelaySurface 2>&1 | grep -q FAIL && echo "RELAY-RED" ; go test ./internal/files/ -run TestWrite_TwoWritersIfMatchRace 2>&1 | grep -q FAIL && echo "BASELINE-RED"</automated>
  </verify>
  <acceptance_criteria>
    - behavior: `newSandboxBackedRemotePeer` exists and its PUT handler performs a real WriteFileAtomic with If-Match→validator translation, mapping ErrPreconditionFailed→412.
    - behavior: `TestRemoteFiles_TwoWriterRace_RelaySurface` exists and FAILS (RED) — the unfixed upstream sandbox lets both writers win through the relay.
    - source: The race test asserts exactly-one-200 / exactly-one-412, final content all-A-or-all-B, and zero `.agenthub-tmp-*` files in the temp root.
    - behavior: Both PUTs traverse `api.RelayHandler()` (the GUI's loopback surface), not the webserver/fixture surface.
    - behavior: Pre-existing TestWrite_TwoWritersIfMatchRace confirmed RED (nilCount=2) and the observed counts recorded in SUMMARY.
  </acceptance_criteria>
  <done>Relay-surface race test + sandbox-backed upstream exist and are RED through the relay loopback; pre-existing race baseline confirmed RED and recorded; no production code modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview/GUI → daemon relay (loopback TCP) | The GUI's actual file-op path; tests here MUST exercise this surface (RelayHandler), not just the webserver fixture |
| daemon proxy → remote tailnet peer (HTTPS + cap) | Untrusted upstream; cap token must never leak into error bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01 | Tampering | Test asserts wrong surface (webserver/fixture only) | mitigate | Wave 0 relay race test drives api.RelayHandler() — the GUI loopback — closing the v3.5 blind spot |
| T-129-02 | Information Disclosure | Cap token in asserted error bodies | mitigate | Task 2 asserts fixtureCap literal is absent from all asserted 502 bodies |
| T-129-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this phase (RESEARCH Package Legitimacy Audit: none); no install tasks |
</threat_model>

<verification>
- `go vet ./internal/webserver/ ./internal/daemon/` compiles the test files (Task 1 may intentionally not compile until Plan 03 — document the gap).
- All three new tests are RED for their documented reasons.
- Pre-existing `TestWrite_TwoWritersIfMatchRace` confirmed RED (nilCount=2).
- No files outside the four test files in `files_modified` are changed.
</verification>

<success_criteria>
- TestCheckHealth_AcceptDNS, TestProxyRemoteFiles_AcceptDNSMessage, TestRemoteFiles_TwoWriterRace_RelaySurface all exist.
- Each fails (RED) for the documented Phase-129 reason — never green, never passing trivially.
- The sandbox-backed upstream write fixture exists for the relay race test.
- No production (non-test) code modified.
</success_criteria>

<output>
Create `.planning/phases/129-write-concurrency-fix-dns-error-ux/129-01-SUMMARY.md` when done. Record the observed `nilCount`/`precondFailCount` from the pre-existing race test baseline.
</output>
