---
milestone: v4.1
milestone_name: Session Chat
audited: 2026-06-28T23:55:00Z
resolved: 2026-06-28T23:59:00Z   # operator dispositions applied (see Resolution)
scope: phases 151-164 (full milestone — supersedes the 151-157 partial audit of 2026-06-27)
status: passed
scores:
  requirements: 45/45   # INSTALL-01 accepted (live install deferred to release-time by decision); traceability gap closed
  phases: 14/14         # all VERIFICATION.md present + status passed; all live UAT discharged
  integration: 14/14    # all cross-phase wiring WIRED at HEAD (1 non-blocking warning)
  flows: 6/6            # M-25 deferred to release-time, not a milestone blocker
gaps:
  requirements: []   # INSTALL-01 accepted-deferral (below); traceability gap RESOLVED 2026-06-28
  traceability: []   # RESOLVED — ALIAS-UI-01/02, SETTINGS-UI-01, ROCHAT-01/02, SEC-RO-01 backfilled into REQUIREMENTS.md
  integration: []    # none — all cross-phase connections WIRED at HEAD
  flows: []
accepted_deferrals:
  - id: "INSTALL-01"
    status: "accepted (code complete; live verify deferred to release-time)"
    phase: "Phase 156"
    decision: >
      Operator decision 2026-06-28: main will NOT be pushed to origin until the team
      is ready to cut the release — consistent with this repo's local-unpushed-until-release
      pattern (v3.6/v4.0 tags are local-unpushed too). scripts/install.sh passes every
      static gate (shellcheck 11/11, sh -n exit 0, WR-01 fixed-string grep -F closed in
      Phase 160). The end-to-end clean-box install (M-25) runs as a release-time step
      AFTER the push makes the raw.githubusercontent install.sh URL resolve. Accepted as
      passed for milestone-close purposes; M-25 remains a tracked release checklist item.
behavior_unverified:
  - id: "INSTALL-03"
    phase: "Phase 156"
    item: >
      winget catalog publish is repo-side complete (distribute.yml first-submission
      path dry-run verified, WINGET_TOKEN present) but actual catalog availability is
      gated on Microsoft's winget-pkgs PR review/merge — an external dependency, as
      designed. Counted satisfied (repo-side) per the requirement's own escape clause.
nyquist:
  compliant_phases: ["151","152","153","154","155","156","157","159","160","161"]
  partial_phases: []
  missing_phases: ["158","162","163","164"]   # VALIDATION.md absent; orthogonal UI/CSS phases, behavior covered by discharged UAT
  overall: compliant_with_gaps
tech_debt:
  - phase: 160-v4-1-chat-closeout
    items:
      - "NOTIF-01 WARNING (integration checker): useChatUnreadListeners.ts:78 effect deps exclude relayPort + openModalSessionId. Core flow works; a relayPort 0→valid arrival after the last effect run, or the stale open-modal exclusion, are order-dependent cosmetic edges. Recommend adding both to the dep array. Non-blocking."
  - phase: 153-session-pty-bridge
    items:
      - "IN-02 closed in Phase 160 (TestInject_ControlOnlyInput, control-only frame → 0 PTY writes)."
      - "IN-04 doc-comment overstatement addressed in Phase 160-05."
  - phase: traceability
    items:
      - "ALIAS-UI-01/02, SETTINGS-UI-01, ROCHAT-01/02, SEC-RO-01 missing from REQUIREMENTS.md (see gaps.traceability)."
---

# v4.1 "Session Chat" — Milestone Audit (Full Scope, Phases 151–164)

**Audited:** 2026-06-28
**Status:** ✓ PASSED (after operator dispositions — see Resolution)
**Scope:** Phases 151–164 (54/54 plans). Supersedes the 2026-06-27 partial audit (151–157).

## Resolution (2026-06-28)

The audit's two non-code gaps were dispositioned by the operator:

1. **INSTALL-01 / M-25 — accepted as passed.** main will not be pushed to origin until
   the team is ready to cut the release (the repo's standard local-unpushed-until-release
   pattern). install.sh is code-complete and passes all static gates; the live clean-box
   install is a **release-time checklist step**, not a milestone-close blocker.
2. **Traceability gap — closed.** ALIAS-UI-01/02 (161), SETTINGS-UI-01 (162), and
   ROCHAT-01/02 + SEC-RO-01 (163) were backfilled into `.planning/REQUIREMENTS.md`
   (requirement list + traceability table + coverage; total now 45/45).

**The milestone is clear for `/gsd-complete-milestone v4.1`.** One non-blocking tech-debt
item remains (the `useChatUnreadListeners` effect-deps warning) and the M-25 live install
is carried on the release checklist.

## Executive Summary

The v4.1 Session Chat milestone is **functionally complete and integration-clean.** Go
builds, frontend `tsc` is clean, and the gsd-integration-checker confirms all
cross-phase wiring for phases 151–164 resolves end-to-end at HEAD with **zero broken
flows.** Every phase carries a `passed` VERIFICATION.md, and every live-UAT item that
phase verification deferred (`human_needed`) has since been **discharged** in the
corresponding UAT.md:

- **158** M-29 (toggle/Send non-overlap) + M-30 (terminal-tab chat overlay, no PTY resize) — **pass** (headless Chromium, real style.css).
- **159** M-31 web-share chat parity on the actually-shared link — Tests 1/2/4 **pass LIVE** over real Tailscale TLS; Test 3 (RO guest can post) **resolved by Phase 163**.
- **162** Terminal-Plugins jump link + Settings search — **3/3 pass**.
- **163** RO guest chat posting + inject still RO-gated — **2/2 pass**, SEC-RO-01 via 163-SECURITY.md (threats_open: 0).
- **164** header-overflow fix + resizable chat width — **9/9 pass**.

**The prior audit's sole BLOCKER (NOTIF-01) is genuinely closed.** Phase 160 added the
missing closed-modal unread source (`useChatUnreadListeners` opens a per-backgrounded-session
read-only RelayClient and reuses ChatPanel's `accrueUnread`) and threaded
`unreadBySessionId` through `HubPanel → SessionCardGrid → SessionCard → ChatBadge` on
**both** card render paths — exactly the dead wiring the 151–157 audit flagged. Phase 160
also shipped a clean SECURITY.md (threats_open: 0) and 15/15 must-haves.

**Phase 163 reversed a security-reviewed decision correctly.** Only `HandleChatSend`'s RO
gate was loosened; `HandleInject` (`ErrReadOnly`) and the `MsgInput` discard remain
byte-for-byte intact on **both** relay and webserver read pumps, guarded by
`TestHandleChatSend_ROCanPostInjectStillGated` (green).

**Two gaps keep this from a clean pass — neither is a code defect:**

1. **INSTALL-01 / M-25 (blocked, not broken).** The Linux clean-box install can't be
   verified end-to-end because **local main is ~381 commits ahead of origin/main** — the
   `raw.githubusercontent.com/.../main/scripts/install.sh` URL 404s until main is pushed.
   install.sh itself passes all static gates. **Operator action:** push/release main to
   origin, then run TESTING.md M-25 (or `/gsd-verify-work 156` Test 1).

2. **Traceability doc gap.** 5 implemented + verified requirements — ALIAS-UI-01/02
   (161), SETTINGS-UI-01 (162), ROCHAT-01/02 + SEC-RO-01 (163) — are absent from
   `.planning/REQUIREMENTS.md`. They are wired and tested; the requirements doc just
   never recorded them. Reconcile before archiving.

## Requirements Coverage (3-source cross-reference)

45 requirements total (39 registered in REQUIREMENTS.md + 6 implemented-but-unregistered).
Cross-checked against each phase VERIFICATION.md table, SUMMARY frontmatter, and the
REQUIREMENTS.md traceability table.

| Outcome | Count | IDs |
|---------|-------|-----|
| Satisfied | 44 | CHAT-01..04, IDENT-01/02, MENTION-01/02/03, PRESENCE-01/02, PERSIST-01/02/03, EXPORT-01, NOTIF-01/02, PARITY-01, WEBCHAT-01..06, CHAT-LAYOUT-01/02, SEC-01/02/03, INSTALL-02/03, VIEW-01..05, CHAT-FIX-01, CHAT-PARITY-01, ALIAS-UI-01/02*, SETTINGS-UI-01*, ROCHAT-01/02*, SEC-RO-01* |
| **Blocked-not-broken** | 1 | **INSTALL-01** — code correct; M-25 live install blocked on origin push |

\* satisfied but **not registered** in REQUIREMENTS.md (traceability gap — see Gaps).

> NOTIF-01 moves from the prior audit's "partial/broken" to **satisfied** — Phase 160
> wired both clauses (toggle badge + Hub-card badge) end-to-end, integration-confirmed.

## Phase Verification Roll-up

| Phase | VERIFICATION | Live UAT | Notes |
|-------|--------------|----------|-------|
| 151 Message Schema + ChatStore | passed 5/5 | n/a | clean |
| 152 Relay Protocol + Identity + Presence | passed 4/5 | discharged | M-18/M-19 live pass |
| 153 @session PTY Bridge | passed 4/4 | n/a | IN-02/IN-04 closed in 160 |
| 154 Desktop Chat UI | passed 5/5 | discharged | NOTIF-01 hub-card later wired by 160 |
| 155 Web-Share + Parity Gate | passed 4/4 | green | PARITY-01 Playwright 24/24 ×3 |
| 156 Install Links & Distribution | passed 2/3 | **M-25 BLOCKED** | M-26 winget pass; M-25 on origin push |
| 157 Terminal Screen-Share (#109) | passed | discharged | M-27/M-28 live pass |
| 158 Chat affordance polish | passed | **M-29/M-30 pass** | TerminalChatHost reuses ChatPanel |
| 159 Web-Share Chat Parity | passed 5/5 | **M-31 pass LIVE** | Test 3 resolved by 163 |
| 160 v4.1 Chat Closeout (NOTIF-01) | passed 15/15 | n/a | **prior BLOCKER closed**; SECURITY clean |
| 161 Chat-Sidebar Alias Control | passed 5/6 | discharged | MsgSelf/MsgAliasSet round-trip live |
| 162 Settings Polish (#108) | passed 5/6 | **3/3 pass** | #settings-plugins anchor |
| 163 RO Guest Chat Posting (D-06) | passed 10/10 | **2/2 pass** | inject/PTY still RO-gated; SECURITY clean |
| 164 Web-Share Chat Layout Polish | passed 9/9 | pass | resizable width, no PTY resize (D-02) |

## Cross-Phase Integration (gsd-integration-checker, full 14-phase sweep)

**14/14 connections WIRED at HEAD. Zero broken flows.** Highlights:

- **NOTIF-01 (prior BLOCKER) → WIRED.** `useChatUnreadListeners.ts:26-78` (closed-modal
  unread source) → `HubPanel.unreadMap` → `SessionCardGrid` (both render sites :287/:339)
  → `SessionCard:450` → `ChatBadge`. In-modal path via `HubInteractiveModal:72 → HubModal:251`.
- **163 RO gate → WIRED + safe.** `hub.go:661-691 HandleChatSend` no RO check;
  `hub.go:582-587 HandleInject` keeps `ErrReadOnly`; MsgInput RO-discard at
  `relay/server.go:342` + `webserver/server.go:1189`.
- **159 redirect → WIRED.** `requireCapability` precedes `handleTerminalPage` 302
  (no-cap → 401, no Location); WEBCHAT-03/04/05 scoping confirmed.
- **164 → WIRED.** `clampChatWidth` + `--chat-panel-width`; resize never calls `sendResize` (D-02).
- **161 → WIRED.** MsgSelf (0x37) emitted on both surfaces; sendAliasSet/MsgAliasSet round-trip.

**WARNING (non-blocking, tech debt):** `useChatUnreadListeners.ts:78` effect deps exclude
`relayPort` and `openModalSessionId` — order-dependent cosmetic edges on the NOTIF-01 flow.

## Blocked, not broken

### INSTALL-01 — Linux install (M-25)

`scripts/install.sh` is correct (all static gates pass). The clean-box end-to-end install
cannot run until `main` is pushed to origin — the raw-GitHub URL 404s on ~381 unpushed
commits. **Operator action:** push/release main to origin, then re-run M-25.

## Traceability Gap (doc, blocking archive-cleanliness)

`.planning/REQUIREMENTS.md` does not list or trace: **ALIAS-UI-01, ALIAS-UI-02,
SETTINGS-UI-01, ROCHAT-01, ROCHAT-02, SEC-RO-01.** All six are implemented, verified
(phase VERIFICATION + UAT), and integration-confirmed WIRED. Add them to the requirements
list + traceability table (mapped to phases 161/162/163) so the milestone record is honest.

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 151–157, 159, 160, 161 | exists | true | — |
| 158, 162, 163, 164 | **missing** | n/a | orthogonal UI/CSS phases; behavior covered by discharged live UAT — optional `/gsd-validate-phase` to formalize |

## Tech Debt (non-blocking)

See YAML. Summary: NOTIF-01 effect-deps warning (160), 153 IN-02/IN-04 (closed in 160),
and the REQUIREMENTS.md traceability gap. None block the milestone goal.

---

_Audited by Claude (gsd-audit-milestone), full 14-phase scope. Integration sweep by
gsd-integration-checker (NOTIF-01 closure independently confirmed at
useChatUnreadListeners.ts + SessionCardGrid.tsx:287/339 + SessionCard.tsx:450; 163 RO
gates at hub.go:582-691)._
