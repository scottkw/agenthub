---
phase: 101-shell-session-surfaces-web-share-gating
plan: 04
type: execute
wave: 2
depends_on:
  - 101-01
files_modified:
  - cmd_cli.go
  - cmd_cli_test.go
  - main.go
  - internal/tui/tui.go
  - internal/tui/modal.go
  - internal/tui/styles.go
  - internal/tui/view.go
  - internal/tui/styles_test.go
  - internal/tui/update_test.go
autonomous: true
requirements:
  - SHELL-02
  - SHELL-03
  - SHELL-06

must_haves:
  truths:
    - "agenthub new shell creates a shell session with cli='shell' (system default) when no --shell flag is given"
    - "agenthub new shell --shell=bash sends cli='bash' to the daemon"
    - "agenthub new shell --shell=zsh sends cli='zsh' to the daemon"
    - "agenthub new shell --shell=pwsh sends cli='pwsh' to the daemon"
    - "agenthub new shell --shell=powershell sends cli='powershell' to the daemon"
    - "agenthub new shell --shell=nope exits with code 1 and stderr matches the locked copy: 'agenthub new shell: unknown shell \"nope\" (allowed: bash, zsh, pwsh, powershell, or omit for system default)'"
    - "agenthub new shell --shell= (empty value with flag set) exits with code 1 and stderr matches: 'agenthub new shell: --shell flag requires a value (one of: bash, zsh, pwsh, powershell)'"
    - "agenthub new shell <path> with no flag sends cli='shell' and workDir=<path>"
    - "agenthub new shell with no positional path sends workDir='' (daemon will resolve to $HOME per Phase 100)"
    - "agenthub new shell -- <extra-args> emits a stderr warning 'agenthub new shell: extra arguments are not forwarded to shell sessions; ignoring [...]' and sends args=nil to the daemon"
    - "agenthub new shell daemon-unreachable produces stderr 'agenthub new shell: daemon unreachable: <err>' with exit code 1"
    - "agenthub usage() output includes the new 'new shell' subcommand documentation per UI-SPEC §CLI"
    - "main.go dispatch handles 'new shell' as a sub-subcommand: cmdArgs[0]=='shell' routes to cmdNewShell"
    - "main.go dispatch for 'new <agent>' (existing) is unchanged when cmdArgs[0] is not 'shell'"
    - "TUI tui.go initializes a detectedShells slice via pty.DiscoverShells() alongside the existing detectedCLIs (per RESEARCH A1: TUI calls pty directly in-process)"
    - "TUI agent picker (modal.go renderAgentPicker) cycles in order: AI CLIs first (existing order), then 'Shell — system default', then per-discovered-shell entries"
    - "TUI agent picker renders 'Shell — DISPLAYNAME' as the picker label for shell entries (single-line, no path)"
    - "TUI styles.go Styles struct contains a new BadgeShell color.Color field"
    - "TUI styles.go newStyles initializes BadgeShell = ld(lipgloss.Color('#3d5a80'), lipgloss.Color('#89ddff'))"
    - "TUI agentBadgeColor returns s.BadgeShell for cli in {shell, bash, zsh, pwsh, powershell}"
    - "TUI session-list-row rendering uses BadgeShell for shell sessions in view.go (no fallthrough to FgMuted)"
  artifacts:
    - path: cmd_cli.go
      provides: "cmdNewShell function + usage() additions"
      contains: "func cmdNewShell"
    - path: main.go
      provides: "dispatch: case 'new' inspects cmdArgs[0] for 'shell' subcommand"
      contains: "cmdNewShell"
    - path: internal/tui/tui.go
      provides: "detectedShells slice initialization via pty.DiscoverShells()"
      contains: "DiscoverShells"
    - path: internal/tui/modal.go
      provides: "renderAgentPicker rendering of shell entries with 'Shell — ' prefix"
      contains: "Shell — "
    - path: internal/tui/styles.go
      provides: "BadgeShell color.Color field + initialization + agentBadgeColor switch case"
      contains: "BadgeShell"
    - path: internal/tui/view.go
      provides: "session-list-row uses BadgeShell for shell sessions"
      contains: "BadgeShell"
    - path: cmd_cli_test.go
      provides: "TestCmdNewShell_* test functions"
      contains: "TestCmdNewShell"
    - path: internal/tui/styles_test.go
      provides: "TestAgentBadgeColor_Shell asserting badge color resolution for shell + bash + zsh + pwsh + powershell"
      contains: "BadgeShell"
    - path: internal/tui/update_test.go
      provides: "TestAgentPicker_ShellEntries asserting picker cycles include shell entries"
      contains: "Shell — "
  key_links:
    - from: cmd_cli.go
      to: internal/daemon.DaemonClient.CreateSession
      via: "cmdNewShell calls client.CreateSession with cli=shell|bash|zsh|pwsh|powershell"
      pattern: "CreateSession"
    - from: main.go
      to: cmd_cli.cmdNewShell
      via: "dispatch on cmdArgs[0]==\"shell\" within case \"new\""
      pattern: "cmdNewShell"
    - from: internal/tui/tui.go
      to: internal/pty.DiscoverShells
      via: "init populates detectedShells field via pty.DiscoverShells() (RESEARCH A1: TUI runs in-process)"
      pattern: "pty\\.DiscoverShells\\("
    - from: internal/tui/styles.go
      to: lipgloss
      via: "BadgeShell uses ld(...) adaptive light/dark helper, same as other badge colors"
      pattern: "BadgeShell"
---

<objective>
Surface shell sessions in the CLI (SHELL-02) and TUI (SHELL-03), and apply the distinct agent badge color to the TUI (SHELL-06 TUI half). The CLI gets a new `agenthub new shell` sub-subcommand with `--shell=bash|zsh|pwsh|powershell` flag. The TUI agent picker is extended with shell entries in a deterministic order. The TUI session-list badge color uses a new BadgeShell color matching the GUI's #89ddff (dark) / #3d5a80 (light) palette.

Purpose: Without this plan, only the GUI can launch shells (Plan 02) and only the GUI can show the distinct badge (Plan 02). CLI and TUI users would be excluded from the new agent type. This plan is Go-only — touches `cmd_cli.go`, `main.go`, and `internal/tui/*` — with zero overlap with Plan 02 (frontend) or Plan 03 (frontend banner), so it runs in Wave 2 in parallel.

Output: 7 Go source files modified (1 CLI, 1 main dispatch, 4 TUI), 3 Go test files modified (cmd_cli_test.go, styles_test.go, update_test.go). Zero new files. Zero new dependencies. Banner gating (SHELL-08) is intentionally GUI-only — per UI-SPEC §Copywriting "Banner shows on the FIRST toggle-ON for any shell session, ever" — CLI/TUI users do not encounter the banner; they enable web sharing via the same daemon API which already has the warned-flag persisted via Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/101-shell-session-surfaces-web-share-gating/101-UI-SPEC.md
@.planning/phases/101-shell-session-surfaces-web-share-gating/101-RESEARCH.md
@.planning/phases/100-shell-session-backend-discovery/100-PATTERNS.md
@.planning/phases/100-shell-session-backend-discovery/100-04-SUMMARY.md
@cmd_cli.go
@main.go
@cmd_cli_test.go
@internal/tui/tui.go
@internal/tui/modal.go
@internal/tui/styles.go
@internal/tui/view.go
@internal/pty/shells.go

<interfaces>
Phase 100 outputs (already in place):

- pty.DiscoverShells() returns []DetectedShell with fields Name, DisplayName, Path, Argv.
- DaemonClient.CreateSession(...) accepts cli (string), workDir (string), args ([]string), and returns sessionID.
- knownShellSpecs in pty: bash, zsh, pwsh, powershell (4 entries per M2 lock-in).

Existing cmdNew shape (cmd_cli.go ~L58):
  func cmdNew(client *DaemonClient, args []string, extraArgs []string, out io.Writer) error
- args[0] is the AI CLI name; args[1] is the workDir.
- extraArgs is the `-- ...` tail. For AI CLIs, this is forwarded as the session args.
- Prints session UUID to out on success.

Existing main.go dispatch (~L171):
  case "new":
      err = cmdNew(client, cmdArgs, extraArgs, os.Stdout)

Locked CLI copy (UI-SPEC §Copywriting + RESEARCH §5):

| Trigger | Stderr text |
|---------|-------------|
| Unknown --shell value | agenthub new shell: unknown shell "VALUE" (allowed: bash, zsh, pwsh, powershell, or omit for system default) |
| Empty --shell= | agenthub new shell: --shell flag requires a value (one of: bash, zsh, pwsh, powershell) |
| Extra args after -- | agenthub new shell: extra arguments are not forwarded to shell sessions; ignoring [ARGS] |
| Daemon unreachable | agenthub new shell: daemon unreachable: ERR |

usage() addition (verbatim per UI-SPEC):
  new shell [<path>]                          Create a new raw shell session
    --shell=bash|zsh|pwsh|powershell           Pick a specific shell (default: system default)

TUI styles.go BadgeShell initialization shape (per UI-SPEC §Component Contracts: TUI):
  BadgeShell: ld(lipgloss.Color("#3d5a80"), lipgloss.Color("#89ddff"))

Existing agentBadgeColor pattern in styles.go (per Phase 100 PATTERNS.md / RESEARCH):
  switch cli {
  case "claude": return s.BadgeClaude
  // ... 6 cases ...
  default: return s.FgMuted
  }

Add case (per RESEARCH §2.7):
  case "shell", "bash", "zsh", "pwsh", "powershell":
      return s.BadgeShell

TUI agent picker cycle order (UI-SPEC §Interaction TUI flow):
  Claude Code → OpenAI Codex → Gemini CLI → OpenCode → Shell — system default → Shell — bash → Shell — zsh → Shell — pwsh → (wraps)

Note on TUI runtime: per RESEARCH §2.7 and Assumption A1, TUI runs in-process with daemon when launched via the agenthub binary (not via the DaemonClient socket). So pty.DiscoverShells() can be called directly from internal/tui/tui.go init — no DaemonClient round-trip needed. Verify this assumption by reading tui.go around line 28 (where detectedCLIs is initialized): if it calls pty.DetectCLIs() directly, shells follow the same pattern. If it uses DaemonClient.DetectCLIs(), shells must use DaemonClient.ListShells() instead. Executor adjusts during implementation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI new shell subcommand (cmd_cli.go, cmd_cli_test.go, main.go) — TDD</name>
  <files>cmd_cli.go, cmd_cli_test.go, main.go</files>
  <read_first>
    - cmd_cli.go full (especially cmdNew at ~L58, usage() at L22-54, the existing flag.NewFlagSet pattern in cmdList)
    - cmd_cli_test.go (existing test patterns — how DaemonClient is mocked or stubbed, how stdout/stderr are captured)
    - main.go (dispatch around L171, look at how cmdNew is invoked and what's in scope)
    - .planning/phases/101-shell-session-surfaces-web-share-gating/101-RESEARCH.md §5 (CLI argv shape) + §2.6
    - .planning/phases/101-shell-session-surfaces-web-share-gating/101-UI-SPEC.md §CLI (locked copy)
  </read_first>
  <behavior>
    Step 1 RED: add test functions to cmd_cli_test.go before the implementation exists.

    Tests (use a stub DaemonClient mock or test-double — match existing test patterns):
    1. TestCmdNewShell_NoArgs_UsesSystemDefault — call cmdNewShell with args=[], extraArgs=nil; assert the stub DaemonClient.CreateSession was called with cli="shell", workDir=""; assert stdout printed a session UUID; assert error is nil.
    2. TestCmdNewShell_PositionalPath_UsesShell — call with args=["/home/user/proj"], extraArgs=nil; assert cli="shell", workDir="/home/user/proj".
    3. TestCmdNewShell_FlagBash — args=["--shell=bash"]; assert cli="bash", workDir="".
    4. TestCmdNewShell_FlagZsh — args=["--shell=zsh"]; assert cli="zsh".
    5. TestCmdNewShell_FlagPwsh — args=["--shell=pwsh"]; assert cli="pwsh".
    6. TestCmdNewShell_FlagPowerShell — args=["--shell=powershell"]; assert cli="powershell".
    7. TestCmdNewShell_FlagAndPath — args=["--shell=zsh", "/tmp"]; assert cli="zsh", workDir="/tmp".
    8. TestCmdNewShell_UnknownShellFlag — args=["--shell=nope"]; assert error returned; assert stderr (captured) contains the literal: `agenthub new shell: unknown shell "nope" (allowed: bash, zsh, pwsh, powershell, or omit for system default)`. Assert DaemonClient.CreateSession was NOT called.
    9. TestCmdNewShell_EmptyShellFlag — args=["--shell="]; assert stderr contains: `agenthub new shell: --shell flag requires a value (one of: bash, zsh, pwsh, powershell)`. Assert exit-equivalent error returned.
    10. TestCmdNewShell_ExtraArgsWarning — args=["/tmp"], extraArgs=["--foo", "bar"]; assert stderr contains `agenthub new shell: extra arguments are not forwarded to shell sessions; ignoring [--foo bar]`; assert CreateSession called with args=nil (NOT extraArgs); assert command succeeds (the warning is non-fatal).
    11. TestCmdNewShell_DaemonError — stub CreateSession to return an error; assert returned error message contains `daemon unreachable` or the propagated error string; assert exit-equivalent return.
    12. TestUsage_IncludesNewShell — call the usage() function (capture its output); assert it contains the literal `new shell [<path>]` AND `--shell=bash|zsh|pwsh|powershell`.

    Step 2 GREEN: implement cmdNewShell + extend usage() + add dispatch.

    cmd_cli.go additions:
    1. Add usage() lines (per UI-SPEC verbatim, place near the existing `new <agent>` line):
         new shell [<path>]                          Create a new raw shell session
           --shell=bash|zsh|pwsh|powershell           Pick a specific shell (default: system default)
    2. Implement cmdNewShell:
         func cmdNewShell(client *DaemonClient, args []string, extraArgs []string, out io.Writer) error {
             fs := flag.NewFlagSet("new shell", flag.ContinueOnError)
             fs.SetOutput(io.Discard)  // we emit our own error copy
             shellFlag := fs.String("shell", "", "")
             if err := fs.Parse(args); err != nil {
                 fmt.Fprintf(os.Stderr, "agenthub new shell: %v\n", err)
                 return err
             }
             // Detect explicit empty: fs.Visit detects whether --shell was passed at all.
             explicitlySet := false
             fs.Visit(func(f *flag.Flag) { if f.Name == "shell" { explicitlySet = true } })
             if explicitlySet && *shellFlag == "" {
                 msg := "agenthub new shell: --shell flag requires a value (one of: bash, zsh, pwsh, powershell)"
                 fmt.Fprintln(os.Stderr, msg)
                 return errors.New("empty --shell value")
             }
             allowed := map[string]bool{"": true, "bash": true, "zsh": true, "pwsh": true, "powershell": true}
             if !allowed[*shellFlag] {
                 msg := fmt.Sprintf("agenthub new shell: unknown shell %q (allowed: bash, zsh, pwsh, powershell, or omit for system default)", *shellFlag)
                 fmt.Fprintln(os.Stderr, msg)
                 return errors.New("unknown --shell value")
             }
             cli := "shell"
             if *shellFlag != "" { cli = *shellFlag }
             workDir := ""
             positionals := fs.Args()
             if len(positionals) > 0 { workDir = positionals[0] }
             if len(extraArgs) > 0 {
                 fmt.Fprintf(os.Stderr, "agenthub new shell: extra arguments are not forwarded to shell sessions; ignoring %v\n", extraArgs)
             }
             id, err := client.CreateSession(cli, "", workDir, nil, 0, 0)  // adjust signature to match existing DaemonClient.CreateSession
             if err != nil {
                 fmt.Fprintf(os.Stderr, "agenthub new shell: daemon unreachable: %v\n", err)
                 return err
             }
             fmt.Fprintln(out, id)
             return nil
         }
       NOTE: Verify the exact `DaemonClient.CreateSession` signature in client.go before pasting the call — adjust arg list and `name` parameter to match (the existing cmdNew likely uses some convention for the tab name; mirror that). If name must be non-empty, use a default like the workDir basename or "shell".

    main.go dispatch change:
       case "new":
           if len(cmdArgs) > 0 && cmdArgs[0] == "shell" {
               err = cmdNewShell(client, cmdArgs[1:], extraArgs, os.Stdout)
           } else {
               err = cmdNew(client, cmdArgs, extraArgs, os.Stdout)
           }

    Run gofmt + go vet.
  </behavior>
  <action>
    Step 1: Add the 12 test functions to cmd_cli_test.go. Use the existing mock/stub pattern for DaemonClient (read the file to find it — there may be a fakeClient or similar). For stderr capture, redirect os.Stderr to a *os.File-backed pipe in t.Setenv-style cleanup; the existing tests should show the pattern.

    Step 2: Implement cmdNewShell as described in <behavior>. Extend usage(). Wire the dispatch in main.go.

    Step 3: gofmt + go vet + run tests.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -run 'TestCmdNewShell|TestUsage_IncludesNewShell' -race -count=1 -v && go vet ./... && gofmt -l cmd_cli.go cmd_cli_test.go main.go</automated>
  </verify>
  <acceptance_criteria>
    - cmd_cli.go contains func cmdNewShell
    - cmd_cli.go usage() output (verifiable via grep) contains literal "new shell [<path>]"
    - cmd_cli.go contains the locked stderr string: "unknown shell %q (allowed: bash, zsh, pwsh, powershell, or omit for system default)"
    - cmd_cli.go contains the locked stderr string: "--shell flag requires a value (one of: bash, zsh, pwsh, powershell)"
    - cmd_cli.go contains the locked stderr string: "extra arguments are not forwarded to shell sessions"
    - cmd_cli.go contains the locked stderr string: "daemon unreachable"
    - main.go contains the literal token cmdNewShell
    - main.go contains an "if" check for cmdArgs[0] == "shell" inside case "new"
    - cmd_cli_test.go contains all 12 new test function names by literal string match
    - go test -run TestCmdNewShell exits 0 (all 11 tests pass)
    - go test -run TestUsage_IncludesNewShell exits 0
    - go vet ./... exits 0
    - gofmt -l cmd_cli.go cmd_cli_test.go main.go produces no output
  </acceptance_criteria>
  <done>CLI subcommand operational. SHELL-02 closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TUI agent picker + BadgeShell color + session-list rendering (TDD)</name>
  <files>internal/tui/tui.go, internal/tui/modal.go, internal/tui/styles.go, internal/tui/view.go, internal/tui/styles_test.go, internal/tui/update_test.go</files>
  <read_first>
    - internal/tui/tui.go (find detectedCLIs init around L28; determine whether it calls pty.DetectCLIs() directly or via DaemonClient — RESEARCH A1)
    - internal/tui/modal.go (renderAgentPicker around L75-96 — see the cycle pattern)
    - internal/tui/styles.go (find Styles struct + the BadgeXxx fields + the ld() helper + the agentBadgeColor switch)
    - internal/tui/view.go (find the session-list-row rendering and the call to agentBadgeColor)
    - internal/tui/styles_test.go (existing pattern for BadgeXxx tests)
    - internal/tui/update_test.go (existing pattern for agent-picker cycle tests)
    - .planning/phases/101-shell-session-surfaces-web-share-gating/101-UI-SPEC.md §Component Contracts: TUI agent picker extension
    - .planning/phases/101-shell-session-surfaces-web-share-gating/101-RESEARCH.md §2.7
  </read_first>
  <behavior>
    Step 1 RED: add tests.

    styles_test.go additions:
    1. TestAgentBadgeColor_Shell — table-driven: for cli in {"shell", "bash", "zsh", "pwsh", "powershell"}, agentBadgeColor(cli) returns the same color value as s.BadgeShell.
    2. TestAgentBadgeColor_AICLIs_Unchanged — regression: claude/opencode/codex/gemini/cursor/aider still return their respective BadgeXxx colors.
    3. TestAgentBadgeColor_Unknown — agentBadgeColor("unknown-tool") returns FgMuted (existing default; verify no regression).
    4. TestBadgeShell_LightDark_Variants — assert BadgeShell uses ld(...) adaptive form with light="#3d5a80" and dark="#89ddff". Inspection method depends on existing tests' pattern; if the existing BadgeClaude test inspects via lipgloss.Color.RGBA() at specific light/dark profile, mirror that. If no such mechanism exists, assert structural inequality between the BadgeShell color and a known different BadgeXxx — i.e., assert it's a distinct color, since lipgloss's adaptive colors aren't directly comparable as strings.

    update_test.go additions:
    5. TestAgentPicker_IncludesShellEntries — initialize a tui model with detectedCLIs=[claude, opencode] and detectedShells=[{name:"shell",displayName:"system default"}, {name:"bash",displayName:"bash"}]; render the agent picker (call renderAgentPicker or the equivalent display function); assert the output contains "Shell — system default" AND "Shell — bash".
    6. TestAgentPicker_CycleOrder — initialize as #5; cycle right through all entries; assert the visited order is: claude → opencode → Shell — system default → Shell — bash → (wraps to claude). Match UI-SPEC §Interaction TUI flow.
    7. TestAgentPicker_OnlyAICLIs_NoShells — initialize with detectedShells=[]; cycle through; assert only AI CLIs appear; no "Shell — " text in the picker output.

    Step 2 GREEN: implement.

    internal/tui/styles.go:
    1. Add to Styles struct:
         BadgeShell color.Color  // Phase 101 SHELL-06
    2. In newStyles, initialize:
         BadgeShell: ld(lipgloss.Color("#3d5a80"), lipgloss.Color("#89ddff")),
    3. In agentBadgeColor switch, add:
         case "shell", "bash", "zsh", "pwsh", "powershell":
             return s.BadgeShell

    internal/tui/tui.go:
    1. In the model initialization (near where detectedCLIs is populated):
         m.detectedShells = pty.DiscoverShells()  // direct, in-process per RESEARCH A1
       Add the field to the model struct:
         detectedShells []pty.DetectedShell
       Sort the shells client-side: system default first, then bash, zsh, pwsh, powershell in that fixed priority order (mirror the GUI Plan 02 sort). Add a small sort helper:
         func sortShellsForPicker(shells []pty.DetectedShell) []pty.DetectedShell {
             priority := map[string]int{"shell": 0, "bash": 1, "zsh": 2, "pwsh": 3, "powershell": 4}
             out := append([]pty.DetectedShell(nil), shells...)
             sort.SliceStable(out, func(i, j int) bool {
                 pi, oki := priority[out[i].Name]; if !oki { pi = 99 }
                 pj, okj := priority[out[j].Name]; if !okj { pj = 99 }
                 return pi < pj
             })
             return out
         }

    internal/tui/modal.go:
    1. Modify renderAgentPicker. The existing implementation cycles through m.detectedCLIs[m.agentIdx]. Extend the conceptual cycle to include shells. Simplest pattern: build a unified slice of "picker entries":
         type agentEntry struct {
             cliKey      string  // "claude", "shell", "bash", etc. — sent to CreateSession
             displayLabel string // "Claude Code", "Shell — system default", "Shell — bash"
         }
         entries := []agentEntry{}
         for _, c := range m.detectedCLIs { entries = append(entries, agentEntry{cliKey: c.Name, displayLabel: c.DisplayName}) }
         for _, sh := range sortShellsForPicker(m.detectedShells) {
             entries = append(entries, agentEntry{cliKey: sh.Name, displayLabel: "Shell — " + sh.DisplayName})
         }
       Then m.agentIdx indexes into entries instead of detectedCLIs. The selected agent at create-session time is entries[m.agentIdx].cliKey.
    2. The render label is entries[m.agentIdx].displayLabel.
    3. cycleAgent semantics unchanged: increment/wrap m.agentIdx modulo len(entries).
    4. Update any code that reads m.detectedCLIs[m.agentIdx] for the create-session path to use entries[m.agentIdx].cliKey.

    internal/tui/view.go:
    1. Find the session-list-row rendering. It currently resolves the badge color via something like styles.agentBadgeColor(sess.CLI) or similar. The Task 2 change to styles.go already handles shells; if view.go has any inline switch or custom logic that bypasses agentBadgeColor, update it. Otherwise, no change is needed in view.go beyond confirming it uses agentBadgeColor for the badge.

    Run gofmt + go vet.
  </behavior>
  <action>
    Step 1: Add tests #1-7 to styles_test.go and update_test.go. Run go test and confirm RED (compilation should fail because BadgeShell + detectedShells + sortShellsForPicker don't exist yet).

    Step 2: Apply the implementation edits described in <behavior> to styles.go, tui.go, modal.go, view.go. Run gofmt and go vet.

    Step 3: Run the tests and confirm GREEN.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test ./internal/tui -run 'AgentBadgeColor|BadgeShell|AgentPicker' -race -count=1 -v && go test ./internal/tui -race -count=1 && go vet ./internal/tui/... && gofmt -l internal/tui/tui.go internal/tui/modal.go internal/tui/styles.go internal/tui/view.go internal/tui/styles_test.go internal/tui/update_test.go</automated>
  </verify>
  <acceptance_criteria>
    - internal/tui/styles.go contains the literal token "BadgeShell" in the Styles struct AND in newStyles initialization AND in agentBadgeColor switch
    - internal/tui/styles.go contains the literal hex "#89ddff" (dark) and "#3d5a80" (light) for BadgeShell init
    - internal/tui/styles.go contains the case for "shell", "bash", "zsh", "pwsh", "powershell" in agentBadgeColor
    - internal/tui/tui.go contains "pty.DiscoverShells()" (or equivalent shell-discovery call)
    - internal/tui/tui.go contains "detectedShells" field declaration
    - internal/tui/modal.go contains the literal "Shell — " in the picker label render
    - internal/tui/styles_test.go contains func TestAgentBadgeColor_Shell
    - internal/tui/styles_test.go contains func TestBadgeShell_LightDark_Variants (or equivalent)
    - internal/tui/update_test.go contains func TestAgentPicker_IncludesShellEntries
    - internal/tui/update_test.go contains func TestAgentPicker_CycleOrder
    - go test ./internal/tui -run 'AgentBadgeColor|BadgeShell|AgentPicker' -race -count=1 exits 0 (all 7 new tests pass)
    - go test ./internal/tui -race -count=1 exits 0 (no regression to existing TUI tests)
    - go vet ./internal/tui/... exits 0
    - gofmt produces no output for the 6 modified files
  </acceptance_criteria>
  <done>TUI agent picker cycles include shell entries. Shell sessions display the BadgeShell color in the session list. SHELL-03 and SHELL-06 TUI half closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Full Go build + cross-platform smoke + verification</name>
  <files>cmd_cli_test.go</files>
  <read_first>
    - Output of go build ./...
    - Output of go test ./... -race -count=1 (full suite)
    - The list of files_modified in this plan
  </read_first>
  <behavior>
    After this task:
    - Full Go build succeeds.
    - Full Go test suite passes under -race, including:
      - All 11 new TestCmdNewShell_* tests + TestUsage_IncludesNewShell
      - All 7 new TUI tests
      - All Phase 100 tests (no regression)
      - All Plan 101-01 tests (no regression)
    - On platforms where pwsh is not available (e.g., macOS dev box), pwsh-specific tests in Plan 101-01 are skipped — same skip pattern as Phase 100.
    - No file outside this plan's files_modified is touched.
  </behavior>
  <action>
    Run the verification commands. If any test fails, fix in the appropriate file from Task 1 or Task 2.

    Special considerations:
    - cmd_cli_test.go may need additional helper code (e.g., a fakeClient that records calls). If the existing file already has such a helper for cmdNew, reuse it; if not, the executor adds a minimal fake at the top of cmd_cli_test.go. This addition counts as a Task 1 modification, not a Task 3 modification.
    - If TUI tests fail because the test fixture doesn't seed detectedShells, update the fixture inline in update_test.go (this is Task 2 territory).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./... && go test ./... -race -count=1 2>&1 | tail -50 && git diff --name-only | sort -u | tee /tmp/plan101-04-diff.txt; for f in $(cat /tmp/plan101-04-diff.txt); do case "$f" in cmd_cli.go|cmd_cli_test.go|main.go|internal/tui/tui.go|internal/tui/modal.go|internal/tui/styles.go|internal/tui/view.go|internal/tui/styles_test.go|internal/tui/update_test.go|.planning/*) ;; *) echo "UNEXPECTED FILE TOUCHED: $f"; exit 1;; esac; done; echo "All touched files within files_modified scope"</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... exits 0
    - go test ./... -race -count=1 exits 0
    - git diff --name-only shows only files in this plan's files_modified set (plus .planning/* files for SUMMARYs etc.)
    - All 12 new CLI tests pass
    - All 7 new TUI tests pass
    - Phase 100 tests still pass (no regression)
    - Plan 101-01 tests still pass (no regression)
  </acceptance_criteria>
  <done>Plan 101-04 complete end-to-end. CLI and TUI both surface shell sessions with locked copy and locked colors. SHELL-02, SHELL-03, and SHELL-06 TUI half all closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv parsing → daemon CreateSession | User-controlled --shell flag value is validated against an allowlist (bash/zsh/pwsh/powershell or empty for system default). No injection possible because the daemon-side resolveShellSpawn (Phase 100) ALSO validates against knownShellSpecs. |
| TUI in-process pty.DiscoverShells call → render | TUI runs in-process with the daemon (per RESEARCH A1). DiscoverShells reads filesystem (PATH lookup + optional /etc/shells). No remote input, no escalation surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-04-01 | Tampering | --shell=ARBITRARY_STRING bypasses validation | mitigate | cmdNewShell explicitly validates --shell value against allowlist before calling CreateSession. Tests TestCmdNewShell_UnknownShellFlag + TestCmdNewShell_EmptyShellFlag lock this. |
| T-101-04-02 | Information Disclosure | Daemon-unreachable error leaks daemon path/socket | accept | Existing CLI error pattern surfaces socket-dial errors verbatim. No new disclosure beyond existing cmdNew behavior. |
| T-101-04-03 | Elevation of Privilege | --extra-args after -- could be forwarded to shell, enabling argv injection | mitigate | extraArgs is explicitly DROPPED for shell sessions (per Phase 100 A6). cmdNewShell emits a stderr warning AND passes args=nil to CreateSession. Test TestCmdNewShell_ExtraArgsWarning locks this. |
| T-101-04-04 | Spoofing | TUI agentBadgeColor returns wrong color for spoofed cli string | accept | The unknown-cli fallback is FgMuted (existing default), not a specific agent color. No spoofing of badge color is possible without daemon compromise. |
</threat_model>

<verification>
After all three tasks complete:

  cd /Users/ken/dev/agenthub
  go test ./... -race -count=1
  go vet ./...
  gofmt -l cmd_cli.go cmd_cli_test.go main.go internal/tui/tui.go internal/tui/modal.go internal/tui/styles.go internal/tui/view.go internal/tui/styles_test.go internal/tui/update_test.go

  # Pattern preservation (filter out comments):
  grep -c "func cmdNewShell" cmd_cli.go                                           # >=1
  grep -c "cmdNewShell" main.go                                                   # >=1 (dispatch)
  grep -c 'new shell \[<path>\]' cmd_cli.go                                       # >=1 (usage)
  grep -c '"shell", "bash", "zsh", "pwsh", "powershell"' internal/tui/styles.go   # >=1 (agentBadgeColor case)
  grep -c "BadgeShell" internal/tui/styles.go                                     # >=3 (struct field + init + case)
  grep -c "pty.DiscoverShells" internal/tui/tui.go                                # >=1
  grep -c "Shell — " internal/tui/modal.go                                        # >=1 (locked label prefix)
  grep -c "#89ddff" internal/tui/styles.go                                        # >=1
  grep -c "#3d5a80" internal/tui/styles.go                                        # >=1
  grep -c 'unknown shell %q' cmd_cli.go                                           # >=1 (locked stderr)
  grep -c 'extra arguments are not forwarded' cmd_cli.go                          # >=1 (locked stderr)

All commands exit 0. No file outside files_modified is touched.
</verification>

<success_criteria>
- 9 files modified (3 CLI/dispatch, 4 TUI source, 2 TUI test); zero new files
- All 12 new CLI tests pass (11 cmdNewShell + 1 usage)
- All 7 new TUI tests pass (4 styles + 3 picker)
- Phase 100 and Plan 101-01 tests still pass (no regression)
- All locked CLI stderr strings appear verbatim per UI-SPEC §CLI
- usage() includes the literal "new shell [<path>]" and "--shell=bash|zsh|pwsh|powershell" lines
- TUI agent picker cycle order matches UI-SPEC §Interaction TUI flow (AI CLIs first, then shells in system-default → bash → zsh → pwsh → powershell order)
- TUI BadgeShell color resolves to #89ddff (dark) / #3d5a80 (light)
- TUI session-list shell row badge uses BadgeShell, NOT FgMuted
- SHELL-02 (CLI), SHELL-03 (TUI), SHELL-06 TUI half all closed
- This plan runs Wave 2 in parallel with Plans 02 and 03 with zero file overlap (verifiable: files_modified lists are disjoint)
</success_criteria>

<output>
After completion, create .planning/phases/101-shell-session-surfaces-web-share-gating/101-04-SUMMARY.md documenting:
- cmdNewShell signature and dispatch site in main.go
- usage() lines added (verbatim from UI-SPEC §CLI)
- All locked stderr strings (unknown shell / empty --shell / extra args warning / daemon unreachable)
- TUI changes:
  - detectedShells field + pty.DiscoverShells() call site
  - Shell sort order (sortShellsForPicker priority map: shell=0, bash=1, zsh=2, pwsh=3, powershell=4)
  - BadgeShell struct field + init color (#3d5a80/#89ddff via ld() adaptive helper)
  - agentBadgeColor switch addition
  - renderAgentPicker entry-slice unification (cliKey + displayLabel pattern)
- Shell-cli-set duplication note: same constant set appears in cmdNewShell allowlist (CLI), engine.go isShellSession (daemon, Phase 100), agentBadgeColor switch (TUI), App.tsx SHELL_CLIS (frontend, Plan 03), TabBar.tsx agentBadgeModifier (frontend, Plan 02). Accept v3.3 duplication; revisit in v3.4 if a pattern emerges (per Phase 100 RESEARCH Pitfall).
- Confirmation that SHELL-02, SHELL-03, and the TUI half of SHELL-06 are closed.
- SHELL-08 banner intentionally NOT applied to CLI/TUI (per UI-SPEC §Copywriting and §Web-share security banner copy — the warning is GUI-only).
</output>
</content>
