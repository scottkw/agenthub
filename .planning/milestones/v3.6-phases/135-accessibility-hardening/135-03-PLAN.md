---
phase: 135-accessibility-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Hub/HubModal.tsx
  - frontend/src/components/Hub/HubModal.test.tsx
autonomous: true
requirements: [A11Y-01, A11Y-02, A11Y-04]
tags: [accessibility, focus-trap, inert, modal, keyboard]

must_haves:
  truths:
    - "While the modal is open, the Hub background is marked inert so Tab cannot reach background cards"
    - "On modal open, initial keyboard focus moves to the close button"
    - "Closing the modal removes inert from the background and returns focus to the originating card"
    - "Escape closes the modal via a dialog-scoped handler that does not globally suppress other Escape handlers"
    - "STATUS_CONFIG in HubModal.tsx mirrors SessionCard.tsx — every status has a unique icon + text label (colorblind-safe, A11Y-01)"
  artifacts:
    - path: "frontend/src/components/Hub/HubModal.tsx"
      provides: "inert focus trap + initial focus + dialog-scoped Escape (WR-05)"
      contains: ".inert = true"
    - path: "frontend/src/components/Hub/HubModal.test.tsx"
      provides: "?raw source-inspection assertions for A11Y-04 + WR-05 + A11Y-01 mirror"
      contains: ".inert = true"
  key_links:
    - from: "frontend/src/components/Hub/HubModal.tsx"
      to: ".hub background element"
      via: "document.querySelector('.hub').inert set/unset in useEffect keyed on phase"
      pattern: "querySelector\\('\\.hub'\\)"
    - from: "frontend/src/components/Hub/HubModal.tsx"
      to: "closeBtnRef"
      via: "initial focus on phase open"
      pattern: "closeBtnRef\\.current\\?\\.focus"
---

<objective>
Close the two highest-value Hub modal accessibility gaps and verify A11Y-01 at source. (1) GAP-135-D / A11Y-04: implement a modal focus trap using the DOM `inert` attribute on the `.hub` background, with initial focus moved to the close button on open and `inert` removed on unmount. (2) GAP-135-C / WR-05: replace the over-broad document-level Escape handler (`stopImmediatePropagation`) with a dialog-scoped `onKeyDown` using `stopPropagation`. (3) A11Y-01: verify at source that `HubModal.tsx` `STATUS_CONFIG` mirrors `SessionCard.tsx` so every status stays colorblind-safe (unique icon shape + text label, color reinforcement only).

Purpose: A11Y-04 (focus trapped in modal, background cards unreachable) is the only entirely-unimplemented requirement; WR-05 was explicitly deferred to this phase; A11Y-01 needs a mirror-drift guard. The user is colorblind — A11Y-01 is verified at source against the STATUS_CONFIG hex/icon/label constants, never by eye.
Output: Modified `HubModal.tsx` plus `?raw` source-inspection assertions in `HubModal.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-accessibility-hardening/135-UI-SPEC.md
@.planning/phases/135-accessibility-hardening/135-RESEARCH.md
@.planning/phases/135-accessibility-hardening/135-PATTERNS.md

<interfaces>
<!-- Source-of-truth facts. Do NOT re-derive by exploring. VERIFIED against source 2026-06-18. -->

HubPanel root element class (VERIFIED grep): `<div className="hub">` at HubPanel.tsx line 445.
  So `document.querySelector('.hub')` is the correct background selector (RESEARCH Assumption A1 confirmed).

useRef/useEffect cleanup template (HubModal.tsx lines 111-117 — cardFocusRef, the focus-RETURN-on-unmount,
  ALREADY EXISTS, do not duplicate or move it):
    const cardFocusRef = useRef<HTMLElement | null>(null)
    useEffect(() => { cardFocusRef.current = document.activeElement as HTMLElement
      return () => { cardFocusRef.current?.focus() } }, [])

A11Y-04 addition — NEW useEffect keyed on [phase], placed AFTER cardFocusRef block:
    const closeBtnRef = useRef<HTMLButtonElement>(null)
    useEffect(() => {
      if (phase !== 'open') return          // Pitfall 3: do not trap during 'entering' animation
      const hubEl = document.querySelector('.hub') as HTMLElement | null
      if (hubEl) hubEl.inert = true
      closeBtnRef.current?.focus()
      return () => { if (hubEl) hubEl.inert = false }   // Pitfall 1: MUST remove inert or Hub keyboard-locks
    }, [phase])
  Do NOT call cardFocusRef.current?.focus() in THIS cleanup — the existing cardFocusRef cleanup handles
  focus return and must run after inert is removed (RESEARCH code example note).

WR-05 — REMOVE the entire document Escape block (HubModal.tsx lines 119-139): the handleCloseRef ref,
  its sync effect, AND the document.addEventListener('keydown', ...) effect. Replace by adding onKeyDown to
  the role="dialog" div (lines 167-176): on e.key === 'Escape' → e.stopPropagation() then handleClose().
  Keep onClick={(e) => e.stopPropagation()} and onAnimationEnd unchanged.

Close button (HubModal.tsx lines 196-203): add ref={closeBtnRef}. Keep type/className/aria-label/onClick.

A11Y-01 (verification only — no SessionCard/HubModal STATUS_CONFIG code change):
  STATUS_CONFIG in HubModal.tsx (~lines 25-35) must mirror SessionCard.tsx (~lines 29-51).
  6 statuses, each unique icon + text label: running/ArrowPathIcon/"Running",
  idle/CheckCircleIcon/"Idle", waiting/PauseCircleIcon/"Needs input",
  errored/ExclamationCircleIcon/"Error", stopped-ok/StopCircleIcon/"Done",
  stopped-err/ExclamationCircleIcon/"Exited {code}". stopped-err vs errored share the icon —
  text label is the WCAG-1.4.1 differentiator (compliant). Color hex in comments is "(reinf.)" only.

Test harness — ?raw source inspection (HubModal.test.tsx already uses `import raw from './HubModal.tsx?raw'`).
  jsdom 29 does NOT implement inert focus suppression (VERIFIED: element.inert returns undefined) — so ALL
  A11Y-04 + WR-05 tests are `?raw` string assertions, NEVER DOM focus-behavior assertions.
  EXISTING test at lines 28-30 asserts toContain('stopImmediatePropagation') — it WILL go RED after WR-05;
  it MUST be replaced (Pitfall 4), not left.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-05 Escape scope fix + A11Y-01 mirror verification (GAP-135-C, A11Y-01)</name>
  <files>frontend/src/components/Hub/HubModal.tsx, frontend/src/components/Hub/HubModal.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/HubModal.tsx lines 119-139 (the document Escape block to REMOVE), lines 167-176 (the role="dialog" div to add onKeyDown to), lines 25-35 (STATUS_CONFIG)
    - frontend/src/components/Hub/SessionCard.tsx lines 29-51 (STATUS_CONFIG source of truth for the A11Y-01 mirror)
    - frontend/src/components/Hub/HubModal.test.tsx lines 1-35 (?raw import + the stopImmediatePropagation test at 28-30 that must be replaced)
    - .planning/phases/135-accessibility-hardening/135-RESEARCH.md Pattern 3 (WR-05) + Pitfall 4 + A11Y-01 Verification Report
    - .planning/phases/135-accessibility-hardening/135-PATTERNS.md "HubModal.tsx — GAP-135-C" and "HubModal.test.tsx" sections
  </read_first>
  <behavior>
    - HubModal source does NOT contain `stopImmediatePropagation`
    - HubModal source does NOT register a `document.addEventListener('keydown', ...)` for Escape
    - Escape is handled by an `onKeyDown` on the `role="dialog"` element that calls `e.stopPropagation()` then `handleClose()`
    - HubModal STATUS_CONFIG defines the same 6 statuses with the same icons + labels as SessionCard (no mirror drift)
  </behavior>
  <action>
    Implement GAP-135-C (WR-05) and verify A11Y-01. (1) Remove the entire document-level Escape block in HubModal.tsx (lines ~119-139): the `handleCloseRef` declaration, its sync `useEffect`, and the `document.addEventListener('keydown', ...)` effect with `stopImmediatePropagation`. (2) Add `onKeyDown` to the `role="dialog"` div (~lines 167-176): on `e.key === 'Escape'` call `e.stopPropagation()` then `handleClose()` directly (handleClose can be referenced directly now — no ref needed). Keep `onClick={(e) => e.stopPropagation()}` and `onAnimationEnd` unchanged. (3) A11Y-01 verification — confirm by reading that HubModal.tsx `STATUS_CONFIG` (~lines 25-35) still mirrors SessionCard.tsx (~lines 29-51): 6 statuses, each unique icon + text label; make NO code change to STATUS_CONFIG. In HubModal.test.tsx: replace the existing `stopImmediatePropagation` test (lines 28-30) with three assertions — `expect(raw).toContain('stopPropagation')`, `expect(raw).not.toContain('stopImmediatePropagation')`, `expect(raw).not.toMatch(/document\.addEventListener\s*\(\s*['"]keydown/)` — and add a source-inspection A11Y-01 mirror assertion confirming HubModal's STATUS_CONFIG contains each status key + its label string.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/Hub/HubModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `raw` does NOT contain `stopImmediatePropagation` (WR-05 fix removed the global suppressor).
    - Source: `raw` does NOT match `/document\.addEventListener\s*\(\s*['"]keydown/` (no document-level Escape listener remains).
    - Source: `raw` contains `stopPropagation` on the dialog `onKeyDown` Escape path (scoped handler present).
    - Source (A11Y-01, colorblind-safe at source not by eye): `raw` contains each of `'running'`/"Running", `'waiting'`/"Needs input", `'errored'`/"Error", `'stopped-err'`/"Exited", `'idle'`/"Idle", `'stopped-ok'`/"Done" — proving the mirror holds and stopped-err is text-differentiated from errored.
    - HubModal.test.tsx passes (old stopImmediatePropagation test is replaced, not failing).
  </acceptance_criteria>
  <done>Escape is handled by a dialog-scoped onKeyDown using stopPropagation (no global suppression, no document listener); HubModal STATUS_CONFIG verified to mirror SessionCard with unique icon+label per status. HubModal.test.tsx green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A11Y-04 modal focus trap via inert + initial focus (GAP-135-D)</name>
  <files>frontend/src/components/Hub/HubModal.tsx, frontend/src/components/Hub/HubModal.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/HubModal.tsx lines 111-117 (cardFocusRef useEffect template — the new effect mirrors its structure), lines 196-203 (close button to add ref to)
    - .planning/phases/135-accessibility-hardening/135-RESEARCH.md Pattern 2 (inert), Pitfalls 1/3/5/6, "A11Y-04: Complete focus trap implementation" code example
    - .planning/phases/135-accessibility-hardening/135-PATTERNS.md "HubModal.tsx — GAP-135-D (A11Y-04)" section
    - .planning/phases/135-accessibility-hardening/135-UI-SPEC.md §A11Y-04 (inert is the preferred WCAG technique; required code changes list)
  </read_first>
  <behavior>
    - On phase === 'open', source sets `document.querySelector('.hub')` element's `.inert = true`
    - The inert effect is gated on `phase !== 'open'` early-return (no trap during the 'entering' animation — Pitfall 3)
    - On effect cleanup, source sets the same hub element's `.inert = false` (Pitfall 1 — prevents Hub keyboard-lock)
    - On open, initial focus moves to the close button via `closeBtnRef.current?.focus()`
    - The close button carries `ref={closeBtnRef}`
  </behavior>
  <action>
    Implement GAP-135-D (A11Y-04). Add `const closeBtnRef = useRef<HTMLButtonElement>(null)`. Add a new `useEffect` keyed on `[phase]`, placed AFTER the existing `cardFocusRef` effect: early-return if `phase !== 'open'` (Pitfall 3 — do not trap during the entering animation); capture `const hubEl = document.querySelector('.hub') as HTMLElement | null`; set `if (hubEl) hubEl.inert = true`; call `closeBtnRef.current?.focus()`; return a cleanup that calls `if (hubEl) hubEl.inert = false` (Pitfall 1). Do NOT call `cardFocusRef.current?.focus()` in this cleanup — the existing cardFocusRef effect owns focus return and must run after inert is removed. Add `ref={closeBtnRef}` to the close button (~lines 196-203), leaving its other props unchanged. Use the `inert` DOM property (no new package; native in WebView2 Chromium 126+ and macOS WKWebView Safari 15.5+ per RESEARCH). In HubModal.test.tsx add a new `describe('HubModal (A11Y-04: focus trap via inert)')` with `?raw` assertions only — jsdom does not implement inert, so NO DOM focus-behavior tests.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/Hub/HubModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `raw` contains `querySelector('.hub')` (correct background selector, verified A1) and `.inert = true`.
    - Source: `raw` contains `.inert = false` (cleanup removes inert — Pitfall 1 guard against Hub keyboard-lock).
    - Source: `raw` contains `phase !== 'open'` (the inert/focus effect is gated to the open phase — Pitfall 3).
    - Source: `raw` contains `closeBtnRef` and `closeBtnRef.current?.focus()` (initial focus to close button) and the close button JSX carries `ref={closeBtnRef}`.
    - Tests are `?raw` source assertions only (no `element.inert === true` DOM assertion — jsdom returns undefined).
    - HubModal.test.tsx full file passes.
  </acceptance_criteria>
  <done>While the modal is open the .hub background is inert (Tab cannot reach background cards), initial focus lands on the close button, and inert is removed on close so focus returns cleanly to the originating card via the existing cardFocusRef. HubModal.test.tsx green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan changes DOM focus management (inert attribute, scoped keydown) in an existing client-side React modal. No network surface, no IPC/relay route, no capability, no new untrusted input. The modal's data flow (TerminalPanel/RelayClient) is unchanged by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-03-01 | Denial of Service | inert never removed → Hub keyboard-lock | mitigate | Pair every `hubEl.inert = true` with a useEffect cleanup `hubEl.inert = false`; tested at source (assert `.inert = false` present). RESEARCH Pitfall 1. This is a usability self-DoS, not an attacker vector. |
| T-135-03-02 | Tampering | dialog-scoped Escape handler (WR-05) | accept | `stopPropagation` only halts DOM event bubbling within the app's own component tree; it does not cross any trust boundary. Replacing `stopImmediatePropagation` REDUCES side-effect scope (a correctness improvement). |
| T-135-03-03 | Spoofing | inert / focus on background | accept | inert is a DOM-only focus/AT suppression on already-rendered local UI; no privilege, no data, no remote surface involved. |
| T-135-03-SC | Tampering | npm/pip/cargo installs | N/A | No packages installed this plan (RESEARCH: zero new dependencies; inert is a native DOM property). Legitimacy gate not applicable. |

No high-severity threats. The only failure mode (T-135-03-01) is a client-side usability lock, mitigated by the mandatory inert-cleanup and asserted at source. DOM-only focus management; no new attack surface.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/Hub/HubModal.test.tsx` — green (A11Y-04 + WR-05 + A11Y-01 mirror `?raw` assertions)
- `cd frontend && npx vitest run` — full suite green (no regression in Phase 134 modal tests)
- jsdom constraint honored: A11Y-04 verified by `?raw` source inspection, never DOM `inert` behavior
- Colorblind constraint: A11Y-01 mirror verified at source against STATUS_CONFIG status keys + labels, not by eye
</verification>

<success_criteria>
- A11Y-04: while the modal is open the `.hub` background is `inert` (Tab cannot reach background cards), initial focus moves to the close button, and `inert` is removed on close.
- A11Y-02 (modal portion / WR-05): Escape is handled by a dialog-scoped `onKeyDown` using `stopPropagation`; no document-level Escape listener and no `stopImmediatePropagation` remain; focus returns to the originating card on close (existing cardFocusRef preserved).
- A11Y-01: HubModal STATUS_CONFIG verified at source to mirror SessionCard — every status uniquely identifiable by icon shape + text label (colorblind-safe).
</success_criteria>

<output>
Create `.planning/phases/135-accessibility-hardening/135-03-SUMMARY.md` when done
</output>
