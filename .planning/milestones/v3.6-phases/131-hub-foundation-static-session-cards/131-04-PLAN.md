---
phase: 131-hub-foundation-static-session-cards
plan: 04
type: execute
wave: 2
depends_on: [131-02, 131-03]
files_modified:
  - frontend/src/components/Hub/SessionCardGrid.tsx
  - frontend/src/components/Hub/SessionCardGrid.test.tsx
  - frontend/src/components/Hub/HubPanel.tsx
  - frontend/src/components/Hub/HubPanel.test.tsx
autonomous: true
requirements: [GRID-02, GRID-04, GRID-05, GRID-06, HUB-03]
must_haves:
  truths:
    - "Cards are grouped by working directory under a group header; sessions with no workDir fall into an 'Other' group"
    - "The HubPanel owns filter + search state, applies them to the session list, and renders the grid or the appropriate empty state"
    - "The / shortcut focuses the search field; an error state renders when polling fails"
  artifacts:
    - path: frontend/src/components/Hub/SessionCardGrid.tsx
      provides: "Working-directory grouping + responsive grid structure of SessionCards"
      contains: "groupByWorkDir"
    - path: frontend/src/components/Hub/HubPanel.tsx
      provides: "Top-level Hub surface composing filter bar + grid + empty/error states"
      min_lines: 60
  key_links:
    - from: "HubPanel.tsx"
      to: "HubFilterBar + SessionCardGrid + HubEmptyState"
      via: "composition with filter/search state applied"
      pattern: "SessionCardGrid"
    - from: "HubPanel.tsx / shortcut"
      to: "searchRef.current.focus()"
      via: "window keydown listener for '/'"
      pattern: "addEventListener\\('keydown'"
---

<objective>
Compose the Wave 1 leaves into the working surface: `SessionCardGrid` (groups sessions by working
directory with group headers and a responsive card row) and `HubPanel` (the top-level surface that
owns filter + search state, applies filtering, mounts the filter bar and grid, handles the `/`
shortcut, and renders the empty/error states). HubPanel receives the polled `sessions` array as a
prop from App.tsx (the poll itself is wired in Plan 05).

Purpose: This is where the Hub becomes a coherent screen.
Output: Two components + their vitest specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/131-hub-foundation-static-session-cards/131-UI-SPEC.md
@.planning/phases/131-hub-foundation-static-session-cards/131-PATTERNS.md
@frontend/src/components/DaemonManagerPanel.tsx
@frontend/src/components/RemoteSessionsPanel.tsx

<interfaces>
<!-- From Plan 02: SessionCard { session: SessionInfo; onRename?: (id, name) => void } (frontend/src/components/Hub/SessionCard.tsx) -->
<!-- From Plan 03: HubFilterBar + HubFilter type; HubEmptyState { variant; onNewSession?; onClearFilter? } -->
<!-- SessionInfo.workDir: string (from Plan 01) — empty string ⇒ "Other" group. -->
<!-- HubPanelProps: { sessions: SessionInfo[]; error: boolean; onNewSession: () => void; onRename: (id, name) => void } -->
<!-- Group header copy: basename(workDir), full path as title tooltip; "Other" for empty. -->
<!-- Error-state copy (UI-SPEC): heading "Couldn't load sessions", body "Check that the daemon is running and try again." -->
<!-- Test helpers: 131-PATTERNS.md "Shared Patterns" (Wails RPC mock + createRoot/act). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionCardGrid component + test</name>
  <files>frontend/src/components/Hub/SessionCardGrid.tsx, frontend/src/components/Hub/SessionCardGrid.test.tsx</files>
  <read_first>
    - 131-PATTERNS.md (SessionCardGrid section — groupByWorkDir, group header, grid pattern)
    - frontend/src/components/RemoteSessionsPanel.tsx (peer-group header + per-group list analog)
    - frontend/src/components/Hub/SessionCard.tsx (created in Plan 02)
  </read_first>
  <behavior>
    - Sessions are grouped by workDir; an empty workDir groups under a header labeled "Other".
    - Each group renders a heading (role/heading level 2) showing the basename of the workDir with the full path as a title tooltip.
    - Each session renders one SessionCard inside a role="list" / role="listitem" structure.
  </behavior>
  <action>
    Create frontend/src/components/Hub/SessionCardGrid.tsx exporting `SessionCardGrid` with props
    `{ sessions: SessionInfo[]; onRename: (id: string, name: string) => void }`. Implement
    `groupByWorkDir(sessions)` returning a Map keyed by `s.workDir || ''`. For each group render a
    `.hub__group` containing a `.hub__group-header` (`<h2>` or role="heading" aria-level=2) showing
    `basename(workDir)` (full path as the title attribute) or "Other" for the empty key, followed by a
    `.hub__card-row` with `role="list"` whose children are `role="listitem"` wrappers around
    `<SessionCard session={s} onRename={onRename} />`. Provide a small local `basename` helper
    (split on '/' and '\\', take the last non-empty segment) — do not import node:path in frontend code.

    Create frontend/src/components/Hub/SessionCardGrid.test.tsx mocking the Wails App module and
    asserting: two distinct workDirs produce two groups; an empty workDir produces an "Other" group;
    group header shows the basename; each session yields one listitem.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm vitest run src/components/Hub/SessionCardGrid.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SessionCardGrid.test.tsx proves grouping by workDir, the "Other" fallback, basename header, and per-session listitems.
    - `grep -c 'groupByWorkDir' frontend/src/components/Hub/SessionCardGrid.tsx` >= 1.
    - No `from 'path'` / `node:path` import in the component.
  </acceptance_criteria>
  <done>SessionCardGrid groups cards by working directory with accessible group headers and a list-semantic grid, tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HubPanel surface + test</name>
  <files>frontend/src/components/Hub/HubPanel.tsx, frontend/src/components/Hub/HubPanel.test.tsx</files>
  <read_first>
    - 131-PATTERNS.md (HubPanel section — props, filter/search state, / shortcut, error state)
    - 131-UI-SPEC.md (Layout Contract, Interaction Contract, error-state copy)
    - frontend/src/components/DaemonManagerPanel.tsx (top-level panel + error rendering analog)
    - frontend/src/components/Hub/HubFilterBar.tsx, HubEmptyState.tsx (Plan 03), SessionCardGrid.tsx (Task 1)
  </read_first>
  <behavior>
    - With sessions=[] and no active filter/search, renders the 'no-sessions' empty state.
    - With sessions present, applies the active filter and search text (name/CLI/host substring) and renders SessionCardGrid with the filtered set.
    - When a filter/search yields zero matches but sessions exist, renders the 'no-matches' empty state whose Clear-filter CTA resets filter and search.
    - Pressing "/" while not focused in an input focuses the search field.
    - When error is true, renders the "Couldn't load sessions" error state instead of the grid.
  </behavior>
  <action>
    Create frontend/src/components/Hub/HubPanel.tsx exporting `HubPanel` with props
    `{ sessions: SessionInfo[]; error: boolean; onNewSession: () => void; onRename: (id, name) => void }`.
    Own `activeFilter` (HubFilter, default 'all'), `searchText`, and a `searchRef`. Add a window keydown
    effect: when key is '/' and the active element is not an INPUT, preventDefault and focus+select the
    searchRef (GRID-05). Implement a `filterSessions(sessions, filter, search)` helper that applies the
    status filter (using the same status derivation as SessionCard/HubFilterBar) and a case-insensitive
    substring match against name, cli, and hostname.

    Render the layout per the UI-SPEC Layout Contract: a `.hub` root with `.hub__header` (title "Hub"
    + New session button), `<HubFilterBar>` (passing sessions for live counts, the filter/search state,
    the searchRef, and the callbacks), and a `.hub__grid-scroll` containing either: the error state
    (when `error`), the 'no-sessions' empty state (sessions empty), the 'no-matches' empty state
    (filtered empty but sessions non-empty), or `<SessionCardGrid sessions={filtered} onRename={onRename} />`.
    Use the exact error-state copy from the UI-SPEC.

    Create frontend/src/components/Hub/HubPanel.test.tsx mocking the Wails App module and covering every
    behavior above (empty → no-sessions; filtered-empty → no-matches + clear resets; filter/search narrows
    the grid; "/" focuses search; error → error state).
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm vitest run src/components/Hub/HubPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - HubPanel.test.tsx proves: no-sessions empty state, no-matches empty state with working Clear-filter, filter+search narrowing, "/" focuses search, error state.
    - `grep -c "addEventListener('keydown'" frontend/src/components/Hub/HubPanel.tsx` >= 1.
    - `grep -c "Couldn't load sessions" frontend/src/components/Hub/HubPanel.tsx` >= 1.
  </acceptance_criteria>
  <done>HubPanel composes the filter bar, grid, and empty/error states with working filter/search/shortcut behavior, tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessions prop → grouped DOM | Session names, hostnames, and workDir paths rendered into group headers and cards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-08 | Tampering (XSS) | workDir path rendered in group header (text + title attr) | mitigate | Rendered via JSX text child and the title attribute (React-escaped); no innerHTML. A crafted path cannot execute. |
| T-131-09 | Tampering | npm installs | accept | No new packages (RESEARCH audit: none). |
</threat_model>

<verification>
- `cd frontend && pnpm vitest run src/components/Hub/SessionCardGrid.test.tsx src/components/Hub/HubPanel.test.tsx` green.
- Full Hub component suite green: `pnpm vitest run src/components/Hub`.
</verification>

<success_criteria>
The Hub surface renders sessions grouped by working directory, filters and searches them, focuses
search on "/", and shows the correct empty/error states — proven by component tests.
</success_criteria>

<output>
Create `.planning/phases/131-hub-foundation-static-session-cards/131-04-SUMMARY.md` when done
</output>
