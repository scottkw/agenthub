---
phase: 131-hub-foundation-static-session-cards
plan: 03
type: execute
wave: 1
depends_on: [131-01]
files_modified:
  - frontend/src/components/Hub/HubFilterBar.tsx
  - frontend/src/components/Hub/HubFilterBar.test.tsx
  - frontend/src/components/Hub/HubEmptyState.tsx
  - frontend/src/components/Hub/HubEmptyState.test.tsx
autonomous: true
requirements: [GRID-04, GRID-05, GRID-06, HUB-03]
must_haves:
  truths:
    - "A status filter bar (All / Working / Needs input / Complete / Error / Idle) shows live counts and fires filter changes"
    - "A search field filters by name/CLI/host and exposes a ref the parent can focus via the / shortcut; Escape clears it"
    - "A New session action fires a callback that opens the existing create flow"
    - "With no sessions an empty state prompts the user to create one; with a non-matching filter it offers to clear the filter"
  artifacts:
    - path: frontend/src/components/Hub/HubFilterBar.tsx
      provides: "Filter pills with live counts + search input + New session button"
      contains: "onFilterChange"
    - path: frontend/src/components/Hub/HubEmptyState.tsx
      provides: "Two-variant empty state (no-sessions / no-matches)"
      contains: "no-sessions"
  key_links:
    - from: "HubFilterBar.tsx search input"
      to: "parent-supplied searchRef"
      via: "ref forwarded so the / shortcut can focus it"
      pattern: "searchRef"
    - from: "HubFilterBar.tsx New session button"
      to: "onNewSession callback"
      via: "click handler"
      pattern: "onNewSession"
---

<objective>
Build the two Hub control components that sit above the grid: `HubFilterBar` (status filter pills
with live counts, search input wired to a parent-owned ref, and the "New session" button) and
`HubEmptyState` (two variants — no sessions, and no filter matches). Both are independent leaves
that the HubPanel (Plan 04) composes.

Purpose: Filtering, search, and the create on-ramp are the Hub's primary interactions besides the
cards themselves.
Output: Two components + their vitest specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/131-hub-foundation-static-session-cards/131-UI-SPEC.md
@.planning/phases/131-hub-foundation-static-session-cards/131-PATTERNS.md
@frontend/src/components/WelcomeTab.tsx

<interfaces>
<!-- SessionInfo fields used here: name, cli, hostname, state, status, exitCode (for count derivation). -->
<!-- HubFilter union: 'all' | 'running' | 'waiting' | 'stopped-ok' | 'stopped-err' | 'idle' -->
<!-- Filter pill copy (UI-SPEC Copywriting Contract): All / Working / Needs input / Complete / Error / Idle -->
<!--   mapping: all→'all', Working→'running', Needs input→'waiting', Complete→'stopped-ok', Error→'stopped-err', Idle→'idle' -->
<!-- Count format: "Working (3)" — count in parens, omitted on the "All" pill. -->
<!-- Search aria-label: "Search sessions by name, CLI, or host"; placeholder "Search sessions…" -->
<!-- Empty-state copy is in the UI-SPEC Copywriting Contract (no-sessions vs filter variants). -->
<!-- Test helpers: 131-PATTERNS.md "Shared Patterns" (createRoot/act render helper; no Wails RPC needed here). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HubFilterBar component + test</name>
  <files>frontend/src/components/Hub/HubFilterBar.tsx, frontend/src/components/Hub/HubFilterBar.test.tsx</files>
  <read_first>
    - 131-UI-SPEC.md (Copywriting Contract for filter pills + search; Interaction Contract Filter Bar)
    - 131-PATTERNS.md (HubFilterBar section — props, filter pill pattern, search input pattern)
    - frontend/src/components/__tests__/Sidebar.test.tsx (createRoot/act render helper)
  </read_first>
  <behavior>
    - Each filter pill (except "All") shows its live count derived from the sessions prop; the active pill has the --active modifier.
    - Clicking a pill fires onFilterChange with the mapped HubFilter key.
    - Typing in the search input fires onSearchChange with the typed value.
    - Pressing Escape in the search input fires onSearchChange('') and blurs.
    - Clicking "New session" fires onNewSession.
    - The search input receives the parent-supplied searchRef (so the / shortcut, owned by the parent, can focus it).
  </behavior>
  <action>
    Create frontend/src/components/Hub/HubFilterBar.tsx exporting the `HubFilter` type union
    ('all'|'running'|'waiting'|'stopped-ok'|'stopped-err'|'idle') and `HubFilterBar` with props
    `{ sessions: SessionInfo[]; activeFilter: HubFilter; searchText: string;
       searchRef: React.RefObject<HTMLInputElement>; onFilterChange; onSearchChange; onNewSession }`.

    Define a FILTER_PILLS array pairing each HubFilter key with its UI-SPEC label (All / Working /
    Needs input / Complete / Error / Idle). Compute live counts by reducing `sessions` through the same
    status derivation used by SessionCard: stopped + exit!=0 → 'stopped-err', stopped + exit 0 →
    'stopped-ok', else the session's status. Render pills with `.hub-filter__pill` and the
    `--active` modifier on the active one; append " (count)" to every pill except "All". Render the
    search input with `ref={searchRef}`, className `hub-filter__search`, placeholder "Search sessions…",
    aria-label "Search sessions by name, CLI, or host", value/onChange wired to props, and an onKeyDown
    that clears+blurs on Escape. Render a "New session" button firing onNewSession.

    Create frontend/src/components/Hub/HubFilterBar.test.tsx covering every behavior above, including
    that counts reflect a mixed sessions array and that Escape clears the search.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm vitest run src/components/Hub/HubFilterBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - HubFilterBar.test.tsx proves live counts, pill click → onFilterChange, typing → onSearchChange, Escape → clear, New session → onNewSession.
    - `grep -c "Search sessions by name, CLI, or host" frontend/src/components/Hub/HubFilterBar.tsx` >= 1.
    - The component exports the HubFilter type.
  </acceptance_criteria>
  <done>HubFilterBar renders pills with live counts, a ref-forwarded search input, and a New session button — all behaviors tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HubEmptyState component + test</name>
  <files>frontend/src/components/Hub/HubEmptyState.tsx, frontend/src/components/Hub/HubEmptyState.test.tsx</files>
  <read_first>
    - 131-UI-SPEC.md (Copywriting Contract empty-state rows)
    - 131-PATTERNS.md (HubEmptyState section — two-variant pattern)
    - frontend/src/components/WelcomeTab.tsx (empty/onboarding CTA pattern)
  </read_first>
  <behavior>
    - variant 'no-sessions' renders heading "No sessions yet", body "Create a session to start an AI coding agent.", and a "New session" button that fires onNewSession.
    - variant 'no-matches' renders heading "No matching sessions", body "Clear the filter or search to see all sessions.", and a "Clear filter" button that fires onClearFilter.
  </behavior>
  <action>
    Create frontend/src/components/Hub/HubEmptyState.tsx exporting `HubEmptyState` with props
    `{ variant: 'no-sessions' | 'no-matches'; onNewSession?: () => void; onClearFilter?: () => void }`.
    Render the exact copy from the UI-SPEC Copywriting Contract for each variant, using `.hub__empty-state`,
    `.hub__empty-heading`, `.hub__empty-body`, `.hub__empty-cta` class names. The no-sessions CTA fires
    onNewSession; the no-matches CTA fires onClearFilter.

    Create frontend/src/components/Hub/HubEmptyState.test.tsx asserting both variants render the correct
    copy and that each CTA fires its callback.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm vitest run src/components/Hub/HubEmptyState.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - HubEmptyState.test.tsx proves both variants' copy and both CTA callbacks.
    - `grep -c "No sessions yet" frontend/src/components/Hub/HubEmptyState.tsx` >= 1.
    - `grep -c "No matching sessions" frontend/src/components/Hub/HubEmptyState.tsx` >= 1.
  </acceptance_criteria>
  <done>HubEmptyState renders both variants with exact UI-SPEC copy and working CTAs, tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search input → filter logic | User-typed text drives client-side filtering only (no eval, no query) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-06 | Tampering (XSS) | session metadata rendered in filter counts / pills | mitigate | All rendering via JSX text; counts are numbers; no innerHTML. React default escaping applies. |
| T-131-07 | Tampering | npm installs | accept | No new packages (RESEARCH audit: none). |
</threat_model>

<verification>
- `cd frontend && pnpm vitest run src/components/Hub/HubFilterBar.test.tsx src/components/Hub/HubEmptyState.test.tsx` green.
</verification>

<success_criteria>
The filter bar filters with live counts and search, the New session action is wired, and both
empty-state variants render with exact copy — all proven by tests.
</success_criteria>

<output>
Create `.planning/phases/131-hub-foundation-static-session-cards/131-03-SUMMARY.md` when done
</output>
