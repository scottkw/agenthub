---
phase: 134-modal-interaction
plan: 04
type: execute
wave: 3
depends_on: ["134-01", "134-03"]
files_modified:
  - frontend/src/components/Hub/HubModal.tsx
autonomous: true
requirements: [MODAL-01, MODAL-02, MODAL-03]
must_haves:
  truths:
    - "HubModal renders an overlay + dialog panel with role=dialog, aria-modal, and a grow/shrink animation phase machine"
    - "Escape, close button, and click-outside all trigger the shrink/close path; focus returns to the originating card on unmount"
    - "HubModal routes to HubInteractiveModal (non-attention) or HubBriefingModal (attention) via isAttentionStatus(deriveHubStatus(session))"
    - "transform-origin is set from the source card's bounding rect so the grow animation originates from the card position"
  artifacts:
    - path: "frontend/src/components/Hub/HubModal.tsx"
      provides: "Modal shell: overlay, dialog, animation phases, Escape/click-outside, focus return, type routing, header strip"
      contains: "role=\"dialog\""
  key_links:
    - from: "HubModal.tsx"
      to: "HubInteractiveModal / HubBriefingModal"
      via: "isAttentionStatus routing of the body"
      pattern: "isAttentionStatus"
    - from: "HubModal.tsx panel"
      to: "sourceRect"
      via: "transformOrigin inline style from card center"
      pattern: "transformOrigin"
---

<objective>
Build the HubModal shell that composes the two leaf components from 134-03. HubModal owns the overlay, the grow/shrink animation phase machine (entering → open → exiting), Escape/click-outside dismissal, focus return to the originating card, the header strip, and the interactive-vs-briefing routing by attention status.

Purpose: MODAL-01 (grow animation from card position), MODAL-02 (shrink-back + focus return on close), MODAL-03 (routes non-blocked sessions to the interactive terminal). The leaf components (HubInteractiveModal, HubBriefingModal) already exist from 134-03 — this plan composes them; no forward import.
Output: HubModal.tsx. Consumed by HubPanel in 134-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-modal-interaction/134-PATTERNS.md
@.planning/phases/134-modal-interaction/134-UI-SPEC.md
@frontend/src/components/RemoteJoinCodeModal.tsx
@frontend/src/lib/hubStatus.ts
@.planning/phases/134-modal-interaction/134-03-SUMMARY.md

<interfaces>
<!-- Contracts the executor uses directly — no codebase exploration needed. -->

Leaf component props (created in 134-03):
```typescript
// HubInteractiveModal
interface HubInteractiveModalProps {
  session: SessionInfo; isOpen: boolean; relayPort: number;
  fontSize: number; theme: ITheme; pluginConfig?: PluginSettings | null;
  onFontSizeChange?: (delta: number) => void
}
// HubBriefingModal
interface HubBriefingModalProps { session: SessionInfo; relayPort: number; onClose: () => void }
```

hubStatus contract (lib/hubStatus.ts):
```typescript
function deriveHubStatus(s: SessionInfo): HubStatus
function isAttentionStatus(status: HubStatus): boolean   // true for waiting | errored | stopped-err
```

HubModal props (this plan defines them — consumed by HubPanel in 134-05):
```typescript
interface HubModalProps {
  session: SessionInfo
  sourceRect: DOMRect
  relayPort: number
  fontSize: number
  theme: ITheme
  pluginConfig?: PluginSettings | null
  onClose: () => void
}
```

Imports:
  import type { SessionInfo } from '../../wailsjs/go/main/App'
  import type { ITheme } from '@xterm/xterm'
  import { daemon } from '../../wailsjs/go/models'  // type PluginSettings = daemon.PluginSettings
  import { deriveHubStatus, isAttentionStatus } from '../../lib/hubStatus'
  import { XMarkIcon, ComputerDesktopIcon, GlobeAltIcon, BellAlertIcon } from '@heroicons/react/24/outline'
  // status icon: reuse the STATUS_CONFIG import block pattern from SessionCard.tsx
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HubModal shell — overlay, animation phases, Escape/click-outside, focus return, header strip, type routing</name>
  <files>frontend/src/components/Hub/HubModal.tsx</files>
  <read_first>
    - frontend/src/components/RemoteJoinCodeModal.tsx (analog: overlay + stopPropagation, Escape document listener, focus pattern, role=dialog/aria-modal — lines 57-101)
    - frontend/src/lib/hubStatus.ts (deriveHubStatus, isAttentionStatus)
    - frontend/src/components/Hub/SessionCard.tsx (STATUS_CONFIG import + status-icon mapping lines ~3-15; the .hub-card__badge class)
    - frontend/src/components/Hub/HubInteractiveModal.tsx (leaf created in 134-03 — its prop shape)
    - frontend/src/components/Hub/HubBriefingModal.tsx (leaf created in 134-03 — its prop shape)
    - .planning/phases/134-modal-interaction/134-PATTERNS.md ("HubModal.tsx" section — imports, overlay+stopPropagation, Escape handler, focus-return, phase state, transform-origin, header strip markup)
    - .planning/phases/134-modal-interaction/134-UI-SPEC.md (Modal Header Strip, Grow/Shrink Animation, Focus Management, Copywriting Contract — aria-label strings, attention badge copy)
    - frontend/src/components/Hub/HubModal.test.tsx (the RED test from 134-01 — make it green)
  </read_first>
  <behavior>
    - Source: contains `role="dialog"`, `aria-modal="true"`, and `aria-label` = `Session terminal: {name}` (interactive) / `Briefing: {name} needs input` (briefing) via template literals.
    - Source: `phase` state typed `'entering' | 'open' | 'exiting'`; panel className includes `hub-modal--${phase}`; overlay className includes `hub-modal-overlay--${phase}`.
    - Source: panel `onAnimationEnd` advances `entering→open` and calls `onClose()` when `exiting`.
    - Source: inline `transformOrigin` from `sourceRect.left + sourceRect.width/2` and `sourceRect.top + sourceRect.height/2`.
    - Source: a `document` keydown listener; on `Escape` calls `e.stopImmediatePropagation()` then the close path.
    - Source: `cardFocusRef` capturing `document.activeElement` on mount and calling `.focus()` in the cleanup (focus return).
    - Source: overlay onClick = close; panel onClick = `e.stopPropagation()`.
    - Source: routes via `isAttentionStatus(deriveHubStatus(session))` → `<HubBriefingModal>` (true) / `<HubInteractiveModal>` (false), importing both.
    - Source: header contains the attention badge (BellAlertIcon + text "Needs attention") ONLY when isBriefing; a close button with aria-label "Close modal".
  </behavior>
  <action>
    Create `HubModal.tsx` with the HubModalProps from the interfaces block. Implement:

    Compute `const hubStatus = deriveHubStatus(session); const isBriefing = isAttentionStatus(hubStatus)`.

    Animation phase machine: `const [phase, setPhase] = useState<'entering' | 'open' | 'exiting'>('entering')`. `handleClose()` sets phase `'exiting'`. Panel className: `hub-modal hub-modal--${isBriefing ? 'briefing' : 'interactive'} hub-modal--${phase}`. Overlay className: `hub-modal-overlay hub-modal-overlay--${phase}`. Panel `onAnimationEnd`: `if (phase === 'entering') setPhase('open'); if (phase === 'exiting') onClose()`.

    Transform origin: inline style `{ transformOrigin: \`${sourceRect.left + sourceRect.width/2}px ${sourceRect.top + sourceRect.height/2}px\` }` on the panel — the ONLY permitted inline style.

    Escape: `useEffect` adding a `document` keydown listener; on `Escape`: `e.stopImmediatePropagation()` then `handleClose()`. Make the handler stable (ref to handleClose, or empty dep array capturing a stable handleClose).

    Focus return (RESEARCH Pattern 6): `const cardFocusRef = useRef<HTMLElement | null>(null)`. In a mount `useEffect`, `cardFocusRef.current = document.activeElement as HTMLElement` (the card still has focus at mount), then move focus into the modal (for briefing, the textarea self-focuses; for interactive, leave terminal focus to TerminalPanel). In the SAME effect's cleanup, call `cardFocusRef.current?.focus()`.

    Overlay/click-outside: outer `div.hub-modal-overlay hub-modal-overlay--${phase}` `onClick={handleClose}`; inner panel `div` `onClick={(e) => e.stopPropagation()}`.

    Header strip `div.hub-modal__header`: status icon from SessionCard's STATUS_CONFIG (aria-hidden), `span.hub-modal__session-name` = session.name, CLI badge reusing `hub-card__badge`, origin marker (ComputerDesktopIcon when local i.e. `!session.hostname`, GlobeAltIcon otherwise + `span.hub-modal__origin-text`), attention badge `span.hub-modal__attn-badge` with BellAlertIcon + "Needs attention" ONLY when isBriefing, a flex spacer (`<span style={{ flex: 1 }} />`), and a `button.hub-modal__close` (XMarkIcon, aria-label "Close modal") calling handleClose.

    Body: `<div className="hub-modal__body">` containing the routed component:
    `{isBriefing ? <HubBriefingModal session={session} relayPort={relayPort} onClose={handleClose} /> : <HubInteractiveModal session={session} isOpen={phase === 'open'} relayPort={relayPort} fontSize={fontSize} theme={theme} pluginConfig={pluginConfig} />}`.

    All classes use the `hub-modal` prefix. No hardcoded hex. No fenced code in markup. The `transformOrigin` is the only inline style except the trivial flex spacer.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run HubModal --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - HubModal.test.tsx (from 134-01) GREEN: asserts `role="dialog"`, `aria-modal="true"`, `transformOrigin`, `stopImmediatePropagation`, `cardFocusRef`, `Escape`, and presence of both `HubBriefingModal` and `HubInteractiveModal`.
    - `grep -c "isAttentionStatus" frontend/src/components/Hub/HubModal.tsx` (filtered `grep -v '^\s*//'`) >= 1.
    - Source contains literal fragments `Session terminal:`, `Briefing:`, `needs input`, `Needs attention`, `Close modal`.
    - `isActive={phase === 'open'}` (or `isOpen={phase === 'open'}`) — the interactive child receives open-gated activation: `grep -c "phase === 'open'" frontend/src/components/Hub/HubModal.tsx` >= 1.
    - No hardcoded hex: `grep -nE '#[0-9a-fA-F]{3,6}' frontend/src/components/Hub/HubModal.tsx` returns nothing.
    - `cd frontend && pnpm exec tsc --noEmit` — no errors (leaf imports resolve; 134-03 is a dependency).
  </acceptance_criteria>
  <done>HubModal renders the overlay/dialog with animation phases, Escape/click-outside close, focus return, header strip, and isAttentionStatus routing into the leaf components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document keydown listener | Modal adds a global Escape listener; must not double-fire with the Hub card menu's own listener |
| session.name / session.hostname → header strip | Session-derived strings rendered into the modal chrome |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-04-01 | Tampering | session.name / origin text in header | mitigate | Rendered as React text content (`{session.name}`); no `dangerouslySetInnerHTML`. |
| T-134-04-02 | Tampering | global Escape keydown listener | mitigate | Escape handler calls `e.stopImmediatePropagation()` so the Hub card menu's Escape listener does not also fire (134-RESEARCH.md Pitfall 6); listener removed in useEffect cleanup. |
| T-134-04-03 | DoS / focus theft | focus return on unmount | accept | Focus management is a UX/a11y concern (Phase 135 hardens the full trap). Returning focus to `document.activeElement` captured at mount is the safe foundation; no security exposure in a Wails desktop context. |
| T-134-SC | Tampering | npm/pip installs | mitigate | No new packages (134-RESEARCH.md audit: none). No install task. |
</threat_model>

<verification>
Run `cd frontend && pnpm test --run HubModal --reporter=verbose` — green. Run `cd frontend && pnpm exec tsc --noEmit` — no errors (HubInteractiveModal/HubBriefingModal exist from 134-03). Full suite green; the HubPanel cap-gate test (134-05) and the CSS test (134-01) remain red until 134-05 lands.
</verification>

<success_criteria>
- HubModal: overlay + dialog + animation phase machine + Escape/click-outside + focus return + isAttentionStatus routing.
- transform-origin originates the grow from the card center.
- Interactive child activated only at the 'open' phase (fit-safe).
- No hardcoded hex; 134-01 HubModal test green.
</success_criteria>

<output>
Create `.planning/phases/134-modal-interaction/134-04-SUMMARY.md` when done.
</output>
