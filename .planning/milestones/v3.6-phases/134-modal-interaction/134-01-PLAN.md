---
phase: 134-modal-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Hub/HubModal.test.tsx
  - frontend/src/components/Hub/HubInteractiveModal.test.tsx
  - frontend/src/components/Hub/HubBriefingModal.test.tsx
  - frontend/src/components/__tests__/style.hub.modal.test.ts
autonomous: true
requirements: [MODAL-01, MODAL-02, MODAL-03, MODAL-04, MODAL-05, MODAL-06]
must_haves:
  truths:
    - "Source-inspection test files exist for all three new modal components and assert the Nyquist behaviors before implementation"
    - "CSS contract test asserts overlay z-index, grow/shrink keyframes, and reduced-motion guard"
  artifacts:
    - path: "frontend/src/components/Hub/HubModal.test.tsx"
      provides: "Failing source-inspection tests for HubModal (MODAL-01/02/03 routing)"
      contains: "describe"
    - path: "frontend/src/components/Hub/HubInteractiveModal.test.tsx"
      provides: "Failing source-inspection tests for HubInteractiveModal (MODAL-03/05)"
      contains: "describe"
    - path: "frontend/src/components/Hub/HubBriefingModal.test.tsx"
      provides: "Failing source-inspection tests for HubBriefingModal (MODAL-04)"
      contains: "describe"
    - path: "frontend/src/components/__tests__/style.hub.modal.test.ts"
      provides: "CSS contract assertions for modal overlay, keyframes, reduced-motion"
      contains: "hub-modal-overlay"
  key_links:
    - from: "HubModal.test.tsx"
      to: "../HubModal.tsx?raw"
      via: "raw import source-inspection"
      pattern: "HubModal.tsx\\?raw"
---

<objective>
Create the Wave-0 test scaffolding for Phase 134 modal interaction. Every new modal component and the modal CSS gets a source-inspection test FILE FIRST, so subsequent implementation plans have a red→green target (Nyquist compliance).

Purpose: VALIDATION.md marks all modal test files as "❌ Wave 0" — they do not exist yet. Without them, downstream tasks have `<automated>MISSING</automated>` verify steps. This plan closes that gap.
Output: 4 test files containing source-inspection assertions that reference component/CSS source files that do NOT yet exist (so they fail until implemented).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-modal-interaction/134-PATTERNS.md
@.planning/phases/134-modal-interaction/134-VALIDATION.md

<interfaces>
<!-- Established Hub test patterns the executor MUST reuse (no exploration needed). -->

Source-inspection via ?raw import (from existing Hub component tests):
```typescript
import raw from '../HubModal.tsx?raw'
import { describe, it, expect } from 'vitest'
describe('HubModal', () => {
  it('uses role="dialog"', () => { expect(raw).toContain('role="dialog"') })
})
```

CSS assertion via readFileSync (from frontend/src/components/__tests__/style.hub.test.ts):
```typescript
import { readFileSync } from 'fs'
import { resolve } from 'path'
const cssRaw = readFileSync(resolve(__dirname, '../../style.css'), 'utf-8')
```

makeSession factory (from HubPanel.test.tsx) and Wails RPC mock pattern are documented in 134-PATTERNS.md "Shared Patterns". These tests are pure source-inspection — they do NOT mount components and do NOT need the factory or RPC mocks unless asserting on rendered output (they should NOT, because xterm needs canvas APIs absent in jsdom).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Component source-inspection test stubs (HubModal, HubInteractiveModal, HubBriefingModal)</name>
  <files>frontend/src/components/Hub/HubModal.test.tsx, frontend/src/components/Hub/HubInteractiveModal.test.tsx, frontend/src/components/Hub/HubBriefingModal.test.tsx</files>
  <read_first>
    - frontend/src/components/Hub/HubPanel.test.tsx (existing Hub test conventions: vitest imports, describe/it shape)
    - frontend/src/components/Hub/SessionCard.test.tsx (existing source-inspection example)
    - .planning/phases/134-modal-interaction/134-PATTERNS.md (Shared Patterns: "Source-inspection test approach", "makeSession test factory")
    - .planning/phases/134-modal-interaction/134-VALIDATION.md (Per-Task Verification Map — exact behaviors to assert)
  </read_first>
  <action>
    Create three `*.test.tsx` files in `frontend/src/components/Hub/`. Each imports its target component source via `?raw` (e.g. `import raw from '../HubModal.tsx?raw'`) — the targets do NOT exist yet, so the `?raw` import will error at test-run time, which is the intended RED state.

    HubModal.test.tsx assertions (MODAL-01/02/03): `raw` contains `role="dialog"`, `aria-modal="true"`, `transformOrigin` (grow-origin inline style), `stopImmediatePropagation` (Escape Pitfall 6 guard), `cardFocusRef` (focus-return ref name), `Escape`, and that it routes by `isAttentionStatus` (contains both `HubBriefingModal` and `HubInteractiveModal` as rendered branches).

    HubInteractiveModal.test.tsx assertions (MODAL-03/05): `raw` contains `TerminalPanel`, `sessionId=`, `isActive=`, `relayPort=`, `theme=`, `pluginConfig=`. Assert isActive is bound to open phase (contains the string `'open'` near `isActive`) — express as `expect(raw).toContain("isActive")` plus `expect(raw).toMatch(/isActive=\{[^}]*open/)`.

    HubBriefingModal.test.tsx assertions (MODAL-04): `raw` contains `GetSessionTailLines`, `RelayClient`, `sendInput`, `onOpen` (race-safe send per Pitfall 5), `maxLength={4096}` (V5 input-validation guard), `responseText.trim()` (Send-disabled-when-empty derivation), and the copy `Send Response`.

    Use `describe`/`it` blocks named by requirement ID. Do NOT mount any component (no `render()`), do NOT import xterm — pure string assertions only.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run HubModal HubInteractiveModal HubBriefingModal --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Test command resolves the three new files and reports them as FAILING (red) because the `?raw` target source files do not yet exist — failure mode is "Failed to resolve import ../HubModal.tsx?raw" or equivalent, NOT "0 tests found".
    - Each file contains `describe(` and at least three `it(` blocks named with their MODAL-0x requirement IDs.
    - HubBriefingModal.test.tsx contains the substring assertions `maxLength={4096}` and `Send Response`.
    - No test uses `render(` from @testing-library or imports `@xterm/xterm`.
  </acceptance_criteria>
  <done>Three test files exist, run, and fail because their `?raw` import targets are not yet implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Modal CSS contract test (style.hub.modal.test.ts)</name>
  <files>frontend/src/components/__tests__/style.hub.modal.test.ts</files>
  <read_first>
    - frontend/src/components/__tests__/style.hub.test.ts (block-finder helper pattern, readFileSync usage, reduced-motion contract test shape)
    - .planning/phases/134-modal-interaction/134-PATTERNS.md ("CSS Test Pattern (for new style.hub.modal.test.ts)" — copy the block-finder assertions verbatim as the starting point)
    - .planning/phases/134-modal-interaction/134-UI-SPEC.md (Animation Keyframes section — keyframe names + reduced-motion contract)
  </read_first>
  <action>
    Create `frontend/src/components/__tests__/style.hub.modal.test.ts` reading `style.css` via `readFileSync(resolve(__dirname, '../../style.css'), 'utf-8')`.

    Assert (all against the raw CSS string):
    - `.hub-modal-overlay` block contains `position: fixed`, `inset: 0`, and `z-index: 200` (use the indexOf block-finder helper from 134-PATTERNS.md so a self-invalidating comment cannot pass it).
    - `.hub-modal` block contains `var(--hub-surface-elevated)` and `var(--hub-border)` and contains NO hardcoded hex inside the panel block (assert the panel block does NOT match `/#[0-9a-fA-F]{3,6}/` — the box-shadow uses rgba which is permitted, so scope the no-hex assertion to the `.hub-modal {` declaration block only).
    - Keyframes declared: `@keyframes hub-modal-grow`, `@keyframes hub-modal-shrink`, `@keyframes hub-modal-overlay-in`, `@keyframes hub-modal-overlay-out`.
    - `.hub-modal--entering` appears AFTER the `prefers-reduced-motion: no-preference` media query index (animation assignment lives inside the no-preference guard).
    - A `prefers-reduced-motion: reduce` block sets `animation: none` on `.hub-modal` (use `lastIndexOf('prefers-reduced-motion: reduce')` block-finder).
    - `.hub-modal__send-btn` block contains `var(--hub-accent)`.
    - `.hub-modal__tail` block contains `var(--hub-preview-bg)`.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run style.hub.modal --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File runs and reports FAILING assertions because the modal CSS does not yet exist in style.css (the keyframe and class assertions return -1 / fail), NOT "0 tests found".
    - Contains explicit assertions for the literal strings `z-index: 200`, `@keyframes hub-modal-grow`, `@keyframes hub-modal-shrink`, and `animation: none`.
    - Uses the indexOf/lastIndexOf block-finder pattern (not a bare `cssRaw.includes` on the whole file for the z-index/reduced-motion checks).
  </acceptance_criteria>
  <done>style.hub.modal.test.ts exists, runs, and fails because modal CSS is not yet written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan creates test files only — no runtime code, no input crosses a trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-01-01 | Tampering | test source files | accept | Test-only artifacts; no runtime surface, no untrusted input. The security-relevant assertions (maxLength=4096 input guard) are AUTHORED here so downstream implementation is held to them. |
| T-134-SC | Tampering | npm/pip installs | mitigate | No new packages introduced (134-RESEARCH.md Package Legitimacy Audit: none). No install task in this plan. |
</threat_model>

<verification>
Run `cd frontend && pnpm test --run HubModal HubInteractiveModal HubBriefingModal style.hub.modal --reporter=verbose 2>&1 | tail -30`. All four new files must be discovered and reported as failing (red) for the correct reason: missing implementation targets, not malformed tests. The rest of the suite must remain green (these files are additive).
</verification>

<success_criteria>
- All four test files exist in their VALIDATION.md-specified locations.
- Each is discovered by vitest and fails because implementation does not yet exist.
- No file mounts a component or imports xterm.
- Pre-existing suite (1638+ tests) remains green.
</success_criteria>

<output>
Create `.planning/phases/134-modal-interaction/134-01-SUMMARY.md` when done.
</output>
