---
phase: 132-unified-grid-mini-preview-named-groups
plan: 04
type: execute
wave: 3
depends_on: ["132-02", "132-03"]
files_modified:
  - frontend/src/components/Hub/SessionCard.tsx
  - frontend/src/components/Hub/SessionCard.test.tsx
  - frontend/src/components/Hub/SessionCardGrid.tsx
  - frontend/src/components/Hub/SessionCardGrid.test.tsx
autonomous: true
requirements: [CARD-07, GROUP-02, GROUP-04]
must_haves:
  truths:
    - "SessionCard renders ROW 6 MiniPreview fed by a previewLines prop; the existing rows 1-5 and the Phase 131 Open button are preserved"
    - "SessionCard is a drag source (draggable, sets text/plain = memberKey(name, workDir) on dragStart)"
    - "SessionCard shows a hover/focus overflow menu with Move to group → group options and Remove from group, keyboard-reachable; selecting an option fires onAssignGroup(memberKey, groupId)"
    - "SessionCardGrid groups by named groups when groupDefs is non-empty (named groups in order, Other last); falls back to groupByWorkDir when groupDefs is empty (Phase 131 behavior preserved)"
    - "Sessions not in any named group appear in the Other group (GROUP-04)"
    - "SessionCardGrid threads previewTails (Map by id) and onAssignGroup down to each SessionCard"
  artifacts:
    - path: "frontend/src/components/Hub/SessionCard.tsx"
      provides: "ROW 6 MiniPreview + drag source + overflow group menu"
      contains: "MiniPreview"
    - path: "frontend/src/components/Hub/SessionCardGrid.tsx"
      provides: "groupByNamedGroups + Other fallback + preview/assign threading"
      contains: "groupByNamedGroups"
  key_links:
    - from: "frontend/src/components/Hub/SessionCard.tsx"
      to: "frontend/src/components/Hub/MiniPreview.tsx"
      via: "<MiniPreview lines={previewLines} />"
      pattern: "MiniPreview"
    - from: "frontend/src/components/Hub/SessionCard.tsx"
      to: "frontend/src/lib/hubGroups"
      via: "memberKey(name, session.workDir)"
      pattern: "memberKey"
    - from: "frontend/src/components/Hub/SessionCardGrid.tsx"
      to: "frontend/src/lib/hubGroups"
      via: "groupByNamedGroups uses memberKey + HubGroupDef"
      pattern: "HubGroupDef"
---

<objective>
Extend the two existing Phase 131 card components: `SessionCard` gains ROW 6 (MiniPreview), a drag-source affordance, and an overflow "Move to group" menu (CARD-07 wiring + GROUP-02 drag source/menu); `SessionCardGrid` gains named-group grouping with an "Other" fallback and threads preview tails + the assign callback through (GROUP-04 + CARD-07 plumbing).

Purpose: This connects the Wave-1/2 leaf modules (MiniPreview, hubGroups) into the card surface. It is the last layer before HubPanel/App wiring in Plan 05.
Output: Two extended components + updated tests, all green. MUST preserve every Phase 131 behavior (rows 1-5, the re-attach "Open" button + handleOpenSessionTab path, and groupByWorkDir fallback).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-unified-grid-mini-preview-named-groups/132-UI-SPEC.md
@.planning/phases/132-unified-grid-mini-preview-named-groups/132-PATTERNS.md

<interfaces>
<!-- SessionCard mods in 132-PATTERNS.md §SessionCard.tsx (lines 539-612). SessionCardGrid mods in §SessionCardGrid.tsx (lines 615-697, includes full groupByNamedGroups body). -->

Existing SessionCard (Phase 131): renders <article className="hub-card{--dim}"> with rows 1-5; has onRename + onOpenSession + the "Open" button (handleOpenSessionTab). PRESERVE these. STATUS_CONFIG + deriveHubStatus already imported.

SessionCard new props (132-PATTERNS.md lines 592-603):
  previewLines?: string[]   (CARD-07; undefined = loading)
  groupDefs?: HubGroupDef[] (GROUP-02 menu options)
  onAssignGroup?: (memberKey: string, groupId: string) => void

Drag source pattern (UI-SPEC §3, 132-PATTERNS.md lines 551-562):
  <article draggable="true" onDragStart={e => { e.dataTransfer.setData('text/plain', memberKeyForSession); e.dataTransfer.effectAllowed='move'; setIsDragging(true) }} onDragEnd={() => setIsDragging(false)}>
  memberKeyForSession = memberKey(name, session.workDir)  (import from ../../lib/hubGroups)

Overflow menu (UI-SPEC §3 "per-card menu" + §Accessibility 5):
  .hub-card__menu-btn (EllipsisHorizontalIcon, aria-expanded, aria-haspopup="menu")
  .hub-card__menu (role="menu"), .hub-card__menu-item (role="menuitem"), .hub-card__menu-divider
  Items: "Move to group" → one sub-item per groupDef ("{name}") + "Other (default)"; "Remove from group" (only when session is currently in a named group)
  Selecting a group sub-item → onAssignGroup(memberKeyForSession, groupId); "Other (default)" and "Remove from group" → onAssignGroup(memberKeyForSession, '__other__') OR a remove path (HubPanel maps '__other__'/remove to removeFromGroup in Plan 05). Use groupId '__other__' sentinel for the Other/remove case.
  Escape closes menu + returns focus to .hub-card__menu-btn; click-outside closes.

SessionCardGrid new props (132-PATTERNS.md lines 658-668):
  groupDefs?: HubGroupDef[]; previewTails?: Map<string,string[]>; onAssignGroup?: (memberKey, groupId) => void
  groupByNamedGroups(sessions, groupDefs) → Map<groupKey,{label,sessions}> (full body at 132-PATTERNS.md lines 675-696): named groups in def order, then '__other__' labelled "Other"; a session goes to the first def whose memberKeys includes memberKey(s.name,s.workDir), else Other.
  When groupDefs is empty/undefined → keep existing groupByWorkDir render path (Phase 131).
  Each SessionCard receives previewLines={previewTails?.get(s.id)} and onAssignGroup + groupDefs.

Source-level comments REQUIRED:
  /* CARD-07: mini preview is plain text snapshot — NO xterm instance; polling interval 3s shared across all cards */
  /* GROUP-04: membership key = "${session.name}:::${session.workDir}" — survives session-id churn */
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionCard — ROW 6 MiniPreview + drag source + overflow group menu</name>
  <files>frontend/src/components/Hub/SessionCard.tsx, frontend/src/components/Hub/SessionCard.test.tsx</files>
  <read_first>
    frontend/src/components/Hub/SessionCard.tsx (FULL existing file, lines 1-228 — must preserve rows 1-5 + Open button),
    frontend/src/components/Hub/SessionCard.test.tsx (existing assertions to keep green),
    132-PATTERNS.md §SessionCard.tsx (lines 539-612),
    132-UI-SPEC.md §3 (drag handle + menu) + §Updated SessionCard Row Layout,
    frontend/src/components/Hub/MiniPreview.tsx (from Plan 03), frontend/src/lib/hubGroups.ts (memberKey)
  </read_first>
  <behavior>
    - renders existing rows 1-5 unchanged AND a ROW 6 .hub-card__preview via <MiniPreview lines={previewLines} />
    - the Phase 131 "Open" button is still present and still calls onOpenSession (regression guard)
    - <article> has draggable="true"; firing dragStart sets dataTransfer text/plain to memberKey(name, workDir)
    - a .hub-card__drag-handle (Bars3Icon) and .hub-card__menu-btn (EllipsisHorizontalIcon) are present
    - clicking .hub-card__menu-btn opens .hub-card__menu (role="menu"); aria-expanded reflects open state
    - menu lists "Move to group" with one item per groupDef plus "Other (default)"; selecting groupDef X fires onAssignGroup(memberKey, X.id)
    - "Remove from group" appears only when the session's memberKey is in some groupDef; selecting it fires onAssignGroup(memberKey, '__other__')
    - Escape while menu open closes the menu
  </behavior>
  <action>
    Modify frontend/src/components/Hub/SessionCard.tsx per 132-PATTERNS.md §SessionCard.tsx. Add imports: Bars3Icon, EllipsisHorizontalIcon from @heroicons/react/24/outline; MiniPreview from './MiniPreview'; memberKey + HubGroupDef from '../../lib/hubGroups'. Add the three new optional props to SessionCardProps. Compute `const memberKeyForSession = memberKey(name, session.workDir)`. Add `draggable="true"` + onDragStart/onDragEnd (local isDragging state controls a dim class). Add the absolute-positioned `.hub-card__drag-handle` (aria-hidden) and the `.hub-card__menu-btn` button (aria-expanded, aria-haspopup="menu", aria-label "Card options for {name}"). Add local `menuOpen` state and render `.hub-card__menu` (role="menu") with "Move to group" header + group sub-items + "Other (default)" + conditional "Remove from group", each role="menuitem". Wire selections to onAssignGroup. Escape closes menu and refocuses the menu button; add a click-outside handler. Append `<MiniPreview lines={previewLines} />` as ROW 6 after the existing ROW 5. Do NOT touch rows 1-5 markup or the Open button logic. Add the CARD-07 source comment near ROW 6.

    Update frontend/src/components/Hub/SessionCard.test.tsx: KEEP all existing Phase 131 assertions; ADD cases for ROW 6 presence, dragStart payload, menu open/close, group-assign callback, conditional Remove-from-group, and an explicit regression assertion that the Open button still fires onOpenSession.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/components/Hub/SessionCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm vitest run src/components/Hub/SessionCard.test.tsx` passes ALL cases (existing Phase 131 + new)
    - `grep -c "MiniPreview" frontend/src/components/Hub/SessionCard.tsx` returns at least 1
    - `grep -c "handleOpenSessionTab\|onOpenSession" frontend/src/components/Hub/SessionCard.tsx` returns at least 1 (Open button preserved)
    - `grep -c "draggable" frontend/src/components/Hub/SessionCard.tsx` returns at least 1
    - `grep -c "role=\"menu\"" frontend/src/components/Hub/SessionCard.tsx` returns at least 1
  </acceptance_criteria>
  <done>SessionCard shows ROW 6 preview, is a drag source, and exposes a keyboard-reachable group menu; all Phase 131 behavior preserved; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SessionCardGrid — groupByNamedGroups + Other fallback + preview/assign threading</name>
  <files>frontend/src/components/Hub/SessionCardGrid.tsx, frontend/src/components/Hub/SessionCardGrid.test.tsx</files>
  <read_first>
    frontend/src/components/Hub/SessionCardGrid.tsx (FULL existing file, lines 1-103),
    frontend/src/components/Hub/SessionCardGrid.test.tsx (existing assertions to keep green),
    132-PATTERNS.md §SessionCardGrid.tsx (lines 615-697, full groupByNamedGroups body),
    frontend/src/lib/hubGroups.ts (memberKey, HubGroupDef)
  </read_first>
  <behavior>
    - groupDefs empty/undefined → existing groupByWorkDir render path (Phase 131 group headers + Other) is unchanged
    - groupDefs non-empty → groupByNamedGroups produces named groups in definition order, then an "Other" group last
    - a session whose memberKey(name,workDir) is in groupDef X renders under X; a session matching no def renders under "Other" (GROUP-04)
    - each rendered SessionCard receives previewLines={previewTails?.get(s.id)} and the onAssignGroup + groupDefs props
  </behavior>
  <action>
    Modify frontend/src/components/Hub/SessionCardGrid.tsx per 132-PATTERNS.md §SessionCardGrid.tsx. Add `import { memberKey, type HubGroupDef } from '../../lib/hubGroups'`. Add the three new optional props (groupDefs, previewTails, onAssignGroup) to SessionCardGridProps. Add the exported `groupByNamedGroups(sessions, groupDefs)` helper using the verbatim body from the patterns doc (Map of groupId → {label, sessions}; named defs first in order, '__other__' labelled "Other" last; match via memberKey + def.memberKeys.includes). In the component, branch: if groupDefs && groupDefs.length > 0 render from groupByNamedGroups (group header label from the entry's label; key from the map key), else keep the existing groupByWorkDir path. In BOTH paths pass `previewLines={previewTails?.get(s.id)}`, `groupDefs={groupDefs}`, and `onAssignGroup={onAssignGroup}` to each SessionCard. Keep role=list/listitem structure. Add the GROUP-04 source comment near groupByNamedGroups.

    Update frontend/src/components/Hub/SessionCardGrid.test.tsx: KEEP existing Phase 131 grouping assertions; ADD cases for named-group grouping (session in a def renders under its name), Other fallback (unmatched session under "Other"), preview threading (a stubbed SessionCard or query confirming previewLines reaches the card), and that empty groupDefs keeps workDir behavior.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest run src/components/Hub/SessionCardGrid.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && pnpm vitest run src/components/Hub/SessionCardGrid.test.tsx` passes ALL cases (existing + new)
    - `grep -c "groupByNamedGroups" frontend/src/components/Hub/SessionCardGrid.tsx` returns at least 1
    - `grep -c "groupByWorkDir" frontend/src/components/Hub/SessionCardGrid.tsx` returns at least 1 (Phase 131 fallback preserved)
    - `grep -c "previewTails" frontend/src/components/Hub/SessionCardGrid.tsx` returns at least 1
  </acceptance_criteria>
  <done>SessionCardGrid groups by named groups with an Other fallback, preserves workDir grouping when no groups exist, and threads preview tails + assign callback; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| previewLines prop → MiniPreview → DOM | Stripped server-side (Plan 01); rendered as React text |
| remote hostname/name → SessionCard rows | Remote-supplied strings rendered via React text escaping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-10 | Injection (XSS) | SessionCard rendering remote name/hostname + preview lines | mitigate | All values rendered as React text children; NO dangerouslySetInnerHTML anywhere in the card |
| T-132-11 | Logic error | named-group grouping dropping sessions | mitigate | Other fallback guarantees every session lands in exactly one group; covered by the Other-fallback test |
| T-132-SC | Tampering | npm installs | n/a | Zero new dependencies |
</threat_model>

<verification>
- `cd frontend && pnpm vitest run src/components/Hub/SessionCard.test.tsx src/components/Hub/SessionCardGrid.test.tsx` green
- `grep -rn "dangerouslySetInnerHTML" frontend/src/components/Hub/SessionCard.tsx` returns nothing
- Phase 131 regression: existing SessionCard/SessionCardGrid tests still pass
</verification>

<success_criteria>
- CARD-07 preview wired into the card via prop (still no xterm, single shared poller arrives in Plan 05)
- GROUP-02 drag source + overflow group menu complete (keyboard-reachable)
- GROUP-04 named-group grouping with Other fallback; workDir fallback preserved
- Every Phase 131 behavior intact (Open button, rows 1-5)
</success_criteria>

<output>
Create `.planning/phases/132-unified-grid-mini-preview-named-groups/132-04-SUMMARY.md` when done
</output>
