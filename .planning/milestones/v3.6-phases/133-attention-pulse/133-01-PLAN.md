---
phase: 133-attention-pulse
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/hubStatus.ts
  - frontend/src/lib/hubStatus.test.ts
autonomous: true
requirements: [ATTN-01]
must_haves:
  truths:
    - "isAttentionStatus returns true for waiting, errored, and stopped-err; false for running, idle, stopped-ok"
    - "A single canonical attention predicate exists that every Hub component imports"
  artifacts:
    - path: "frontend/src/lib/hubStatus.ts"
      provides: "isAttentionStatus(status: HubStatus): boolean export"
      contains: "export function isAttentionStatus"
    - path: "frontend/src/lib/hubStatus.test.ts"
      provides: "unit coverage for all six HubStatus values"
      contains: "isAttentionStatus"
  key_links:
    - from: "frontend/src/lib/hubStatus.ts"
      to: "HubStatus type"
      via: "predicate over existing enum"
      pattern: "status === 'waiting'"
---

<objective>
Add the canonical `isAttentionStatus()` predicate to `hubStatus.ts` — the single source of truth for whether a session needs attention (ATTN-01). Every Phase 133 component (SessionCard, SessionCardGrid, GroupSidebar, HubPanel) consumes this one function so attention logic never diverges.

Purpose: This is the foundation contract for the entire phase. Defining it first (Wave 1) means the consuming components in Waves 2-3 import a stable, tested predicate rather than re-implementing inline status checks.

Output: `isAttentionStatus` export in `hubStatus.ts` + a new `hubStatus.test.ts` covering all six `HubStatus` values.

NO backend work. The `HubStatus` type already includes `'waiting' | 'errored' | 'stopped-err'` — no type change is needed. This is a pure frontend predicate over the existing enum.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-attention-pulse/133-UI-SPEC.md
@.planning/phases/133-attention-pulse/133-PATTERNS.md

<interfaces>
<!-- Current full contents of hubStatus.ts (28 lines) — extracted from codebase, no exploration needed. -->

From frontend/src/lib/hubStatus.ts:
```typescript
import type { SessionInfo } from '../wailsjs/go/main/App'

export type HubStatus = 'running' | 'idle' | 'waiting' | 'errored' | 'stopped-ok' | 'stopped-err'

export function deriveHubStatus(s: SessionInfo): HubStatus {
  if (s.state === 'stopped') {
    return (s.exitCode ?? 0) !== 0 ? 'stopped-err' : 'stopped-ok'
  }
  return s.status as HubStatus
}
```

Attention set (from 133-UI-SPEC.md Attention Detection Contract, LOCKED):
`isAttentionStatus(status)` is true iff status is one of `waiting`, `errored`, `stopped-err`.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Add isAttentionStatus predicate (RED → GREEN)</name>
  <files>frontend/src/lib/hubStatus.ts, frontend/src/lib/hubStatus.test.ts</files>
  <read_first>
    - frontend/src/lib/hubStatus.ts (full file — 28 lines; the `deriveHubStatus` export is the exact-match analog to copy the exported-function style from)
    - .planning/phases/133-attention-pulse/133-PATTERNS.md (hubStatus.ts section, lines 24-50 — analog + exact addition pattern)
    - .planning/phases/133-attention-pulse/133-UI-SPEC.md (Attention Detection Contract — locked predicate definition)
  </read_first>
  <behavior>
    - Test: isAttentionStatus('waiting') === true
    - Test: isAttentionStatus('errored') === true
    - Test: isAttentionStatus('stopped-err') === true
    - Test: isAttentionStatus('running') === false
    - Test: isAttentionStatus('idle') === false
    - Test: isAttentionStatus('stopped-ok') === false
  </behavior>
  <action>
    Create `frontend/src/lib/hubStatus.test.ts` FIRST. Import `isAttentionStatus` (and `HubStatus` type) from `./hubStatus`. Write one `describe('isAttentionStatus')` block with six `it` cases asserting the truth table above. Run the suite and confirm it fails (RED) because the export does not yet exist.

    Then append `isAttentionStatus` to `frontend/src/lib/hubStatus.ts` after `deriveHubStatus`, copying the exported-function style of `deriveHubStatus`. Signature: `export function isAttentionStatus(status: HubStatus): boolean`. Body returns `status === 'waiting' || status === 'errored' || status === 'stopped-err'`. Prefix the function with the source comment: `/* ATTN-01: canonical attention predicate — waiting, errored, or non-zero-exit sessions need attention */`.

    The predicate takes a `HubStatus`, NOT a `SessionInfo` — callers call `deriveHubStatus(s)` first. Do not add a `SessionInfo` overload. Do not modify the `HubStatus` type (it already includes all three attention values). Run the suite again and confirm GREEN.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run src/lib/hubStatus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function isAttentionStatus" frontend/src/lib/hubStatus.ts` returns 1
    - `frontend/src/lib/hubStatus.test.ts` exists and contains six assertions covering all HubStatus values
    - `cd frontend && pnpm test -- --run src/lib/hubStatus.test.ts` exits 0 with 6 passing cases
    - `frontend/src/lib/hubStatus.ts` `HubStatus` type union is byte-for-byte unchanged (no new status values)
  </acceptance_criteria>
  <done>isAttentionStatus is exported, returns the correct truth table for all six HubStatus values, and the dedicated test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure derivation over already-fetched session status; no new data ingress, RPC, input, or storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Information Disclosure | isAttentionStatus predicate | accept | Pure boolean over an existing enum; exposes no data not already rendered by Phase 131/132 |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: none installed); no install step |
</threat_model>

<verification>
- `cd frontend && pnpm test -- --run src/lib/hubStatus.test.ts` passes (6 cases)
- `cd frontend && pnpm tsc --noEmit` shows no new type errors from hubStatus.ts
</verification>

<success_criteria>
- `isAttentionStatus(status: HubStatus): boolean` is exported from hubStatus.ts
- Truth table verified by unit test for all six HubStatus values
- No change to the HubStatus type union
</success_criteria>

<output>
Create `.planning/phases/133-attention-pulse/133-01-SUMMARY.md` when done
</output>
