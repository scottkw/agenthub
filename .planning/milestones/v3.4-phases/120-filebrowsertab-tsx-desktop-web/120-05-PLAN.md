---
phase: 120
plan: 05
type: execute
wave: 4
depends_on: [120-01, 120-04]
files_modified:
  - frontend/e2e/files-browser.spec.ts
  - frontend/e2e/fixture-env.ts
  - frontend/playwright.config.ts
autonomous: false
requirements: [UI-14, UI-01, UI-02, UI-04, UI-05, UI-06, UI-07, UI-08, UI-09, UI-10, UI-11, UI-12, UI-13]
must_haves:
  truths:
    - "Playwright suite frontend/e2e/files-browser.spec.ts covers all 12 UI-14 scenarios on Chromium + Firefox + WebKit"
    - "Suite is a hard merge gate — phase verification requires all 36 (12 × 3) cells GREEN"
    - "Suite exercises the same /app/ webserver route + /api/files/* API used by real remote users (no Wails-specific shortcuts)"
    - "Web-CSP regression spec continues to pass — adding the file browser tab introduces ZERO CSP violations"
  artifacts:
    - path: "frontend/e2e/files-browser.spec.ts"
      provides: "Cross-browser e2e merge-gate suite for Phase 120 (12 scenarios)"
    - path: "frontend/playwright.config.ts"
      provides: "Updated to include the new spec file in default test discovery (no other config changes)"
  key_links:
    - from: "frontend/e2e/files-browser.spec.ts"
      to: "Plan 01 webserver /app/ + Plan 01 fixture seed tree"
      via: "appUrl(env) + env.cap + env.viewerCap"
      pattern: "appUrl"
    - from: "frontend/e2e/files-browser.spec.ts"
      to: "Plans 03-04 data-testid taxonomy"
      via: "page.getByTestId('file-browser-*')"
      pattern: "file-browser-"
---

<objective>
Phase 120 merge-gate Playwright suite. Twelve scenarios covering every UI requirement, executed across Chromium + Firefox + WebKit against the real webserver `/app/` route serving the real React bundle, with the Plan 01 fixture seeding deterministic test files. This is the LAST plan in Phase 120 — once green on all three browsers, the phase is shippable.

Purpose: Exercise the entire stack end-to-end (FilesApiClient → /api/files/* → internal/files Handler → seeded testdata tree) the same way a real remote viewer would, and verify the colorblind-safe / keyboard-accessible / non-base64 / CSP-clean contracts hold in real browsers.
Output: One new Playwright spec file covering the 12 UI-14 scenarios; small fixture-env helper additions if needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-filebrowsertab-tsx-desktop-web/120-CONTEXT.md
@.planning/phases/120-filebrowsertab-tsx-desktop-web/120-RESEARCH.md
@.planning/phases/120-filebrowsertab-tsx-desktop-web/120-UI-SPEC.md
@.planning/phases/120-filebrowsertab-tsx-desktop-web/120-01-PLAN.md
@.planning/phases/120-filebrowsertab-tsx-desktop-web/120-04-PLAN.md
@frontend/playwright.config.ts
@frontend/e2e/global-setup.ts
@frontend/e2e/fixture-env.ts
@frontend/e2e/web-csp.spec.ts
@frontend/e2e/web-vendor-parity.spec.ts

<interfaces>
<!-- Plan 01 deliverables -->

Fixture stdout emits (in order): BASE_URL, CAP (owner: read,write,files.read), VIEWER_CAP (viewer: read), SESSION_CWD, ADMIN_URL, READY=1

Fixture seeds session cwd with:
- hello.txt — "Hello, world!\n" (14 bytes)
- notes.md — GFM markdown with a table and a task list
- image.png — valid 1×1 PNG
- binary.bin — 64 bytes of alternating 0x00 0xFF (binary)
- large.txt — 5*1024*1024+1 bytes of 'A' (over 5 MiB cap)
- empty.txt — 0 bytes
- subdir/nested.txt — "nested\n"

fixture-env.ts FixtureEnv has: { baseURL, cap, viewerCap, sessionCwd, adminURL, pid }; appUrl(env) helper builds `{baseURL}/app/?session=playwright-test-session&cap={cap}`

<!-- Plan 03/04 data-testid taxonomy (canonical names from UI-SPEC §Test Hooks) -->

| Role | data-testid |
|---|---|
| Tab root | file-browser-tab |
| Breadcrumb | file-browser-breadcrumb |
| Breadcrumb segment N | file-browser-breadcrumb-segment-{n} |
| Refresh button | file-browser-refresh |
| List pane | file-browser-list |
| List row by name | file-browser-row-{name} |
| Column header (name/size/modified) | file-browser-col-{name\|size\|modified} |
| Preview pane | file-browser-preview |
| Preview filename | file-browser-preview-name |
| Download button | file-browser-download |
| Filter input | file-browser-filter |
| Status line | file-browser-status |
| Empty state | file-browser-empty |
| Permission-denied takeover | file-browser-permission-denied |
| Network error | file-browser-network-error |
| Over-cap refusal | file-browser-over-cap |
| Binary refusal | file-browser-binary |
| Markdown preview body | file-browser-preview-markdown |
| Image preview body | file-browser-preview-image |
| Text preview body | file-browser-preview-text |

<!-- Existing playwright config -->

Already configured for Chromium + Firefox + WebKit in `frontend/playwright.config.ts`. testMatch is `/.*\.spec\.ts$/` so the new file will be auto-discovered with no config edit required.

ignoreHTTPSErrors: true (self-signed cert from fixture — required to load `/app/`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author files-browser.spec.ts — 12 cross-browser e2e scenarios</name>
  <files>frontend/e2e/files-browser.spec.ts</files>
  <read_first>
    - frontend/e2e/web-csp.spec.ts (existing spec — mirror its imports, setup hooks, and page-error collection pattern)
    - frontend/e2e/web-vendor-parity.spec.ts (existing — mirror its page.goto + getByTestId + expect.toBeVisible patterns)
    - frontend/e2e/fixture-env.ts (loadFixtureEnv + appUrl helpers)
    - .planning/phases/120-filebrowsertab-tsx-desktop-web/120-RESEARCH.md §"Validation Architecture" + §"Phase Requirements → Test Map" (test name guidance for `pnpm exec playwright test -g` filters)
  </read_first>
  <behavior>
    Single spec file with 12 `test(...)` blocks under one `test.describe('FileBrowserTab', ...)` group. Each test is independent (no shared state between tests beyond fixture). Use `test.beforeEach` to navigate to `appUrl(env)` (owner cap) UNLESS the scenario tests viewer cap or no cap.

    **Helper: openFileBrowser(page, sessionId)** — clicks the "Sessions" tab in the sidebar/tabbar, then clicks the "Browse files" button next to the test session row. Returns once `getByTestId('file-browser-tab')` is visible. Used by tests 1, 4, 5, 6, 7, 8, 9, 10, 11.

    The 12 scenarios (each a Playwright `test`, runs on all 3 projects):

    1. **Open tab from DaemonManagerPanel** (UI-01)
       - Navigate to appUrl(env, owner)
       - Wait for Sessions panel to render (it's not the default — may need to click sidebar "Sessions" link first; depends on App.tsx initial route; if necessary, navigate directly to a hash-anchored view OR use the WelcomeTab "Sessions" CTA)
       - Click `data-testid="daemon-panel-browse-playwright-test-session"`
       - Assert `data-testid="file-browser-tab"` visible
       - Assert tab name in tab bar contains "Playwright Test Session — Files" (or equivalent — sessionName comes from fixture L65)
       - Assert breadcrumb has root segment "session"

    2. **Open tab from TabBar context menu** (UI-01)
       - Navigate, open Sessions, click into the test session terminal tab so a terminal tab exists in the TabBar
       - Right-click the terminal tab in the TabBar
       - Click `data-testid="tab-context-browse-files"`
       - Assert `file-browser-tab` visible; assert it's a NEW tab (find-or-add singleton — clicking again should not create a second tab)

    3. **List cwd shows seeded files** (UI-02, UI-03)
       - openFileBrowser
       - Assert `file-browser-row-hello.txt`, `file-browser-row-notes.md`, `file-browser-row-image.png`, `file-browser-row-binary.bin`, `file-browser-row-empty.txt`, `file-browser-row-large.txt`, `file-browser-row-subdir` all visible
       - Assert subdir row appears BEFORE all file rows (directories sticky on top)

    4. **Navigate into subdirectory and back via breadcrumb** (UI-05)
       - openFileBrowser
       - Double-click `file-browser-row-subdir`
       - Assert URL/state shows path subdir; breadcrumb has 2 segments
       - Assert `file-browser-row-nested.txt` visible
       - Click `file-browser-breadcrumb-segment-0` (the "session" root) — assert it's non-clickable text (cursor:default; no aria-current page); use the parent route instead: click segment-1 if present, or alternatively press Backspace
       - Press Backspace; assert back at root
       - Press Backspace AGAIN at root; assert no navigation occurs (UI-05 cwd boundary)

    5. **Preview text file** (UI-08)
       - openFileBrowser
       - Click `file-browser-row-hello.txt`
       - Wait for `file-browser-preview-text` visible
       - Assert preview content contains literal `Hello, world!`
       - Assert font-family is monospace (`expect(locator).toHaveCSS('font-family', /Cascadia|monospace/i)`)
       - Assert preview header `file-browser-preview-name` shows "hello.txt"

    6. **Preview markdown file with GFM table** (UI-07)
       - openFileBrowser
       - Click `file-browser-row-notes.md`
       - Wait for `file-browser-preview-markdown` visible
       - Assert rendered DOM contains a `<table>` element (proves remark-gfm is active)
       - Assert rendered DOM contains `<input type="checkbox" disabled>` (proves task list rendering)
       - Assert NO `<style>` or `<script>` tag inside the markdown preview body (rehype-raw NOT active)

    7. **Preview image via direct src URL** (UI-09)
       - openFileBrowser
       - Click `file-browser-row-image.png`
       - Wait for `file-browser-preview-image` visible
       - Locate `<img>` inside; assert `src` attribute starts with `/api/files/read?` AND contains `path=image.png` AND contains `cap=`
       - Assert `src` is NOT `data:image/` (Pitfall 10 — base64 forbidden)
       - Wait for image natural load: `await page.locator('[data-testid="file-browser-preview-image"] img').evaluate((img: HTMLImageElement) => img.complete && img.naturalWidth > 0)` returns true

    8. **Binary file shows Unsupported refusal + Download button** (UI-06, UI-10)
       - openFileBrowser
       - Click `file-browser-row-binary.bin`
       - Wait for `file-browser-binary` visible (UnsupportedFile component kind=unsupported)
       - Assert heading copy contains "can't display" (case-insensitive)
       - Assert `file-browser-download` visible
       - Get the Download href: it starts with `/api/files/read?` AND contains `path=binary.bin` AND `cap=`

    9. **Over-cap file shows Over-cap refusal + Download** (UI-06)
       - openFileBrowser
       - Click `file-browser-row-large.txt`
       - Wait for `file-browser-over-cap` visible
       - Assert body copy contains "5 MB" (the documented cap)
       - Assert `file-browser-download` visible

    10. **Download button issues GET on /api/files/read** (UI-10)
        - openFileBrowser
        - Click hello.txt → preview text shows
        - Set up `const [download] = await Promise.all([page.waitForEvent('download'), page.locator('[data-testid="file-browser-download"]').click()])`
        - Assert `download.suggestedFilename()` equals `hello.txt`
        - Assert the URL hit was `/api/files/read?...path=hello.txt...`
        - Note: this only fires if there's a Download button in the preview header for text files. Per UI-SPEC, Download IS shown in all preview kinds (text + markdown + image + unsupported + over-cap); verify by reading PreviewPane.tsx output

    11. **Capability-denied viewer sees PermissionDeniedTakeover** (UI-13)
        - Build viewerUrl: `${env.baseURL}/app/?session=playwright-test-session&cap=${encodeURIComponent(env.viewerCap)}`
        - Navigate, open file browser
        - Wait for `file-browser-permission-denied` visible
        - Assert body copy contains "files.read" (UI-13 explicit copy requirement)
        - Assert `file-browser-list` is NOT visible (full-tab takeover per UI-SPEC §State machine)

    12. **Type-ahead filter (/) + Escape clears + keyboard nav + empty filter no-match** (UI-04, UI-12)
        - openFileBrowser
        - Press `Tab` until focus is inside the file-browser tab content (use `page.keyboard.press('Tab')` a few times, or directly focus the list pane via `page.getByTestId('file-browser-list').focus()`)
        - Press `/` — assert `file-browser-filter` visible AND focused
        - Type `hello` — assert ONLY `file-browser-row-hello.txt` row visible (other rows hidden or removed)
        - Press Escape — assert filter dismissed; full list returned
        - Press `/`, type `zzznomatch` — assert `file-browser-no-match` element visible (or `file-browser-empty` — exact testid per Plan 03's no-match copy block)
        - Press Escape; press ArrowDown — assert selection moves to second row (keyboard nav UI-12)
        - Press Backspace at root → no-op (UI-05 boundary verified again)

    **Cross-cutting assertions (run in beforeEach or as a separate describe):**
    - Page error collector: `page.on('pageerror', e => errors.push(e))` and `page.on('console', m => { if (m.type() === 'error') errors.push(m.text()) })`. After each test, assert `errors.filter(e => !ALLOWED.includes(e)).length === 0`. The ALLOWED list is empty for new tests (mirror web-csp.spec.ts pattern).
    - CSP violation check: integrate the existing web-csp.spec.ts pattern OR add a single assertion `await page.evaluate(() => (window as any).__cspViolations || [])` returning 0 violations per scenario.

    **Cross-browser determinism:**
    - WebKit (Safari) handles `<a download>` differently — use `page.waitForEvent('download')` not `page.waitForRequest` for test 10
    - Firefox may differ on `font-family` CSS resolution — match against a regex that accepts any of Cascadia / Fira / monospace
    - Use `expect(locator).toBeVisible()` with default timeout (5s) — increase to 10s for first scenario only if WebKit boot is slow

    **Use of viewerCap (test 11) does NOT require new fixture work** — Plan 01 already exposes viewerCap in env.
  </behavior>
  <action>
    1. Author `frontend/e2e/files-browser.spec.ts` per the 12 scenarios above (~500-700 lines).
    2. If `appUrl` helper signature doesn't match what tests need (e.g., need a separate `viewerAppUrl`), extend `frontend/e2e/fixture-env.ts` with `viewerAppUrl(env)` returning `${env.baseURL}/app/?session=playwright-test-session&cap=${encodeURIComponent(env.viewerCap)}`.
    3. Do NOT modify `playwright.config.ts` — testMatch regex auto-picks up the new file.
    4. Make sure CSP error collector accepts the same allow-list as web-csp.spec.ts (zero new violations).

    Execution model:
    - `cd frontend && pnpm exec playwright test files-browser.spec.ts --project=chromium` for fast local iteration
    - Full gate: `cd frontend && pnpm exec playwright test files-browser.spec.ts` (all 3 projects = 36 cells)
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub/frontend && pnpm exec playwright test files-browser.spec.ts --project=chromium --reporter=list 2>&1 | tail -20 | grep -E '12 passed|all tests passed' && echo PASS</automated>
  </verify>
  <done>
    - 12 scenarios on Chromium PASS (fast iteration loop)
    - 12 scenarios on Firefox PASS
    - 12 scenarios on WebKit PASS
    - Existing Phase 93+ specs (web-csp.spec.ts, web-vendor-parity.spec.ts, etc.) continue to PASS with no regression
    - Zero CSP violations introduced (verified by web-csp.spec.ts continuing to pass)
    - `grep -c 'test\\(' frontend/e2e/files-browser.spec.ts` ≥ 12
    - `grep -c 'file-browser-' frontend/e2e/files-browser.spec.ts` ≥ 30 (testids used throughout)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full-matrix Playwright run + sign-off on UI-14 merge gate</name>
  <files>(no files modified — full-matrix Playwright run + operator sign-off)</files>
  <what-built>The 12-scenario suite on all 3 browsers — Phase 120's primary acceptance gate per ROADMAP Success Criterion #5.</what-built>
  <how-to-verify>
    Run from frontend/:
    ```
    pnpm exec playwright test files-browser.spec.ts --reporter=list
    ```
    Expected output: `36 passed` (12 scenarios × 3 projects). If any cell fails:
    - Browser-specific failure (e.g., WebKit only) → likely a CSS / keyboard / download-API quirk; iterate scenario and re-run that single project.
    - Same scenario fails on all 3 → likely a real bug in Plan 03/04 component; file a gap and route to gap-closure plan.

    Also run the full existing suite to verify no regressions:
    ```
    pnpm exec playwright test --reporter=list
    ```
    Expected: previous total + 36 new = green.
  </how-to-verify>
  <action>Operator runs `cd frontend && pnpm exec playwright test files-browser.spec.ts --reporter=list` and then `pnpm exec playwright test --reporter=list` from how-to-verify above; reviews output for 36/36 green and no regressions; types "approved" in resume-signal or pastes failing cells.</action>
  <verify>Operator confirms in chat: `36 passed` for files-browser.spec.ts across Chromium + Firefox + WebKit AND full suite green (no regressions in existing Phase 93+ specs).</verify>
  <done>Operator typed "approved" — Phase 120 merge gate cleared; phase is shippable. If failing cells reported, route to /gsd:plan-phase 120 --gaps.</done>
  <resume-signal>Type "approved" if 36/36 PASS and no existing-suite regressions. Otherwise paste the failing cell(s) and route to gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright browser→/app/ | Real-browser HTTPS load of the React bundle through the cap gate; same path a remote viewer takes |
| Spec file→fixture stdout | Spec reads owner + viewer cap from fixture-env.json (Plan 01 wrote them) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-18 | Tampering | Test bypasses cap gate | accept | All tests use either owner cap (env.cap) or viewer cap (env.viewerCap); no cap-token forgery in the spec |
| T-120-19 | Information Disclosure | Test downloads leak from /tmp | accept | Playwright downloads go to its managed download dir; cleaned up by the harness |
| T-120-20 | Denial of Service | Large file download hangs WebKit | mitigate | Test 10 uses hello.txt (14 bytes) for download, NOT large.txt; large.txt is only used for the over-cap REFUSAL test (no actual download) |
| T-120-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- 36/36 (12 scenarios × 3 browsers) PASS
- `pnpm exec playwright test` full suite green (no regressions in web-csp / web-vendor-parity / web-plugin-hot-swap / web-links-live-toggle / progress)
- No console.error / pageerror events captured outside the existing allow-list
</verification>

<success_criteria>
- All 14 UI-XX requirements have at least one passing e2e cell across all three browsers
- Phase 120's ROADMAP Success Criterion #5 (Playwright e2e on Chromium + Firefox + WebKit covering 12 scenarios) PASSES
- No CSP regressions
</success_criteria>

<output>
Create `.planning/phases/120-filebrowsertab-tsx-desktop-web/120-05-SUMMARY.md` when done.
</output>
