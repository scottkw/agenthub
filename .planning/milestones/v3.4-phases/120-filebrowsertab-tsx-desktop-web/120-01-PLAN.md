---
phase: 120
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/webserver/server.go
  - internal/webserver/files_routes_test.go
  - cmd/playwright-fixture/main.go
  - cmd/playwright-fixture/testdata/files/hello.txt
  - cmd/playwright-fixture/testdata/files/notes.md
  - cmd/playwright-fixture/testdata/files/image.png
  - cmd/playwright-fixture/testdata/files/binary.bin
  - cmd/playwright-fixture/testdata/files/large.txt
  - cmd/playwright-fixture/testdata/files/empty.txt
  - cmd/playwright-fixture/testdata/files/subdir/nested.txt
  - frontend/e2e/fixture-env.ts
autonomous: true
requirements: [UI-11, UI-14]
must_haves:
  truths:
    - "Webserver serves the React app bundle (frontend/dist) at /app under a cap-gated route so a remote/web-share viewer can load FileBrowserTab over HTTPS"
    - "Playwright fixture seeds a deterministic testdata/files/ tree into the test session's cwd so all 12 UI-14 e2e scenarios have known inputs"
    - "Existing /api/files/* routes (Phase 119) and existing /sessions/{id} terminal route continue to pass without regression"
  artifacts:
    - path: "internal/webserver/server.go"
      provides: "GET /app and GET /app/* routes serving frontend/dist via http.FS + http.StripPrefix, gated by requireCapability (cap presence) and cspHeaders, with optional fs.FS provider for fixture/test injection"
    - path: "cmd/playwright-fixture/main.go"
      provides: "Fixture binary seeds testdata/files/ into the playwright-test-session cwd, mints owner cap (read,write,files.read) and viewer cap (read only) and emits both on stdout, sets ws.SetFilesHandler with internal/files.Handler scoped to the seeded cwd"
    - path: "cmd/playwright-fixture/testdata/files/"
      provides: "Deterministic seed tree: hello.txt (text), notes.md (markdown w/ GFM table), image.png (PNG bytes), binary.bin (random bytes), large.txt (>5MiB), empty.txt (0 bytes), subdir/nested.txt"
    - path: "frontend/e2e/fixture-env.ts"
      provides: "FixtureEnv extended with viewerCap and sessionCwd fields; loadFixtureEnv() returns them; new helper appUrl(env) builds {baseURL}/app?session={sid}&cap={cap}"
  key_links:
    - from: "internal/webserver/server.go"
      to: "frontend/dist (embedded via embed.FS at build time, or live FS in dev/fixture)"
      via: "http.FileServer(http.FS(staticFS))"
      pattern: "mux.Handle.*GET /app"
    - from: "cmd/playwright-fixture/main.go"
      to: "internal/files.Handler"
      via: "ws.SetFilesHandler(files.NewHandler(resolverFn))"
      pattern: "SetFilesHandler"
    - from: "frontend/e2e/fixture-env.ts"
      to: ".playwright/fixture-env.json"
      via: "JSON read; new viewerCap + sessionCwd fields"
      pattern: "viewerCap"
---

<objective>
Backend foundation for Phase 120: (1) mount the React `frontend/dist` bundle on the webserver under a cap-gated `/app` route so the web surface (D-01 from CONTEXT) can load the same FileBrowserTab React code over Tailscale HTTPS, and (2) extend `cmd/playwright-fixture` to seed a known `testdata/files/` tree into the test session's cwd and emit BOTH an owner cap (with `files.read`) AND a viewer cap (without `files.read`) so the UI-14 12-scenario merge gate (delivered in Plan 05) has deterministic inputs.

Purpose: Without this plan, the React tab can only run inside Wails (UI-11 strict reading fails) and the e2e suite has no files to preview. Both are merge-gate blockers for Phase 120.
Output: Webserver `/app` route serving the bundle, fixture binary seeding the file tree, viewer cap on stdout, fixture-env.ts loader updated. Existing Phase 119 `/api/files/*` routes and Phase 93+ terminal/CSP/hot-swap specs continue to pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/120-filebrowsertab-tsx-desktop-web/120-CONTEXT.md
@.planning/phases/120-filebrowsertab-tsx-desktop-web/120-RESEARCH.md
@.planning/phases/118-fs-sandbox-core-workdir-gap-daemon-routes-fuzz-corpus-capabi/118-02-SUMMARY.md
@.planning/phases/119-webserver-routes-files-read-capability-plumbing/119-01-SUMMARY.md
@internal/webserver/server.go
@internal/webserver/files_routes_test.go
@cmd/playwright-fixture/main.go
@frontend/e2e/global-setup.ts
@frontend/e2e/fixture-env.ts
@web/embed.go

<interfaces>
<!-- Phase 119 invariants the executor MUST preserve -->

Existing route registrations (from internal/webserver/server.go ~L481-484, do not alter):
```
mux.HandleFunc("GET /api/files/list", ws.requireFilesRead(filesDispatch(...)))
mux.HandleFunc("GET /api/files/stat", ws.requireFilesRead(filesDispatch(...)))
mux.HandleFunc("GET /api/files/read", ws.requireFilesRead(filesDispatch(...)))
mux.HandleFunc("HEAD /api/files/read", ws.requireFilesRead(filesDispatch(...)))
```

Existing capability shape (internal/capability/capability.go via Phase 118):
- `Claims{SID, Perms, IAT, GrantID, V}` — `Perms` is comma-separated string
- Owner cap: `Perms="read,write,files.read"`
- Viewer cap: `Perms="read"` (NO files.read)
- `HasPerm(perms, "files.read")` whole-token match (set-membership, not substring)

Existing fixture contract (cmd/playwright-fixture/main.go):
- Emits BASE_URL, CAP, ADMIN_URL, then READY=1, then blocks on SIGTERM
- Single owner cap minted with `Perms="read,write"` at L139-140
- Build tag `playwrightfixture` so it's never linked into release builds
- internal/files.Handler is currently NOT wired here (Phase 119 wired it in real daemon paths only)

FileEntry JSON wire (from 118-02-SUMMARY):
- Lowercase tags: `name`, `size`, `mtime`, `mode`, `isDir`, `isSymlink`, `isBinary`, `mime`

Existing static-asset pattern (server.go ~L522-524, mirror this style):
```
mux.Handle("GET /assets/xterm/", http.StripPrefix("/assets/xterm/", ...))
mux.Handle("GET /assets/",       http.StripPrefix("/assets/",        ...))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount /app and /app/* on webserver mux serving frontend/dist (cap-gated)</name>
  <files>internal/webserver/server.go, internal/webserver/files_routes_test.go</files>
  <read_first>
    - internal/webserver/server.go (whole file — note setupRoutes structure L408+, cspHeaders L~600+, assetsNoStore L846-, requireCapability)
    - internal/webserver/files_routes_test.go (whole file — copy newFilesTestServer helper style)
    - web/embed.go (existing embed.FS pattern; you will mirror this in a NEW frontend/dist embed slot WITHOUT modifying web/embed.go)
    - .planning/phases/120-filebrowsertab-tsx-desktop-web/120-RESEARCH.md (§Open Question Q1 Option A, which user has locked per D-01)
  </read_first>
  <behavior>
    - Webserver exposes a NEW `staticAppFS fs.FS` field defaulting to nil; `SetStaticAppFS(fsys fs.FS)` setter installs it (mirrors SetFilesHandler shape from Phase 119)
    - `setupRoutes()` registers `GET /app` (redirects to `/app/`) and `GET /app/` + `GET /app/{path...}` serving the bundle
    - The `/app/` route is wrapped by `ws.requireCapability` (cap presence required — same gate as `/sessions/{id}` per server.go L489-496) AND by `ws.cspHeaders` (existing CSP middleware)
    - When `staticAppFS` is nil at request time → 503 with body `"app bundle not configured"` (mirror Phase 119 filesHandler nil-defense pattern at server.go L471-478)
    - Index file (`/app/`) MUST serve `index.html` from the FS with `Content-Type: text/html` and inject the cap+session via the SAME query-string mechanism `/sessions/{id}` already uses — do NOT inject into a `<meta>` tag in v3.4; frontend reads `window.location.search` directly (research §Pattern 2)
    - 405 on POST/PUT/DELETE (Go 1.22+ mux auto-rejects since only GET is registered)
    - Cap-token verification reuses requireCapability without modification (T-119-07 invariant)
    - New integration tests live in `internal/webserver/files_routes_test.go` (appended to the existing file — DO NOT create files_app_test.go; reuse newFilesTestServer harness)
  </behavior>
  <action>
    Add to `internal/webserver/server.go`:
    1. New field `staticAppFS fs.FS` on `WebServer` struct (between `filesHandler` L101 and `pluginConfigMu`)
    2. New setter `func (ws *WebServer) SetStaticAppFS(fsys fs.FS) { ws.staticAppFS = fsys }` adjacent to SetFilesHandler L153
    3. In `setupRoutes()`, immediately AFTER the `/api/files/*` block (after L484), add:
       ```
       mux.HandleFunc("GET /app", func(w http.ResponseWriter, r *http.Request) {
           http.Redirect(w, r, "/app/", http.StatusMovedPermanently)
       })
       mux.Handle("GET /app/", ws.requireCapability(ws.cspHeaders(ws.appBundleHandler())))
       ```
       Define `appBundleHandler()` method that returns a handler reading `ws.staticAppFS` at request time, falling back to 503 on nil, and otherwise serving via `http.StripPrefix("/app/", http.FileServer(http.FS(ws.staticAppFS)))`. Required imports: `io/fs`, `net/http`.
    4. Add `import "io/fs"` to the existing import block.

    Add to `internal/webserver/files_routes_test.go` (extend existing file):
    1. `TestAppBundle_ServesIndexWithCap` — owner cap → 200 + body contains `<!doctype html>` or `<!DOCTYPE html>` (case-insensitive); use a temp `fstest.MapFS` injected via SetStaticAppFS so the test is hermetic.
    2. `TestAppBundle_MissingCapReturns401` — no `?cap=` → 401 (requireCapability gate fires before bundle lookup)
    3. `TestAppBundle_NilFSReturns503` — SetStaticAppFS NOT called → 503 + body contains `"app bundle not configured"`
    4. `TestAppBundle_StaticAsset` — request `/app/assets/main.js` with owner cap, fstest.MapFS contains `assets/main.js: "console.log('x')"` → 200 + body `console.log('x')`
    5. `TestAppBundle_RedirectFromBareApp` — `GET /app` (no trailing slash) → 301 Location: /app/ (do NOT include `?cap=` propagation in v3.4; the bare /app URL is reserved for typed/bookmarked entry; the redirect preserves the query string per http.Redirect default — verify behavior)
    6. `TestAppBundle_PostReturns405` — POST /app/ → 405

    Reuse `newFilesTestServer(t)` from existing file; add a sibling `newAppBundleTestServer(t)` helper if signature divergence (no files handler needed) is cleaner. Inject `fstest.MapFS` via `ws.SetStaticAppFS(fstest.MapFS{"index.html": &fstest.MapFile{Data: []byte("<!DOCTYPE html><html></html>")}, "assets/main.js": &fstest.MapFile{Data: []byte("console.log('x')")}})`.

    DO NOT modify `internal/daemon/api.go` in this task — daemon wiring of the real `frontend/dist` (via embed.FS or live FS) is deferred to Plan 04 when the React tab actually exists. This task only ships the route + nil-fallback.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build ./internal/... && go vet ./internal/webserver/ && go test -run '^TestAppBundle_|^TestFilesRoutes_' -count=1 ./internal/webserver/</automated>
  </verify>
  <done>
    - `go build ./...` clean
    - 6 new TestAppBundle_* subtests pass
    - Existing 15 TestFilesRoutes_* subtests still pass (no regression)
    - `grep -c 'staticAppFS' internal/webserver/server.go` ≥ 3 (field + setter + handler)
    - `grep -c 'SetStaticAppFS' internal/webserver/server.go` == 1 (setter declaration)
    - `grep -c '"GET /app' internal/webserver/server.go` == 2 (bare + slash forms)
    - `grep -c 'requireCapability' internal/webserver/server.go` increased by exactly 1 vs Phase 119 baseline (the /app/ registration)
    - requireCapability and requireFilesRead bodies UNCHANGED — verified by existing `TestRequireCapability_UnchangedByPhase118` continuing to pass
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend Playwright fixture to seed testdata/files/ + mint viewer cap + wire files.Handler</name>
  <files>cmd/playwright-fixture/main.go, cmd/playwright-fixture/testdata/files/hello.txt, cmd/playwright-fixture/testdata/files/notes.md, cmd/playwright-fixture/testdata/files/image.png, cmd/playwright-fixture/testdata/files/binary.bin, cmd/playwright-fixture/testdata/files/large.txt, cmd/playwright-fixture/testdata/files/empty.txt, cmd/playwright-fixture/testdata/files/subdir/nested.txt, frontend/e2e/fixture-env.ts</files>
  <read_first>
    - cmd/playwright-fixture/main.go (whole file — note structure: TLS L86, hub manager L92, webserver L100, signing-key L111, claim minting L138-150)
    - frontend/e2e/fixture-env.ts (27 lines — extend FixtureEnv interface)
    - frontend/e2e/global-setup.ts (98 lines — note how stdout KEY=VALUE lines are parsed L72-79; you will add VIEWER_CAP and SESSION_CWD keys)
    - internal/files/types.go (FileEntry shape; not modified here)
    - internal/files/handler.go (Handler type, NewHandler signature)
    - cmd/playwright-fixture/testdata/files/ (verify directory doesn't already exist before creating)
  </read_first>
  <behavior>
    - Fixture creates an OS temp dir per run, populates it with the seed tree, and uses it as the session cwd
    - Fixture instantiates `*files.Handler` via `files.NewHandler(resolverFn)` where resolverFn returns a `*files.Sandbox` rooted at the temp dir for `sessionID=="playwright-test-session"` and an error otherwise
    - Fixture calls `ws.SetFilesHandler(filesHandler)` BEFORE `ws.Start()` (Pitfall 5 ordering)
    - Fixture mints TWO cap tokens:
      - Owner: `Perms="read,write,files.read"` (replaces existing L138-140 cap)
      - Viewer: `Perms="read"` (new — for the capability-denied UI-14 scenario)
      - Both via `capability.Sign(claims, fixedTestKey)`; both via `ws.AddGrant(sessionID, grantID)` with distinct GrantIDs (`grant-playwright-fixture-owner` and `grant-playwright-fixture-viewer`)
    - Fixture emits on stdout: `BASE_URL=`, `CAP=` (owner — backwards-compatible with existing specs), `VIEWER_CAP=`, `SESSION_CWD=` (absolute path of temp dir), `ADMIN_URL=`, then `READY=1`
    - Fixture cleans up the temp dir on SIGTERM (`defer os.RemoveAll(tempDir)`)
    - `frontend/e2e/fixture-env.ts` FixtureEnv interface gains `viewerCap: string` and `sessionCwd: string` fields; `global-setup.ts` parses two additional KEY=VALUE lines and writes them to the JSON
    - Seed tree contents (deterministic — Playwright assertions compare exact bytes/strings):
      - `hello.txt` → exactly `Hello, world!\n` (14 bytes)
      - `notes.md` → markdown body containing a GFM table AND a task list (for UI-07 verification; concrete content below)
      - `image.png` → 1×1 PNG, valid PNG magic bytes (8-byte sig + IHDR + IDAT + IEND); embed as Go `[]byte` literal in main.go and write at startup
      - `binary.bin` → 64 bytes of `bytes.Repeat([]byte{0x00, 0xFF}, 32)` (non-text, MIME cascade flags as binary)
      - `large.txt` → exactly `5*1024*1024 + 1` bytes (5 MiB + 1 → triggers 413 from /read per Phase 118 cap) filled with byte `'A'`
      - `empty.txt` → 0 bytes (triggers FS-07 short-circuit)
      - `subdir/nested.txt` → exactly `nested\n` (7 bytes) — for navigate-into-subdirectory scenario
  </behavior>
  <action>
    1. Create `cmd/playwright-fixture/testdata/files/` directory tree. Files `hello.txt`, `notes.md`, `subdir/nested.txt`, `empty.txt` checked in as static fixtures. `image.png`, `binary.bin`, `large.txt` are written at runtime from byte literals/loops in main.go (avoid committing a 5 MiB blob).

       `notes.md` exact content (anchored for Playwright assertions):
       ```
       # Notes

       | Feature | Status |
       | --- | --- |
       | Browse | Done |
       | Preview | Done |

       - [x] Render markdown
       - [ ] Edit markdown
       ```

    2. Modify `cmd/playwright-fixture/main.go`:
       a. Add imports: `"path/filepath"`, `"os"` (likely already imported — verify), `"github.com/scottkw/agenthub/internal/files"`. The PNG-bytes literal lives in a top-level `var onePixelPNG = []byte{ 0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a, /* IHDR */ 0x00, 0x00, 0x00, 0x0d, 0x49, 0x48, 0x44, 0x52, 0x00, 0x00, 0x00, 0x01, 0x00, 0x00, 0x00, 0x01, 0x08, 0x06, 0x00, 0x00, 0x00, 0x1f, 0x15, 0xc4, 0x89, /* IDAT */ 0x00, 0x00, 0x00, 0x0b, 0x49, 0x44, 0x41, 0x54, 0x78, 0x9c, 0x63, 0x00, 0x01, 0x00, 0x00, 0x05, 0x00, 0x01, 0x0d, 0x0a, 0x2d, 0xb4, /* IEND */ 0x00, 0x00, 0x00, 0x00, 0x49, 0x45, 0x4e, 0x44, 0xae, 0x42, 0x60, 0x82 }` (or any valid 1×1 PNG byte literal that `wailsapp/mimetype` detects as image/png).
       b. After L97 `_ = inputCaptureR` and BEFORE `webserver.NewWebServer`, build the session cwd:
          - `sessionCwd, err := os.MkdirTemp("", "agenthub-playwright-files-*")` — fatal on err
          - Copy/write the 4 static files from `cmd/playwright-fixture/testdata/files/` into `sessionCwd` using `os.ReadFile` + `os.WriteFile` (use `//go:embed testdata/files/**` if embed is preferred — but DO NOT use embed for `large.txt`/`binary.bin`/`image.png` which are generated)
          - Write generated files: `os.WriteFile(filepath.Join(sessionCwd, "image.png"), onePixelPNG, 0644)`, `os.WriteFile(filepath.Join(sessionCwd, "binary.bin"), bytes.Repeat([]byte{0x00, 0xFF}, 32), 0644)`, `os.WriteFile(filepath.Join(sessionCwd, "large.txt"), bytes.Repeat([]byte{'A'}, 5*1024*1024+1), 0644)`
          - `defer os.RemoveAll(sessionCwd)` immediately after the MkdirTemp success
       c. After `ws.SetSigningKey(fixedTestKey)` (currently L111) and BEFORE `ws.Start()`, add the files handler wire:
          ```go
          sandbox, err := files.NewSandbox(sessionCwd)
          if err != nil { log.Fatalf("files.NewSandbox: %v", err) }
          filesHandler := files.NewHandler(func(sid string) (*files.Sandbox, error) {
              if sid != sessionID {
                  return nil, fmt.Errorf("unknown session %q", sid)
              }
              return sandbox, nil
          })
          ws.SetFilesHandler(filesHandler)
          ```
          (Verify `files.NewSandbox` signature against `internal/files/sandbox.go`; if it differs, use the actual constructor name — the planner has confirmed Phase 118 ships `Sandbox` + `NewHandler(resolver)` but the exact sandbox constructor may be `OpenSandbox`/`Open` — executor uses whichever the package exports.)
       d. Change owner claim at L138 from `Perms: "read,write"` to `Perms: "read,write,files.read"` per CONTEXT D-01 (session owners get files.read by default).
       e. After L149 `ws.AddGrant(sessionID, claims.GrantID)`, mint a viewer cap:
          ```go
          viewerClaims := capability.Claims{
              SID:     sessionID,
              Perms:   "read",
              IAT:     time.Now().Unix(),
              GrantID: "grant-playwright-fixture-viewer",
              V:       1,
          }
          viewerToken, err := capability.Sign(viewerClaims, fixedTestKey)
          if err != nil { log.Fatalf("capability.Sign viewer: %v", err) }
          ws.AddGrant(sessionID, viewerClaims.GrantID)
          ```
       f. Modify stdout block at L198-201 to emit two additional lines:
          ```go
          fmt.Printf("VIEWER_CAP=%s\n", viewerToken)
          fmt.Printf("SESSION_CWD=%s\n", sessionCwd)
          ```
          BEFORE the final `READY=1` line.

    3. Modify `frontend/e2e/global-setup.ts`:
       - Add `let viewerCap = ''` and `let sessionCwd = ''` next to L65-67 declarations
       - Add parse cases at L72-74:
          ```ts
          if (line.startsWith('VIEWER_CAP=')) viewerCap = line.slice('VIEWER_CAP='.length).trim()
          if (line.startsWith('SESSION_CWD=')) sessionCwd = line.slice('SESSION_CWD='.length).trim()
          ```
       - Add to the FixtureEnv resolve at L77: `resolveEnv({ baseURL, cap, viewerCap, sessionCwd, adminURL, pid: fixtureProc!.pid! })`

    4. Modify `frontend/e2e/fixture-env.ts`:
       - Extend the FixtureEnv interface at L8-13 with `viewerCap: string` and `sessionCwd: string`
       - Add new exported helper `appUrl(env: FixtureEnv = loadFixtureEnv()): string` returning `${env.baseURL}/app/?session=playwright-test-session&cap=${encodeURIComponent(env.cap)}` (this is the URL Plan 05's e2e specs hit)
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build -tags=playwrightfixture -o /tmp/pwfixture-120 ./cmd/playwright-fixture && /tmp/pwfixture-120 &> /tmp/pwf.log & sleep 4 ; kill %1 2>/dev/null ; wait 2>/dev/null ; grep -c '^READY=1' /tmp/pwf.log | grep -q '^1$' && grep -c '^CAP=' /tmp/pwf.log | grep -q '^1$' && grep -c '^VIEWER_CAP=' /tmp/pwf.log | grep -q '^1$' && grep -c '^SESSION_CWD=' /tmp/pwf.log | grep -q '^1$' && echo PASS</automated>
  </verify>
  <done>
    - `go build -tags=playwrightfixture ./cmd/playwright-fixture` clean
    - Fixture stdout emits CAP, VIEWER_CAP, SESSION_CWD, ADMIN_URL, READY=1 in that order
    - `find cmd/playwright-fixture/testdata/files -type f | wc -l` ≥ 4 (static seeds: hello.txt, notes.md, empty.txt, subdir/nested.txt)
    - frontend/e2e/fixture-env.ts FixtureEnv interface has viewerCap + sessionCwd fields (verify via `grep -c 'viewerCap' frontend/e2e/fixture-env.ts` ≥ 2 and `grep -c 'sessionCwd' frontend/e2e/fixture-env.ts` ≥ 2)
    - Existing Phase 93+ specs (web-csp.spec.ts, web-vendor-parity.spec.ts, etc.) continue to read `env.cap` unchanged
    - `cd frontend && pnpm exec tsc --noEmit` clean (interface change preserves backward compat with existing specs)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→webserver (`/app/*`) | Untrusted client requests the bundle; cap gate is the auth check |
| Playwright fixture process→OS temp dir | Fixture writes seed files inside an isolated temp dir; cleanup on SIGTERM |
| Playwright fixture stdout→global-setup.ts | KEY=VALUE protocol; new keys VIEWER_CAP and SESSION_CWD must not break existing parsers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Information Disclosure | /app/ bundle | mitigate | requireCapability gate fires before bundle bytes are served; without a valid cap, 401 with no Body bytes leaked. Reuses Phase 119 verified middleware. |
| T-120-02 | Tampering | /app/{path...} traversal | mitigate | http.FileServer + http.FS rejects `..` paths via fs.ValidPath; covered by stdlib test surface |
| T-120-03 | Denial of Service | nil staticAppFS at request time | mitigate | 503 response (mirrors Phase 119 filesHandler nil-defense), no panic |
| T-120-04 | Elevation of Privilege | wrong route picks up cap | accept | requireCapability is the only gate added; no perm-bit changes; no Claims field reads |
| T-120-05 | Tampering | Playwright fixture seed-tree poisoning | accept | fixture is build-tagged `playwrightfixture`; never linked into release builds (existing T-93 invariant preserved) |
| T-120-06 | Information Disclosure | viewer cap leaks files.read | mitigate | Viewer cap Perms="read" only; HasPerm whole-token check (Phase 118 FS-10) rejects |
| T-120-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (Plan 02 handles react-markdown + remark-gfm with explicit slopcheck gate); pure Go + TypeScript edits |
</threat_model>

<verification>
- `go build ./...` clean
- `go test ./internal/webserver/ -count=1 -short` PASS (15 existing TestFilesRoutes_* + 6 new TestAppBundle_*)
- `go build -tags=playwrightfixture ./cmd/playwright-fixture` clean
- Fixture smoke run emits all five KEY=VALUE lines before READY=1
- `cd frontend && pnpm exec tsc --noEmit` clean (fixture-env.ts interface extension compiles)
- requireCapability and requireFilesRead bodies grep-equal to Phase 119 (Phase 118's `TestRequireCapability_UnchangedByPhase118` continues to pass)
</verification>

<success_criteria>
- New `GET /app/` route on webserver mounts `frontend/dist` (or a test FS) behind requireCapability + cspHeaders; nil-FS → 503; missing cap → 401; non-GET → 405
- Playwright fixture seeds 7 files (4 static, 3 generated) in an OS temp dir, mints owner+viewer caps, wires `*files.Handler` rooted at the seed dir, emits VIEWER_CAP + SESSION_CWD on stdout before READY=1
- frontend/e2e/fixture-env.ts FixtureEnv interface has viewerCap + sessionCwd; new `appUrl(env)` helper builds the Plan-05 e2e entry URL
- Existing Phase 119 routes + Phase 93+ Playwright specs continue to pass
</success_criteria>

<output>
Create `.planning/phases/120-filebrowsertab-tsx-desktop-web/120-01-SUMMARY.md` when done.
</output>
