---
phase: 122
plan: 05
type: execute
wave: 3
depends_on: ["122-01", "122-02", "122-03", "122-04"]
files_modified:
  - internal/integration/remote_files_parity_test.go
  - frontend/e2e/files-browser.spec.ts
  - frontend/e2e/global-setup.ts
  - frontend/e2e/fixtures/remote-peer-setup.ts
  - .planning/phases/122-remote-session-file-browse-wiring/122-VERIFICATION.md
autonomous: false
requirements: [REMOTE-05]
tags: [integration, e2e, cross-surface, parity, playwright, go]

user_setup: []

must_haves:
  truths:
    - "A single Go integration test exercises the daemon proxy + RemoteCapStore + RemoteFilesClient against a fixture remote webserver and observes identical file-list responses across both consumers."
    - "A Playwright e2e scenario exercises the desktop GUI's join-code modal end-to-end against a fixture remote peer and observes the same file list rendering FileBrowserTab as the local-session scenario."
    - "Source-grep guard verifies the 'File browser not available for remote sessions' toast is removed."
    - "All five REMOTE-XX requirements are listed in the traceability section of 122-VERIFICATION.md with passing test evidence."
  artifacts:
    - path: "internal/integration/remote_files_parity_test.go"
      provides: "Cross-surface Go parity test: spins up a fixture remote webserver, deposits a cap, drives the daemon proxy AND a RemoteFilesClient against the same upstream, asserts identical responses"
      contains: "TestRemoteFiles_CrossSurface_Parity"
    - path: "frontend/e2e/files-browser.spec.ts"
      provides: "Scenario 16: desktop GUI opens remote-session FileBrowserTab through the join-code modal flow"
      contains: "remote-session browse via owner cap"
    - path: ".planning/phases/122-remote-session-file-browse-wiring/122-VERIFICATION.md"
      provides: "Requirements traceability + manual UAT checklist + cross-surface evidence summary"
      contains: "REMOTE-01"
  key_links:
    - from: "remote_files_parity_test.go"
      to: "daemon proxy + RemoteFilesClient (Plans 01 + 04)"
      via: "shared httptest.NewTLSServer fixture serving as 'remote peer'"
      pattern: "httptest.NewTLSServer"
    - from: "files-browser.spec.ts scenario 16"
      to: "App.tsx remote tab gate (Plan 03)"
      via: "Playwright drives the RemoteJoinCodeModal flow with a mock-Wails ExchangeJoinCodeAtURL"
      pattern: "browse-files-button"
---

<objective>
Land the cross-surface integration evidence that proves REMOTE-05 (parity between desktop GUI, TUI, and web). Three deliverables: (1) a Go integration test that drives both the daemon proxy AND the TUI's RemoteFilesClient against the same fixture remote webserver and asserts byte-identical responses, (2) a Playwright e2e scenario that exercises the desktop GUI's join-code modal end-to-end, (3) a 122-VERIFICATION.md compiling the full requirements traceability.

Purpose: REMOTE-05 is the codified version of the project's "cross-surface parity is release-blocking" memory. This plan is the merge gate that proves all three surfaces (GUI / TUI / web — the web surface is the existing Phase 120-06 Playwright scenarios 13+14 which are unaffected by Phase 122) deliver the same observable behavior when browsing a remote session's files.

Output: One new Go integration test file; modifications to the existing Playwright spec; a new Playwright fixture for the "remote peer"; the verification doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/122-remote-session-file-browse-wiring/122-CONTEXT.md
@.planning/phases/122-remote-session-file-browse-wiring/122-RESEARCH.md
@.planning/phases/122-remote-session-file-browse-wiring/122-01-PLAN.md
@.planning/phases/122-remote-session-file-browse-wiring/122-02-PLAN.md
@.planning/phases/122-remote-session-file-browse-wiring/122-03-PLAN.md
@.planning/phases/122-remote-session-file-browse-wiring/122-04-PLAN.md
@.planning/phases/120-filebrowsertab-tsx-desktop-web/120-06-SUMMARY.md

@frontend/e2e/files-browser.spec.ts
@frontend/e2e/global-setup.ts
@frontend/e2e/fixture-env.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-surface Go parity test — daemon proxy + RemoteFilesClient against same fixture</name>
  <files>internal/integration/remote_files_parity_test.go</files>
  <read_first>
    @.planning/phases/122-remote-session-file-browse-wiring/122-01-PLAN.md (Plan 01 test approach with httptest.NewTLSServer — we re-use the same fixture shape)
    @internal/files/types.go (FileEntry + FileListResponse — the response shape both surfaces must observe identically)
    @internal/daemon/api.go (how to instantiate an API for testing — find existing test setup helpers like `newTestAPI` or similar; if none exists, this plan creates a minimal one)
    Look for existing integration test infrastructure: `ls internal/integration/ 2>/dev/null || find internal/ -name '*integration*' -type f`
  </read_first>
  <behavior>
    - Test `TestRemoteFiles_CrossSurface_Parity`:
      1. Spin up a fixture "remote peer" via `httptest.NewTLSServer` that serves:
         - `POST /join/exchange` → 303 to `/sessions/{sid}?cap=FIXTURE_CAP`
         - `GET /api/files/list?session={sid}&path={p}&cap=FIXTURE_CAP` → 200 with a canned FileListResponse containing two entries: `{Name: "a.txt", Size: 100, IsDir: false}` and `{Name: "sub", Size: 0, IsDir: true}`
         - `GET /api/files/stat?session={sid}&path=a.txt&cap=FIXTURE_CAP` → 200 with the matching FileEntry
         - `GET /api/files/read?session={sid}&path=a.txt&cap=FIXTURE_CAP` → 200 with body "hello world" and Content-Type "text/plain"
         - 401 on any of the above when the cap query param is missing or doesn't match FIXTURE_CAP
      2. Spin up a local daemon API (or the minimal subset needed for the proxy + cap store), point it at a UnixSocket / in-memory HTTPServer. Inject the fixture's `srv.Client()` cert into the daemon's outbound HTTPS transport so it trusts the test cert.
      3. Deposit the cap via `POST /api/remote-files/caps` body `{sessionId: "sid1", baseUrl: srv.URL, capToken: "FIXTURE_CAP"}` → assert 204.
      4. Construct a direct `tui.RemoteFilesClient(srv.URL, "FIXTURE_CAP")` (using the same `srv.Client()` cert injection via the test-only constructor exposed in Plan 04 Task 1).
      5. Fetch the same operations through BOTH surfaces:
         - Daemon proxy: `GET {daemonURL}/api/files/remote/sid1/list?path=.` → parse FileListResponse.
         - RemoteFilesClient: `client.ListFiles(ctx, "sid1", ".")` → returns ([]FileEntry, bool, error).
      6. Assert the two response bodies are byte-identical for list / stat / read.
      7. Assert that with NO cap deposited (drop the deposit step OR call `DELETE /api/remote-files/caps?sid=sid1` if such an endpoint exists; if not, use a fresh daemon instance), the daemon proxy returns 401 with the marker `"remote session not joined"` while the direct RemoteFilesClient (still has the cap) returns 200. (This proves the asymmetry: the proxy gate is local-daemon-side; the TUI client carries the cap directly.)
  </behavior>
  <action>
    Create the file `internal/integration/remote_files_parity_test.go` (create the directory if it doesn't exist). Test design notes:

    1. **Daemon setup**: Use `internal/daemon` package's existing test helpers if available (grep `internal/daemon/api_test.go` for patterns — `func newTestAPI(t *testing.T) *API` or similar). If no helper exists, construct the minimal API needed:
       ```
       api := &daemon.API{...}  // with remoteCaps populated; mux registered via registerRoutes
       ```
       Note: `internal/daemon` is package-private; if integration tests need to reach into unexported fields, place this test file IN the `daemon` package (rename to `internal/daemon/remote_files_parity_test.go`) OR add a small exported test-only constructor `daemon.NewAPIForTest()`.

       DECISION: Place the test in `internal/daemon/` package and name the file `remote_files_parity_test.go`. Justification: the test exercises both the daemon proxy AND a TUI client; importing `internal/tui` from `internal/daemon` is not a cycle (daemon doesn't import tui). The reverse — placing in `internal/tui` — WOULD be a cycle. The `internal/integration/` package is NOT actually established in the project per the read_first grep; we don't introduce one for one test.

    2. **TLS cert injection**: Both the daemon's outbound transport AND the RemoteFilesClient need to trust the test's self-signed cert. Two options:
       - (a) Use `srv.Client().Transport` for both. Requires exposing the transport on both consumers via a test-only constructor.
       - (b) Use `srv.Certificate()` to construct a `*x509.CertPool` and pass it via `tls.Config{RootCAs: pool}`. Requires similar test-only knobs.

       Use (a). Plan 04 Task 1 already added a test-only constructor `newRemoteFilesClientWithHTTP(baseURL, cap, *http.Client)`; do the same for the daemon's outbound (add `daemon.newRemoteFilesClientForTest(...)` accepting the test's `*http.Client` and assigning it to the proxy's package-internal transport).

    3. **Cap-deposit shortcut**: Rather than spinning up a full daemon socket, call `api.handleRegisterRemoteCap` directly with a `httptest.NewRecorder` + crafted `*http.Request`. Likewise for the proxy: invoke `api.handleRemoteFilesList` directly. This is more brittle than going through `httptest.NewServer(api.mux)` but avoids the multi-process orchestration. PREFERRED: go the full `httptest.NewServer(api.mux)` route — it's more honest about what the test is proving.

    4. **Cap leakage assertion** (defense-in-depth — restates Plan 01 Task 2 Test 8 in a cross-surface context): After running all the parity round-trips, source-grep the test's captured log output and any error strings for `"FIXTURE_CAP"`. The cap MUST appear in the URL query strings (the test's wire-level expectation) but MUST NOT appear in any returned `error.Error()` string.
  </action>
  <verify>
    <automated>go test ./internal/daemon/ -run TestRemoteFiles_CrossSurface_Parity -race -count=1 -v</automated>
  </verify>
  <done>
    `TestRemoteFiles_CrossSurface_Parity` exists and passes; the test exercises daemon proxy + RemoteFilesClient against a single fixture; byte-identical response assertion passes for list / stat / read; cap-absence asymmetry assertion passes; cap-leakage source-grep is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Playwright e2e scenario — desktop GUI remote-session browse via join-code modal</name>
  <files>frontend/e2e/files-browser.spec.ts, frontend/e2e/global-setup.ts, frontend/e2e/fixtures/remote-peer-setup.ts, cmd/playwright-fixture/main.go</files>
  <read_first>
    @frontend/e2e/files-browser.spec.ts (scenarios 13 + 14 — existing remote-mode DOM cells; we add scenario 16 mirroring the pattern)
    @.planning/phases/120-filebrowsertab-tsx-desktop-web/120-06-SUMMARY.md (the playwright fixture embed pattern + global-setup dist-copy strategy)
    @cmd/playwright-fixture/main.go (the fixture's main — we extend it with a second port mocking a "remote peer")
  </read_first>
  <behavior>
    - Scenario 16: "remote-session browse via owner cap (join-code modal flow)"
      1. Setup: the global-setup spawns the existing playwright fixture (Machine B simulation) AND a second small process (or in-process additional HTTP server) on a known port acting as the "remote peer" (Machine A simulation) — serves `/api/sessions`, `/join/exchange`, `/api/files/{list,stat,read}` with the canned shape from Task 1's fixture.
      2. The fixture's `GetRemoteSessions` Wails binding is mocked (via Playwright's `route.fulfill` API) to return the remote peer at the known URL.
      3. Playwright opens the desktop GUI in the fixture, navigates to the Remote Sessions panel.
      4. Asserts the "Browse files" button is visible next to "Open Session" for the mocked remote session.
      5. Clicks "Browse files". Asserts the RemoteJoinCodeModal appears.
      6. Mocks the Wails `ExchangeJoinCodeAtURL` binding to return "FIXTURE_CAP" on a join code of "ABCDE", and the `RegisterRemoteCap` binding to return success.
      7. Types "ABCDE" into the modal, clicks Submit, asserts the modal closes.
      8. Asserts a FileBrowserTab opens for the remote session.
      9. Asserts the file list shows the canned entries (a.txt, sub).
      10. Asserts the network request to fetch the file list went to `http://127.0.0.1:{relayPort}/api/files/remote/{sid}/list` (same-origin proxy), NOT directly to the "remote peer" URL.

    - Scenario 17: "remote-session browse — 401 from upstream → EnableWebSharingTakeover"
      1. Set up as scenario 16 but the mock "remote peer" returns 401 on /api/files/list.
      2. After modal submit, FileBrowserTab opens but the list fetch fails 401.
      3. Asserts `EnableWebSharingTakeover` renders with the verbatim copy "Remote session must be web-shared to browse files. Ask the owner to enable sharing."
      4. Asserts the "Re-enter join code" button is visible and re-opens the modal when clicked.
  </behavior>
  <action>
    ### Fixture extension

    Extend `cmd/playwright-fixture/main.go` to optionally spin up a second HTTPServer (gated by an env var like `FIXTURE_REMOTE_PEER_PORT`) serving the "remote peer" shape:
    - `GET /api/sessions` → JSON list with one canned session (id=peer-sid, name="Mocked Peer Session")
    - `POST /join/exchange` → 303 to `/sessions/peer-sid?cap=FIXTURE_CAP` if body `code=ABCDE`; else 303 to `/join?error=invalid`
    - `GET /api/files/list?session=peer-sid&path=.&cap=FIXTURE_CAP` → 200 canned response
    - `GET /api/files/list?session=peer-sid&path=.` (no cap) → 401
    - `GET /api/files/stat`, `/read` → canned 200 responses with FIXTURE_CAP

    Add a new test helper `frontend/e2e/fixtures/remote-peer-setup.ts` that:
    - Reads the FIXTURE_REMOTE_PEER_PORT env var (set by global-setup)
    - Exposes a `remotePeerURL()` helper for tests to reference

    ### global-setup.ts extension

    Extend the existing setup to:
    - Allocate a second free port for the remote peer
    - Set `FIXTURE_REMOTE_PEER_PORT` env var before spawning the fixture process
    - Wait for both servers to be ready

    ### files-browser.spec.ts additions

    Add scenarios 16 and 17 following the existing scenario-13/14 pattern (mode-aware tab gate). Use `page.route(...)` to mock the Wails bindings:
    ```
    await page.route('**/wails/**', (route) => {
      // Intercept and respond per the test's mock contract
    })
    ```

    Or, if the playwright fixture's main.go has a binding-mock layer (per Phase 120-06 setup), reuse that.

    CRITICAL: Per Pitfall 1, the test MUST assert via `page.on('request', ...)` that the file-list fetch goes to `http://127.0.0.1:{relayPort}/api/files/remote/...` and NOT directly to the mocked remote peer URL. Use Playwright's network observation API to capture all requests during the scenario and assert no request URL contains the remote-peer hostname after the initial join-code exchange (which DOES legitimately go to the remote peer URL).
  </action>
  <verify>
    <automated>pnpm --filter frontend exec playwright test --grep "remote-session" -- --reporter=list</automated>
  </verify>
  <done>
    Scenarios 16 and 17 pass in chromium + firefox + webkit (the existing 3-browser matrix); the network-request assertion catches any accidental direct cross-origin fetch; pre-existing scenarios (1-15) still pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual cross-surface UAT — actual two-machine setup</name>
  <what-built>
    Phase 122 wires desktop GUI + TUI for remote-session file browsing via paste-join-code modal + daemon proxy (GUI) / direct HTTPS+cap (TUI). All three surfaces (GUI, TUI, web — web from Phase 120-06) now deliver parity.
  </what-built>
  <how-to-verify>
    REAL two-machine UAT — automated tests can only fixture-simulate the remote peer; this step proves the actual tailnet path works. Use any two machines on the user's tailnet (e.g., a Mac + a Linux box, or two Macs). Per project memory "Bite-sized runbook chunks" — execute one numbered step at a time, then await the next chunk.

    Machine A (host):
    1. Launch agenthub. Create a session running a shell or AI CLI in a directory with a few visible files (use `/tmp/parity-uat-{date}` with `echo hi > a.txt; mkdir sub; echo nested > sub/b.txt`).
    2. Enable web-share for that session via the Daemon Manager panel.
    3. Copy the 5-character join code from the panel.

    Machine B (desktop GUI consumer):
    4. Launch agenthub (Wails desktop build with `-tags wailsassets` per project memory).
    5. Open the Remote Sessions panel. Confirm Machine A's session appears with a "Browse files" button.
    6. Click "Browse files". Modal appears.
    7. Paste the join code from step 3. Click Submit.
    8. Confirm FileBrowserTab opens showing `a.txt` and `sub/`.
    9. Click `sub/`. Confirm `b.txt` appears.
    10. Click `b.txt`. Confirm preview pane shows "nested".

    Machine B (TUI consumer — same machine OR a third tailnet machine):
    11. Launch the TUI: `agenthub tui`.
    12. Navigate to Machine A's remote session in the Sessions list.
    13. Press `f`. Confirm the join-code prompt modal appears (NOT the "File browser not available" toast from v3.4).
    14. Type the join code from step 3 (or generate a fresh one on Machine A — codes are single-use; if step 7 already consumed the code, generate a new code from Machine A's Daemon Manager first).
    15. Press Enter. Confirm tabFiles opens showing the same `a.txt` and `sub/` from Machine A.
    16. Press Down to highlight `a.txt`. Confirm preview pane shows "hi".
    17. Press `/` and type `sub`. Confirm filter shows only `sub/`.
    18. Press Esc. Press Backspace at the root. Confirm it's a no-op (cwd protection from Phase 121 TUI-03 still works for remote).

    Cross-surface parity check (REMOTE-05):
    19. Open a regular browser (NOT Wails) on Machine B. Navigate to `https://{machine-a-fqdn}:{port}/join`. Enter the join code (or use a fresh one). Confirm the web file browser opens against Machine A and shows the same `a.txt` + `sub/`. (This is the Phase 120-06 web-share path — should be unaffected by Phase 122, but proving it ALL works confirms REMOTE-05.)

    Failure-mode UAT:
    20. On Machine A, disable web-share for the session.
    21. On Machine B desktop GUI: click "Browse files" on the (now stale) session. Confirm `EnableWebSharingTakeover` renders with the verbatim copy "Remote session must be web-shared to browse files. Ask the owner to enable sharing." plus a "Re-enter join code" button.
    22. On Machine B TUI: press `f` on the (stale) session. Confirm the status line shows the same verbatim copy (per Plan 04's 401-handling).

    Sign off each step with PASS / FAIL. Any FAIL is a release-blocking parity gap (per project memory "Cross-surface parity is release-blocking").
  </how-to-verify>
  <resume-signal>Reply with the step number of any FAIL and a short description; otherwise reply "all 22 steps PASS".</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Phase 122 VERIFICATION.md — requirements traceability + evidence summary</name>
  <files>.planning/phases/122-remote-session-file-browse-wiring/122-VERIFICATION.md</files>
  <read_first>
    @.planning/phases/121-tui-files-view/121-02-SUMMARY.md (for the VERIFICATION.md style precedent)
    Any prior `*-VERIFICATION.md` in `.planning/phases/` — look for the established format.
  </read_first>
  <behavior>
    - The doc lists all five REMOTE-XX requirements with explicit evidence (test name + file path).
    - The doc lists the Task 3 manual UAT results.
    - The doc lists the source-grep verification gates with their actual counts.
    - The doc lists every Plan 01-05 SUMMARY.md it depends on.
  </behavior>
  <action>
    Create `.planning/phases/122-remote-session-file-browse-wiring/122-VERIFICATION.md` with the following sections:

    1. **Requirements Traceability** — table mapping REMOTE-01..05 to test evidence:
       | Req | Plans | Tests | Status |
       | REMOTE-01 | 01, 03 | TestRemoteFiles_CrossSurface_Parity + Playwright scenario 16 | PASS |
       | ... |

    2. **Source-grep gate results** — actual counts from running every grep gate in Plans 01-05.

    3. **Manual UAT log** — copy the Task 3 sign-off from the operator.

    4. **Deferred items** — anything caught during execution that the operator deferred to a follow-on phase.

    5. **Sign-off** — date + git SHA of the merge commit.
  </action>
  <verify>
    <automated>test -f .planning/phases/122-remote-session-file-browse-wiring/122-VERIFICATION.md && grep -c REMOTE-01 .planning/phases/122-remote-session-file-browse-wiring/122-VERIFICATION.md</automated>
  </verify>
  <done>
    VERIFICATION.md exists with all five REMOTE-XX traceability rows + manual UAT log copied verbatim from Task 3 + grep-gate evidence table.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Plan 05 is verification-only and introduces no new runtime trust boundaries. Test fixtures use self-signed certs scoped to httptest.NewTLSServer.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-05-01 | Information disclosure | Test artifact leaks cap token | mitigate | Task 1 explicitly grep-asserts cap-token absence in error strings. Test cap value is the literal `"FIXTURE_CAP"` (not a real cap), so leakage in test logs is harmless — but the assertion enforces the engineering discipline. |
| T-122-05-02 | Tampering | Playwright fixture spoofs the local-daemon proxy and reports false PASS | accept | The Network-request assertion in scenarios 16+17 explicitly checks the request URL is the LOCAL proxy, not the remote peer. If the fixture itself spoofs the proxy, the test is moot — but that's a property of the test harness, not the production code. |
| T-122-05-SC | Tampering | No new packages | mitigate | Zero new dependencies — uses existing Go stdlib + existing Playwright + existing project test infrastructure. |
</threat_model>

<verification>
## Per-task automated

```
go test ./internal/daemon/ -run TestRemoteFiles_CrossSurface_Parity -race -count=1
pnpm --filter frontend exec playwright test --grep "remote-session"
test -f .planning/phases/122-remote-session-file-browse-wiring/122-VERIFICATION.md
```

## Whole-plan grep gates

| Check | Expected | Command |
|-------|----------|---------|
| Cross-surface test exists | 1 | `grep -c 'TestRemoteFiles_CrossSurface_Parity' internal/daemon/remote_files_parity_test.go` |
| Playwright scenarios 16+17 exist | 2 | `grep -cE "(scenario 16\|scenario 17)" frontend/e2e/files-browser.spec.ts` |
| VERIFICATION.md exists with all five REMOTE-XX | 5 | `grep -cE 'REMOTE-0[1-5]' .planning/phases/122-remote-session-file-browse-wiring/122-VERIFICATION.md` |

## Regression (full release gate)

```
go test ./... -race -count=1
pnpm --filter frontend test -- --run
pnpm --filter frontend exec tsc --noEmit
pnpm --filter frontend exec playwright test
go build -tags wailsassets ./...
```

ALL must pass. Phase 118-121 tests must remain 100% green.
</verification>

<success_criteria>
1. `TestRemoteFiles_CrossSurface_Parity` exists and proves daemon proxy + RemoteFilesClient deliver byte-identical responses against a single fixture remote webserver.
2. Playwright scenarios 16 (success path) and 17 (401-takeover path) pass on chromium + firefox + webkit.
3. Network-request assertion in scenarios 16+17 catches any accidental direct cross-origin fetch (Pitfall 1 enforcement).
4. Manual two-machine UAT (Task 3) signs off all 22 steps.
5. `122-VERIFICATION.md` exists with full REMOTE-01..05 traceability + manual UAT log + grep-gate evidence.
6. Full release gate green: `go test ./... -race` + `pnpm test` + `pnpm tsc --noEmit` + `pnpm exec playwright test` + `go build -tags wailsassets ./...`.
</success_criteria>

<output>
Create `.planning/phases/122-remote-session-file-browse-wiring/122-05-SUMMARY.md` when done.
</output>
