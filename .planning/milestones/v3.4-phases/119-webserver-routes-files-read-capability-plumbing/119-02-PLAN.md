---
phase: 119-webserver-routes-files-read-capability-plumbing
plan: 02
type: execute
wave: 2
depends_on: ["119-01"]
files_modified:
  - internal/webserver/files_routes_test.go
  - internal/webserver/capability_mw.go
  - internal/files/handler.go
autonomous: true
requirements: [WEB-05]
tags: [webserver, csp, security, docstring, web-05]

must_haves:
  truths:
    - "GET /api/files/list responds with Content-Type starting 'application/json' — never 'text/html'."
    - "GET /api/files/read responds with no Content-Security-Policy header (CSP is HTML-only; JSON/octet-stream routes do not go through cspHeaders middleware)."
    - "Existing CSP e2e tests (TestBrowserCSP_TerminalNoViolations, TestBrowserCSP_DashboardNoViolations, TestBrowserCSP_JoinNoViolations) still pass after Phase 119 mounts the file routes — i.e. Phase 119 introduces no CSP regression on HTML-serving pages."
    - "Phase 118 docstrings that incorrectly reference 'SetFilesHandlerProvider' are updated to refer to 'SetFilesHandler' (the actual API)."
  artifacts:
    - path: "internal/webserver/files_routes_test.go"
      provides: "Two new defense-in-depth subtests asserting no CSP header and no HTML Content-Type on file routes"
      contains: "TestFilesRoutes_NoCSPHeader"
    - path: "internal/webserver/capability_mw.go"
      provides: "Docstring on requireFilesRead updated: 'SetFilesHandlerProvider' → 'SetFilesHandler'"
      contains: "SetFilesHandler"
    - path: "internal/files/handler.go"
      provides: "Docstring updated: 'SetFilesHandlerProvider' → 'SetFilesHandler'"
      contains: "SetFilesHandler"
  key_links:
    - from: "internal/webserver/files_routes_test.go::TestFilesRoutes_NoCSPHeader"
      to: "the mounted GET /api/files/list route"
      via: "asserts resp.Header.Get(\"Content-Security-Policy\") == \"\" — regression guard against future mistakenly wrapping file routes in cspHeaders"
      pattern: "Content-Security-Policy"
    - from: "internal/webserver/files_routes_test.go::TestFilesRoutes_NoHTMLContentType"
      to: "the mounted GET /api/files/list route"
      via: "asserts !strings.HasPrefix(Content-Type, \"text/html\") — regression guard against accidental HTML serving"
      pattern: "text/html"
---

<objective>
Cover WEB-05 ("zero new CSP amendments / zero new CSP violations") with defense-in-depth unit assertions plus a re-run of the existing CSP e2e suite, and update two Phase 118 docstrings that reference the no-longer-planned `SetFilesHandlerProvider` symbol name.

Purpose: Phase 118's docstrings (`capability_mw.go:99` + `handler.go:6`) reference "SetFilesHandlerProvider" — a name that was simplified to `SetFilesHandler` during Phase 119 plan-phase research (since the Handler is stateless and passing the constructed instance directly is cleaner than passing a constructor closure). Those comments must be updated so future readers find the correct symbol. For WEB-05: file routes do NOT go through `cspHeaders` middleware (only HTML routes do), so the "zero new violations" gate is really about preventing two regressions — (a) accidentally serving HTML from a file route, (b) accidentally wrapping a file route in cspHeaders. Two unit assertions cover both. The "complete file browse flow" Playwright criterion in the ROADMAP cannot fire in Phase 119 because no file-browser UI exists yet (Phase 120 deliverable) — confirmed by RESEARCH.md §CSP Impact Analysis.

Output: 2 new subtests in `files_routes_test.go` (~30 lines total), 2 docstring edits totaling ~4 lines of diff. Existing CSP e2e suite re-run as a sanity check (no code changes there).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/119-webserver-routes-files-read-capability-plumbing/119-CONTEXT.md
@.planning/phases/119-webserver-routes-files-read-capability-plumbing/119-RESEARCH.md
@.planning/phases/119-webserver-routes-files-read-capability-plumbing/119-01-PLAN.md
@internal/webserver/capability_mw.go
@internal/files/handler.go
@internal/webserver/csp_mw.go
@internal/webserver/browser_csp_e2e_test.go

<interfaces>
<!-- Key references the executor needs. -->

From internal/webserver/csp_mw.go (DO NOT MODIFY):
```
// cspHeaders is mounted only on HTML routes (/dashboard, /join, /sessions/{id}).
// JSON / octet-stream routes (/api/sessions, /api/files/*, etc.) do NOT go
// through this middleware — adding CSP headers to JSON responses adds zero
// security and confuses CSP measurements.
```

From internal/webserver/browser_csp_e2e_test.go (DO NOT MODIFY):
```
// Build tag: -tags=e2e
// Tests: TestBrowserCSP_TerminalNoViolations, _DashboardNoViolations, _JoinNoViolations
// Self-skip if chromedp binary unavailable — that's the documented fallback.
```

From internal/webserver/capability_mw.go (Phase 118, lines 98-101 — docstring to update):
```
// MOUNT TIMING: defined in Phase 118 but not yet mounted on any route. Phase
// 119 attaches it to /api/files/list, /stat, /read via SetFilesHandlerProvider.
//                                                       ^^^^^^^^^^^^^^^^^^^^^^^^
//                                                       UPDATE: SetFilesHandler
```

From internal/files/handler.go (Phase 118, line ~6 — docstring to update):
```
// Phase 119 mounts these on the webserver via SetFilesHandlerProvider (...).
//                                              ^^^^^^^^^^^^^^^^^^^^^^^^
//                                              UPDATE: SetFilesHandler
// Read the actual file to confirm exact wording and surrounding context.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add defense-in-depth CSP/Content-Type assertions for /api/files/* routes</name>
  <files>internal/webserver/files_routes_test.go</files>
  <read_first>
    - internal/webserver/files_routes_test.go (created in Plan 01 — read the existing `newFilesTestServer` harness to reuse it)
    - internal/webserver/csp_mw.go (confirm cspHeaders is only mounted on HTML routes — NOT on /api/files/*)
    - .planning/phases/119-webserver-routes-files-read-capability-plumbing/119-RESEARCH.md (§"CSP Impact Analysis" — confirms the JSON-route CSP non-coupling)
  </read_first>
  <behavior>
    Append two new subtests (or top-level test functions) to `internal/webserver/files_routes_test.go`:

    | Subtest | What it asserts |
    |---|---|
    | `TestFilesRoutes_NoCSPHeader` | `GET /api/files/list?...&cap=<owner-token>` returns 200 AND `resp.Header.Get("Content-Security-Policy") == ""`. Regression guard: if a future change accidentally wraps the file routes in `cspHeaders`, this test fires immediately. |
    | `TestFilesRoutes_NoHTMLContentType` | `GET /api/files/list?...&cap=<owner-token>` returns 200 AND `resp.Header.Get("Content-Type")` starts with `"application/json"` (never `"text/html"`). Regression guard against any future change that serves an HTML error page from a file route. |
  </behavior>
  <action>
    Append two test functions to `internal/webserver/files_routes_test.go` reusing the `newFilesTestServer(t)` harness from Plan 01:

    ```
    // TestFilesRoutes_NoCSPHeader is a WEB-05 defense-in-depth regression guard.
    // The CSP middleware (csp_mw.go) is intentionally mounted only on HTML
    // routes — /api/files/* serves JSON/octet-stream and does not need (and
    // must not have) CSP headers. If a future maintainer accidentally wraps
    // these routes in cspHeaders, this test fires.
    func TestFilesRoutes_NoCSPHeader(t *testing.T) {
        ws, client, sid, _ := newFilesTestServer(t)
        token := issueCapFor(t, ws, sid, "read,write,files.read")
        resp, err := client.Get(ws.BaseURL() + "/api/files/list?session=" + sid + "&path=.&cap=" + token)
        if err != nil { t.Fatalf("GET /api/files/list: %v", err) }
        defer resp.Body.Close()
        if resp.StatusCode != http.StatusOK { t.Fatalf("expected 200, got %d", resp.StatusCode) }
        if got := resp.Header.Get("Content-Security-Policy"); got != "" {
            t.Errorf("unexpected CSP header on JSON route: %q", got)
        }
    }

    // TestFilesRoutes_NoHTMLContentType is a WEB-05 defense-in-depth regression
    // guard. File routes MUST return JSON (List/Stat) or the resolved file MIME
    // (Read) — never text/html. If a future change accidentally serves an HTML
    // error page from a file route, the WEB-05 promise ("file browser flow
    // adds no CSP surface") breaks silently. This test catches that at the
    // package-test layer without needing a browser.
    func TestFilesRoutes_NoHTMLContentType(t *testing.T) {
        ws, client, sid, _ := newFilesTestServer(t)
        token := issueCapFor(t, ws, sid, "read,write,files.read")
        resp, err := client.Get(ws.BaseURL() + "/api/files/list?session=" + sid + "&path=.&cap=" + token)
        if err != nil { t.Fatalf("GET /api/files/list: %v", err) }
        defer resp.Body.Close()
        if resp.StatusCode != http.StatusOK { t.Fatalf("expected 200, got %d", resp.StatusCode) }
        ct := resp.Header.Get("Content-Type")
        if !strings.HasPrefix(ct, "application/json") {
            t.Errorf("expected Content-Type to start with application/json, got %q", ct)
        }
    }
    ```

    Imports should already include `"net/http"` and `"strings"` from Plan 01 — no new import additions expected.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -run '^TestFilesRoutes_No(CSPHeader|HTMLContentType)$' ./internal/webserver/ -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test -run '^TestFilesRoutes_NoCSPHeader$' ./internal/webserver/ -count=1` exits 0 with PASS
    - `go test -run '^TestFilesRoutes_NoHTMLContentType$' ./internal/webserver/ -count=1` exits 0 with PASS
    - `grep -c '^func TestFilesRoutes_NoCSPHeader' internal/webserver/files_routes_test.go` returns 1
    - `grep -c '^func TestFilesRoutes_NoHTMLContentType' internal/webserver/files_routes_test.go` returns 1
    - `grep -c 'Content-Security-Policy' internal/webserver/files_routes_test.go` returns ≥1
    - `grep -c 'application/json' internal/webserver/files_routes_test.go` returns ≥1
    - Full webserver suite green: `go test ./internal/webserver/ -count=1 -short` exits 0
  </acceptance_criteria>
  <done>
    Two new subtests added to files_routes_test.go covering the WEB-05 defense-in-depth invariants (no CSP header, no text/html content type on file routes). Both pass. Full webserver suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Phase 118 docstrings referring to SetFilesHandlerProvider → SetFilesHandler</name>
  <files>internal/webserver/capability_mw.go, internal/files/handler.go</files>
  <read_first>
    - internal/webserver/capability_mw.go (line 99 area — the docstring on requireFilesRead's MOUNT TIMING section)
    - internal/files/handler.go (line 6 area — the file-level docstring mentioning the webserver mount path)
  </read_first>
  <behavior>
    Both docstrings refer to `SetFilesHandlerProvider` (the originally-proposed name). Plan 119-01 implemented `SetFilesHandler` (no "Provider" suffix). Update each docstring to reference the actual symbol so grep-driven readers find a real method.
  </behavior>
  <action>
    1. **`internal/webserver/capability_mw.go`**: In the `requireFilesRead` docstring (around line 98-101), change the text "via SetFilesHandlerProvider" to "via SetFilesHandler". Other surrounding text (which routes, when, why) is correct and stays unchanged.

    2. **`internal/files/handler.go`**: In the file-level or `Handler`-level docstring (around line 6 — read the file to find the exact line), change the text "SetFilesHandlerProvider" to "SetFilesHandler". Other surrounding text stays unchanged.

    Do NOT change any function signatures, types, or test code in either file. These are pure comment edits.

    Do NOT rename anything else, even if the docstrings reference other names — only update the literal "SetFilesHandlerProvider" → "SetFilesHandler".
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && grep -rn 'SetFilesHandlerProvider' internal/ ; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'SetFilesHandlerProvider' internal/` returns NO matches (exit 1 from grep — the absence of matches IS the success signal)
    - `grep -c 'SetFilesHandler' internal/webserver/capability_mw.go` returns ≥1 (the updated reference)
    - `grep -c 'SetFilesHandler' internal/files/handler.go` returns ≥1 (the updated reference)
    - `go build ./internal/webserver/ ./internal/files/` exits 0 (the edits are pure comments; build must still pass)
    - `go vet ./internal/webserver/ ./internal/files/` reports no diagnostics
    - Cross-package test suite still green: `go test ./internal/files/ ./internal/capability/ ./internal/webserver/ ./internal/daemon/ -count=1 -short` exits 0
  </acceptance_criteria>
  <done>
    Both Phase 118 docstrings now refer to the actual SetFilesHandler symbol. No production code touched. All four affected packages still build and test clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-run existing CSP e2e suite to confirm zero regressions (sanity gate, no code changes)</name>
  <files></files>
  <read_first>
    - internal/webserver/browser_csp_e2e_test.go (confirm the build tag is `-tags=e2e` and the self-skip-on-no-chromedp path exists)
    - .planning/phases/119-webserver-routes-files-read-capability-plumbing/119-RESEARCH.md (§"Environment Availability" — chromedp v0.15.1 in go.mod, may self-skip)
  </read_first>
  <behavior>
    Run the existing chromedp-based CSP e2e tests under the `-tags=e2e` build tag. The three relevant tests (`TestBrowserCSP_TerminalNoViolations`, `TestBrowserCSP_DashboardNoViolations`, `TestBrowserCSP_JoinNoViolations`) navigate to /sessions/{id}, /dashboard, /join respectively — Phase 119 does NOT modify any of these HTML pages, so all three tests MUST still report zero CSP violations.

    If chromedp self-skips (no Chromium binary on the executor's machine), record the skip explicitly in the SUMMARY rather than treating it as a pass. Per RESEARCH.md §Environment Availability the self-skip is acceptable and documented behavior.
  </behavior>
  <action>
    Execute:

    ```
    cd /Users/ken/dev/agenthub
    go test -tags=e2e -run '^TestBrowserCSP_' ./internal/webserver/ -count=1 -v 2>&1 | tee /tmp/119-csp-e2e.log
    ```

    Three possible outcomes:

    1. **All three tests PASS** — record in SUMMARY: "Existing CSP e2e suite passes unchanged; Phase 119 introduces no CSP regression on /dashboard, /join, /sessions/{id}." Done.

    2. **All three tests SKIP** (chromedp self-skip path fires) — record in SUMMARY: "chromedp/Chromium unavailable on executor; CSP e2e suite self-skipped per browser_csp_e2e_test.go:71-73 documented fallback. WEB-05 unit-level coverage (Plan 01 Task 2 + Plan 02 Task 1) still satisfied." Done. Operator must re-run in CI for full coverage.

    3. **Any test FAILS** — STOP. Phase 119 has somehow introduced a CSP regression on a page it did not touch. Investigate: read the failure output, identify which page emitted a CSP violation, identify which Phase 119 file change could possibly have caused it (most likely culprit: an accidental edit to setupRoutes that broke route ordering or a docstring change that touched code). Report the failure as an Auth Gate / Deviation per `revision` mode rules; DO NOT mark this task done.

    No production code edits in this task — this is purely a verification gate.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test -tags=e2e -run '^TestBrowserCSP_' ./internal/webserver/ -count=1 -v; test $? -eq 0 || grep -q "SKIP" /tmp/119-csp-e2e.log</automated>
  </verify>
  <acceptance_criteria>
    - `go test -tags=e2e -run '^TestBrowserCSP_' ./internal/webserver/ -count=1` either exits 0 (PASS) or exits 0 with all three tests marked SKIP (chromedp unavailable)
    - If FAIL: the SUMMARY explicitly records the failure as an Auth Gate / Deviation per `execute-plan` rules; the gate-failure protocol is followed
    - The full short-mode webserver suite still passes: `go test ./internal/webserver/ -count=1 -short` exits 0
    - Cross-package suite green: `go test ./internal/files/ ./internal/capability/ ./internal/webserver/ ./internal/daemon/ -count=1 -short` exits 0
  </acceptance_criteria>
  <done>
    The existing browser CSP e2e suite either PASS or SKIP, confirming Phase 119 introduces no CSP regression on /dashboard, /join, /sessions/{id}. WEB-05 coverage is complete: unit-level (Plan 02 Task 1) + existing HTML-page e2e (this task).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → webserver (HTML page render) | Untrusted; CSP policy is the gate. Phase 119 changes nothing here, but introduces new (JSON) routes that must NOT accidentally widen the HTML attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-09 | Tampering | future regression: file route accidentally wrapped in cspHeaders | mitigate | TestFilesRoutes_NoCSPHeader asserts the absence; new code that wraps file routes in cspHeaders fails the test immediately |
| T-119-10 | Tampering | future regression: file route accidentally serves HTML error | mitigate | TestFilesRoutes_NoHTMLContentType asserts Content-Type prefix; any text/html response fires the test |
| T-119-11 | Information disclosure | docstring drift confusing future maintainers about the API surface | accept | Task 2 updates both stale references; risk reduced but not zero (a future planner could re-introduce the wrong name). Low impact. |
| T-119-12 | Tampering | Phase 119 inadvertently breaks CSP on an HTML page it did not touch | mitigate | Task 3 re-runs the existing TestBrowserCSP_* suite; failure halts the phase. No automated mitigation prevents the regression — the test is the gate. |
</threat_model>

<verification>
After all three tasks complete:

```
cd /Users/ken/dev/agenthub
go test -run '^TestFilesRoutes_' ./internal/webserver/ -count=1 -v && \
go test -tags=e2e -run '^TestBrowserCSP_' ./internal/webserver/ -count=1 && \
grep -rn 'SetFilesHandlerProvider' internal/ ; echo "stale-refs-exit=$?" && \
go test ./internal/files/ ./internal/capability/ ./internal/webserver/ ./internal/daemon/ -count=1 -short
```

Expected:
- All `TestFilesRoutes_*` subtests (15 from Plan 01 + 2 from this plan = 17 total) PASS
- `TestBrowserCSP_*` PASS or SKIP (never FAIL)
- `grep -rn 'SetFilesHandlerProvider' internal/` returns no matches (`stale-refs-exit=1`)
- Cross-package suite exits 0
</verification>

<success_criteria>
- [ ] TestFilesRoutes_NoCSPHeader PASS — /api/files/list response has no CSP header
- [ ] TestFilesRoutes_NoHTMLContentType PASS — /api/files/list Content-Type starts with application/json
- [ ] Existing TestBrowserCSP_* tests PASS or self-SKIP (no FAIL) — Phase 119 introduces no CSP regression
- [ ] Zero `SetFilesHandlerProvider` references remain in `internal/` after the docstring updates
- [ ] Cross-package suite (`internal/files`, `internal/capability`, `internal/webserver`, `internal/daemon`) green in short mode
- [ ] WEB-05 satisfied: zero new CSP amendments to the codebase; defense-in-depth tests in place to catch future regressions; HTML-page CSP e2e unchanged
</success_criteria>

<output>
Create `.planning/phases/119-webserver-routes-files-read-capability-plumbing/119-02-SUMMARY.md` when done.
</output>
