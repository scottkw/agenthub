---
phase: 89-vendored-terminal-assets-csp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/vendor/xterm/xterm.js
  - web/vendor/xterm/xterm.css
  - web/vendor/xterm/addon-fit.js
  - web/vendor/xterm/VERSION
  - internal/webserver/vendor_drift_test.go
autonomous: true
requirements: [SEC-07]
requirements_addressed: [SEC-07]
threat_model_ref: [T-89-02, T-89-05]
tags: [go, csp, security, vendor]

must_haves:
  truths:
    - "xterm.js, xterm.css, and addon-fit.js exist as vendored third-party files under web/vendor/xterm/"
    - "web/vendor/xterm/VERSION records the exact resolved versions from frontend/pnpm-lock.yaml"
    - "A Go test fails if frontend/pnpm-lock.yaml and VERSION ever diverge"
  artifacts:
    - path: web/vendor/xterm/xterm.js
      provides: "Vendored xterm core library (byte-for-byte copy of frontend/node_modules/@xterm/xterm/lib/xterm.js, ~476 KB)"
    - path: web/vendor/xterm/xterm.css
      provides: "Vendored xterm CSS (byte-for-byte copy of frontend/node_modules/@xterm/xterm/css/xterm.css)"
    - path: web/vendor/xterm/addon-fit.js
      provides: "Vendored xterm fit addon (byte-for-byte copy of frontend/node_modules/@xterm/addon-fit/lib/addon-fit.js)"
    - path: web/vendor/xterm/VERSION
      provides: "Two-line plaintext version manifest (@xterm/xterm@6.0.0, @xterm/addon-fit@0.11.0)"
    - path: internal/webserver/vendor_drift_test.go
      provides: "TestXtermVendorVersionsMatchPnpmLock — D-04/D-20 drift guard"
  key_links:
    - from: internal/webserver/vendor_drift_test.go
      to: frontend/pnpm-lock.yaml
      via: os.ReadFile + regexp line-scan
      pattern: "^\\s*'@xterm/(xterm|addon-fit)@"
    - from: internal/webserver/vendor_drift_test.go
      to: web/vendor/xterm/VERSION
      via: os.ReadFile + line-scan
      pattern: "@xterm/"
---

<objective>
Commit the xterm library files to the repository as vendored third-party assets under `web/vendor/xterm/` (per user decision D-01/D-02 locked in 89-CONTEXT.md and 89-DISCUSSION-LOG.md Q4) and add a Go source-parse test that fails if those files ever drift from the resolved versions in `frontend/pnpm-lock.yaml`.

Purpose: Close the runtime dependency on `cdn.jsdelivr.net` (SEC-07 Finding 4). The three xterm files + VERSION file are the only net-new bytes needed at this stage; actual CDN-URL replacement in HTML lives in Plan 02; embed wiring lives in Plan 04.

**URL vs disk path:** on-disk layout is `web/vendor/xterm/*` per D-01/D-02. The URL space served to browsers remains `/assets/xterm/*` — Plan 04 maps the URL to the vendor tree via a secondary `fs.Sub` mount. This plan touches only the on-disk layout.

Output:
- `web/vendor/xterm/xterm.js`, `xterm.css`, `addon-fit.js` (byte-for-byte copies)
- `web/vendor/xterm/VERSION` (two-line plaintext)
- `internal/webserver/vendor_drift_test.go` with `TestXtermVendorVersionsMatchPnpmLock`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-vendored-terminal-assets-csp/89-CONTEXT.md
@.planning/phases/89-vendored-terminal-assets-csp/89-RESEARCH.md
@.planning/phases/89-vendored-terminal-assets-csp/89-PATTERNS.md
@.planning/phases/89-vendored-terminal-assets-csp/89-VALIDATION.md
@frontend/pnpm-lock.yaml
@internal/webserver/security_regression_test.go

<interfaces>
<!-- Shape of the drift test — mirrors Phase 88 security_regression_test.go pattern -->
<!-- Working directory of `go test ./internal/webserver/...` is `internal/webserver/`, so relative paths use `../../` -->

Required reads (absolute paths from test file's cwd = internal/webserver/):
- "../../frontend/pnpm-lock.yaml" — source of truth for resolved versions
- "../../web/vendor/xterm/VERSION" — vendored manifest, must match

Resolved versions from pnpm-lock.yaml (verified 2026-04-22, lines 359-369 and 1043-1049):
  @xterm/xterm@6.0.0
  @xterm/addon-fit@0.11.0

pnpm-lock.yaml top-level package key format (verified, stable since pnpm v7):
  '@xterm/xterm@6.0.0':
    resolution: {integrity: sha512-...}

Regex for parsing: `^  '(@xterm/(?:xterm|addon-fit))@([0-9.]+)':$`
  Group 1 = package key ("@xterm/xterm" or "@xterm/addon-fit")
  Group 2 = resolved version ("6.0.0" or "0.11.0")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy xterm vendor files from node_modules and write VERSION manifest</name>
  <files>
    - web/vendor/xterm/xterm.js
    - web/vendor/xterm/xterm.css
    - web/vendor/xterm/addon-fit.js
    - web/vendor/xterm/VERSION
  </files>
  <read_first>
    - frontend/pnpm-lock.yaml (search for the two '@xterm/xterm@' and '@xterm/addon-fit@' top-level keys — confirm versions are 6.0.0 and 0.11.0 respectively; if they differ, use the actual resolved versions from the lockfile — the lockfile is the source of truth)
    - .planning/phases/89-vendored-terminal-assets-csp/89-CONTEXT.md §D-02, D-03, D-05 (vendoring strategy — vendor path LOCKED at web/vendor/xterm/)
    - .planning/phases/89-vendored-terminal-assets-csp/89-RESEARCH.md §Q3 (exact file paths and sizes)
  </read_first>
  <action>
Step 1 — Verify source files exist. Run:
  ls -l frontend/node_modules/@xterm/xterm/lib/xterm.js
  ls -l frontend/node_modules/@xterm/xterm/css/xterm.css
  ls -l frontend/node_modules/@xterm/addon-fit/lib/addon-fit.js

Expected sizes (from Research Q3, 2026-04-22 measurement):
  xterm.js         ≈ 487,763 bytes (476 KB)
  xterm.css        ≈   7,112 bytes (7 KB)
  addon-fit.js     ≈   1,521 bytes (2 KB)
  TOTAL            ≈ 496,396 bytes (485 KB)

If any are missing, run `cd frontend && pnpm install` first, then re-verify.

Step 2 — Create target directory (per D-01/D-02 — vendor path LOCKED):
  mkdir -p web/vendor/xterm

Step 3 — Copy files byte-for-byte (NOT symlinks, NOT edits — literal cp):
  cp frontend/node_modules/@xterm/xterm/lib/xterm.js         web/vendor/xterm/xterm.js
  cp frontend/node_modules/@xterm/xterm/css/xterm.css        web/vendor/xterm/xterm.css
  cp frontend/node_modules/@xterm/addon-fit/lib/addon-fit.js web/vendor/xterm/addon-fit.js

Do NOT commit any `.js.map` siblings. Do NOT edit file contents. Do NOT add a license header — the files already contain their own MIT license headers.

Step 4 — Write VERSION file. First, extract the exact resolved versions from the lockfile:
  grep -E "^  '@xterm/(xterm|addon-fit)@[0-9.]+':" frontend/pnpm-lock.yaml

Expected output (verified 2026-04-22):
  '@xterm/addon-fit@0.11.0':
  '@xterm/xterm@6.0.0':

Write VERSION as plaintext with this EXACT content (two lines + trailing newline). Use literal versions from the grep output above; if they differ from 6.0.0/0.11.0, use the actual grep values:

  @xterm/xterm@6.0.0
  @xterm/addon-fit@0.11.0

No leading comments, no blank lines, no BOM, no CRLF (LF line endings only).

Step 5 — Confirm .gitignore does NOT exclude these files. Research Q8 verified the repo's .gitignore has no pattern under `web/`, but double-check:
  git check-ignore web/vendor/xterm/xterm.js web/vendor/xterm/VERSION
That command must return NOTHING (files are trackable). If it lists the files, STOP — a .gitignore rule must be investigated before committing.
  </action>
  <verify>
    <automated>test -f web/vendor/xterm/xterm.js && test -f web/vendor/xterm/xterm.css && test -f web/vendor/xterm/addon-fit.js && test -f web/vendor/xterm/VERSION && grep -q "@xterm/xterm@" web/vendor/xterm/VERSION && grep -q "@xterm/addon-fit@" web/vendor/xterm/VERSION</automated>
    <automated>cmp frontend/node_modules/@xterm/xterm/lib/xterm.js web/vendor/xterm/xterm.js</automated>
    <automated>cmp frontend/node_modules/@xterm/xterm/css/xterm.css web/vendor/xterm/xterm.css</automated>
    <automated>cmp frontend/node_modules/@xterm/addon-fit/lib/addon-fit.js web/vendor/xterm/addon-fit.js</automated>
    <automated>! git check-ignore web/vendor/xterm/xterm.js web/vendor/xterm/VERSION</automated>
  </verify>
  <acceptance_criteria>
    - File `web/vendor/xterm/xterm.js` exists and is byte-identical to `frontend/node_modules/@xterm/xterm/lib/xterm.js` (cmp exits 0)
    - File `web/vendor/xterm/xterm.css` exists and is byte-identical to `frontend/node_modules/@xterm/xterm/css/xterm.css`
    - File `web/vendor/xterm/addon-fit.js` exists and is byte-identical to `frontend/node_modules/@xterm/addon-fit/lib/addon-fit.js`
    - File `web/vendor/xterm/VERSION` contains exactly two non-empty lines: `@xterm/xterm@<version>` and `@xterm/addon-fit@<version>` where `<version>` matches the value in `frontend/pnpm-lock.yaml`
    - `git check-ignore web/vendor/xterm/xterm.js` outputs nothing (file is trackable)
    - No `.js.map` files were copied to `web/vendor/xterm/`
  </acceptance_criteria>
  <done>Four files (xterm.js, xterm.css, addon-fit.js, VERSION) exist under web/vendor/xterm/, byte-identical to source (for the three JS/CSS files), and committed to git.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write vendor-drift source-parse test (D-04/D-20)</name>
  <files>
    - internal/webserver/vendor_drift_test.go
  </files>
  <read_first>
    - internal/webserver/security_regression_test.go (mirror its shape: os.ReadFile + string-scan + actionable t.Errorf message; note relative path style `server.go` means test is run from `internal/webserver/` cwd)
    - web/vendor/xterm/VERSION (the file Task 1 just wrote — confirm two-line format)
    - frontend/pnpm-lock.yaml (search for `'@xterm/xterm@` and `'@xterm/addon-fit@` at column 1+2-space indent to confirm regex pattern)
    - .planning/phases/89-vendored-terminal-assets-csp/89-RESEARCH.md §Q3 and the code example at RESEARCH.md lines 907-965 (full reference implementation)
  </read_first>
  <behavior>
    - Test TestXtermVendorVersionsMatchPnpmLock passes when web/vendor/xterm/VERSION exactly matches the two resolved versions in frontend/pnpm-lock.yaml
    - Test fails with an actionable error message (naming the package, both versions, and the remediation command) when @xterm/xterm or @xterm/addon-fit resolved version in pnpm-lock.yaml differs from the value in VERSION
    - Test fails with t.Fatalf when either source file cannot be read (I/O errors surface cleanly)
    - Test fails with t.Fatalf if pnpm-lock.yaml's top-level parser fails to find either @xterm/xterm or @xterm/addon-fit (catches structural breakage of the lockfile format)
  </behavior>
  <action>
Create `internal/webserver/vendor_drift_test.go` with package `webserver` (internal test — matches the package of all other _test.go files in this dir).

The test MUST:

1. Read `../../frontend/pnpm-lock.yaml` via `os.ReadFile`. On error: `t.Fatalf("ReadFile pnpm-lock.yaml: %v", err)`.

2. Scan lines with the regexp `^  '(@xterm/(?:xterm|addon-fit))@([0-9.]+)':$` (two-space indent, quoted package@version, trailing colon). Populate a map `pnpmVersions map[string]string` where keys are `@xterm/xterm` and `@xterm/addon-fit` and values are the resolved version strings.

3. If `len(pnpmVersions) < 2`: `t.Fatalf("failed to parse @xterm/xterm and @xterm/addon-fit from pnpm-lock.yaml: found %v (Phase 89 D-04 — the lockfile format may have changed; see 89-RESEARCH.md Q3)", pnpmVersions)`.

4. Read `../../web/vendor/xterm/VERSION` via `os.ReadFile`. On error: `t.Fatalf(...)`.

5. Parse each non-empty, non-`#`-prefixed line as `@<scope>/<pkg>@<version>`. Use `strings.SplitN(line, "@", 3)` where the result is `["", "xterm/xterm", "6.0.0"]` (leading empty element because line starts with `@`). Rebuild map key as `"@" + parts[1]`, value as `parts[2]`. Populate `vendorVersions map[string]string`.

6. For each `pkg, wantV := range pnpmVersions`:
   - If `_, ok := vendorVersions[pkg]; !ok`:
     `t.Errorf("web/vendor/xterm/VERSION missing entry for %s (Phase 89 D-04: re-copy files from frontend/node_modules/%s/ and update VERSION to include this line)", pkg, pkg)`
   - Else if `gotV := vendorVersions[pkg]; gotV != wantV`:
     `t.Errorf("version drift for %s: pnpm-lock=%s, VERSION=%s — update VERSION AND re-copy files from frontend/node_modules/%s/ per Phase 89 D-05 (pnpm update + cp). Do NOT edit VERSION alone without re-copying the files.", pkg, wantV, gotV, pkg)`

   IMPORTANT: the error format string literal MUST be exactly `version drift for %s: pnpm-lock=%s, VERSION=%s` (these eight tokens in this order) — the acceptance_criteria greps for this substring.

7. Use test function name EXACTLY: `TestXtermVendorVersionsMatchPnpmLock` (so CI commands can `-run TestXterm` prefix-match it).

Imports needed:
  "os"
  "regexp"
  "strings"
  "testing"

The full reference implementation at 89-RESEARCH.md lines 907-965 is the canonical template. Reproduce its structure.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go test ./internal/webserver/ -run TestXtermVendorVersionsMatchPnpmLock -count=1 -v</automated>
    <automated>cd /Users/ken/dev/agenthub && go vet ./internal/webserver/</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/webserver/vendor_drift_test.go` exists
    - Package declaration is `package webserver`
    - Test function named EXACTLY `TestXtermVendorVersionsMatchPnpmLock`
    - `go test ./internal/webserver/ -run TestXtermVendorVersionsMatchPnpmLock -count=1` exits 0 (PASS)
    - `go vet ./internal/webserver/` exits 0
    - `grep -q "Phase 89 D-04" internal/webserver/vendor_drift_test.go` (error messages cite decision ID for traceability)
    - `grep -q "../../frontend/pnpm-lock.yaml" internal/webserver/vendor_drift_test.go` (uses correct relative path)
    - `grep -q "../../web/vendor/xterm/VERSION" internal/webserver/vendor_drift_test.go` (uses correct relative path — vendor layout per D-02)
    - `grep -q 'version drift for %s: pnpm-lock=%s, VERSION=%s' internal/webserver/vendor_drift_test.go` (drift-error format string is present in source — proves the error path exists without mutating VERSION)
  </acceptance_criteria>
  <done>TestXtermVendorVersionsMatchPnpmLock exists, passes with the current files, and contains the documented drift-error format string in source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source-tree → committed-binary | Vendored xterm files cross from dev-machine node_modules (npm registry trust) into repo history. Integrity anchored by pnpm-lock integrity hashes + future supply-chain review. |
| pnpm-lock.yaml → VERSION manifest | Drift window. Developer must re-copy files AND update VERSION together. Caught by D-04/D-20 test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-02 | Tampering | CDN supply-chain (cdn.jsdelivr.net) | mitigate | This plan removes the runtime CDN dep by committing the exact bytes; downstream plans (02, 04) remove the `https://cdn.jsdelivr.net/...` references from HTML |
| T-89-05 | Tampering | Version drift between vendored xterm and frontend package.json | mitigate | TestXtermVendorVersionsMatchPnpmLock (this plan, Task 2) — cross-file regex compare of pnpm-lock.yaml and VERSION; CI fails if they diverge. Actionable error message directs maintainer to `pnpm update` + `cp` workflow per D-05 |
</threat_model>

<verification>
Run after both tasks commit:
  go test ./internal/webserver/ -run TestXterm -count=1
  git ls-files web/vendor/xterm/
  # Should list exactly: xterm.js, xterm.css, addon-fit.js, VERSION (4 files)
  wc -c web/vendor/xterm/*.js web/vendor/xterm/*.css
  # xterm.js must be ≥ 400 KB, xterm.css ≥ 6 KB, addon-fit.js ≥ 1 KB
</verification>

<success_criteria>
- `web/vendor/xterm/{xterm.js,xterm.css,addon-fit.js,VERSION}` exist, tracked in git, byte-identical to source (except VERSION which is new)
- `internal/webserver/vendor_drift_test.go` exists and `TestXtermVendorVersionsMatchPnpmLock` passes
- The full test suite (`go test ./...`) does not regress because of this plan (Plan 02's HTML edits are a separate wave, but Plan 01's changes are isolated)
- Vendor payload ≈ 485 KB committed to git, no `.js.map` files
</success_criteria>

<output>
After completion, create `.planning/phases/89-vendored-terminal-assets-csp/89-01-SUMMARY.md` with:
- Exact file sizes of the four vendored files
- Resolved versions captured in VERSION
- Confirmation that the drift test passes
</output>
</output>
