---
phase: quick
plan: 260407-w91
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/style.css
autonomous: true
requirements: [quick-task]
must_haves:
  truths:
    - "Hamburger, plus, and gear icons appear similar brightness to globe icon"
    - "All four toolbar buttons appear visually consistent in size"
    - "Hover state still works and is visually distinct"
  artifacts:
    - path: "frontend/src/style.css"
      provides: "Updated tab-bar__btn styles for brightness and size parity"
      contains: "tab-bar__btn--remote"
  key_links: []
---

<objective>
Make the three text-glyph toolbar icons (hamburger, plus, gear) match the globe emoji's visual size and brightness in the tab bar.

Purpose: The globe emoji renders as a colorful, bright icon while the other three buttons use text glyphs that inherit a dim `#565f89` color and appear smaller. This creates visual inconsistency.
Output: Updated CSS so all four toolbar buttons look balanced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/style.css
@frontend/src/components/TabBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update toolbar icon CSS for brightness and size parity</name>
  <files>frontend/src/style.css</files>
  <action>
In `frontend/src/style.css`, make the following changes to the `.tab-bar__btn` block and add modifier styles:

1. **Brighten base color:** Change `color: #565f89` to `color: #9aa5ce` (Tokyo Night foreground-dark — visible but not glaring, closer to the globe's visual weight).

2. **Bump font-size for text glyphs:** Change base `font-size: 18px` to `font-size: 20px` so the text characters (hamburger, plus, gear) render closer to the globe emoji's visual size.

3. **Scale down the globe emoji slightly** so it doesn't overpower the text icons. Add a modifier rule:
```css
.tab-bar__btn--remote {
  font-size: 17px; /* emoji renders larger than text at same font-size */
}
```

4. **Keep hover state contrast.** The existing hover color `#c0caf5` is still brighter than the new base `#9aa5ce`, so hover remains visually distinct. No change needed.

Do NOT touch the TabBar.tsx file — this is CSS-only.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && grep -A 15 'tab-bar__btn {' frontend/src/style.css | head -20 && grep -A 3 'tab-bar__btn--remote' frontend/src/style.css</automated>
  </verify>
  <done>
- Base color is `#9aa5ce` (brighter than old `#565f89`)
- Base font-size is `20px` (up from `18px`)
- Globe button has `font-size: 17px` via `.tab-bar__btn--remote` to visually balance with text glyphs
- Hover state unchanged and still provides contrast
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Updated toolbar icon brightness and sizing so hamburger, plus, and gear icons visually match the globe emoji</what-built>
  <how-to-verify>
    1. Launch the app with `cd /Users/ken/dev/agenthub && wails3 dev`
    2. Look at the four toolbar icons in the tab bar (globe, hamburger, plus, gear)
    3. Verify all four appear similar in brightness — none should look "dimmed out"
    4. Verify all four appear similar in size — the globe should not dwarf the others
    5. Hover over each button — confirm hover highlight still works
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to adjust</resume-signal>
</task>

</tasks>

<verification>
- All four toolbar buttons appear visually balanced in size and brightness
- No regressions to hover states or button click behavior
</verification>

<success_criteria>
The hamburger, plus, and gear toolbar icons are no longer visually dim compared to the globe emoji. All four icons appear consistent in the tab bar.
</success_criteria>

<output>
After completion, create `.planning/quick/260407-w91-make-toolbar-icons-match-globe-icon-size/260407-w91-SUMMARY.md`
</output>
