---
phase: quick
plan: 260406-s0e
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/daemon/path.go
  - internal/daemon/process.go
  - main.go
autonomous: true
requirements: [fix-cli-detection]
must_haves:
  truths:
    - "GUI app launched from Finder/Dock detects CLIs in /usr/local/bin, /opt/homebrew/bin, ~/.volta/bin, etc."
    - "Daemon still augments PATH at startup (no regression)"
  artifacts:
    - path: "internal/daemon/path.go"
      provides: "Exported AugmentServicePath function"
      contains: "func AugmentServicePath"
    - path: "main.go"
      provides: "PATH augmentation before wails.Run"
      contains: "daemon.AugmentServicePath"
  key_links:
    - from: "main.go"
      to: "internal/daemon/path.go"
      via: "daemon.AugmentServicePath() call in runGUI"
      pattern: "daemon\\.AugmentServicePath"
---

<objective>
Fix CLI detection in the GUI app. When launched from Finder/Dock, macOS gives a minimal PATH
(/usr/bin:/bin:/usr/sbin:/sbin), so exec.LookPath cannot find CLIs installed in common user
directories. The daemon already has augmentServicePath() that fixes this — export it and call
it in the GUI process too.

Purpose: Users see their installed CLIs instead of "No CLIs detected"
Output: Three-file fix — export function, update call site, add call in GUI startup
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@internal/daemon/path.go
@internal/daemon/process.go
@main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export AugmentServicePath and call it in GUI startup</name>
  <files>internal/daemon/path.go, internal/daemon/process.go, main.go</files>
  <action>
1. In `internal/daemon/path.go`: Rename `augmentServicePath` to `AugmentServicePath` (export it).
   Update the godoc comment to reflect it is now called from both daemon and GUI processes.

2. In `internal/daemon/process.go` line 25: Update the call from `augmentServicePath()` to
   `AugmentServicePath()`.

3. In `main.go` `runGUI()` function: Add `daemon.AugmentServicePath()` as the FIRST line of
   `runGUI()`, before `NewApp()`. The import for `github.com/scottkw/agenthub/internal/daemon`
   is already present.

This is a three-line change across three files. No new dependencies or imports needed.
  </action>
  <verify>
    <automated>cd /Users/ken/dev/agenthub && go build -tags wailsassets -o /dev/null . 2>&1 || go vet ./...</automated>
  </verify>
  <done>
    - `AugmentServicePath` is exported in path.go
    - Daemon still calls it (process.go updated)
    - GUI calls it before wails.Run (main.go updated)
    - Project compiles without errors
  </done>
</task>

</tasks>

<verification>
- `go vet ./...` passes
- `grep -n "AugmentServicePath" internal/daemon/path.go internal/daemon/process.go main.go` shows the function exported and called in both places
</verification>

<success_criteria>
- The exported function AugmentServicePath exists in internal/daemon/path.go
- Both daemon (process.go) and GUI (main.go) call daemon.AugmentServicePath()
- The project compiles successfully
</success_criteria>

<output>
After completion, create `.planning/quick/260406-s0e-fix-cli-detection-app-shows-no-clis-dete/260406-s0e-SUMMARY.md`
</output>
