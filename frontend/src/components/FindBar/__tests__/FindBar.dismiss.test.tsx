// Phase 94 Wave 0 RED scaffold — Plan 94-03 will implement.
// See: .planning/phases/94-search-addon-find-bar-desktop-web/94-VALIDATION.md
//      ## Per-Task Verification Map (row 03-findbar wave 2)
// UI-SPEC §"Closing the Find Bar"
import { describe, it, expect } from 'vitest'

describe('FindBar — dismiss (SRC-01) — Plan 94-03 implementation', () => {
  it('Esc dismisses + restores xterm focus', () => {
    expect.fail(
      'RED scaffold — Plan 94-03 implements FindBar.tsx Esc handler + xterm.focus() restore. ' +
        'See 94-VALIDATION.md row 03-findbar wave 2; UI-SPEC §"Closing the Find Bar".',
    )
  })
})
