# Go build artifacts
/agenthub
/agenthub.exe
/agenthub-cli
/agenthub-cli.exe
*.exe

# Node / frontend
frontend/node_modules/
frontend/dist/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Wails build artifacts
build/bin/
# Untrack darwin build outputs but track config plists needed for distribution
build/darwin/*
!build/darwin/Info.plist
!build/darwin/Info.dev.plist
!build/darwin/iconfile.icns
frontend/package.json.md5
# Wails-generated Go bindings (regenerated by wails dev/build)
# Only track the runtime stubs needed for CI builds
frontend/src/wailsjs/wailsjs/go/

# Test artifacts
*.test
*.out
coverage.html
.superpowers/

# Tailscale cert files (local testing artifacts, not committed)
*.ts.net.crt
*.ts.net.key

# Third-party security review artifacts (not committed)
# Dot-prefixed so `go build ./...` / `go vet ./...` skip it (it holds two
# conflicting package decls in one dir, which otherwise breaks scoped builds).
.security-review/
security-review/

# Phase 93 Plan 05 — Playwright e2e fixture runtime artifacts
# (built fixture binary, fixture-env.json, playwright-report/, test-results/)
.playwright/
frontend/playwright-report/
frontend/test-results/

# Phase 120-06 — playwright-fixture local dist embed (copied from frontend/dist
# at global-setup time so //go:embed can reach it without escaping the package).
cmd/playwright-fixture/dist/
